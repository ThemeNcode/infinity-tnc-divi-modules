import { ModuleClassnamesParams } from '@divi/module';
import { TypeWriterAttrs } from './types';

export const moduleClassnames = ({
  classnamesInstance,
  attrs,
}: ModuleClassnamesParams<TypeWriterAttrs>): void => {
  // No additional classnames needed.
};
