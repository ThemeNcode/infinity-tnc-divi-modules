import { ModuleClassnamesParams } from '@divi/module';
import { BreadCrumbsAttrs } from './types';


export const moduleClassnames = ({
  classnamesInstance,
  attrs,
}: ModuleClassnamesParams<BreadCrumbsAttrs>): void => {
  // No additional classnames needed for breadcrumbs module.
};
