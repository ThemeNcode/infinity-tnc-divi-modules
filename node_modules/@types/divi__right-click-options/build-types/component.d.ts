import { type ReactElement } from 'react';
import { type GeneratedStyle, type RightClickOptionsProps } from './types';
import './style.scss';
/**
 * Get horizontal style's property and value for right click options.
 *
 * @since ??
 */
declare const getHorizontalStyle: ({ cursorX, windowWidth, builderBarWidth, sidebarLeftWidth, windowLocation, }: {
    cursorX: number;
    windowWidth: number;
    builderBarWidth: number;
    sidebarLeftWidth: number;
    windowLocation: RightClickOptionsProps['windowLocation'];
}) => GeneratedStyle;
/**
 * Get vertical style's property and value for right click options.
 *
 * @since ??
 */
declare const getVerticalStyle: ({ cursorY, windowHeight, appWindowOffsetTop, adminBarHeight, pageBarHeight, windowLocation, }: {
    cursorY: number;
    windowHeight: number;
    appWindowOffsetTop: number;
    adminBarHeight: number;
    pageBarHeight: number;
    windowLocation: RightClickOptionsProps['windowLocation'];
}) => GeneratedStyle;
/**
 * Right Click Options.
 *
 * Right click options component.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {object} props.cursorPosition Cursor position.
 * @param {number} props.windowHeight (Top) window's height.
 * @param {number} props.windowWidth (Top) window's width.
 * @param {object} props.options Options parameter.
 * @param {number} props.selected Selected option.
 * @param {Array<number>} props.selectedParent Selected parent.
 * @param {number} props.appWindowOffset App window offset.
 * @param {Function} props.onMount Callback function to handle `mount` event. Defaults to `noop`.
 * @param {Function} props.onUnmount Callback function to handle `unmounted` event. Defaults to `noop`.
 * @param {Function} props.onOptionsContextMenu Callback function to handle `options context menu` click event.
 * Defaults to `noop`.
 * @param {Function} props.onOptionMouseEnter Callback function to handle option mouse enter event. Defaults to `noop`.
 * @param {Function} props.onOverlayClick Callback function to handle `overlay` click event. Defaults to `noop`.
 * @param {Function} props.onOverlayContextMenu Callback function to handle `overlay context menu` click event.
 * Defaults to `noop`.
 * @param {number} props.adminBarHeight WP Admin Bar Height.
 * @param {number} props.builderBarWidth Builder Bar Width.
 * @param {number} props.pageBarHeight Page Bar Height.
 * @param {number} props.sidebarLeftWidth Left Sidebar Width.
 * @param {string} props.windowLocation Location of the window. Defaults to `top`.
 *
 * @returns {ReactElement}
 */
declare const RightClickOptions: ({ cursorPosition, windowHeight, windowWidth, options, selected, selectedParent, appWindowOffset, onMount, onUnmount, onOptionsContextMenu, onOptionMouseEnter, onOverlayClick, onOverlayContextMenu, adminBarHeight, builderBarWidth, pageBarHeight, sidebarLeftWidth, windowLocation, }: RightClickOptionsProps) => ReactElement;
export { getHorizontalStyle, getVerticalStyle, RightClickOptions, };
