import { type RightClickOptions } from '@divi/types';
/**
 * Get Modal Tab Options.
 *
 * Get options of modal tab's right click options.
 *
 * @since ??
 *
 * @param {object} owner Function parameters.
 * @param {string} owner.modalName Modal name.
 * @param {string} owner.modalTab Modal tab name.
 * @param {ReactNode} owner.fieldsNode Modal tab fields node.
 * @param {string} owner.id Modal tab id.
 * @param {string} owner.optionType Option type. Should be `modalTab`.
 *
 * @returns {object} Modal owner.
 */
export declare const getModalTabOptions: (owner: RightClickOptions.Owner.ModalTab) => RightClickOptions.Option.Items;
