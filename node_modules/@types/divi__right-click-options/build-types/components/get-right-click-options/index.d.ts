import { type RightClickOptions } from '@divi/types';
/**
 * Get Right Click Options.
 *
 * Get options of right click options.
 *
 * @since ??
 *
 * @param {string} optionType Option type. One of:
 * `module`, `modalGroup`, `modalTab`, `modalHeader`, `modalField`, `modalGroupTab`,
 * `gradientStop` or `modalDraggableChildModuleList`.
 * @param {object} optionOwner Option's owner data.
 *
 * @returns {Array<object>}
 */
export declare const getRightClickOptions: (optionType: RightClickOptions.Owner.Item['optionType'], optionOwner: RightClickOptions.Owner.Item) => RightClickOptions.Option.Items;
