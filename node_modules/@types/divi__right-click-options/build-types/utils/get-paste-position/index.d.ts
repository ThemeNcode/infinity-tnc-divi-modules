import { type Params, type PastePosition } from './types';
/**
 * Get Paste Position.
 *
 * Get paste position based on clipboard single item relationship to origin element where this right click takes place.
 *
 * @since ??
 *
 * @param {object} params Function parameters.
 * @param {string} params.ownerParentModuleType Module type of the owner parent module.
 * @param {string} params.ownerModuleType Module type of the owner module.
 * @param {string} params.clipboardModuleType Module type of the clipboard module.
 *
 * @returns {boolean|string} One of `false`, `after` or `inside`.
 */
export declare const getPastePosition: ({ ownerParentModuleType, ownerModuleType, clipboardModuleType, }: Params) => PastePosition;
