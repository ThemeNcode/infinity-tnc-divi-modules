interface ModuleLabel {
    structure: string;
    structures: string;
    name: string;
}
/**
 * Get Module Label By Id.
 *
 * Get module label (title) based on given module id.
 *
 * @since ??
 *
 * @param {string} moduleId Module id.
 *
 * @returns {object} Object with the structure: `{structure: string, structures: string, name: string}`.
 */
export declare const getModuleLabelById: (moduleId: string) => ModuleLabel;
export {};
