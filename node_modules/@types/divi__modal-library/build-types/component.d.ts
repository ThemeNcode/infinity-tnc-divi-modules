import { type ReactElement } from 'react';
/**
 * Modal Library component which renders all registered modals.
 *
 * @since ??
 *
 * @returns {ReactElement}
 */
export declare const ModalLibraryComponent: () => ReactElement;
