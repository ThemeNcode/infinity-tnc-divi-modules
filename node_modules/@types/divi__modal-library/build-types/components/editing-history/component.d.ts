import { type ReactElement } from 'react';
import { type EditingHistoryModalProps } from './types';
/**
 * Editing History's modal component.
 *
 * @since ??
 *
 * @param {EditingHistoryModalProps} props Component props.
 *
 * @returns {ReactElement}
 */
export declare const EditingHistoryModal: ({ closeModal, tab, }: EditingHistoryModalProps) => ReactElement;
