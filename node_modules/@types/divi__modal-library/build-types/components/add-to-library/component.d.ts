import { type ReactElement } from 'react';
import { type Props } from './types';
import './style.scss';
/**
 * Modal component for adding current layout to library.
 *
 * @since ??
 *
 * @returns {ReactElement}
 */
export declare const AddToLibraryModal: ({ moduleName, moduleType, saveToDiviCloud, saveAsGlobal, hasSaveAsGlobal, addCategoryName, addTagName, category, tag, onSaveClick, onHelpClick, hasSubmitError, topWindowHeight, modalDimension, modalOffset, }: Props) => ReactElement;
