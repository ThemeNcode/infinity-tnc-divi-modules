import { type Fonts } from '@divi/types';
export interface FontDataFile {
    fileName: string;
    file: File;
}
export interface FontData {
    fontName: string;
    fontWeight: Record<string, string>;
    files: Record<string, FontDataFile>;
}
export interface UploadFontModalProps {
    supportedFileFormats: string[];
    customFonts: Fonts.Custom.Fonts;
    uploadFont: (newCustomFonts: Fonts.Custom.Fonts) => void;
    closeModal: () => void;
}
export interface AjaxResponseSuccessAdd {
    uploaded_font: string;
    updated_fonts: Fonts.Custom.Fonts;
}
export interface AjaxResponseFailAdd {
    code: string;
    message: string;
}
