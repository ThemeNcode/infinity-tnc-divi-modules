import { type MouseEvent } from 'react';
import { type ModalStates } from '@types/divi__modal';
export type ContainerProps = ModalStates;
export interface Props {
    name: ContainerProps['name'];
    dimension: ContainerProps['dimension'];
    offset: ContainerProps['offset'];
    onOptionClick: (event: MouseEvent) => void;
    selected?: string;
    isColumnInner?: boolean;
    sectionColumnType?: string;
    bodySiblingHeight: number;
}
