import { type ReactElement } from 'react';
import { type LoadLayoutModalProps } from './types';
import './style.scss';
/**
 * Modal component for load layout.
 *
 * @since ??
 *
 * @todo (D5i) this component is example of alert modal. Once actual alert modal.
 *
 * @returns {ReactElement}
 */
export declare const LoadLayoutModal: ({ bodySiblingHeight, onCloudUseItem, openTab, tab, }: LoadLayoutModalProps) => ReactElement;
