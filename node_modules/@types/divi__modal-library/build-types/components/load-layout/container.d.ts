import { type ReactElement } from 'react';
import { type ModalLibrary } from '@divi/types';
/**
 * <LoadLayoutModal /> + Container HOC which passed necessary data to render help modal component.
 *
 * @since ??
 *
 * @returns {FC<ModalLibrary.Component.Props>}
 */
export declare const LoadLayoutModalContainer: (props: ModalLibrary.Component.Props) => ReactElement;
