import { type ReactElement } from 'react';
import { type Props } from './types';
import './style.scss';
/**
 * Clear layout modal dimension; it has fixed dimension.
 */
export declare const dimension: {
    width: number;
    height: number;
};
/**
 * Modal component for removing existing layout (clear layout).
 *
 * @since ??
 *
 * @returns {ReactElement}
 */
export declare const ClearLayoutModal: ({ onClearClick, }: Props) => ReactElement;
