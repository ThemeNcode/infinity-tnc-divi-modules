import { type ModuleAttr } from '@divi/types';
/**
 * Get layer module title based on the admin label and default title.
 *
 * This function retrieves the title of a layer module. It first tries to get the value from the 'adminLabel' attribute
 * under 'desktop' using Lodash's `get` method. If that value is not available, it falls back to the provided 'title'.
 * If neither are available, it defaults to 'Unknown Module'. The result is then typecasted to a string to ensure the
 * correct return type.
 *
 * We use Lodash `get` and `string` typecasting to make sure it returns correct value
 * when the `adminLabel` is not found.
 *
 * @since ??
 *
 * @param {ModuleAttr} attrs The attributes of the module.
 * @param {string} title The fallback title of the module.
 *
 * @returns {string} The title of the layer module.
 */
export declare const getLayerModuleTitle: (attrs: ModuleAttr, title: string) => string;
