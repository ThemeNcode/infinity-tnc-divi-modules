import { type GeneratedLayerModules, type GenerateLayerModuleIds } from './types';
/**
 * Generate layer modules based on provided module IDs.
 *
 * @since ??
 *
 * @param {GenerateLayerModuleIds} moduleIds An array of module IDs.
 *
 * @returns {GeneratedLayerModules} A record representing generated layer modules.
 */
export declare const generateLayerModules: (moduleIds: GenerateLayerModuleIds) => GeneratedLayerModules;
