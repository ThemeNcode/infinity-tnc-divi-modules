import { type ReactElement } from 'react';
import { type LayerIconProps } from './types';
/**
 * Render given module icon.
 *
 * @since ??
 *
 * @param {string} moduleIcon Module icon.
 *
 * @returns {ReactElement}
 */
export declare const LayerIcon: ({ moduleIconName }: LayerIconProps) => ReactElement;
