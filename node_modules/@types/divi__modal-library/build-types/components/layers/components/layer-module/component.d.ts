import { type ReactElement } from 'react';
import type { LayerModuleProps } from './types';
import './styles.scss';
/**
 * Layer module component.
 *
 * Display module inside layers modal tree.
 *
 * @since ??
 *
 * @param {LayerModuleProps} props The properties for the layer module.
 * @param {string} props.id The module ID.
 * @param {number} props.level The module level.
 * @param {string} props.moduleName The module name.
 * @param {string} props.moduleIcon The module icon.
 * @param {string} props.moduleTitle The module title.
 * @param {string} props.moduleType The module type.
 * @param {ImmutableArray<string>} props.childrenModuleIds The child module IDs.
 * @param {React.ReactNode} props.layerChildModules The child modules.
 * @param {boolean} props.isHovered Flag indicating whether the module is hovered.
 * @param {boolean} props.isDragged Flag indicating whether the module is being dragged.
 * @param {boolean} props.isDragging Flag indicating whether the module is being dragged.
 * @param {boolean} props.hasDropZone Flag indicating whether the module has a drop zone.
 * @param {LayerHoveredArea} props.hoveredArea The hovered area.
 * @param {Function} props.onMouseDown The function to be called when the module is clicked.
 * @param {Function} props.onMouseEnter The function to be called when the mouse enters the module.
 * @param {Function} props.onMouseMove The function to be called when the mouse moves over the module.
 * @param {string | null} props.activeLayerModuleId The active module ID.
 * @param {Function} props.setActiveLayerModuleId The function to be called when the active module ID is set.
 * @param {boolean} props.expandAll Flag indicating whether all modules are expanded.
 * @param {LayerModuleData} props.layerModule The layer module data.
 *
 * @returns {ReactElement} The rendered layer module.
 */
export declare const LayerModule: ({ id, level, moduleName, moduleIcon, moduleTitle, moduleType, childrenModuleIds, layerChildModules, isHovered, isDragged, isDragging, hasDropZone, hoveredArea, onMouseDown, onMouseEnter, onMouseMove, activeLayerModuleId, setActiveLayerModuleId, expandAll, layerModule, isChildModuleHovered, }: LayerModuleProps) => ReactElement;
