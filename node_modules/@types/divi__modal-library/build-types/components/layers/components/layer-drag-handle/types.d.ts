import type SeamlessImmutable from 'seamless-immutable';
import { type ModuleAttrs, type ModuleFlatObject } from '@divi/types';
export type LayerDragHandleProps = {
    moduleIcon: string;
    moduleName: string;
};
export interface DragHandleProps {
    draggedModule: SeamlessImmutable.ImmutableObject<ModuleFlatObject<ModuleAttrs>>;
}
