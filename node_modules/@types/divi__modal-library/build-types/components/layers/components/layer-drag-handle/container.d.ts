/// <reference types="react" />
import { type DragHandleProps } from './types';
/**
 * Container for the Layers view drag handle component.
 * It selects data from the WordPress data store based on the dragged module's name
 * and passes the resolved module icon and name to the LayerDragHandle component
 * for rendering the drag handle in the layers view.
 *
 * @since ??
 *
 * @param {DragHandleProps} DragHandleProps The props expected by the component.
 * @param {object} DragHandleProps.draggedModule Data pertaining to the module currently being dragged.
 * @param {string} DragHandleProps.draggedModule.name The name of the dragged module.
 *
 * @returns {React.ReactElement} A LayerDragHandle component with the icon and name of the dragged module.
 */
export declare const LayerDragHandleContainer: ({ draggedModule }: DragHandleProps) => JSX.Element;
