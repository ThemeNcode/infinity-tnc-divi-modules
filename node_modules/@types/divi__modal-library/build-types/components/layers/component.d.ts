import { type ReactElement } from 'react';
import { type Props } from './types';
import './styles.scss';
/**
 * Layers View modal component.
 *
 * @since ??
 *
 * @todo (D5i) this component only render the modal for the time being. The layers view
 * functionality still need to be added.
 *
 * @param {Props} props Component props.
 *
 * @returns {ReactElement}
 */
export declare const LayersModal: (props: Props) => ReactElement;
