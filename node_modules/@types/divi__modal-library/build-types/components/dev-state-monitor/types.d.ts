import { type ImmutableObject } from 'seamless-immutable';
import { type ModalStates } from '@types/divi__modal';
import { type AppUi, type AttrState, type Breakpoint, type EditPost, type Events, type KeyboardShortcuts, type ModuleFlatObject } from '@divi/types';
export type ContaninerProps = ModalStates;
export interface DevStateMonitorProps {
    name: ModalStates['name'];
    modules: EditPost.Store.ImmutableState['content'];
    hoveredModule: Events.Hovered.Module;
    selectedModules: string[];
    draggedModules: string[];
    rightClickedModuleId: string;
    lastModuleClipboard?: Events.Selected.Module;
    pressedKeys: KeyboardShortcuts.Store.ImmutableState['pressedKeys'];
    currentShortcut: ImmutableObject<KeyboardShortcuts.Shortcut.Item>;
    activeModalSetting?: string;
    expandedModuleIds?: string[];
    setExpandedModuleIds: (moduleIds: string[]) => void;
    attributeState: AttrState;
    breakpoint: Breakpoint;
    view: AppUi.View.Type;
}
export interface ModuleProps {
    module: ModuleFlatObject;
}
