import { type ReactElement } from 'react';
import { type ContainerProps } from './types';
/**
 * Container component of Add to Library modal.
 *
 * @since ??
 *
 * @returns {ReactElement}
 */
export declare const PortabilityModalContainer: (props: ContainerProps) => ReactElement;
