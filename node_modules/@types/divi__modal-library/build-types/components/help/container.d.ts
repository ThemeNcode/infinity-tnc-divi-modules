import { type ReactElement } from 'react';
import { type ContainerProps } from './types';
/**
 * <HelpModal /> + Container HOC which passed necessary data to render help modal component.
 *
 * @since ??
 *
 * @param {ContainerProps} props Component props.
 *
 * @returns {ReactElement}
 */
export declare const HelpModalContainer: (props: ContainerProps) => ReactElement;
