import { type ModalHeightParams } from './types';
/**
 * Get modal heights after calculating it.
 *
 * @since ??
 *
 * @param {ModalHeightParams} modalHeightParams Modal height parameters.
 *
 * @returns {number} Calculated modal height.
 */
export declare const getModalHeight: ({ width, videoRatio, videos, currentVideoList, overlayMode, }: ModalHeightParams) => number;
