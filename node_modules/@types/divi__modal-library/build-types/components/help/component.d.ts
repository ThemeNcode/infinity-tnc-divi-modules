import { type ReactElement } from 'react';
import { type VideoComponentProps } from './types';
import './style.scss';
/**
 * Help modal component.
 *
 * @since ??
 *
 * @todo (D5i) Worked on the video panel.
 *
 * @param {VideoComponentProps} props Component props.
 *
 * @returns {ReactElement}
 */
export declare const HelpModal: (props: VideoComponentProps) => ReactElement;
