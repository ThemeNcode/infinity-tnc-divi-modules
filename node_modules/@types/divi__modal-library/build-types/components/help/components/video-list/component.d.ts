import { type ReactElement } from 'react';
import { type VideoListProps } from './types';
import './styles.scss';
/**
 * VideoList component.
 *
 * Since ??
 *
 * @param {VideoListProps} props Component props.
 *
 * @returns {ReactElement} VideoList component.
 */
export declare const VideoList: (props: VideoListProps) => ReactElement;
