import { type ElementDragReleaseParams } from '@types/divi__draggable';
import { type ModalOffset } from '@types/divi__modal';
import { type Modal } from '@divi/types';
export interface OnElementClickParams {
    snappable: boolean;
    modalDimension: Modal.Dimension;
    modalOffset: ModalOffset;
}
export interface OnElementReleaseParams {
    modalName: string;
    snappable: boolean;
    modalDimension: Modal.Dimension;
}
export interface OnElementDragParams {
    modalOffset?: ModalOffset;
    modalWidth?: number;
    direction?: string;
}
export type DraggableCallback = (callbackParams: OnElementDragParams) => (params: ElementDragReleaseParams) => void;
export interface StartDragValue {
    dimension: Modal.Dimension;
    offset: ModalOffset;
}
