import { type ReactElement } from 'react';
import { type ModuleModalProps } from './types';
/**
 * Module modal component, tailored specifically for module's modal.
 *
 * @since ??
 *
 * @param {ModuleModalProps} props Component props.
 *
 * @returns {ReactElement}
 */
export declare const ModuleModal: ({ attrs, owner, tab, group, moduleName, moduleLabel, moduleInfo, isChildModule, parentAttrs, onContextMenu, onClickBackButton, onHelpClick, }: ModuleModalProps) => ReactElement;
