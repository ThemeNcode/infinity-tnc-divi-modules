import { type ReactElement } from 'react';
import { type BodyPanelProps } from './types';
/**
 * Module settings modal's individual panel.
 *
 * @since ??
 *
 * @returns {ReactElement}
 */
export declare const BodyPanel: ({ attrs, parentAttrs, modulePanel, id, isMultiPanels, label, onHelpClick, moduleCredit, official, moduleLabel, }: BodyPanelProps) => ReactElement;
