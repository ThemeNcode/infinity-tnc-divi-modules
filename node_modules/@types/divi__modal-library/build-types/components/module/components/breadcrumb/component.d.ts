import React, { type ReactElement } from 'react';
import './styles.scss';
/**
 * Component for module settings' modal breadcrumb navigation.
 *
 * TODO feat(Improvement) Maybe abstracting this into <Header /> component so any modal can use breadcrumb navigation.
 * Right now, this is only added for module settings' modal due to time constraint.
 *
 * @since ??
 */
export declare const Breadcrumb: React.MemoExoticComponent<({ moduleId, }: {
    moduleId: string;
}) => ReactElement>;
