import { type ReactElement } from 'react';
import { type ResponsiveStatePickerProps } from './types';
import './style.scss';
/**
 * Component for picking responsive mode or state.
 *
 * @since ??
 *
 * @param {ResponsiveStatePickerProps} param0 Component props.
 *
 * @returns {ReactElement}
 */
export declare const ResponsiveStatePicker: ({ expand, value, onToggleExpandClick, onOverlayClick, onChange, style, options, selected, }: ResponsiveStatePickerProps) => ReactElement;
