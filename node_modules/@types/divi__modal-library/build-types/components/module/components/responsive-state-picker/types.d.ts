import { type CSSProperties, type ReactNode } from 'react';
export interface ResponsiveStatePickerProps {
    expand: boolean;
    value: string;
    selected: number;
    onToggleExpandClick: () => void;
    onOverlayClick: () => void;
    onChange: (name: string) => void;
    style?: CSSProperties;
    options?: Option[];
}
export type OptionType = 'desktop' | 'tablet' | 'phone' | 'desktop-hover' | 'desktop-sticky';
export interface Option {
    name: OptionType;
    label: string;
    icon: ReactNode;
    onClick?: (name: string) => void;
}
export interface ModalAttributes {
    responsiveStatePickerSelected: number;
    responsiveStatePickerOptions: Option[];
}
