import { type Option } from './types';
/**
 * Responsive and state options.
 * NOTE: The *-value implies default state; There are currently three
 * supported states: default (*-value), hover, and sticky.
 *
 * @since ??
 */
export declare const options: Option[];
