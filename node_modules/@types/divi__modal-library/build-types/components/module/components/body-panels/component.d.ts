import { type ReactElement } from 'react';
import { type BodyPanelsProps } from './types';
import './styles.scss';
/**
 * Object of body panel names.
 *
 * @since ??
 *
 * @returns {Record<string, string>}
 */
export declare const bodyPanelNames: Record<string, string>;
/**
 * Default divi/module modal's panels.
 *
 * @since ??
 *
 * @returns {ReactElement}
 */
export declare const BodyPanels: ({ attrs, parentAttrs, moduleSettings, onHelpClick, moduleLabel, }: BodyPanelsProps) => ReactElement;
