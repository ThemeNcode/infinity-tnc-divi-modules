import { type MouseEvent } from 'react';
import { type ImmutableObject } from 'seamless-immutable';
import { type ModalStates } from '@types/divi__modal';
import { type ModuleAttrs, type ModuleLibrary } from '@divi/types';
export interface ModuleModalContainerProps {
    owner: string;
}
export interface ModuleModalProps extends ModuleModalContainerProps {
    group: ModalStates['group'];
    isChildModule: boolean;
    moduleName: string;
    moduleLabel: string;
    moduleInfo: ModuleLibrary.Module.Settings;
    onContextMenu: (event: MouseEvent) => void;
    onClickBackButton: (e: MouseEvent) => void;
    tab: string;
    attrs: ImmutableObject<ModuleAttrs>;
    parentAttrs: ImmutableObject<ModuleAttrs>;
    onHelpClick: () => void;
}
