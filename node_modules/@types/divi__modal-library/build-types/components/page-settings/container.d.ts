import { type ReactElement } from 'react';
import { type ModalLibrary } from '@divi/types';
/**
 * <PageSettingsModal /> + Container HOC which passed necessary data to render Page Settings modal component.
 *
 * @since ??
 *
 * @returns {FC<ModalLibrary.Component.Props>}
 */
export declare const PageSettingsModalContainer: (props: ModalLibrary.Component.Props) => ReactElement;
