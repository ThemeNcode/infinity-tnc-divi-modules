import { type ReactElement } from 'react';
import { type PageSettingsFieldProps } from './types';
/**
 * Page Settings Field Component.
 *
 * This component is used to render a field in the page settings modal.
 *
 * @since ??
 *
 * @param {PageSettingsFieldProps} props PageSettingsField props.
 *
 * @returns {React.ReactElement} PageSettingsField component.
 */
export declare const PageSettingsField: (props: PageSettingsFieldProps) => ReactElement;
