import { type PageSettings } from '@divi/types';
export type PageSettingsModalProps = {
    tab: string;
    group: Record<string, string>;
    settings: PageSettings.Store.State;
    onHelpClick?: () => void;
};
