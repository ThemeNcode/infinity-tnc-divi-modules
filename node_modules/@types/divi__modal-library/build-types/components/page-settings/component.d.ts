import { type ReactElement } from 'react';
import { type PageSettingsModalProps } from './types';
/**
 * Page Settings' modal component.
 *
 * @since ??
 *
 * @param {PageSettingsModalProps} props Component props.
 *
 * @returns {ReactElement}
 */
export declare const PageSettingsModal: ({ tab, group, onHelpClick, }: PageSettingsModalProps) => ReactElement;
