import { type ModalOffset } from '@types/divi__modal';
import { type Modal } from '@divi/types';
export interface ContainerProps {
    dimension: Modal.Dimension;
    offset: ModalOffset;
}
export interface Props {
    dimension: Modal.Dimension;
    offset: ModalOffset;
    onCloseButton: () => void;
}
