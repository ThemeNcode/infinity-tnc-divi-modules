import { type ModalLibrary } from '@divi/types';
import { QuickActionsModal } from './component';
/**
 * TODO(D5, Modal):
 * In current milestone we don't have "Quick Action" modal in D5, so `multiInstanceModal` is set as its type
 * to avoid error. New modal type needs to be introduced when "Quick Action" is going to be implemented in D5.
 */
export declare const quickActionModalDefinition: ModalLibrary.Store.ModalDefinition;
export { QuickActionsModal, };
