import { type ReactElement } from 'react';
import { type Props } from './types';
import './style.scss';
export declare const name = "divi/quick-actions";
export declare const type = "floating";
/**
 * Quick Actions modal component.
 *
 * @since ??
 *
 * @todo (D5i) this component only render the modal for the time being. The quick actions
 * functionality still need to be added.
 *
 * @returns {ReactElement}
 */
export declare const QuickActionsModal: ({ onCloseButton, }: Props) => ReactElement;
