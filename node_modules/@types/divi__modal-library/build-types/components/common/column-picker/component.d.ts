import { type ReactElement } from 'react';
import { type Props } from './types';
import './style.scss';
/**
 * Column picker component. Abstracted from choose-column modal.
 *
 * @since ??
 *
 * @param {Props} props Component props.
 *
 * @returns {ReactElement}
 */
export declare const ColumnPicker: (props: Props) => ReactElement;
