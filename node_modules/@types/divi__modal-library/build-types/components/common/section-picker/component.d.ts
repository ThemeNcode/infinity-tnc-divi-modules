import { type ReactElement } from 'react';
import { type Props } from './types';
import './style.scss';
/**
 * Section picker component.
 *
 * @since ??
 *
 * @param {Props} props Component props.
 *
 * @returns  {ReactElement}
 */
export declare const SectionPicker: (props: Props) => ReactElement;
