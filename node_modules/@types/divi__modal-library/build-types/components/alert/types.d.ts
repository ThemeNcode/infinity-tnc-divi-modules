import { type ReactNode } from 'react';
import { type FooterButtons, type ModalStates } from '@types/divi__modal';
export type ContainerProps = ModalStates<ModalAttributes>;
export interface ModalAttributes {
    title?: string;
    content: ReactNode;
    footerButtons: FooterButtons;
}
export interface Props {
    name: ModalStates['name'];
    title?: string;
    children: ReactNode;
    footerButtons: FooterButtons;
}
