import { type MouseEvent, type ReactElement } from 'react';
import { type ContainerProps } from './types';
export declare const name = "divi/alert";
export declare const footerButtonDefault: {
    name: string;
    label: string;
    onClick: (e: MouseEvent) => void;
};
/**
 * Alert modal component.
 *
 * @since ??
 *
 * @todo (D5i) this component is example of alert modal. Once actual alert modal.
 *
 * @returns {ReactElement}
 */
export declare const AlertModalContainer: ({ attributes, }: ContainerProps) => ReactElement;
