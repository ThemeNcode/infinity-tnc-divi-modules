import { type ReactElement } from 'react';
import { type Props } from './types';
import './styles.scss';
/**
 * Alert modal component.
 *
 * @since ??
 *
 * @todo (D5i) this component is example of alert modal. Once actual alert modal.
 *
 * @returns {ReactElement}
 */
export declare const AlertModal: ({ name, children, footerButtons, title, }: Props) => ReactElement;
