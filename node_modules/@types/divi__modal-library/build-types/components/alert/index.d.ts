import { type ModalLibrary } from '@divi/types';
import { AlertModal } from './component';
/**
 * TODO(D5, Modal):
 * In current milestone we don't have "Alert" modal in D5, so `multiInstanceModal` is set as its type to avoid error.
 * New modal type needs to be introduced when "Alert" modal is going to be implemented in D5.
 */
export declare const alertModalDefinition: ModalLibrary.Store.ModalDefinition;
export { AlertModal, };
