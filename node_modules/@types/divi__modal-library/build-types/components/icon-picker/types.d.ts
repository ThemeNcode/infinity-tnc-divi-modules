import { type FieldLibrary, type Modal } from '@divi/types';
export type IconPickerModalProps = Partial<FieldLibrary.IconPicker.Props> & {
    closeModal: () => void;
    dimension: Modal.Dimension;
    selectedIcon: FieldLibrary.IconPicker.Props['value'];
};
export type IconPickerModalContainerProps = {
    attributes: IconPickerModalProps;
    centered: boolean;
    dimension: Modal.Dimension;
    name: string;
};
