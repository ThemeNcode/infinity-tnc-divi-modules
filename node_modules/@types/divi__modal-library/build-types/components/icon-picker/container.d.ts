import { type ReactElement } from 'react';
import { type IconPickerModalContainerProps } from './types';
export declare const IconPickerModalContainer: (props: IconPickerModalContainerProps) => ReactElement;
