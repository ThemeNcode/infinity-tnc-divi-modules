import { type ReactElement } from 'react';
import { type IconPickerModalProps } from './types';
import './styles.scss';
export declare const name = "divi/icon-picker";
/**
 * Icon Picker Modal component.
 *
 * @since ??
 *
 * @param {IconPickerModalProps} props Icon picker modal component props.
 *
 * @returns {ReactElement}
 */
export declare const IconPickerModal: ({ icons, showFilter, showSearch, selectedIcon, onChange, registerShortcuts, unregisterShortcuts, dimension, }: IconPickerModalProps) => ReactElement;
