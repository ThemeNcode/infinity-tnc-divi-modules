import { type AppUi } from '@divi/types';
/**
 * Event callback that gets triggered whenever the user change the mode
 * on the builder settings.
 *
 * @since ??
 *
 * @param {AppUi.Mode.Type} mode Mode.
 */
export declare const onChangeMode: (mode: AppUi.Mode.Type) => void;
