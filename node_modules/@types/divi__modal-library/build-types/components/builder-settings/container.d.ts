import { type ReactElement } from 'react';
/**
 * <BuilderSettingsModal /> wrapped by HOC which automatically passed necessary data from
 * store and updates itself whenever the said data state is updated on store.
 *
 * @since ??
 */
export declare const BuilderSettingsModalContainer: () => ReactElement;
