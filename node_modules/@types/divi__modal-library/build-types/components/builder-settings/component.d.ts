import { type ReactElement } from 'react';
import { type Props } from './types';
import './style.scss';
/**
 * Modal component for builder settings.
 *
 * @since ??
 *
 * @returns {ReactElement}
 */
export declare const BuilderSettingsModal: ({ defaults, onChangeMode, onChangeView, onChange, preferences, bodySiblingHeight, }: Props) => ReactElement;
