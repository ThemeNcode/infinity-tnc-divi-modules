import { type ReactElement } from 'react';
import { type ContainerProps } from './types';
/**
 * <AddModuleModal /> + Container HOC which automatically passed necessary data.
 *
 * @since ??
 *
 * @param {ContainerProps} props Component props.
 *
 * @returns {ReactElement}
 */
export declare const AddModuleModalContainer: (props: ContainerProps) => ReactElement;
