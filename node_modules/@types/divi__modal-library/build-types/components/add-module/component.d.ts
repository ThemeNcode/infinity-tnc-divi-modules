import { type ReactElement } from 'react';
import { type Props } from './types';
import './style.scss';
/**
 * Modal component for adding new module, row, row inner, section, and specialty section.
 *
 * @since ??
 *
 * @param {Props} props Component props.
 *
 * @returns {ReactElement}
 */
export declare const AddModuleModal: ({ name, tab, moduleFolderList, moduleFolder, moduleFilter, selectedOption, parentModule, type, onButtonClick, onBackButtonClick, onFilterOptionChange, onOptionKeyDown, onOptionMouseEnter, onOptionMouseLeave, adminBarHeight, topWindowHeight, topWindowWidth, openTab, onCloudUseItem, userCanAddFromLibrary, }: Props) => ReactElement;
