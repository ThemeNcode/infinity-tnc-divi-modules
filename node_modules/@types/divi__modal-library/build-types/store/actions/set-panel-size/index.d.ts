import { type ModalLibrary } from '@divi/types';
/**
 * Set modal's panel size. Panel size is needed for rendering multi-panels layout
 * when modal's wide is larger that defined breakpoint.
 *
 * @since ??
 *
 */
export declare const setPanelSize: (params: ModalLibrary.Store.Actions.SetPanelSize.Params) => ModalLibrary.Store.Actions.SetPanelSize.ReturnValue;
