import { type ModalLibrary } from '@divi/types';
/**
 * Set modal's body sibling height.
 *
 * Body sibling height is the height of `<Body>` component's
 * sibling. This value is needed to calculate the height of `<Body>` which is modal's height
 * minus body sibling's height.
 *
 * @since ??
 *
 */
export declare const setBodySiblingHeight: (params: ModalLibrary.Store.Actions.SetBodySiblingHeight.Params) => ModalLibrary.Store.Actions.SetBodySiblingHeight.ReturnValue;
