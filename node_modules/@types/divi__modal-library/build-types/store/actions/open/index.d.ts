import { type ModalLibrary } from '@divi/types';
/**
 * Open (registered) modal.
 *
 * @since ??
 *
 * @param {ModalLibrary.Store.Actions.Open.Params} params Parameters for opening modal.
 *
 * @returns {ModalLibrary.Store.Actions.Open.ReturnValue}
 */
export declare const open: <TAttributes = Record<string, any>>(params: ModalLibrary.Store.Actions.Open.Params<TAttributes>) => ModalLibrary.Store.Actions.Open.ReturnValue<TAttributes>;
