import { type ModalLibrary } from '@divi/types';
/**
 * Close modal.
 *
 * @since ??
 *
 * @param {ModalLibrary.Store.Actions.Close.Params} params Parameter for closing modal.
 *
 * @returns {ModalLibrary.Store.Actions.Close.ReturnValue}
 */
export declare const close: (params: ModalLibrary.Store.Actions.Close.Params) => ModalLibrary.Store.Actions.Close.ReturnValue;
