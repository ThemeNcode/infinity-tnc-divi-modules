import { type ModalLibrary } from '@divi/types';
/**
 * Sets layers view module for divi/layers state.
 *
 * This action sets the module with id of a layers modal with the divi/layers name.
 *
 * TODO feat(D5, Layers View) The `moduleInfo` is there as temporary solution to collect
 * all module info. So, we don't need to pass modal name when running dispatch. There is
 * a task to move Layers View to its own component.
 *
 * @link https://github.com/elegantthemes/Divi/issues/36253.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/modal-library').setLayersViewModule({
 *   moduleId: 'module-id-string',
 *   checkChildren: true,
 *   data: {
 *     expand: ! layerModule?.expand,
 *   },
 * });
 * ```
 */
export declare const setLayersViewModule: (moduleInfo: ModalLibrary.Store.Actions.SetLayersViewModule.Params['moduleInfo']) => ModalLibrary.Store.Actions.SetLayersViewModule.ReturnValue;
