import { type ModalLibrary } from '@divi/types';
/**
 * Set modal's dragging state.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function
 *
 * @example Set modal's dragging state.
 * ```ts
 * window.divi.data.dispatch('divi/modal-library').setDraggingState({
 *   name: 'divi/page-settings',
 *   status: true,
 * });
 * ```
 */
export declare const setDraggingState: (params: ModalLibrary.Store.Actions.SetDraggingState.Params) => ModalLibrary.Store.Actions.SetDraggingState.ReturnValue;
