import { type ModalLibrary } from '@divi/types';
/**
 * Get active modal.
 *
 * TODO(D5, Modal Library): `getActiveModal` should be renamed to `getActiveModals`, however this is not possible at
 * the moment, because we're keeping backward compatibility with non-D5Enhanced store, once the old store is removed
 * we can rename this selector and also change its select calls in different components.
 * This selector should return all active modals.
 *
 * @since ??
 *
 * @param {ModalLibrary.Store.ImmutableState} state Store state.
 * @param {string} modalType Modal type.
 *
 * @returns {ModalLibrary.Store.Modal | ModalLibrary.Store.State}
 */
export declare const getActiveModal: (state: ModalLibrary.Store.ImmutableState, modalType: string) => ModalLibrary.Store.Modal | ModalLibrary.Store.State;
