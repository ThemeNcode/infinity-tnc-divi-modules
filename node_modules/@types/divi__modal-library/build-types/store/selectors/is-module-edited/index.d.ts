import { type ModalLibrary } from '@divi/types';
/**
 * Check if `divi/module`'s modal is currently opened for editing module with given `moduleId`.
 *
 * @since ??
 *
 * @param {ModalLibrary.Store.ImmutableState} state Modal state.
 * @param {string} moduleId Module id.
 *
 * @returns {boolean}
 */
export declare const isModuleEdited: (state: ModalLibrary.Store.ImmutableState, moduleId: string) => boolean;
