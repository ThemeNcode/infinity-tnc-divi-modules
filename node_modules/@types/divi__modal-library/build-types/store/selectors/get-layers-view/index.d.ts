import { type ModalLibrary } from '@divi/types';
/**
 * Get the layers view from the provided state.
 *
 * @since ??
 *
 * @param {ModalLibrary.Store.ImmutableState} state The enhanced D5 state containing layer information.
 *
 * @returns {ModalLibrary.Store.ModalDefinition.layersView} The layers view from the state.
 */
export declare const getLayersView: (state: ModalLibrary.Store.ImmutableState) => ModalLibrary.Store.ModalDefinition['layersView'];
