import { type ModalLibrary } from '@divi/types';
/**
 * Check if modal is active / rendered.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example Check if `divi/page-settings` modal is active.
 * ```ts
 * const isActive = window.divi.data.select('divi/modal-library').isModalActive('divi/page-settings');
 * ```
 */
export declare const isModalActive: (state: ModalLibrary.Store.ImmutableState, modalName: string) => boolean;
