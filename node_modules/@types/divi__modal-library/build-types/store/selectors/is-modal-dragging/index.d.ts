import { type ModalLibrary } from '@divi/types';
/**
 * Check if modal is being dragged.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example Check if `divi/page-settings` modal is being dragged.
 * ```ts
 * const isDragging = window.divi.data.select('divi/modal-library').isModalDragging('divi/page-settings');
 * ```
 */
export declare const isModalDragging: (state: ModalLibrary.Store.ImmutableState, modalName: string) => boolean;
