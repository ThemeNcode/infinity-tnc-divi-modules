import { type ModalLibrary } from '@divi/types';
/**
 * Retrieves the state of a specific layers view module from the application state.
 *
 * @since ??
 *
 * @param {ModalLibrary.Store.ImmutableState} state The current application state.
 * @param {string} moduleId The unique identifier for the layers view module.
 * @returns {ModalLibrary.Store.LayerModuleData} The state of the specified layers view module.
 *
 * @example
 * ```ts
 * const layerModule = useSelect(select => select('divi/modal-library').getLayersViewModule('module-id'));
 * ```
 */
export declare const getLayersViewModule: (state: ModalLibrary.Store.ImmutableState, moduleId: string) => ModalLibrary.Store.LayerModuleData;
