import { type ModalLibrary } from '@divi/types';
/**
 * Get modal's owner
 * `owner` is used to refer to which entity "owned" / "opened" this currently opened modal.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example Get `divi/module` modal's owner.'
 * ```ts
 * const owner = window.divi.data.select('divi/modal-library').getModalOwner('divi/module');
 * ```
 */
export declare const getModalOwner: (state: ModalLibrary.Store.ImmutableState, modalName: string) => string;
