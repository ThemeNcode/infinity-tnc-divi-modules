import { type ModalLibrary } from '@divi/types';
/**
 * Retrieves the expansion state of all layers view modules from the application state.
 *
 * @since ??
 *
 * @param {ModalLibrary.Store.ImmutableState} state The current application state.
 * @returns {boolean} The expansion state for all layers view modules. Defaults to `false` if not found.
 *
 * @example
 * ```ts
 * const expandAll = useSelect(select => select('divi/modal-library').getLayersViewExpandAll());
 * ```
 */
export declare const getLayersViewExpandAll: (state: ModalLibrary.Store.ImmutableState) => boolean;
