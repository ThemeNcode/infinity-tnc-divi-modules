import { type ModalLibrary } from '@divi/types';
/**
 * Middleware effect for removing modal from sidebar row if currently closed modal is rendered in sidebar and
 * safe to be removed from sidebar row based on its type.
 *
 * @since ??
 */
export declare const maybeRemoveFromSidebarRow: (action: ModalLibrary.Store.Actions.Close.ReturnValue, store: ModalLibrary.Store.Instance) => void;
