import { type ModalLibrary } from '@divi/types';
/**
 * Reset app state to `default` whenever `divi/module` modal is closed.
 *
 * @since ??
 *
 */
export declare const resetAppState: (action: ModalLibrary.Store.Actions.Close.ReturnValue) => void;
