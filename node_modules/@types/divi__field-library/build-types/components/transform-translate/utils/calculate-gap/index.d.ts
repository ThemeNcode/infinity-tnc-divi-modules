import { type FieldLibrary } from '@divi/types';
/**
 * Calculate gap number between axis.
 *
 * @since ??
 *
 * @param  {number} gap Gap to calculate.
 * @param  {object} step Step for both axis.
 * @param  {object} dividend Axis as dividend.
 * @param  {object} divisor Axis as divisor.
 *
 * @returns {number} Calculated gap.
 */
export declare const calculateGap: (gap: number, step: FieldLibrary.TransformTranslate.Step, dividend: FieldLibrary.TransformTranslate.Axis, divisor: FieldLibrary.TransformTranslate.Axis) => number;
