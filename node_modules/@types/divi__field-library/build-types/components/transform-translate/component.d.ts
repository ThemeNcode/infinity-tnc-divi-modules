import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import './style.scss';
/**
 * TransformTranslate field component.
 *
 * Use this component to render Transform Translate field.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.name Field name.
 * @param {string[]} props.allowedUnits Allowed units.
 * @param {string} props.className Field class name.
 * @param {string} props.defaultUnit Default unit.
 * @param {object} props.defaultValue Field default value.
 * @param {Function} props.formatter Field formatter.
 * @param {number} props.max Maximum value.
 * @param {number} props.min Minimum value.
 * @param {boolean} props.readonly Flag to make the field readonly.
 * @param {object} props.value Field value.
 * @param {Function} props.onChange Field change callback.
 *
 * @returns {ReactElement} React component.
 */
declare const TransformTranslate: {
    ({ name, allowedUnits, className, defaultUnit, defaultValue, formatter, max, min, readonly, value, onChange, }: FieldLibrary.TransformTranslate.Props): ReactElement;
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
declare const name = "divi/transform-translate";
export { name, TransformTranslate, };
