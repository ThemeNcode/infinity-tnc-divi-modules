import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import './style.scss';
/**
 * Condition field component.
 *
 * This component is used to render condition field.
 *
 * @since ??
 *
 * @param {object} props Props for condition field.
 * @param {Array} props.value Value of condition field.
 * @param {string} props.className Classname of condition field.
 * @param {object} props.fields Fields of condition field.
 * @param {boolean} props.readonly Readonly of condition field.
 * @param {Function} props.onChange On change of condition field.
 * @param {boolean} props.showDeleteButton Show delete button of condition field.
 * @param {boolean} props.showAddButton Show add button of condition field.
 *
 * @returns {ReactElement} Condition field component.
 */
declare const Condition: {
    ({ value, className, fields, readonly, onChange, showDeleteButton, showAddButton, }: FieldLibrary.Conditions.Props): ReactElement;
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
declare const name = "divi/conditions";
export { name, Condition, };
