import { type Axis, type Mode } from '../../types';
/**
 * Get rotator circle center data by mode.
 *
 * @since ??
 *
 * @param {Mode} mode Circle mode.
 * @param {Axis} radius Rotator radius data.
 *
 * @returns {Axis}
 */
export declare const getRotatorCenter: (mode: Mode, radius: Axis) => Axis;
