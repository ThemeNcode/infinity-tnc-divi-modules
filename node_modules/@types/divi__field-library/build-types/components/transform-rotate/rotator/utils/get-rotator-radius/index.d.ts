import { type Axis, type Mode } from '../../types';
/**
 * Get rotator circle radius data by mode.
 *
 * @since ??
 *
 * @param {Mode} mode Circle mode.
 *
 * @returns {Axis}
 */
export declare const getRotatorRadius: (mode: Mode) => Axis;
