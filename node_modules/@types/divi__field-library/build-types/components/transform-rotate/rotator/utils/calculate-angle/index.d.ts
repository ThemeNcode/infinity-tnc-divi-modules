/**
 * Calculate the angle of rotator position.
 *
 * @since ??
 *
 * @param {MouseEvent} event Mouse event data.
 * @param {HTMLElement} container Container element of the rotator.
 *
 * @returns {number}
 */
export declare const calculateAngle: (event: MouseEvent, container: HTMLElement) => number;
