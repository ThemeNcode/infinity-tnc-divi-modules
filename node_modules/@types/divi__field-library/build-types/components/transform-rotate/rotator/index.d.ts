import { type ReactElement } from 'react';
import { type Props } from './types';
import './style.scss';
declare const Rotator: {
    ({ angle, className, readonly, mode, onRotateStart, onRotate, onRotateEnd, }: Props): ReactElement;
    defaultProps: {
        angle: number;
        onRotate: (...args: any[]) => void;
        onRotateStart: (...args: any[]) => void;
        onRotateEnd: (...args: any[]) => void;
    };
};
export { Rotator };
