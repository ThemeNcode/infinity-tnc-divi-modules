import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import './style.scss';
/**
 * TransformRotate field component.
 *
 * Used to render the transform rotate field.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.name Field name.
 * @param {string[]} props.allowedUnits Allowed units.
 * @param {string} props.className Additional class name.
 * @param {string} props.defaultUnit Default unit.
 * @param {object} props.defaultValue Default value.
 * @param {Function} props.formatter Value formatter.
 * @param {number} props.max Max value.
 * @param {number} props.min Min value.
 * @param {number} props.maxResetStepValue Max reset step value.
 * @param {number} props.minResetStepValue Min reset step value.
 * @param {boolean} props.readonly Flag to make the field readonly.
 * @param {object} props.value Current value.
 * @param {Function} props.onChange Value change callback.
 *
 * @returns {ReactElement} TransformRotate component.
 */
declare const TransformRotate: {
    ({ name, allowedUnits, className, defaultUnit, defaultValue, formatter, max, min, maxResetStepValue, minResetStepValue, readonly, value, onChange, }: FieldLibrary.TransformRotate.Props): ReactElement;
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
declare const name = "divi/transform-rotate";
export { name, TransformRotate, };
