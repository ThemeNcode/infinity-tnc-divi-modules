declare global {
    interface Window {
        wpCookies: WpCookies;
    }
}
export interface WpCookies {
    get(name: string): string;
    set?(name: string, value: string, expires: number, path: string, domain: string | boolean, secure: boolean): void;
}
