import { type ReactElement } from 'react';
import { type BufferedIconListProps } from './types';
import './style.scss';
/**
 * Truncated list of icons set by iconsToLoad.
 *
 * @see IconPicker
 *
 * @since ??
 *
 * @param {BufferedIconListProps} props Props for component.
 *
 * @returns {ReactElement}
 */
export declare const BufferedIconList: ({ icons, iconsToLoad, inputValue, onClickIcon, readonly, selectedIcon, }: BufferedIconListProps) => ReactElement;
