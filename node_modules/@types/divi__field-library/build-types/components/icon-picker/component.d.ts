import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import './style.scss';
/**
 * IconPicker field component.
 *
 * This component is used to render icon picker field.
 *
 * @since ??
 *
 * @param {object} props Props for select field.
 * @param {object[]} props.icons List of icons to be rendered.
 * @param {boolean} props.scrollToSelected Whether to scroll to selected icon.
 * @param {boolean} props.showFilter Whether to show filter.
 * @param {boolean} props.showModal Whether to show modal.
 * @param {boolean} props.showSearch Whether to show search.
 * @param {string} props.defaultValue Default value for icon picker.
 * @param {boolean} props.findReplaceMode Whether to render find\replace mode.
 * @param {Function} props.onChange Callback function when icon is selected.
 * @param {Function} props.openModal Callback function to open modal.
 * @param {boolean} props.readonly Whether icon picker is readonly.
 * @param {Function} props.registerShortcuts Callback function to register shortcuts.
 * @param {number} props.rowCount Number of rows to be rendered.
 * @param {Function} props.unregisterShortcuts Callback function to unregister shortcuts.
 * @param {string} props.value Value for icon picker.
 *
 * @returns {ReactElement} IconPicker component.
 */
declare const IconPicker: {
    ({ icons, scrollToSelected, showFilter, showModal, showSearch, defaultValue, findReplaceMode, onChange, openModal, readonly, registerShortcuts, rowCount, unregisterShortcuts, value, }: FieldLibrary.IconPicker.Props): ReactElement;
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
declare const name = "divi/icon-picker";
export { name, IconPicker, };
