import { type Filters } from '@types/divi__icon-library';
import { type Icon } from '@divi/types';
/**
 * Gets a list of icons that result of the search term and filter type.
 *
 * @since ??
 *
 * @param {{searchWord?: string; searchFilter?: Filters;}} params Word to search and filter to use.
 *
 * @returns {Array} List of icons.
 */
export declare const getFilteredSearchIcons: ({ icons, searchWord, searchFilter, }: {
    icons: Icon.Type['data'][];
    searchWord?: string;
    searchFilter?: Filters;
}) => Icon.Type['data'][];
