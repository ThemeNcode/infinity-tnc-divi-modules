import { type ReactElement } from 'react';
import { type IconFilterProps } from './types';
import './style.scss';
/**
 * Text search and selectable filter for icons.
 *
 * @since ??
 *
 * @see ExpandedIcon
 *
 * @param {IconFilterProps} props Icon filter props.
 * @returns {ReactElement}
 */
export declare const IconFilter: ({ dropdownActive, searchFilter, searchWord, maybeCloseDropdown, onFilterDropdownToggle, onFilterReset, onFilterSelect, onSearchInput, showFilter, showSearch, }: IconFilterProps) => ReactElement;
