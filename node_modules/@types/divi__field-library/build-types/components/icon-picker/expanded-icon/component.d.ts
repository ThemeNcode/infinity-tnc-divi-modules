import { type ReactElement } from 'react';
import { type ExpandedIconProps } from './types';
/**
 * ExpandedIcon Scrollable icon selector.
 *
 * @since ??
 *
 * @param {ExpandedIconProps} props Props for ExpandedIcon.
 *
 * @returns {ReactElement}
 *
 */
export declare const IconPickerExpandedIcon: ({ name, rowCount, customHeight, icons, onClickIcon, readonly, scrollToSelected, selectedIcon, setScrollArea, showFilter, showSearch, value, }: ExpandedIconProps) => ReactElement;
