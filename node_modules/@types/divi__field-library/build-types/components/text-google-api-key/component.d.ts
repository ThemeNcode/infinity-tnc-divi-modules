import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import '../map/style.scss';
/**
 * TextGoogleAPIKey field component.
 *
 * This component is used to render a text field with a button to change the API key.
 *
 * @since ??
 *
 * @param {object} props Component props.
 *
 * @param {string} props.name Field name.
 * @param {string} props.googleMapAPIKey Google map API key.
 * @param {string} props.changeAPIKeyUrl Change API key URL.
 *
 * @returns {ReactElement} TextGoogleAPIKey component.
 */
declare const TextGoogleAPIKey: {
    ({ name, googleMapAPIKey, changeAPIKeyUrl, }: FieldLibrary.TextGoogleAPIKey.Props): ReactElement;
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy and paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
declare const name = "divi/google-api-key-field";
export { name, TextGoogleAPIKey, };
