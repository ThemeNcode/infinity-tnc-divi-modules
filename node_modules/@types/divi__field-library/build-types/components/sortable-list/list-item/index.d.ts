import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
/**
 * Sortable List Item
 * This one is child element of the Sortable List input field.
 *
 * @since ??
 *
 * @param {object} props Props for sortable list field.
 * @param {string} props.className Class names for the item.
 * @param {number} props.index Index of the item.
 * @param {string} props.dragID Unique id for drag and drop functionality.
 * @param {string} props.value Item value.
 * @param {object} props.link Item link.
 * @param {boolean} props.isChecked If the item show the radio or checkbox input,
 * the radio or checkbox is checked or not checked.
 * @param {boolean} props.isCheckbox Show the checkbox input.
 * @param {boolean} props.isRadio Show the Radio input.
 * @param {object} props.actions All actions for the item.
 * @param {Function} props.onChange Callback function when value changes.
 * @param {Function} props.onCopy Callback function when the item is copied.
 * @param {Function} props.onDelete Callback function when the item is deleted.
 * @param {Function} props.onMove Callback function when the item is moved.
 * @param {Function} props.onFind Callback function to find the index.
 * @param {Function} props.onAdd Callback function to add a new item to the list.
 * @param {Function} props.onRadioChecked Callback function to call on radio input checked.
 *
 * @returns {ReactElement}
 */
export declare const SortableListItem: ({ className, index, dragID, value, link, isChecked, isCheckbox, isRadio, actions, onChange, onCopy, onDelete, onMove, onFind, onAdd, onRadioChecked, }: FieldLibrary.SortableList.ItemProps) => ReactElement;
