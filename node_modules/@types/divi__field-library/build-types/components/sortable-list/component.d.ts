import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import './style.scss';
/**
 * Sortable List
 * Sortable List input field.
 *
 * @since ??
 *
 * @param {object} props Props for sortable list field.
 * @param {boolean} props.allowEmpty Allow empty list.
 * @param {string} props.buttonLabel Add new button label.
 * @param {boolean} props.useAddNewButton Enable or disable add new button.
 * @param {Array} props.value Value for the sortable list.
 * @param {Function} props.onChange Callback function when value changes.
 * @param {boolean} props.isCheckbox To render checkbox in the list.
 * @param {boolean} props.isRadio To render radio in the list.
 * @param {boolean} props.addFirstOnMount If it set true and value is empty and
 * allowEmpty is set to false, it will fire onChange callback with first item.
 * @param {string} props.firstValue First item value.
 * @param {object} props.actions All actions for the sortable list item.
 *
 * @returns {ReactElement}
 */
declare const SortableList: {
    ({ allowEmpty, buttonLabel, isCheckbox, isRadio, addFirstOnMount, firstValue, actions, useAddNewButton, value, onChange, }: FieldLibrary.SortableList.Props): ReactElement;
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
declare const name = "divi/sortable-list";
export { name, SortableList, };
