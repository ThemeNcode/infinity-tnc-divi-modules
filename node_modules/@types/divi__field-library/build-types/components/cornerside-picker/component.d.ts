import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import './style.scss';
/**
 * CornerSidePicker field component.
 *
 * This component is used to display the corner side picker field.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} [props.className] Additional class name.
 * @param {string} [props.defaultValue] Default value.
 * @param {'absolute' | 'fixed'} [props.mode] Picker mode.
 * @param {string} props.name Field name.
 * @param {Function} props.onChange On change callback.
 * @param {string} props.value Field value.
 *
 *
 * @returns {ReactElement} CornerSidePicker component.
 */
declare const CornerSidePicker: {
    (props: FieldLibrary.CornerSidePicker.Props): ReactElement;
    defaultProps: {
        className: string;
        mode: FieldLibrary.CornerSidePicker.Mode;
    };
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
declare const name = "divi/cornerside-picker";
export { name, CornerSidePicker, };
