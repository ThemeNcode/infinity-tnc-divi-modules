import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import './style.scss';
/**
 * Spacing field component.
 *
 * Used to render spacing field in the Visual Builder.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.name Field name.
 * @param {string} [props.className] Additional classnames.
 * @param {Function} props.onChange Callback function to handle changes.
 * @param {number} [props.min] Minimum value.
 * @param {object} [props.value] Current value.
 * @param {object} [props.defaultValue] Default value.
 * @param {Array} [props.allowedUnits] Allowed units.
 *
 * @returns {ReactElement} Spacing field component.
 */
declare const Spacing: {
    (props: FieldLibrary.Spacing.Props): ReactElement;
    defaultProps: {
        className: string;
        min: any;
        defaultValue: {
            top: string;
            right: string;
            bottom: string;
            left: string;
            syncVertical: "off";
            syncHorizontal: "off";
        };
    };
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
declare const name = "divi/spacing";
export { name, Spacing, };
