import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import './style.scss';
/**
 * Button Options field component.
 *
 * Use this component to render Button Options field.
 *
 * @since ??
 *
 * @param {object} props Component Props.
 * @param {string} props.name Field name.
 * @param {string} [props.className] Additional classnames.
 * @param {string} [props.optionsClassName] Additional classnames for options.
 * @param {number} [props.column] Number of column.
 * @param {boolean} [props.multipleChoices] Whether to allow multiple choices.
 * @param {string} [props.value] Field value.
 * @param {Function} [props.onChange] On change callback.
 * @param {object} [props.options] Field options.
 * @param {boolean} [props.showLabel] Whether to show label.
 * @param {boolean} [props.fullwidth] Whether to make the field fullwidth.
 * @param {boolean} [props.tabUi] Whether to use tab UI.
 * @param {string} [props.defaultValue] Default value.
 * @param {boolean} [props.renderEmptyStringAsDefaultValue] Whether to render empty string as default value.
 * @param {boolean} [props.allowClickToUnset] Whether to allow click to unset.
 *
 * @returns {ReactElement} Button Options component.
 */
declare const ButtonOptions: {
    (props: FieldLibrary.ButtonOptions.Props): ReactElement;
    defaultProps: {
        className: string;
        optionsClassName: string;
        showLabel: boolean;
        fullwidth: boolean;
        renderEmptyStringAsDefaultValue: boolean;
    };
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
declare const name = "divi/button-options";
export { name, ButtonOptions, };
