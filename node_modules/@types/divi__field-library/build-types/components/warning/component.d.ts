import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import './style.scss';
/**
   * Warning field component.
   *
   * This component is used to render warning field in the Visual Builder.
   *
   * @since ??
   *
   * @param {object} props Component props.
   * @param {string} props.name The field name.
   * @param {string} props.value The field value.
   *
   * @returns {ReactElement} Warning field component.
   */
declare const Warning: {
    (props: FieldLibrary.Warning.Props): ReactElement;
    defaultProps: {
        value: string;
    };
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
declare const name = "divi/warning";
export { name, Warning, };
