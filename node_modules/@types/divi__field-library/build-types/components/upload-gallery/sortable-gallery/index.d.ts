import { type ReactElement } from 'react';
import { type Props } from './types';
/**
 * UploadGallery field component.
 *
 * @since ??
 *
 * @param {Props} props Component props.
 *
 * @returns {ReactElement}
 */
export declare const SortableGallery: ({ find, idsArray, move, onRemove, readonly, }: Props) => ReactElement;
