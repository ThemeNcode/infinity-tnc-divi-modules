export type Props = {
    id: string;
    onRemove: (id: string) => void;
    readonly: boolean;
};
export type MediaResult = {
    source_url: string;
    media_details: {
        sizes: {
            thumbnail: {
                source_url: string;
            };
        };
    };
};
