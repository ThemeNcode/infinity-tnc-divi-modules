import { type MediaDefaults } from './types';
/**
 * Opens the upload media modal and returns a Promise on selection.
 *
 * @since ??
 *
 * @param {MediaDefaults} options WP Media frame options.
 *
 * @returns {Promise}
 */
export declare const upload: (options?: MediaDefaults) => Promise<{
    [key: string]: string;
}>;
