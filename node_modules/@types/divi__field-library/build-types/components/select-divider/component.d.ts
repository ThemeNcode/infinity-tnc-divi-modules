import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import './style.scss';
/**
 * SelectDivider field component.
 *
 * This component is used to render the Select Divider field.
 *
 * @since ??
 *
 * @param {object} props Props for Select divider field.
 * @param {string[]} props.flip Flip status.
 * @param {string} props.placement Placement of the divider.
 * @param {string} props.className Classname for the field.
 * @param {string} props.name Name of the field.
 * @param {string} props.value Value of the field.
 * @param {string} props.defaultValue Default value of the field.
 * @param {Function} props.onChange Callback function to handle the change of the field.
 *
 * @returns {ReactElement} SelectDivider field component.
 */
declare const SelectDivider: {
    ({ flip, placement, className, name, value, defaultValue, onChange, }: FieldLibrary.SelectDivider.Props): ReactElement;
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
declare const name = "divi/select-divider";
export { name, SelectDivider, };
