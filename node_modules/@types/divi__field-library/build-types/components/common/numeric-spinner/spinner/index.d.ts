import React, { type ReactElement } from 'react';
import { type Props } from './types';
import './style.scss';
export declare const Spinner: React.MemoExoticComponent<({ dragScale, animationSpeed, onSpinStart, onSpin, onSpinEnd, }: Props) => ReactElement>;
