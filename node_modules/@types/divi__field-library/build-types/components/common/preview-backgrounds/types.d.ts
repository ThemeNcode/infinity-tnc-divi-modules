import { type CSSProperties, type MouseEvent } from 'react';
import { type Module } from '@divi/types';
/**
 * @typedef {object} PreviewBackgroundsProps
 * @property {string} addTitle Add title.
 * @property {object} backgroundAttr Background attribute.
 * @property {boolean} isEmptyValue Is empty value.
 * @property {Function} [onClickCanvas] On click canvas.
 * @property {Function} [onClickAddButton] On click add button.
 * @property {Function} [onClickRemoveButton] On click remove button.
 * @property {Function} [onClickResetButton] On click reset button.
 * @property {boolean} [showResetButton] Show reset button.
 */
export type PreviewBackgroundsProps = {
    addTitle: string;
    backgroundAttr: Module.Element.Decoration.Background.AttributeValue;
    isEmptyValue: boolean;
    onClickCanvas?: (event: MouseEvent) => void;
    onClickAddButton?: (event: MouseEvent) => void;
    onClickRemoveButton?: (event: MouseEvent) => void;
    onClickResetButton?: (event: MouseEvent) => void;
    showResetButton?: boolean;
};
/**
 * @typedef {object} PreviewBackgroundStyles
 * @property {object} background Background.
 * @property {object} pattern Pattern.
 * @property {object} mask Mask.
 */
export type PreviewBackgroundStyles = {
    background: CSSProperties;
    pattern: CSSProperties;
    mask: CSSProperties;
};
