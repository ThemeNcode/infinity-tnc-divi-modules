import { type ReactElement } from 'react';
import { type PreviewBackgroundsProps } from './types';
import './style.scss';
export declare const name = "divi/preview-backgrounds";
/**
 * Preview Backgrounds Component.
 *
 * Shows Preview For all Backgrounds (Color, Gradient, Image, Video, Pattern and Mask).
 *
 * @since ??
 *
 * @param {object} props Needed props to render preview components.
 * @param {string} [props.addTitle] Title for the add button.
 * @param {object} [props.backgroundAttr] Background attributes.
 * @param {boolean} [props.isEmptyValue] Whether the value is empty.
 * @param {Function} [props.onClickCanvas] Callback for clicking the canvas.
 * @param {Function} [props.onClickAddButton] Callback for clicking the add button.
 * @param {Function} [props.onClickRemoveButton] Callback for clicking the remove button.
 * @param {Function} [props.onClickResetButton] Callback for clicking the reset button.
 * @param {boolean} [props.showResetButton] Whether to show the reset button.
 *
 * @returns {ReactElement} ReactElement.
 */
export declare const PreviewBackgrounds: ({ addTitle, backgroundAttr, isEmptyValue, onClickCanvas, onClickAddButton, onClickRemoveButton, onClickResetButton, showResetButton, }: PreviewBackgroundsProps) => ReactElement;
