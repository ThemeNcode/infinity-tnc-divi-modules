import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import './style.scss';
export declare const name = "divi/common-preview";
/**
 * Preview Component.
 *
 * Mostly used for upload and color field component.
 *
 * @since ??
 *
 * @param {object} props Preview component props.
 * @param {string} [props.className] Additional class names.
 * @param {string} [props.caption] Caption to display below the preview.
 * @param {string} [props.addTitle] Title for the add button.
 * @param {object} [props.previewStyle] Style object for the preview.
 * @param {ReactNode} [props.children] Children to render inside the preview.
 * @param {boolean} [props.showAddButton] Whether to show the add button.
 * @param {boolean} [props.showEditButton] Whether to show the edit button.
 * @param {boolean} [props.showSwapButton] Whether to show the swap button.
 * @param {boolean} [props.showRemoveButton] Whether to show the remove button.
 * @param {boolean} [props.showResetButton] Whether to show the reset button.
 * @param {boolean} [props.showReverseButton] Whether to show the reverse button.
 * @param {boolean} [props.isEmptyValue] Whether the value is empty.
 * @param {Function} [props.onClickCanvas] Callback for clicking the canvas.
 * @param {Function} [props.onClickAddButton] Callback for clicking the add button.
 * @param {Function} [props.onClickEditButton] Callback for clicking the edit button.
 * @param {Function} [props.onClickSwapButton] Callback for clicking the swap button.
 * @param {Function} [props.onClickRemoveButton] Callback for clicking the remove button.
 * @param {Function} [props.onClickResetButton] Callback for clicking the reset button.
 * @param {Function} [props.onClickReverseButton] Callback for clicking the reverse button.
 *
 * @returns {ReactElement} Preview component.
 */
export declare const Preview: ({ className, caption, addTitle, previewStyle, children, showAddButton, showEditButton, showSwapButton, showRemoveButton, showResetButton, showReverseButton, isEmptyValue, onClickCanvas, onClickAddButton, onClickEditButton, onClickSwapButton, onClickRemoveButton, onClickResetButton, onClickReverseButton, }: FieldLibrary.Preview.Props) => ReactElement;
