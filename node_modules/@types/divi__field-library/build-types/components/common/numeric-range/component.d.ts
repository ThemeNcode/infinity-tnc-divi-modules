import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import './style.scss';
/**
 * Common field: NumericRange.
 *
 * NumericRange is a component that combines the NumericInput and RangeInput components.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.name The field name.
 * @param {Array} props.allowedUnits The allowed units.
 * @param {string} props.className The field class name.
 * @param {string} props.defaultUnit The default unit.
 * @param {string} props.defaultValue The default value.
 * @param {number} props.maxRange The range max limit.
 * @param {number} props.minRange The range min limit.
 * @param {number} props.maxNumeric The numeric max limit.
 * @param {number} props.minNumeric The numeric min limit.
 * @param {boolean} props.readonly The readonly flag.
 * @param {number} props.step The numeric step.
 * @param {string} props.value The field value.
 * @param {object} props.valueMapping The value mapping.
 * @param {Function} props.formatter The value formatter.
 * @param {Function} props.onChange The field onChange callback.
 *
 * @returns {ReactElement} React component.
 */
declare const NumericRange: ({ name, allowedUnits, className, defaultUnit, defaultValue, maxRange, minRange, maxNumeric, minNumeric, readonly, step, value, valueMapping, formatter, onChange, }: FieldLibrary.NumericRange.Props) => ReactElement;
declare const updateSources: {
    SOURCE_RANGE: string;
    SOURCE_INPUT: string;
    SOURCE_ARROW_UP: string;
    SOURCE_ARROW_DOWN: string;
};
export declare const name = "divi/common-numeric-range";
export { updateSources, NumericRange, };
