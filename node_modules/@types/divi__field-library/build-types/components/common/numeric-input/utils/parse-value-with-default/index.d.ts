import { type FieldLibrary } from '@divi/types';
/**
 * Parse value as number and unit by account default value as fallback.
 *
 * @since ??
 *
 * @param  {string} value Raw value.
 *
 * @returns {object | null} Will return null on failure.
 */
export declare const parseValueWithDefault: (value: string, defaultValue?: string) => FieldLibrary.NumericInput.ValueNumberUnit | null;
