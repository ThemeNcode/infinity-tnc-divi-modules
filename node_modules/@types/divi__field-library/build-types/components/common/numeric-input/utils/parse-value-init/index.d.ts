/**
 * Extract CSS unit from a value.
 *
 * @since ??
 *
 * @param  {string} value Raw value.
 * @param  {string} fallbackValue Fallback value on failure.
 *
 * @returns {string} Will return fallbackValue on failure. Or null when no fallback value provided.
 */
export declare const parseValueUnit: (value: string, fallbackValue?: string) => string | null;
