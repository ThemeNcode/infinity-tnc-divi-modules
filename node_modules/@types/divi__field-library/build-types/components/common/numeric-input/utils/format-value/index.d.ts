import { type FieldLibrary } from '@divi/types';
/**
 * Format value to proper format.
 *
 * This helper will append/remove the CSS unit based on defined defaultUnit, allowedUnits params.
 * The defaultUnit params will be appended as CSS unit
 * when the passed value unit is empty or not listed in allowedUnits params.
 *
 * Will also formats a number using fixed-point notation based on defined digits params.
 *
 * @since ??
 *
 * @param {string} value The raw value to be formatted.
 * @param {string} defaultUnit The value default CSS unit.
 * @param {string} allowedUnits The allowed CSS unit list.
 * @param {Function} formatter Format of the value.
 * @param {number} minValue Min value for the range.
 * @param {number} maxValue Max value for the range.
 *
 * @returns {string}
 */
export declare const formatValue: (value: string, defaultUnit?: string, allowedUnits?: string[], formatter?: FieldLibrary.NumericInput.Props['formatter'], minValue?: number, maxValue?: number) => string;
