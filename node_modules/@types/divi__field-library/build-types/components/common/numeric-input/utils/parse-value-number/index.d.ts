/**
 * Extract number from a value.
 *
 * @since ??
 *
 * @param  {string} value Raw value.
 * @param  {number} fallbackValue Fallback value on failure.
 *
 * @returns {number} Will return fallbackValue on failure. Or null when no fallback value provided.
 */
export declare const parseValueNumber: (value: string, fallbackValue?: number) => number | null;
