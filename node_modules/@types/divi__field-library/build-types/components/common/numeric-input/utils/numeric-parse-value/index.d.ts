import { type FieldLibrary } from '@divi/types';
/**
 * Parse value as number and CSS unit.
 *
 * @since ??
 *
 * @param  {string} value Raw value.
 *
 * @returns {object | null} Will return null on failure.
 */
export declare const numericParseValue: (value: string) => FieldLibrary.NumericInput.ValueNumberUnit | null;
