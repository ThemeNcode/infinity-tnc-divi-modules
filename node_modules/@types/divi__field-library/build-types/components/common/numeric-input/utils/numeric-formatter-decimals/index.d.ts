import { type FieldLibrary } from '@divi/types';
/**
 * Floating number with number of decimals for numeric input formatter.
 *
 * @since ??
 *
 * @param  {number} decimal Decimal number.
 * @param  {boolean} fixed  Fixed decimal.
 *
 * @returns {Function} Will return null on failure.
 */
export declare const numericFormatterDecimals: (decimal: number, fixed?: boolean) => FieldLibrary.NumericInput.Props['formatter'];
