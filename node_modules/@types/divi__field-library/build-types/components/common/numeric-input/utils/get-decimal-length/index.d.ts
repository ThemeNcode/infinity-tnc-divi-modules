/**
 * Takes a number as input and returns the length of its decimal part.
 *
 * @since ??
 *
 * @param {number} value A number for which we want to determine the length of the decimal part.
 *
 * @returns {number} The length of the decimal part of the input number.
 */
export declare const getDecimalLength: (value: number) => number;
