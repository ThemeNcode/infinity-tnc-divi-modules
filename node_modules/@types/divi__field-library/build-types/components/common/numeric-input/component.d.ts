import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import './style.scss';
/**
 * Common field: NumericInput.
 *
 * This component is used to render the numeric input field.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.name Field name.
 * @param {string[]} props.allowedUnits Allowed CSS units.
 * @param {string} props.className Additional class name.
 * @param {string} props.defaultUnit Default CSS unit.
 * @param {string} props.defaultValue Default value.
 * @param {number} props.max Maximum value.
 * @param {number} props.min Minimum value.
 * @param {number} props.maxResetStepValue Maximum value for reset step.
 * @param {number} props.minResetStepValue Minimum value for reset step.
 * @param {boolean} props.readonly Is readonly.
 * @param {number} props.step Step value.
 * @param {string} props.value Current value.
 * @param {object[]} props.valueMapping Value mapping.
 * @param {string[]} props.allowedValues Allowed values.
 * @param {Function} props.formatter Formatter function.
 * @param {Function} props.onChange On change callback.
 * @param {Function} props.onFocus On focus callback.
 * @param {Function} props.onBlur On blur callback.
 *
 * @returns {ReactElement} React component.
 */
declare const NumericInput: ({ name, allowedUnits, className, defaultUnit, defaultValue, max, min, maxResetStepValue, minResetStepValue, readonly, step, value, valueMapping, allowedValues, formatter, onChange, onFocus, onBlur, }: FieldLibrary.NumericInput.Props) => ReactElement;
declare const updateSources: {
    SOURCE_INPUT: string;
    SOURCE_ARROW_UP: string;
    SOURCE_ARROW_DOWN: string;
};
export declare const name = "divi/common-numeric-input";
export { updateSources, NumericInput, };
