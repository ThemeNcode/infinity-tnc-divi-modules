import React, { type ReactElement } from 'react';
import { type Props } from './types';
import './style.scss';
export declare const Incrementor: React.MemoExoticComponent<{
    (props: Props): ReactElement;
    defaultProps: {
        preventDefault: boolean;
        readonly: boolean;
    };
}>;
