import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import './style.scss';
/**
 * BorderStylesPreview field component.
 *
 * Use this component to render border styles preview.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {object} props.value Border styles value.
 * @param {object} props.defaultValue Default border styles value.
 *
 * @returns {ReactElement} BorderStylesPreview component.
 */
declare const BorderStylesPreview: {
    ({ value, defaultValue, }: FieldLibrary.BorderStylesPreview.Props): ReactElement;
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
declare const name = "divi/border-styles-preview";
export { name, BorderStylesPreview, };
