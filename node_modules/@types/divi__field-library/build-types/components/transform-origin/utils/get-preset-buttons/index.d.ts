import { type FieldLibrary } from '@divi/types';
/**
 * Get the preset buttons list.
 *
 * @since ??
 *
 * @param {'top' | 'center' | 'bottom'} row Row of the preset buttons.
 *
 * @returns {object[]} Preset buttons list.
 */
export declare const getPresetButtons: (row: 'top' | 'center' | 'bottom') => FieldLibrary.TransformOrigin.ValuePreset[];
