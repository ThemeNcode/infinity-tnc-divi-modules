import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import './style.scss';
/**
 * TransformOrigin field component.
 *
 * Used to control the transform origin value.
 *
 * @example
 * ```tsx
 * <TransformOrigin
 *  name="transform-origin"
 *  allowedUnits={['%', 'em', 'rem', 'px', 'vw', 'vh']}
 *  defaultUnit="%"
 *  defaultValue={{
 *  x: '50%',
 *  y: '50%',
 *  }}
 *  formatter={value => value}
 *  max={150}
 *  min={-50}
 *  readonly={false}
 *  value={{
 *  x: '50%',
 *  y: '50%',
 *  }}
 *  onChange={value => console.log(value)}
 *  />
 *  ```
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.name Field name.
 * @param {string[]} props.allowedUnits Allowed units.
 * @param {string} props.className Additional class name.
 * @param {string} props.defaultUnit Default unit.
 * @param {object} props.defaultValue Default value.
 * @param {Function} props.formatter Value formatter.
 * @param {number} props.max Max value.
 * @param {number} props.min Min value.
 * @param {boolean} props.readonly Flag to make the field readonly.
 * @param {object} props.value Current value.
 * @param {Function} props.onChange Callback function when the value is changed.
 *
 * @returns {ReactElement} TransformOrigin component.
 */
declare const TransformOrigin: {
    ({ name, allowedUnits, className, defaultUnit, defaultValue, formatter, max, min, readonly, value, onChange, }: FieldLibrary.TransformOrigin.Props): ReactElement;
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
declare const name = "divi/transform-origin";
export { name, TransformOrigin, };
