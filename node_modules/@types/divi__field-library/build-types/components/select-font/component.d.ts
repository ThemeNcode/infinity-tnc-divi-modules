import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import './style.scss';
/**
 * SelectFont field component.
 *
 * Used for selecting font from Google Fonts and uploaded fonts.
 *
 * @since ??
 *
 * @param {object} props Props for Select font field.
 * @param {Array} props.googleFonts List of Google Fonts.
 * @param {object} props.customFonts List of uploaded fonts.
 * @param {string} props.className Classname for Select font field.
 * @param {string} props.defaultValue Default value for Select font field.
 * @param {string} props.name Name for Select font field.
 * @param {string} props.value Value for Select font field.
 * @param {Function} props.onChange Callback function when Select font field value is changed.
 * @param {Function} props.onDeleteFont Callback function when font is deleted.
 * @param {Function} props.onHoverFont Callback function when font is hovered.
 * @param {Function} props.openModal Callback function to open modal.
 * @param {Function} props.registerShortcuts Callback function to register shortcuts.
 * @param {Function} props.unregisterShortcuts Callback function to unregister shortcuts.
 *
 * @returns {ReactElement} Select font field component.
 */
declare const SelectFont: {
    ({ googleFonts, customFonts, className, defaultValue, name, value, onChange, onDeleteFont, onHoverFont, openModal, registerShortcuts, unregisterShortcuts, }: FieldLibrary.SelectFont.Props): ReactElement;
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
declare const name = "divi/select-font";
export { name, SelectFont, };
