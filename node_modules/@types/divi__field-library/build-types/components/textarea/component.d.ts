import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import './style.scss';
/**
 * Textarea field component.
 *
 * This component is used to render textarea field in the settings modal.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.value Textarea value.
 * @param {Function} props.onChange Textarea change callback.
 * @param {string} [props.className] Additional class name.
 * @param {string} [props.name] Textarea name.
 * @param {boolean} [props.readonly] Textarea readonly state.
 * @param {string} [props.processing] Textarea processing type.
 *
 * @returns {ReactElement} Textarea field component.
 */
declare const TextArea: {
    ({ value, onChange, className, name, readonly, processing, }: FieldLibrary.TextArea.Props): ReactElement;
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
declare const name = "divi/textarea";
export { name, TextArea, };
