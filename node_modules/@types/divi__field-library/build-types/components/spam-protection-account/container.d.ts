import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import { SpamProtectionAccount } from './component';
/**
 * Container component for SpamProtectionAccount component.
 *
 * @since ??
 *
 * @param {FieldLibrary.SpamProtectionAccount.ContainerProps} props Component props.
 *
 * @returns {ReactElement} SpamProtectionAccount component.
 */
declare const SpamProtectionAccountContainer: {
    (props: FieldLibrary.SpamProtectionAccount.ContainerProps): ReactElement;
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
export { SpamProtectionAccountContainer, };
