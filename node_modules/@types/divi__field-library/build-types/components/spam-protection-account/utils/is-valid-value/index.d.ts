import { type SpamProtection } from '@divi/types';
/**
 * Checks if a given value is a valid account and listed in the spam protection service accounts.
 *
 * @since ??
 *
 * @param {string} value The value to check.
 * @param {Record<string, SpamProtection.Service.Account>} accounts The spam protection service accounts.
 *
 * @returns {boolean} Returns true if the value is a valid, otherwise false.
 */
export declare const isValidValue: (value: string, accounts: Record<string, SpamProtection.Service.Account>) => boolean;
