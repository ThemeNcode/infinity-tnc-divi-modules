import { type FieldLibrary, type SpamProtection } from '@divi/types';
/**
 * Populates the options for a select field with the spam protection service accounts and lists.
 *
 * @since ??
 *
 * @param {SpamProtection.Service.Provider} spamProtectionService The spam protection service provider.
 *
 * @returns {FieldLibrary.Select.Options} Returns an object of options for a select field.
 */
export declare const populateOptions: (spamProtectionService: SpamProtection.Service.Provider) => FieldLibrary.Select.Options;
