import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import './style.scss';
/**
 * Renders a component for managing spam protection accounts.
 *
 * @since ??
 *
 * @param {FieldLibrary.SpamProtectionAccount.Props} props The component props.
 *
 * @returns {ReactElement} The rendered component.
 */
declare const SpamProtectionAccount: {
    ({ defaultValue, name, onAdd, onChange, onRemove, value, provider, }: FieldLibrary.SpamProtectionAccount.Props): ReactElement;
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
declare const name = "divi/spam-protection-account";
export { name, SpamProtectionAccount, };
