import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import './style.scss';
/**
 * Range field component.
 *
 * This component is used to render a range input element and a numeric spinner.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {number} props.min Minimum value.
 * @param {number} props.max Maximum value.
 * @param {string} props.name Field name.
 * @param {string} props.className Additional class name.
 * @param {Function} props.onChange Callback function when value changed.
 * @param {string} props.value Current value.
 * @param {number} props.step Step value.
 * @param {number} props.maxLimit Maximum limit.
 * @param {number} props.minLimit Minimum limit.
 * @param {boolean} props.allowEmpty Allow empty value.
 * @param {boolean} props.readonly Read only flag.
 * @param {string} props.getDefaultFrom Get default value from another field.
 * @param {object} props.defaultValuesMapping Mapping of default values.
 * @param {string} props.defaultValue Default value.
 * @param {string[]} props.allowedValues Allowed values.
 * @param {string[]} props.allowedUnits Allowed units.
 * @param {string} props.defaultUnit Default unit.
 * @param {string} props.placeholder Placeholder value.
 * @param {object[]} props.valueMapping Value mapping.
 *
 * @returns {ReactElement} Range component.
 */
declare const Range: {
    (props: FieldLibrary.Range.Props): ReactElement;
    defaultProps: {
        className: string;
    };
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
declare const name = "divi/range";
export { name, Range, };
