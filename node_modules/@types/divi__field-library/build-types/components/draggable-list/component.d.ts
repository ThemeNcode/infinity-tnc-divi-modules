import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import './style.scss';
/**
 * Draggable List field component.
 *
 * This component is used to render a list of items that can be dragged and dropped.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} [props.className] Class name.
 * @param {Array} [props.defaultValue] Default value.
 * @param {Array} [props.value] Value.
 * @param {string} [props.addTitle] Add button title.
 * @param {boolean} [props.disableDuplicate] Disable duplicate.
 * @param {boolean} [props.disableDelete] Disable delete.
 * @param {boolean} [props.readonly] Read only.
 * @param {boolean} [props.hideAddButton] Hide add button.
 * @param {Function} [props.onChange] On change callback.
 * @param {Function} [props.onClickDelete] On click delete callback.
 * @param {Function} [props.onClickDuplicate] On click duplicate callback.
 * @param {Function} [props.onClickSettings] On click settings callback.
 * @param {Function} [props.onClickMore] On click more callback.
 * @param {Function} [props.onAddNew] On add new callback.
 * @param {Function} [props.onContextMenu] On context menu callback.
 * @param {Function} [props.onDragStart] On drag start callback.
 * @param {Function} [props.onDragEnd] On drag end callback.
 *
 * @returns {ReactElement} Draggable List component.
 */
declare const DraggableList: {
    ({ className, defaultValue, value, addTitle, disableDuplicate, disableDelete, readonly, hideAddButton, onChange, onClickDelete, onClickDuplicate, onClickSettings, onClickMore, onAddNew, onContextMenu, onDragStart, onDragEnd, }: FieldLibrary.DraggableList.Props): ReactElement;
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
declare const name = "divi/draggable-list";
export { name, DraggableList, };
