import { type ReactElement } from 'react';
import { type Props } from './types';
import './style.scss';
/**
 * Draggable List field item component.
 *
 * @since ??
 *
 * @param {Props} props Component props.
 *
 * @returns {ReactElement}
 */
declare const ListItem: ({ id, title, disableDuplicate, disableDelete, readonly, onClickSettings, onClickDuplicate, onClickDelete, onClickMore, onMove, onFind, onContextMenu, onDragStart, onDragEnd, }: Props) => ReactElement;
export { ListItem, };
