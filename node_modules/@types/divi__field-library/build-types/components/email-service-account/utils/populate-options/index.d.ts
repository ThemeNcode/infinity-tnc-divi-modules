import { type EmailMarketing, type FieldLibrary } from '@divi/types';
/**
 * Populates the options for a select field with the email service accounts and lists.
 *
 * @since ??
 *
 * @param {EmailMarketing.Service.Provider} emailService The email service provider.
 * @param {boolean} [combineLabel] Whether to combine the account and list name into a single label.
 *
 * @returns {FieldLibrary.Select.Options} Returns an object of options for a select field.
 */
export declare const populateOptions: (emailService: EmailMarketing.Service.Provider, combineLabel?: boolean) => FieldLibrary.Select.Options;
