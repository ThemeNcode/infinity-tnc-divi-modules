/**
 * Parses the value of the email service account.
 *
 * @param {string} value The value to parse.
 *
 * @returns {{ account: string, list?: string }} Returns an object with properties `account` and `list`.
 */
export declare const parseValue: (value: string) => {
    account: string;
    list?: string;
};
