import { type EmailMarketing } from '@divi/types';
/**
 * Checks if a given value is a valid account and listed in the email service accounts.
 *
 * @since ??
 *
 * @param {string} value The value to check.
 * @param {Record<string, EmailMarketing.Service.Account>} accounts The email service accounts.
 *
 * @returns {boolean} Returns true if the value is a valid, otherwise false.
 */
export declare const isValidValue: (value: string, accounts: Record<string, EmailMarketing.Service.Account>) => boolean;
