import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import './style.scss';
/**
 * Renders a component for managing email service accounts.
 *
 * @since ??
 *
 * @param {FieldLibrary.EmailServiceAccount.Props} props The component props.
 *
 * @returns {ReactElement} The rendered component.
 */
declare const EmailServiceAccount: {
    ({ defaultValue, name, onAdd, onChange, onFetch, onRemove, value, provider, }: FieldLibrary.EmailServiceAccount.Props): ReactElement;
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
declare const name = "divi/email-service-account";
export { name, EmailServiceAccount, };
