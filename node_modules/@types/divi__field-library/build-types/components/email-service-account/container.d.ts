import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import { EmailServiceAccount } from './component';
/**
 * Container component for EmailServiceAccount component.
 *
 * @since ??
 *
 * @param {FieldLibrary.EmailServiceAccount.ContainerProps} props Component props.
 *
 * @returns {ReactElement} EmailServiceAccount component.
 */
declare const EmailServiceAccountContainer: {
    (props: FieldLibrary.EmailServiceAccount.ContainerProps): ReactElement;
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
export { EmailServiceAccountContainer, };
