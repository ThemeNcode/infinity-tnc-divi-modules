import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import './style.scss';
/**
 * Select field component.
 *
 * This component is used to render Select field inside the settings modal.
 *
 * @since ??
 *
 * @param {object} props Props for select field.
 * @param {string} props.name Name for select field.
 * @param {Function} props.onChange On change callback for select field.
 * @param {object} props.options List of options.
 * @param {string} props.value Value for select field.
 * @param {boolean} props.activeOnLoad Active on load.
 * @param {object} props.additionalOptions Additional options.
 * @param {Function} props.afterList After list callback.
 * @param {Function} props.beforeList Before list callback.
 * @param {string} props.cookiePath Cookie path.
 * @param {string} props.className Classname for select field.
 * @param {string} props.defaultValue Default value for select field.
 * @param {boolean} props.displayCheckMark Display check mark.
 * @param {boolean} props.displayRecent Display recent.
 * @param {string} props.emptyLabel Empty label.
 * @param {boolean} props.groupSeparator Group separator.
 * @param {Function} props.labelFilter Label filter callback.
 * @param {Function} props.onClose On close callback.
 * @param {Function} props.onOpen On open callback.
 * @param {Function} props.onHoverOption On hover option callback.
 * @param {boolean} props.readonly Readonly.
 * @param {string} props.recentCategory Recent category.
 * @param {boolean} props.searchable Searchable.
 * @param {string} props.searchPlaceholder Search placeholder.
 * @param {Function} props.valuesToUpdateCallback Values to update callback.
 * @param {Function} props.registerShortcuts Register shortcuts callback.
 * @param {Function} props.unregisterShortcuts Unregister shortcuts callback.
 * @param {boolean} props.updateMenuPositionOnLoad Update menu position on load.
 *
 * @returns {ReactElement} Select field component.
 */
declare const Select: {
    ({ name, onChange, options, value, activeOnLoad, additionalOptions, afterList, beforeList, cookiePath, className, defaultValue, displayCheckMark, displayRecent, emptyLabel, groupSeparator, labelFilter, onClose, onOpen, onHoverOption, readonly, recentCategory, searchable, searchPlaceholder, valuesToUpdateCallback, registerShortcuts, unregisterShortcuts, updateMenuPositionOnLoad, }: FieldLibrary.Select.Props): ReactElement;
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
declare const name = "divi/select";
export { name, Select, };
