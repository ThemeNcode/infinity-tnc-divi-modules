import React, { type ReactElement } from 'react';
import { type Props } from './types';
import './style.scss';
export declare const Track: React.MemoExoticComponent<{
    (props: Props): ReactElement;
    defaultProps: {
        className: string;
        offset: number;
        width: number;
    };
}>;
