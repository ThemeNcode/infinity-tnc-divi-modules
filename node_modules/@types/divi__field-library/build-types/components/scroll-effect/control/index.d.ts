import React, { type ReactElement } from 'react';
import { type Module } from '@divi/types';
import { type Props } from './types';
import './style.scss';
export declare const MotionControl: React.MemoExoticComponent<{
    (props: Props): ReactElement;
    defaultProps: {
        value: {};
        defaultValue: Module.Element.Decoration.Scroll.ItemValue;
        i10n: {
            startTitle: string;
            endTitle: string;
        };
    };
}>;
