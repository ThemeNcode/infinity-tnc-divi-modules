import React, { type ReactElement } from 'react';
import { type Props } from './types';
import './style.scss';
export declare const InputsList: React.MemoExoticComponent<{
    (props: Props): ReactElement;
    defaultProps: {
        value: string;
        defaultValue: string;
    };
}>;
