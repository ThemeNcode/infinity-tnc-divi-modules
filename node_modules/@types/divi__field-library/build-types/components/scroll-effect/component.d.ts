import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import './style.scss';
/**
 * ScrollEffect field component.
 *
 * This component is used to render the scroll effect field.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {object} props.value Scroll effect value.
 * @param {string} props.name Scroll effect name.
 * @param {string} props.className Scroll effect class name.
 * @param {string} props.type Scroll effect type.
 * @param {Function} props.onChange Scroll effect on change callback.
 * @param {object} props.defaultValue Scroll effect default value.
 *
 * @returns {ReactElement} ScrollEffect component.
 */
declare const ScrollEffect: {
    (props: FieldLibrary.ScrollEffect.Props): ReactElement;
    defaultProps: {
        className: string;
    };
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
declare const name = "divi/scroll-effect";
export { name, ScrollEffect, };
