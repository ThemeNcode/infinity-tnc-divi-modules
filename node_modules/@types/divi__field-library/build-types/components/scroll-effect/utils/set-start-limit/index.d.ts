import { type Module } from '@divi/types';
/**
 * Set start limit.
 *
 * If limit:
 * - is not a numeric value, return original motionValue
 * - is lower then 0, set limit to 0
 * - is higher then start middle, set limit equal to start middle.
 *
 * @since ??
 *
 * @param {number} value Value to set.
 * @param {Module.Element.Decoration.Scroll.ItemValue} motionValue Full value to update.
 * @returns {Module.Element.Decoration.Scroll.ItemValue}
 */
export declare const setStartLimit: (value: string, motionValue: Module.Element.Decoration.Scroll.ItemValue) => Module.Element.Decoration.Scroll.ItemValue;
