import { type Module } from '@divi/types';
/**
 * Check if the motion range has a track between start middle and end middle
 * Basically the range should not be with a single middle.
 *
 * @since ??
 *
 * @param {Module.Element.Decoration.Scroll.ItemValue} value Full value to process.
 * @returns {boolean}
 */
export declare const hasTrack: (value: Module.Element.Decoration.Scroll.ItemValue) => boolean;
