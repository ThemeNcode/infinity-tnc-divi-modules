import { type Module } from '@divi/types';
/**
 * Returns empty string if the applied function over value
 * equals to applied function over default value,
 * otherwise return applied function over value.
 *
 * @since ??
 *
 * @param {Function} fn Function to apply.
 * @param {string} defaultValue Default value.
 * @param {string} value Current value.
 * @returns {string|number}
 */
export declare const emptyOnDefault: (fn: (value: Module.Element.Decoration.Scroll.ItemValue) => string | number, defaultValue: Module.Element.Decoration.Scroll.ItemValue, value: Module.Element.Decoration.Scroll.ItemValue) => string | number;
