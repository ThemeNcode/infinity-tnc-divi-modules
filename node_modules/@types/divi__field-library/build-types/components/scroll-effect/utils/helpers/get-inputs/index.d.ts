import { type Module } from '@divi/types';
/**
 * Returns motion start input offset
 * Normally the offset is equal to start limit,
 * but it needs to prevent overlapping when inputs are too close.
 *
 * @since ??
 *
 * @param {Module.Element.Decoration.Scroll.ItemValue} value Value to process.
 * @param {number} width Whole number.
 * @returns {number[]}
 */
export declare const getInputs: (value: Module.Element.Decoration.Scroll.ItemValue, width: number) => number[][];
