import { type Module } from '@divi/types';
/**
 * Check if the range has a single middle and it the end bound limit.
 *
 * @since ??
 *
 * @param {Module.Element.Decoration.Scroll.ItemValue} value Full value to process.
 * @returns {boolean}
 */
export declare const isMaxBound: (value: Module.Element.Decoration.Scroll.ItemValue) => boolean;
