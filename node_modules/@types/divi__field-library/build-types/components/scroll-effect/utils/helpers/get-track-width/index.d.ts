import { type Module } from '@divi/types';
/**
 * Returns the range track length in %
 * The track length is the difference between start middle and end middle.
 *
 * @since ??
 *
 * @param {Module.Element.Decoration.Scroll.ItemValue} value Full value to process.
 * @returns {number}
 */
export declare const getTrackWidth: (value: Module.Element.Decoration.Scroll.ItemValue) => number;
