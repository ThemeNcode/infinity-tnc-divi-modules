import { type Module } from '@divi/types';
/**
 * Returns range counters list grouped if the distance between them is lower then 35px.
 *
 * @since ??
 *
 * @param {Module.Element.Decoration.Scroll.ItemValue} value Value to process.
 * @param {number} width Whole number.
 * @returns {number[]}
 */
export declare const getCounters: (value: Module.Element.Decoration.Scroll.ItemValue, width: number) => number[][];
