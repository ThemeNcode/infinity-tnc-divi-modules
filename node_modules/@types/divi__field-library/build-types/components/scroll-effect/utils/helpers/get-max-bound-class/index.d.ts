import { type Module } from '@divi/types';
/**
 * Check if the range is at max bound and returns the proper css class name,
 * otherwise returns empty string.
 *
 * @since ??
 *
 * @param {Module.Element.Decoration.Scroll.ItemValue} value Full value to process.
 * @returns {string}
 */
export declare const getMaxBoundClass: (value: Module.Element.Decoration.Scroll.ItemValue) => string;
