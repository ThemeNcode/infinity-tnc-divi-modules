import { type Module } from '@divi/types';
/**
 * Returns the start limit title: `34%`
 * If the start limit value equal to zero, return empty string.
 *
 * @since ??
 *
 * @param {Module.Element.Decoration.Scroll.ItemValue} value Full value to process.
 * @returns {string}
 */
export declare const getStartLimitTitle: (value: Module.Element.Decoration.Scroll.ItemValue) => string;
