import { type Module } from '@divi/types';
/**
 * Check if the range is at min bound and returns the proper css class name,
 * otherwise returns empty string.
 *
 * @since ??
 *
 * @param {Module.Element.Decoration.Scroll.ItemValue} value Full value to process.
 * @returns {string}
 */
export declare const getMinBoundClass: (value: Module.Element.Decoration.Scroll.ItemValue) => string;
