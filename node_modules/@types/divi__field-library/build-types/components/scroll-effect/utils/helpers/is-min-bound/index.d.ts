import { type Module } from '@divi/types';
/**
 * Check if the range has a single middle and it the start bound limit.
 *
 * @since ??
 *
 * @param {Module.Element.Decoration.Scroll.ItemValue} value Full value to process.
 * @returns {boolean}
 */
export declare const isMinBound: (value: Module.Element.Decoration.Scroll.ItemValue) => boolean;
