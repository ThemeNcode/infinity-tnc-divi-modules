/**
 * Check whether given value can be printed or not (string).
 * Originally a simpler way to check against empty string.
 * But later several checks were added as well to avoid unnecessary repetition.
 *
 * A value considered empty if:
 * - is an empty string
 * - is undefined
 * - is false.
 *
 * @since ??
 *
 * @param {string|number|boolean} value Value to check.
 * @returns {boolean} True if value is not empty.
 */
export declare const hasValue: (value: (string | number | boolean)) => boolean;
/**
 * Return given value or its default if current value returns false against hasValue()
 * Mainly used to return default if given value is empty string.
 *
 * @since ??
 *
 * @param {string|number} value Desired value.
 * @param {string} defaultValue Value that will be return if value fails hasValue().
 *
 * @returns {string|number} Value|default.
 */
export declare const get: (value: (string | number), defaultValue: string) => (string | number);
/**
 * Test if provided string is a valid JSON.
 *
 * @since ??
 *
 * @param {string} string String to check.
 * @returns {boolean} True if string is a valid JSON.
 *
 * @example
 * ```ts
 * isJson('{"foo": "bar"}'); // true
 *
 * isJson(''); // false
 *
 * isJson('foo'); // false
 * ```
 */
export declare const isJson: (string: string) => boolean;
/**
 * Test if provided string is a valid HTML string.
 *
 * @since ??
 *
 * @param {string} html HTML string to check.
 * @returns {boolean} True if string is a valid HTML.
 */
export declare const isValidHtml: (html: string) => boolean;
/**
 * Check if parameter value equals to `on`.
 *
 * @since ??
 *
 * @param {string} value Value to check.
 * @returns {boolean} True if value equals to `on`.
 *
 * @example
 * ```ts
 * isOn('on'); // true
 *
 * isOn('off'); // false
 * ```
 */
export declare const isOn: (value: string) => boolean;
/**
 * Check if parameter value equals to `off`.
 *
 * @since ??
 *
 * @param {string} value Value to check.
 * @returns {boolean} True if value equals to `off`.
 *
 * @example
 * ```ts
 * isOff('off'); // true
 *
 * isOff('on'); // false
 * ```
 */
export declare const isOff: (value: string) => boolean;
/**
 * Check if parameter value equals to `on` or `off`.
 *
 * @since ??
 *
 * @param {string} value Value to check.
 * @returns {boolean} True if value equals to `on` or `off`.
 *
 * @example
 * ```ts
 * isOnOff('on'); // true
 *
 * isOnOff('off'); // true
 *
 * isOnOff('foo'); // false
 * ```
 */
export declare const isOnOff: (value: string) => boolean;
/**
 * Converts the given value to "on/off" notation.
 *
 * @since ??
 *
 * @param {boolean} value Value to convert.
 *
 * @returns {string} The boolish converted to "on" or "off".
 *
 * @example
 * ```ts
 * toOnOff(true); // 'on'
 *
 * toOnOff(false); // 'off'
 * ```
 */
export declare const toOnOff: (value: boolean) => string;
/**
 * Check if parameter value equals to `yes`.
 *
 * @since ??
 *
 * @param {string} value Value to check.
 * @returns {boolean} True if value equals to `yes`.
 *
 * @example
 * ```ts
 * isYes('yes'); // true
 *
 * isYes('no'); // false
 *
 * isYes('foo'); // false
 * ```
 */
export declare const isYes: (value: string) => boolean;
/**
 * Check if parameter value equals to `no`.
 *
 * @since ??
 *
 * @param {string} value Value to check.
 * @returns {boolean} True if value equals to `no`.
 *
 * @example
 * ```ts
 * isNo('no'); // true
 *
 * isNo('yes'); // false
 *
 * isNo('foo'); // false
 * ```
 */
export declare const isNo: (value: string) => boolean;
/**
 * Check if parameter value equals to `default`.
 *
 * @since ??
 *
 * @param {string} value Value to check.
 * @returns {boolean} True if value equals to `default`.
 *
 * @example
 * ```ts
 * isDefault('default'); // true
 *
 * isDefault('foo'); // false
 * ```
 */
export declare const isDefault: (value: string) => boolean;
/**
 * Check if provided url or path point to a file with specific extension.
 *
 * @since ??
 *
 * @param {string} url URL to check.
 * @param {string} extension Extension to look for.
 * @returns {boolean} True if url or path point to a file with specific extension.
 *
 * @example
 * ```ts
 * isFileExtension('https://example.com/image.jpg', 'jpg'); // true
 *
 * isFileExtension('https://example.com/image.jpg', 'png'); // false
 *
 * isFileExtension('https://example.com/image.jpg?foo=bar', 'jpg'); // true
 *
 * isFileExtension('https://example.com/image.jpg?foo=bar', 'png'); // false
 * ```
 */
export declare const isFileExtension: (url: string, extension: string) => boolean;
/**
 * Generates CSS input placeholders styles for provided selectors.
 *
 * @since ??
 *
 * @param {Array} selectors Array of CSS selectors.
 * @param {string} declaration CSS declaration.
 * @returns {Array} Array of objects with selector and declaration.
 */
export declare const generatePlaceholderCss: (selectors: string[], declaration: string) => Record<string, string>[];
/**
 * Replace string entities to their character representation.
 *
 * @since ??
 *
 * @param {string} content String to process.
 * @returns {string} String with replaced html entities.
 *
 * @example
 * ```ts
 * replaceCodeContentEntities('&#039;'); // '
 *
 * replaceCodeContentEntities('&#091;'); // [
 *
 * replaceCodeContentEntities('&#093;'); // ]
 *
 * replaceCodeContentEntities('&#215;'); // x
 *
 * replaceCodeContentEntities('&#039;&#091;&#093;&#215;'); // [x]
 *
 * replaceCodeContentEntities('&#039;&#091;&#093;&#215;&#039;&#091;&#093;&#215;'); // [x][x]
 * ```
 */
export declare const replaceCodeContentEntities: (content: string) => string;
/**
 * Check if the string is a numeric value
 * Numeric values are strings that contain a numeric value or starts with a numeric value.
 *
 * @since ??
 *
 * @param {string} value Value to check.
 * @returns {boolean} True if the string is a numeric value.
 *
 * @example
 * ```ts
 * hasNumericValue('1'); // true
 *
 * hasNumericValue('1px'); // true
 *
 * hasNumericValue('px1'); // false
 *
 * hasNumericValue('px'); // false
 *
 * hasNumericValue(''); // false
 *
 * hasNumericValue('foo'); // false
 *
 * hasNumericValue('foo1'); // false
 * ```
 */
export declare const hasNumericValue: (value: string) => boolean;
/**
 * Replace `”` and `″` fancy quote characters with empty string.
 * That is similar to the actual builder output's counterpart.
 *
 * @since ??
 *
 * @param {string} string Value to process.
 * @returns {string} String with replaced fancy quotes.
 *
 * @example
 * ```ts
 * removeFancyQuotes('”'); // ''
 *
 * removeFancyQuotes('″'); // ''
 *
 * removeFancyQuotes('”″'); // ''
 *
 * removeFancyQuotes('foo'); // 'foo'
 *
 * removeFancyQuotes('foo”'); // 'foo'
 * ```
 */
export declare const removeFancyQuotes: (string: string) => string;
/**
 * Returns allowed corners names list.
 *
 * @since ??
 *
 * @returns {string[]} Array of allowed corners names.
 */
export declare const getCorners: () => string[];
/**
 * Return specific corner name by index.
 *
 * @since ??
 *
 * @param {number} index Corner index.
 * @returns {string} Corner name.
 *
 * @example
 * ```ts
 * getCorner(0); // 'top'
 *
 * getCorner(1); // 'right'
 *
 * getCorner(2); // 'bottom'
 *
 * getCorner(3); // 'left'
 * ```
 */
export declare const getCorner: (index: number) => string;
/**
 * Returns spacing value by provided corner.
 *
 * @since ??
 *
 * @param {string} spacing Full value.
 * @param {string} corner Corner to get value for.
 * @param {string} defaultValue Fallback value.
 * @returns {string} Spacing value.
 */
export declare const getSpacing: (spacing: string, corner: string, defaultValue?: string) => string;
/**
 * Converts a value to string by checking if the value is a valid value using `hasValue`
 * If value doesn't pass the check, return empty string.
 *
 * @since ??
 *
 * @param {string|number|boolean} value Value to convert.
 * @returns {string} Converted value.
 *
 * @example
 * ```ts
 * toString('foo'); // 'foo'
 *
 * toString(1); // '1'
 *
 * toString(true); // 'true'
 *
 * toString(false); // 'false'
 *
 * toString(null); // ''
 *
 * toString(undefined); // ''
 * ```
 */
export declare const toString: (value: (string | number | boolean)) => string;
/**
 * Return the object property value, in case the value does not satisfy empty validation,
 * return the defaultValue.
 *
 * @since ??
 *
 * @param {string} defaultValue Value to return if nothing found.
 * @param {string} propName Property name.
 * @param {Record<string, string|number>} object Full properties collection.
 * @returns {*} Property value or default value.
 */
export declare const prop: (defaultValue: string, propName: string, object: Record<string, string | number>) => (string | number | boolean);
/**
 * Sets object property with provided value.
 *
 * @since ??
 *
 * @param {string} propName Property name to update.
 * @param {string|number|boolean} value Value to set.
 * @param {Record<string, string|number>} object Full properties collection.
 * @returns {object} Updated object.
 */
export declare const set: (propName: string, value: (string | number | boolean), object: Record<string, string | number | boolean>) => Record<string, string | number | boolean>;
/**
 * Check if current userAgent is mobile device.
 *
 * @since ??
 *
 * @returns {boolean} True if current userAgent is mobile device.
 *
 * @example
 * ```ts
 * isRealMobileDevice(); // true
 *
 * isRealMobileDevice(); // false
 * ```
 */
export declare function isRealMobileDevice(): boolean;
/**
 * Get percentage of given value.
 *
 * @since ??
 *
 * @param {number} value Value to get percentage from.
 * @param {string} percentage Percent to get from the value.
 *
 * @returns {number} Percentage of the value.
 */
export declare const getPercentage: (value: number, percentage: string) => number;
