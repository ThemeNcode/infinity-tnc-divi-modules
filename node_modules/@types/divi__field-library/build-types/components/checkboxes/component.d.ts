import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import './style.scss';
/**
 * Checkboxes field component.
 *
 * Use this component to render a list of checkboxes.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.name Field name.
 * @param {string} props.className Field class name.
 * @param {string[]} props.defaultValue Default value.
 * @param {Function} props.onChange On change callback.
 * @param {string[]} props.value Current value.
 * @param {object[]} props.options List of options.
 *
 * @returns {ReactElement} Checkboxes component.
 */
declare const Checkboxes: {
    ({ name, className, defaultValue, onChange, value, options, }: FieldLibrary.Checkboxes.Props): ReactElement;
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
declare const name = "divi/checkboxes";
export { name, Checkboxes, };
