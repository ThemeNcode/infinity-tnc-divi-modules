import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import './style.scss';
/**
 * Text field component.
 *
 * This component is used to render text field in the Visual Builder.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.name Name.
 * @param {string} [props.className] Class name.
 * @param {Function} props.onChange On change callback.
 * @param {Function} [props.onKeyDown] On key down callback.
 * @param {string} props.value Value.
 * @param {string} [props.defaultValue] Default value.
 * @param {boolean} [props.autofocus] Autofocus.
 * @param {boolean} [props.readonly] Read only.
 * @param {boolean} [props.showPlaceholderOnEmpty] Show placeholder on empty.
 * @param {Function} [props.renderBefore] Render before.
 * @param {Function} [props.renderAfter] Render after.
 *
 * @returns {ReactElement} Text field component.
 */
declare const Text: {
    (props: FieldLibrary.Text.Props): ReactElement;
    defaultProps: {
        className: string;
        onkeydown: (...args: any[]) => void;
        defaultValue: any;
        autofocus: boolean;
        readonly: boolean;
    };
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
declare const name = "divi/text";
export { name, Text, };
