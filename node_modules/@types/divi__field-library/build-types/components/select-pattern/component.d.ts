import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import './style.scss';
/**
 * SelectPattern field component.
 *
 * This component is used to render the SelectPattern field.
 *
 * @since ??
 *
 * @param {object} props Props for Select pattern field.
 * @param {string} props.name Name of the field.
 * @param {Function} props.onChange Callback function to handle change event.
 * @param {Function} props.onReset Callback function to handle reset event.
 * @param {string} props.value Value of the field.
 * @param {string} props.addTitle Title for add button on preview area.
 * @param {object} props.backgroundAttr Background attribute object.
 * @param {string} props.className Classname for the field.
 * @param {string} props.defaultValue Default value for the field.
 * @param {boolean} props.hasManager Whether to show manager or not.
 * @param {boolean} props.hasPreview Whether to show preview or not.
 *
 * @returns {ReactElement} SelectPattern component.
 */
declare const SelectPattern: {
    ({ name, onChange, onReset, value, addTitle, backgroundAttr, className, defaultValue, hasManager, hasPreview, }: FieldLibrary.SelectPattern.Props): ReactElement;
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
declare const name = "divi/select-pattern";
export { name, SelectPattern, };
