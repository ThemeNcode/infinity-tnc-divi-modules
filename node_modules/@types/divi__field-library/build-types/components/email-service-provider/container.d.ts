import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import { EmailServiceProvider } from './component';
/**
 * Container component for EmailServiceProvider component.
 *
 * @since ??
 *
 * @param {FieldLibrary.EmailServiceProvider.Props} props Component props.
 *
 * @returns {ReactElement} EmailServiceProvider component.
 */
declare const EmailServiceProviderContainer: {
    (props: FieldLibrary.EmailServiceProvider.Props): ReactElement;
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
export { EmailServiceProviderContainer, };
