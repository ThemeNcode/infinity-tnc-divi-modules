import { type EmailMarketing, type FieldLibrary } from '@divi/types';
/**
 * The function `populateOptions` takes in a record of email services and returns an object with
 * options for a select field, using the email service labels as the option labels.
 *
 * @since ??
 *
 * @param {Record<string, EmailMarketing.Service.Provider>} emailServices The email services.
 *
 * @returns {FieldLibrary.Select.Options} Returns an object of options for a select field.
 */
export declare const populateOptions: (emailServices: Record<string, EmailMarketing.Service.Provider>) => FieldLibrary.Select.Options;
