import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
/**
 * Renders a component for managing email service providers.
 *
 * @since ??
 *
 * @param {FieldLibrary.EmailServiceProvider.Props} props The component props.
 *
 * @returns {ReactElement} The rendered component.
 */
declare const EmailServiceProvider: {
    ({ name, value, defaultValue, onChange, providers, }: FieldLibrary.EmailServiceProvider.Props): ReactElement;
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
declare const name = "divi/email-service-provider";
export { name, EmailServiceProvider, };
