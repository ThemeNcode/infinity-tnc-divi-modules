import { type FieldLibrary } from '@divi/types';
/**
 * Regex pattern mapping that used to determine to show the autocompletion panel.
 *
 * @since ??
 */
export declare const completionRegex: Partial<Record<FieldLibrary.Code.Props['mode'], RegExp>>;
