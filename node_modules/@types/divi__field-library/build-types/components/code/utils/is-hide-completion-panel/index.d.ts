import type CodeMirror from 'codemirror';
import { type FieldLibrary } from '@divi/types';
/**
 * Determines whether to hide auto completion panel.
 *
 * @since ??
 *
 * @param {CodeMirror.Editor} instance Codemirror instance.
 * @param {KeyboardEvent} event Keyboard event data.
 * @param {string} mode Editor mode configuration.
 *
 * @returns {boolean} True if show completion modal.
 */
export declare const isHideCompletionPanel: (instance: CodeMirror.Editor, event: KeyboardEvent, mode: FieldLibrary.Code.Props['mode']) => boolean;
