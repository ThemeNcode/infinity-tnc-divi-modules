import { type FieldLibrary } from '@divi/types';
/**
 * Replaces newline placeholders with newlines.
 *
 * @since ??
 *
 * @param {string} value Content.
 *
 * @returns {string}
 */
export declare const normalizeValue: (value: string, mode: FieldLibrary.Code.Props['mode']) => string;
