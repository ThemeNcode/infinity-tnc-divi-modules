import type CodeMirror from 'codemirror';
import { type FieldLibrary } from '@divi/types';
declare module 'codemirror' {
    interface EditorState {
        completionActive?: CodeMirror.CompletionHandle;
    }
    interface ModeSpecOptions {
        /** Below options are supported in CSS mode. */
        /**
         * Only available when mode is `css`. When enabled will cause the editor in inline CSS mode.
         * So the user doesn't need to input the CSS selector.
         */
        inline?: boolean;
    }
    interface ColorpickerConfiguration {
        mode: 'edit' | 'view';
        hideColorsets?: boolean;
        container?: HTMLElement;
    }
    interface EditorConfiguration {
        colorpicker?: ColorpickerConfiguration;
        lineBreakPlaceholder?: RegExp;
    }
}
/**
 * @typedef {object} DefaultProps
 * @property {string} [mode] Mode.
 * @property {Function} [onChange] On change callback.
 * @property {string} [inlineCssSelector] Inline CSS selector.
 */
export type DefaultProps = Pick<FieldLibrary.Code.Props, 'mode' | 'onChange' | 'inlineCssSelector'>;
