import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import { type DefaultProps } from './types';
import './style.scss';
import 'codemirror/addon/dialog/dialog';
import 'codemirror/addon/display/placeholder';
import 'codemirror/addon/edit/closebrackets';
import 'codemirror/addon/edit/closetag';
import 'codemirror/addon/edit/matchbrackets';
import 'codemirror/addon/edit/matchtags';
import 'codemirror/addon/hint/css-hint';
import 'codemirror/addon/hint/html-hint';
import 'codemirror/addon/hint/show-hint';
import 'codemirror/addon/search/jump-to-line';
import 'codemirror/addon/search/searchcursor';
import 'codemirror/addon/search/search';
import 'codemirror/addon/selection/active-line';
import 'codemirror/keymap/sublime';
import 'codemirror/mode/css/css';
import 'codemirror/mode/htmlmixed/htmlmixed';
import 'codemirror-colorpicker';
/**
 * Code field component.
 *
 * Use this component to render Code field.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.className Field class name.
 * @param {string} props.defaultValue Field default value.
 * @param {string} props.inlineCssSelector Inline CSS selector.
 * @param {string} props.mode Editor mode.
 * @param {string} props.name Field name.
 * @param {boolean} props.readonly Whether field is readonly.
 * @param {string} props.value Field value.
 * @param {Function} props.onChange Field change callback.
 *
 * @returns {ReactElement} Code field component.
 */
declare const Code: {
    ({ className, defaultValue, inlineCssSelector, mode, name, readonly, value, onChange, }: FieldLibrary.Code.Props): ReactElement;
    defaultProps: DefaultProps;
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
declare const name = "divi/code";
export { name, Code, };
