import { type Annotation } from 'codemirror/addon/lint/lint';
import { type LinterMode } from './types';
/**
 * Get CodeMirror linter annotations.
 *
 * @since ??
 *
 * @param {string} text Editor value.
 * @param {LinterMode} linterMode Linter mode.
 *
 * @returns {Annotation[]}
 */
export declare const getAnnotations: (text: string, linterMode: LinterMode) => Annotation[];
