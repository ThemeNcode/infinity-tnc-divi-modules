import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import './style.scss';
/**
 * Gradient field component.
 *
 * Use this component to render gradient field.
 *
 * @since ??
 *
 * @param {object} props Props for gradient field.
 * @param {object} props.backgroundAttrs Background attributes.
 * @param {string} props.defaultValue Default value of the field.
 * @param {Function} props.onChange Callback function when value changes.
 * @param {string} props.length Length of the field.
 * @param {string} props.value Value of the field.
 * @param {boolean} props.readonly Whether field is readonly or not.
 * @param {Function} props.setRightClickOptions Callback function to set right click options.
 *
 * @returns {ReactElement} Gradient field component.
 */
declare const Gradient: {
    ({ backgroundAttrs, defaultValue, onChange, length, value, readonly, setRightClickOptions, }: FieldLibrary.Gradient.Props): ReactElement;
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
declare const name = "divi/gradient";
export { name, Gradient, };
