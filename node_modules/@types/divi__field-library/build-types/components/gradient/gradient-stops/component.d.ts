import { type ReactElement } from 'react';
import { type GradientStopsProps } from './types';
import './style.scss';
/**
 * Component that cointains all stops and slider from a gradient.
 *
 * @param {GradientStopsProps} props Gradient Stops Props.
 * @returns {ReactElement} The GradientStops component.
 */
declare const GradientStops: (props: GradientStopsProps) => ReactElement;
export { GradientStops, };
