import { type ReactElement } from 'react';
import { type StopThumbProps } from '../types';
import './style.scss';
/**
 * Thumb for gradient stops slider.
 *
 * @since ??
 *
 * @param {StopThumbProps} props Stop Data.
 *
 * @returns {ReactElement} Single grandient thumb.
 */
export declare const StopThumb: (props: StopThumbProps) => ReactElement;
