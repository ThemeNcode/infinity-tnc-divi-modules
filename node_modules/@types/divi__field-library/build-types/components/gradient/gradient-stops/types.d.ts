import { type CSSProperties } from 'react';
import { type sizingUnits } from '@types/divi__style-library';
import { type FieldLibrary, type Module } from '@divi/types';
export type ParsedStop = {
    color: Module.Element.Decoration.Background.GradientStop['color'];
    index: number;
    position: number;
    unit: typeof sizingUnits[number];
    uuid: string | null;
};
export type GradientStopsProps = {
    activeStop: ParsedStop;
    handleStopsChange: (stops: ParsedStop[], newActiveShop?: ParsedStop | null) => void;
    handleActiveStopChange: (stop: ParsedStop) => void;
    length: Module.Element.Decoration.Background.AttributeValue['gradient']['length'];
    parsedStops: ParsedStop[];
    readonly: boolean;
    setActiveStop: React.Dispatch<React.SetStateAction<ParsedStop>>;
    setShowColorPicker: React.Dispatch<React.SetStateAction<boolean>>;
    setRightClickOptions?: FieldLibrary.Gradient.Props['setRightClickOptions'];
    unit?: typeof sizingUnits[number];
};
export type StopThumbProps = {
    parsedStop: ParsedStop;
    className?: string;
    style?: CSSProperties;
    newThumb?: boolean;
    unit: typeof sizingUnits[number];
};
export type StopsRailProps = {
    addStop: (stop: ParsedStop) => void;
    className: string;
    parsedStops: ParsedStop[];
    unit: typeof sizingUnits[number];
    readonly: boolean;
    min: number;
    max: number;
    step: number;
    snapDistance: number;
};
