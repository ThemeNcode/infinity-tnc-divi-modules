import { type ReactElement } from 'react';
import { type StopsRailProps } from '../types';
/**
 * Rail where gradient stop thumbs slide.
 *
 * @since ??
 *
 * @param {StopsRailProps} props Stop RAils component props.
 *
 * @returns {ReactElement} StopsRail Component.
 */
export declare const StopsRail: (props: StopsRailProps) => ReactElement;
