import { type ParsedStop } from '../../gradient-stops/types';
/**
 * Reverse the order of gradient stops.
 *
 * @param {ParsedStop[]} parsedGradient Array of parsed gradient stop objects.
 * @param {number} gradientLength The length (that is, max value) of the gradient.
 *
 * @returns {ParsedStop[]} Array of GradientStop objects.
 *
 * @since ??
 */
export declare const reverseGradientStops: (parsedGradient: ParsedStop[], gradientLength: number) => ParsedStop[];
