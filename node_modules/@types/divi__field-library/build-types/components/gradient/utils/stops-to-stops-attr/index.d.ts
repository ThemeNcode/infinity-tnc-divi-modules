import { type Module } from '@divi/types';
import { type ParsedStop } from '../../gradient-stops/types';
/**
 * Transforms parsed stops into stop attribute format.
 *
 * @since ??
 *
 * @param {ParsedStop[]} stops The parsed stops.
 *
 * @returns {Module.Element.Decoration.Background.AttributeValue.gradient.stops} Stops transformed.
 */
export declare const stopsToStopsAttr: (stops: ParsedStop[]) => Module.Element.Decoration.Background.AttributeValue['gradient']['stops'];
