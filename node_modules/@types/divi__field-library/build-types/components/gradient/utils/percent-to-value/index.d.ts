/**
 * Calculates a value in min <-> max range based on the given percent.
 *
 * @since ??
 *
 * @param {number} min The min range value.
 * @param {number} max The max range value.
 * @param {number} percent The percent.
 *
 * @returns {number} The percent value.
 */
export declare const percentToValue: (min: number, max: number, percent: number) => number;
