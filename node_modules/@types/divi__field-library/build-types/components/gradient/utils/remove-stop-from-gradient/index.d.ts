import { type ParsedStop } from '../../gradient-stops/types';
/**
 * Remove stop from gradient at specified position.
 *
 * @param {number} stopIndex Index of the selected gradient stop to be removed.
 * @param {Array} gradient Array of parsed gradient stopID objects.
 *
 * @returns {Array} Gradient with stop removed (or false if params missing).
 *
 * @since 4.16.0 Added.
 */
export declare const removeStopFromGradient: (stopIndex: number, gradient: ParsedStop[]) => ParsedStop[] | null;
