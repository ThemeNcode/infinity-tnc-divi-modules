import { type sizingUnits } from '@types/divi__style-library';
import { type ParsedStop } from '../../gradient-stops/types';
/**
 * Update stop (primarily used to update color) within gradient array at the specified position.
 *
 * @param {*} activeStop The selected gradient stop to be updated.
 * @param {string} [activeStop.color] The selected gradient stop to be updated.
 * @param {number} [activeStop.position] The selected gradient stop to be updated.
 * @param {number} [activeStop.index] The selected gradient stop to be updated.
 * @param {string} [activeStop.uuid] The unique identifier of the gradient stop to be updated.
 * @param {Array} parsedStops Array of parsed gradient stop objects.
 * @param {typeof sizingUnits[]} unit Selected unit of measurement for gradient stops.
 *
 * @returns {boolean|Array} Gradient with stop updated (or false if params missing).
 *
 * @since 4.16.0 Added.
 */
export declare const updateStopWithinGradient: (activeStop: ParsedStop, parsedStops: ParsedStop[], unit: (typeof sizingUnits)[number]) => ParsedStop[] | null;
