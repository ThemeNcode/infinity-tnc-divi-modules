/**
 * Helper method to determine the type of click event.
 *
 * @since ??
 *
 * @param {MouseEvent} event The event to examine.
 *
 * @returns {boolean} Whether click event is a primary/left click.
 */
export declare const isLeftClick: (event: MouseEvent) => boolean;
