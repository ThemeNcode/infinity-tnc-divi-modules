import { type NearestColorStops } from './types';
/**
 * Finds the nearest color stop positions from the both sides of the position.
 *
 * @param {number} position The position to search from.
 * @param {ParsedStop[]} stops The array of current stops.
 *
 * @returns {{low: ParsedStop, high: ParsedStop}} Object containing the low and high nearest positions.
 */
export declare const findNearestColorStops: NearestColorStops;
