/**
 * Rounds the given value according to the given slider step.
 *
 * @since ??
 *
 * @param {number} value The value to be rounded.
 * @param {number} step Slider step (only integer step supported).
 * @param {number} min The slider min range value.
 *
 * @returns {number} The rounded value.
 */
export declare const roundValueToStep: (value: number, step: number, min: number) => number;
