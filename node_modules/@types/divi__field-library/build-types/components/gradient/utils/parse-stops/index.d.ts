import { type sizingUnits } from '@types/divi__style-library';
import { type Module } from '@divi/types';
import { type ParsedStop } from '../../gradient-stops/types';
/**
 * Add extra information to gradient stops.
 *
 * @since ??
 *
 * @param {Module.Element.Decoration.Background.AttributeValue.gradient.stops} stops Gradient stops attribute value.
 * @param {typeof sizingUnits[]} unit Gradient unit attribute value.
 * @param {number} length Defined length is also the maximum allowed position for the gradient.
 *
 * @returns {ParsedStop[]} Parsed gradient stops.
 */
export declare const parseStops: (stops: Module.Element.Decoration.Background.GradientStop[], unit: (typeof sizingUnits)[number], length: number) => ParsedStop[];
