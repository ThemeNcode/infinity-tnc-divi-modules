/**
 * Helps to find index of the nearest value in the range values array.
 *
 * @since ??
 *
 * @param {number []} values The range values array to search nearest index.
 * @param {number} newValue The new value.
 * @param {number} indexToSkip The range array values index to skip.
 *
 * @returns {{index: number, distance: number}} Index of the array and distance to nearest value.
 */
export declare const findNearestIndex: (values: number[], newValue: number, indexToSkip?: number) => {
    index: number;
    distance: number;
};
