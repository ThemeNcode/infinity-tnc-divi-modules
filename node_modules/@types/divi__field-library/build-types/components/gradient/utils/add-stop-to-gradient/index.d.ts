import { type ParsedStop } from '../../gradient-stops/types';
/**
 * Add stop to gradient at specified position.
 *
 * @param {*} stop The gradient stop object to be added.
 * @param {Array} gradient Array of parsed gradient stop objects.
 * @param {number} index Index of gradient stop at which to insert the new stop.
 *
 * @returns {boolean|Array} Gradient with stop removed (or false if params missing).
 *
 * @since 4.16.0 Added.
 */
export declare const addStopToGradient: (stop: ParsedStop, gradient: ParsedStop[], index: number) => ParsedStop[] | null;
