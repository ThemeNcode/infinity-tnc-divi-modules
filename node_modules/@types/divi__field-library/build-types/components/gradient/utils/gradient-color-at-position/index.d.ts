import { type ParsedStop } from '../../gradient-stops/types';
/**
 * Calculates interpolated color between two gradient stops at given position.
 *
 * @param {*} start The first GradientStop object.
 * @param {string} start.color The first gradient stop color (either in HEX or RGB(A) format).
 * @param {number} start.position The first gradient stop location.
 * @param {*} end The second GradientStop object.
 * @param {string} end.color The second gradient stop color (either in HEX or RGB(A) format).
 * @param {number} end.position The second gradient stop location.
 * @param {number} cursorPosition The cursor position.
 *
 * @returns {string} The result color in RGBA color.
 *
 * @since 4.16.0 Added.
 */
export declare const gradientColorAtPosition: (start: ParsedStop, end: ParsedStop, cursorPosition: number) => string;
