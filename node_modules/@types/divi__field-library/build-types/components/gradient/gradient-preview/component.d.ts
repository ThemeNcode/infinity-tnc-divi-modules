import { type ReactElement } from 'react';
import { type GradientPreviewProps } from './types';
/**
 * Shows color picker or gradient preview.
 *
 * @since ??
 *
 * @param {GradientPreviewProps} props Needed props to render color picker and preview components.
 *
 * @returns {ReactElement} One or the other toggled components.
 */
export declare const GradientPreview: (props: GradientPreviewProps) => ReactElement;
