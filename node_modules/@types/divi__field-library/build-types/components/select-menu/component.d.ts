import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
/**
 * Select Menu field component.
 *
 * Used to render Select Menu field in the Visual Builder.
 *
 * @since ??
 *
 * @param {object} props Props for Select Menu field.
 * @param {string} [props.defaultValue] Default value.
 * @param {object} [props.value] Current value.
 * @param {string} props.name Field name.
 * @param {Function} props.onChange Callback function to handle changes.
 * @param {Array} [props.options] Options.
 * @param {Function} [props.registerShortcuts] Register shortcuts.
 * @param {Function} [props.unregisterShortcuts] Unregister shortcuts.
 *
 * @returns {ReactElement} Select Menu field component.
 */
declare const SelectMenu: {
    ({ defaultValue, value, name, onChange, options, registerShortcuts, unregisterShortcuts, }: FieldLibrary.SelectMenu.Props): ReactElement;
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
declare const name = "divi/select-menu";
export { name, SelectMenu, };
