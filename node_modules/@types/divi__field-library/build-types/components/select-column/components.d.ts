import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import './style.scss';
/**
 * SelectColumn field component.
 *
 * This component is used to render SelectColumn field inside the settings modal.
 *
 * @since ??
 *
 * @param {object} props Props for Select column field.
 * @param {Array} props.columns List of columns.
 * @param {string} props.className Classname for Select column field.
 * @param {string} props.defaultValue Default value for Select column field.
 * @param {string} props.value Value for Select column field.
 * @param {string} props.name Name for Select column field.
 * @param {Function} props.onChange On change callback for Select column field.
 *
 * @returns {ReactElement} SelectColumn field component.
 */
declare const SelectColumn: {
    ({ columns, className, defaultValue, value, name, onChange, }: FieldLibrary.SelectColumn.Props): ReactElement;
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
declare const name = "divi/select-column";
export { name, SelectColumn, };
