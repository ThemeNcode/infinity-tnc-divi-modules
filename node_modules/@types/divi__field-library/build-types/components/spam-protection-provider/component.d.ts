import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
/**
 * Renders a component for managing spam protection providers.
 *
 * @since ??
 *
 * @param {FieldLibrary.SpamProtectionProvider.Props} props The component props.
 *
 * @returns {ReactElement} The rendered component.
 */
declare const SpamProtectionProvider: {
    ({ name, value, defaultValue, onChange, providers, }: FieldLibrary.SpamProtectionProvider.Props): ReactElement;
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
declare const name = "divi/spam-protection-provider";
export { name, SpamProtectionProvider, };
