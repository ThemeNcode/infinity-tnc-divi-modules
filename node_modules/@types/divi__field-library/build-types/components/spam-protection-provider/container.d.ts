import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import { SpamProtectionProvider } from './component';
/**
 * Container component for SpamProtectionProvider component.
 *
 * @since ??
 *
 * @param {FieldLibrary.SpamProtectionProvider.Props} props Component props.
 *
 * @returns {ReactElement} SpamProtectionProvider component.
 */
declare const SpamProtectionProviderContainer: {
    (props: FieldLibrary.SpamProtectionProvider.Props): ReactElement;
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
export { SpamProtectionProviderContainer, };
