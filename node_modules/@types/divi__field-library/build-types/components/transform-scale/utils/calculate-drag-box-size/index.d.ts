import { type FieldLibrary } from '@divi/types';
/**
 * Calculate darg box size.
 *
 * @since ??
 *
 * @param  {number} x X axis move distance.
 * @param  {number} y Y axis move distance.
 * @param  {boolean} flipDragBox Wether the drag flip box enabled or not.
 *
 * @returns {object} Drag box size.
 */
export declare const calculateDragBoxSize: (x: number, y: number, flipDragBox?: boolean) => FieldLibrary.TransformScale.DragBoxSize;
