import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import './style.scss';
/**
 * Map field component.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.name Field name.
 * @param {string} props.className Field class name.
 * @param {Function} props.onChange Field change callback.
 * @param {object} props.value Field value.
 * @param {object} props.defaultValue Field default value.
 * @param {boolean} props.readonly Field readonly.
 * @param {boolean} props.hideUpdateButton Hide update button.
 * @param {boolean} props.hasMarker Has marker.
 * @param {boolean} props.hasAPIKey Has API key.
 * @param {string} props.imagesUri Images URI.
 * @param {Array} props.extraMarkers Extra markers.
 *
 * @returns {ReactElement} Map field component.
 */
declare const Map: {
    ({ name, className, onChange, value, defaultValue, readonly, hideUpdateButton, hasMarker, hasAPIKey, imagesUri, extraMarkers, }: FieldLibrary.MapField.Props): ReactElement;
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
declare const name = "divi/map";
export { name, Map, };
