/// <reference types="google.maps" />
/**
 * Convert string latitude and longitude to google latitude and longitude object.
 *
 * @since ??
 *
 * @param {string} address String latitude and longitude with comma separated.
 *
 * @returns {object} Latitude and longitude google object.
 */
export declare const addressToLatLng: (address: string) => google.maps.LatLng;
