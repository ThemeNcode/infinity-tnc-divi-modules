import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import './style.scss';
/**
 * SelectMask field component.
 *
 * This component is used to render the SelectMask field.
 *
 * @since ??
 *
 * @param {object} props Props for Select mask field.
 * @param {object} props.backgroundAttr Background attributes.
 * @param {string} props.name Field name.
 * @param {Function} props.onChange On change callback.
 * @param {Function} props.onReset On reset callback.
 * @param {string} props.value Field value.
 * @param {string} [props.addTitle] Add title.
 * @param {string} [props.className] Class name.
 * @param {string} [props.defaultValue] Default value.
 * @param {boolean} [props.hasManager] Has manager.
 * @param {boolean} [props.hasPreview] Has preview.
 *
 * @returns {ReactElement} SelectMask component.
 */
declare const SelectMask: {
    ({ backgroundAttr, name, onChange, onReset, value, addTitle, className, defaultValue, hasManager, hasPreview, }: FieldLibrary.SelectMask.Props): ReactElement;
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
declare const name = "divi/select-mask";
export { name, SelectMask, };
