import { type ReactElement } from 'react';
import { type Props } from './types';
/**
 * Upload preview component.
 *
 * @since ??
 *
 * @param {Props} props Component props.
 *
 * @returns {ReactElement}
 */
export declare const UploadPreview: (props: Props) => ReactElement;
