import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import './style.scss';
/**
 * Upload field component.
 *
 * Use this component to render Upload field.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.name Field name.
 * @param {string} props.value Field value.
 * @param {boolean} props.isBackgroundType Is background type.
 * @param {boolean} props.readonly Is readonly.
 * @param {boolean} props.hideMetadata Hide metadata.
 * @param {string} props.attachmentId Attachment id.
 * @param {string} props.defaultValue Field default value.
 * @param {boolean} props.showResetButton Show reset button.
 * @param {string} props.className Field class name.
 * @param {string} props.dataType Field data type.
 * @param {boolean} props.embed Embed media.
 * @param {string} props.chooseText Choose text.
 * @param {string} props.uploadButtonText Upload button text.
 * @param {object} props.backgroundAttrs Background attributes.
 * @param {Function} props.onChange Field change callback.
 * @param {Function} props.onReset Field reset callback.
 * @param {Function} props.renderPreview Render preview callback.
 * @param {Function} props.renderBefore Render before callback.
 * @param {Function} props.renderAfter Render after callback.
 *
 * @returns {ReactElement} React component.
 */
declare const Upload: {
    ({ name, value, isBackgroundType, readonly, hideMetadata, attachmentId, defaultValue, showResetButton, className, dataType, embed, chooseText, uploadButtonText, backgroundAttrs, onChange, onReset, renderPreview, renderBefore, renderAfter, }: FieldLibrary.Upload.Props): ReactElement;
    defaultProps: {
        className: string;
        dataType: string;
        embed: boolean;
        chooseText: string;
        uploadButtonText: string;
        backgroundAttrs: {};
    };
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
declare const name = "divi/upload";
export { name, Upload, };
