import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
/**
 * Color picker field component.
 *
 * Use this component to render color picker field.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.addTitle Add title.
 * @param {string} props.attrName Attribute name.
 * @param {ReactNode} props.children Children.
 * @param {string} props.cookiePath Cookie path.
 * @param {string[]} props.colorPalette Color palette.
 * @param {string[]} props.globalColorPalette Optional global color palette.
 * @param {string} props.defaultValue Default value.
 * @param {string} props.groupName Group name.
 * @param {string} props.id Field ID.
 * @param {string} props.label Field label.
 * @param {object} props.lastClipboardItem Optional. Last clipboard item.
 * @param {string} props.name Field name.
 * @param {Function} props.onChange On change callback.
 * @param {Function} props.onReset On reset callback.
 * @param {Function} props.updateDefaultColors Update default colors callback.
 * @param {string} props.value Field value.
 * @param {boolean} props.animate Animate.
 * @param {string} props.className Class name.
 * @param {boolean} props.hasPreview Has preview.
 * @param {boolean} props.hasColorManager Has color manager.
 * @param {boolean} props.isAlpha Is alpha.
 * @param {boolean} props.readonly Readonly.
 * @param {boolean} props.showAddButton Show add button.
 * @param {boolean} props.showCustomInput Show custom input.
 * @param {boolean} props.showEditButton Show edit button.
 * @param {boolean} props.showRemoveButton Show remove button.
 * @param {boolean} props.showResetButton Show reset button.
 * @param {boolean} props.showPaletteOnPickerActive Show palette on picker active.
 * @param {boolean} props.showPickerPalettes Show picker palettes.
 * @param {string} props.subName Attribute sub name.
 * @param {boolean} props.pickerOnStart Picker on start.
 * @param {Function} props.onCloseColorPicker On close color picker callback.
 * @param {Function} props.onDeleteGlobalColor On delete global color callback.
 * @param {Function} props.onAddGlobalColor On add global color callback.
 * @param {Function} props.onChangeGlobalColorValue On change global color value callback.
 * @param {Function} props.onEditGlobalColor On edit global color callback.
 * @param {Function} props.setRightClickOptions Set right click options callback.
 * @param {Function} props.getGlobalColorValue Get global color value.
 * @param {Function} props.isActiveGlobalColor Check if global color is active.
 * @param {Function} props.toggleModuleInteraction Toggle module interaction callback.
 *
 * @returns {ReactElement} Color picker field component.
 */
declare const ColorPicker: {
    ({ addTitle, attrName, children, cookiePath, colorPalette, defaultValue, globalColorPalette, groupName, id, label, name, onChange, onReset, updateDefaultColors, value, onAddGlobalColor, onChangeGlobalColorValue, onDeleteGlobalColor, onEditGlobalColor, setRightClickOptions, subName, toggleModuleInteraction, animate, className, hasPreview, hasColorManager, isAlpha, lastClipboardItem, readonly, showAddButton, showCustomInput, showEditButton, showRemoveButton, showResetButton, showPaletteOnPickerActive, showPickerPalettes, pickerOnStart, onCloseColorPicker, getGlobalColorValue, isActiveGlobalColor, }: FieldLibrary.ColorPicker.Props): ReactElement;
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
declare const name = "divi/color-picker";
export { name, ColorPicker, };
