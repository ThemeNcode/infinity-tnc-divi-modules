import { type ChangeEvent } from 'react';
import { type ColorUI } from './colorpicker/types';
declare global {
    interface JQuery {
        wpColorPicker: (config?: WpColorPickerConfig | string, value?: unknown) => void | string;
    }
}
export * from './colorpicker/types';
export interface WpColorPickerConfig {
    alpha?: boolean;
    change: (event: ChangeEvent, ui: ColorUI) => void;
    clear: () => void;
    defaultColor: string;
    diviColorpicker: boolean;
    hide: boolean;
    onClose: () => void;
    palettes: string[];
    mode?: string;
    slider?: string;
    type?: 'full' | 'hue' | null;
    value?: string;
    width?: number;
}
