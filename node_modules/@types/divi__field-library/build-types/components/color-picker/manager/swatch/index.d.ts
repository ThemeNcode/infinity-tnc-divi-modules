import { type ReactElement } from 'react';
import { type Props } from './types';
/**
 * Swatch component for color palette component.
 *
 * @since ??
 *
 * @param {Props} props Component props.
 *
 * @returns {ReactElement}
 */
export declare const ColorPickerManagerSwatch: ({ id, paletteMode, color, isEmptyColor, isEditMode, onClick, onColorSwatchContextMenu, onEdit, onDeleteGlobalColor, isGlobalColor, tooltipId, }: Props) => ReactElement;
