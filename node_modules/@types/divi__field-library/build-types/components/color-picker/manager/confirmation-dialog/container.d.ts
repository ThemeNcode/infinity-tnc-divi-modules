import { type ReactElement } from 'react';
import { type ConfirmationDialogProps } from './types';
/**
   * ConfirmationDialog container.
   *
   * This container component passes necessary store data to render confirmation dialog component.
   *
   * @since ??
   *
   * @param {object} props Component props.
   * @param {ReactElement} props.children Children to be rendered by the component instead of inner HTML.
   * @param {string} props.dangerousInnerHTML Inner HTML to be rendered by the component instead of children.
   * @param {Function} props.onConfirm On confirm callback.
   * @param {Function} props.onCancel On cancel callback.
   *
   * @returns {ReactElement}
   */
export declare const ConfirmationDialogContainer: ({ children, dangerousInnerHTML, onConfirm, onCancel, }: ConfirmationDialogProps) => ReactElement;
