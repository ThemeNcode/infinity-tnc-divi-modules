import { type ReactElement } from 'react';
import { type Props } from './type';
/**
 * Current color component for color manager component.
 *
 * @since ??
 *
 * @param {Props} props Component props.
 *
 * @returns {ReactElement}
 */
export declare const CurrentColor: ({ id, color, canExpand, onClick, onColorSwatchContextMenu, togglePalette, isCurrentColorGlobal, }: Props) => ReactElement;
