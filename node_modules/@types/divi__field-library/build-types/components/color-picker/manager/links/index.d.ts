import { type ReactElement } from 'react';
import { type Props } from './types';
/**
 * Colorpicker manager links component.
 *
 * @since ??
 *
 * @param {Props} props Component props.
 *
 * @returns {ReactElement}
 */
export declare const ColorPickerManagerLinks: ({ paletteMode, palettes, setMode, onEditMode, hasMultiLineGlobalColors, }: Props) => ReactElement;
