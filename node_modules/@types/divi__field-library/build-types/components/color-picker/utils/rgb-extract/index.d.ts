/**
 * Extracts number values from rgb or rgba color definition string.
 *
 * @since ??
 *
 * @param {string} color RGB color value.
 *
 * @returns {number[]} Array of red, blue, green and opacity values,
 * else empty array in case of incorrect color string.
 */
export declare const rgbExtract: (color: string) => number[];
