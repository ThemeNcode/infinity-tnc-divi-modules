/**
 * Check if the color is valid.
 *
 * @since ??
 *
 * @see https://dev.elegantthemes.com/docs/api-reference/visual-builder/packages/field-library/src/components/color-picker/utils/get-color-type getColorType
 *
 * @param {string} color Color value.
 *
 * @returns {boolean} True if the color is valid, false otherwise.
 *
 * @example
 *```ts
 * isColorValid('#fff');
 * // true
 *
 * isColorValid('rgb(255, 255, 255)');
 * // true
 *
 * isColorValid('rgba(255, 255, 255, 1)');
 * // true
 *
 * isColorValid('hsl(0, 0%, 100%)');
 * // true
 *
 * isColorValid('hsla(0, 0%, 100%, 1)');
 * // true
 *
 * isColorValid('invalid');
 * // false
 *```
 */
export declare const isColorValid: (color: string) => boolean;
