/**
 * Check if the color is a valid RGB color.
 *
 * This function is used to check if the color is a valid RGB color.
 *
 * @see https://developer.mozilla.org/en-US/docs/Web/CSS/color_value#rgb()
 *
 * @since ??
 *
 * @param {string} color Color value.
 *
 * @returns {boolean} True if the color is a valid RGB color, false otherwise.
 *
 * @example
 *
 * ```ts
 * isRgb('rgb(255, 255, 255)');
 * // true
 *
 * isRgb('rgb(255, 255, 255, 0.5)');
 * // false
 * ```
 */
export declare const isRgb: (color: string) => boolean;
