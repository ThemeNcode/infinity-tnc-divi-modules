/**
 * Converts HEX color representation to RGB.
 *
 * @since ??
 *
 * @param {string} hex String value. May contains `#` symbol
 * and be in short 3-digit or long 6-digit format.
 *
 * @returns {number[]} Array of red, blue and green values between 0 and 255,
 * else empty array in case of invalid hex value.
 */
export declare const hexToRgb: (hex: string) => number[];
