/**
 * Converts RGB to HSL.
 * Assumes r, g and b between 0 and 255.
 *
 * @since ??
 *
 * @param {number} r Red color value.
 * @param {number} g Green color value.
 * @param {number} b Blue color value.
 *
 * @returns {number[]} Array of hue, saturation and lightness in integer.
 * Like hue = 195, saturation = 15, lightness = 95.
 */
export declare const rgbToHsl: (r: number, g: number, b: number) => number[];
