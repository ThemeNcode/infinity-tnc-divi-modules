/**
 * Calculates the best contrast to use against a color.
 *
 * @param {string} color The current color (either in HEX or RGB(A) format).
 *
 * @returns {string} The contrast color to use.
 *
 * @since 4.16.0 Added.
 */
export declare const getContrastColor: (color: string) => string;
