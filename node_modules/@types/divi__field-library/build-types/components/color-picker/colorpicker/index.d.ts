import { type ReactElement } from 'react';
import { type ColorPickerComponentProps } from './types';
import './style.scss';
/**
 * Colorpicker component for colorpicker field.
 *
 * @since ??
 *
 * @param {ColorPickerComponentProps} props Component props.
 *
 * @returns {ReactElement}
 */
export declare const ColorPickerComponent: ({ isAlpha, showPalettes, hideResultButton, autoFocus, value, colorPalette, onClose, onChange, onClear, }: ColorPickerComponentProps) => ReactElement;
