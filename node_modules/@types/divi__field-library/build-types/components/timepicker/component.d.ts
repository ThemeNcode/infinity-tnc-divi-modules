import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import 'react-dates/lib/css/_datepicker.css';
import './style.scss';
/**
 * Timepicker field component.
 *
 * Used for selecting time.
 *
 * @since ??
 *
 * @param {object} props Props for select field.
 * @param {string} props.name Name of the field.
 * @param {string} props.className Classname for the field.
 * @param {Function} props.onChange Callback for when the value changes.
 * @param {string} props.defaultValue Default value for the field.
 * @param {string} props.value Value for the field.
 *
 * @returns {ReactElement} Timepicker field.
 */
declare const Timepicker: {
    ({ name, className, onChange, defaultValue, value, }: FieldLibrary.Timepicker.Props): ReactElement;
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
declare const name = "divi/timepicker";
export { Timepicker, name, };
