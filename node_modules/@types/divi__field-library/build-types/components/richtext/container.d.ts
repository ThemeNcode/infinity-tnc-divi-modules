import { type ReactElement } from 'react';
/**
 * Container for RichText field component.
 *
 * Use component without explicitly passing props on the declared component; let container
 * component treat this component as props.children and passing props accordingly.
 *
 * @since ??
 *
 * @param {Record<string, any>} props Component props.
 *
 * @returns {ReactElement} RichText field component.
 *
 * @example Basic usage.
 * ```tsx
 * <RichTextContainer />
 * ```
 *
 * @example With props.
 * ```tsx
 * <RichTextContainer
 *   name="summer-sale-cta"
 *   value="Click here for the deal of the century!"
 *   defaultValue={"Click here!"}
 *   toggleSlug=''
 *   tinymceCSSFiles='/css/tinymce-styles.css'
 *   tinymceSkinUrl='/vendor/css/my-tinymce-skin'
 *   isTinyMCEAllowed={true}
 *   onModeSwitch={(mode) => console.log('switched mode:', mode)}
 *   onChange={(event, inputValue) => console.log('value changed!', 'event: ', event, 'inputValue: ', inputValue})}
 *   tinyMCEPlugins = [
 *     'autolink',
 *     'link',
 *     'image',
 *     'lists',
 *     'print',
 *     'preview',
 *     'autoresize',
 *     'textcolor',
 *     'table',
 *     'paste',
 *     'fullscreen',
 *     'charmap',
 *     'emoticons',
 *     'wpview',
 *   ]
 * />
 * ```
 */
declare const RichTextContainer: {
    (props: Record<string, any>): ReactElement;
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
export { RichTextContainer, };
