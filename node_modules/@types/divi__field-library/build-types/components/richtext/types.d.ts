import { type EditorManager } from 'tinymce';
declare global {
    interface Window {
        getUserSetting(prop: string): string;
        setUserSetting(prop: string, value: string): void;
        send_to_editor?: (html: unknown) => void;
        QTags?: {
            insertContent: (html: string) => void;
        };
        tb_remove?: () => void;
        wpActiveEditor: string | boolean;
        tinymce: EditorManager;
    }
}
/**
 * @typedef {object} EditorInput
 * @property {string} [value] Value.
 */
export interface RichTextEditorInput extends HTMLElement {
    value?: string;
}
/**
 * @typedef {object} RichTextSendAttachment
 * @property {string} [align] Align.
 * @property {string} [alt] Alt.
 * @property {boolean} [canEmbed] Can embed.
 * @property {string} [caption] Caption.
 * @property {string} [captionId] Caption ID.
 * @property {string[]} [classes] Classes.
 * @property {number} [height] Height.
 * @property {string} [link] Link.
 * @property {string} [linkUrl] Link URL.
 * @property {string} [size] Size.
 * @property {string} [src] Source.
 * @property {string} [title] Title.
 * @property {string} [type] Type.
 * @property {string} [url] URL.
 * @property {number} [width] Width.
 */
export interface RichTextSendAttachment {
    align?: string;
    alt?: string;
    canEmbed?: boolean;
    caption?: string;
    captionId?: string;
    classes?: string[];
    height?: number;
    link?: string;
    linkUrl?: string;
    size?: string;
    src?: string;
    title?: string;
    type?: string;
    url?: string;
    width?: number;
}
/**
 * @typedef {object} SendAttachmentOptionProps
 * @property {string} align Align.
 * @property {string} size Size.
 * @property {string} alt Alt.
 */
export interface RichTextSendAttachmentOptionProps {
    align: string;
    size: string;
    alt: string;
}
