/**
 * Process content for text editor.
 *
 * @since ??
 *
 * @param {string} content Content to process.
 * @param {boolean} isRemoveP `p` tag will be removed from if it's set as true. Default: true.
 *
 * @returns {string} Processed content.
 */
export declare const processContent: (content: string, isRemoveP?: boolean) => string;
