import { type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
import './style.scss';
/**
 * Richtext field component.
 *
 * This component is used to render the richtext field in the Visual Builder.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.name Field name.
 * @param {string} props.value Field value.
 * @param {string} props.defaultValue Field default value.
 * @param {string[]} props.tinyMCEPlugins TinyMCE plugins.
 * @param {string} props.toggleSlug Toggle slug.
 * @param {string} props.tinymceCSSFiles TinyMCE CSS files.
 * @param {string} props.tinymceSkinUrl TinyMCE skin URL.
 * @param {boolean} props.isTinyMCEAllowed Is TinyMCE allowed.
 * @param {Function} props.onModeSwitch Mode switch callback.
 * @param {Function} props.onChange Change callback.
 *
 * @returns {ReactElement} RichText component.
 */
declare const RichText: {
    ({ name, value, defaultValue, tinyMCEPlugins, toggleSlug, tinymceCSSFiles, tinymceSkinUrl, isTinyMCEAllowed, onModeSwitch, onChange, }: FieldLibrary.RichText.Props): ReactElement;
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    fieldName: string;
};
declare const name = "divi/richtext";
export { name, RichText, };
