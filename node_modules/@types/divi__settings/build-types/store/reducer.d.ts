import { type Settings } from '@divi/types';
/**
 * Divi/settings store Reducer. Currently, the reducer does nothing except for returning store state
 * because there's no actions for this store yet.
 *
 * @since ??
 */
export declare const reducer: (state: Settings.Store.ImmutableState, action: Settings.Store.Actions.ReturnValue) => Settings.Store.ImmutableState;
