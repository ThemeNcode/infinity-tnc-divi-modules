import React from 'react';
import { createRegistry as createRegistryWp, createRegistryControl as createRegistryControlWp, createRegistrySelector as createRegistrySelectorWp, plugins } from '@wordpress/data';
import { type AppPreferences, type AppUi, type Clipboard, type CloudApp, type Colors, type Data, type EditPost, type EmailMarketing, type Events, type Fonts, type GlobalData, type GlobalLayouts, type Help, type History, type KeyboardShortcuts, type ModalLibrary, type Module, type ModuleLibrary, type PageSettings, type Rest, type RightClickOptions, type SerializedPost, type Settings, type ShortcodeModule, type SpamProtection } from '@divi/types';
declare module '@divi/data';
/**
 * Enhancing Types for @wordpress/data with Module Augmentation.
 *
 * The `@wordpress/data` package lacks predefined store types. By using TypeScript's Module Augmentation
 * we can enhance the package with these types.
 * This ensures that functions like `dispatch()` accurately return types corresponding to the specified store name.
 */
declare module '@wordpress/data' {
    function dispatch(key: 'divi/app-ui'): AppUi.Store.Actions.FunctionMap;
    function dispatch(key: 'divi/app-preferences'): AppPreferences.Store.Actions.FunctionMap;
    function dispatch(key: 'divi/clipboard'): Clipboard.Store.Actions.FunctionMap;
    function dispatch(key: 'divi/cloud-app'): CloudApp.Store.Actions.FunctionMap;
    function dispatch(key: 'divi/colors'): Colors.Store.Actions.FunctionMap;
    function dispatch(key: 'divi/edit-post'): EditPost.Store.Actions.FunctionMap;
    function dispatch(key: 'divi/email-marketing'): EmailMarketing.Store.Actions.FunctionMap;
    function dispatch(key: 'divi/events'): Events.Store.Actions.FunctionMap;
    function dispatch(key: 'divi/fonts'): Fonts.Store.Actions.FunctionMap;
    function dispatch(key: 'divi/global-data'): GlobalData.Store.Actions.FunctionMap;
    function dispatch(key: 'divi/global-layouts'): GlobalLayouts.Store.Actions.FunctionMap;
    function dispatch(key: 'divi/help'): Help.Store.Actions.FunctionMap;
    function dispatch(key: 'divi/history'): History.Store.Actions.FunctionMap;
    function dispatch(key: 'divi/keyboard-shortcuts'): KeyboardShortcuts.Store.Actions.FunctionMap;
    function dispatch(key: 'divi/modal-library'): ModalLibrary.Store.Actions.FunctionMap;
    function dispatch(key: 'divi/module-library'): ModuleLibrary.Store.Actions.FunctionMap;
    function dispatch(key: 'divi/module'): Module.Store.Actions.FunctionMap;
    function dispatch(key: 'divi/page-settings'): PageSettings.Store.Actions.FunctionMap;
    function dispatch(key: 'divi/rest'): Rest.Store.Actions.FunctionMap;
    function dispatch(key: 'divi/right-click-options'): RightClickOptions.Store.Actions.FunctionMap;
    function dispatch(key: 'divi/serialized-post'): SerializedPost.Store.Actions.FunctionMap;
    function dispatch(key: 'divi/settings'): Settings.Store.Actions.FunctionMap;
    function dispatch(key: 'divi/shortcode-module'): ShortcodeModule.Store.Actions.FunctionMap;
    function dispatch(key: 'divi/spam-protection'): SpamProtection.Store.Actions.FunctionMap;
    function select(key: 'divi/app-ui'): AppUi.Store.Selectors.FunctionMap;
    function select(key: 'divi/app-preferences'): AppPreferences.Store.Selectors.FunctionMap;
    function select(key: 'divi/clipboard'): Clipboard.Store.Selectors.FunctionMap;
    function select(key: 'divi/cloud-app'): CloudApp.Store.Selectors.FunctionMap;
    function select(key: 'divi/colors'): Colors.Store.Selectors.FunctionMap;
    function select(key: 'divi/edit-post'): EditPost.Store.Selectors.FunctionMap;
    function select(key: 'divi/email-marketing'): EmailMarketing.Store.Selectors.FunctionMap;
    function select(key: 'divi/events'): Events.Store.Selectors.FunctionMap;
    function select(key: 'divi/fonts'): Fonts.Store.Selectors.FunctionMap;
    function select(key: 'divi/global-data'): GlobalData.Store.Selectors.FunctionMap;
    function select(key: 'divi/global-layouts'): GlobalLayouts.Store.Selectors.FunctionMap;
    function select(key: 'divi/help'): Help.Store.Selectors.FunctionMap;
    function select(key: 'divi/history'): History.Store.Selectors.FunctionMap;
    function select(key: 'divi/keyboard-shortcuts'): KeyboardShortcuts.Store.Selectors.FunctionMap;
    function select(key: 'divi/modal-library'): ModalLibrary.Store.Selectors.FunctionMap;
    function select(key: 'divi/module-library'): ModuleLibrary.Store.Selectors.FunctionMap;
    function select(key: 'divi/module'): Module.Store.Selectors.FunctionMap;
    function select(key: 'divi/page-settings'): PageSettings.Store.Selectors.FunctionMap;
    function select(key: 'divi/rest'): Rest.Store.Selectors.FunctionMap;
    function select(key: 'divi/right-click-options'): RightClickOptions.Store.Selectors.FunctionMap;
    function select(key: 'divi/serialized-post'): SerializedPost.Store.Selectors.FunctionMap;
    function select(key: 'divi/settings'): Settings.Store.Selectors.FunctionMap;
    function select(key: 'divi/shortcode-module'): ShortcodeModule.Store.Selectors.FunctionMap;
    function select(key: 'divi/spam-protection'): SpamProtection.Store.Selectors.FunctionMap;
}
/**
 * Create Registry.
 *
 * Creates a new store registry, given an optional object of initial store
 * configurations.
 *
 * @since ??
 *
 * @param {object}  storeConfigs Initial store configurations.
 * @param {object?} parent       Parent registry.
 *
 * @returns {object} Data registry.
 */
export declare const createRegistry: typeof createRegistryWp;
/**
 * Create Registry Selector.
 *
 * Creates a selector function that takes additional curried argument with the
 * registry `select` function. While a regular selector has signature.
 *
 * @since ??
 *
 * @param {Function} registrySelector Function receiving a registry `select` function and returning a state selector.
 *
 * @returns {Function} Registry selector that can be registered with a store.
 */
export declare const createRegistrySelector: typeof createRegistrySelectorWp;
/**
 * Create Registry Control.
 *
 * Creates a control function that takes additional curried argument with the `registry` object.
 * While a regular control has signature `( action ) => ( iteratorOrPromise )`
 * where the control works with the `action` that it's bound to, a registry control has signature:
 * `( registry ) => ( action ) => ( iteratorOrPromise )` A registry control is typically used to
 * \select data or dispatch an action to a registered store. When registering a control created with
 * `createRegistryControl` with a store, the store knows which calling convention to use when executing the control.
 *
 * @since ??
 *
 * @param {Function} registryControl Function receiving a registry object and returning a control.
 *
 * @returns {Function} Registry control that can be registered with a store.
 */
export declare const createRegistryControl: typeof createRegistryControlWp;
/**
 * Combine Reducers.
 *
 * The combineReducers helper function turns an object whose values are different
 * reducing functions into a single reducing function you can pass to registerReducer.
 *
 * @since ??
 *
 * @param {object} reducers All reducers.
 *
 *
 * @returns {Function} Combined reducer.
 */
export declare const combineReducers: typeof import("@wordpress/data").combineReducers;
/**
 * Dispatch.
 *
 * Given a store descriptor, returns an object of the store's action creators.
 * Calling an action creator will cause it to be dispatched, updating the state value accordingly.
 *
 * Note: Action creators returned by the dispatch will return a promise when
 * they are called.
 *
 * @since ??
 *
 * @param {string|object} storeNameOrDescriptor The store descriptor.
 *
 * @returns {object} Containing the action creators.
 */
export declare const dispatch: Data.Dispatch;
/**
 * Register Generic Store.
 *
 * Registers a generic store instance.
 *
 * @since ??
 *
 * @param {string} name  Store registry name.
 * @param {object} store Store instance (`{ getSelectors, getActions, subscribe }`).
 */
export declare const registerGenericStore: Function;
/**
 * Register Store.
 *
 * Registers a standard `@wordpress/data` store.
 *
 * @since ??
 *
 * @param {string} storeName Unique namespace identifier for the store.
 * @param {object} options   Store description (reducer, actions, selectors, resolvers).
 *
 * @returns {object} Registered store object.
 */
export declare const registerStore: Data.RegisterStore;
/**
 * Select.
 *
 * Given a store descriptor, returns an object of the store's selectors.
 * The selector functions are been pre-bound to pass the current state automatically.
 * As a consumer, you need only pass arguments of the selector, if applicable.
 *
 * @since ??
 *
 * @param {string|object} storeNameOrDescriptor The store descriptor.
 *
 * @returns Object containing the store's selectors.
 */
export declare const select: Data.Select;
/**
 * Subscribe.
 *
 * Given a listener function, the function will be called any time the state value
 * of one of the registered stores has changed. If you specify the optional
 * `storeNameOrDescriptor` parameter, the listener function will be called only
 * on updates on that one specific registered store.
 *
 * This function returns an `unsubscribe` function used to stop the subscription.
 *
 * @since ??
 *
 * @param {Function} listener Callback function.
 * @param {string|object} storeNameOrDescriptor Optional store name.
 */
export declare const subscribe: Function;
/**
 * Use.
 *
 * Extends a registry to inherit functionality provided by a given plugin. A
 * plugin is an object with properties aligning to that of a registry, merged
 * to extend the default registry behavior.
 *
 * @since ??
 *
 * @param {object} plugin Plugin object.
 */
export declare const use: any;
/**
 * Use Select.
 *
 * Custom react hook for retrieving props from registered selectors.
 *
 * In general, this custom React hook follows the
 * [rules of hooks](https://reactjs.org/docs/hooks-rules.html).
 *
 * @since ??
 *
 * @param {Function} mapSelect Function called on every state change. It will return the selected value from the state.
 * @param {Array}   deps If provided, this memoizes the mapSelect.
 *
 * @returns {any} Selected value from the registry/state.
 */
export declare const useSelect: Data.UseSelect;
/**
 * Use Dispatch.
 *
 * A custom react hook returning the current registry dispatch actions creators.
 *
 * Note: The component using this hook must be within the context of a
 * RegistryProvider.
 *
 * @since ??
 *
 * @param {string|object} [storeNameOrDescriptor] Store name or descriptor.
 *
 *
 * @returns {object} All actions for the store.
 */
export declare const useDispatch: Data.UseDispatch;
/**
 * With Select.
 *
 * Higher-order component used to inject state-derived props using registered
 * selectors.
 *
 * @since ??
 *
 * @todo Fix the type issue without using unknown / typecasing.
 *
 * @param {Function} mapSelectToProps Function called on state change. It will return the selected value from the state.
 *
 * @returns {React.Component} Enhanced component with merged state data props.
 */
export declare const withSelect: Data.WithSelect;
/**
 * With Dispatch.
 *
 * Higher-order component used to add dispatch props using registered action
 * creators.
 *
 * @since ??
 *
 * @todo Fix the type issue without using unknown / typecasing.
 *
 * @param {Function} mapDispatchToProps A function of returning an object of props where value is the dispatch actions.
 *
 * @returns {React.Component} Enhanced component with merged dispatcher props.
 */
export declare const withDispatch: Data.WithDispatch;
/**
 * With Registry.
 *
 * Higher-order component which renders the original component with the current
 * registry context passed as its `registry` prop.
 *
 * @since ??
 *
 * @param {React.Component} OriginalComponent Original component.
 *
 * @returns {React.Component} Enhanced component.
 */
export declare const withRegistry: (Inner: React.ComponentType<any>) => (props: any) => JSX.Element;
/**
 * State management package used for managing state based on @wordpress/data package. Visual builder
 * abstraction needs to be added to accommodate Gutenberg's frequent API changes.
 *
 * @since ??
 */
export { plugins, };
