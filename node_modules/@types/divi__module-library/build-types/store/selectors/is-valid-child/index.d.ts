import { type ModuleAttrs, type ModuleLibrary } from '@divi/types';
/**
 * Check if given module name & attrs can have child of given child module name.
 *
 * @since ??
 */
export declare const isValidChild: (state: ModuleLibrary.Store.ImmutableState, moduleName: string, moduleAttrs: ModuleAttrs, childModuleName: string) => boolean;
