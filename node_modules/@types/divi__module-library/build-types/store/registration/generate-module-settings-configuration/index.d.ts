import { type Module } from '@divi/types';
import { type GenerateModuleSettingsConfigurationParams } from './types';
/**
 * Generate module settings configuration for generating module settings from module metadata (module.json).
 *
 * @since ??
 *
 * @param {object} param0  Function parameters.
 * @param {object} param0.moduleMetadata Module metadata (module.json).
 * @param {object} param0.presetAttrs Preset attrs.
 *
 * @returns {object} Module settings configuration.
 */
export declare const generateModuleSettingsConfiguration: ({ moduleMetadata, presetAttrs, }: GenerateModuleSettingsConfigurationParams) => Module.Settings.Configuration.Value;
