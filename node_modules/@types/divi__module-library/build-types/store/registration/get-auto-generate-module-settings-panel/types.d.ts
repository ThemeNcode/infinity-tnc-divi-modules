import { type Module, type ModuleAttrs, type ModuleMetadata } from '@divi/types';
export interface GetAutoGenerateModuleSettingsPanelParams {
    moduleMetadata: ModuleMetadata;
    panel: Module.Settings.Panel.Name;
    presetAttrs?: ModuleAttrs;
}
