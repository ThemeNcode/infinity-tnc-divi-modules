import { type ReactElement } from 'react';
import { type EditContainerProps } from './types';
export declare const EditContainer: ({ props, metadata, config, defaultAttrs, defaultPrintedStyleAttrs, }: EditContainerProps) => ReactElement;
