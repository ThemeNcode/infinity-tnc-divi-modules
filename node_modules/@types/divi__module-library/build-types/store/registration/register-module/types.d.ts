import { type Module, type ModuleAttrs, type ModuleAttrsWithChildren, type ModuleLibrary, type ModuleMetadata } from '@divi/types';
export type SettingContainerPropsConfig = Omit<ModuleLibrary.Module.RegisterDefinition, 'metadata'>;
export interface SettingContainerProps {
    props: Module.SettingsProps;
    metadata: ModuleMetadata;
    config: SettingContainerPropsConfig;
    defaultSettingsAttrs: Record<string, Module.Element.Attributes>;
    panel: keyof SettingContainerPropsConfig['settings'];
}
export interface EditContainerProps {
    props: ModuleLibrary.Module.Component.EditProps<ModuleAttrs, ModuleAttrsWithChildren>;
    metadata: ModuleMetadata;
    config: SettingContainerPropsConfig;
    defaultAttrs: Record<string, Module.Element.Attributes>;
    defaultPrintedStyleAttrs: Record<string, Module.Element.Attributes>;
}
