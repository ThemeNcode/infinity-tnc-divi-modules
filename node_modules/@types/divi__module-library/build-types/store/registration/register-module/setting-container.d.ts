import { type ReactElement } from 'react';
import { type SettingContainerProps } from './types';
/**
 * Renders the setting container for a module setting panel.
 *
 * @since ??
 *
 * @param {object} param0  Function parameters.
 * @param {object} param0.metadata The metadata of the module.
 * @param {object} param0.config The configuration of the module.
 * @param {object} param0.props The props of the module setting component.
 * @param {object} param0.defaultSettingsAttrs The default settings attributes of the module.
 * @param {string} param0.panel The panel to render.
 *
 * @returns {ReactElement} The rendered setting container or null.
 */
export declare const SettingContainer: ({ metadata, config, props, defaultSettingsAttrs, panel, }: SettingContainerProps) => ReactElement;
