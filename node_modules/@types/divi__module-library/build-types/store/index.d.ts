/**
 * Register Module Library Store.
 *
 * Register module library store with all modules that available in moduleMapping.
 *
 * @since ??
 *
 * @param {boolean} verifyFrontEndRenderer Whether to verify whether a module has frontend rendering or not.
 *
 * @returns {void}
 *
 * @example
 * ```tsx
 * registerModuleLibraryStore(); // It will register all modules that available in moduleMapping.
 * ```
 *
 * @example
 * ```tsx
 * registerModuleLibraryStore(true); // It will register only modules that have frontend rendering.
 * ```
 */
export declare const registerModuleLibraryStore: (verifyFrontEndRenderer?: boolean) => void;
