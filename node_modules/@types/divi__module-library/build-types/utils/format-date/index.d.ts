/**
 * Format a date using PHP date format characters.
 *
 * This was developed to allow for the formatting of dates in JavaScript like PHP can do. Always
 * refer to PHP’s date documentation for how to use it.
 *
 * @see https://www.php.net/manual/en/datetime.format.php
 *
 * @since ??
 *
 * @param {string} format       The format to use.
 * @param {Date}   dateToFormat The date to format.
 *
 * @returns {string} The formatted date.
 */
export declare const formatDate: (format: string, dateToFormat: Date) => string;
