/**
 * Checks if the current layout is the theme builder layout or not.
 *
 * @since ??
 *
 * @returns {boolean} Will return "on" if the current layout is the theme builder layout, otherwise "off".
 */
export declare const isTBLayout: () => boolean;
