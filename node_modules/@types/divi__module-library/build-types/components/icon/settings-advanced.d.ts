import { type ReactElement } from 'react';
import { type IconAttrs, type Module } from '@divi/types';
export declare const SettingsAdvanced: ({ attrs, groupConfiguration, }: Module.Settings.Panel.Props<IconAttrs>) => ReactElement;
