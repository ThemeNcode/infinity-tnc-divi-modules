import { type ReactElement } from 'react';
import { type IconEditProps } from './types';
/**
 * Icon Edit component of visual builder.
 *
 * @since ??
 *
 * @param {IconEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const IconEdit: ({ attrs, id, name, elements, }: IconEditProps) => ReactElement;
export { IconEdit, };
