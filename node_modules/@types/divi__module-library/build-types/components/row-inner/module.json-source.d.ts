import { type ModuleMetadata, type RowAttrs } from '@divi/types';
/**
 * Row Inner Meta Data.
 *
 * Note: The module metadata will be used to generate `module.json` upon build.
 * Variable name must end with `ModuleMetaData` to be picked up by the build script.
 */
declare const rowInnerModuleMetaData: ModuleMetadata<RowAttrs>;
export { rowInnerModuleMetaData, };
