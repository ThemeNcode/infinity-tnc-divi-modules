import { type ReactElement } from 'react';
import { type PostTitleEditProps } from './types';
/**
 * PostTitle component of visual builder.
 *
 * @since ??
 *
 * @param {PostTitleEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const PostTitleEdit: ({ attrs, defaultPrintedStyleAttrs, id, name, elements, }: PostTitleEditProps) => ReactElement;
export { PostTitleEdit, };
