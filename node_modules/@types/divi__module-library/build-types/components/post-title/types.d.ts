import { type Module, type PostTitleAttrs } from '@divi/types';
import { type ModuleEditProps } from '../types-module';
declare global {
    interface Date {
        format?: (format: string) => string;
    }
}
export type PostTitleEditProps = ModuleEditProps<PostTitleAttrs>;
export type PostTitleFieldCallbackParams = Module.Settings.Field.CallbackParams<PostTitleAttrs>;
