import { type ReactElement } from 'react';
import { type Module, type PostTitleAttrs } from '@divi/types';
/**
 * Content panel component for the PostTitle module settings modal.
 *
 * This component is registered as the `content` panel for the PostTitle module.
 *
 * @since ??
 *
 * @param {object} param0 Content panel props.
 * @param {object} param0.defaultSettingsAttrs Default settings attributes.
 * @param {object} param0.groupConfiguration Module settings group configuration.
 * @param {object} param0.attrs Module attributes value.
 *
 * @returns {ReactElement} Content panel component.
 */
export declare const SettingsContent: ({ defaultSettingsAttrs, attrs, groupConfiguration, }: Module.Settings.Panel.Props<PostTitleAttrs>) => ReactElement;
