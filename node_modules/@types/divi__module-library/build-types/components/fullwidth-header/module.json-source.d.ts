import { type FullwidthHeaderAttrs, type ModuleMetadata } from '@divi/types';
/**
 * Fullwitdth Header Meta Data.
 *
 * Note: The module metadata will be used to generate `module.json` upon build.
 * Variable name must end with `ModuleMetaData` to be picked up by the build script.
 */
declare const fullwidthHeaderModuleMetaData: ModuleMetadata<FullwidthHeaderAttrs>;
export { fullwidthHeaderModuleMetaData, };
