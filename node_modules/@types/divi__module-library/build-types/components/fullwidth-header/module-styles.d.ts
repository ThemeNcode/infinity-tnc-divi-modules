import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type FullwidthHeaderAttrs } from '@divi/types';
/**
 * Fullwidth Header Module's style components.
 *
 * @since ??
 */
declare const ModuleStyles: ({ mode, state, noStyleTag, orderClass, attrs, elements, settings, defaultPrintedStyleAttrs, }: StylesProps<FullwidthHeaderAttrs>) => ReactElement;
export { ModuleStyles, };
