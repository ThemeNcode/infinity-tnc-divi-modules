import { type ReactElement } from 'react';
import { type ScriptDataProps } from '@types/divi__module';
import { type FullwidthHeaderAttrs } from '@divi/types';
/**
 * Fullwidth Header module's script data component.
 *
 * @since ??
 *
 * @returns {ReactElement}
 */
export declare const ModuleScriptData: <TProps extends ScriptDataProps<FullwidthHeaderAttrs>>({ elements, }: TProps) => ReactElement;
