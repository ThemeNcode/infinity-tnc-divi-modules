import { type ReactElement } from 'react';
import { type FullwidthHeaderEditProps } from './types';
/**
 * Fullwidth Header component of visual builder.
 *
 * @since ??
 *
 * @param {FullwidthHeaderEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const FullwidthHeaderEdit: ({ attrs, defaultPrintedStyleAttrs, id, name, elements, }: FullwidthHeaderEditProps) => ReactElement;
export { FullwidthHeaderEdit, };
