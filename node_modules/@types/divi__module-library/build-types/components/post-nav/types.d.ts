import { type Module, type PostNavAttrs } from '@divi/types';
import { type ModuleEditProps } from '../types-module';
export interface PostNavEditProps extends ModuleEditProps<PostNavAttrs> {
    postId?: string;
}
export type PostNavFieldCallbackParams = Module.Settings.Field.CallbackParams<PostNavAttrs>;
