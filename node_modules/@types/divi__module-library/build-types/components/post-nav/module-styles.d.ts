import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type PostNavAttrs } from '@divi/types';
export declare const ModuleStyles: ({ attrs, elements, defaultPrintedStyleAttrs, mode, orderClass, state, noStyleTag, settings, }: StylesProps<PostNavAttrs>) => ReactElement;
