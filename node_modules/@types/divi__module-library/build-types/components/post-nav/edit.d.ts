import { type ReactElement } from 'react';
import { type PostNavEditProps } from './types';
/**
 * Post Navigation component of visual builder.
 *
 * @since ??
 *
 * @param {PostNavEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const PostNavEdit: ({ attrs, defaultPrintedStyleAttrs, id, name, elements, postId, }: PostNavEditProps) => ReactElement;
export { PostNavEdit, };
