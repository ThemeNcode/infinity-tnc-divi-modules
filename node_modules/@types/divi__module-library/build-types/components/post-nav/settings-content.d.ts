import { type ReactElement } from 'react';
import { type Module, type PostNavAttrs } from '@divi/types';
/**
 * Content panel component for the Post Navigation module settings modal.
 *
 * This component is registered as the `content` panel for the Post Navigation module.
 *
 * @since ??
 *
 * @param {object} param0 Content panel props.
 * @param {object} param0.attrs Module attributes.
 * @param {object} param0.defaultSettingsAttrs Default settings attributes.
 *
 * @returns {ReactElement} Content panel component.
 */
export declare const SettingsContent: ({ attrs, defaultSettingsAttrs, }: Module.Settings.Panel.Props<PostNavAttrs>) => ReactElement;
