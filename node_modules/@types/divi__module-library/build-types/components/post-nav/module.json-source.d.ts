import { type ModuleMetadata, type PostNavAttrs } from '@divi/types';
/**
 * Post Navigation Meta Data.
 *
 * Note: The module metadata will be used to generate `module.json` upon build.
 * Variable name must end with `ModuleMetaData` to be picked up by the build script.
 */
declare const postNavModuleMetaData: ModuleMetadata<PostNavAttrs>;
export { postNavModuleMetaData, };
