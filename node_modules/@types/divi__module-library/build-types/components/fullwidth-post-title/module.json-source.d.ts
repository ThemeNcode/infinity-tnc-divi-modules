import { type FullwidthPostTitleAttrs, type ModuleMetadata } from '@divi/types';
/**
 * Fullwidth Post Title Module Meta Data.
 *
 * Note: The module metadata will be used to generate `module.json` upon build.
 * Variable name must end with `ModuleMetaData` to be picked up by the build script.
 */
declare const fullwidthPostTitleModuleMetaData: ModuleMetadata<FullwidthPostTitleAttrs>;
export { fullwidthPostTitleModuleMetaData, };
