import { type FullwidthPostTitleAttrs, type Module } from '@divi/types';
import { type ModuleEditProps } from '../types-module';
declare global {
    interface Date {
        format?: (format: string) => string;
    }
}
export type FullwidthPostTitleEditProps = ModuleEditProps<FullwidthPostTitleAttrs>;
export type FullwidthPostTitleFieldCallbackParams = Module.Settings.Field.CallbackParams<FullwidthPostTitleAttrs>;
