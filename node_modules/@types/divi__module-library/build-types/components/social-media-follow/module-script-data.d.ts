import { type ReactElement } from 'react';
import { type ScriptDataProps } from '@types/divi__module';
import { type SocialMediaFollowAttrs } from '@divi/types';
/**
 * Social Media Follow module's script data component.
 *
 * @since ??
 *
 * @returns {ReactElement}
 */
export declare const ModuleScriptData: <TProps extends ScriptDataProps<SocialMediaFollowAttrs>>({ elements, }: TProps) => ReactElement;
