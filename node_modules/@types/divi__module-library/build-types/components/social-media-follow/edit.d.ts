import { type ReactElement } from 'react';
import { type SocialMediaFollowEditProps } from './types';
/**
 * Social Media Follow component of visual builder.
 *
 * @since ??
 *
 * @param {SocialMediaFollowEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const SocialMediaFollowEdit: ({ attrs, defaultPrintedStyleAttrs, elements, id, name, childrenIds, }: SocialMediaFollowEditProps) => ReactElement;
export { SocialMediaFollowEdit, };
