import { type ModuleMetadata, type SocialMediaFollowAttrs } from '@divi/types';
/**
 * Social Media Follow Meta Data.
 *
 * Note: The module metadata will be used to generate `module.json` upon build.
 * Variable name must end with `ModuleMetaData` to be picked up by the build script.
 */
declare const socialMediaFollowModuleMetaData: ModuleMetadata<SocialMediaFollowAttrs>;
export { socialMediaFollowModuleMetaData, };
