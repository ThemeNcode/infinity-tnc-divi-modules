import { type ReactElement } from 'react';
import { type Module, type SocialMediaFollowAttrs } from '@divi/types';
export declare const SettingsDesign: ({ defaultSettingsAttrs, }: Module.Settings.Panel.Props<SocialMediaFollowAttrs>) => ReactElement;
