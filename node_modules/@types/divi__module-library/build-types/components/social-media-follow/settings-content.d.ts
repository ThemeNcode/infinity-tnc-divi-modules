import { type ReactElement } from 'react';
import { type Module, type SocialMediaFollowAttrs } from '@divi/types';
export declare const SettingsContent: ({ defaultSettingsAttrs, }: Module.Settings.Panel.Props<SocialMediaFollowAttrs>) => ReactElement;
