import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type SocialMediaFollowAttrs } from '@divi/types';
/**
 * Social Media Follow Item Module's style components.
 *
 * @since ??
 */
declare const ModuleStyles: ({ attrs, defaultPrintedStyleAttrs, elements, settings, orderClass, mode, state, noStyleTag, }: StylesProps<SocialMediaFollowAttrs>) => ReactElement;
export { ModuleStyles, };
