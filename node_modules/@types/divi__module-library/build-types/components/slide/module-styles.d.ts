import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type SlideAttrs } from '@divi/types';
/**
 * Slide Module's style components.
 *
 * @since ??
 */
declare const ModuleStyles: ({ id, mode, attrs, elements, defaultPrintedStyleAttrs, orderClass, parentOrderClass, state, noStyleTag, settings, }: StylesProps<SlideAttrs>) => ReactElement;
export { ModuleStyles, };
