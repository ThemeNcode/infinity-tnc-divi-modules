import { type ReactElement } from 'react';
import { type SlideEditProps } from './types';
/**
 * Slide component of visual builder.
 *
 * @since ??
 *
 * @param {SlideEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const SlideEdit: (props: SlideEditProps) => ReactElement;
export { SlideEdit, };
