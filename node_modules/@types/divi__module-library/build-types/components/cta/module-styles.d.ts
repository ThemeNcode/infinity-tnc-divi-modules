import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type CtaAttrs } from '@divi/types';
/**
 * CTA Module's style components.
 *
 * @since ??
 */
declare const ModuleStyles: <TProps extends StylesProps<CtaAttrs>>({ attrs, elements, settings, orderClass, mode, state, noStyleTag, }: TProps) => ReactElement;
export { ModuleStyles, };
