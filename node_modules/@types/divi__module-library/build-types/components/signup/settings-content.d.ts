import { type ReactElement } from 'react';
import { type Module, type SignupAttrs } from '@divi/types';
/**
 * Content panel component for the Email Optin module settings modal.
 *
 * @since ??
 *
 * @param {Module.Settings.Panel.Props} param0 Content panel props.
 *
 * @returns {ReactElement}
 */
export declare const SettingsContent: ({ attrs, groupConfiguration, }: Module.Settings.Panel.Props<SignupAttrs>) => ReactElement;
