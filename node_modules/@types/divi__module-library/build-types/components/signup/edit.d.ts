import { type ReactElement } from 'react';
import { type SignupEditProps } from './types';
/**
 * Edit component of visual builder of Email Opt In Module.
 *
 * @since ??
 *
 * @param {SignupEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const SignupEdit: ({ attrs, id, name, childrenIds, elements, defaultPrintedStyleAttrs, }: SignupEditProps) => ReactElement;
export { SignupEdit, };
