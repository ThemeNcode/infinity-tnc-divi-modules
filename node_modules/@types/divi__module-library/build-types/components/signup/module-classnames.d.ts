import { type ModuleClassnamesParams } from '@types/divi__module';
import { type SignupAttrs } from '@divi/types';
/**
 * Set CSS class names to the module.
 *
 * @since ??
 *
 * @param {ModuleClassnamesParams<SignupAttrs>} param0 Function parameters.
 */
export declare const moduleClassnames: ({ classnamesInstance, attrs, breakpoint, state, }: ModuleClassnamesParams<SignupAttrs>) => void;
