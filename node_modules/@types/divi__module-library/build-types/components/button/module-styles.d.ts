import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type ButtonAttrs } from '@divi/types';
/**
 * Text Module's style components.
 *
 * @since ??
 */
declare const ModuleStyles: <TProps extends StylesProps<ButtonAttrs>>({ attrs, elements, settings, wrapperOrderClass, orderClass, mode, state, noStyleTag, }: TProps) => ReactElement;
export { ModuleStyles, };
