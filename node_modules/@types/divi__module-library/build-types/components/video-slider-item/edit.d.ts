import { type ReactElement } from 'react';
import { type VideoSliderItemEditProps } from './types';
/**
 * Video Slider Item component of visual builder.
 *
 * @since ??
 *
 * @param {VideoSliderItemEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const VideoSliderItemEdit: ({ attrs, defaultPrintedStyleAttrs, id, moduleOrderIndex, name, parentAttrs, parentId, parentName, isFirst, parentCallback, elements, }: VideoSliderItemEditProps) => ReactElement;
export { VideoSliderItemEdit, };
