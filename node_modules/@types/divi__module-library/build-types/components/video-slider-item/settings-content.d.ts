import { type ReactElement } from 'react';
import { type Module, type VideoSliderItemAttrs } from '@divi/types';
/**
 * Content panel component for the Video slider item module settings modal.
 *
 * @since ??
 *
 * @param {Module.Settings.Panel.Props} param0 Content panel props.
 *
 * @returns {ReactElement}
 */
export declare const SettingsContent: ({ defaultSettingsAttrs, attrs, }: Module.Settings.Panel.Props<VideoSliderItemAttrs>) => ReactElement;
