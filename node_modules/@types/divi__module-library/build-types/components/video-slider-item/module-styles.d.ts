import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type VideoSliderAttrs, type VideoSliderItemAttrs } from '@divi/types';
/**
 * Video Slider Item Module's style components.
 *
 * @since ??
 */
declare const ModuleStyles: ({ attrs, defaultPrintedStyleAttrs, mode, state, noStyleTag, orderClass, elements, }: StylesProps<VideoSliderItemAttrs, VideoSliderAttrs>) => ReactElement;
export { ModuleStyles, };
