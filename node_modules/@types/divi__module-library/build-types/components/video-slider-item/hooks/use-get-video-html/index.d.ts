import { type UseGetVideoHTML } from './types';
/**
 * Gets video content from Divi WP REST API.
 *
 * @returns {{
 * getVideoHTML: GetVideoHTML,
 * videoHTML: string,
 * isVideoHTMLLoading: boolean
 * }} Returns an object consisted of:
 *
 * `getVideoHTML()`: Initiates a fetch request which retrieves the data from the backend.
 *
 * `videoHTML`: Video as an iframe or video tag included with url, Initial value is an empty string.
 *
 * `isVideoHTMLLoading`: Loading state for `getVideoHTML()`.
 */
export declare const useGetVideoHTML: () => UseGetVideoHTML;
