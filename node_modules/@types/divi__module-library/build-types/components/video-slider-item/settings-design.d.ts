import { type ReactElement } from 'react';
import { type Module, type VideoSliderItemAttrs } from '@divi/types';
/**
 * Design panel component for the Video slider item module settings modal.
 *
 * @since ??
 *
 * @param {Module.Settings.Panel.Props} param0 Design panel props.
 *
 * @returns {ReactElement}
 */
export declare const SettingsDesign: ({ defaultSettingsAttrs, }: Module.Settings.Panel.Props<VideoSliderItemAttrs>) => ReactElement;
