import { type ModuleMetadata, type VideoSliderItemAttrs } from '@divi/types';
/**
 * Video Slider Item Meta Data.
 *
 * Note: The module metadata will be used to generate `module.json` upon build.
 * Variable name must end with `ModuleMetaData` to be picked up by the build script.
 */
declare const videoSliderItemModuleMetaData: ModuleMetadata<VideoSliderItemAttrs>;
export { videoSliderItemModuleMetaData, };
