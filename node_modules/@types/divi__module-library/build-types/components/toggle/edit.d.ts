import { type ReactElement } from 'react';
import { type ToggleEditProps } from './types';
/**
 * Toggle component of visual builder.
 *
 * @since ??
 *
 * @param {ToggleEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const ToggleEdit: ({ attrs, defaultPrintedStyleAttrs, id, name, elements, }: ToggleEditProps) => ReactElement;
export { ToggleEdit, };
