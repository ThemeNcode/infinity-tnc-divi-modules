import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type ToggleAttrs } from '@divi/types';
/**
 * Text Module's style components.
 *
 * @since ??
 */
declare const ModuleStyles: ({ attrs, elements, mode, state, orderClass, noStyleTag, settings, }: StylesProps<ToggleAttrs>) => ReactElement;
export { ModuleStyles, };
