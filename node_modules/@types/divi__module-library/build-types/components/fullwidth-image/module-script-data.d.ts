import { type ReactElement } from 'react';
import { type ScriptDataProps } from '@types/divi__module';
import { type FullwidthImageAttrs } from '@divi/types';
/**
 * Fullwidth Image module's script data component.
 *
 * @since ??
 *
 * @returns {ReactElement}
 */
export declare const ModuleScriptData: <TProps extends ScriptDataProps<FullwidthImageAttrs>>({ elements, }: TProps) => ReactElement;
