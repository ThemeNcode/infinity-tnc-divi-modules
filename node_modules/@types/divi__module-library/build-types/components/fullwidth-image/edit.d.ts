import { type ReactElement } from 'react';
import { type FullwidthImageEditProps } from './types';
/**
 * Fullwidth Image component of visual builder.
 *
 * @since ??
 *
 * @param {FullwidthImageEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const FullwidthImageEdit: ({ attrs, defaultPrintedStyleAttrs, id, name, elements, }: FullwidthImageEditProps) => ReactElement;
export { FullwidthImageEdit, };
