import { type TabAttrs, type TabsAttrs } from '@divi/types';
import { type ModuleEditProps } from '../types-module';
declare global {
    interface Window {
        et_pb_tabs_init?: ($tabs: JQuery<HTMLElement>) => void;
    }
}
export type TabsEditProps = ModuleEditProps<TabsAttrs, unknown, TabAttrs>;
