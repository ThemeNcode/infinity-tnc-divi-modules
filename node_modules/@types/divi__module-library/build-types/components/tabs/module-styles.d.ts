import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type TabsAttrs } from '@divi/types';
/**
 * Tabs Module's style components.
 *
 * @since ??
 */
declare const ModuleStyles: ({ settings, mode, state, noStyleTag, orderClass, attrs, elements, }: StylesProps<TabsAttrs>) => ReactElement;
export { ModuleStyles, };
