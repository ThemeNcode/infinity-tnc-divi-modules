import { type ReactElement } from 'react';
import { type TabsEditProps } from './types';
/**
 * Tabs component of visual builder.
 *
 * @since ??
 *
 * @param {TabsEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const TabsEdit: ({ attrs, defaultPrintedStyleAttrs, elements, id, name, childrenIds, childModules, }: TabsEditProps) => ReactElement;
export { TabsEdit, };
