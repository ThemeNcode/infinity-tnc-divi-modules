import { type ModuleMetadata, type VideoSliderAttrs } from '@divi/types';
/**
 * Video Slider Meta Data.
 *
 * Note: The module metadata will be used to generate `module.json` upon build.
 * Variable name must end with `ModuleMetaData` to be picked up by the build script.
 */
declare const videoSliderModuleMetaData: ModuleMetadata<VideoSliderAttrs>;
export { videoSliderModuleMetaData, };
