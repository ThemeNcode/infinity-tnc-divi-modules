import { type ReactElement } from 'react';
import { type Module, type VideoSliderAttrs } from '@divi/types';
/**
 * Content panel component for the Video slider module settings modal.
 *
 * @since ??
 *
 * @param {Module.Settings.Panel.Props} param0 Content panel props.
 *
 * @returns {ReactElement}
 */
export declare const SettingsContent: ({ defaultSettingsAttrs, }: Module.Settings.Panel.Props<VideoSliderAttrs>) => ReactElement;
