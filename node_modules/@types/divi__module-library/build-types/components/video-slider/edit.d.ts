import { type ReactElement } from 'react';
import { type VideoSliderEditProps } from './types';
/**
 * Video Slider component of visual builder.
 *
 * @since ??
 *
 * @param {VideoSliderEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const VideoSliderEdit: ({ attrs, defaultPrintedStyleAttrs, id, name, childrenIds, elements, }: VideoSliderEditProps) => ReactElement;
export { VideoSliderEdit, };
