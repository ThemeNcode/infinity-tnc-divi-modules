import { type ReactElement } from 'react';
import { type ScriptDataProps } from '@types/divi__module';
import { type VideoSliderAttrs } from '@divi/types';
/**
 * Video slider module's script data component.
 *
 * @since ??
 *
 * @returns {ReactElement}
 */
export declare const ModuleScriptData: <TProps extends ScriptDataProps<VideoSliderAttrs>>({ elements, }: TProps) => ReactElement;
