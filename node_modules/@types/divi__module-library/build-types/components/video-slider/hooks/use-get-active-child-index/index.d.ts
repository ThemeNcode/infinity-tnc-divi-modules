import { type UseGetActiveChildIndex } from './types';
/**
 * Gets active child slide index.
 *
 * @returns {{
 * getActiveChildIndex: GetActiveChildIndex,
 * isModuleEdited: boolean
 * }} Returns an object consisted of:
 *
 * `getChildActiveSlideIndex()`: gets current active child slide index.
 *
 * `activeChildSlideIndex`: child slide index.
 *
 * `isModuleEdited`: tracks if module is edited or not`.
 */
export declare const useGetActiveChildIndex: () => UseGetActiveChildIndex;
