import { type ReactElement } from 'react';
import { type Module, type PortfolioAttrs } from '@divi/types';
/**
 * Content panel component for the Portfolio module settings modal.
 *
 * This component is registered as the `content` panel for the Portfolio module.
 *
 * @since ??
 *
 * @param {object} param0 Content panel props.
 * @param {object} param0.defaultSettingsAttrs Default settings attributes.
 *
 * @returns {ReactElement} Content panel component.
 */
export declare const SettingsContent: ({ defaultSettingsAttrs, }: Module.Settings.Panel.Props<PortfolioAttrs>) => ReactElement;
