import { type ReactElement } from 'react';
import { type PortfolioEditProps } from './types';
/**
 * Portfolio component of visual builder.
 *
 * @since ??
 *
 * @param {PortfolioEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const PortfolioEdit: ({ attrs, defaultPrintedStyleAttrs, id, name, elements, }: PortfolioEditProps) => ReactElement;
export { PortfolioEdit, };
