import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type PortfolioAttrs } from '@divi/types';
export declare const ModuleStyles: ({ attrs, elements, defaultPrintedStyleAttrs, orderClass, mode, state, noStyleTag, settings, }: StylesProps<PortfolioAttrs>) => ReactElement;
