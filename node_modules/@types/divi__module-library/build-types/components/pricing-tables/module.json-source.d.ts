import { type ModuleMetadata, type PricingTablesAttrs } from '@divi/types';
/**
 * Pricing Tables Meta Data.
 *
 * Note: The module metadata will be used to generate `module.json` upon build.
 * Variable name must end with `ModuleMetaData` to be picked up by the build script.
 */
declare const pricingTablesModuleMetaData: ModuleMetadata<PricingTablesAttrs>;
export { pricingTablesModuleMetaData, };
