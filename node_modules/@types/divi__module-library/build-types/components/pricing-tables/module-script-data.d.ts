import { type ReactElement } from 'react';
import { type ScriptDataProps } from '@types/divi__module';
import { type PricingTablesAttrs } from '@divi/types';
/**
 * Pricing Tables module's script data component.
 *
 * @since ??
 *
 * @returns {ReactElement}
 */
export declare const ModuleScriptData: <TProps extends ScriptDataProps<PricingTablesAttrs>>({ elements, }: TProps) => ReactElement;
