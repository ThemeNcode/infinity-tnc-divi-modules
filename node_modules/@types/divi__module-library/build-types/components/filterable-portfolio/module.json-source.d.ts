import { type FilterablePortfolioAttrs, type ModuleMetadata } from '@divi/types';
/**
 * Filterable Portfolio Meta Data.
 *
 * Note: The module metadata will be used to generate `module.json` upon build.
 * Variable name must end with `ModuleMetaData` to be picked up by the build script.
 */
declare const filterablePortfolioModuleMetaData: ModuleMetadata<FilterablePortfolioAttrs>;
export { filterablePortfolioModuleMetaData, };
