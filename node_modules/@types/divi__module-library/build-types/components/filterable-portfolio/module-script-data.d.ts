import { type ReactElement } from 'react';
import { type ModuleScriptDataProps } from '@types/divi__module';
import { type FilterablePortfolioAttrs } from '@divi/types';
/**
 * Filterable Portfolio module's script data component.
 *
 * @since ??
 *
 * @returns {ReactElement}
 */
export declare const ModuleScriptData: ({ elements, }: ModuleScriptDataProps<FilterablePortfolioAttrs>) => ReactElement;
