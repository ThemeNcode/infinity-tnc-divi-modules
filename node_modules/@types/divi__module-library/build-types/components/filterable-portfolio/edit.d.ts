import { type ReactElement } from 'react';
import { type FilterablePortfolioEditProps } from './types';
/**
 * Filterable Portfolio component of visual builder.
 *
 * @since ??
 *
 * @param {FilterablePortfolioEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const FilterablePortfolioEdit: ({ attrs, defaultPrintedStyleAttrs, id, name, elements, }: FilterablePortfolioEditProps) => ReactElement;
export { FilterablePortfolioEdit, };
