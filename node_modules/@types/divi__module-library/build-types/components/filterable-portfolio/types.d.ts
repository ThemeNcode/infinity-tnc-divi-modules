import { type FilterablePortfolioAttrs } from '@divi/types';
import { type ModuleEditProps } from '../types-module';
declare global {
    interface Window {
        et_pb_filterable_portfolio_init?: ($module: JQuery<HTMLElement>) => void;
        set_filterable_portfolio_init?: ($module: JQuery<HTMLElement>) => void;
    }
}
export type FilterablePortfolioEditProps = ModuleEditProps<FilterablePortfolioAttrs>;
export type CustomCssFieldKeys = 'portfolioFilters' | 'activePortfolioFilter' | 'portfolioImage' | 'overlay' | 'overlayIcon' | 'portfolioTitle' | 'portfolioPostMeta' | 'portfolioPagination' | 'portfolioPaginationActive';
