import { type ModuleMetadata, type SectionAttrs } from '@divi/types';
/**
 * Section Meta Data.
 *
 * Note: The module metadata will be used to generate `module.json` upon build.
 * Variable name must end with `ModuleMetaData` to be picked up by the build script.
 */
declare const sectionModuleMetaData: ModuleMetadata<SectionAttrs>;
export { sectionModuleMetaData, };
