import { type ReactElement } from 'react';
import { type SectionEditProps } from './types';
/**
 * Section component of visual builder.
 *
 * @since ??
 *
 * @param {SectionEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const SectionEdit: ({ attrs, elements, defaultPrintedStyleAttrs, childrenIds, id, isFirst, isLast, name, siblingAttrs, pageSettings, }: SectionEditProps) => ReactElement;
export { SectionEdit, };
