import { type DeclarationFunctionProps } from '@types/divi__module';
import { type Module } from '@divi/types';
/**
 * Get Section CSS declaration based on given attrValue.
 *
 * Generate zIndex Style only if the box shadow position is set as outer.
 *
 * @since ??
 *
 * @param {DeclarationFunctionProps<Module.Element.Decoration.BoxShadow.AttributeValue>} param0
 * Style declaration params.
 *
 * @returns {string | Record<string, string>}
 */
export declare const sectionBoxShadowStyleDeclaration: ({ attrValue, }: DeclarationFunctionProps<Module.Element.Decoration.BoxShadow.AttributeValue>) => string;
