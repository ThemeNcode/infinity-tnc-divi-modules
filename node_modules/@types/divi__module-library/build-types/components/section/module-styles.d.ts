import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type SectionAttrs } from '@divi/types';
/**
 * Section's style components.
 *
 * @since ??
 */
declare const ModuleStyles: ({ attrs, elements, defaultPrintedStyleAttrs, siblingAttrs, pageSettings, settings, mode, state, noStyleTag, }: StylesProps<SectionAttrs>) => ReactElement;
export { ModuleStyles, };
