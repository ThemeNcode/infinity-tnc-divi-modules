import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type SearchAttrs } from '@divi/types';
/**
 * Search Module's style components.
 *
 * @since ??
 */
declare const ModuleStyles: ({ mode, state, noStyleTag, orderClass, attrs, elements, defaultPrintedStyleAttrs, settings, }: StylesProps<SearchAttrs>) => ReactElement;
export { ModuleStyles, };
