import { type ReactElement } from 'react';
import { type ScriptDataProps } from '@types/divi__module';
import { type SearchAttrs } from '@divi/types';
/**
 * Search module's script data component.
 *
 * @since ??
 *
 * @returns {ReactElement}
 */
export declare const ModuleScriptData: <TProps extends ScriptDataProps<SearchAttrs>>({ elements, }: TProps) => ReactElement;
