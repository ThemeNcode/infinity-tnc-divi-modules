import { type ReactElement } from 'react';
import { type SearchEditProps } from './types';
/**
 * Search component of visual builder.
 *
 * @since ??
 *
 * @param {SearchEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const SearchEdit: ({ attrs, defaultPrintedStyleAttrs, id, name, elements, urls, }: SearchEditProps) => ReactElement;
export { SearchEdit, };
