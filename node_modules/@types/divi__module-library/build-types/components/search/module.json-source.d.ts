import { type ModuleMetadata, type SearchAttrs } from '@divi/types';
/**
 * Search Meta Data.
 *
 * Note: The module metadata will be used to generate `module.json` upon build.
 * Variable name must end with `ModuleMetaData` to be picked up by the build script.
 */
declare const searchModuleMetaData: ModuleMetadata<SearchAttrs>;
export { searchModuleMetaData, };
