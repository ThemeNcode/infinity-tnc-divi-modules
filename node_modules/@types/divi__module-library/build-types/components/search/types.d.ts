import { type SearchAttrs } from '@divi/types';
import { type ModuleEditProps } from '../types-module';
export interface CategoryAttr {
    id?: string;
    name?: string;
}
export interface SearchEditProps extends ModuleEditProps<SearchAttrs> {
    urls?: {
        homeUrl: string;
    };
}
