import { type ReactElement } from 'react';
import { type Module, type SearchAttrs } from '@divi/types';
/**
 * Design panel component for the Search module settings modal.
 *
 * @since ??
 *
 * @param {Module.Settings.Panel.Props} param0 Design panel props.
 *
 * @returns {ReactElement}
 */
export declare const SettingsDesign: ({ groupConfiguration, }: Module.Settings.Panel.Props<SearchAttrs>) => ReactElement;
