import { type ModuleMetadata, type TextAttrs } from '@divi/types';
/**
 * Text Meta Data.
 *
 * Note: The module metadata will be used to generate `module.json` upon build.
 * Variable name must end with `ModuleMetaData` to be picked up by the build script.
 */
declare const textModuleMetaData: ModuleMetadata<TextAttrs>;
export { textModuleMetaData, };
