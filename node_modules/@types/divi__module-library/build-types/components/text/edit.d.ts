import { type ReactElement } from 'react';
import { type TextEditProps } from './types';
/**
 * Text component of visual builder.
 *
 * @since ??
 *
 * @param {TextEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const TextEdit: ({ attrs, id, name, elements, }: TextEditProps) => ReactElement;
export { TextEdit, };
