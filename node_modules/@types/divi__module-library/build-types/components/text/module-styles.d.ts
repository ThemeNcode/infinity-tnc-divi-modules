import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type TextAttrs } from '@divi/types';
/**
 * Text Module's style components.
 *
 * @since ??
 */
declare const ModuleStyles: ({ attrs, elements, settings, mode, state, noStyleTag, }: StylesProps<TextAttrs>) => ReactElement;
export { ModuleStyles, };
