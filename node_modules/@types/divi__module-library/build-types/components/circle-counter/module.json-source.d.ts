import { type CircleCounterAttrs, type ModuleMetadata } from '@divi/types';
/**
 * Circle Counter Module Meta Data.
 *
 * Note: The module metadata will be used to generate `module.json` upon build.
 * Variable name must end with `ModuleMetaData` to be picked up by the build script.
 */
declare const circleCounterModuleMetaData: ModuleMetadata<CircleCounterAttrs>;
export { circleCounterModuleMetaData, };
