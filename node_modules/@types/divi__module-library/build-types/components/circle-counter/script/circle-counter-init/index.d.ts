import { type CircleCounterInit } from '../types';
/**
 * Initialize circle counter or update it if it's already initialized.
 *
 * @since ??
 *
 * @param {JQuery} $element The jQuery element that the circle counter is applied to.
 * @param {CircleCounterFilteredData} data  The circle counter data.
 * @param {boolean} disableAnimation Flag to disable animation.
 */
declare const circleCounterInit: CircleCounterInit;
export { circleCounterInit };
