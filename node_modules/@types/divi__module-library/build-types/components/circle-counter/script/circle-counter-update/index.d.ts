import { type CircleCounterUpdate } from '../types';
/**
 * Update circle counter easyPieChart data on custom mode.
 *
 * @since 3.25.3
 * @since ?? Refactored
 *
 * @param {JQuery}                    $element    Circle counter jQuery element.
 * @param {CircleCounterFilteredData} data        Circle counter data.
 * @param {boolean}                   forceUpdate Flag to destroy the existing instance and create a new one.
 */
declare const circleCounterUpdate: CircleCounterUpdate;
export { circleCounterUpdate };
