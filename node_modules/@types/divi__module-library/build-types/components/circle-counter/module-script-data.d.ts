import { type ReactElement } from 'react';
import { type ScriptDataProps } from '@types/divi__module';
import { type CircleCounterAttrs } from '@divi/types';
/**
 * Circle Counter module's script data component.
 *
 * @since ??
 *
 * @returns {ReactElement}
 */
export declare const ModuleScriptData: <TProps extends ScriptDataProps<CircleCounterAttrs>>({ elements, }: TProps) => ReactElement;
