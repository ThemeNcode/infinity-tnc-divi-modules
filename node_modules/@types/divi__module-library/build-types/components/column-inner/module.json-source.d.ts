import { type ColumnAttrs, type ModuleMetadata } from '@divi/types';
/**
 * Column Inner Meta Data.
 *
 * Note: The module metadata will be used to generate `module.json` upon build.
 * Variable name must end with `ModuleMetaData` to be picked up by the build script.
 */
declare const columnInnerModuleMetaData: ModuleMetadata<ColumnAttrs>;
export { columnInnerModuleMetaData, };
