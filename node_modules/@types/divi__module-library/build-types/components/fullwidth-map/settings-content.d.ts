import { type ReactElement } from 'react';
import { type FullwidthMapAttrs, type Module } from '@divi/types';
/**
 * Content panel component for the Full Width Map module settings modal.
 *
 * @since ??
 *
 * @param {Module.Settings.Panel.Props} param0 Content panel props.
 *
 * @returns {ReactElement}
 */
export declare const SettingsContent: ({ defaultSettingsAttrs, }: Module.Settings.Panel.Props<FullwidthMapAttrs>) => ReactElement;
