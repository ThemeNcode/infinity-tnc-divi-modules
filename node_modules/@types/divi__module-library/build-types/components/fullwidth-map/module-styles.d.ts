import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type FullwidthMapAttrs } from '@divi/types';
declare const ModuleStyles: <TProps extends StylesProps<FullwidthMapAttrs>>({ attrs, elements, settings, orderClass, mode, state, noStyleTag, }: TProps) => ReactElement;
export { ModuleStyles, };
