import { type ReactElement } from 'react';
import { type ScriptDataProps } from '@types/divi__module';
import { type FullwidthMapAttrs } from '@divi/types';
/**
 * Fullwidth Map module's script data component.
 *
 * @since ??
 *
 * @returns {ReactElement}
 */
export declare const ModuleScriptData: <TProps extends ScriptDataProps<FullwidthMapAttrs>>({ elements, }: TProps) => ReactElement;
