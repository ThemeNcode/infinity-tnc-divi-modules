import { type FullwidthMapAttrs } from '@divi/types';
import { type ModuleEditProps } from '../types-module';
declare global {
    interface Window {
        ETBuilderInitGoogleMaps?: () => void;
        et_pb_map_init?: ($node: JQuery) => void;
    }
}
export type FullwidthMapEditProps = ModuleEditProps<FullwidthMapAttrs>;
