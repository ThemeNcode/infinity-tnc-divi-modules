import { type FullwidthMapAttrs, type ModuleMetadata } from '@divi/types';
/**
 * Fullwidth Map Meta Data.
 *
 * Note: The module metadata will be used to generate `module.json` upon build.
 * Variable name must end with `ModuleMetaData` to be picked up by the build script.
 */
declare const fullwidthMapModuleMetaData: ModuleMetadata<FullwidthMapAttrs>;
export { fullwidthMapModuleMetaData, };
