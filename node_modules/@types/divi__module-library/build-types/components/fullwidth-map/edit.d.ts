import { type ReactElement } from 'react';
import { type FullwidthMapEditProps } from './types';
/**
 * FullwidthMap component of visual builder.
 *
 * @since ??
 *
 * @param {FullwidthMapEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const FullwidthMapEdit: ({ attrs, elements, id, childrenIds, name, }: FullwidthMapEditProps) => ReactElement;
export { FullwidthMapEdit, };
