import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type TabAttrs } from '@divi/types';
/**
 * Tab Module's style components.
 *
 * @since ??
 */
declare const ModuleStyles: ({ attrs, elements, mode, state, noStyleTag, }: StylesProps<TabAttrs>) => ReactElement;
export { ModuleStyles, };
