import { type ReactElement } from 'react';
import { type TabEditProps } from './types';
/**
 * Tab component of visual builder.
 *
 * @since ??
 *
 * @param {TabEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const TabEdit: ({ attrs, id, name, isFirst, elements, parentId, parentName, }: TabEditProps) => ReactElement;
export { TabEdit, };
