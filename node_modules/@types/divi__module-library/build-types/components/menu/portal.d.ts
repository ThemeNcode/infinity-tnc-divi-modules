import { type ReactPortal } from 'react';
import { type MenuPortalProps } from './types';
/**
 * <Portal />.
 *
 * Used to render a portal inside a dynamically appended <li> element
 * to the menu which is rendered using dangerouslySetInnerHTML.
 *
 * @since ??
 *
 * @param {MenuPortalProps} props React component props.
 *
 * @returns {ReactPortal} The react portal. Will return null if no slot is falsy.
 */
export declare const Portal: ({ slot, children }: MenuPortalProps) => ReactPortal;
