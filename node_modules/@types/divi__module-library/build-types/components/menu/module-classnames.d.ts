import { type ModuleClassnamesParams } from '@types/divi__module';
import { type MenuAttrs } from '@divi/types';
/**
 * Module classnames function for Menu module.
 *
 * @since ??
 *
 * @param {ModuleClassnamesParams<MenuAttrs>} param0 Function parameters.
 */
export declare const moduleClassnames: ({ classnamesInstance, attrs, breakpoint, state, }: ModuleClassnamesParams<MenuAttrs>) => void;
