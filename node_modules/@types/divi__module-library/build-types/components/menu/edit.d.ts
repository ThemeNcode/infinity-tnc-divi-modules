import { type ReactElement } from 'react';
import { type MenuEditProps } from './types';
/**
 * Menu component of visual builder.
 *
 * @since ??
 *
 * @param { MenuEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const MenuEdit: ({ attrs, defaultPrintedStyleAttrs, id, name, elements, }: MenuEditProps) => ReactElement;
export { MenuEdit, };
