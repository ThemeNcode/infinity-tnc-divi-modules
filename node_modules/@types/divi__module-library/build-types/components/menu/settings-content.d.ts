import { type ReactElement } from 'react';
import { type MenuAttrs, type Module } from '@divi/types';
/**
 * Content panel component for the Menu module settings modal.
 *
 * This component is registered as the `content` panel for the Menu module.
 *
 * @since ??
 *
 * @param {Module.Settings.Panel.Props} param0 Content panel props.
 *
 * @returns {ReactElement} Content panel component.
 */
export declare const SettingsContent: ({ attrs, groupConfiguration, }: Module.Settings.Panel.Props<MenuAttrs>) => ReactElement;
