import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type MenuAttrs } from '@divi/types';
/**
 * Menu Module's style components.
 *
 * @since ??
 */
export declare const ModuleStyles: ({ attrs, elements, defaultPrintedStyleAttrs, orderClass, mode, state, noStyleTag, settings, }: StylesProps<MenuAttrs>) => ReactElement;
