import { type ReactElement } from 'react';
import { type MenuAttrs, type Module } from '@divi/types';
/**
 * Design panel component for the Menu module settings modal.
 *
 * @since ??
 *
 * @param {Module.Settings.Panel.Props} param0 Design panel props.
 *
 * @returns {ReactElement}
 */
export declare const SettingsDesign: ({ attrs, groupConfiguration, }: Module.Settings.Panel.Props<MenuAttrs>) => ReactElement;
