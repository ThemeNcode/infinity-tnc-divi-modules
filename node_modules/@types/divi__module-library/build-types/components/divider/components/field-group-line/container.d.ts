import { type ReactElement } from 'react';
import { type FieldGroupLineContainerProps } from './types';
/**
 * Container component of <FieldGroupLine /> that automatically passes props based on store state.
 *
 * @since ??
 *
 * @returns {ReactElement}
 */
export declare const FieldGroupLineContainer: (props: FieldGroupLineContainerProps) => ReactElement;
