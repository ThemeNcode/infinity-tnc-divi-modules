import { type ReactElement } from 'react';
import { type DividerEditProps } from './types';
/**
 * Divider component of visual builder.
 *
 * @since ??
 *
 * @param {DividerEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const DividerEdit: ({ attrs, elements, id, name, }: DividerEditProps) => ReactElement;
export { DividerEdit, };
