import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type DividerAttrs } from '@divi/types';
/**
 * Divider Module's style components.
 *
 * @since ??
 */
declare const ModuleStyles: ({ settings, mode, state, noStyleTag, orderClass, attrs, defaultPrintedStyleAttrs, elements, }: StylesProps<DividerAttrs>) => ReactElement;
export { ModuleStyles, };
