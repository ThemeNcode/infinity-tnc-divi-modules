import { type ReactElement } from 'react';
import { type SliderEditProps } from './types';
/**
 * Text component of visual builder.
 *
 * @since ??
 *
 * @param {SliderEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const SliderEdit: ({ attrs, elements, defaultPrintedStyleAttrs, id, name, childrenIds, }: SliderEditProps) => ReactElement;
export { SliderEdit, };
