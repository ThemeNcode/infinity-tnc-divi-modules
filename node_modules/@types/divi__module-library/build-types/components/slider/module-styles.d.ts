import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type SliderAttrs } from '@divi/types';
/**
 * Text Module's style components.
 *
 * @since ??
 */
declare const ModuleStyles: ({ settings, mode, state, noStyleTag, orderClass, attrs, elements, defaultPrintedStyleAttrs, }: StylesProps<SliderAttrs>) => ReactElement;
export { ModuleStyles, };
