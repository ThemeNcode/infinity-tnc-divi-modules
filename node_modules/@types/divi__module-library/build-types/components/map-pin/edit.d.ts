import { type ReactElement } from 'react';
import { type MapPinEditProps } from './types';
/**
 * Map component of visual builder.
 *
 * @since ??
 *
 * @param {MapPinEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const MapPinEdit: ({ attrs, id, name, parentAttrs, elements, }: MapPinEditProps) => ReactElement;
export { MapPinEdit, };
