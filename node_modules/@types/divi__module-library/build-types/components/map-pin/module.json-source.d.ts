import { type MapPinAttrs, type ModuleMetadata } from '@divi/types';
/**
 * Map Pin Meta Data.
 *
 * Note: The module metadata will be used to generate `module.json` upon build.
 * Variable name must end with `ModuleMetaData` to be picked up by the build script.
 */
declare const mapPinModuleMetaData: ModuleMetadata<MapPinAttrs>;
export { mapPinModuleMetaData, };
