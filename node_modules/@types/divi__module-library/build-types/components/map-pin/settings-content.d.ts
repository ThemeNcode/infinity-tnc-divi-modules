import { type ReactElement } from 'react';
import { type MapPinAttrs, type Module } from '@divi/types';
/**
 * Content panel component for the Map Pin module settings modal.
 *
 * @since ??
 *
 * @param {Module.Settings.Panel.Props} param0 Content panel props.
 *
 * @returns {ReactElement}
 */
export declare const SettingsContent: ({ defaultSettingsAttrs, }: Module.Settings.Panel.Props<MapPinAttrs>) => ReactElement;
