import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type SidebarAttrs } from '@divi/types';
/**
 * Sidebar Module's style components.
 *
 * @since ??
 */
declare const ModuleStyles: <TProps extends StylesProps<SidebarAttrs>>({ attrs, elements, orderClass, mode, settings, state, noStyleTag, }: TProps) => ReactElement;
export { ModuleStyles, };
