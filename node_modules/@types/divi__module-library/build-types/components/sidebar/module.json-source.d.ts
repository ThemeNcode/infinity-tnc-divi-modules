import { type ModuleMetadata, type SidebarAttrs } from '@divi/types';
/**
 * Sidebar Meta Data.
 *
 * Note: The module metadata will be used to generate `module.json` upon build.
 * Variable name must end with `ModuleMetaData` to be picked up by the build script.
 */
declare const sidebarModuleMetaData: ModuleMetadata<SidebarAttrs>;
export { sidebarModuleMetaData, };
