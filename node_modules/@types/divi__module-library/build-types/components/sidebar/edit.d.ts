import { type ReactElement } from 'react';
import { type SidebarEditProps } from './types';
/**
 * Sidebar Edit component of visual builder.
 *
 * @since ??
 *
 * @param {SidebarEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const SidebarEdit: ({ attrs, defaultPrintedStyleAttrs, id, name, elements, }: SidebarEditProps) => ReactElement;
export { SidebarEdit, };
