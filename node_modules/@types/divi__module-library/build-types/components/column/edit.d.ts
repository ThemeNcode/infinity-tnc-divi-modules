import { type ReactElement } from 'react';
import { type ColumnEditProps } from './types';
/**
 * Column component of visual builder.
 *
 * @since ??
 *
 * @param {ColumnEditProps} props Component props.
 *
 * @returns {ReactElement}
 */
declare const ColumnEdit: ({ attrs, elements, defaultPrintedStyleAttrs, childrenIds, id, isLast, name, parentName, parentId, }: ColumnEditProps) => ReactElement;
export { ColumnEdit, };
