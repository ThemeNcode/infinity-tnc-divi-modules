import { type ModuleClassnamesParams } from '@types/divi__module';
import { type ColumnAttrs } from '@divi/types';
/**
 * Module classnames function for column module.
 *
 * @since ??
 *
 * @param {ModuleClassnamesParams<ColumnAttrs>} param0 Function parameters.
 */
export declare const moduleClassnames: ({ attrs, classnamesInstance, name, isLast, breakpoint, state, }: ModuleClassnamesParams<ColumnAttrs>) => void;
