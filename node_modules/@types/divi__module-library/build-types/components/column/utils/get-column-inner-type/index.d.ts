/**
 * Get column inner's column type which is affected by its own column type and column parent's type.
 *
 * @since ??
 *
 * @param {string} sectionColumnType Section column type which is passed down as .
 * @param {string} columnType Column inner's column type.
 *
 * @returns {string}
 */
export declare const getColumnInnerType: (sectionColumnType: string, columnType: string) => string;
