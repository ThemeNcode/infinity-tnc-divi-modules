import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type ColumnAttrs } from '@divi/types';
/**
 * Column's style components.
 *
 * @since ??
 *
 * @param {StylesProps<ColumnAttrs>} param0 Column style component props.
 *
 * @returns {ReactElement}
 */
declare const ModuleStyles: ({ attrs, elements, defaultPrintedStyleAttrs, settings, mode, state, noStyleTag, }: StylesProps<ColumnAttrs>) => ReactElement;
export { ModuleStyles, };
