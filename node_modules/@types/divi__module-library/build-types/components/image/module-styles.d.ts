import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type ImageAttrs } from '@divi/types';
/**
 * Image Module's style components.
 *
 * @since ??
 */
declare const ModuleStyles: ({ attrs, elements, settings, orderClass, mode, state, noStyleTag, }: StylesProps<ImageAttrs>) => ReactElement;
export { ModuleStyles, };
