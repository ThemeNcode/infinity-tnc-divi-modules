import { type ReactElement } from 'react';
import { type ScriptDataProps } from '@types/divi__module';
import { type ImageAttrs } from '@divi/types';
/**
 * Image module's script data component.
 *
 * @since ??
 *
 * @returns {ReactElement}
 */
export declare const ModuleScriptData: <TProps extends ScriptDataProps<ImageAttrs>>({ elements, }: TProps) => ReactElement;
