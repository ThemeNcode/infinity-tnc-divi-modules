import { type ModuleClassnamesParams } from '@types/divi__module';
import { type ImageAttrs } from '@divi/types';
/**
 * Module classnames function for image module.
 *
 * @since ??
 *
 * @param {ModuleClassnamesParams<ImageAttrs>} param0 Function parameters.
 */
export declare const moduleClassnames: ({ classnamesInstance, attrs, breakpoint, state, }: ModuleClassnamesParams<ImageAttrs>) => void;
