import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
declare const name = "divi/link";
/**
 * Module field group component: Link Option.
 *
 * @since ??
 *
 * @param {Module.Settings.Group.Library.Link.GroupProps} props Component props.
 *
 * @returns {ReactElement}
 */
declare const FieldGroupLink: {
    ({ grouped, groupLabel, attrName, visible, }: Module.Settings.Group.Library.Link.GroupProps): ReactElement;
    /**
     * Static `groupName` is required so `<GroupContainer />` can recognize nested module field group
     * component that is used inside it. This is used for copy paste style.
     *
     * @since ??
     */
    groupName: string;
};
/**
 * Export alias for consistent named export on all module field groups component.
 *
 * @since ??
 */
declare const component: {
    ({ grouped, groupLabel, attrName, visible, }: Module.Settings.Group.Library.Link.GroupProps): ReactElement;
    /**
     * Static `groupName` is required so `<GroupContainer />` can recognize nested module field group
     * component that is used inside it. This is used for copy paste style.
     *
     * @since ??
     */
    groupName: string;
};
export { name, FieldGroupLink, component, };
