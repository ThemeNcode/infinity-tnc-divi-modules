import { type ReactElement } from 'react';
import { type ImageModuleSizingStyleProps } from './types';
/**
 * Get sizing (width, max-width, alignment, min-height, height, max-height) styles.
 *
 * @since ??
 *
 * @param {ImageModuleSizingStyleProps} styleParams Parameter for CSS declaration.
 *
 * @returns {ReactElement}
 */
declare const SizingStyle: ({ selector, imageSelector, attr, important, asStyle, orderClass, }: ImageModuleSizingStyleProps) => ReactElement;
export { SizingStyle, };
