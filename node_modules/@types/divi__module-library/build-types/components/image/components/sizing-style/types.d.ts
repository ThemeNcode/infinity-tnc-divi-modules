import { type StyleProps } from '@types/divi__module';
import { type SizingStyleCssProperties } from '@types/divi__style-library';
import { type FormatStyleImportant, type Module } from '@divi/types';
export interface ImageModuleSizingStyleProps extends StyleProps<Module.Element.Decoration.Sizing.Attributes> {
    imageSelector: string;
    important?: boolean | FormatStyleImportant<SizingStyleCssProperties>;
}
export interface DeclarationParams {
    important: boolean;
    attrValue: Module.Element.Decoration.Sizing.AttributeValue;
}
