import { type ReactElement } from 'react';
import { type Props } from './types';
/**
 * Specific module settings group for image module's overlay group.
 *
 * @since ??
 *
 * @param {Props} param0 Component props.
 *
 * @returns {ReactElement}
 */
export declare const FieldGroupOverlay: ({ visible, }: Props) => ReactElement;
