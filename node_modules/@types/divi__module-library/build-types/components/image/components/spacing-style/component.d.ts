import { type ReactElement } from 'react';
import { type ImageModuleSpacingStyleProps } from './types';
/**
 * Spacing Style.
 *
 * Get spacing (margin & padding) CSS statements as `style` element.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.selector Selector for the style.
 * @param {string} props.attr Attribute for the style.
 * @param {boolean} props.important Whether to add `!important` to the style or not.
 * @param {boolean} props.asStyle Optional. Whether to return the style as a style or as a string.
 * Defaults to `true`.
 * @param {string} props.orderClass Optional. The selector class name.
 *
 * @returns {ReactElement}
 */
export declare const SpacingStyle: ({ selector, attr, important, asStyle, orderClass, }: ImageModuleSpacingStyleProps) => ReactElement;
