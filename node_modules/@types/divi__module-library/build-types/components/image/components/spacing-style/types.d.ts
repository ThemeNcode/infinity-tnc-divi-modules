import { type StyleProps } from '@types/divi__module';
import { type SpacingStyleCssProperties } from '@types/divi__style-library';
import { type FormatStyleImportant, type ImageSpacingGroupAttr, type Module } from '@divi/types';
export interface SpacingAttr {
    margin?: Module.Element.Decoration.Spacing.ItemValue;
    padding?: Module.Element.Decoration.Spacing.ItemValue;
    showBottomSpace?: string;
}
export interface DeclarationParams {
    important: boolean;
    attrValue: SpacingAttr;
}
export interface ImageModuleSpacingStyleProps extends StyleProps<ImageSpacingGroupAttr> {
    important?: boolean | FormatStyleImportant<SpacingStyleCssProperties>;
}
