import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
declare const name = "divi/image-attributes";
/**
 * Module field group component: image module's attributes group.
 *
 * @since ??
 *
 * @param {Module.Settings.Group.Library.AttributesRel.GroupProps} props Component props.
 *
 * @returns {ReactElement}
 */
declare const FieldGroupAttributes: {
    ({ grouped, groupLabel, attrName, visible, defaultGroupAttr, }: Module.Settings.Group.Library.AttributesRel.GroupProps): ReactElement;
    /**
     * Static `groupName` is required so `<GroupContainer />` can recognize nested module field group
     * component that is used inside it. This is used for copy paste style.
     *
     * @since ??
     */
    groupName: string;
};
/**
 * Export alias for consistent named export on all module field groups component.
 *
 * @since ??
 */
declare const component: {
    ({ grouped, groupLabel, attrName, visible, defaultGroupAttr, }: Module.Settings.Group.Library.AttributesRel.GroupProps): ReactElement;
    /**
     * Static `groupName` is required so `<GroupContainer />` can recognize nested module field group
     * component that is used inside it. This is used for copy paste style.
     *
     * @since ??
     */
    groupName: string;
};
export { name, FieldGroupAttributes, component, };
