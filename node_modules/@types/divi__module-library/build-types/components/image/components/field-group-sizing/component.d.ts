import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
declare const name = "divi/sizing";
/**
 * Field Group Sizing.
 *
 * Module field group component for Sizing in image module.
 * How this differs from the usual `<Image />`:
 * - Force Fullwidth option
 * - Width, max width, and module alignment is hidden when Force Fullwidth is on.
 *
 * @since ??
 *
 * @param {object} props Module Field Group properties.
 * @param {boolean} props.grouped Optional. Whether the group is grouped. Defaults to `true`.
 * @param {string} props.groupLabel Optional group label. Defaults to `Sizing`.
 * @param {string} props.attrName Optional attribute name. Defaults to `module.decoration.sizing`.
 * @param {string} props.fieldLabel Optional field label. Defaults to empty string.
 * @param {boolean} props.visible Optional. Whether the group is visible or not. Defaults to `true`.
 * @param {object} props.defaultGroupAttr Default group attribute.
 *
 * @returns {ReactElement}
 */
declare const FieldGroupSizing: {
    ({ grouped, groupLabel, attrName, fieldLabel, visible, defaultGroupAttr, }: Module.Settings.Group.Library.Sizing.GroupProps): ReactElement;
    /**
     * Static `groupName` is required so `<GroupContainer />` can recognize nested module field group
     * component that is used inside it. This is used for copy paste style.
     *
     * @since ??
     */
    groupName: string;
};
/**
 * Export alias for consistent named export on all module field groups component.
 *
 * @since ??
 */
declare const component: {
    ({ grouped, groupLabel, attrName, fieldLabel, visible, defaultGroupAttr, }: Module.Settings.Group.Library.Sizing.GroupProps): ReactElement;
    /**
     * Static `groupName` is required so `<GroupContainer />` can recognize nested module field group
     * component that is used inside it. This is used for copy paste style.
     *
     * @since ??
     */
    groupName: string;
};
export { name, FieldGroupSizing, component, };
