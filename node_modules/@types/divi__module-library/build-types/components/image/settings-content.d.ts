import { type ReactElement } from 'react';
import { type ImageAttrs, type Module } from '@divi/types';
/**
 * Content panel component for the Search module settings modal.
 *
 * @since ??
 *
 * @param {Module.Settings.Panel.Props} param0 Content panel props.
 *
 * @returns {ReactElement}
 */
export declare const SettingsContent: ({ attrs, groupConfiguration, }: Module.Settings.Panel.Props<ImageAttrs>) => ReactElement;
