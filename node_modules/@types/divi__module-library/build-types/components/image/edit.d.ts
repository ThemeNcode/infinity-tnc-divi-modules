import { type ReactElement } from 'react';
import { type ImageEditProps } from './types';
/**
 * Image component of visual builder.
 *
 * @since ??
 *
 * @param {ImageEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const ImageEdit: ({ attrs, id, name, elements, }: ImageEditProps) => ReactElement;
export { ImageEdit, };
