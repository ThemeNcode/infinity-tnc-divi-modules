import { type ImageAttrs, type Module } from '@divi/types';
/**
 * Determine whether `Width`, `Max Width`, and `Alignment` should be visible or not
 * based on value of `Force Fullwidth` field.
 *
 * @since ??
 *
 * @param {Module.Settings.Field.CallbackParams} params Function parameters.
 *
 * @returns {boolean}
 */
export declare const onHorizontalSizingVisible: (params: Module.Settings.Field.CallbackParams<ImageAttrs>) => boolean;
