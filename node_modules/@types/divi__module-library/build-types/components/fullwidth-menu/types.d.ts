import { type ReactNode } from 'react';
import { type FullwidthMenuAttrs, type Module } from '@divi/types';
import { type ModuleEditProps } from '../types-module';
declare global {
    interface Window {
        et_pb_menu_inject_inline_centered_logo?: (element: HTMLElement) => HTMLElement;
        et_pb_reposition_menu_module_dropdowns?: (element: HTMLElement) => void;
    }
}
export type FullwidthMenuEditProps = ModuleEditProps<FullwidthMenuAttrs>;
export type FullwidthMenuFieldCallbackParams = Module.Settings.Field.CallbackParams<FullwidthMenuAttrs>;
export interface FullwidthMenuTerm {
    term_id: number;
    name: string;
    slug: string;
    term_group: number;
    term_taxonomy_id: number;
    taxonomy: string;
    description: string;
    parent: number;
    count: number;
    filter: string;
}
export interface FullwidthMenuPortalProps {
    slot: HTMLElement;
    children: ReactNode;
}
