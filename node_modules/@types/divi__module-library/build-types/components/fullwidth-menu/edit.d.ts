import { type ReactElement } from 'react';
import { type FullwidthMenuEditProps } from './types';
/**
 * Fullwidth Menu component of visual builder.
 *
 * @since ??
 *
 * @param { FullwidthMenuEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const FullwidthMenuEdit: ({ attrs, defaultPrintedStyleAttrs, id, name, elements, }: FullwidthMenuEditProps) => ReactElement;
export { FullwidthMenuEdit, };
