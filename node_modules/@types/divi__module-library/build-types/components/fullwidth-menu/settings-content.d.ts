import { type ReactElement } from 'react';
import { type FullwidthMenuAttrs, type Module } from '@divi/types';
/**
 * Content panel component for the Fullwidth Menu module settings modal.
 *
 * This component is registered as the `content` panel for the Fullwidth Menu module.
 *
 * @since ??
 *
 * @param {object} param0 Content panel props.
 * @param {object} param0.attrs Fullwidth menu attributes.
 * @param {object} param0.groupConfiguration Module group configuration.
 *
 * @returns {ReactElement} Content panel component.
 */
export declare const SettingsContent: ({ attrs, groupConfiguration, }: Module.Settings.Panel.Props<FullwidthMenuAttrs>) => ReactElement;
