import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type FullwidthMenuAttrs } from '@divi/types';
/**
 * Fullwidth Menu Module's style components.
 *
 * @since ??
 */
export declare const ModuleStyles: ({ attrs, elements, defaultPrintedStyleAttrs, orderClass, mode, state, noStyleTag, settings, }: StylesProps<FullwidthMenuAttrs>) => ReactElement;
