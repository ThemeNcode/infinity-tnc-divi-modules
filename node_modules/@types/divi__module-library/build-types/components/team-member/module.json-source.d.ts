import { type ModuleMetadata, type TeamMemberAttrs } from '@divi/types';
/**
 * Team Member Meta Data.
 *
 * Note: The module metadata will be used to generate `module.json` upon build.
 * Variable name must end with `ModuleMetaData` to be picked up by the build script.
 */
declare const teamMemberModuleMetaData: ModuleMetadata<TeamMemberAttrs>;
export { teamMemberModuleMetaData, };
