import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type TeamMemberAttrs } from '@divi/types';
/**
 * Text Module's style components.
 *
 * @since ??
 */
declare const ModuleStyles: ({ attrs, elements, settings, orderClass, mode, state, noStyleTag, }: StylesProps<TeamMemberAttrs>) => ReactElement;
export { ModuleStyles, };
