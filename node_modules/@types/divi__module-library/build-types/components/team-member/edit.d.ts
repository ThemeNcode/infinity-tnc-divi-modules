import { type ReactElement } from 'react';
import { type TeamMemberEditProps } from './types';
/**
 * Team Member component of visual builder.
 *
 * @since ??
 *
 * @param {TeamMemberEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const TeamMemberEdit: ({ attrs, id, name, elements, }: TeamMemberEditProps) => ReactElement;
export { TeamMemberEdit, };
