import { type ReactElement } from 'react';
import { type ScriptDataProps } from '@types/divi__module';
import { type CodeAttrs } from '@divi/types';
/**
 * Code module's script data component.
 *
 * @since ??
 *
 * @returns {ReactElement}
 */
export declare const ModuleScriptData: <TProps extends ScriptDataProps<CodeAttrs>>({ elements, }: TProps) => ReactElement;
