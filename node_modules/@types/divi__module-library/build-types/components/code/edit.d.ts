import { type ReactElement } from 'react';
import { type CodeEditProps } from './types';
/**
 * Code Edit component of visual builder.
 *
 * @since ??
 *
 * @param {CodeEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const CodeEdit: ({ attrs, id, name, elements, }: CodeEditProps) => ReactElement;
export { CodeEdit, };
