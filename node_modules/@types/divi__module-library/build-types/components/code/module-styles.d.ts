import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type CodeAttrs } from '@divi/types';
/**
 * Code Module's style components.
 *
 * @since ??
 */
declare const ModuleStyles: <TProps extends StylesProps<CodeAttrs>>({ attrs, elements, mode, settings, state, noStyleTag, }: TProps) => ReactElement;
export { ModuleStyles, };
