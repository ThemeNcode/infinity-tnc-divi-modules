import { type ReactElement } from 'react';
import { type HeadingEditProps } from './types';
/**
 * Heading edit component of visual builder.
 *
 * @since ??
 *
 * @param {HeadingEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const HeadingEdit: ({ attrs, defaultPrintedStyleAttrs, id, name, elements, }: HeadingEditProps) => ReactElement;
export { HeadingEdit, };
