import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type HeadingAttrs } from '@divi/types';
/**
 * Heading Module's style components.
 *
 * @since ??
 */
declare const ModuleStyles: <TProps extends StylesProps<HeadingAttrs>>({ attrs, defaultPrintedStyleAttrs, elements, settings, orderClass, mode, state, noStyleTag, }: TProps) => ReactElement;
export { ModuleStyles, };
