import { type HeadingAttrs, type ModuleMetadata } from '@divi/types';
/**
 * Heading Module Meta Data.
 *
 * Note: The module metadata will be used to generate `module.json` upon build.
 * Variable name must end with `ModuleMetaData` to be picked up by the build script.
 */
declare const headingModuleMetaData: ModuleMetadata<HeadingAttrs>;
export { headingModuleMetaData, };
