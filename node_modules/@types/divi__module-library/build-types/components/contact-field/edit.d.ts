import { type ReactElement } from 'react';
import { type ContactFieldEditProps } from './types';
/**
 * Edit component of visual builder.
 *
 * @since ??
 *
 * @param { ContactFieldEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const ContactFieldEdit: ({ attrs, id, name, elements, defaultPrintedStyleAttrs, moduleOrderIndex, }: ContactFieldEditProps) => ReactElement;
export { ContactFieldEdit, };
