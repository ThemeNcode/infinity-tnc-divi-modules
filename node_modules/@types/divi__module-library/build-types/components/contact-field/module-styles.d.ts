import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type ContactFieldAttrs } from '@divi/types';
/**
 * Set CSS styles to the module.
 *
 * @param {StylesProps<ContactFieldAttrs>} props React component props.
 *
 * @returns {ReactElement} The CSS styles for the module.
 */
export declare const ModuleStyles: ({ attrs, elements, defaultPrintedStyleAttrs, orderClass, mode, state, noStyleTag, settings, }: StylesProps<ContactFieldAttrs>) => ReactElement;
