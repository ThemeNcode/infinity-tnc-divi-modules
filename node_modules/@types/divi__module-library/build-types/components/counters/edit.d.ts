import { type ReactElement } from 'react';
import { type BarCountersEditProps } from './types';
/**
 * Bar Counter component of visual builder.
 *
 * @since ??
 *
 * @param {BarCountersEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const BarCountersEdit: ({ attrs, defaultPrintedStyleAttrs, elements, childrenIds, id, name, }: BarCountersEditProps) => ReactElement;
export { BarCountersEdit, };
