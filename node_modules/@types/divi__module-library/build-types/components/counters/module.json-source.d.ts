import { type BarCountersAttrs, type ModuleMetadata } from '@divi/types';
/**
 * Bar Counters Meta Data.
 *
 * Note: The module metadata will be used to generate `module.json` upon build.
 * Variable name must end with `ModuleMetaData` to be picked up by the build script.
 */
declare const countersModuleMetaData: ModuleMetadata<BarCountersAttrs>;
export { countersModuleMetaData, };
