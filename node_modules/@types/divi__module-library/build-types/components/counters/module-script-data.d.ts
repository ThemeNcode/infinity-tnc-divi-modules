import { type ReactElement } from 'react';
import { type ScriptDataProps } from '@types/divi__module';
import { type BarCountersAttrs } from '@divi/types';
/**
 * Bar Counters module's script data component.
 *
 * @since ??
 *
 * @returns {ReactElement}
 */
export declare const ModuleScriptData: <TProps extends ScriptDataProps<BarCountersAttrs>>({ elements, }: TProps) => ReactElement;
