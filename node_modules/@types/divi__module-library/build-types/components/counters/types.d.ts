import { type BarCountersAttrs } from '@divi/types';
import { type ModuleEditProps } from '../types-module';
declare global {
    interface Window {
        et_bar_counters_init?: ($counter: JQuery) => ReturnType<typeof setTimeout>;
    }
}
export type BarCountersEditProps = ModuleEditProps<BarCountersAttrs>;
