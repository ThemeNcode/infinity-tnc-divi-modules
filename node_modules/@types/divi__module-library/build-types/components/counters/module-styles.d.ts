import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type BarCountersAttrs } from '@divi/types';
/**
 * Text Module's style components.
 *
 * @since ??
 */
declare const ModuleStyles: <TProps extends StylesProps<BarCountersAttrs>>({ attrs, elements, settings, mode, state, noStyleTag, }: TProps) => ReactElement;
export { ModuleStyles, };
