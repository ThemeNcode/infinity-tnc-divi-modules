import { type NumberCounterInit } from '../types';
/**
 * Initialize number counter or update if it's already initialized.
 *
 * @since ??
 *
 * @param {JQuery}                    $element The jQuery element that the number counter is applied to.
 * @param {NumberCounterFilteredData} data     The number counter data.
 */
declare const numberCounterInit: NumberCounterInit;
export { numberCounterInit };
