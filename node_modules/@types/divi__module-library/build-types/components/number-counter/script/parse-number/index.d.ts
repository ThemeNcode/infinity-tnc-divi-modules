import { type ParseNumber } from './types';
/**
 * It takes a number or a string and returns an object with the number's value, the separator (if any)
 * and the number of decimals (if any).
 *
 * @since
 *
 * @param {string | number} number The string to be parsed.
 *
 * @returns {ParseNumber}
 */
declare const parseNumber: (number: string) => ParseNumber;
export { parseNumber };
