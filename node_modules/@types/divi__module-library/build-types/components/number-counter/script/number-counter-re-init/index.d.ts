import { type NumberCounterReInit } from '../types';
/**
 * Reinitialize the number counter modules.
 *
 * @since ??
 *
 * @param {NumberCounterData[]} [customData] Custom data to be used, otherwise will use
 * `window.diviModuleNumberCounterData`.
 */
declare const numberCounterReInit: NumberCounterReInit;
export { numberCounterReInit };
