import { type ReactElement } from 'react';
import { type NumberCounterEditProps } from './types';
/**
 * Number Counter component of visual builder.
 *
 * @since ??
 *
 * @param {NumberCounterEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const NumberCounterEdit: ({ attrs, defaultPrintedStyleAttrs, id, name, elements, }: NumberCounterEditProps) => ReactElement;
export { NumberCounterEdit, };
