import { type ReactElement } from 'react';
import { type BarCounterEditProps } from './types';
import './style.scss';
/**
 * Bar Counter component of visual builder.
 *
 * @since ??
 *
 * @param {BarCounterEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const BarCounterEdit: ({ attrs, defaultPrintedStyleAttrs, id, name, parentAttrs, elements, }: BarCounterEditProps) => ReactElement;
export { BarCounterEdit, };
