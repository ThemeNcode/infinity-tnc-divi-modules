import { type DeclarationFunctionProps } from '@types/divi__module';
/**
 * Style declaration for setting counter's bar width.
 * Custom style declaration is needed to enforce percentage-based value.
 *
 * @since ??
 *
 * @param {DeclarationFunctionProps<string>} param0 Style declaration params.
 *
 * @returns {string}
 */
export declare const barWidthStyleDeclaration: ({ attrValue, }: DeclarationFunctionProps<string>) => string;
