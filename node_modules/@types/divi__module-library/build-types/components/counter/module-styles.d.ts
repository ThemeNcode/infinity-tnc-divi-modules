import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type BarCounterAttrs, type BarCountersAttrs } from '@divi/types';
/**
 * Text Module's style components.
 *
 * @since ??
 */
declare const ModuleStyles: <TProps extends StylesProps<BarCounterAttrs, BarCountersAttrs>>({ attrs, elements, settings, parentAttrs, orderClass, mode, state, noStyleTag, }: TProps) => ReactElement;
export { ModuleStyles, };
