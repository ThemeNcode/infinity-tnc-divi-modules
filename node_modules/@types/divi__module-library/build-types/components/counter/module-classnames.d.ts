import { type ModuleClassnamesParams } from '@types/divi__module';
import { type BarCounterAttrs, type BarCountersAttrs } from '@divi/types';
/**
 * Module classnames function for bar counters module.
 *
 * @since ??
 *
 * @param {ModuleClassnamesParams<BarCounterAttrs>} param0 Function parameters.
 */
export declare const moduleClassnames: ({ classnamesInstance, parentAttrs, attrs, breakpoint, state, }: ModuleClassnamesParams<BarCounterAttrs, BarCountersAttrs>) => void;
