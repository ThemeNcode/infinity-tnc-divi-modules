import { type FullwidthCodeAttrs, type ModuleMetadata } from '@divi/types';
/**
 * Fullwitdth Code Meta Data.
 *
 * Note: The module metadata will be used to generate `module.json` upon build.
 * Variable name must end with `ModuleMetaData` to be picked up by the build script.
 */
declare const fullwidthCodeModuleMetaData: ModuleMetadata<FullwidthCodeAttrs>;
export { fullwidthCodeModuleMetaData, };
