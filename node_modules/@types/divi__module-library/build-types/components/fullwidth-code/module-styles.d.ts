import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type FullwidthCodeAttrs } from '@divi/types';
/**
 * Code Module's style components.
 *
 * @since ??
 */
declare const ModuleStyles: <TProps extends StylesProps<FullwidthCodeAttrs>>({ attrs, defaultPrintedStyleAttrs, settings, mode, state, noStyleTag, elements, }: TProps) => ReactElement;
export { ModuleStyles, };
