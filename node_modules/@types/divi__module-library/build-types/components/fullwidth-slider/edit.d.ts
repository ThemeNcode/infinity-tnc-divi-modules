import { type ReactElement } from 'react';
import { type FullwidthSliderEditProps } from './types';
/**
 * Fullwidth Slider component of visual builder.
 *
 * @since ??
 *
 * @param {FullwidthSliderEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const FullwidthSliderEdit: ({ attrs, elements, id, name, childrenIds, }: FullwidthSliderEditProps) => ReactElement;
export { FullwidthSliderEdit, };
