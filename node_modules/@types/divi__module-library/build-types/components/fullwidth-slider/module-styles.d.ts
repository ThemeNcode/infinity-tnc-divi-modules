import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type FullwidthSliderAttrs } from '@divi/types';
/**
 * Fullwidth Slider Module's style components.
 *
 * @since ??
 */
declare const ModuleStyles: ({ settings, mode, state, noStyleTag, orderClass, attrs, elements, defaultPrintedStyleAttrs, }: StylesProps<FullwidthSliderAttrs>) => ReactElement;
export { ModuleStyles, };
