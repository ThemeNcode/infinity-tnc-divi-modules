import { type ModuleMetadata, type TestimonialAttrs } from '@divi/types';
/**
 * Testimonial Meta Data.
 *
 * Note: The module metadata will be used to generate `module.json` upon build.
 * Variable name must end with `ModuleMetaData` to be picked up by the build script.
 */
declare const testimonialModuleMetaData: ModuleMetadata<TestimonialAttrs>;
export { testimonialModuleMetaData, };
