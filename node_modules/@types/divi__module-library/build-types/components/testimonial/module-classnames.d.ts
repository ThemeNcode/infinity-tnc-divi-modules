import { type ModuleClassnamesParams } from '@types/divi__module';
import { type TestimonialAttrs } from '@divi/types';
/**
 * Module classnames function for testimonial module.
 *
 * @since ??
 *
 * @param {ModuleClassnamesParams<TestimonialAttrs>} param0 Function parameters.
 */
export declare const moduleClassnames: ({ attrs, classnamesInstance, breakpoint, state, }: ModuleClassnamesParams<TestimonialAttrs>) => void;
