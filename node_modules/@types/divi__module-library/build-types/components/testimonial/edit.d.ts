import { type ReactElement } from 'react';
import { type TestimonialEditProps } from './types';
/**
 * Testimonial component of visual builder.
 *
 * @since ??
 *
 * @param {TestimonialEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const TestimonialEdit: ({ attrs, defaultPrintedStyleAttrs, id, name, elements, }: TestimonialEditProps) => ReactElement;
export { TestimonialEdit, };
