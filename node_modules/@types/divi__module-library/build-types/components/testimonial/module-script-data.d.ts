import { type ReactElement } from 'react';
import { type ModuleScriptDataProps } from '@types/divi__module';
import { type TestimonialAttrs } from '@divi/types';
/**
 * Testimonial module's script data component.
 *
 * @since ??
 *
 * @returns {ReactElement}
 */
export declare const ModuleScriptData: ({ elements, }: ModuleScriptDataProps<TestimonialAttrs>) => ReactElement;
