import { type ReactElement } from 'react';
import { type Module, type TestimonialAttrs } from '@divi/types';
export declare const SettingsAdvanced: ({ defaultSettingsAttrs, }: Module.Settings.Panel.Props<TestimonialAttrs>) => ReactElement;
