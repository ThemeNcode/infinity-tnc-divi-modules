import { type TestimonialAttrs } from '@divi/types';
import { type ModuleEditProps } from '../types-module';
declare global {
    interface Window {
        et_fix_testimonial_inner_width?: () => void;
    }
}
export type TestimonialEditProps = ModuleEditProps<TestimonialAttrs>;
