import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type TestimonialAttrs } from '@divi/types';
/**
 * Testimonial Module's style components.
 *
 * @since ??
 */
declare const ModuleStyles: ({ attrs, elements, defaultPrintedStyleAttrs, settings, orderClass, mode, state, noStyleTag, }: StylesProps<TestimonialAttrs>) => ReactElement;
export { ModuleStyles, };
