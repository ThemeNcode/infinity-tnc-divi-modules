import { type ReactElement } from 'react';
import { type Module, type TestimonialAttrs } from '@divi/types';
export declare const SettingsDesign: ({ defaultSettingsAttrs, }: Module.Settings.Panel.Props<TestimonialAttrs>) => ReactElement;
