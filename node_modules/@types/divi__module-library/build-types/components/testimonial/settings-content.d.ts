import { type ReactElement } from 'react';
import { type Module, type TestimonialAttrs } from '@divi/types';
export declare const SettingsContent: ({ defaultSettingsAttrs, }: Module.Settings.Panel.Props<TestimonialAttrs>) => ReactElement;
