import { type ReactElement } from 'react';
import { type SignupCustomFieldEditProps } from './types';
/**
 * Edit component of visual builder.
 *
 * @since ??
 *
 * @param { SignupCustomFieldEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const SignupCustomFieldEdit: ({ attrs, id, name, elements, defaultPrintedStyleAttrs, moduleOrderIndex, }: SignupCustomFieldEditProps) => ReactElement;
export { SignupCustomFieldEdit, };
