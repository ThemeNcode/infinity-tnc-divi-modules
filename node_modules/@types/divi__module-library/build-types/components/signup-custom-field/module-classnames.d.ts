import { type ModuleClassnamesParams } from '@types/divi__module';
import { type SignupCustomFieldAttrs } from '@divi/types';
/**
 * Set CSS class names to the module.
 *
 * @since ??
 *
 * @param {ModuleClassnamesParams<SignupCustomFieldAttrs>} param0 Function parameters.
 */
export declare const moduleClassnames: ({ classnamesInstance, attrs, breakpoint, state, }: ModuleClassnamesParams<SignupCustomFieldAttrs>) => void;
