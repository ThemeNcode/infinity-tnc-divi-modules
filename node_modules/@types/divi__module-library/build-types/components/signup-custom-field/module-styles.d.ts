import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type SignupCustomFieldAttrs } from '@divi/types';
/**
 * Set CSS styles to the module.
 *
 * @param {StylesProps<SignupCustomFieldAttrs>} props React component props.
 *
 * @returns {ReactElement} The CSS styles for the module.
 */
export declare const ModuleStyles: ({ attrs, elements, defaultPrintedStyleAttrs, orderClass, mode, state, noStyleTag, settings, }: StylesProps<SignupCustomFieldAttrs>) => ReactElement;
