import { type ReactElement } from 'react';
import { type FullwidthPortfolioAttrs, type Module } from '@divi/types';
/**
 * Content panel component for the Fullwidth Portfolio module settings modal.
 *
 * This component is registered as the `content` panel for the Fullwidth Portfolio module.
 *
 * @since ??
 *
 * @param {object} param0 Content panel props.
 * @param {object} param0.attrs Default settings attributes.
 * @param {object} param0.defaultSettingsAttrs Default settings attributes.
 *
 * @returns {ReactElement} Content panel component.
 */
export declare const SettingsContent: ({ attrs, defaultSettingsAttrs, }: Module.Settings.Panel.Props<FullwidthPortfolioAttrs>) => ReactElement;
