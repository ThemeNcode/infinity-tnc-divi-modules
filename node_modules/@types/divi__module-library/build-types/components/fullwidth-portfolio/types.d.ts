import { type FullwidthPortfolioAttrs } from '@divi/types';
import { type ModuleEditProps } from '../types-module';
declare global {
    interface Window {
        et_fullwidth_portfolio_init?: ($module: JQuery<HTMLElement>) => void;
    }
}
export type FullwidthPortfolioEditProps = ModuleEditProps<FullwidthPortfolioAttrs>;
