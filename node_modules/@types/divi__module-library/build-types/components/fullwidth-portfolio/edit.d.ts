import { type ReactElement } from 'react';
import { type FullwidthPortfolioEditProps } from './types';
/**
 * Fullwidth Portfolio component of visual builder.
 *
 * @since ??
 *
 * @param {FullwidthPortfolioEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const FullwidthPortfolioEdit: ({ attrs, defaultPrintedStyleAttrs, id, name, elements, }: FullwidthPortfolioEditProps) => ReactElement;
export { FullwidthPortfolioEdit, };
