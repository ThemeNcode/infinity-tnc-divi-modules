import { type FullwidthPortfolioAttrs, type ModuleMetadata } from '@divi/types';
/**
 * Fullwidth Portfolio Meta Data.
 *
 * Note: The module metadata will be used to generate `module.json` upon build.
 * Variable name must end with `ModuleMetaData` to be picked up by the build script.
 */
declare const fullwidthPortfolioModuleMetaData: ModuleMetadata<FullwidthPortfolioAttrs>;
export { fullwidthPortfolioModuleMetaData, };
