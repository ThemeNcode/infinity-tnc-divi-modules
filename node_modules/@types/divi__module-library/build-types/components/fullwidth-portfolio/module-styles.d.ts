import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type FullwidthPortfolioAttrs } from '@divi/types';
export declare const ModuleStyles: ({ elements, defaultPrintedStyleAttrs, orderClass, mode, state, noStyleTag, settings, attrs, }: StylesProps<FullwidthPortfolioAttrs>) => ReactElement;
