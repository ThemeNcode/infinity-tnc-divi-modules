import { type CountdownTimerAttrs, type ModuleMetadata } from '@divi/types';
/**
 * Countdown Timer Meta Data.
 *
 * Note: The module metadata will be used to generate `module.json` upon build.
 * Variable name must end with `ModuleMetaData` to be picked up by the build script.
 */
declare const countdownTimerModuleMetaData: ModuleMetadata<CountdownTimerAttrs>;
export { countdownTimerModuleMetaData, };
