import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type CountdownTimerAttrs } from '@divi/types';
/**
 * Text Module's style components.
 *
 * @since ??
 */
declare const ModuleStyles: <TProps extends StylesProps<CountdownTimerAttrs>>({ attrs, elements, settings, orderClass, mode, state, noStyleTag, }: TProps) => ReactElement;
export { ModuleStyles, };
