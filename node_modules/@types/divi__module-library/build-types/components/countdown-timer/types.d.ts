import { type CountdownTimerAttrs } from '@divi/types';
import { type ModuleEditProps } from '../types-module';
declare global {
    interface Window {
        et_pb_countdown_timer_init?: ($countdown: JQuery<HTMLElement>) => ReturnType<typeof setTimeout>;
    }
}
export interface CountdownTimerEditProps extends ModuleEditProps<CountdownTimerAttrs> {
    siteSettings: {
        gmtOffsetString?: string;
    };
}
