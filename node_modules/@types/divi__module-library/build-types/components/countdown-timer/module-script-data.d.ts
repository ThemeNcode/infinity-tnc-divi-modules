import { type ReactElement } from 'react';
import { type ScriptDataProps } from '@types/divi__module';
import { type CountdownTimerAttrs } from '@divi/types';
/**
 * Countdown Timer module's script data component.
 *
 * @since ??
 *
 * @returns {ReactElement}
 */
export declare const ModuleScriptData: <TProps extends ScriptDataProps<CountdownTimerAttrs>>({ elements, }: TProps) => ReactElement;
