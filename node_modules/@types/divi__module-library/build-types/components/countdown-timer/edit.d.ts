import { type ReactElement } from 'react';
import { type CountdownTimerEditProps } from './types';
/**
 * CountdownTimer Edit component of visual builder.
 *
 * @since ??
 *
 * @param {CountdownTimerEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const CountdownTimerEdit: ({ attrs, id, name, siteSettings, elements, }: CountdownTimerEditProps) => ReactElement;
export { CountdownTimerEdit, };
