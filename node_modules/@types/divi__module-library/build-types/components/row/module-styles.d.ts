import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type RowAttrs } from '@divi/types';
/**
 * Section's style components.
 *
 * @since ??
 */
declare const ModuleStyles: ({ attrs, elements, defaultPrintedStyleAttrs, settings, mode, state, noStyleTag, }: StylesProps<RowAttrs>) => ReactElement;
export { ModuleStyles, };
