import { type ReactElement } from 'react';
import { type Module, type RowAttrs } from '@divi/types';
/**
 * Design panel component for the Row module settings modal.
 *
 * @since ??
 *
 * @returns {ReactElement}
 */
export declare const SettingsDesign: ({ defaultSettingsAttrs, }: Module.Settings.Panel.Props<RowAttrs>) => ReactElement;
