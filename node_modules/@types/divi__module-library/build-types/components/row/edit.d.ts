import { type ReactElement } from 'react';
import { type RowEditProps } from './types';
/**
 * Row component of visual builder.
 *
 * @since ??
 *
 * @param {RowEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const RowEdit: ({ attrs, elements, defaultPrintedStyleAttrs, childrenIds, hasModule, id, name, parentAttrs, }: RowEditProps) => ReactElement;
export { RowEdit, };
