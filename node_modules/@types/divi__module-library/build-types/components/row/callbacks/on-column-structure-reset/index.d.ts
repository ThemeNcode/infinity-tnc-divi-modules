import { type Module } from '@divi/types';
/**
 * Function callback for column structure's reset button.
 * When clicked, reset column layout to one column (`4_4`) layout.
 *
 * @since ??
 *
 * @param {Module.Settings.Field.OnResetParams} param0 Callback parameters.
 */
export declare const onColumnStructureReset: ({ moduleId, }: Module.Settings.Field.OnResetParams) => void;
