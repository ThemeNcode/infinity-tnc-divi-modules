import { type ModuleClassnamesParams } from '@types/divi__module';
import { type RowAttrs } from '@divi/types';
/**
 * Module classnames function for row module.
 *
 * @since ??
 *
 * @param {ModuleClassnamesParams<RowAttrs>} param0 Function parameters.
 */
export declare const moduleClassnames: ({ classnamesInstance, attrs, hasModule, name, breakpoint, state, }: ModuleClassnamesParams<RowAttrs>) => void;
