import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type GalleryAttrs } from '@divi/types';
/**
 * Set CSS styles to the module.
 *
 * @param {StylesProps<GalleryAttrs>} props React component props.
 *
 * @returns {ReactElement} The CSS styles for the module.
 */
export declare const ModuleStyles: ({ attrs, elements, defaultPrintedStyleAttrs, mode, state, noStyleTag, settings, orderClass, }: StylesProps<GalleryAttrs>) => ReactElement;
