import { type ModuleClassnamesParams } from '@types/divi__module';
import { type GalleryAttrs } from '@divi/types';
/**
 * Set CSS class names to the module.
 *
 * @since ??
 *
 * @param {ModuleClassnamesParams<GalleryAttrs>} param0 Function parameters.
 */
export declare const moduleClassnames: ({ classnamesInstance, attrs, breakpoint, state, }: ModuleClassnamesParams<GalleryAttrs>) => void;
