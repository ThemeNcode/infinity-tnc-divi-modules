import { type GalleryAttrs } from '@divi/types';
import { type ModuleEditProps } from '../types-module';
declare global {
    interface Window {
        et_pb_gallery_init: ($gallery: JQuery) => void;
        et_pb_slider_init: ($slider: JQuery) => void;
        et_pb_init_gallery_items: (gallery: JQuery) => void;
        et_pb_set_responsive_grid: (grid: JQuery) => void;
    }
}
export type GalleryEditProps = ModuleEditProps<GalleryAttrs>;
export type ClassNameType = Record<string, boolean>;
export type GalleryItem = Record<string, string>;
