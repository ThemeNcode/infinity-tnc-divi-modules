import { type ReactElement } from 'react';
import { type GalleryEditProps } from './types';
/**
 * Gallery component of visual builder.
 *
 * @since ??
 *
 * @param {GalleryEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
export declare const GalleryEdit: ({ attrs, defaultPrintedStyleAttrs, id, name, elements, }: GalleryEditProps) => ReactElement;
