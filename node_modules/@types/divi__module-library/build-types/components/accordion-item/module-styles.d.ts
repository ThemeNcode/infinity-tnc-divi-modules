import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type AccordionAttrs, type AccordionItemAttrs } from '@divi/types';
/**
 * Text Module's style components.
 *
 * @since ??
 */
declare const ModuleStyles: ({ attrs, defaultPrintedStyleAttrs, parentAttrs, mode, state, orderClass, noStyleTag, elements, }: StylesProps<AccordionItemAttrs, AccordionAttrs>) => ReactElement;
export { ModuleStyles, };
