import { type ReactElement } from 'react';
import { type AccordionItemAttrs, type Module } from '@divi/types';
export declare const SettingsAdvanced: ({ defaultSettingsAttrs, }: Module.Settings.Panel.Props<AccordionItemAttrs>) => ReactElement;
