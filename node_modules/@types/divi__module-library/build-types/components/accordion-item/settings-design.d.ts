import { type ReactElement } from 'react';
import { type AccordionAttrs, type AccordionItemAttrs, type Module } from '@divi/types';
export declare const SettingsDesign: ({ parentAttrs, defaultSettingsAttrs, }: Module.Settings.Panel.Props<AccordionItemAttrs, AccordionAttrs>) => ReactElement;
