import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type CommentsAttrs } from '@divi/types';
/**
 * Set CSS styles to the module.
 *
 * @param {StylesProps<CommentsAttrs>} props React component props.
 *
 * @returns {ReactElement} The CSS styles for the module.
 */
export declare const ModuleStyles: <TProps extends StylesProps<CommentsAttrs>>({ attrs, defaultPrintedStyleAttrs, elements, settings, orderClass, mode, state, noStyleTag, }: TProps) => ReactElement;
