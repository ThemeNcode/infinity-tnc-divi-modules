import { type ReactElement } from 'react';
import { type CommentsAttrs, type Module } from '@divi/types';
/**
 * Design panel component for the Comments module settings modal.
 *
 * @since ??
 *
 * @param {Module.Settings.Panel.Props} param0 Design panel props.
 *
 * @returns {ReactElement}
 */
export declare const SettingsDesign: ({ attrs, groupConfiguration, }: Module.Settings.Panel.Props<CommentsAttrs>) => ReactElement;
