import { type CommentsAttrs } from '@divi/types';
import { type ModuleEditProps } from '../types-module';
declare global {
    interface Window {
        et_pb_comments_init?: ($module: JQuery<HTMLElement>) => void;
        et_pb_form_placeholders_init?: ($module: JQuery<HTMLElement>) => void;
    }
}
export type CommentsEditProps = ModuleEditProps<CommentsAttrs>;
