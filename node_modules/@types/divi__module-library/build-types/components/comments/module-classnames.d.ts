import { type ModuleClassnamesParams } from '@types/divi__module';
import { type CommentsAttrs } from '@divi/types';
/**
 * Set CSS class names to the module.
 *
 * @since ??
 *
 * @param {ModuleClassnamesParams<CommentsAttrs>} param0 Function parameters.
 */
export declare const moduleClassnames: ({ classnamesInstance, attrs, breakpoint, state, }: ModuleClassnamesParams<CommentsAttrs>) => void;
