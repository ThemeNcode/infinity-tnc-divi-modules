import { type ReactElement } from 'react';
import { type CommentsEditProps } from './types';
/**
 * Edit component of visual builder.
 *
 * @since ??
 *
 * @param { CommentsEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const CommentsEdit: ({ attrs, id, name, elements, defaultPrintedStyleAttrs, }: CommentsEditProps) => ReactElement;
export { CommentsEdit, };
