import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type PostSliderAttrs } from '@divi/types';
/**
 * Set CSS styles to the module.
 *
 * @param {StylesProps<PostSliderAttrs>} props React component props.
 *
 * @returns {ReactElement} The CSS styles for the module.
 */
export declare const ModuleStyles: ({ attrs, orderClass, elements, defaultPrintedStyleAttrs, mode, state, noStyleTag, settings, }: StylesProps<PostSliderAttrs>) => ReactElement;
