import { type ModuleClassnamesParams } from '@types/divi__module';
import { type PostSliderAttrs } from '@divi/types';
/**
 * Set CSS class names to the module.
 *
 * @since ??
 *
 * @param {ModuleClassnamesParams<PostSliderAttrs>} param0 Function parameters.
 */
export declare const moduleClassnames: ({ classnamesInstance, attrs, breakpoint, state, }: ModuleClassnamesParams<PostSliderAttrs>) => void;
