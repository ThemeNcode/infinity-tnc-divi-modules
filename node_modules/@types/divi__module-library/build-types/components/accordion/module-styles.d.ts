import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type AccordionAttrs } from '@divi/types';
/**
 * Text Module's style components.
 *
 * @since ??
 */
declare const ModuleStyles: ({ attrs, defaultPrintedStyleAttrs, settings, mode, state, orderClass, noStyleTag, elements, }: StylesProps<AccordionAttrs>) => ReactElement;
export { ModuleStyles, };
