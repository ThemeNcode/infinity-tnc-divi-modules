import { type ReactElement } from 'react';
import { type AccordionAttrs, type Module } from '@divi/types';
export declare const SettingsDesign: ({ defaultSettingsAttrs, }: Module.Settings.Panel.Props<AccordionAttrs>) => ReactElement;
