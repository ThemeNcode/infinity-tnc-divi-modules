import { type ReactElement } from 'react';
import { type AccordionAttrs, type Module } from '@divi/types';
export declare const SettingsContent: ({ defaultSettingsAttrs, }: Module.Settings.Panel.Props<AccordionAttrs>) => ReactElement;
