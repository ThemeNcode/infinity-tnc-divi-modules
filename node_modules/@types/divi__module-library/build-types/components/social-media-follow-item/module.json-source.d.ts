import { type ModuleMetadata, type SocialMediaFollowItemAttrs } from '@divi/types';
/**
 * Social Media Follow Item Meta Data.
 *
 * Note: The module metadata will be used to generate `module.json` upon build.
 * Variable name must end with `ModuleMetaData` to be picked up by the build script.
 */
declare const socialMediaFollowItemModuleMetaData: ModuleMetadata<SocialMediaFollowItemAttrs>;
export { socialMediaFollowItemModuleMetaData, };
