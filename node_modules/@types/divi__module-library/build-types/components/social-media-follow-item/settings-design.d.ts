import { type ReactElement } from 'react';
import { type Module, type SocialMediaFollowAttrs, type SocialMediaFollowItemAttrs } from '@divi/types';
export declare const SettingsDesign: ({ attrs, defaultSettingsAttrs, parentAttrs, }: Module.Settings.Panel.Props<SocialMediaFollowItemAttrs, SocialMediaFollowAttrs>) => ReactElement;
