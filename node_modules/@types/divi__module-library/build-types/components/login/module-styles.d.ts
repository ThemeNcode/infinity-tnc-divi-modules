import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type LoginAttrs } from '@divi/types';
/**
 * Login Module's style components.
 *
 * @since ??
 */
declare const ModuleStyles: <TProps extends StylesProps<LoginAttrs>>({ attrs, elements, orderClass, mode, settings, state, noStyleTag, }: TProps) => ReactElement;
export { ModuleStyles, };
