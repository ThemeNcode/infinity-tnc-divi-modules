import { type ModuleMetadata, type VideoAttrs } from '@divi/types';
/**
 * Video Meta Data.
 *
 * Note: The module metadata will be used to generate `module.json` upon build.
 * Variable name must end with `ModuleMetaData` to be picked up by the build script.
 */
declare const videoModuleMetaData: ModuleMetadata<VideoAttrs>;
export { videoModuleMetaData, };
