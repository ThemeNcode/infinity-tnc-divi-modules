import { type ReactElement } from 'react';
import { type Module, type VideoAttrs } from '@divi/types';
/**
 * Content panel component for the Video module settings modal.
 *
 * @since ??
 *
 * @param {Module.Settings.Panel.Props} param0 Content panel props.
 *
 * @returns {ReactElement}
 */
export declare const SettingsContent: ({ defaultSettingsAttrs, attrs, }: Module.Settings.Panel.Props<VideoAttrs>) => ReactElement;
