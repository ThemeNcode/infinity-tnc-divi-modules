import { type ReactElement } from 'react';
import { type VideoEditProps } from './types';
/**
 * Video component of visual builder.
 *
 * @since ??
 *
 * @param {VideoEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const VideoEdit: ({ attrs, defaultPrintedStyleAttrs, id, name, elements, }: VideoEditProps) => ReactElement;
export { VideoEdit, };
