import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type VideoAttrs } from '@divi/types';
/**
 * Video Module's style components.
 *
 * @since ??
 */
declare const ModuleStyles: ({ mode, state, noStyleTag, orderClass, attrs, defaultPrintedStyleAttrs, settings, elements, }: StylesProps<VideoAttrs>) => ReactElement;
export { ModuleStyles, };
