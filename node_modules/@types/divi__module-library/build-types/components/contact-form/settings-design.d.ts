import { type ReactElement } from 'react';
import { type ContactFormAttrs, type Module } from '@divi/types';
/**
 * Design panel component for the contact form module settings modal.
 *
 * @since ??
 *
 * @param {Module.Settings.Panel.Props} param0 Design panel props.
 *
 * @returns {ReactElement}
 */
export declare const SettingsDesign: ({ groupConfiguration, }: Module.Settings.Panel.Props<ContactFormAttrs>) => ReactElement;
