import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type ContactFormAttrs } from '@divi/types';
/**
 * Set CSS styles to the module.
 *
 * @param {StylesProps<ContactFormAttrs>} props React component props.
 *
 * @returns {ReactElement} The CSS styles for the module.
 */
export declare const ModuleStyles: ({ attrs, elements, orderClass, defaultPrintedStyleAttrs, mode, state, noStyleTag, settings, }: StylesProps<ContactFormAttrs>) => ReactElement;
