import { type ReactElement } from 'react';
import { type ContactFormAttrs, type Module } from '@divi/types';
/**
 * Content panel component for the contact form module settings modal.
 *
 * @since ??
 *
 * @param {Module.Settings.Panel.Props} param0 Content panel props.
 *
 * @returns {ReactElement}
 */
export declare const SettingsContent: ({ attrs, groupConfiguration, }: Module.Settings.Panel.Props<ContactFormAttrs>) => ReactElement;
