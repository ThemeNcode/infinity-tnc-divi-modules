import { type ReactElement } from 'react';
import { type ContactFormEditProps } from './types';
/**
 * Edit component of visual builder.
 *
 * @since ??
 *
 * @param { ContactFormEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const ContactFormEdit: ({ attrs, id, childrenIds, name, elements, urls, defaultPrintedStyleAttrs, }: ContactFormEditProps) => ReactElement;
export { ContactFormEdit, };
