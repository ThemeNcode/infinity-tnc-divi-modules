import { type ModuleMetadata, type PostSliderAttrs } from '@divi/types';
/**
 * Post Slider Module Meta Data.
 *
 * Note: The module metadata will be used to generate `module.json` upon build.
 * Variable name must end with `ModuleMetaData` to be picked up by the build script.
 */
declare const postSliderModuleMetaData: ModuleMetadata<PostSliderAttrs>;
export { postSliderModuleMetaData, };
