import { type ReactElement } from 'react';
import { type PostSliderEditProps } from './types';
import './post-slider.scss';
/**
 * Edit component of visual builder.
 *
 * @since ??
 *
 * @param { PostSliderEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const PostSliderEdit: ({ attrs, id, name, elements, defaultPrintedStyleAttrs, }: PostSliderEditProps) => ReactElement;
export { PostSliderEdit, };
