import { type ReactElement } from 'react';
import { type MapAttrs, type Module } from '@divi/types';
/**
 * Content panel component for the Map module settings modal.
 *
 * @since ??
 *
 * @param {Module.Settings.Panel.Props} param0 Content panel props.
 *
 * @returns {ReactElement}
 */
export declare const SettingsContent: ({ defaultSettingsAttrs, }: Module.Settings.Panel.Props<MapAttrs>) => ReactElement;
