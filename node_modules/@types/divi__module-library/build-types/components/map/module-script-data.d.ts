import { type ReactElement } from 'react';
import { type ScriptDataProps } from '@types/divi__module';
import { type MapAttrs } from '@divi/types';
/**
 * Map module's script data component.
 *
 * @since ??
 *
 * @returns {ReactElement}
 */
export declare const ModuleScriptData: <TProps extends ScriptDataProps<MapAttrs>>({ elements, }: TProps) => ReactElement;
