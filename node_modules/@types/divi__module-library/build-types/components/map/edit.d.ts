import { type ReactElement } from 'react';
import { type MapEditProps } from './types';
import './styles.scss';
/**
 * Map component of visual builder.
 *
 * @since ??
 *
 * @param {MapEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const MapEdit: ({ attrs, elements, defaultPrintedStyleAttrs, id, childrenIds, name, }: MapEditProps) => ReactElement;
export { MapEdit, };
