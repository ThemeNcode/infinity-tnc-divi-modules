import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type MapAttrs } from '@divi/types';
declare const ModuleStyles: <TProps extends StylesProps<MapAttrs>>({ attrs, elements, defaultPrintedStyleAttrs, settings, mode, state, noStyleTag, }: TProps) => ReactElement;
export { ModuleStyles, };
