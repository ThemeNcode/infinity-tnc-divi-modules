import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type AudioAttrs } from '@divi/types';
/**
 * Audio Module's style components.
 *
 * @since ??
 */
declare const ModuleStyles: <TProps extends StylesProps<AudioAttrs>>({ attrs, elements, defaultPrintedStyleAttrs, orderClass, mode, settings, state, noStyleTag, }: TProps) => ReactElement;
export { ModuleStyles, };
