import { type ReactElement } from 'react';
/**
 * Audio element component for audio module.
 * Audio element needs to be abstracted into its own component so that media element initialize
 * can be executed in accurate timing which is right after the audioHTML is rendered as DOM.
 * Calling this as element on audio edit.tsx component ends up with media element being called too early
 * before the component is rendered as actual DOM.
 *
 * @since ??
 *
 * @param {Record<'id' | 'audioHTML', string>} param0 Component props.
 *
 * @returns {ReactElement}
 */
export declare const AudioElement: ({ audioHTML, id, }: {
    audioHTML: string;
    id: string;
}) => ReactElement;
