import { type ReactElement } from 'react';
import { type AudioEditProps } from './types';
import './audio.scss';
/**
 * Audio component of visual builder.
 *
 * @since ??
 *
 * @param {AudioEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const AudioEdit: ({ attrs, defaultPrintedStyleAttrs, id, name, elements, }: AudioEditProps) => ReactElement;
export { AudioEdit, };
