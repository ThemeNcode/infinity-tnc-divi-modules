import { type GlobalLayoutAttrs } from '@divi/types';
import { type ModuleEditProps } from '../types-module';
export interface GlobalLayoutEditProps extends ModuleEditProps<GlobalLayoutAttrs> {
    globalModule?: string;
}
