import { type ReactElement } from 'react';
interface PreloaderProps {
    id: string;
    owner: string;
}
/**
 * Wrapper of Preloader component withSelect.
 *
 * @since ??
 *
 * @param {PreloaderProps} props Component props.
 *
 * @returns {ReactElement}
 */
export declare const Preloader: (props: PreloaderProps) => ReactElement;
export {};
