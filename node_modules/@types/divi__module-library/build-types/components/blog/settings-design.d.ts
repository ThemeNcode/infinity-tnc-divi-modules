import { type ReactElement } from 'react';
import { type BlogAttrs, type Module } from '@divi/types';
/**
 * Design panel component for the Blog module settings modal.
 *
 * @since ??
 *
 * @param {Module.Settings.Panel.Props} param0 Design panel props.
 *
 * @returns {ReactElement}
 */
export declare const SettingsDesign: ({ defaultSettingsAttrs, }: Module.Settings.Panel.Props<BlogAttrs>) => ReactElement;
