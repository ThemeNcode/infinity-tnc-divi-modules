import { type ReactElement } from 'react';
import { type StylesProps } from '@types/divi__module';
import { type BlogAttrs } from '@divi/types';
/**
 * Blog Module's style components.
 *
 * @since ??
 */
declare const ModuleStyles: ({ attrs, elements, orderClass, settings, mode, state, noStyleTag, }: StylesProps<BlogAttrs>) => ReactElement;
export { ModuleStyles, };
