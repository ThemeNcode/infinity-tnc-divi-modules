import { type ModuleConversionOutline } from '@divi/types';
/**
 * Get Possible Module Conversion Outline.
 *
 * Get conversion map of a module from the shortcode module definitions.
 *
 * @since ??
 *
 * @param {string} moduleName String Module name.
 *
 * @returns {object} Key is object addresses and value is the correspondent attribute value.
 *
 * @example
 * ```tsx
 * getPossibleModuleConversionOutline('et_pb_blurb');
 * // It will output full conversion map of et_pb_blurb module.
 * ```
 */
export declare const getPossibleModuleConversionOutline: (moduleName: string) => ModuleConversionOutline;
