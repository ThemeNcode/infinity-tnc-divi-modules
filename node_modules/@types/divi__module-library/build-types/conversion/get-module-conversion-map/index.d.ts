import { type ModuleConversionMap, type ModuleConversionOutline } from '@divi/types';
/**
 * Get Module Conversion Map.
 *
 * Get Module's Full Attributes Conversion map based on module's conversion outline.
 *
 * @since ??
 *
 * @param {object} conversionOutline Module's conversion map.
 *
 * @returns {object} Module's full conversion map.
 *
 * @example
 * ```tsx
 * const conversionOutline = {
 *  advanced: {
 *   admin_label:     'module.meta.adminLabel',
 *   z_index:         'module.decoration.zIndex',
 *  }
 * };
 * getModuleConversionMap(conversionOutline);
 * // {
 * //   attributeMap: {
 * //     admin_label: 'module.meta.adminLabel.*',
 * //     z_index:     'module.decoration.zIndex.*',
 * //   },
 * //   optionEnableMap: {}
 * //   valueExpansionFunctionMap: {}
 * // }
 * ```
 */
export declare const getModuleConversionMap: (conversionOutline: ModuleConversionOutline) => ModuleConversionMap;
