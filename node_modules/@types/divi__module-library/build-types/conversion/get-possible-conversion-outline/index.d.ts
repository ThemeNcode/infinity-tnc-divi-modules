import { type ModuleConversionOutline, type ShortcodeModule } from '@divi/types';
/**
 * Get Possible Conversion Outline.
 *
 * Get conversion map of a module from module fields.
 *
 * @since ??
 *
 * @param {object} moduleFields Module fields.
 * @param {boolean} isChildModule Check if Child module.
 *
 * @returns {object} Key is object addresses and value is the correspondent attribute value.
 *
 * @example
 * ```tsx
 * const moduleFields = {
 *  module: {
 *   title: {}
 *   test_attr: {}
 *  },
 *  advanced: {
 *   animation:  [],
 *   background: [],
 *   z_index:    [],
 *  },
 *  css: {
 *   before: {},
 *   after:  {},
 *  }
 * };
 * getPossibleConversionOutline(moduleFields);
 * // {
 * //   advanced: {
 * //     animation: 'animation',
 * //     background: 'background',
 * //     z_index: 'zIndex',
 * //   },
 * //   css: {
 * //     before: 'css.*.before',
 * //     after: 'css.*.after',
 * //   },
 * //   module: {
 * //     title: 'title.*',
 * //     test_attr: 'testAttr.*',
 * //   },
 * // }
 * ```
 *
 * @example
 * ```tsx
 * const moduleFields = {
 *  module: {
 *   title: {}
 *   test_attr: {}
 *  },
 *  advanced: {
 *   animation:  [],
 *   background: [],
 *   z_index:    [],
 *  },
 *  css: {
 *   before: {},
 *   after:  {},
 *  }
 * };
 * getPossibleConversionOutline(moduleFields, true);
 * // {
 * //   advanced: {
 * //     background: 'background',
 * //     z_index: 'zIndex',
 * //   },
 * //   css: {
 * //     before: 'css.*.before',
 * //     after: 'css.*.after',
 * //   },
 * //   module: {
 * //     title: 'title.*',
 * //     test_attr: 'testAttr.*',
 * //   },
 * // }
 * ```
 */
export declare const getPossibleConversionOutline: (moduleFields: ShortcodeModule.Module.Fields, isChildModule?: boolean) => ModuleConversionOutline;
