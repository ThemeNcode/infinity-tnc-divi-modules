/// <reference types="react" />
import { type ModuleLibrary } from '@divi/types';
/**
 * Component for rendering style based on given props using modules' style component that has been registered to
 * `divi/module-library` store.
 *
 * PO NOTE: This component isn't geared toward rendering preset-style only. Technically it'll simply receives
 * props then render it as styles. As I work on this, I just realized that maybe this can be used to render
 * VB styles the way styles are being rendered on FE as well. Instead of being rendered inside module element,
 * it can be rendered at once at the end of the layout.
 *
 * @since ??
 */
export declare const ModuleLibraryStyles: ({ styles, }: ModuleLibrary.Styles.Component.Props) => JSX.Element;
