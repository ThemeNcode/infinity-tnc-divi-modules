import { type ReactElement } from 'react';
import { type ShortcodeModuleEditProps } from './types';
import './styles.scss';
/**
 * Shortcode Module component of visual builder.
 *
 * There are two types of unsupported modules:
 *
 * 1. D5/Gutenberg formatted modules that are not registered.
 * This module saved content is rendered in the builder but is not editable.
 *
 * 2. Shortcode based unsupported module that cannot be converted to D5.
 * This module will have nonconvertible prop set to 'yes' and since this module has not
 * content, a placeholder is rendered instead.
 *
 * @since ??
 *
 * @param {ShortcodeModuleEditProps} props React component props.
 *
 * @returns {ReactElement}
 */
declare const ShortcodeModuleEdit: ({ attrs, id, innerHTML, name, postId, shortcodeTags, }: ShortcodeModuleEditProps) => ReactElement;
export { ShortcodeModuleEdit, };
