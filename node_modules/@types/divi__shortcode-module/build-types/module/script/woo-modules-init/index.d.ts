/**
 * Initialize all Woo Modules related scripts.
 *
 * @since ??
 *
 * TODO feat(D5, Woo Modules): This function is added intentionally to fix Woo Modules
 * appearance issue in D5 Shortcode module. It's probably just temporary solution, so it
 * is not supposed to be replacement for init() method and should be limited to builder
 * to avoid unexpected issue. We need to evaluate it again once Woo Modules support D5.
 */
declare const wooModulesInit: () => void;
export { wooModulesInit };
