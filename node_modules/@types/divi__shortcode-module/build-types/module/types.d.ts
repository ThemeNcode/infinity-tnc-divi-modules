import { type ModuleEditProps } from '@types/divi__module-library';
import { type ShortcodeModule } from '@divi/types';
declare global {
    interface Window {
        et_pb_init_modules: () => void;
        et_pb_init_woo_modules: () => void;
    }
}
export interface ShortcodeModuleEditProps extends ModuleEditProps<ShortcodeModule.Attributes> {
    innerHTML?: string;
    postId?: string;
    shortcodeTags?: string[];
}
