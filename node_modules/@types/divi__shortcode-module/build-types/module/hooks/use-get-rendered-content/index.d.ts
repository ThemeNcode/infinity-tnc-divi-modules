import { type UseGetRenderedContent } from './types';
/**
 * Gets rendered content from Divi WP REST API.
 *
 * @returns {{
 * getRenderedContent: string,
 * renderedContent: string,
 * isLoading: boolean
 * }} Returns an object consisted of:
 *
 * `getRenderedContent()`: Initiates a fetch request which retrieves rendered content from the backend.
 *
 * `renderedContent`: Rendered content as an string.
 *
 * `isLoading`: Loading state for `getRenderedContent()`, `true` if data has not been retrieved yet, `false` otherwise.
 */
export declare const useGetRenderedContent: () => UseGetRenderedContent;
