/**
 * Callback that is being executed when the shortcode change to update the module's shortcode attribute.
 *
 * @since ??
 *
 * @param {object} param0 Callback parameters.
 * @param {string} param0.moduleId Module ID.
 * @param {string} param0.moduleSlug Module slug.
 * @param {Record<string, string>} param0.shortcodeAttrs Shortcode attributes.
 * @param {string} param0.shortcodeContent Shortcode content.
 * @param {string} param0.shortcode Shortcode.
 *
 * @returns {void}
 */
export declare const onShortcodeChange: ({ moduleId, moduleSlug, shortcodeAttrs, shortcodeContent, shortcode, }: {
    moduleId: string;
    moduleSlug: string;
    shortcodeAttrs: Record<string, string>;
    shortcodeContent: string;
    shortcode: string;
}) => void;
