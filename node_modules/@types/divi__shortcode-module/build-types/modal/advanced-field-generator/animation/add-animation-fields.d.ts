import { type ShortcodeModule } from '@divi/types';
import { type AddAnimationFieldsParams } from './types';
/**
 * Return position and z-index (group) fields based on given advanced field definition.
 * Equivalent of `ET_Builder_Element->_add_position_fields()` in TS.
 *
 * @since ??
 *
 * @param {AddAnimationFieldsParams} param0 Function parameters.
 *
 * @returns {Record<string, ShortcodeModule.Field.Item>}
 */
export declare const addAnimationFields: ({ advancedFieldDefinition, shortcodeName, isChildModule, }: AddAnimationFieldsParams) => Record<string, ShortcodeModule.Field.Item>;
