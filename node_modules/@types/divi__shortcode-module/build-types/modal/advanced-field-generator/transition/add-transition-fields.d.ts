import { type ShortcodeModule } from '@divi/types';
/**
 * Return transition (group) fields based on given advanced field definition.
 * Equivalent of `ET_Builder_Element->_add_additional_transition_fields()` in TS.
 *
 * @since ??
 *
 * @returns {Record<string, ShortcodeModule.Field.Item>}
 */
export declare const addTransitionFields: () => Record<string, ShortcodeModule.Field.Item>;
