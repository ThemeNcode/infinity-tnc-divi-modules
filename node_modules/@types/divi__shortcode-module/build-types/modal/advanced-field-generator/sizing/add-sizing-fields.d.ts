import { type ShortcodeModule } from '@divi/types';
import { type AddAdvancedFieldsParams } from '../types';
/**
 * Return sizing (group) fields based on given advanced field definition.
 *
 * @since ??
 */
export declare const addSizingFields: ({ advancedFieldDefinition, }: AddAdvancedFieldsParams) => Record<string, ShortcodeModule.Field.Item>;
