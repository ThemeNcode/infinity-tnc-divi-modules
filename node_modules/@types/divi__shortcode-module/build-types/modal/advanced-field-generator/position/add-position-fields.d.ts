import { type ShortcodeModule } from '@divi/types';
import { type AddAdvancedFieldsParams } from '../types';
/**
 * Return position and z-index (group) fields based on given advanced field definition.
 * Equivalent of `ET_Builder_Element->_add_position_fields()` in TS.
 *
 * @since ??
 *
 * @param {AddAdvancedFieldsParams} param0 Function parameters.
 *
 * @returns {Record<string, ShortcodeModule.Field.Item>}
 */
export declare const addPositionFields: ({ advancedFieldDefinition, }: AddAdvancedFieldsParams) => Record<string, ShortcodeModule.Field.Item>;
