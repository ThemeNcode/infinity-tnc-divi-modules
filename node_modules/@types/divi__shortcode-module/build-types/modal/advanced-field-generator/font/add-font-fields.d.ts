import { type ShortcodeModule } from '@divi/types';
import { type AddFontFieldsParams } from './types';
/**
 * Generate font fields definition.
 *
 * @since ??
 *
 * @param {AddFontFieldsParams} param0 Function parameters.
 *
 * @returns {Record<string, ShortcodeModule.Field.Item>}
 */
export declare const addFontFields: ({ advancedFieldDefinition, isChildModule, shortcodeName, stylesSettings, }: AddFontFieldsParams) => Record<string, ShortcodeModule.Field.Item>;
