import { type ShortcodeModule } from '@divi/types';
import { type GetSpacingFieldsParams } from './types';
/**
 * Get spacing fields.
 * NOTE: Just like how it works in D4, addSpacingFields() doesn't actually use this.
 * This function is used by other add*Fields() such as addImageIconFields().
 *
 * @since ??
 *
 * @param {GetSpacingFieldsParams} params Function parameters.
 *
 * @returns {Record<string, ShortcodeModule.Field.Item>} Spacing fields.
 */
export declare const getSpacingFields: (params: GetSpacingFieldsParams) => Record<string, ShortcodeModule.Field.Item>;
