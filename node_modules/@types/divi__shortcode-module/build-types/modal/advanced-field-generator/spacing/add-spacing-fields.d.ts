import { type ShortcodeModule } from '@divi/types';
import { type AddAdvancedFieldsParams } from '../types';
/**
 * Return spacing (group) fields based on given advanced field definition.
 * Equivalent of `ET_Builder_Element->_add_margin_padding_fields()` in TS.
 *
 * @since ??
 *
 * @param {AddAdvancedFieldsParams} advancedFieldDefinition Advanced field definition.
 *
 * @returns {Record<string, ShortcodeModule.Field.Item>}
 */
export declare const addSpacingFields: ({ advancedFieldDefinition, }: AddAdvancedFieldsParams) => Record<string, ShortcodeModule.Field.Item>;
