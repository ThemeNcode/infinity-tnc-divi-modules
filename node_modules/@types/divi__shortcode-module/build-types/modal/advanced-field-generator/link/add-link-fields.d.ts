import { type ShortcodeModule } from '@divi/types';
import { type AddLinkFieldsParams } from './types';
/**
 * Return link (group) fields based on given advanced field definition.
 *
 * @since ??
 *
 * @param {AddLinkFieldsParams} param Function params.
 *
 * @returns {Record<string, ShortcodeModule.Field.Item>}
 */
export declare const addLinkFields: ({ advancedFieldDefinition, shortcodeName, }: AddLinkFieldsParams) => Record<string, ShortcodeModule.Field.Item>;
