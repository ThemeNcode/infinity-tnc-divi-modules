import { type AddAdvancedFieldsParams } from '../types';
import { type OverflowField } from './types';
/**
 * Return overflow fields based on given advanced field definition.
 *
 * @since ??
 *
 * @param {AddAdvancedFieldsParams} param0 Function parameters.
 *
 * @returns {Record<string, OverflowField>}
 */
export declare const addOverflowFields: ({ advancedFieldDefinition, }: AddAdvancedFieldsParams) => Record<string, OverflowField>;
