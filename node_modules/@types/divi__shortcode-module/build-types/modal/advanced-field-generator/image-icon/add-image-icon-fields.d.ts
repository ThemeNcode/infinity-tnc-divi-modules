import { type ShortcodeModule } from '@divi/types';
import { type AddAdvancedFieldsParams } from '../types';
/**
 * Return image icon fields that will be used on the module.
 *
 * @since ??
 *
 * @param {AddAdvancedFieldsParams} param0 Function parameters.
 *
 * @returns {Record<string, ShortcodeModule.Field.Item>} Image icon fields.
 */
export declare const addImageIconFields: ({ advancedFieldDefinition, }: AddAdvancedFieldsParams) => Record<string, ShortcodeModule.Field.Item>;
