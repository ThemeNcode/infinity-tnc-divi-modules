import { type ShortcodeModule } from '@divi/types';
import { type AddAdvancedFieldsParams } from '../types';
/**
 * Return transform (group) fields based on given advanced field definition.
 *
 * @since ??
 *
 * @param {AddAdvancedFieldsParams} param0 Advanced field definition.
 *
 * @returns {Record<string, ShortcodeModule.Field.Item>}
 */
export declare const addTransformFields: ({ advancedFieldDefinition, }: AddAdvancedFieldsParams) => Record<string, ShortcodeModule.Field.Item>;
