import { type ShortcodeModule } from '@divi/types';
import { type AddStickyFieldsParams } from './types';
/**
 * Return sticky (group) fields based on given advanced field definition.
 *
 * @since ??
 *
 * @param {AddStickyFieldsParams} param0 Advanced field definition.
 *
 * @returns {Record<string, ShortcodeModule.Field.Item>}
 */
export declare const addStickyFields: ({ advancedFieldDefinition, shortcodeName, }: AddStickyFieldsParams) => Record<string, ShortcodeModule.Field.Item>;
