import { type ShortcodeModule } from '@divi/types';
import { type AddBackgroundFieldsParams } from './types';
/**
 * Add background fields to module's fields definition.
 *
 * @since ??
 *
 * @param {AddBackgroundFieldsParams} param0 Function parameters.
 *
 * @returns {Record<string, ShortcodeModule.Field.Item>} Background field definitions.
 */
export declare const addBackgroundFields: ({ advancedFieldDefinition, isChildModule, shortcodeName, isSpecialtySection, }: AddBackgroundFieldsParams) => Record<string, ShortcodeModule.Field.Item>;
