import { type ShortcodeModule } from '@divi/types';
import { type GenerateBackgroundOptionsParams } from './types';
/**
 * Generate background options fields.
 *
 * @since ??
 *
 * @param {GenerateBackgroundOptionsParams} param0 Function params.
 *
 * @returns {Record<string, ShortcodeModule.Field.Item>}
 */
export declare const generateBackgroundOptions: ({ base_name, background_tab, tab_slug, toggle_slug, backgroundContext, options_filters, customStylesSettings, advancedFieldDefinitionBackground, specialty, addNameProperty, }: GenerateBackgroundOptionsParams) => Record<string, ShortcodeModule.Field.Item>;
