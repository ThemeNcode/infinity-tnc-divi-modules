import { type ShortcodeModule } from '@divi/types';
import { type AddAdvancedFieldsParams } from '../types';
/**
 * Return filters (group) fields based on given advanced field definition.
 * Equivalent of `ET_Builder_Element->_add_filter_fields()` in TS.
 *
 * @since ??
 *
 * @param {AddAdvancedFieldsParams} param0 Function parameters.
 *
 * @returns {Record<string, ShortcodeModule.Field.Item>}
 */
export declare const addFiltersFields: ({ advancedFieldDefinition, }: AddAdvancedFieldsParams) => Record<string, ShortcodeModule.Field.Item>;
