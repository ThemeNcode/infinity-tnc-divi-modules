import { type ReactElement } from 'react';
import { type ModuleShortcodeModalProps } from './types';
/**
 * Module modal component tailored specifically for the module's modal.
 *
 * This component represents the modal for managing module details.
 *
 * @since ??
 *
 * @param {object} params Parameters for the ModuleShortcodeModal component.
 * @param {string} params.adminOptionsGeneralUrl The URL for admin options.
 * @param {Array} params.childModules Child modules.
 * @param {object} params.parentModule Parent module.
 * @param {object} params.group Group information.
 * @param {boolean} params.hasStickyState Whether it has a sticky state.
 * @param {string} params.moduleName The name of the module.
 * @param {Function} params.onAddNewChildModule Function to add a new child module.
 * @param {Function} params.onChangeOrderChildModule Function to change the order of child modules.
 * @param {Function} params.onClickBackButton Function for the back button click.
 * @param {Function} params.onClickDeleteChildModule Function to delete a child module.
 * @param {Function} params.onClickDuplicateChildModule Function to duplicate a child module.
 * @param {Function} params.onClickSettingsChildModule Function to handle settings for a child module.
 * @param {Function} params.onContextMenu Function to handle the context menu event.
 * @param {object} params.shortcodeAttrs Attributes for the shortcode.
 * @param {string} params.shortcodeContent Content of the shortcode.
 * @param {object} params.shortcodeModuleDefinition Definition of the shortcode module.
 * @param {object} params.shortcodeModuleDefinitionChild Definition of the child shortcode module.
 * @param {string} params.shortcodeName The name of the shortcode.
 * @param {string} params.tab The tab.
 * @param {Function} params.updateShortcodeAttrs Function to update shortcode attributes.
 * @param {Function} params.updateShortcodeContent Function to update shortcode content.
 *
 * @returns {ReactElement} React element representing the module modal.
 */
export declare const ModuleShortcodeModal: ({ adminOptionsGeneralUrl, group, moduleName, childModules, hasStickyState, parentModule, onClickBackButton, onClickSettingsChildModule, onClickDuplicateChildModule, onClickDeleteChildModule, onChangeOrderChildModule, onAddNewChildModule, onContextMenu, owner, shortcodeAttrs, shortcodeContent, shortcodeModuleDefinition, shortcodeModuleDefinitionChild, shortcodeName, tab, updateShortcodeAttrs, updateShortcodeContent, }: ModuleShortcodeModalProps) => ReactElement;
