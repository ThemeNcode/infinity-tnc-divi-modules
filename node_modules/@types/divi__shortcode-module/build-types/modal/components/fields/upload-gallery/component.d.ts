import { type ReactElement } from 'react';
import { type ShortcodeModuleFieldUploadGalleryProps } from './types';
/**
 * UploadGallery field component for shortcode module's modal settings. It takes D4 formatted props
 * and adjust it so it can be passed into D5's range field component.
 *
 * @since ??
 *
 * @param {ShortcodeModuleFieldUploadGalleryProps} param0 Component props.
 *
 * @returns {ReactElement}
 */
export declare const ShortcodeModuleFieldUploadGallery: ({ chooseText, name, updateShortcodeAttrs, value, }: ShortcodeModuleFieldUploadGalleryProps) => ReactElement;
