import { type ReactElement } from 'react';
import { type ShortcodeModuleFieldPositionProps } from './types';
/**
 * Position field component for shortcode module's modal settings. It takes D4 formatted props
 * and adjust it so it can be passed into D5's field component.
 *
 * @since ??
 *
 * @param {ShortcodeModuleFieldPositionProps} param0 Component props.
 *
 * @returns {ReactElement}
 */
export declare const ShortcodeModuleFieldPosition: ({ defaultValue, name, updateShortcodeAttrs, value, }: ShortcodeModuleFieldPositionProps) => ReactElement;
