import { type ReactElement } from 'react';
import { type ShortcodeModuleFieldTextAreaProps } from './types';
/**
 * TextArea field component for shortcode module's modal settings. It takes D4 formatted props
 * and adjust it so it can be passed into D5's range field component.
 *
 * @since ??
 *
 * @param {ShortcodeModuleFieldTextAreaProps} param0 Component props.
 *
 * @returns {ReactElement}
 */
export declare const ShortcodeModuleFieldTextArea: ({ name, updateShortcodeAttrs, value, }: ShortcodeModuleFieldTextAreaProps) => ReactElement;
