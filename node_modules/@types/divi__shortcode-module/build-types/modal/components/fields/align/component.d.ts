import { type ReactElement } from 'react';
import { type ShortcodeModuleFieldAlignProps } from './types';
/**
 * Align field component for shortcode module's modal settings. It takes D4 formatted props
 * and adjust it so it can be passed into D5's range field component.
 *
 * @since ??
 *
 * @param {ShortcodeModuleFieldAlignProps} param0 Component props.
 *
 * @returns {ReactElement}
 */
export declare const ShortcodeModuleFieldAlign: ({ defaultValue, name, updateShortcodeAttrs, value, }: ShortcodeModuleFieldAlignProps) => ReactElement;
