import { type ReactElement } from 'react';
import { type ShortcodeModuleFieldCategoriesProps } from './types';
/**
 * Categories field component for shortcode module's modal settings. It takes D4 formatted
 * props and adjust it so it can be passed into D5's checkboxes field component. This
 * field is separated from `ShortcodeModuleFieldCheckboxes` because they have different
 * options and value format.
 *
 * @since ??
 *
 * @param {ShortcodeModuleFieldCategoriesProps} param0 Component props.
 *
 * @returns {ReactElement}
 */
export declare const ShortcodeModuleFieldCategories: ({ name, onChange, options, value, }: ShortcodeModuleFieldCategoriesProps) => ReactElement;
