import { type ReactElement } from 'react';
import { type ShortcodeModuleFieldRichTextProps } from './types';
/**
 * RichText field component for shortcode module's modal settings. It takes D4 formatted props
 * and adjust it so it can be passed into D5's range field component.
 *
 * @since ??
 *
 * @param {ShortcodeModuleFieldRichTextProps} param0 Component props.
 *
 * @returns {ReactElement}
 */
export declare const ShortcodeModuleFieldRichText: ({ name, updateShortcodeAttrs, value, }: ShortcodeModuleFieldRichTextProps) => ReactElement;
