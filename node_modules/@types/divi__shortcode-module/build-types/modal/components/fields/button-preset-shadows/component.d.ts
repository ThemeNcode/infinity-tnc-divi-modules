import { type ReactElement } from 'react';
import { type ShortcodeModuleFieldButtonPresetShadowsProps } from './types';
/**
 * Button Preset Shadows field component for shortcode module's modal settings. It takes D4 formatted
 * props and adjust it so it can be passed into D5's button options field component.
 *
 * Both of Box Shadow and Text Shadow presets have the same options and value format.
 * The only differences are the class name for the preset icons and number of columns.
 * Due to that reason, it's better to keep them as single component.
 *
 * @since ??
 *
 * @param {ShortcodeModuleFieldButtonPresetShadowsProps} param0 Component props.
 *
 * @returns {ReactElement}
 */
export declare const ShortcodeModuleFieldButtonPresetShadows: ({ defaultValue, name, type, updateShortcodeAttrs, value, }: ShortcodeModuleFieldButtonPresetShadowsProps) => ReactElement;
