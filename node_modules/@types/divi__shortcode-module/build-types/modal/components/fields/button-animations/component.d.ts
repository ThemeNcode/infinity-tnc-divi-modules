import { type ReactElement } from 'react';
import { type ShortcodeModuleFieldButtonAnimationsProps } from './types';
/**
 * Button Animations field component for shortcode module's modal settings. It takes D4 formatted
 * props and adjust it so it can be passed into D5's button options field component.
 *
 * @since ??
 *
 * @param {ShortcodeModuleFieldButtonAnimationsProps} param0 Component props.
 *
 * @returns {ReactElement}
 */
export declare const ShortcodeModuleFieldButtonAnimations: ({ defaultValue, name, updateShortcodeAttrs, value, }: ShortcodeModuleFieldButtonAnimationsProps) => ReactElement;
