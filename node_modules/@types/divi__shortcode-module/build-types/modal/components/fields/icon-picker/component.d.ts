import { type ReactElement } from 'react';
import { type ShortcodeModuleFieldIconPickerProps } from './types';
/**
 * Icon Picker field component for shortcode module's modal settings. It takes D4 formatted
 * props and adjust it so it can be passed into D5's icon picker field component.
 *
 * @since ??
 *
 * @param {ShortcodeModuleFieldIconPickerProps} param0 Component props.
 *
 * @returns {ReactElement}
 */
export declare const ShortcodeModuleFieldIconPicker: ({ defaultValue, name, updateShortcodeAttrs, value, }: ShortcodeModuleFieldIconPickerProps) => ReactElement;
