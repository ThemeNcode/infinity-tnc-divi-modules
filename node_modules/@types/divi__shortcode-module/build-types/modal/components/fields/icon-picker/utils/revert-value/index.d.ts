import { type Icon } from '@divi/types';
/**
 * Revert value from D5 to D4 format.
 *
 * @since ??
 *
 * @param {Icon.Font.AttributeValue} value The value in D5 format.
 * @param {string} defaultValue The default value in D4 format.
 *
 * @returns {string} The value in D4 format.
 */
export declare const revertValue: (value: Icon.Font.AttributeValue, defaultValue: string) => string;
