import { type ReactElement } from 'react';
import { type ShortcodeModuleFieldSelectProps } from './types';
/**
 * Select field component for shortcode module's modal settings. It takes D4 formatted props
 * and adjust it so it can be passed into D5's select field component.
 *
 * @since ??
 *
 * @param {ShortcodeModuleFieldSelectProps} param0 Component props.
 *
 * @returns {ReactElement}
 */
export declare const ShortcodeModuleFieldSelect: ({ defaultValue, name, onChange, options, readonly, value, }: ShortcodeModuleFieldSelectProps) => ReactElement;
