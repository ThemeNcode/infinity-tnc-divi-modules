import { type ReactElement } from 'react';
import { type ShortcodeModuleFieldBorderRadiusProps } from './types';
/**
 * BorderRadius field component for shortcode module's modal settings. It takes D4 formatted
 * props and adjust it so it can be passed into D5's border radius field component.
 *
 * @since ??
 *
 * @param {ShortcodeModuleFieldBorderRadiusProps} param0 Component props.
 *
 * @returns {ReactElement}
 */
export declare const ShortcodeModuleFieldBorderRadius: ({ allowedUnits, defaultValue, name, updateShortcodeAttrs, value, }: ShortcodeModuleFieldBorderRadiusProps) => ReactElement;
