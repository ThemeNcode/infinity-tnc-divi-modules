import { type ShortcodeModuleFieldFontValue } from '../../types';
/**
 * Revert value from D5 to D4 format.
 *
 * @since ??
 *
 * @param {ShortcodeModuleFieldFontValue} value The value in D5 format.
 * @param {string} defaultValue The default value in D4 format.
 *
 * @returns {string} The value in D4 format.
 */
export declare const revertValue: (value: ShortcodeModuleFieldFontValue, defaultValue: string) => string;
