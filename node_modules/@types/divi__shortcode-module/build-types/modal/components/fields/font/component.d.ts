import { type ReactElement } from 'react';
import { type ShortcodeModuleFieldFontProps } from './types';
/**
 * Font field component for shortcode module's modal settings. It takes D4 formatted
 * props and adjust it so it can be passed into D5's font field component.
 *
 * @since ??
 *
 * @param {ShortcodeModuleFieldFontProps} param0 Component props.
 *
 * @returns {ReactElement}
 */
export declare const ShortcodeModuleFieldFont: ({ activeOptionTab, defaultValue, fieldLabel, name, updateShortcodeAttrs, value, }: ShortcodeModuleFieldFontProps) => ReactElement;
