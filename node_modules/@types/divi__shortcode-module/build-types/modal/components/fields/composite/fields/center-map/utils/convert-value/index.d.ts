import { type Element, type ShortcodeModule } from '@divi/types';
/**
 * Converts a center_map field value from D4 format to D5 format.
 *
 * @since ??
 *
 * @param {string} value The value in D4 format.
 *
 * @returns {Element.InnerContent.Map.AttributeValue} The value in D5 format.
 */
export declare const convertValue: (value: string, controls: Record<string, ShortcodeModule.Field.Item>) => Element.InnerContent.Map.AttributeValue;
