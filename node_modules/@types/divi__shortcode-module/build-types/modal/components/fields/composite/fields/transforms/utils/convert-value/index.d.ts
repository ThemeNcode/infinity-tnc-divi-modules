import { type ShortcodeModuleFieldCompositeTransformsValue } from '../../types';
/**
 * Converts a transform field value from D4 format to D5 format.
 *
 * @since ??
 *
 * @param {string} value Combined all transform values as JSON string.
 *
 * @returns {ShortcodeModuleFieldCompositeTransformsValue} Combined all transform values as object with D5 format.
 */
export declare const convertValue: (value: string) => ShortcodeModuleFieldCompositeTransformsValue;
