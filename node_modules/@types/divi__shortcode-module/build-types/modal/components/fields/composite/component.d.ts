import { type ReactElement } from 'react';
import { type ShortcodeModuleFieldCompositeProps } from './types';
/**
 * CompositeTransforms field component for shortcode module's modal settings. It takes D4 formatted props
 * and adjust it so it can be passed into D5's select field component.
 *
 * @since ??
 *
 * @param {ShortcodeModuleFieldCompositeProps} param0 Component props.
 *
 * @returns {ReactElement}
 */
export declare const ShortcodeModuleFieldComposite: ({ activeOptionTab, defaultValue, field, name, shortcodeAttrs, updateShortcodeAttrs, value, childModules, hasStickyState, }: ShortcodeModuleFieldCompositeProps) => ReactElement;
