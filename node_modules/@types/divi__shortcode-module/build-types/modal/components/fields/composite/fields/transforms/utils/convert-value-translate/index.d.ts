import { type FieldLibrary } from '@divi/types';
/**
 * Converts a transform translate value from D4 format to D5 format.
 *
 * @since ??
 *
 * @param {string} value The transform translate value in D4 format.
 * @param {string} linked The transform translate linked value in D4 format.
 *
 * @returns {object} The transform translate value in D5 format.
 */
export declare const convertValueTranslate: (value: string, linked: string) => FieldLibrary.TransformTranslate.Value;
