import { type ReactElement } from 'react';
import { type ShortcodeModuleFieldCompositeScrollEffectProps } from './types';
/**
 * CompositeScrollEffect field component for shortcode module's modal settings. It takes D4 formatted props
 * and adjust it so it can be passed into D5's field Map component.
 *
 * @since ??
 *
 * @param {ShortcodeModuleFieldCompositeScrollEffectProps} param0 Component props.
 *
 * @returns {ReactElement}
 */
export declare const ShortcodeModuleFieldCompositeScrollEffect: ({ activeOptionTab, defaultValue, name, updateShortcodeAttrs, value, }: ShortcodeModuleFieldCompositeScrollEffectProps) => ReactElement;
