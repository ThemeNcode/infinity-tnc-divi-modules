import { type FieldLibrary } from '@divi/types';
/**
 * Converts a transform scale value from D4 format to D5 format.
 *
 * @since ??
 *
 * @param {string} value The transform scale value in D4 format.
 * @param {string} linked The transform scale linked value in D4 format.
 *
 * @returns {object} The transform scale value in D5 format.
 */
export declare const convertValueScale: (value: string, linked: string) => FieldLibrary.TransformScale.Value;
