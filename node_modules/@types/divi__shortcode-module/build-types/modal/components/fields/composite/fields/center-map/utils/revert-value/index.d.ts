import { type Element, type ShortcodeModule } from '@divi/types';
/**
 * Reverts a center_map field value from D5 format to D4 format.
 *
 * @since ??
 *
 * @param {string} value The value in D5 format.
 *
 * @returns {Element.InnerContent.Map.AttributeValue} The value in D4 format.
 */
export declare const revertValue: (value: Element.InnerContent.Map.AttributeValue, controls: Record<string, ShortcodeModule.Field.Item>) => Record<string, string>;
