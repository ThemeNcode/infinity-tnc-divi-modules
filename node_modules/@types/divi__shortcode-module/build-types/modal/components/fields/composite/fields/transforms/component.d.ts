import { type ReactElement } from 'react';
import { type ShortcodeModuleFieldCompositeTransformsProps } from './types';
/**
 * CompositeTransforms field component for shortcode module's modal settings. It takes D4 formatted props
 * and adjust it so it can be passed into D5's select field component.
 *
 * @since ??
 *
 * @param {ShortcodeModuleFieldCompositeTransformsProps} param0 Component props.
 *
 * @returns {ReactElement}
 */
export declare const ShortcodeModuleFieldCompositeTransforms: ({ activeOptionTab, defaultValue, name, updateShortcodeAttrs, value, }: ShortcodeModuleFieldCompositeTransformsProps) => ReactElement;
