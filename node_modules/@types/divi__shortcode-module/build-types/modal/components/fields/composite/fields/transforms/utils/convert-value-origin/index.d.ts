import { type FieldLibrary } from '@divi/types';
/**
 * Converts a transform origin value from D4 format to D5 format.
 *
 * @since ??
 *
 * @param {string} value The transform origin value in D4 format.
 *
 * @returns {object} The transform origin value in D5 format.
 */
export declare const convertValueOrigin: (value: string) => FieldLibrary.TransformOrigin.Value;
