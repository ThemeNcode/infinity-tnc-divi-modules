import { type ReactElement } from 'react';
import { type ShortcodeModuleFieldCompositeBorderStylesProps } from './types';
/**
 * Border Styles composite field component for shortcode module's modal settings. It takes D4 formatted props
 * and adjust it so it can be passed into D5's select field component.
 *
 * @since ??
 *
 * @param {ShortcodeModuleFieldCompositeBorderStylesProps} param0 Component props.
 *
 * @returns {ReactElement}
 */
export declare const ShortcodeModuleFieldCompositeBorderStyles: ({ activeOptionTab, compositeStructure, defaultValue, name, shortcodeAttrs, updateShortcodeAttrs, value, hasStickyState, }: ShortcodeModuleFieldCompositeBorderStylesProps) => ReactElement;
