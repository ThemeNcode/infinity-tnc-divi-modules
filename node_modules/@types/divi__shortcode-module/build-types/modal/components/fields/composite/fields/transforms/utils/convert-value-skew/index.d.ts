import { type FieldLibrary } from '@divi/types';
/**
 * Converts a transform skew value from D4 format to D5 format.
 *
 * @since ??
 *
 * @param {string} value The transform skew value in D4 format.
 * @param {string} linked The transform skew linked value in D4 format.
 *
 * @returns {object} The transform skew value in D5 format.
 */
export declare const convertValueSkew: (value: string, linked: string) => FieldLibrary.TransformSkew.Value;
