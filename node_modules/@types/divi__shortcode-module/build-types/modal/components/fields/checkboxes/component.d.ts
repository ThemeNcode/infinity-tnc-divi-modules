import { type ReactElement } from 'react';
import { type ShortcodeModuleFieldCheckboxesProps } from './types';
/**
 * Checkboxes field component for shortcode module's modal settings. It takes D4 formatted
 * props and adjust it so it can be passed into D5's checkboxes field component. This
 * field is separated from `ShortcodeModuleFieldCategories` because they have different
 * options and value format.
 *
 * @since ??
 *
 * @param {ShortcodeModuleFieldCheckboxesProps} param0 Component props.
 *
 * @returns {ReactElement}
 */
export declare const ShortcodeModuleFieldCheckboxes: ({ name, onChange, options, value, }: ShortcodeModuleFieldCheckboxesProps) => ReactElement;
