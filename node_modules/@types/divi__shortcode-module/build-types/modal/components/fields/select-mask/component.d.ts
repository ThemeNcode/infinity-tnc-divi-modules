import { type ReactElement } from 'react';
import { type ShortcodeModuleFieldSelectMaskProps } from './types';
/**
 * SelectMask field component for shortcode module's modal settings. It takes D4 formatted props
 * and adjust it so it can be passed into D5's select field component.
 *
 * @since ??
 *
 * @param {ShortcodeModuleFieldSelectMaskProps} param0 Component props.
 *
 * @returns {ReactElement}
 */
export declare const ShortcodeModuleFieldSelectMask: ({ backgroundAttr, defaultValue, name, updateShortcodeAttrs, value, }: ShortcodeModuleFieldSelectMaskProps) => ReactElement;
