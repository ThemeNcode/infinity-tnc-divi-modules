import { type ReactElement } from 'react';
import { type ShortcodeModuleFieldUploadProps } from './types';
/**
 * Upload field component for shortcode module's modal settings. It takes D4 formatted props
 * and adjust it so it can be passed into D5's range field component.
 *
 * @since ??
 *
 * @param {ShortcodeModuleFieldUploadProps} param0 Component props.
 *
 * @returns {ReactElement}
 */
export declare const ShortcodeModuleFieldUpload: ({ chooseText, dataType, defaultValue, name, updateShortcodeAttrs, uploadButtonText, value, }: ShortcodeModuleFieldUploadProps) => ReactElement;
