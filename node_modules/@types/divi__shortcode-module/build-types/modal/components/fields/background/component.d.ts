import { type ReactElement } from 'react';
import { type ShortcodeModuleFieldBackgroundProps } from './types';
/**
 * ShortcodeModuleFieldBackground field component for shortcode module's modal settings. It takes D4 formatted props
 * and adjust it so it can be passed into D5's field component.
 *
 * @since ??
 *
 * @param {ShortcodeModuleFieldBackgroundProps} param0 Component props.
 *
 * @returns {ReactElement}
 */
export declare const ShortcodeModuleFieldBackground: ({ activeOptionTab, childModules, backgroundFields, defaultValue, name, parentModule, shortcodeAttrs, updateShortcodeAttrs, value, hasStickyState, }: ShortcodeModuleFieldBackgroundProps) => ReactElement;
