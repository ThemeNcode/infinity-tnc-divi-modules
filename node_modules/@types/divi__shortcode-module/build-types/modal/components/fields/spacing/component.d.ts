import { type ReactElement } from 'react';
import { type ShortcodeModuleFieldSpacingProps } from './types';
/**
 * Spacing field component for shortcode module's modal settings. It takes D4 formatted
 * props and adjust it so it can be passed into D5's spacing field component.
 *
 * @since ??
 *
 * @param {ShortcodeModuleFieldSpacingProps} param0 Component props.
 *
 * @returns {ReactElement}
 */
export declare const ShortcodeModuleFieldSpacing: ({ allowedUnits, defaultValue, min, name, updateShortcodeAttrs, value, }: ShortcodeModuleFieldSpacingProps) => ReactElement;
