import { type ReactElement } from 'react';
import { type ShortcodeModuleFieldCodeProps } from './types';
/**
 * Code field component for shortcode module's modal settings. It takes D4 formatted props
 * and adjust it so it can be passed into D5's range field component.
 *
 * @since ??
 *
 * @param {ShortcodeModuleFieldCodeProps} param0 Component props.
 *
 * @returns {ReactElement}
 */
export declare const ShortcodeModuleFieldCode: ({ defaultValue, name, updateShortcodeAttrs, value, }: ShortcodeModuleFieldCodeProps) => ReactElement;
