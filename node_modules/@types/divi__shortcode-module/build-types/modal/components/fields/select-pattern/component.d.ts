import { type ReactElement } from 'react';
import { type ShortcodeModuleFieldSelectPatternProps } from './types';
/**
 * SelectPattern field component for shortcode module's modal settings. It takes D4 formatted props
 * and adjust it so it can be passed into D5's select field component.
 *
 * @since ??
 *
 * @param {ShortcodeModuleFieldSelectPatternProps} param0 Component props.
 *
 * @returns {ReactElement}
 */
export declare const ShortcodeModuleFieldSelectPattern: ({ backgroundAttr, defaultValue, name, updateShortcodeAttrs, value, }: ShortcodeModuleFieldSelectPatternProps) => ReactElement;
