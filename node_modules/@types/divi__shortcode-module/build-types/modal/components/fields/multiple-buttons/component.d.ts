import { type ReactElement } from 'react';
import { type ShortcodeModuleFieldMultipleButtonsProps } from './types';
/**
 * MultipleButtons field component for shortcode module's modal settings. It takes D4 formatted
 * props and adjust it so it can be passed into D5's font field component.
 *
 * @since ??
 *
 * @param {ShortcodeModuleFieldMultipleButtonsProps} param0 Component props.
 *
 * @returns {ReactElement}
 */
export declare const ShortcodeModuleFieldMultipleButtons: ({ defaultValue, field, name, updateShortcodeAttrs, value, }: ShortcodeModuleFieldMultipleButtonsProps) => ReactElement;
