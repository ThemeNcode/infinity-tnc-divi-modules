import { type ReactElement } from 'react';
import { type ShortcodeModuleFieldConditionProps } from './types';
/**
 * Condition field component for shortcode module's modal settings. It takes D4 formatted props
 * and adjust it so it can be passed into D5's range field component.
 *
 * @since ??
 *
 * @param {ShortcodeModuleFieldConditionProps} param0 Component props.
 *
 * @returns {ReactElement}
 */
export declare const ShortcodeModuleFieldCondition: ({ name, parentModule, shortcodeAttrs, updateShortcodeAttrs, value, }: ShortcodeModuleFieldConditionProps) => ReactElement;
