/**
 * Decodes a value that has been encoded by the encodeValue function.
 *
 * It takes a string, replaces all double quotes, left brackets, and right
 * brackets URL encoded equivalents with the actual characters, and then parses the string as JSON.
 *
 * @since ??
 *
 * @param {string} value The value to decode.
 *
 * @returns {Record<string, string>[]} Decoded value.
 */
export declare const decodeValue: <TValue = Record<string, string>[]>(value: string) => [] | TValue;
