/**
 * Encodes a value.
 *
 * It takes a value, stringifies it, and then replaces all double quotes, left brackets, and right
 * brackets with their URL encoded equivalents.
 *
 * @since ??
 *
 * @param {Record<string, string>[]} value The value to encode.
 *
 * @returns {string} Encoded value.
 */
export declare const encodeValue: <TValue = Record<string, string>[]>(value: TValue) => string;
