import { type ReactElement } from 'react';
import { type ShortcodeModuleFieldToggleProps } from './types';
/**
 * Toggle field component for shortcode module's modal settings. It takes D4 formatted
 * props and adjust it so it can be passed into D5's toggle field component.
 *
 * @since ??
 *
 * @param {ShortcodeModuleFieldToggleProps} param0 Component props.
 *
 * @returns {ReactElement}
 */
export declare const ShortcodeModuleFieldToggle: ({ defaultValue, name, onChange, options, readonly, value, }: ShortcodeModuleFieldToggleProps) => ReactElement;
