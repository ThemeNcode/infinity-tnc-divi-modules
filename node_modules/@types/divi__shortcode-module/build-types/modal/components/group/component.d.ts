import { type ReactElement } from 'react';
import { type ShortcodeModuleGroupProps } from './types';
/**
 * Shortcode module group component.
 *
 * @since ??
 *
 * @param {object} params Parameters for the ShortcodeModuleGroup component.
 * @param {string} params.activeOptionTab Active option tab.
 * @param {Array} params.childModules Child modules.
 * @param {boolean} params.hasStickyState Whether it has a sticky state.
 * @param {object} params.groupFields Group fields.
 * @param {object} params.groupFieldsChild Group fields for child modules.
 * @param {string} params.groupLabel Group label.
 * @param {string} params.groupName Group name.
 * @param {string} params.panelName Panel name.
 * @param {object} params.shortcodeAttrs Shortcode attributes.
 * @param {string} params.shortcodeContent Content of the shortcode.
 * @param {object} params.subToggles Sub toggles.
 * @param {Function} params.updateShortcodeAttrs Function to update shortcode attributes.
 * @param {Function} params.updateShortcodeContent Function to update shortcode content.
 * @param {object} params.parentModule Parent module.
 *
 * @returns {ReactElement} React element representing the shortcode module group component.
 */
export declare const ShortcodeModuleGroup: ({ activeOptionTab, childModules, hasStickyState, groupFields, groupFieldsChild, groupLabel, groupName, panelName, shortcodeAttrs, shortcodeContent, subToggles, updateShortcodeAttrs, updateShortcodeContent, parentModule, }: ShortcodeModuleGroupProps) => ReactElement;
