import { type FilteredField, type FilterFieldsParams } from './types';
/**
 * Filters fields based on certain conditions and returns a list of filtered fields.
 *
 * - If the field is not belong to the current group, then it will be hidden.
 * - If the field type is hidden, skip, or computed, then it will be hidden.
 * - If the field has a sub toggle property, and it is not match with current active sub toggle, then it will be hidden.
 * - If the field has a dependency, and the dependency rule is not met, then it will be hidden.
 *
 * @since ??
 */
export declare const filterFields: ({ activeOptionTab, groupFields, groupFieldsChild, groupName, shortcodeAttrs, subToggle, hasStickyState, }: FilterFieldsParams) => FilteredField[];
