import { type ReactElement } from 'react';
import { type ShortcodeModuleFieldComponentProps } from './types';
/**
 * Get suitable field component for current field.
 *
 * The tricky part of this is how to keep the mechanism maintainable. Had we abstracted
 * this into separate function, we would have to pass a lot of props to it. On top of that:
 * - Each field component has different props.
 * - The equivalent component (eg, select) might have different formatting.
 * So far keeping the switch in group component offers the most flexibility and preventing
 * us to pass large amount of props (where many of them will not be used) to field level component.
 *
 *
 * @since ??
 *
 * @param {object} params Parameters for the ShortcodeModuleFieldComponent component.
 * @param {string} params.activeOptionTab Active option tab.
 * @param {string} params.defaultValue Default value of the field.
 * @param {object} params.field Field object.
 * @param {string} params.name Field name.
 * @param {object} params.shortcodeAttrs Shortcode attributes.
 * @param {Function} params.updateShortcodeAttrs Function to update shortcode attributes.
 * @param {string} params.value Current value of the field.
 * @param {Array} params.childModules Child modules.
 * @param {object} params.parentModule Parent module.
 * @param {boolean} params.hasStickyState Whether it has a sticky state.
 *
 * @returns {ReactElement} React element representing the suitable field component.
 */
export declare const ShortcodeModuleFieldComponent: ({ activeOptionTab, defaultValue, field, name, shortcodeAttrs, updateShortcodeAttrs, value, childModules, parentModule, hasStickyState, }: ShortcodeModuleFieldComponentProps) => ReactElement;
