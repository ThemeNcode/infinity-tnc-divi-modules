import { type ReactElement } from 'react';
import { type ShortcodeModuleBodyPanelsProps } from './types';
export declare const bodyPanelNames: Record<string, string>;
export declare const bodyPanelNameMap: Record<string, keyof typeof bodyPanelNames>;
/**
 * Body panels for shortcode module modal.
 *
 * @since ??
 *
 * @param {object} params Parameters for the ShortcodeModuleBodyPanels component.
 * @param {string} params.adminOptionsGeneralUrl The URL for admin options.
 * @param {object} params.moduleDefinition Definition of the shortcode module.
 * @param {object} params.moduleDefinitionChild Definition of the child shortcode module.
 * @param {Array} params.childModules Child modules.
 * @param {boolean} params.hasStickyState Whether it has a sticky state.
 * @param {object} params.parentModule Parent module.
 * @param {Function} params.onAddNewChildModule Function to add a new child module.
 * @param {Function} params.onChangeOrderChildModule Function to change the order of child modules.
 * @param {Function} params.onClickDeleteChildModule Function to delete a child module.
 * @param {Function} params.onClickDuplicateChildModule Function to duplicate a child module.
 * @param {Function} params.onClickSettingsChildModule Function to handle settings for a child module.
 * @param {string} params.owner The owner of the module.
 * @param {object} params.shortcodeAttrs Attributes for the shortcode.
 * @param {string} params.shortcodeContent Content of the shortcode.
 * @param {Function} params.updateShortcodeAttrs Function to update shortcode attributes.
 * @param {Function} params.updateShortcodeContent Function to update shortcode content.
 *
 * @returns {ReactElement} Body panel react component.
 */
export declare const ShortcodeModuleBodyPanels: ({ adminOptionsGeneralUrl, moduleDefinition, moduleDefinitionChild, childModules, hasStickyState, parentModule, onAddNewChildModule, onChangeOrderChildModule, onClickDeleteChildModule, onClickDuplicateChildModule, onClickSettingsChildModule, owner, shortcodeAttrs, shortcodeContent, updateShortcodeAttrs, updateShortcodeContent, }: ShortcodeModuleBodyPanelsProps) => ReactElement;
