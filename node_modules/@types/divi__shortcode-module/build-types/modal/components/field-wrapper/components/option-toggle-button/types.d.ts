import { type CSSProperties, type MouseEvent } from 'react';
export interface ShortcodeModuleOptionToggleButtonProps {
    active: boolean;
    iconName: string;
    styles?: CSSProperties;
    onClick: (event: MouseEvent) => void;
}
