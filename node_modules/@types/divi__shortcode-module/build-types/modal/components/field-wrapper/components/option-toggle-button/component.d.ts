import { type ReactElement } from 'react';
import { type ShortcodeModuleOptionToggleButtonProps } from './types';
/**
 * Option toggle button component for responsive, hover, and sticky options in shortcode module modal's field.
 *
 * @since ??
 *
 * @param {ShortcodeModuleOptionToggleButtonProps} props Component props.
 * @param {boolean} props.active Whether the button is active or not.
 * @param {string} props.iconName Icon name of the button.
 * @param {object} props.styles CSS Styles for the icon.
 * @param {Function} props.onClick Click event handler.
 *
 * @returns {ReactElement} React component.
 */
export declare const OptionToggleButton: ({ active, iconName, styles, onClick, }: ShortcodeModuleOptionToggleButtonProps) => ReactElement;
