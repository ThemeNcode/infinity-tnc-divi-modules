import { type ReactElement } from 'react';
import { type ShortcodeModuleFieldWrapperProps } from './types';
import './style.scss';
/**
 * Field wrapper for shortcode module modal's option. THis component provides wrapper, label,
 * options toggle (responsive, hover, and sticky), and description for inputs.
 *
 * @since ??
 *
 * @param {ShortcodeModuleFieldWrapperProps} props Component props.
 *
 * @returns {ReactElement}
 */
export declare const ShortcodeModuleFieldWrapper: ({ activeOptionTab, description, enableReset, label, onOptionTabClick, onContextMenu, onReset, options, optionsValues, onHoverClick, onResponsiveClick, onStickyClick, children, }: ShortcodeModuleFieldWrapperProps) => ReactElement;
