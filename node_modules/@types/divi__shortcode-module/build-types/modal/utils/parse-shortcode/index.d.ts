import { type ShortcodeObject } from './types';
/**
 * It takes a string of shortcode, and returns an array of objects that represent the shortcodes.
 *
 * @since ??
 *
 * @param {string} shortcode String - The shortcode string to parse.
 *
 * @returns {ShortcodeObject[]} An array of objects.
 */
export declare const parseShortcode: (shortcode: string) => ShortcodeObject[];
