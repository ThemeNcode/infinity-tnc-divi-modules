import { type OnOff } from '@divi/types';
/**
 * Checks if the current post type is "post".
 *
 * @since ??
 *
 * @returns {OnOff} Will return "on" if the post type is "post", otherwise "off".
 */
export declare const callbackIsPost: () => OnOff;
