import { type OnOff } from '@divi/types';
/**
 * Checks if the current post type is "post" or if the current layout is a theme builder layout.
 *
 * @since ??
 *
 * @returns {OnOff} Will return "on" if the post type is "post" or it is a theme builder layout.
 */
export declare const callbackIsPostOrTBLayout: () => OnOff;
