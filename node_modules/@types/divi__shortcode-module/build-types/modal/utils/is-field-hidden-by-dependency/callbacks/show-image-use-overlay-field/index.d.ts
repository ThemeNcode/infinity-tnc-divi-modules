/**
 * Checks if the the image overlay should be used or not.
 *
 * @since ??
 *
 * @param {Record<string, string>} shortcodeAttrs An Object containing key-value pairs of attributes for a shortcode.
 *
 * @returns {'on' | 'off'} Will return "on" if the image overlay should be used, otherwise "off".
 */
export declare const callbackShowImageUseOverlayField: (shortcodeAttrs: Record<string, string>) => 'on' | 'off';
