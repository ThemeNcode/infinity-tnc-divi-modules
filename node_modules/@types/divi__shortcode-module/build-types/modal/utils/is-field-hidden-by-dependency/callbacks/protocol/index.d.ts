/**
 * Checks if the current protocol is "https" or "http".
 *
 * @since ??
 *
 * @returns {'https' | 'http'} Will return "https" if the current protocol is "https", otherwise "http".
 */
export declare const callbackProtocol: () => 'https' | 'http';
