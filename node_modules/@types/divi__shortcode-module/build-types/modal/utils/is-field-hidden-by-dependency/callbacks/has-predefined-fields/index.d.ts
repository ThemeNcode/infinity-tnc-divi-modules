import { type OnOff } from '@divi/types';
import { type FieldFlatten } from '../../../../types';
/**
 * This function determines whether a callback has predefined fields based on shortcode attributes and
 * field data.
 *
 * @since ??
 *
 * @param {Record<string, string>} shortcodeAttrs An object containing attributes of a shortcode.
 * @param {FieldFlatten[]} fieldsFlatten It is an array of flattened fields that are used in a form.
 * @param {FieldFlatten[]} fieldsFlattenChild An array of flattened fields of the child form.
 *
 * @returns {OnOff} Either the string 'on' or 'off'.
 */
export declare const callbackHasPredefinedFields: (shortcodeAttrs: Record<string, string>, fieldsFlatten: FieldFlatten[], fieldsFlattenChild: FieldFlatten[]) => OnOff;
