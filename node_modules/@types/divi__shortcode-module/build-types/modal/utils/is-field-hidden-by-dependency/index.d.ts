import { type IsFieldHiddenParams } from './types';
/**
 * Determines if a field should be hidden by checking its dependencies.
 *
 * The field is assumed to be visible unless it is explicitly defined as hidden by calculating
 * the following field properties:
 *
 * - show_if
 * - show_if_not
 * - depends_on
 * - depends_on_responsive
 * - depends_show_if
 * - depends_show_if_not
 * .
 *
 * @since ??
 *
 * @param {IsFieldHiddenParams} params0 Function parameters.
 *
 * @returns {boolean} Wether the field is hidden or not.
 */
export declare const isFieldHiddenByDependency: ({ activeOptionTab, field, shortcodeAttrs, fieldsFlatten, fieldsFlattenChild, hasStickyState, }: IsFieldHiddenParams) => boolean;
