import { type ShortcodeModule } from '@divi/types';
import { type GetGroupedFieldsParams } from './types';
/**
 * Generate object of grouped processed fields based module's fields definition.
 *
 * @since ??
 */
export declare const getGroupedFields: ({ fieldsDefinition, shortcodeName, settings, isChildModule, isSpecialtySection, adminOptionsGeneralUrl, orderClass, }: GetGroupedFieldsParams) => ShortcodeModule.GroupField.Item;
