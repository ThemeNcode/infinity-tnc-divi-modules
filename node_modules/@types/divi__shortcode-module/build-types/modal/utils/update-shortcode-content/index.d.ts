import { type UpdateShortcodeContent } from '../update-shortcode/types';
/**
 * Update the content of a shortcode.
 *
 * @since ??
 *
 * @param {UpdateShortcodeContent} content The content to be updated.
 * @param {string} shortcode The shortcode string to be updated.
 * @param {null | number[]} targetIndex The index of the shortcode to update. If null, all shortcodes will be updated.
 *
 * @returns {string} Shortcode string with the content replaced.
 */
export declare const updateShortcodeContent: (content: UpdateShortcodeContent, shortcode: string, targetIndex?: null | number[]) => string;
