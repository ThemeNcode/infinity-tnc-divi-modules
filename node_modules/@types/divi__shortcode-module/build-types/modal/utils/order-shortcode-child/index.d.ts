/**
 * It takes a shortcode and an array of indexes, and returns a new shortcode with the children of the
 * shortcode in the order of the indexes.
 *
 * @since ??
 *
 * @param {number[]} oldIndexes The old indexes of the children in the parent shortcode.
 * @param {string} shortcode The shortcode to be reordered.
 *
 * @returns {string} The shortcode with the children in the order of the indexes.
 */
export declare const orderShortcodeChild: (oldIndexes: number[], shortcode: string) => string;
