import { type ShortcodeModule } from '@divi/types';
import { type FieldFlatten } from '../../types';
import { type Comparator } from './types';
/**
 * Find field in a flattened fields array.
 *
 * @since ??
 */
export declare const findField: (fieldsFlattened: FieldFlatten[], propertyName: keyof ShortcodeModule.Field.Item | 'fieldName', propertyValue: unknown, comparator?: Comparator) => FieldFlatten;
