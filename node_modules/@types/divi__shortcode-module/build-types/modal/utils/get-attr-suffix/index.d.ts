import { type GetAttrSuffixParams } from './types';
/**
 * Get attribute name with suffix.
 *
 * This function will account the the following data:
 *
 * - active tab
 * - field definition
 * - base field definition if provided
 * - shortcode attributes.
 *
 * @since ??
 *
 * @param {GetAttrSuffixParams} params The function params.
 *
 * @returns {string} The suffix of the attribute name.
 */
export declare const getAttrSuffix: (params: GetAttrSuffixParams) => string;
