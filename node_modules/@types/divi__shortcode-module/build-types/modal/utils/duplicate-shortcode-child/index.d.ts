/**
 * It takes a shortcode string and a target index, and returns a new shortcode string with the child at
 * the target index duplicated.
 *
 * @since ??
 *
 * @param {number} targetIndex The index of the child shortcode to duplicate.
 * @param {string} shortcode The shortcode string to be parsed.
 *
 * @returns {string} The shortcode string with the child at the target index duplicated.
 */
export declare const duplicateShortcodeChild: (targetIndex: number, shortcode: string) => string;
