import { type ShortcodeModule } from '@divi/types';
/**
 * Checks if the field definition is sticky enabled.
 *
 * @since ??
 */
export declare const isFieldSticky: (field: ShortcodeModule.Field.Item) => boolean;
