import { type ShortcodeModule } from '@divi/types';
/**
 * It filters out the grouped processed fields which are not needed to be displayed in the modal.
 *
 * @since ??
 */
export declare const filterGroupedFields: (groupedFields: ShortcodeModule.GroupField.Item) => ShortcodeModule.GroupField.Item;
