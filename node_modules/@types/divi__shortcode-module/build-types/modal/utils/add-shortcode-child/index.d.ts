/**
 * It takes a shortcode string, parses it, and then adds a new child shortcode to the end of the parent
 * shortcode's content.
 *
 * @since ??
 *
 * @param {string} shortcode The shortcode to add a child to.
 *
 * @returns {string} The shortcode with the new child shortcode added to the end of the parent shortcode's content.
 */
export declare const addShortcodeChild: (shortcode: string) => string;
