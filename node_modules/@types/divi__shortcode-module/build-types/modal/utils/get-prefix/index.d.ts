/**
 * Prefixes a string key with a prefix string using the provided delimiter
 * In case the prefix is empty, original key is returned.
 * Equivalent of `et_builder_add_prefix()` in PHP.
 *
 * @since ??
 *
 * @param {string} prefix  Prefix string.
 * @param {string} key String key.
 * @param {string} delimiter  Delimiter.
 *
 * @returns {string}
 */
export declare const getPrefix: (prefix: string, key: string, delimiter?: string) => string;
