import { type ShortcodeModule } from '@divi/types';
/**
 * Check if the field definition is sticky and the sticky enabled attribute is set to 'on'.
 *
 * @since ??
 */
export declare const isAttrStickyEnabled: (attrNameBase: string, field: ShortcodeModule.Field.Item, shortcodeAttrs: Record<string, string>) => boolean;
