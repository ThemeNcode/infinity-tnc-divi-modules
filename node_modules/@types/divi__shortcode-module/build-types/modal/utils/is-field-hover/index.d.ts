import { type ShortcodeModule } from '@divi/types';
/**
 * Checks if the field definition is hover enabled.
 *
 * @since ??
 */
export declare const isFieldHover: (field: ShortcodeModule.Field.Item) => boolean;
