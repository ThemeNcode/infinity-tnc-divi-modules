/**
 * It takes a shortcode and a target index, and returns a new shortcode with the child at the target
 * index removed.
 *
 * @since ??
 *
 * @param {number} targetIndex The index of the child shortcode you want to delete.
 * @param {string} shortcode The shortcode to be updated.
 *
 * @returns {string} The shortcode with the child at the target index removed.
 */
export declare const deleteShortcodeChild: (targetIndex: number, shortcode: string) => string;
