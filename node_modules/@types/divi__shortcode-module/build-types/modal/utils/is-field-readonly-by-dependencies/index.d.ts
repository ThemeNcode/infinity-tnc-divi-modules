import { type IsFieldReadonlyByDependenciesParams } from './types';
/**
 * Checks if the field definition supports readonly condition and it's active.
 *
 * @since ??
 */
export declare const isFieldReadonlyByDependencies: ({ field, parentShortcodeAttrs, shortcodeAttrs, }: IsFieldReadonlyByDependenciesParams) => boolean;
