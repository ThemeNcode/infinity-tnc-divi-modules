/**
 * Register history / log store.
 *
 * History is captured in various context such as modal, interaction, post, etc.
 * Some contexts allows for undo / redo via UI / keyboard shortcut.
 * Some contexts are simply for logging error reports.
 *
 * @since ??
 *
 * @returns {void}
 */
export declare const registerHistoryStore: () => void;
