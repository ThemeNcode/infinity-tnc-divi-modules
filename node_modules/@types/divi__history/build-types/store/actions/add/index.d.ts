import { type History } from '@divi/types';
/**
 * Adds a history item.
 *
 * This action adds a history item to the specified category to the data store.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/history').add(
 *   'edit-post',
 *    'SYNC_TO_SERVER',
 *   'serialize-to-gb-button',
 *   {
 *     label: 'Serialize to GB Button',
 *     details: 'Serialize to GB Button has been clicked',
 *   },
 * );
 * ```
 */
export declare const add: (category: string, actionType: string, trigger: string, payload: History.Store.Actions.Add.Payload) => History.Store.Actions.Add.ReturnValue;
