import { type History } from '@divi/types';
/**
 * Retrieves the entire state object.
 *
 * Returns the entire history state object from the store.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example
 * ```
 * window.divi.data.select('divi/history').allState();
 * ```
 */
export declare const allState: (state: History.Store.ImmutableState) => History.Store.ImmutableState;
