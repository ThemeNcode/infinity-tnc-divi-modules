import { type History } from '@divi/types';
/**
 * Gets sub history.
 *
 * Retrieves sub history from a given category.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * const subHistory = window.divi.data.select('divi/history').getSubHistory('edit-post');
 * console.log(subHistory);
 * // Output:
 * // [
 * //   {
 * //     0:{
 * //       actionType: "SYNC_TO_SERVER",
 * //       payload: {
 * //         details: "Serialize to GB Button has been clicked",
 * //         label: "Serialize to GB Button"
 * //       },
 * //       trigger: "serialize-to-gb-button"
 * //     }
 * //   }
 * // ]
 * ```
 */
export declare const getSubHistory: (state: History.Store.ImmutableState, category: string, defaultValue?: any) => History.Store.Item[];
