import { type History } from '@divi/types';
/**
 * Gets recent sub history.
 *
 * Retrieves a subset of the recent sub-history for a specified category from the store state.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.select('divi/history').getRecentSubHistory('category', 5);
 * ```
 */
export declare const getRecentSubHistory: (state: History.Store.ImmutableState, category: string, max?: number) => History.Store.Item[];
