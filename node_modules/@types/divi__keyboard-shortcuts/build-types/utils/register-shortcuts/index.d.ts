import { type KeyboardShortcuts } from '@divi/types';
/**
 * Register shortcuts.
 *
 * Register multiple shortcuts at once.
 * This function calls `registerShortcut`.
 *
 * @since ??
 *
 * @see {@link registerShortcut}
 *
 * @param {Array<object>} shortcuts Shortcut configurations.
 * @param {string} shortcuts.shortcut.name Shortcut name.
 * @param {string} shortcuts.shortcut.label Shortcut label.
 * @param {string} shortcuts.shortcut.category Shortcut category.
 * @param {Array<string>} shortcuts.shortcut.keys Shortcut keys.
 * @param {Array<string>} shortcuts.shortcut.paramKeys Optional param keys.
 * @param {string} shortcuts.shortcut.isEnabled Optional callback back function to check if shortcut is enabled.
 * @param {string} shortcuts.shortcut.action Callback function to call when shortcut is used.
 *
 * @returns {void}
 *
 * @example
 * ```ts
 * window.divi.keyboardShortcuts.registerShortcuts([{
 *  name: 'myShortcut',
 *  label: 'My Shortcut',
 *  category: 'myCategory',
 *  keys: ['Alt', 'M'],
 *  action: () => console.log('Shortcut triggered!')
 *  }]);
 * ```
 */
export declare const registerShortcuts: (shortcuts: KeyboardShortcuts.Shortcut.Item[]) => void;
