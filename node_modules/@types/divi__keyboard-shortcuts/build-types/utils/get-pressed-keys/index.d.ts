/**
 * Get pressed keys.
 *
 * Gets pressed keys when the keyboard shortcuts are active.
 *
 * @since ??
 *
 * @returns {Array<string>} The pressed keys.
 *
 * @example
 * ```ts
 * const pressedKeys = window.divi.keyboardShortcuts.getPressedKeys();
 * ```
 */
export declare const getPressedKeys: () => string[];
