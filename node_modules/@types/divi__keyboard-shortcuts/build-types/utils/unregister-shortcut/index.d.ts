/**
 * Unregister Shortcut.
 *
 * Unregister shortcut from the keyboard shortcuts registry.
 *
 * @since ??
 *
 * @see {@link unregisterShortcuts}
 *
 * @param {string} name Shortcut name.
 *
 * @returns {void}
 *
 * @example
 * ```ts
 * window.divi.keyboardShortcuts.unregisterShortcut('myShortcut');
 * ```
 *
 * @example
 * ```ts
 * window.divi.keyboardShortcuts.unregisterShortcut('closeEditingHistoryModal');
 * ```
 */
export declare const unregisterShortcut: (name: string) => void;
