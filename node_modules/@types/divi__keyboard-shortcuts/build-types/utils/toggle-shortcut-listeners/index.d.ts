/**
 * Toggle Shortcut Listeners.
 *
 * Toggle keyboard shortcuts listeners.
 *
 * @since ??
 *
 * @param {boolean} enableListeners Whether listeners should be active.
 *
 * @returns {void}
 *
 * @example
 * ```ts
 * // Disable shortcut listeners.
 * window.divi.keyboardShortcuts.toggleShortcutListeners(false);
 * ```
 *
 * @example
 * ```ts
 * // Enable shortcut listeners.
 * window.divi.keyboardShortcuts.toggleShortcutListeners(true);
 * ```
 */
export declare const toggleShortcutListeners: (enableListeners: boolean) => void;
