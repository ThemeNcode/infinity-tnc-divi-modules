import { type KeyboardShortcuts } from '@divi/types';
/**
 * Register Shortcut.
 *
 * Register shortcut in the keyboard shortcut registry.
 *
 * @since ??
 *
 * @see {@link registerShortcuts}
 *
 * @param {object} shortcut Function parameters.
 * @param {string} shortcut.name Shortcut name.
 * @param {string} shortcut.label Shortcut label.
 * @param {string} shortcut.category Shortcut category.
 * @param {Array<string>} shortcut.keys Shortcut keys.
 * @param {Array<string>} shortcut.paramKeys Optional param keys.
 * @param {string} shortcut.isEnabled Optional callback back function to check if shortcut is enabled.
 * @param {string} shortcut.action Callback function to call when shortcut is used.
 *
 * @returns {void}
 *
 * @example
 * ```
 * window.divi.keyboardShortcuts.registerShortcut({
 *   name: 'myShortcut',
 *   label: 'My Shortcut',
 *   category: 'myCategory',
 *   keys: ['Alt', 'M'],
 *   action: () => console.log('Shortcut triggered!')
 * });
 * ```
 */
export declare const registerShortcut: (shortcut: KeyboardShortcuts.Shortcut.Item) => void;
