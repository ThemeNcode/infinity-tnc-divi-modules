/**
 * Unregister Shortcuts.
 *
 * Unregister multiple shortcuts at once from the keyboard shortcuts registry.
 * This function calls `unregisterShortcut`.
 *
 * @since ??
 *
 * @see {@link unregisterShortcut}
 *
 * @param {Array<string>} names Shortcut names.
 *
 * @returns {void}
 *
 * @example
 * ```ts
 * window.divi.keyboardShortcuts.unregisterShortcuts(['myShortcut', 'closeEditingHistoryModal']);
 * ```
 */
export declare const unregisterShortcuts: (names: string[]) => void;
