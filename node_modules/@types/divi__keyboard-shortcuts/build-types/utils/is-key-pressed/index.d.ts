/**
 * Is Key Pressed.
 *
 * Check if given key is currently being pressed.
 *
 * @since ??
 *
 * @param {string|Array<string>} keys Key / key combination.
 *
 * @returns {boolean}
 *
 * @example
 * ```ts
 * window.divi.keyboardShortcuts.isKeyPressed('Alt');
 * // -> true|false
 * ```
 *
 * @example
 * ```ts
 * window.divi.keyboardShortcuts.isKeyPressed(['Alt', 'M']);
 * // -> true|false
 * ```
 */
export declare const isKeyPressed: (keys: string | string[]) => boolean;
