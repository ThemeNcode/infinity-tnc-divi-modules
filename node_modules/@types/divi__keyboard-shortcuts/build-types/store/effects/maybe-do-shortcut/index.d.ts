import { type KeyboardShortcuts } from '@divi/types';
/**
 * Check if currently pressed key combination matches with any registered shortcut. If it found
 * a match, dispatch doShortcut().
 *
 * @since ??
 */
export declare const maybeDoShortcut: (action: KeyboardShortcuts.Store.Actions.ReturnValue, store: KeyboardShortcuts.Store.Instance) => void;
