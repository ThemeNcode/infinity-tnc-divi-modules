import { type KeyboardShortcuts } from '@divi/types';
/**
 * Actually execute the registered shortcut action function when "do" state is updated by
 * doShortcut().
 *
 * @since ??
 */
export declare const onDoShortcut: (action: KeyboardShortcuts.Store.Actions.ReturnValue, store: KeyboardShortcuts.Store.Instance) => void;
