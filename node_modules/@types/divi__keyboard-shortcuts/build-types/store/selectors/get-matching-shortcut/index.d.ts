import { type ImmutableObject } from 'seamless-immutable';
import { type KeyboardShortcuts } from '@divi/types';
/**
 * Retrieves matching shortcut(s).
 *
 * Returns any registered shortcuts that matches
 * the given array of keys or `false` if no match is found.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * window.divi.data.select('divi/keyboard-shortcuts').getMatchingShortcut(['ctrl', 'shift', 'z']);
 * ```
 */
export declare const getMatchingShortcut: (state: KeyboardShortcuts.Store.ImmutableState, keys: string[]) => false | ImmutableObject<KeyboardShortcuts.Shortcut.Item>;
