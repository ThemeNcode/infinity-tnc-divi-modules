import { type ImmutableObject } from 'seamless-immutable';
import { type KeyboardShortcuts } from '@divi/types';
/**
 * Gets registered shortcuts.
 *
 * Retrieves registered shortcuts from the state based on the specified grouping option.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * const shortcuts = window.divi.data.select('divi/keyboard-shortcuts').getShortcuts();
 * ```
 *
 * @example
 * ```ts
 * const shortcuts = window.divi.data.select('divi/keyboard-shortcuts').getShortcuts(false);
 * ```
 *
 * @example
 * ```ts
 * const shortcuts = window.divi.data.select('divi/keyboard-shortcuts').getShortcuts('category');
 * ```
 */
export declare const getShortcuts: (state: KeyboardShortcuts.Store.ImmutableState, groupBy?: KeyboardShortcuts.Shortcut.Group) => ImmutableObject<any> | KeyboardShortcuts.Shortcut.Items;
