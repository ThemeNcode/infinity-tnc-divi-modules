import { type KeyboardShortcuts } from '@divi/types';
/**
 * Gets pressed keys.
 *
 * This function retrieves currently pressed keys from the store state.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * const pressedKeys = window.divi.data.select('divi/keyboard-shortcuts').getPressedKeys();
 * ```
 */
export declare const getPressedKeys: (state: KeyboardShortcuts.Store.ImmutableState) => KeyboardShortcuts.Store.ImmutableState['pressedKeys'];
