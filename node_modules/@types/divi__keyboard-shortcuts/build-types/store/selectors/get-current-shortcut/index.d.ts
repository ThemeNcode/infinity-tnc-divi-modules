import { type KeyboardShortcuts } from '@divi/types';
/**
 * Gets current shortcuts.
 *
 * Retrieves one or more current shortcuts from the store state.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * const currentShortcut = window.divi.data.select('divi/keyboard-shortcuts').getCurrentShortcut();
 * ```
 */
export declare const getCurrentShortcut: (state: KeyboardShortcuts.Store.ImmutableState) => KeyboardShortcuts.Store.ImmutableState['do'];
