/**
 * On Window Blur.
 *
 * Event listener callback for window `blur` event which pass update the state of pressed keys on
 * `divi/keyboard-shortcuts` store.
 *
 * @since ??
 *
 * @returns {void}
 */
export declare const onWindowBlur: () => void;
