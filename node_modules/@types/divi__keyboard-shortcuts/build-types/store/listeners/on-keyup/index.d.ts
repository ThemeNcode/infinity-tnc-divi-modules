/**
 * On Keyup.
 *
 * Event listener callback for `keyup` Keyboard Event on app and top window which pass the
 * unpressed key info to `divi/keyboard-shortcuts` store.
 *
 * @since ??
 *
 * @param {object} event Keyboard event parameters.
 * @param {string} event.code Keyboard event code.
 * @param {number} event.keyCode Keyboard event key code.
 * @param {string} event.key Keyboard event key.
 *
 * @returns {void}
 */
export declare const onKeyup: (event: KeyboardEvent) => void;
