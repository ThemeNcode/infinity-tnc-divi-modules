/**
 * Registers Keyboard Shortcuts Listeners.
 *
 * Registers event listener callback for all events that are related to keyboard shortcuts.
 * It registers the events `keydown`, `keyup` and `blur` on both `topWindow` and `app` window.
 *
 * @since ??
 *
 * @returns {void}
 */
export declare const registerKeyboardShortcutsListeners: () => void;
