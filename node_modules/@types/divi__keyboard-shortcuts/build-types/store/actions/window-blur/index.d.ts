import { type KeyboardShortcuts } from '@divi/types';
/**
 * Keeps track of when window is blurred.
 *
 * This action updates the state of pressed keys on the
 * 'divi/keyboard-shortcuts' store when the window loses cursor focus.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/keyboard-shortcuts').windowBlur();
 * ```
 */
export declare const windowBlur: () => KeyboardShortcuts.Store.Actions.WindowBlur.ReturnValue;
