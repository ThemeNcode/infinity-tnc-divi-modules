import { type KeyboardShortcuts } from '@divi/types';
/**
 * Stores pressed key.
 *
 * This action saves the pressed keyboard key information to the store.
 * This action works in conjunction with the event listener callback for
 * the `keydown` Keyboard Event on the app and top window.
 *
 * @since ??
 *
 * @remarks Data Store Dispatch Function
 *
 * @group Dispatch Functions
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/keyboard-shortcuts').keyDown(null, 'a');
 * ```
 */
export declare const keyDown: (physicalKeyCode: string, key: string) => KeyboardShortcuts.Store.Actions.KeyDown.ReturnValue;
