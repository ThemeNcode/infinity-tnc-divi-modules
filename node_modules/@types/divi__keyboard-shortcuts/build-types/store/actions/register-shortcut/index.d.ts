import { type KeyboardShortcuts } from '@divi/types';
/**
 * Registers a shortcut.
 *
 * This action registers a shortcut in the keyboard shortcut registry.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```
 * window.divi.data.dispatch('divi/keyboard-shortcuts').registerShortcut({
 *   name: 'myShortcut',
 *   label: 'My Shortcut',
 *   category: 'myCategory',
 *   keys: ['Alt', 'M'],
 *   action: () => console.log('Shortcut triggered!')
 * });
 * ```
 */
export declare const registerShortcut: (shortcut: KeyboardShortcuts.Store.Actions.RegisterShortcut.ReturnValue['shortcut']) => KeyboardShortcuts.Store.Actions.RegisterShortcut.ReturnValue;
