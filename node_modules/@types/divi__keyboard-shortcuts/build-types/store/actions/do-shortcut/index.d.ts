import { type KeyboardShortcuts } from '@divi/types';
/**
 * Performs shortcut action.
 *
 * This action executes the registered keyboard shortcut action.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/keyboard-shortcuts').doShortcut({
 *   name:     'my-shortcut',
 *   label:    'My Shortcut',
 *   keys:     ['ctrl', 'shift', 'm'],
 *   category: 'My Category',
 *   action:   () => console.log('My shortcut was triggered!')
 * });
 * ```
 */
export declare const doShortcut: (shortcut: KeyboardShortcuts.Store.Actions.DoShortcut.ReturnValue['shortcut']) => KeyboardShortcuts.Store.Actions.DoShortcut.ReturnValue;
