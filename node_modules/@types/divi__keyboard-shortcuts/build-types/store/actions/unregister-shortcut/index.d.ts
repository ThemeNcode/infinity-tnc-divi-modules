import { type KeyboardShortcuts } from '@divi/types';
/**
 * De-registers a shortcut.
 *
 * This action removes a keyboard shortcut with the
 * specified name from the keyboard shortcuts registry.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function.
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/keyboard-shortcuts').unregisterShortcut('myShortcut');
 * ```
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/keyboard-shortcuts').unregisterShortcut('closeEditingHistoryModal');
 * ```
 */
export declare const unregisterShortcut: (name: KeyboardShortcuts.Store.Actions.UnregisterShortcut.ReturnValue['name']) => KeyboardShortcuts.Store.Actions.UnregisterShortcut.ReturnValue;
