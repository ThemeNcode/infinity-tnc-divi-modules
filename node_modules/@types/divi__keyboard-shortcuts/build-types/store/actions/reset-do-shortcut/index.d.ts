import { type KeyboardShortcuts } from '@divi/types';
/**
 * Resets "do" shortcut value.
 *
 * This action resets the value of the "do" state on the store, setting it to an empty value.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/keyboard-shortcuts').resetDoShortcut();
 * ```
 */
export declare const resetDoShortcut: () => KeyboardShortcuts.Store.Actions.ResetDoShortcut.ReturnValue;
