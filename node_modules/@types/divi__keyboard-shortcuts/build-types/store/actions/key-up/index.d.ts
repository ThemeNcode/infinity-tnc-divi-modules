import { type KeyboardShortcuts } from '@divi/types';
/**
 * Keeps track of unpressed keys.
 *
 * This action keeps track of when a key is unpressed.
 * It works in conjunction with the vent listener callback for the `keyup`
 * Keyboard Event on the app and top window.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/keyboard-shortcuts').keyUp('KeyA', 'a');
 * ```
 */
export declare const keyUp: (physicalKeyCode: string, key: string) => KeyboardShortcuts.Store.Actions.KeyUp.ReturnValue;
