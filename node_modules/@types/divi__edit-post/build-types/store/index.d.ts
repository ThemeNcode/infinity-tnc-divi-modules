import { type EditPost } from '@divi/types';
/**
 * Register edit post store.
 *
 * Registers the `divi/edit-post` store.
 *
 * @since ??
 *
 * @param {object} storeInitialState Optional initial state of the store.
 *
 * @returns {void}
 */
export declare const registerEditPostStore: <TState = EditPost.Store.ImmutableState<EditPost.Store.State<import("@divi/types").ModuleFlatObjects>>>(storeInitialState?: TState) => void;
