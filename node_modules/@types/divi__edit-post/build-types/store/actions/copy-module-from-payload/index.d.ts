import { type EditPost } from '@divi/types';
/**
 * Copies a module to a specified position.
 *
 * This action copies a module from the given payload to a target position in a layout.
 *
 * @since ??
 * @group Dispatch Functions
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * // Copies a module from the payload and sets it after the target module.
 * const copiedModule = window.divi.data.dispatch('divi/edit-post').copyModuleFromPayload(
 *   {
 *     // module payload data
 *   },
 *   'after',
 *   'module-1'
 * );
 * ```
 *
 * @example
 * ```ts
 * // Copies a module from the payload and sets it inside the target module.
 * const copiedModule = window.divi.data.dispatch('divi/edit-post').copyModuleFromPayload(
 *   {
 *     // module payload data
 *   },
 *   'inside',
 *   'module-2'
 * );
 * ```
 */
export declare const copyModuleFromPayload: (payload: EditPost.Store.Actions.CopyModuleFromPayload.ReturnValue['payload'], position: EditPost.Store.Actions.CopyModuleFromPayload.ReturnValue['position'], targetId: string, caller?: EditPost.Store.Caller) => EditPost.Store.Actions.CopyModuleFromPayload.ReturnValue;
