import { type ImmutableObject } from 'seamless-immutable';
import { type EditPost, type ModuleAttr } from '@divi/types';
/**
 * Sets the globalParent attribute on a module.
 *
 * This action sets the global parent attribute of a module, which is used to
 * determine the parent module of a given module.
 *
 * @since ??
 * @group Dispatch Functions
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/edit-post').setGlobalParentAttribute('module1', 'parentModule');
 * ```
 */
export declare const setGlobalParentAttribute: (id: string, value: ImmutableObject<ModuleAttr> | string) => EditPost.Store.Actions.SetGlobalParentAttribute.ReturnValue;
