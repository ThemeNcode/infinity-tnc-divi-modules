import { type EditPost } from '@divi/types';
/**
 * Adds a module to the layout.
 *
 * This action will add a module inside a specified parent.
 *
 * @since ??
 * @group Dispatch Functions
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * const newModule = window.divi.data.dispatch('divi/edit-post').addModule(
 *   'module-1',
 *   'text',
 *   {},
 *   'inside',
 *   'module-2',
 *   'user'
 * );
 * ```
 *
 * @example
 * ```ts
 * const newModule = window.divi.data.dispatch('divi/edit-post').addModule(
 *   'module-2',
 *   'button',
 *   {},
 *   'after',
 *   'new-button',
 *   'global-layout'
 * );
 * ```
 */
export declare const addModule: (id: string, name: string, props?: EditPost.Store.Actions.AddModule.ReturnValue['props'], position?: EditPost.Store.Actions.AddModule.ReturnValue['position'], newId?: string, caller?: EditPost.Store.Caller) => EditPost.Store.Actions.AddModule.ReturnValue;
