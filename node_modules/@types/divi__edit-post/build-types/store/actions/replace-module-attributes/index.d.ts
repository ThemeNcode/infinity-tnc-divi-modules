import { type ImmutableObject } from 'seamless-immutable';
import { type EditPost, type ModuleAttrs } from '@divi/types';
/**
 * Replaces specified attribute values within a module.
 *
 * This action replaces the attributes of a module identified by the given id.
 *
 * @since ??
 * @group Dispatch Functions
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * const id = 'module1';
 * const attrs = {
 *   attr1: 'value1',
 *   attr2: 'value2',
 * };
 * window.divi.data.dispatch('divi/edit-post').replaceModuleAttributes(id, attrs);
 * ```
 */
export declare const replaceModuleAttributes: (id: string, attrs: ImmutableObject<ModuleAttrs>, caller?: EditPost.Store.Caller) => EditPost.Store.Actions.ReplaceModuleAttributes.ReturnValue;
