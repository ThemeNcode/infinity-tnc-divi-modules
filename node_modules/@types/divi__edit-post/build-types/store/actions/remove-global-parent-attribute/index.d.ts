import { type EditPost } from '@divi/types';
/**
 * Removes the global parent attribute from a module.
 *
 * This action removes a global parent attribute with the given ID. This happens when
 * a child module is moved from one parent module to another.
 *
 * @since ??
 * @group Dispatch Functions
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * // Remove global parent attribute with ID "abc123"
 * window.divi.data.dispatch('divi/edit-post').removeGlobalParentAttribute("abc123");
 * ```
 *
 * @example
 * ```ts
 * // Remove global parent attribute with ID "xyz789"
 * window.divi.data.dispatch('divi/edit-post').removeGlobalParentAttribute("xyz789");
 * ```
 */
export declare const removeGlobalParentAttribute: (id: string) => EditPost.Store.Actions.RemoveGlobalParentAttribute.ReturnValue;
