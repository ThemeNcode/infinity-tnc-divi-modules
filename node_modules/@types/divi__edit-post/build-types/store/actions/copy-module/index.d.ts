import { type EditPost } from '@divi/types';
/**
 * Copy module action.
 *
 * This action is copying a single module to target position.
 *
 * @since ??
 * @group Dispatch Functions
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/edit-post').copyModule('module123', 'before', 'targetModule456');
 * ```
 */
export declare const copyModule: (id: string, position: EditPost.Store.Actions.CopyModule.ReturnValue['position'], targetId: string) => EditPost.Store.Actions.CopyModule.ReturnValue;
