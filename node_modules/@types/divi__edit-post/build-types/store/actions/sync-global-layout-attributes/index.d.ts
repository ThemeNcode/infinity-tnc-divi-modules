import { type EditPost } from '@divi/types';
/**
 * Sync global layout attributes.
 *
 * This action is used to sync the global layout attributes when the global layout is updated.
 * It will replace the module attributes with the new global layout attributes.
 *
 * @since ??
 * @group Dispatch Functions
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/edit-post').syncGlobalLayoutAttributes(
 *   'layout123',
 *   {title: 'Layout Title'},
 *   ['attr1', 'attr2']
 * );
 * ```
 */
export declare const syncGlobalLayoutAttributes: (id: EditPost.Store.Actions.SyncGlobalLayoutAttributes.ReturnValue['id'], attrs: EditPost.Store.Actions.SyncGlobalLayoutAttributes.ReturnValue['attrs'], unsyncAttrs: EditPost.Store.Actions.SyncGlobalLayoutAttributes.ReturnValue['unsyncAttrs']) => EditPost.Store.Actions.SyncGlobalLayoutAttributes.ReturnValue;
