import { type EditPost } from '@divi/types';
/**
 * Adds a new row to the layout.
 *
 * This action adds a new row and its accompanying column module(s) based on the value passed by
 * the `columnStructure` parameter.
 *
 * @since ??
 * @group Dispatch Functions
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('edit-post').addRow(
 *   'row-123',
 *   'inside',
 *   '1_2,1_2',
 *   false,
 *   'user',
 * );
 * ```
 */
export declare const addRow: (id: string, position: EditPost.Store.Actions.AddRow.ReturnValue['position'], columnStructure: string, inner?: boolean, caller?: EditPost.Store.Caller) => EditPost.Store.Actions.AddRow.ReturnValue;
