import { type EditPost } from '@divi/types';
/**
 * Copies a module from the Divi Library.
 *
 * This action copies a module from the Divi Library to a specified location in the active layout.
 *
 * @since ??
 * @group Dispatch Functions
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/edit-post').copyModuleFromLibrary(
 *   {
 *     // payload data
 *   },
 *   'after',
 *   'module-123',
 *   'user',
 * );
 * ```
 */
export declare const copyModuleFromLibrary: (payload: EditPost.Store.Actions.CopyModuleFromLibrary.ReturnValue['payload'], position: EditPost.Store.Actions.CopyModuleFromLibrary.ReturnValue['position'], targetId: string, caller?: EditPost.Store.Caller) => EditPost.Store.Actions.CopyModuleFromLibrary.ReturnValue;
