import { type ImmutableObject } from 'seamless-immutable';
import { type EditPost, type ModuleAttr } from '@divi/types';
/**
 * Edits a module attribute.
 *
 * This action is used to update a module's attribute value.
 *
 * @since ??
 * @group Dispatch Functions
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * // save 'Hello World' to the content attribute of `module1`.
 * window.divi.data.dispatch('divi/edit-post').editModuleAttribute(
 *  'module123', 'content', 'Hello World', 'global-layout'
 * );
 * ```
 */
export declare const editModuleAttribute: (id: string, attrName: string, value: ImmutableObject<ModuleAttr> | string, caller?: EditPost.Store.Caller) => EditPost.Store.Actions.EditModuleAttribute.ReturnValue;
