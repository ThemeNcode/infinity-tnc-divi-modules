import { type ImmutableObject } from 'seamless-immutable';
import { type EditPost, type ModuleAttr } from '@divi/types';
/**
 * Indicate that a module is global.
 *
 * This action sets the `globalModule` attribute, which indicates that changes made to this
 * module should be reflected in all instances of the specific module.
 *
 * @since ??
 * @group Dispatch Functions
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/edit-post').setGlobalModuleAttribute('module1', '999999999');
 * ```
 */
export declare const setGlobalModuleAttribute: (id: string, value: ImmutableObject<ModuleAttr> | string) => EditPost.Store.Actions.SetGlobalModuleAttribute.ReturnValue;
