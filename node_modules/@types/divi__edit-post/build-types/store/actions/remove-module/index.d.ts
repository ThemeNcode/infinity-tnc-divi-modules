import { type EditPost } from '@divi/types';
/**
 * Removes a module.
 *
 * This action removes a module with the specified ID.
 *
 * @since ??
 * @group Dispatch Functions
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/edit-post').removeModule('module123');
 * ```
 */
export declare const removeModule: (id: string, caller?: EditPost.Store.Caller) => EditPost.Store.Actions.RemoveModule.ReturnValue;
