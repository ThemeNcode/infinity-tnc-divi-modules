import { type EditPost } from '@divi/types';
/**
 * Adds a Specialty section.
 *
 * This action adds a specialty section to the active layout.
 *
 * @since ??
 * @group Dispatch Functions
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * // Add a specialty section to the layout
 * window.divi.data.dispatch('divi/edit-post').addSpecialtySection(
 *   'section1', 'before', '4_5,1_5', '1,0'
 * );
 * ```
 *
 * @example
 * ```ts
 * // Add a specialty section to the layout
 * window.divi.data.dispatch('divi/edit-post').addSpecialtySection(
 *   'section1', 'after', '1_4,1_4,1_2', '0,1,1'
 * );
 * ```
 */
export declare const addSpecialtySection: (id: string, position: EditPost.Store.Actions.AddSpecialtySection.ReturnValue['position'], columnStructure: string, innerPosition: string) => EditPost.Store.Actions.AddSpecialtySection.ReturnValue;
