import { type EditPost } from '@divi/types';
/**
 * Removes the global attribute from a module.
 *
 * This action removes the `globalModule` attribute, indicating that any changes made
 * to this instance of the module should no longer be synchronized to other instances
 * of that module in this or other layouts on the website.
 *
 * @since ??
 * @group Dispatch Functions
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/edit-post').removeGlobalModuleAttribute('module123');
 * ```
 */
export declare const removeGlobalModuleAttribute: (id: string) => EditPost.Store.Actions.RemoveGlobalModuleAttribute.ReturnValue;
