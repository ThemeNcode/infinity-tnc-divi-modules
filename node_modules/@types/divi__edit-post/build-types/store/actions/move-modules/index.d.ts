import { type EditPost } from '@divi/types';
/**
 * Moves multiple modules.
 *
 * This action is used to move one or more modules to a specified position.
 *
 * @since ??
 * @group Dispatch Functions
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * // Single module move example
 * const action = window.divi.data.dispatch('divi/edit-post').moveModules(['module1'], 'after', 'module2');
 * ```
 *
 * @example
 * ```ts
 * // Multiple modules move example
 * const action = window.divi.data.dispatch('divi/edit-post').moveModules(
 *   ['module1', 'module2', 'module3'],
 *   'before',
 *   'module4'
 * );
 * ```
 */
export declare const moveModules: (ids: string[], position: EditPost.Store.Actions.MoveModules.ReturnValue['position'], targetId: string) => EditPost.Store.Actions.MoveModules.ReturnValue;
