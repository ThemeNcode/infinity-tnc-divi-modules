import { type EditPost } from '@divi/types';
/**
 * Imports a saved layout into the active layout.
 *
 * This action imports content into the active Builder instance, either appending
 * or replacing (based on passed attributes) the contents of the current layout.
 *
 * @since ??
 * @group Dispatch Functions
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/edit-post').importContent('Lorem ipsum...', {
 *   replaceLayout: 'on',
 *   importBackUp: 'off',
 *   includeGlobalPresets: 'off',
 * });
 * ```
 */
export declare const importContent: (content: string, importOptionsValues: EditPost.Store.Actions.ImportContent.ReturnValue['importOptionsValues']) => EditPost.Store.Actions.ImportContent.ReturnValue;
