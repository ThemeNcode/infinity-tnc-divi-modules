import { type EditPost } from '@divi/types';
/**
 * Clones a column.
 *
 * This action clones an existing column, including any child modules that it contains.
 *
 * @since ??
 * @group Dispatch Functions
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * // Dispatch the CloneColumn action
 * window.divi.data.dispatch('divi/edit-post').cloneColumn('column-1');
 * ```
 */
export declare const cloneColumn: (id: string) => EditPost.Store.Actions.CloneColumn.ReturnValue;
