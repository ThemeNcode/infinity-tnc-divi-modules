import { type EditPost } from '@divi/types';
/**
 * Edits global module attributes that are not synced.
 *
 * This action edits the unsynced attributes of a global module.
 *
 * @since ??
 * @group Dispatch Functions
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/edit-post').editUnsyncAttrs('module-1', 'title', true, 'global-layout');
 * ```
 */
export declare const editUnsyncAttrs: (id: string, attribute: EditPost.Store.Actions.EditUnsyncAttrs.ReturnValue['attribute'], isSynced: boolean, caller?: EditPost.Store.Caller) => EditPost.Store.Actions.EditUnsyncAttrs.ReturnValue;
