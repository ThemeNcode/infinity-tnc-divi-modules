import { type EditPost, type ModuleFlatObjects } from '@divi/types';
/**
 * Merges global modules.
 *
 * This action merges global modules into the `edit-post` store.
 *
 * @since ??
 * @group Dispatch Functions
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/edit-post').mergeGlobalModules({
 *   id: 'module-123',
 *   type: 'text',
 *   parentId: 'row-456',
 *   index: 0,
 *   attrs: {
 *     content: 'Hello, world!',
 *     fontSize: '16px',
 *     textColor: '#000000',
 *   },
 *   children: [],
 * }, 'module123');
 * ```
 */
export declare const mergeGlobalModules: (content: ModuleFlatObjects, owner: string) => EditPost.Store.Actions.MergeGlobalModules.ReturnValue;
