import { type EditPost } from '@divi/types';
/**
 * Clears the entire layout.
 *
 * This action is used to clear the layout.
 *
 * Note that after the layout is cleared a Standard section will be automatically added
 * to the page, and then the Insert Row modal will automatically pop up within that new
 * section.
 *
 * @since ??
 * @group Dispatch Functions
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/edit-post').clearLayout();
 * ```
 */ export declare const clearLayout: () => EditPost.Store.Actions.ClearLayout.ReturnValue;
