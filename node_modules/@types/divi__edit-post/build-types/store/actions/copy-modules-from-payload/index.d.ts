import { type EditPost } from '@divi/types';
/**
 * Copies modules from a payload.
 *
 * This action copies one or more modules from a payload to a given target position.
 *
 * @since ??
 * @group Dispatch Functions
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/edit-post').copyModulesFromPayload(
 *   {
 *     // module payload data
 *   },
 *   'after',
 *   'module-123',
 *   'user'
 * );
 * ```
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/edit-post').copyModuleFromPayload(
 *   {
 *     // module payload data
 *   },
 *   'inside',
 *   'module-123',
 *   'global-layout'
 * );
 * ```
 */
export declare const copyModulesFromPayload: (payload: EditPost.Store.Actions.CopyModulesFromPayload.ReturnValue['payload'], position: EditPost.Store.Actions.CopyModulesFromPayload.ReturnValue['position'], targetId: string, caller?: EditPost.Store.Caller) => EditPost.Store.Actions.CopyModulesFromPayload.ReturnValue;
