import { type EditPost } from '@divi/types';
/**
 * Clones a module.
 *
 * This action will create a new module with the same settings and children as the module with the given id.
 *
 * @since ??
 * @group Dispatch Functions
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/edit-post').cloneModule('module-123');
 * ```
 */
export declare const cloneModule: (id: string, caller?: EditPost.Store.Caller) => EditPost.Store.Actions.CloneModule.ReturnValue;
