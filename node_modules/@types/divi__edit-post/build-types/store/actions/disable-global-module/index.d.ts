import { type EditPost } from '@divi/types';
/**
 * Disables the 'global' attribute on a module.
 *
 * This action is for removing globalModule attribute from the module and globalParent from module's children.
 *
 * @since ??
 * @group Dispatch Functions
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/edit-post').disableGlobalModule('module-123');
 * ```
 */
export declare const disableGlobalModule: (id: string) => EditPost.Store.Actions.DisableGlobalModule.ReturnValue;
