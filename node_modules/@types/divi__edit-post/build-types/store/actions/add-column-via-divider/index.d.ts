import { type EditPost } from '@divi/types';
/**
 * Adds a column via divider "Add Column" button.
 *
 * This action is for adding a column via a divider.
 *
 * Adding column via divider has its own action because:
 * 1. The action is performed on a row,
 * 2. After the column is added, the row's column structure needs to be updated,
 * 3. Afterward, the column's type attribute needs to be updated as well.
 *
 * @since ??
 * @group Dispatch Functions
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/edit-post').addColumnViaDivider('column-123', 2, 'global-layout');
 * ```
 */
export declare const addColumnViaDivider: (id: string, dividerPosition: number, caller?: EditPost.Store.Caller) => EditPost.Store.Actions.AddColumnViaDivider.ReturnValue;
