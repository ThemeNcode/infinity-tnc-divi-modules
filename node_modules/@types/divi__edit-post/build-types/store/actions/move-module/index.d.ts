import { type EditPost } from '@divi/types';
/**
 * Moves a single module.
 *
 * This action moves a module to a different position within a layout.
 *
 * @since ??
 * @group Dispatch Functions
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * // Move module 'moduleID' to a different position within the layout
 * const action = window.divi.data.dispatch('divi/edit-post').moveModule('module1', 'before', 'module134', 'user');
 * ```
 *
 * @example
 * ```ts
 * // Move module 'moduleID' to the end of the layout
 * const action = window.divi.data.dispatch('divi/edit-post').moveModule('module2', 'end', 'layout123', 'builder');
 * ```
 *
 * @example
 * ```ts
 * // Move module 'moduleID' to the end of the layout
 * const action = window.divi.data.dispatch('divi/edit-post').moveModule('module3', 'inside', 'layout345', 'user');
 * ```
 */
export declare const moveModule: (id: string, position: EditPost.Store.Actions.MoveModule.ReturnValue['position'], targetId: string, caller?: EditPost.Store.Caller) => EditPost.Store.Actions.MoveModule.ReturnValue;
