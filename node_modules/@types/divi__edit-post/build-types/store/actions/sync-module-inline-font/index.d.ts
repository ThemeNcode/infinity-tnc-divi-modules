import { type SyncModuleInlineFont } from './types';
/**
 * Synchronizes module inline font.
 *
 * This action synchronizes the module inline font with the module inline styles.
 * The action extracts the font families used in the module inline styles and
 * sets it as the module inline font. This is to ensure that the module inline font
 * is always up-to-date with the module inline styles.
 *
 * @since ??
 *
 * @returns {SyncModuleInlineFont}
 */
export declare const syncModuleInlineFont: () => SyncModuleInlineFont;
