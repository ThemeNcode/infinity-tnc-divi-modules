import { type EditPost } from '@divi/types';
/**
 * Removes a column from a section.
 *
 * This action removes a specified column from its container section in a layout.
 *
 * @since ??
 * @group Dispatch Functions
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/edit-post').removeColumn('column-1', 'global-layout');
 * ```
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/edit-post').removeColumn('column-2', 'user');
 * ```
 */
export declare const removeColumn: (id: string, caller?: EditPost.Store.Caller) => EditPost.Store.Actions.RemoveColumn.ReturnValue;
