import { type EditPost } from '@divi/types';
/**
 * Checks if a module or any of its children have been edited and if so, closes the module.
 *
 * @since ??
 *
 * @param {EditPost.Store.Actions.ReturnValue} action The action object containing the ID of the module to check.
 */
export declare const maybeCloseModuleSettings: (action: EditPost.Store.Effects.MaybeCloseModuleSettings.Action) => void;
