import { type EditPost } from '@divi/types';
/**
 * Automatically add child modules based on it's parent module's template.
 *
 * @since ??
 */
export declare const addChildModules: (action: EditPost.Store.Actions.AddModule.ReturnValue, store: EditPost.Store.Instance) => void;
