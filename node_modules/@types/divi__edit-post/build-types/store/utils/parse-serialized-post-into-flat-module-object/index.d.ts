import { type ModuleFlatObject, type ModuleFlatObjects } from '@divi/types';
/**
 * Parse Serialized Post Into Flat Module Object.
 *
 * Parse serialized GB-format post content into flat module objects.
 *
 * @since ??
 *
 * @param {string} serializedPost GB-format content.
 * @param {object} customRoot Custom root object.
 * @param {Function} uuidFunction Function to generate a UUID.
 *
 * @returns {object}
 */
export declare const parseSerializedPostIntoFlatModuleObject: (serializedPost: string, customRoot?: ModuleFlatObject, uuidFunction?: (<T extends ArrayLike<number>>(options: import("uuid").V4Options, buffer: T, offset?: number) => T) & ((options?: import("uuid").V4Options) => string)) => ModuleFlatObjects;
