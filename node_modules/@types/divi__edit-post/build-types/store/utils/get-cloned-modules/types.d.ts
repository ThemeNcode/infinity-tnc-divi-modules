import { type ImmutableObject } from 'seamless-immutable';
import { type ModuleFlatObjects } from '@divi/types';
/**
 * Interface of cloned modules.
 *
 * @since ??
 */
export interface ClonedModules {
    clonedModuleId: string;
    clonedParent: string;
    clonedParentChildren: string[];
    clonedModules: ImmutableObject<ModuleFlatObjects>;
}
