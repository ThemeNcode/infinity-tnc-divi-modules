import { type EditPost } from '@divi/types';
import { type ClonedModules } from './types';
/**
 * Get Cloned Modules.
 *
 * Get cloned modules, parent id, and updated parent's children property of the cloned modules.
 *
 * @since ??
 */
export declare const getClonedModules: (state: EditPost.Store.ImmutableState['content'], moduleId: string, targetParentId?: string) => ClonedModules;
