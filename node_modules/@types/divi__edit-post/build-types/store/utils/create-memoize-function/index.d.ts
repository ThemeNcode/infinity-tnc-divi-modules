import { type CreateMemoizeFunction } from './types';
/**
 * Create Memoize Function.
 *
 * Create memoize function of given function using fast-memoize, variadic strategy, and debug()
 * + clear() function for clearing key-value map built-in.
 *
 * @since ??
 *
 * @param {Function} func Function to be memoized.
 *
 * @returns {CreateMemoizeFunction}
 */
export declare const createMemoizeFunction: <TParams extends any[], TReturn, TFunc extends (...args: TParams) => TReturn>(func: TFunc) => CreateMemoizeFunction;
