/**
 * Extracts the font families used in the module inline styles.
 *
 * @since ??
 *
 * @param {string} htmlString The module flat object.
 *
 * @returns {string[]} The list of font families used in the module inline styles.
 */
export declare const extractInlineFontFamilies: (htmlString: string) => string[];
