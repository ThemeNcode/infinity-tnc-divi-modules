import { type ImmutableObject } from 'seamless-immutable';
import { type EditPost, type ModuleFlatObjects } from '@divi/types';
/**
 * Filter modules by given module ids.
 *
 * @since ??
 *
 * @param {Array<string>} ids Module ids.
 * @param {object} modules Object with flat object modules.
 * @param {boolean} mutable Should returned value be mutable or immutable.
 *
 * @returns {object} Immutable/mutable object of module based on value of `mutable` param.
 */
declare const filterModulesByIds: <T extends boolean>(ids: string[], modules: ImmutableObject<ModuleFlatObjects>, mutable: T) => EditPost.Store.FilteredModulesByIds<T>;
/**
 * Memoized function of `filterModulesByIds()`.
 *
 * @since ??
 *
 * @returns {object} Memoized immutable/mutable object of module based on value of `mutable` param.
 */
declare const memoizedFilterModulesByIds: <T extends boolean>(ids: string[], modules: ImmutableObject<ModuleFlatObjects>, mutable: T) => EditPost.Store.FilteredModulesByIds<T>;
/**
 * Clear Memoized Filter Modules By Ids.
 *
 * Clears key-value store of memoized function `filterModulesByIds()`.
 *
 * @since ??
 *
 * @returns {void}
 */
declare const clearMemoizedFilterModulesByIds: () => void;
export { filterModulesByIds, memoizedFilterModulesByIds, clearMemoizedFilterModulesByIds, };
