/**
 * Insert Id Into Ids List.
 *
 * Insert a new id before/after given (reference) id in an array of ids.
 *
 * @since ??
 *
 * @param {string} insertedId Id that will be inserted.
 * @param {string} referenceId InsertedId will be inserted after this referenceId.
 * @param {Array<string>} idList Array of ids where `insertedId` will be inserted.
 * @param {string} position Position to insert value at,
 * either `before` or `after` the `referenceId`, defaults to `after`.
 *
 * @returns {Array<string>}
 */
export declare const insertIdIntoIdList: (insertedId: string, referenceId: string, idList: string[], position?: 'before' | 'after') => string[];
