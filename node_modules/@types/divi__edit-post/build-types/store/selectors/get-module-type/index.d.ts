import { type EditPost, type ModuleType } from '@divi/types';
/**
 * Gets the type of a specified module.
 *
 * This selector function extracts the name and attributes of the module with the specified ID from the state,
 * and then uses the `generateModuleType` function to determine its module type.
 *
 * @see generateModuleType
 *
 * @since ??
 * @group Selector Functions
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * window.divi.data.select('divi/edit-post').getModuleType('module-123')
 * ```
 */
export declare const getModuleType: (state: EditPost.Store.ImmutableState, id: string) => ModuleType;
