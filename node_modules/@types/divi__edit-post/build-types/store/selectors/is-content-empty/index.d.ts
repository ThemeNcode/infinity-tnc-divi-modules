import { type EditPost } from '@divi/types';
/**
 * Determines whether the Builder content area is empty.
 *
 *
 * The builder content is considered as NOT empty under the following conditions:
 * 1) Page content with 2 or more section.
 * 2) Page content with only 1 section, but with some attributes values.
 * 3) There is more than 1 ID found in the content tree.
 *
 * @since ??
 * @group Selector Functions
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * window.divi.data.select('divi/edit-post').isContentEmpty();
 * ```
 */
export declare const isContentEmpty: (state: EditPost.Store.ImmutableState) => boolean;
