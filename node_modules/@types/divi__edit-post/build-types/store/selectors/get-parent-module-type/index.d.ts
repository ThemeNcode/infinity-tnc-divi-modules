import { type EditPost, type ModuleType } from '@divi/types';
/**
 * Gets the type of a module's parent.
 *
 * Retrieves the type info for a parent module based on the specified module ID.
 *
 * @since ??
 * @group Selector Functions
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * const parentModuleType = window.divi.data.select('divi/edit-post').getParentModuleType('module-id');
 * ```
 */
export declare const getParentModuleType: (state: EditPost.Store.ImmutableState, id: string) => ModuleType;
