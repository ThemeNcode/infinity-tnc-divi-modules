import { type EditPost } from '@divi/types';
/**
 * Gets the name of a module.
 *
 * When given a module ID, this retrieves the name of the module from the content in
 * the store state.
 *
 * @since ??
 * @group Selector Functions
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * window.divi.data.select('divi/edit-post').getModuleName('module-123');
 * ```
 *
 */
export declare const getModuleName: (state: EditPost.Store.ImmutableState, id: string) => string;
