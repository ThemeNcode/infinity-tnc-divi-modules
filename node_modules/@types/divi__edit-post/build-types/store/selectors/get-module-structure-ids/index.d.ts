import { type EditPost } from '@divi/types';
/**
 * Gets the IDs of all modules contained within a specified module.
 *
 * This function returns an array of module structure ids starting from the given id and including all of its children.
 * The module structure ids represent the hierarchical structure of the modules in the post content.
 * The ids are extracted recursively by traversing the module tree using a depth-first search approach.
 *
 * @since ??
 * @group Selector Functions
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * const moduleStructureIds = window.divi.data.select('divi/edit-post').getModuleStructureIds('module-1');
 * console.log(moduleStructureIds);
 * // expected output: ['module-1', 'module-2', 'child-1', 'child-2', 'grandchild-1']
 * ```
 */
export declare const getModuleStructureIds: (state: EditPost.Store.ImmutableState, id: string) => string[];
