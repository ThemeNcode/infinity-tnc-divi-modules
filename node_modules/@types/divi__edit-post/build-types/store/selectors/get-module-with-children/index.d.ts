import { type EditPost, type ModuleFlatObjects } from '@divi/types';
/**
 * Gets a module, along with any children it has.
 *
 * Fetches the module with its children from the store.
 *
 * @since ??
 * @group Selector Functions
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * window.divi.data.select('divi/edit-post').getModuleWithChildren('module-123');
 * ```
 */
export declare const getModuleWithChildren: (state: EditPost.Store.ImmutableState, id: string) => ModuleFlatObjects;
