import { type ImmutableArray } from 'seamless-immutable';
import { type EditPost } from '@divi/types';
/**
 * Get global layout owners.
 *
 * Get global layout owners based on the provided layout ID.
 *
 * @since ??
 * @group Selector Functions
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * const owners = window.divi.data.select('divi/edit-post').getGlobalLayoutOwners('layout123');
 * ```
 */
export declare const getGlobalLayoutOwners: (state: EditPost.Store.ImmutableState, id: string) => ImmutableArray<string>;
