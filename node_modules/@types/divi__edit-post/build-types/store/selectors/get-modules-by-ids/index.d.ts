import { type EditPost } from '@divi/types';
/**
 * Gets one or more modules by their IDs.
 *
 * This function retrieves the module objects that correspond to the given module ids.
 * It filters the modules based on their ids and the provided mutable flag.
 * The `mutable` flag determines whether the retrieved module objects can be modified.
 *
 * @since ??
 * @group Selector Functions
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * const moduleIds = ['module1', 'module2'];
 * const mutable = true;
 * const filteredModules = window.divi.data.select('divi/edit-post').getModulesByIds(moduleIds, mutable);
 * console.log(filteredModules);
 * ```
 */
export declare const getModulesByIds: <T extends boolean>(state: EditPost.Store.ImmutableState, ids: string[], mutable: T) => EditPost.Store.FilteredModulesByIds<T>;
