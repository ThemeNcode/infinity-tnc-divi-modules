import { type EditPost } from '@divi/types';
/**
 * Gets all sibling module ids.
 *
 * Retrieves the module IDs of sibling modules for a given module ID (Sibling modules
 * are modules that share the same parent module).
 *
 * @since ??
 * @group Selector Functions
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * const siblingModuleIds = window.divi.data.select('divi/edit-post').getSiblingModuleIds('module-1');
 * ```
 */
export declare const getSiblingModuleIds: (state: EditPost.Store.ImmutableState, id: string) => string[];
