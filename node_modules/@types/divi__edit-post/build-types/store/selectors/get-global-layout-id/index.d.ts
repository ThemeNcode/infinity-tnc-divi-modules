import { type EditPost } from '@divi/types';
/**
 * Get global layout ID for a specific module.
 *
 * Retrieves the global layout ID for a specific module.
 * If the module is not global, `null` is returned.
 *
 * @since ??
 * @group Selector Functions
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * const globalLayoutId = window.divi.data.select('divi/edit-post').getGlobalLayoutId('module123');
 * console.log(globalLayoutId);
 * // Output: "layout1"
 * ```
 */
export declare const getGlobalLayoutId: (state: EditPost.Store.ImmutableState, id: string) => string | null;
