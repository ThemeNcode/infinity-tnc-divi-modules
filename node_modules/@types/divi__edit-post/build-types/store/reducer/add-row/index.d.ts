import { type EditPost } from '@divi/types';
/**
 * Add row and its column based on given columnStructure parameter.
 *
 * @since ??
 *
 */
export declare const addRow: (state: EditPost.Store.ImmutableState['content'], id: string, position: 'before' | 'after' | 'inside', columnStructure: string, inner?: boolean, newId?: string) => EditPost.Store.ImmutableState['content'];
