import { type EditPost } from '@divi/types';
/**
 * Move multiple modules into new position relative to the given target id.
 *
 * @since ??
 */
export declare const moveModules: (state: EditPost.Store.ImmutableState['content'], ids: string[], position: EditPost.Store.NewModulePosition, targetId: string) => EditPost.Store.ImmutableState['content'];
