import { type EditPost } from '@divi/types';
/**
 * Move module into new position relative to the given target id.
 *
 * @since ??
 *
 */
export declare const moveModule: (state: EditPost.Store.ImmutableState['content'], id: string, position: EditPost.Store.NewModulePosition, targetId: string) => EditPost.Store.ImmutableState['content'];
