import { type EditPost } from '@divi/types';
/**
 * Synchronizes module inline font.
 *
 * Synchronizes the module inline font with the module inline styles.
 * This function extracts the font families used in the module inline styles and
 * sets it as the module inline font. This is to ensure that the module inline font
 * is always up-to-date with the module inline styles.
 *
 * @since ??
 *
 * @param {EditPost.Store.State['content']} state The store state.
 *
 * @returns {void}
 */
export declare const syncModuleInlineFont: (state: EditPost.Store.ImmutableState['content']) => EditPost.Store.ImmutableState['content'];
