import { type EditPost, type ModuleFlatObjects } from '@divi/types';
/**
 * Merge global modules into edit-post store.
 *
 * @since ??
 */
export declare const mergeGlobalModules: (state: EditPost.Store.ImmutableState['content'], content: ModuleFlatObjects, owner: string) => EditPost.Store.ImmutableState['content'];
