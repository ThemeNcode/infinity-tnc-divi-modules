import { type EditPost } from '@divi/types';
/**
 * Add column via column divider's add column button.
 *
 * @since ??
 */
export declare const addColumnViaDivider: (stateContent: EditPost.Store.ImmutableState['content'], rowId: string, dividerPosition: number) => EditPost.Store.ImmutableState['content'];
