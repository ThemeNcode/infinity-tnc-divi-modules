import { type EditPost, type ModuleFlatObject } from '@divi/types';
/**
 * Add module after given id to store state.
 *
 * @since ??
 */
export declare const addModule: (state: EditPost.Store.ImmutableState['content'], id: string, name: string, props: ModuleFlatObject['props'], position?: 'before' | 'after' | 'inside', newId?: string) => EditPost.Store.ImmutableState['content'];
