import { type EditPost } from '@divi/types';
/**
 * Remove globalModule attribute from the module and globalParent attribute from modules children.
 *
 * @since ??
 */
export declare const disableGlobalModule: (stateContent: EditPost.Store.ImmutableState['content'], id: string) => EditPost.Store.ImmutableState['content'];
