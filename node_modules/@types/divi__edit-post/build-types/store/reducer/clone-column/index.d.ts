import { type EditPost } from '@divi/types';
/**
 * Clone module (and all of its child modules) based on given module id from the given state
 * and inserted cloned module  right after last child module of the to-be-cloned module.
 *
 * @since ??
 *
 */
export declare const cloneColumn: (state: EditPost.Store.ImmutableState['content'], id: string) => EditPost.Store.ImmutableState['content'];
