import { type EditPost } from '@divi/types';
/**
 * Remove module from the given state. Module removal is optimized for performance so when
 * structure element is removed, its child module is not immediately removed from the state tree.
 * Instead the removed module id is registered for garbage collector to remove when the
 * app is idle. This function returns new flat object based on given state to prevent mutation.
 *
 * @since ??
 *
 */
export declare const removeColumn: (state: EditPost.Store.ImmutableState['content'], id: string) => EditPost.Store.ImmutableState['content'];
