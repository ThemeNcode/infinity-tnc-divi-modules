import { type EditPost } from '@divi/types';
/**
 * Change children of module.
 *
 * @since ??
 *
 */
export declare const editChildren: (state: EditPost.Store.ImmutableState['content'], id: string, children: string[]) => EditPost.Store.ImmutableState['content'];
