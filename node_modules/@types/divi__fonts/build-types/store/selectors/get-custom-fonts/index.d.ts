import { type Fonts } from '@divi/types';
/**
 * Gets custom fonts.
 *
 * Retrieves custom fonts from the state by accessing the `customFonts`
 * property of the `state` object.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * const customFonts = window.divi.data.select('divi/fonts').getCustomFonts();
 * console.log(customFonts);
 * ```
 */
export declare const getCustomFonts: (state: Fonts.Store.ImmutableState) => Fonts.Custom.Fonts;
