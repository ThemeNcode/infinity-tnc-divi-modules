import { type ImmutableObject } from 'seamless-immutable';
import { type SpamProtection } from '@divi/types';
/**
 * Gets Spam Protection Services.
 *
 * Retrieves the Spam Protection Services from the store state.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * const emailService = window.divi.data.select('divi/spam-protection').getServices();
 * ```
 */
export declare const getServices: (state: SpamProtection.Store.ImmutableState) => ImmutableObject<Record<string, SpamProtection.Service.Provider>>;
