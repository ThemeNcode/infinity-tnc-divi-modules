import { type Fonts } from '@divi/types';
/**
 * Checks whether specified font is loaded.
 *
 * Checks if a specified font name has been loaded in the store state.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example
 * ```
 * const loaded = window.divi.data.select('divi/fonts').isLoaded('Open Sans');
 * console.log(loaded); // true/false
 * ```
 */
export declare const isLoaded: (state: Fonts.Store.ImmutableState, name: string) => boolean;
