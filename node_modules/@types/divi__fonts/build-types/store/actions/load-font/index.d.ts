import { type Fonts } from '@divi/types';
/**
 * Enqueues a font.
 *
 * This action enqueues a with the given name font by adding a link tag to the document.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/fonts').loadFont('Arial');
 * ```
 */
export declare const loadFont: (name: string) => Fonts.Store.Actions.LoadFont.ReturnValue;
