/**
 * Register spam protection store.
 *
 * Registers `divi/spam-protection` store.
 * Requires `divi/settings` store.
 * If `divi/settings` is not registered yet, it will be registered first before
 * continuing to register `divi/spam-protection`.
 *
 * @since ??
 *
 * @returns {void}
 */
export declare const registerSpamProtectionStore: () => void;
