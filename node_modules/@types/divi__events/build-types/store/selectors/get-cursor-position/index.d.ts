import { type Events } from '@divi/types';
/**
 * Gets cursor position.
 *
 * Retrieves the cursor position from the store state.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * const cursorPosition = window.divi.data.select('divi/events').getCursorPosition();
 * ```
 */
export declare const getCursorPosition: (state: Events.Store.ImmutableState) => Events.Cursor.Position;
