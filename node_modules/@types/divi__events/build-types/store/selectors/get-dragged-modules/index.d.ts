import { type Events } from '@divi/types';
/**
 * Gets currently dragged modules.
 *
 * Retrieves modules that are currently being dragged by a user in the Divi Visual Builder.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example
 * ```tsx
 * const draggedModules = window.divi.data.select('divi/events').getDraggedModules();
 * ```
 */
export declare const getDraggedModules: (state: Events.Store.ImmutableState) => Events.Store.ImmutableState['draggedModules'];
