import { type Events, type Module } from '@divi/types';
/**
 * Gets the dragged module's drop zone properties.
 *
 * Retrieves the dragged module drop zone properties from the store state.
 * If there is no dragged module drop zone, it returns `null`.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * window.divi.data.select('divi/events').getDraggedModuleDropZone();
 * ```
 */
export declare const getDraggedModuleDropZone: (state: Events.Store.ImmutableState) => null | Module.DropZone.Props;
