import { type Events } from '@divi/types';
/**
 * Gets currently hovered module.
 *
 * Retrieves the currently hovered module from the store state.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * const hoveredModule = window.divi.data.select('divi/events').getHoveredModule();
 * ```
 */
export declare const getHoveredModule: (state: Events.Store.ImmutableState) => Events.Hovered.Module;
