import { type Events } from '@divi/types';
/**
 * Checks if a module is being dragged.
 *
 * Checks if there is a module that is currently being dragged.
 * This is any module that has an initiated drag event that has not yet completed.
 *
 * @see {@link startDrag} dispatch function
 * @see {@link endDrag} dispatch function
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * const draggingStatus = window.divi.data.select('divi/events').getDraggingStatus();
 * ```
 */
export declare const getDraggingStatus: (state: Events.Store.ImmutableState, dragLocation?: string) => boolean;
