import { type Events } from '@divi/types';
/**
 * Gets dragged module.
 *
 * Retrieves a dragged module with the specified ID from the data store.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * const draggedModule = window.divi.data.select('divi/events').getDraggedModule('module-123');
 * ```
 */
export declare const getDraggedModule: (state: Events.Store.ImmutableState, id: string) => Events.Dragged.Module;
