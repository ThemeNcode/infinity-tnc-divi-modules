import { type Events } from '@divi/types';
/**
 * Sets module hovered area.
 *
 * This action sets the hovered area of the currently hovered module.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/events').setHoveredArea('bottom');
 * ```
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/events').setHoveredArea('inside');
 * ```
 */
export declare const setHoveredArea: (area: Events.Hovered.Area) => Events.Store.Actions.SetHoveredArea.ReturnValue;
