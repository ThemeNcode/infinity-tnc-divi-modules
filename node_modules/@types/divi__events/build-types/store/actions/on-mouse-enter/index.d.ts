import { type Events } from '@divi/types';
/**
 * Sets mouse-enter element.
 *
 * This action sets the mouse-enter element when the mouse enters a specific area.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```
 * window.divi.data.dispatch('divi/events').onMouseEnter(
 *   'module',
 *   'section1',
 *   { label: 'divi/section' },
 *   'top'
 * );
 * ```
 */
export declare const onMouseEnter: (target: string, id: string, payload: Events.Store.Actions.OnMouseEnter.Payload, area: Events.Hovered.Area) => Events.Store.Actions.OnMouseEnter.ReturnValue;
