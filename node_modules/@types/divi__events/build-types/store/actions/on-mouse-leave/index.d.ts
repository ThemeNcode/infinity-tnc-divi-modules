import { type Events } from '@divi/types';
/**
 * Sets mouse-leave element.
 *
 * This action sets the mouse-leave element when the mouse leaves a specific area.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/events').onMouseLeave(
 *   'module-456',
 *   'module-123',
 *   { label: 'divi/module' }
 * );
 * ```
 */
export declare const onMouseLeave: (target: string, id: string, payload: Events.Store.Actions.OnMouseEnter.Payload) => Events.Store.Actions.OnMouseLeave.ReturnValue;
