import { type Events } from '@divi/types';
/**
 * Sets start of a drag and drop action.
 *
 * This action initiates dragging of modules and saves the dragged modules to the store.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/events').startDrag([
 *   {
 *     id:         'module-123',
 *     moduleType: 'section',
 *     styles:     {}
 *   }
 * ]);
 * ```
 */
export declare const startDrag: (items: Events.Dragged.Modules, dragLocation: Events.Dragged.DragLocation) => Events.Store.Actions.StartDrag.ReturnValue;
