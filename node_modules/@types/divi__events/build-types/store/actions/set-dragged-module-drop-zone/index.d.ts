import { type DropZoneProps } from '@types/divi__module';
import { type Events } from '@divi/types';
/**
 * Sets the drop zone state for a dragged module.
 *
 * This action saves the drop zone props for a dragged module.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * const dropZoneProps: DropZoneProps = {
 *   position:            'top',
 *   isWireframe:         true,
 *   draggedModuleType:   'module',
 *   draggedModuleStyles: {
 *     top:  10,
 *     left: 10,
 *   },
 *   type:                     'spacer',
 *   setDraggedModuleDropZone: () => console.log('Dragged dropzone set'),
 * };
 *
 * window.divi.data.dispatch('divi/events').setDraggedModuleDropZone(dropZoneProps);
 * ```
 */
export declare const setDraggedModuleDropZone: (props: null | DropZoneProps) => Events.Store.Actions.SetDraggedModuleDropZone.ReturnValue;
