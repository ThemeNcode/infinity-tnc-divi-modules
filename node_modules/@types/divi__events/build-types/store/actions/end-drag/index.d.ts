import { type Events } from '@divi/types';
/**
 * Sets the end drag and drop state.
 *
 * This action sets the end drag when a user stops dragging a module during a drag and drop operation.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/events').endDrag();
 * ```
 */
export declare const endDrag: () => Events.Store.Actions.EndDrag.ReturnValue;
