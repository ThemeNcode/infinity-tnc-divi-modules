import { type Events } from '@divi/types';
/**
 * Sets mouse cursor position.
 *
 * Sets the mouse cursor position based on the provided coordinates.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```
 * window.divi.data.dispatch('divi/events').setCursorPosition(100, 200, 50, 100);
 * ```
 */
export declare const setCursorPosition: (pageX: number, pageY: number, clientX: number, clientY: number) => Events.Store.Actions.SetCursorPosition.ReturnValue;
