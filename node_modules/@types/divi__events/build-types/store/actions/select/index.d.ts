import { type Events, type ModuleType } from '@divi/types';
/**
 * Saves a selected module.
 *
 * This action saves the ID of a selected module in the store state.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.select('divi/events').select('section-1', 'section');
 * ```
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/events').select('module-123', 'module');
 * ```
 */
export declare const select: (id: string, moduleType: ModuleType) => Events.Store.Actions.Select.ReturnValue;
