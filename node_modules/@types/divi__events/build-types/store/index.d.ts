import { type Events } from '@divi/types';
/**
 * Initial States.
 * Initial state gets exported so test can use the exact same initial state as the store.
 *
 * @since ??
 *
 * @type {object}
 * @property {object} parentHover
 * @property {object} parentSelect
 * @property {object} parentSelectMap
 * @property {object} currentHover
 * @property {object} cursorPosition
 * @property {boolean} isDragging
 * @property {object[]} draggedModules
 * @property {object} draggedModuleDropZone
 * @property {object[]} selectedModules
 */
export declare const initialState: Events.Store.ImmutableState;
/**
 * Register saved / serialized post store.
 *
 * @since ??
 */
export declare const registerEventsStore: () => void;
