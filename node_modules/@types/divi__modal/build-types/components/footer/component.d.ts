import { type ReactElement } from 'react';
import { type FooterProps } from './types';
import './style.scss';
/**
 * Modal footer component.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {Array<object>} props.buttons Buttons to display in footer.
 * @param {string} props.buttons.x.label Button label.
 * @param {string} props.buttons.x.icon Button icon.
 * @param {string} props.buttons.x.backgroundColor Button background color.
 * @param {boolean} props.buttons.x.showIcon Whether to show button icon.
 * @param {boolean} props.buttons.x.showLabel Whether to show button label.
 * @param {boolean} props.buttons.x.showTooltip Whether to show tooltip.
 *
 *
 * @returns {ReactElement}
 */
export declare const Footer: (props: FooterProps) => ReactElement;
