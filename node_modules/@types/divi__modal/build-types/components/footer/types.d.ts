import { type ComponentPropsWithoutRef } from 'react';
export interface FooterButton extends Required<Pick<ComponentPropsWithoutRef<'button'>, 'onClick' | 'name'>> {
    label: string;
    icon?: string;
    showIcon?: boolean;
    showLabel?: boolean;
    showTooltip?: boolean;
    backgroundColor?: string;
}
export type FooterButtons = FooterButton[];
export interface FooterProps {
    buttons: FooterButtons;
}
