import { type ReactElement } from 'react';
import { type BodyContainerProps } from './types';
/**
 * Modal Body container component.
 *
 * This is a wrapper component for `Body` component.
 * It is used to pass the necessary state to the `Body` component.
 *
 * @since ??
 *
 * @param {object} params Component props.
 * @param {ReactNode} params.children Modal body content.
 * @param {string} params.className Additional CSS class name(s).
 *
 * @returns {ReactElement} The rendered `Body` component.
 */
export declare const BodyContainer: ({ children, className, }: BodyContainerProps) => ReactElement;
