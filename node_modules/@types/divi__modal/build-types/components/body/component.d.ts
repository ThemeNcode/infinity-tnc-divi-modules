import { type ReactElement } from 'react';
import { type BodyComponentProps } from './types';
import './style.scss';
/**
 * Modal Body component.
 *
 * React component for rendering modal body.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {ReactNode} props.children Modal body content.
 * @param {string} props.className Additional CSS class name(s).
 * @param {Function} props.setBodySiblingHeight Callback function to set sibling elements height.
 *
 * @returns {ReactElement}
 */
export declare const Body: (props: BodyComponentProps) => ReactElement;
