import { type ResizeHandlePosition } from '../../types';
/**
 * Get CSS rotate declaration so icon resize is positioned correctly.
 *
 * @since ??
 *
 * @param {ResizeHandlePosition} position Resize handle position.
 *
 * @returns {string}
 */
export declare const transformRotate: (position: ResizeHandlePosition) => string;
