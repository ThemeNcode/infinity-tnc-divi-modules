import { type ReactElement } from 'react';
import { type ResizeHandleProps } from './types';
import './style.scss';
/**
 * Modal Resize Handle.
 *
 * Component for rendering modal's resize handle for resizing modal.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.position Position of resize handle.
 *
 * @returns {ReactElement}
 */
export declare const ResizeHandle: (props: ResizeHandleProps) => ReactElement;
