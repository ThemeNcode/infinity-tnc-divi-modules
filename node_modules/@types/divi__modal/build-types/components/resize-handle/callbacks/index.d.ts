import { type ResizeHandleDraggableCallback } from '../types';
/**
 * Generate event callback for modal resize-handle's onClick / onMouseDown based on callback params
 * given which is based on context value passed down from wrapper component via React Context.
 *
 * @since ??
 *
 * @param {ResizeHandleOnElementClickParams} callbackParams Callback parameters.
 *
 * @todo Move this into its own sub-dir. Right now it shares `valueOnClick` variable hence current arrangement.
 *
 * @returns {ResizeHandleDraggableCallback}
 */
export declare const onElementClick: ResizeHandleDraggableCallback;
/**
 * Generate event callback for modal's resize-handle's onMouseMove based on callback params given
 * which is based on context value passed down from wrapper component via React Context.
 *
 * @since ??
 *
 * @param {ResizeHandleOnElementDragParams} callbackParams Callback parameters.
 *
 * @todo Move this into its own sub-dir. Right now it shares `valueOnClick` variable hence current arrangement.
 *
 * @returns {ResizeHandleDraggableCallback}
 */
export declare const onElementDrag: ResizeHandleDraggableCallback;
