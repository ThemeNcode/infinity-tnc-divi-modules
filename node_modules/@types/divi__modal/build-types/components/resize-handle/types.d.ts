import { type ElementDragReleaseParams } from '@types/divi__draggable';
import { type Modal } from '@divi/types';
export type ResizeHandlePosition = 'top' | 'left' | 'right' | 'corner';
export interface ResizeHandleProps {
    position: ResizeHandlePosition;
}
export type ResizeHandleDraggableCallback = (callbackParams: ResizeHandleOnElementClickParams | ResizeHandleOnElementDragParams) => (params?: ElementDragReleaseParams) => void;
export interface ResizeHandleOnElementClickParams {
    modalDimension: Modal.Dimension;
}
export interface ResizeHandleOnElementDragParams {
    modalName: string;
    dimensionMinimum: {
        width: number;
        height: number;
    };
}
export interface ResizeHandleStartDragValue {
    dimension: Modal.Dimension;
}
