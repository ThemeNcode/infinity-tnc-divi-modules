import { type ReactElement } from 'react';
import { type HeaderButtonProps } from './types';
/**
 * Modal Button.
 *
 * Modal header's common button component.
 *
 * @since ??
 *
 * @param {object} props Header button props.
 * @param {string} props.name Button name.
 * @param {string} props.label Button label.
 * @param {string} props.icon Optional button icon.
 * @param {boolean} props.visible Optional. Whether the button is visible.
 * @param {Function} props.onButtonClick Callback function to be called when button is clicked.
 *
 * @returns {ReactElement}
 */
declare const Button: {
    (props: HeaderButtonProps): ReactElement;
    defaultProps: {
        visible: boolean;
        icon: string;
    };
};
export { Button, };
