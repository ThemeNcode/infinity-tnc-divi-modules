import { type ReactElement } from 'react';
import { type HeaderProps } from './types';
import './style.scss';
/**
 * Modal Header.
 *
 * Component for rendering modal header.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.name Modal header name.
 * @param {boolean} props.closeButton Whether to show close button.
 * @param {boolean} props.backButton Whether to show back button.
 * @param {Function} props.onContextMenu Context menu callback handler.
 * @param {Function} props.onClickBackButton Click callback handler for back button.
 * @param {string} props.type Modal header type.
 *
 * @returns {ReactElement}
 */
declare const Header: {
    (props: HeaderProps): ReactElement;
    defaultProps: {
        optionsButton: string;
        moreButton: boolean;
        closeButton: boolean;
        onContextMenu: (...args: any[]) => void;
    };
};
export { Header, };
