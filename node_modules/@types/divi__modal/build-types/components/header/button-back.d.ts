import { type ReactElement } from 'react';
import { type BackButtonProps } from './types';
/**
 * Back button component for modal header.
 *
 * @since ??
 *
 * @param {BackButtonProps} props Back button props.
 *
 * @returns {ReactElement}
 */
declare const BackButton: ({ modalName, onClickBackButton, primary, }: BackButtonProps) => ReactElement;
export { BackButton, };
