import { type ReactElement } from 'react';
import { type HeaderOptionsButtonProps } from './types';
/**
 * Modal Options Button.
 *
 * Modal's header options button (preset-like button below modal's title).
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.label Optional button label. Defaults to `''`.
 * @param {Function} props.onButtonClick Optional click callback handler for options button.
 * Defaults to `lodash`'s `noop`.
 * @param {string} props.tooltipId Optional tooltip ID. Defaults to `''`.
 * @param {string} props.tooltipText Optional tooltip text. Defaults to `''`.
 *
 * @returns {ReactElement}
 */
declare const OptionsButton: ({ label, onButtonClick, tooltipId, tooltipText, }: HeaderOptionsButtonProps) => ReactElement;
export { OptionsButton, };
