import React, { type ReactElement } from 'react';
import { type CloseButtonProps } from './types';
/**
 * Close Button.
 *
 * Close button component for modal header.
 * If closeable is false, the button will not be rendered.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.modalName Modal name.
 * @param {boolean} props.closeable Whether the button is closeable.
 *
 * @returns {ReactElement}
 */
export declare const CloseButton: React.MemoExoticComponent<(props: CloseButtonProps) => ReactElement>;
