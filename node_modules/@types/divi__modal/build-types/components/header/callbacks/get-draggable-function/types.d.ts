import { type ElementClickParams, type ElementDragReleaseParams } from '@types/divi__draggable';
import { type Modal } from '@divi/types';
export declare namespace GetDraggableFunction {
    interface StartDragValue {
        dimension: Modal.Dimension;
        offset: Modal.Offset;
    }
    namespace Click {
        interface Params {
            snappable: boolean;
            modalDimension: Modal.Dimension;
            modalName: string;
            modalOffset: Modal.Offset;
        }
        type Callback = (params: ElementClickParams) => void;
    }
    namespace Drag {
        interface Params {
            modalName: string;
            snappable: boolean;
        }
        type Callback = (params: ElementDragReleaseParams) => void;
    }
    namespace Release {
        interface Params {
            modalName: string;
            snappable: boolean;
            modalDimension: Modal.Dimension;
        }
        type Callback = (params: ElementDragReleaseParams) => void;
    }
}
