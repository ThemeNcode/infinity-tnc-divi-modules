import { type GetDraggableFunction } from './types';
/**
 * Object of draggable functions that gets used by modal header component to handle drag and drop capability.
 *
 * @since ??
 */
export declare const getDraggableFunction: {
    /**
     * Callback function that gets executed when header is clicked / pressed / about to be dragged.
     *
     * @since ??
     */
    onElementClick: ({ snappable, modalDimension, modalName, modalOffset, }: GetDraggableFunction.Click.Params) => GetDraggableFunction.Click.Callback;
    /**
     * Callback function that gets executed when header is being dragged.
     *
     * @since ??
     */
    onElementDrag: ({ modalName, snappable, }: GetDraggableFunction.Drag.Params) => GetDraggableFunction.Drag.Callback;
    /**
     * Callback function that gets executed when header is released / dropped.
     *
     * @since ??
     */
    onElementRelease: ({ modalName, snappable, }: GetDraggableFunction.Release.Params) => GetDraggableFunction.Release.Callback;
};
