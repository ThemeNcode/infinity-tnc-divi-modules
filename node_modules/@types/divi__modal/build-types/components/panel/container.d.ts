import { type ReactElement } from 'react';
import { type PanelContainerProps } from './types';
/**
 * Modal Panel Container.
 *
 * Container component for panel which automatically handle props and events for panel switching.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.id ID of the panel.
 * @param {boolean} props.isMultiPanels Optional. Whether the panel is multi-panels.
 * @param {boolean} props.opened Optional. Whether the panel is opened.
 * @param {ReactNode} props.children  Children to render.
 *
 * @returns {ReactElement}
 */
export declare const PanelContainer: (props: PanelContainerProps) => ReactElement;
