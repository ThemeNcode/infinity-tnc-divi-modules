import { type ReactElement } from 'react';
import { type PanelProps } from './types';
import './style.scss';
/**
 * Modal Body Panel.
 *
 * Modal's body-panel component.
 *
 * If `opened` is `false`, null will be returned.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.id ID of the panel.
 * @param {boolean} props.opened Whether the panel is opened or not.
 * @param {boolean} props.hasGroupOpened Whether the panel has group opened or not.
 * @param {string} props.height Optional height of the panel.
 * @param {ReactNode} props.children Optional children to render.
 *
 * @returns {ReactElement}
 */
export declare const Panel: (props: PanelProps) => ReactElement;
