import { type ReactNode } from 'react';
export interface PanelProps {
    children?: ReactNode;
    id: string;
    opened: boolean;
    hasGroupOpened?: boolean;
    height?: number;
}
export interface PanelContainerProps {
    children: ReactNode;
    id: string;
    label?: string;
    opened?: boolean;
    hasGroupOpened?: boolean;
    isMultiPanels?: boolean;
}
