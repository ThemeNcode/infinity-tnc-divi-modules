import { type ReactElement } from 'react';
import { type TabsProps } from './types';
import './style.scss';
/**
 * Modal Tabs.
 *
 * Component for rendering modal tabs.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {ReactNode} props.children Optional children to render inside tabs.
 * @param {string} props.className Optional class name to add to tabs.
 *
 * @returns {ReactElement}
 */
export declare const Tabs: (props: TabsProps) => ReactElement;
