import { type ReactElement } from 'react';
import { type GroupTabsProps } from './types';
import './style.scss';
/**
 * Modal Group Tabs.
 *
 * Component for constructing nav tabs inside modal's group.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {object} props.tabs The tabs to be grouped.
 * @param {boolean} props.showIcon Optional. Whether to show tab icon.
 * @param {boolean} props.showLabel Optional. Whether to show tab label.
 * @param {Function} props.onClick Click callback handler.
 * @param {Function} props.onContextMenu Optional. Context menu callback handler.
 * @param {string} props.activeTab Active tab name.
 *
 * @returns {ReactElement}
 */
declare const GroupTabs: {
    (props: GroupTabsProps): ReactElement;
    defaultProps: {
        showIcon: boolean;
        showLabel: boolean;
        onContextMenu: (...args: any[]) => void;
    };
};
export { GroupTabs, };
