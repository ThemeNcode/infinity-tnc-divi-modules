import { type MouseEvent } from 'react';
export interface GroupTab {
    label: string;
    icon: string;
}
export interface GroupTabsProps {
    tabs: Record<string, GroupTab>;
    showIcon?: boolean;
    showLabel?: boolean;
    onClick: (e: MouseEvent) => void;
    onContextMenu?: (event: MouseEvent) => void;
    activeTab: string;
}
