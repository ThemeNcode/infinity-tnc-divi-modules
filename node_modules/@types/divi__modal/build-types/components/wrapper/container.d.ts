import { type ReactElement } from 'react';
import { type WrapperContainerProps } from './types';
import './style.scss';
/**
 * Modal Wrapper Container.
 *
 * Composed Wrapper component that passes necessary store state value to modal `Wrapper` component.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {object} props.dimension Modal dimensions object.
 * @param {object} props.offset Modal offset object.
 * @param {string} props.modalName Optional name of modal.
 * @param {boolean} props.caretStaysOnAppWindowClickPosition Optional. Whether caret stays on app window click position.
 * @param {boolean} props.centered Optional. Whether modal is centered or not.
 * @param {boolean} props.snappable Optional. Whether modal is snappable or not.
 *
 * @returns {ReactElement} The composed `Wrapper` component.
 */
export declare const WrapperContainer: (props: WrapperContainerProps) => ReactElement;
