import { type ReactElement } from 'react';
import { type WrapperProps } from './types';
import './style.scss';
/**
 * Modal Wrapper.
 *
 * Component for rendering modal wrapper.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {object} props.offset Modal offset.
 * @param {object} props.dimension Modal dimension.
 * @param {number} props.adminBarHeight Optional admin bar height. Default to `32`.
 * @param {number} props.appWindowPageYOffset Optional app window page Y offset. Default to `0`.
 * @param {number} props.bodySiblingHeight Optional body sibling height.
 * @param {boolean} props.caret Optional. Whether to show caret. Default to `false`.
 * @param {string} props.caretColor Optional caret color. Default to `#ffffff`.
 * @param {string} props.caretTopColor Optional caret top color. Default to `''`.
 * @param {boolean} props.caretStaysOnAppWindowClickPosition Optional.
 * Whether caret stays on app window click position. Default to `false`.
 * @param {boolean} props.centered Optional. Whether to center modal.
 * @param {ReactNode} props.children Optional modal children.
 * @param {object} props.dimensionMinimum Optional modal minimum dimension. Default to `{ width: 350, height: 350 }`.
 * @param {boolean} props.draggable Optional. Whether modal is draggable. Default to `false`.
 * @param {boolean} props.expand Optional. Whether modal is expanded. Default to `false`.
 * @param {boolean} props.expandable Optional. Whether modal is expandable. Default to `false`.
 * @param {string} props.modalActiveTab Optional modal active tab. Default to `content`.
 * @param {object} props.modalGroup Optional modal group. Defaults to `{}`.
 * @param {string} props.modalName Optional modal name.
 * @param {boolean} props.multiPanels Optional. Whether modal has multiple panels. Default to `false`.
 * @param {boolean} props.isGlobal Optional. Whether modal is global.
 * @param {boolean} props.isDragging Optional. Whether modal is dragging.
 * @param {object} props.panelSize Optional modal panel size.
 * @param {boolean} props.resizable Optional. Whether modal is resizable. Default to `false`.
 * @param {boolean} props.snappable Optional. Whether modal is snappable. Default to `false`.
 * @param {object} props.style Optional modal style. Default to `{}`.
 * @param {number} props.topWindowHeight Optional top window height. Default to `900`.
 * @param {number} props.topWindowWidth Optional top window width. Default to `1400`.
 * @param {boolean} props.isResizing Optional. Whether modal is resizing.
 * @param {boolean} props.builderBarWidth Optional. Builder Bar Width.
 * @param {boolean} props.pageBarHeight Optional. Page Bar Height.
 * @param {boolean} props.sideBarLeftWidth Optional. Left Sidebar Width.
 * @param {string} props.owner Optional. Owner of the modal.
 *
 * @returns {ReactElement}
 */
export declare const Wrapper: ({ adminBarHeight, appWindowPageYOffset, bodySiblingHeight, caret, caretColor, caretTopColor, caretStaysOnAppWindowClickPosition, centered, children, dimension, dimensionMinimum, draggable, expand, expandable, modalActiveTab, modalGroup, modalName, multiPanels, offset, owner, isGlobal, panelSize, resizable, snappable, style, topWindowHeight, topWindowWidth, isDragging, isResizing, builderBarWidth, pageBarHeight, sideBarLeftWidth, }: WrapperProps) => ReactElement;
