import { type ReactElement } from 'react';
import { type GroupContainerProps } from './types';
/**
 * Modal Group Container.
 *
 * Container component for group which automatically set event callback for switching group.
 * This component also automatically hide the group if it's not visible.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.groupName Group name.
 * @param {boolean} props.grouped Whether the group is grouped.
 * @param {ReactNode} props.children Optional group content.
 * @param {Function} props.onMouseEnter Optional mouse enter callback.
 * @param {Function} props.onMouseLeave Optional mouse leave callback.
 * @param {number} props.tabIndex Optional tab index.
 * @param {string} props.title Group title.
 * @param {string} props.id Group ID.
 * @param {boolean} props.visible Whether the group is visible.
 * @param {Function} props.clipboardAttrsMapsFormatter Optional clipboard attrs maps formatter.
 * @param {string} props.clipboardCategory Optional clipboard category. One of `content` or `style`. Default `content`.
 *
 * @returns {ReactElement}
 *
 * @example Basic usage.
 * ```js
 * <GroupContainer id="group_container_one" />
 * ```
 *
 * @example With props.
 * ```js
 * <GroupContainer
 *   groupName="group-two"
 *   grouped={true}
 *   title="Group Title"
 *   id="group_container_two"
 *   visible={true}
 *   clipboardAttrsMapsFormatter={(fieldsNode) => {
 *     // do stuff
 *     return fieldsNode;
 *   }}
 *   clipboardCategory="content"
 *   tabIndex={0}
 *   onMouseEnter={() => console.log('mouse enter')}
 *   onMouseLeave={() => console.log('mouse leave')}
 * >
 *  <div>Group Content</div>
 * </GroupContainer>
 * ```
 */
declare const GroupContainer: {
    (props: GroupContainerProps): ReactElement;
    defaultProps: {
        tabIndex: number;
        grouped: boolean;
        visible: boolean;
    };
};
export { GroupContainer, };
