import React from 'react';
import { type GroupProps } from './types';
import './style.scss';
/**
 * Modal Group.
 *
 * Modal's option-group component.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {ReactNode} props.children Optional group content.
 * @param {boolean} props.opened Optional. Whether the group is opened.
 * @param {Function} props.onTitleClick Optional title click callback.
 * @param {Function} props.onMouseEnter Optional mouse enter callback.
 * @param {Function} props.onMouseLeave Optional mouse leave callback.
 * @param {Function} props.onContextMenu Optional context menu callback.
 * @param {number} props.tabIndex Optional tab index.
 * @param {string} props.title Group title.
 * @param {string} props.id Group ID.
 *
 * @returns {ReactElement}
 */
declare const Group: React.ForwardRefExoticComponent<GroupProps & React.RefAttributes<HTMLDivElement>>;
export { Group, };
