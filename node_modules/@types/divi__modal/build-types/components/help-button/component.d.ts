import React, { type ReactElement } from 'react';
import { type HelpButtonProps } from './types';
import './styles.scss';
/**
 * Modal Help Button.
 *
 * Help button component.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {Function} props.onHelpClick Click callback handler for help button.
 * Defaults to `lodash`'s `noop`.
 *
 * @returns {ReactElement}
 */
export declare const HelpButton: React.MemoExoticComponent<(props: HelpButtonProps) => ReactElement>;
