import { type ReactNode } from 'react';
export interface BodyPanelWrapperProps {
    children: ReactNode;
    beforeTabItems?: ReactNode;
    afterTabItems?: ReactNode;
    isMultiPanels?: boolean;
}
