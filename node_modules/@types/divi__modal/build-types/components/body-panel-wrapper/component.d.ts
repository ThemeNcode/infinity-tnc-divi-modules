import { type ReactElement } from 'react';
import { type BodyPanelWrapperProps } from './types';
/**
 * Modal Body Panel Wrapper component.
 *
 * Wrapper component for Panel / PanelContainer component which automatically generate the tabs
 * based on given panels props and wrapped the <Panel*> inside `<Body>` component.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {ReactNode} props.children Modal body content.
 * @param {ReactNode} props.beforeTabItems Optional tab items to be rendered before the auto-generated tabs.
 * @param {ReactNode} props.afterTabItems Optional tab items to be rendered after the auto-generated tabs.
 * @param {boolean} props.isMultiPanels Optional. Whether the modal has multiple panels.
 *
 * @returns {ReactElement}
 */
declare const BodyPanelWrapper: (props: BodyPanelWrapperProps) => ReactElement;
export { BodyPanelWrapper, };
