import { type ReactElement } from 'react';
import { type BodyPanelWrapperProps } from './types';
/**
 * Modal Body Panel Wrapper container component.
 *
 * Container component for Panel wrapper which automatically generate the tabs based on given panels props.
 * This component is used to pass the necessary state to the `Body` component.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {ReactElement} props.children Modal body content.
 * @param {ReactElement} props.beforeTabItems Optional tab items to be rendered before the auto-generated tabs.
 * @param {ReactElement} props.afterTabItems Optional tab items to be rendered after the auto-generated tabs.
 *
 * @returns {ReactElement} The rendered `BodyPanelWrapper` component.
 */
declare const BodyPanelWrapperContainer: (props: BodyPanelWrapperProps) => ReactElement;
export { BodyPanelWrapperContainer, };
