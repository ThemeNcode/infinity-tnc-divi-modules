import React, { type MouseEvent, type ReactElement } from 'react';
/**
 * Button reset component.
 * This button is being used to reset the field value.
 *
 * @since ??
 */
export declare const ResetButton: React.MemoExoticComponent<({ enableReset, onReset, }: {
    enableReset: boolean;
    onReset: (event: MouseEvent) => void;
}) => ReactElement>;
