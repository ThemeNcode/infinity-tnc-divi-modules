import { type ReactElement } from 'react';
import { type FieldWrapperProps } from './types';
import './style.scss';
/**
 * Field Wrapper.
 *
 * Input wrapper which provides wrapper, label, and description for inputs.
 *
 * @since ??
 *
 * @param {FieldWrapperProps} props Component props.
 * @param {string} props.id Input ID.
 * @param {ReactNode} props.children Input component.
 * @param {string} props.description Optional. Input description.
 * @param {string} props.label Input label.
 * @param {Function} props.onContextMenu Optional. Context menu callback. Defaults to lodash's `noop`.
 * @param {Function} props.onReset Optional. Reset callback. Defaults to lodash's `noop`.
 * @param {boolean} props.enable Optional. Whether the input is enabled.  Defaults to `true`.
 * @param {boolean} props.enableReset Optional. Whether the input is resettable. Defaults to `false`.
 * @param {string} props.disabledDescription Optional. Disabled description.
 * Shown in tooltip when input is disabled. Defaults to `This field is disabled`.
 * @param {boolean} props.hasSync Optional. Whether the input has sync. Defaults to `false`.
 * @param {boolean} props.isSynced Optional. Whether the input is synced. Defautls to `true`.
 * @param {Function} props.onUnsyncGlobalModuleAttribute Optional. Unsync callback.
 *
 * @returns {ReactElement}
 */
declare const FieldWrapper: ({ id, children, description, label, onContextMenu, onReset, enable, enableReset, disabledDescription, hasSync, isSynced, onUnsyncGlobalModuleAttribute, dependencyCallback, dependencyList, }: FieldWrapperProps) => ReactElement;
export { FieldWrapper, };
