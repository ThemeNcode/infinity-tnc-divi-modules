import React, { type ReactElement } from 'react';
/**
 * Button description component.
 * This button is used to toggle field description visibility.
 *
 * @since ??
 */
export declare const DescriptionButton: React.MemoExoticComponent<({ description, showDescription, toggleDescription, }: {
    description: string;
    showDescription: boolean;
    toggleDescription: (isVisible: boolean) => void;
}) => ReactElement>;
