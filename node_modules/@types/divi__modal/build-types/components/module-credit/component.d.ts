import { type ReactElement } from 'react';
import { type ModuleCreditProps } from './types';
import './styles.scss';
/**
 * Modal Module Credit.
 *
 * Module credit component, use for module attribution.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.moduleURI Module URI.
 * @param {string} props.author Module author.
 * @param {string} props.authorURI Module author URI.
 * @param {string} props.moduleLabel Module label.
 * @param {boolean} props.official Whether the module is official or not.
 *
 * @returns {ReactElement}
 */
export declare const ModuleCredit: ({ moduleURI, author, authorURI, moduleLabel, official, }: ModuleCreditProps) => ReactElement;
