import { type ReactElement } from 'react';
import { type LocalOverlayProps } from './types';
import './styles.scss';
/**
 * Modal Local Overlay.
 *
 * Modal local overlay component.
 *
 * @since ??
 *
 * @param {string} props Component props.
 * @param {string} props.className CSS class name(s) for the component.
 * @param {ReactNode} props.children Children to render.
 *
 * @returns {ReactElement}
 */
export declare const LocalOverlay: (props: LocalOverlayProps) => ReactElement;
