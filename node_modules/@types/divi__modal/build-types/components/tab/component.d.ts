import { type ReactElement } from 'react';
import { type TabProps } from './types';
import './style.scss';
/**
 * Modal Tab.
 *
 * Component for rendering modal's tab.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {boolean} props.active Whether tab is active or not.
 * @param {Function} props.onClick Function to call when tab is clicked.
 * @param {Function} props.onContextMenu Optional function to call when tab is right clicked.
 * @param {ReactNode} props.children Children to render inside tab.
 *
 * @returns {ReactElement}
 */
declare const Tab: {
    (props: TabProps): ReactElement;
    defaultProps: {
        onContextMenu: (...args: any[]) => void;
    };
};
export { Tab, };
