import { type ReactElement } from 'react';
import { type TabContainerProps } from './types';
/**
 * Modal Table Container.
 *
 * Container component for modal `Tab` that passes necessary state to modal `Tab` component.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.id Tab id.
 * @param {ReactNode} props.children Tab content.
 *
 * @returns {ReactElement} The `Tab` component.
 */
declare const TabContainer: (props: TabContainerProps) => ReactElement;
export { TabContainer, };
