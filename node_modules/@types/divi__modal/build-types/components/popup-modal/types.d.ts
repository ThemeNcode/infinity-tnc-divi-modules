import { type ReactNode } from 'react';
export interface PopupModalProps {
    children: ReactNode;
    open: boolean;
    className?: string;
    onCancel?: () => void;
    onConfirm?: () => void;
}
