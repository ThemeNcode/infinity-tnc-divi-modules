import { type ReactElement } from 'react';
import { type PopupModalProps } from './types';
import './style.scss';
/**
 * Popup Modal.
 *
 * This popup modal is only opened inside the Module Settings Modal via `React`'s `createPortal()`.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {ReactNode} props.children Children to render.
 * @param {boolean} props.open Whether the modal is opened. Defaults to `false`.
 * @param {string} props.className Optional. Additional class name.
 * @param {Function} props.onCancel Optional. Callback function when the cancel button is clicked.
 * @param {Function} props.onConfirm Optional. Callback function when the confirm button is clicked.
 *
 * @returns {ReactElement}
 */
export declare const PopupModal: ({ children, open, className, onCancel, onConfirm, }: PopupModalProps) => ReactElement;
