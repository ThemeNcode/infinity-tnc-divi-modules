import { type ReactElement } from 'react';
import type { FilterButtonProps } from './types';
import './style.scss';
/**
 * FilterButton component is a button that triggers a dropdown for filtering options.
 *
 * @since ??
 *
 * @param {FilterButtonProps} props The properties of the FilterButton component.
 * @param {string} props.label The label displayed on the button.
 *
 * @returns {React.ReactElement} The rendered FilterButton component.
 */
declare const FilterButton: ({ label, }: FilterButtonProps) => ReactElement;
export { FilterButton, };
