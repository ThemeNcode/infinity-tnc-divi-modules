import { type ReactElement } from 'react';
import { FilterDropdown } from './component';
/**
 * FilterDropdownContainer.
 *
 * This container component is responsible for fetching and filtering modules from the module library
 * and passing the filtered modules as props to the FilterDropdown component.
 *
 * @since ??
 *
 * @returns {ReactElement} JSX element representing the FilterDropdownContainer component.
 *
 * @example
 * // Example usage of the FilterDropdownContainer component
 * <FilterDropdownContainer />
 */
export declare const FilterDropdownContainer: (props: Parameters<typeof FilterDropdown>[0]) => ReactElement;
