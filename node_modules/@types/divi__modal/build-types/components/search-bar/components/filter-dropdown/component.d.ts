import { type ReactElement } from 'react';
import type { FilterDropdownProps } from './types';
import './style.scss';
/**
 * FilterDropdown Component.
 *
 * This component provides a dropdown with a search input to filter a list of modules.
 *
 * @since ??
 *
 * @param {object} FilterDropdownProps Component props.
 * @param {FilterDropdownProps["placeholder"]} FilterDropdownProps.placeholder Placeholder text for the search input.
 * @param {FilterDropdownProps["filteredModules"]} FilterDropdownProps.filteredModules List of modules to be filtered.
 *
 * @returns {ReactElement} JSX element representing the FilterDropdown component.
 *
 * @example
 * // Example usage of the FilterDropdown component
 * <FilterDropdown
 *   placeholder="Type to filter"
 *   filteredModules={modulesList}
 * />
 */
declare const FilterDropdown: ({ placeholder, filteredModules, }: FilterDropdownProps) => ReactElement;
export { FilterDropdown, };
