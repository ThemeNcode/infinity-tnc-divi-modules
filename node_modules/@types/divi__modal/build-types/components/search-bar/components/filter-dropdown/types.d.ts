import type { ModuleAttrs, ModuleLibrary } from '@divi/types';
export interface FilterDropdownProps {
    placeholder?: string;
    filteredModules?: ModuleLibrary.Module.Settings<ModuleAttrs, ModuleAttrs>[];
}
