import { type ReactElement } from 'react';
import type { SearchBarProps } from './types';
import './style.scss';
/**
 * Modal Search Bar component.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {object} props.placeholder Optional. Input placeholder text.
 * @param {Function} props.onChange Optional. On change callback handler.
 *
 * @returns {ReactElement}
 */
declare const SearchBar: ({ placeholder, onChange, }: SearchBarProps) => ReactElement;
export { SearchBar, };
