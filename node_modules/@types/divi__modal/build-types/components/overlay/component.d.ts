import { type ReactElement } from 'react';
import { type OverlayProps } from './types';
import './style.scss';
/**
 * Modal Overlay.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.backgroundColor Optional background color of the overlay. Defaults to `rgba(255,255,255,.8)`.
 * @param {number} props.zIndex Z-index of the overlay. Defaults to `100025`.
 * @param {Function} props.onOverlayClick Click callback handler for overlay. Defaults to `lodash`'s `noop`.
 *
 *
 * @returns {ReactElement}
 */
declare const Overlay: {
    (props: OverlayProps): ReactElement;
    defaultProps: {
        backgroundColor: string;
        zIndex: number;
        onOverlayClick: (...args: any[]) => void;
    };
};
export { Overlay, };
