import { type BlockConfiguration } from '@wordpress/blocks';
export interface ModuleDefinitionSettings extends BlockConfiguration {
    childrenName?: string[];
    d4Shortcode?: string;
}
export type ModuleCollections = Record<string, ModuleDefinitionSettings>;
