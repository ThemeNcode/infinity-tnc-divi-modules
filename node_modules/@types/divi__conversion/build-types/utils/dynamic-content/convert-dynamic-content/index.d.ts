/**
 * Regex pattern to match dynamic content.
 *
 * @see https://regex101.com/r/ehh9z8/1
 */
export declare const regex: RegExp;
/**
 * Converts dynamic content in a string to a JSON-like format.
 *
 * @since ??
 *
 * @param {string} value The string to convert.
 *
 * @returns {string} The converted string. Will return the original string if the conversion fails.
 */
export declare const convertDynamicContent: (value: string) => string;
