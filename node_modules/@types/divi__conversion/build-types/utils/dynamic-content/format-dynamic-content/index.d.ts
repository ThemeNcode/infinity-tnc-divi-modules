/**
 * Formats dynamic content into JSON-like string.
 *
 * @since ??
 *
 * @param {string} name The type of dynamic content being formatted.
 * @param {Record<string, string>} settings The additional settings for the dynamic content.
 *
 * @returns {string} Returns a string with the formatted dynamic content.
 */
export declare const formatDynamicContent: (name: string, settings: Record<string, string>) => string;
