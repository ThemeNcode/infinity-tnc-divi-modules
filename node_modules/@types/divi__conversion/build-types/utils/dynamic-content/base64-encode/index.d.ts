/**
 * Encodes a given string into base64 format with UTF-8 support.
 *
 * This is a modified version of the base64 decoder from the base64-utf8 package.
 *
 * @see https://github.com/vilien/base64-utf8/blob/master/src/base64.ts
 *
 * @since ??
 *
 * @param {string} input The string to encode.
 *
 * @returns {string} The encoded string.
 */
export declare const base64Encode: (input: string) => string;
