import { type StickyStatus, type StringObject } from '../../types';
/**
 * Determine Sticky status.
 *
 * Determines if sticky options is enabled on a module and on what view port it is activated.
 * If sticky is only enabled on a responsive view then viewport would be the view where it is activated.
 *
 * @since ??
 *
 * @param {object} attrs A string object of all module attributes.
 *
 * @returns {object} An object that looks like: {active: boolean, viewport: 'desktop' | 'tablet' | 'phone'}.
 */
export declare const stickyStatus: (attrs: StringObject) => StickyStatus;
