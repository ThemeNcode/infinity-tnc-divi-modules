import { type NumberObject, type StringObject } from '../../types';
/**
 * Get Attr Map.
 *
 * Generates an object of addresses that represent a single attribute.
 * For example:
 * For the attribute `transform_skew='20px|30px'` will return the object:
 * `{ 'transform.desktop.value.skew.x': '20px', 'transform.desktop.value.skew.y': '30px' }`.
 *
 * @since ??
 *
 * @param {object} attrs A string object of the module's attributes.
 * @param {string} attrName The name of the attribute to map.
 * @param {string} moduleName Module slug for module specific conversions.
 *
 * @returns {object} Object keys are object addresses and the value is the corresponding attribute value.
 */
export declare const getAttrMap: (attrs: StringObject, attrName: string, moduleName: string) => StringObject | NumberObject;
