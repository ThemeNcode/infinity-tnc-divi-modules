import { type ModuleConversionMap } from '@divi/types';
import { type StringObject } from '../../types';
/**
 * Responsive, hover or sticky state enabled.
 *
 * Determines if responsive, hover or sticky are enabled on an attribute, based on the desktop name.
 * The return value is determined by whether the attribute (responsive/state) starts with 'on' or 'off'.
 *
 * @since ??
 *
 * @param {string} type 'hover' | 'sticky' | 'responsive'.
 * @param {string} desktopName Attribute name without prefix.
 * @param {object} attrs A string object of all module attributes.
 * @param {object} optionEnableMap Enable option map to get correct attribute that define option status.
 *
 * @returns {boolean} True if responsive/state attribute starts with 'on'.
 */
export declare const enabled: (type: 'hover' | 'sticky' | 'responsive', desktopName: string, attrs: StringObject, optionEnableMap?: ModuleConversionMap['optionEnableMap']) => boolean;
