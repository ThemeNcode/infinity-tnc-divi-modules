import { type Filters, type FontIconTypes } from './types';
/**
 * Font Icon Types.
 *
 * One of `divi` or `fa`.
 *
 * @since ??
 *
 * @constant
 * @returns {string[]}
 */
declare const fontIconTypes: FontIconTypes[];
/**
 * Font Icon Filters.
 *
 * One of `divi`, `fa`, `solid`, `line` or empty string.
 *
 * @since ??
 *
 * @constant
 * @returns {string[]}
 */
declare const fontIconFilters: Filters[];
export { fontIconTypes, fontIconFilters, };
