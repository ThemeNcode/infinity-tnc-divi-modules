import { type Icon } from '@divi/types';
/**
 * Icon List.
 *
 * An array of objects of icons metadata that includes:
 * `search terms`, `unicode` value of icon, `name`, applicable styles, `fontWeight` and `decodedUnicode` for each icon.
 *
 * @since ??
 *
 * @constant
 * @returns {Array<object>}
 */
declare const iconList: Icon.Data[];
/**
 * Down icons.
 *
 * An array of objects of `down indicator` icons metadata that includes:
 * `search terms`, `unicode` value of icon, `name`, applicable styles, `fontWeight` and `decodedUnicode` for each icon.
 * These are icons are used in `FullWidth` header module.
 *
 * @since ??
 *
 * @constant
 * @returns {Array<object>}
 */
declare const downIcons: Icon.Data[];
export * from './types';
export * from './constants';
export { iconList, downIcons, };
