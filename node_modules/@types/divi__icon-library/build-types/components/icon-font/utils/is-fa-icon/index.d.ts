import { type Icon } from '@divi/types';
/**
 * Is FA Icon.
 *
 * Check if the given icon font is FontAwesome icon font.
 * The icon is  FontAwesome icon font when `icon.type` value is `fa`.
 *
 * @since ??
 *
 * @param {string} icon Icon attribute value.
 * @param {string} icon.type Icon type.
 *
 * @returns {boolean}
 */
export declare const isFaIcon: (icon?: Icon.Font.AttributeValue) => boolean;
