import { type Icon } from '@divi/types';
/**
 * Is Black Icon Font Weight.
 *
 * Check if the icon font weight is black font weight.
 *
 * This function uses `isBlackFontWeightIcon` under the hood, passing the provided icon weight to this function.
 * The difference between the 2 is that this function accepts an object of icon attrs.
 *
 * @since ??
 *
 * @param {object} icon Icon attribute value.
 * @param {string} icon.weight Icon attribute value.
 *
 * @returns {boolean}
 */
export declare const isBlackIconFontWeight: (icon?: Icon.Font.AttributeValue) => boolean;
