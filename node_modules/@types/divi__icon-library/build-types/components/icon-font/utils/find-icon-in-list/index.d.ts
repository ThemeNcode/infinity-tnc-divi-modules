import { type Icon } from '@divi/types';
/**
 * Find Icon in List.
 *
 * Obtains the icon data of a given icon attribute from a provided list of icons
 * If the weight, type or unicode of the icon is not provided, it will return null.
 *
 * @since ??
 *
 * @param {Array<object>} iconList List of icons.
 * @param {object} icon Icon to find.
 * @param {string} icon.unicode Optional unicode of the icon.
 * @param {string} icon.weight Optional font weight of the icon.
 * One of `100`, `200`,`300`,`400`, `500`, `600`, `700`, `800` or `900`.
 * @param {string} icon.type Optional icon type.
 *
 * @returns {object|null} The found icon or null.
 */
export declare const findIconInList: (iconList: Icon.Type['data'][], icon: Icon.Font.AttributeValue) => Icon.Type['data'] | null;
