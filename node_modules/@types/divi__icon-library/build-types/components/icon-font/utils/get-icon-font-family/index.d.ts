/**
 * Get Icon Font Family.
 *
 * Check and return icon's font family based on icon type.
 * If the icon is `FA icon`, it will return `FontAwesome`.
 * Otherwise will return `ETmodules` as default.
 *
 * @since ??
 *
 * @param {string} type Icon type value.
 *
 * @returns {string} Icon font family. One of `FontAwesome` or `ETmodules`.
 */
export declare const getIconFontFamily: (type: string) => string;
