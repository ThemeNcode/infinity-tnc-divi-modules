import { type Icon } from '@divi/types';
/**
 * Process font icon.
 *
 * Process the font icon and return the decoded unicode.
 * If the provided icon has empty `unicode` value `null` will will be returned.
 * If the icon `type` is not found in `fontIconTypes`, `null` will be returned.
 *
 * @since ??
 *
 * @param {object} icon Icon attribute value.
 * @param {string} icon.unicode Icon unicode value.
 * @param {string} icon.type Icon type.
 * @param {boolean} isFontIconsDown Whether the icon is one the down indicator icons, used in FullWidth header module.
 * This is used to limit the search to either the `downIcons` or `iconList`.
 *
 * @returns  {string|null} The decoded unicode value or null.
 */
export declare const processFontIcon: (icon?: Icon.Font.AttributeValue, isFontIconsDown?: boolean) => string;
