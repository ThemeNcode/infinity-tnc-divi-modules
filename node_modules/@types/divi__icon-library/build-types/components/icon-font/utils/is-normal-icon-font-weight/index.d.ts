import { type Icon } from '@divi/types';
/**
 * Is Normal Icon Font Weight.
 *
 * Check if the icon font weight is normal font weight.
 * This function uses `isNormalFontWeightIcon` under the hood, passing the provided icon weight to this function.
 * The difference between the 2 is that this function accepts an object of icon attrs.
 *
 * @since ??
 *
 * @param {object} icon Icon attribute value.
 * @param {string} icon.weight Icon font weight value.
 *
 * @returns {boolean}
 */
export declare const isNormalIconFontWeight: (icon?: Icon.Font.AttributeValue) => boolean;
