/**
 * Escapes font icon.
 *
 * When font icon is printed as CSS content property, there is specific character that that has special meaning in CSS
 * needs to be escaped. For example a backslash character. This character delimits the beginning of an escape sequence,
 * a sequence of characters used to collectively represent a different character.
 *
 * @see http://www.merttol.com/articles/code/introduction-to-css-escape-sequences.html
 *
 * @since ??
 *
 * @param {string} icon The font icon string to escape.
 *
 * @returns {string} The escaped font icon string.
 */
export declare const escapeFontIcon: (icon: string) => string;
