import { type ReactElement } from 'react';
import { type IconSvgProps } from './types';
import './style.scss';
/**
 * IconSvg component.
 *
 * Generates an SVG wrapped in a div tag with any provided styles applied to the wrapper div.
 * The icon is wrapped in error boundary to prevent the app from crashing.
 * Note: If icon name is not found in the icon map, or empty value is provided, the component will return null.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.animation Optional predefined animation class name. Accepts only `rotate-center`.
 * @param {string} props.name Icon name.
 * @param {string} props.className Optional additional class names.
 * @param {string} props.backgroundColor Optional icon background color. Defaults to `transparent`.
 * @param {string} props.color Optional icon color. Defaults to `#273039`.
 * @param {number} props.size Optional icon size. Defaults to 14.
 * @param {string} props.viewBox Optional icon viewBox. Defaults to `0 0 28 28`.
 * @param {object} props.styles Optional additional styles to apply to the SVG wrapper.
 * @param {object} props.iconComponentProps Optional additional props to pass to the icon component.
 *
 * @returns {ReactElement}
 */
declare const IconSvg: ({ animation, name, className, backgroundColor, color, size, viewBox, styles, iconComponentProps, }: IconSvgProps) => ReactElement;
export { IconSvg, };
