import { type CSSProperties } from 'react';
import { type IconProps } from './components/types';
/**
 * Predefined animation name that will be applied to the icon.
 *
 * @since ??
 */
export type IconsAnimation = 'rotate-center';
/**
 * Icon component props.
 *
 * @since ??
 */
export interface IconSvgProps {
    name: string;
    className?: string;
    backgroundColor?: string;
    color?: string;
    size?: number;
    viewBox?: string;
    styles?: CSSProperties;
    animation?: IconsAnimation;
    iconComponentProps?: IconProps;
}
