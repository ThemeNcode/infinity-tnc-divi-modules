import { type ReactElement } from 'react';
import { type IconProps } from '../types';
export declare const name = "divi/layers-enhancement";
export declare const component: ({ color, }: IconProps) => ReactElement;
