/**
 * Register App Preferences Store.
 *
 * Register `divi/app-preferences` store.
 * This requires settings store so it will register this store first if not already registered.
 *
 * @since ??
 *
 * @returns {void}
 */
export declare const registerAppPreferencesStore: () => void;
