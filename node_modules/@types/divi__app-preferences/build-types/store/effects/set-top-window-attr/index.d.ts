import { type set } from '../../actions/set';
/**
 * Sync divi/app-preferences store value into top window's divi/app-preferences.
 *
 * @since ??
 *
 * @param {ReturnType<typeof set>} action Returned action value.
 */
export declare const setTopWindowAttr: (action: ReturnType<typeof set>) => void;
