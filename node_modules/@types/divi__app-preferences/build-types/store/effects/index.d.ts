/**
 * Store Effects.
 *
 * @since ??
 * @private
 */
export declare const effects: {
    SET: (action: {
        type: "SET";
        path: [keyof import("@divi/types/src/app-preferences/store").Store.State, never];
        value: never;
    }) => void;
};
