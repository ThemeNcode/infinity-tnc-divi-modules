import { type AppPreferences } from '@divi/types';
/**
 * Saves a value in app preferences.
 *
 * This action sets a value in the app preferences store at the specified path.
 *
 * @since ??
 * @group Dispatch Functions
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/app-preferences').set(['modal', 'alwaysCollapseGroups'], false);
 * ```
 */
export declare const set: <TKey1 extends keyof AppPreferences.Store.State, TKey2 extends keyof AppPreferences.Store.State[TKey1]>(path: [TKey1, TKey2], value: AppPreferences.Store.State[TKey1][TKey2]) => {
    type: 'SET';
    path: [TKey1, TKey2];
    value: AppPreferences.Store.State[TKey1][TKey2];
};
