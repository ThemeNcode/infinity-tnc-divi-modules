/**
 * These objects are not used on D5i. These are added as a reference for future debugging purpose.
 *
 * Q: Why changing the object property?
 *
 * A1: D4 uses snake_case while D5i uses camelCase. Exceptions are avoided so this will get
 * renamed anyway. Thus better change the structure into something that is more organized
 * while we're at it. On top of these, it won't be 1:1 anyway because some of the property
 * is obsolete in D5i (see: modal related properties).
 */
export declare const d4AppPreferencesSettingsMap: {
    settings_bar_location: string;
    toolbar_click: string;
    toolbar_desktop: string;
    toolbar_grid: string;
    toolbar_hover: string;
    toolbar_phone: string;
    toolbar_tablet: string;
    toolbar_wireframe: string;
    toolbar_zoom: string;
    builder_animation: string;
    builder_enable_dummy_content: string;
    hide_disabled_modules: string;
    event_mode: string;
    view_mode: string;
    history_intervals: string;
    page_creation_flow: string;
    quick_actions_always_start_with: string;
    quick_actions_show_recent_queries: string;
    quick_actions_recent_queries: string;
    quick_actions_recent_categories: string;
    modal_preference: string;
    builder_display_modal_settings: string;
    modal_snap_location: string;
    modal_snap: string;
    modal_fullscreen: string;
    modal_dimension_width: string;
    modal_dimension_height: string;
    modal_position_x: string;
    modal_position_y: string;
    lv_modal_dimension_height: string;
    lv_modal_dimension_width: string;
    lv_modal_position_x: string;
    lv_modal_position_y: string;
    responsive_tablet_width: string;
    responsive_tablet_height: string;
    responsive_phone_width: string;
    responsive_phone_height: string;
    responsive_minimum_width: string;
    responsive_maximum_width: string;
};
