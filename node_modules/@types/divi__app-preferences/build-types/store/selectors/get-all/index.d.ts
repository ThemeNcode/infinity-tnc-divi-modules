import { type AppPreferences } from '@divi/types';
/**
 * Gets all app preferences.
 *
 * Retrieves all app preferences from the store.
 *
 * @since ??
 * @group Selector Functions
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * window.divi.data.select('divi/app-preferences').getAll();
 * ```
 */
export declare const getAll: (state: AppPreferences.Store.ImmutableState, includeDefault?: boolean) => AppPreferences.Store.ImmutableState;
