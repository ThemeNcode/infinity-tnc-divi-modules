import { Component, type ErrorInfo, type ReactNode } from 'react';
import PropTypes from 'prop-types';
import { type ErrorBoundaryProps, type ErrorBoundaryState } from './types';
import './style.scss';
/**
 * Error Boundary.
 *
 * Wrapper component that catches errors and displays a friendly messages to the user.
 * The user is given a choice to report the issue or retry the action.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.componentName The name of the component.
 *
 * @returns {ReactElement}
 */
export declare class ErrorBoundary extends Component<ErrorBoundaryProps, ErrorBoundaryState> {
    static propTypes: {
        children: PropTypes.Validator<PropTypes.ReactElementLike>;
        componentName: PropTypes.Validator<string>;
    };
    /**
     * Class constructor.
     *
     * @since ??
     *
     * @param {ErrorBoundaryProps} props Component props.
     */
    constructor(props: ErrorBoundaryProps);
    /**
     * React method to define a class as error boundaries.
     *
     * @since ??
     *
     * @param {Error} error Error title.
     * @param {ErrorInfo} info Error info.
     *
     * @returns {void}
     */
    componentDidCatch(error: Error, info: ErrorInfo): void;
    /**
     * Component render.
     *
     * @since ??
     *
     * @returns {ReactNode} React component.
     */
    render(): ReactNode;
}
