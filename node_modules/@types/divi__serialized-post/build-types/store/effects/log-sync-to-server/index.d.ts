import { type SerializedPost } from '@divi/types';
/**
 * Sync to Server Logger.
 *
 * @since ??
 */
export declare const logSyncToServer: (action: SerializedPost.Store.Actions.SyncToServer.ReturnValue, store: SerializedPost.Store.Instance) => void;
