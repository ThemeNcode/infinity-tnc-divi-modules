/**
 * Extracts global color IDs from a string.
 *
 * @param {string} content The input string to search for global color IDs.
 * @returns {string[]} - The array of global color IDs found in the input string.
 */
export declare const extractGlobalColorIds: (content: string) => string[];
