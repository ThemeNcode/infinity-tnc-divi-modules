import { type ModuleInstance } from '@types/divi__module-library';
import { type ModuleAttrs, type ModuleLibrary } from '@divi/types';
/**
 * Parse all attributes definition from Divi Builder's flat modules object.
 *
 * This attributes definition will be used to generate the module data attributes when
 * serializing the builder content that will be saved into database.
 *
 * @since ??
 *
 * @param {object[]} blocks Flat block instances.
 *
 * @returns {object} All Modules attributes definition.
 */
export declare const parseAttrsDefinitionFromFlatBlockInstances: (blocks: ModuleInstance<ModuleLibrary.Module.Component.SerializerAttrs<ModuleAttrs>>[]) => Record<string, ModuleLibrary.Module.Attributes>;
