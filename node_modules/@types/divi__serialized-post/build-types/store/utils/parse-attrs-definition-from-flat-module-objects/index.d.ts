import { type ModuleFlatObjects, type ModuleLibrary } from '@divi/types';
/**
 * Parse all attributes definition from Divi Builder's flat modules object.
 *
 * This attributes definition will be used to generate the module data attributes when
 * serializing the builder content that will be saved into the database.
 *
 * @since ??
 *
 * @param {object} moduleObjects Flat module objects.
 *
 * @returns {object} All Modules attributes definition.
 */
export declare const parseAttrsDefinitionFromFlatModuleObjects: (moduleObjects: ModuleFlatObjects) => Record<string, ModuleLibrary.Module.Attributes>;
