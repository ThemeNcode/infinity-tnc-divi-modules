import { type ModuleFlatObjects } from '@divi/types';
/**
 * Serialize WordPress block instances array data into Gutenberg's format post content.
 *
 * This function is a wrapper for the built-in serialize function from @wordpress/blocks.
 *
 * @since ??
 *
 * @param {object} moduleObjects The flat module objects to be serialized.
 * @param {string} root Object key of moduleObjects to be used as the root.
 *
 * @returns {string} The serialized post content in Gutenberg's format.
 */
export declare const serializeModuleFlatObjects: (moduleObjects: ModuleFlatObjects, root?: string) => string;
