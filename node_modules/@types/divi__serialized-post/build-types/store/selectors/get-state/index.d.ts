import { type SerializedPost } from '@divi/types';
/**
 * Get serialized post state.
 *
 * @todo (D5i) This should be refactored into selector which its returned value type isn't any.
 *
 * @since ??
 */
export declare const getState: (state: SerializedPost.Store.ImmutableState, path: SerializedPost.Store.ItemPath, defaultValue?: any) => string;
