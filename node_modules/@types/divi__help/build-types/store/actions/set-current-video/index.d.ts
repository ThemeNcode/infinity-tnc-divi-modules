import { type Help } from '@divi/types';
/**
 * Sets current video.
 *
 * This action sets the current video in the help section of the Visual Builder.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/help').setCurrentVideo(0);
 * ```
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/help').setCurrentVideo(123);
 * ```
 */
export declare const setCurrentVideo: (payload: Help.Store.Actions.SetCurrentVideo.ReturnValue['payload']) => Help.Store.Actions.SetCurrentVideo.ReturnValue;
