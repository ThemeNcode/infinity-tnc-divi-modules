import { type Help } from '@divi/types';
/**
 * Sets the video index relative to the current video.
 *
 * This action sets the index of the video to display, but instead of using the
 * desired video's position in the help video list, it sets the video based on
 * its position in the list relative to the currently loaded video. This is
 * typically used when a user clicks on a direction arrow (left/right, up/down)
 * to navigate to either the previous or the next video in the list.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function
 *
 * @example
 *
 * User clicks on a left/up arrow
 *
 * ```ts
 * window.divi.data.dispatch('divi/help').setJumpToVideo(-1);
 * ```
 *
 * @example
 *
 * User clicks on a right/down arrow
 *
 * ```ts
 * window.divi.data.dispatch('divi/help').setJumpToVideo(1);
 * ```
 */
export declare const setJumpToVideo: (payload: Help.Store.Actions.SetJumpToVideo.ReturnValue['payload']) => Help.Store.Actions.SetJumpToVideo.ReturnValue;
