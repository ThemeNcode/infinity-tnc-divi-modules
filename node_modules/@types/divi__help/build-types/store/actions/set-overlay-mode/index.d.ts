import { type Help } from '@divi/types';
/**
 * Sets overlay mode.
 *
 * This action sets the overlay mode flag for the help component which determines
 * if the help component is in overlay mode or not.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```
 * window.divi.data.dispatch('divi/help').setOverlayMode(true);
 * ```
 *
 * @example
 * ```
 * window.divi.data.dispatch('divi/help').setOverlayMode(false);
 * ```
 */
export declare const setOverlayMode: (payload: Help.Store.Actions.SetOverlayMode.ReturnValue['payload']) => Help.Store.Actions.SetOverlayMode.ReturnValue;
