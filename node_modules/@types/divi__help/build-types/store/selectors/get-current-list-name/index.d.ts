import { type Help } from '@divi/types';
/**
 * Gets current video list name.
 *
 * Retrieves the current video list name from the store state.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example
 * ```
 * const currentListName = window.divi.data.select('divi/help').getCurrentListName();
 * ```
 */
export declare const getCurrentListName: (state: Help.Store.ImmutableState) => Help.Store.ImmutableState['currentListName'];
