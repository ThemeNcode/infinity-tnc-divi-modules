import { type Help } from '@divi/types';
/**
 * Gets current video.
 *
 * Retrieves the current video from the store state.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example
 * ```
 * window.divi.data.select('divi/help').getCurrentVideo();
 * ```
 */
export declare const getCurrentVideo: (state: Help.Store.ImmutableState) => Help.Store.ImmutableState['currentVideo'];
