import { type Help } from '@divi/types';
/**
 * Gets the overlay mode.
 *
 * Retrieves the overlay mode value from the store state.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * const overlayMode = window.divi.data.select('divi/help').getOverlayMode();
 * ```
 */
export declare const getOverlayMode: (state: Help.Store.ImmutableState) => Help.Store.ImmutableState['overlayMode'];
