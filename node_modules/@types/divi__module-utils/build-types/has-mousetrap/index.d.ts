import { type HasMousetrapParams } from './types';
/**
 * Has Mousetrap.
 *
 * Determine whether mousetrap should be rendered or not.
 *
 * @since ??
 *
 * @param {object} param Params for mousetrap.
 * @param {boolean} param.isChildModule Whether the module is a child module or not.
 * @param {boolean} param.isChildHovered Whether direct child module of the module is hovered or not..
 * @param {boolean} param.isCurrentModuleSettingsOpened Whether current module's module settings is opened or not.
 * @param {boolean} param.isHovered Whether the module is currently being hovered or not.
 * @param {boolean} param.isParentModule Whether the module is a parent module or not.
 * @param {boolean} param.isSelected Whether the module is currently selected or not.
 *
 * @returns {boolean} Whether mousetrap should be rendered or not.
 *
 * @example
 * ```js
 * hasMousetrap({
 *  isHovered: false,
 * });
 * // => false
 * ```
 *
 * @example
 * ```js
 * hasMousetrap({
 *  isHovered: true,
 * });
 * // => true
 * ```
 */
export declare const hasMousetrap: ({ isChildHovered, isChildModule, isCurrentModuleSettingsOpened, isHovered, isParentModule, isSelected, }: HasMousetrapParams) => boolean;
