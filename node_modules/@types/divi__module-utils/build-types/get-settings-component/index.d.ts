import { type ReactElement } from 'react';
/**
 * Get Settings Component.
 *
 * Get module's settings component from the module library by module name.
 *
 * @since ??
 *
 * @param {string} moduleName Module name.
 *
 * @returns {ReactElement[]} Module's settings component.
 *
 * @example
 * ```js
 * getSettingsComponent('divi/text'); // It will return the settings component of the text module.
 * ```
 */
export declare const getSettingsComponent: (moduleName: string) => ReactElement[];
