/**
 * In Own Module.
 *
 * This function will identify if the module is a Divi core module.
 *
 * @since ??
 *
 * @param {string} namespacedModuleName Module namespace.
 *
 * @returns {string} True if the module is a Divi core module.
 *
 * @example
 * ```
 * isOwnModule( 'divi/accordion' );
 * // => true
 * ```
 *
 * @example
 * ```js
 * isOwnModule( 'example/module' );
 * // => false
 * ```
 */
export declare const isOwnModule: (namespacedModuleName: string) => boolean;
