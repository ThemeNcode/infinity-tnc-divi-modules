/**
 * Loads inline fonts for a module.
 *
 * This function enqueues the inline font to/from a module's inline fonts list,
 * such that the font assets will be loaded in the browser.
 *
 * @since ??
 *
 * @param {string} moduleId The id of the module to be updated.
 *
 * @returns {void}
 *
 * @example
 * ```ts
 * loadModuleInlineFont('module-123');
 * ```
 */
declare const loadModuleInlineFont: (moduleId: string) => void;
export { loadModuleInlineFont, };
