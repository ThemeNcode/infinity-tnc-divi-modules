import { type SaveInlineEditorValueParams } from './types';
/**
 * Save Inline Editor Value.
 *
 * Set attribute value for Inline Text Editor to the edit-post store.
 *
 * @since ??
 *
 * @param {object} params Callback parameters.
 * @param {string} params.moduleId The ID of the module to save the value for.
 * @param {string} params.attrName The name of the attribute to save the value for.
 * @param {string} [params.attrSubName] The name of the sub-attribute to save the value for (optional).
 * @param {string} params.breakpoint The breakpoint to save the value for.
 * @param {string} params.state The state to save the value for.
 * @param {any} params.value The value to save.
 *
 * @returns {void}
 *
 * @example
 * ```js
 * saveInlineEditorValue({
 *  moduleId: 'module1',
 *  attrName: 'content',
 *  breakpoint: 'desktop',
 *  state: 'value',
 *  value: 'Hello World',
 * }); // it will save 'Hello World' to the content attribute of `module1`.
 * ```
 */
export declare const saveInlineEditorValue: ({ moduleId, attrName, attrSubName, breakpoint, state, value, }: SaveInlineEditorValueParams) => void;
