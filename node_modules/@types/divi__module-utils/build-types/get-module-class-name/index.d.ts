/**
 * Get module class name defined in module.json config.
 *
 * - If moduleClassName property in module.json config is falsy, it will fallback to
 * use convert module name to class name.
 *
 * @since ??
 *
 * @param {string} moduleName Module name.
 *
 * @returns {string} Module class name configured in module.json config. Will return empty string on failure.
 */
export declare const getModuleClassName: (moduleName: string) => string;
