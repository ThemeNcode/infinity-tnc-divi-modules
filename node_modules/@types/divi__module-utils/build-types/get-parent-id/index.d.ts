import { type ModuleFlatObjects } from '@divi/types';
/**
 * Get Parent ID.
 *
 * Get the parent id of a module.
 *
 * @since ??
 *
 * @param {object} moduleObjects Module flat objects.
 * @param {string} moduleId Module ID.
 *
 * @returns {string} Parent ID.
 *
 * @example
 * ```js
 * getParentId(moduleObjects, 'moduleId');
 * // => 'parentModuleId'
 * ```
 */
export declare const getParentId: (moduleObjects: ModuleFlatObjects, moduleId: string) => string;
