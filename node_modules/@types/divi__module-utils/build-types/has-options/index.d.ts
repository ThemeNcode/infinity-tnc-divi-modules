import { type Params } from './types';
/**
 * Has Options.
 *
 * Determine whether options should be rendered or not.
 *
 * @since ??
 *
 * @param {object} param Parameters for options.
 * @param {boolean} param.displayOptions Whether options should be displayed.
 * @param {boolean} param.displayOptionsOnChildClick Whether options should be displayed on child click.
 * @param {boolean} param.displayOptionsOnChildHover Whether options should be displayed on child hover.
 * @param {boolean} param.displayOptionsOnHover Whether options should be displayed on hover.
 * @param {boolean} param.isChildHovered Whether the child is currently hovered.
 * @param {boolean} param.isChildSelected Whether the child is currently selected.
 * @param {boolean} param.isColumn Whether the module is a column.
 * @param {boolean} param.isHovered Whether the module is currently hovered.
 * @param {boolean} param.isRowEmpty Whether the row is currently empty.
 * @param {boolean} param.isSelected Whether the module is currently selected.
 * @param {boolean} param.isRow Whether the module is a row or not.
 * @param {boolean} param.isSection Whether the module is a section or not.
 *
 * @returns {boolean} Whether options should be rendered or not.
 *
 * @example
 * ```js
 * hasOptions({
 *  isChildHovered: true,
 * });
 * // => false
 *
 * ```
 *
 * @example
 * ```js
 * hasOptions({
 *  isRowEmpty: true,
 * });
 * // => true
 */
export declare const hasOptions: ({ displayOptions, displayOptionsOnChildClick, displayOptionsOnChildHover, displayOptionsOnHover, isChildHovered, isChildSelected, isColumn, isHovered, isRowEmpty, isSelected, isRow, isSection, }: Params) => boolean;
