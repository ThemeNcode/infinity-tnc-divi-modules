import { type AttrState, type Breakpoint } from '@divi/types';
/**
 * Get inherit state for a given breakpoint and state.
 *
 * @since ??
 *
 * @param {Breakpoint} breakpoint Query breakpoint.
 * @param {AttrState} state Query state.
 *
 * @returns {AttrState} The inherit state.
 */
export declare const getInheritState: (breakpoint: Breakpoint, state: AttrState) => AttrState;
