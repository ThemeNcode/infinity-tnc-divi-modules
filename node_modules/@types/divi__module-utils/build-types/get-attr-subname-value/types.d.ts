import { type GetAttrValueParams } from '../get-attr-value/types';
export interface GetAttrSubnameValueParams<TAttrValue, TSubName extends keyof TAttrValue> extends Omit<GetAttrValueParams<TAttrValue>, 'defaultValue'> {
    subname?: TSubName;
    defaultValue?: TAttrValue[TSubName];
}
