import { type InheritAttrValueParams } from './types';
/**
 * Inherit Attr Value.
 *
 * Get inherited attribute value based on given `inheritMode`.
 *
 * @since ??
 *
 * @param {object} param Function parameters.
 * @param {object} param.attr Object containing attribute values for different breakpoints.
 * @param {string} param.breakpoint Current breakpoint.
 * @param {string} param.state Current state of the attribute.
 * @param {string} param.inheritMode Inherit mode for the attribute.
 *
 * @returns {string} Inherited attribute values.
 *
 * @example
 * ```js
 * const attr = {
 *  desktop: {
 *    value: 'desktop value',
 *    hover: 'desktop hover value',
 *    sticky: 'desktop sticky value',
 *  },
 *  tablet: {
 *    value: 'tablet value',
 *  },
 * };
 *
 * inheritAttrValue({
 *  attr,
 *  breakpoint: 'desktop',
 *  state: 'value',
 * });
 * // => 'desktop value'
 *
 * inheritAttrValue({
 *  attr,
 *  breakpoint: 'tablet',
 *  state: 'hover',
 * });
 * // => 'tablet value'
 *
 * inheritAttrValue({
 *  attr,
 *  breakpoint: 'phone',
 *  state: 'value',
 * });
 * // => 'tablet value'
 * ```
 */
export declare const inheritAttrValue: <TAttrValue>({ attr, breakpoint, state, inheritMode, }: InheritAttrValueParams<TAttrValue>) => TAttrValue;
