import { type AttrState, type Breakpoint, type FormatBreakpointStateAttr } from '@divi/types';
export interface InheritAttrValueParams<TAttrValue> {
    attr: FormatBreakpointStateAttr<TAttrValue>;
    breakpoint: Breakpoint;
    state: AttrState;
    inheritMode?: 'all' | // If the attr value is object, combine all possible inherited value.
    'closest';
}
