import { type ModuleDefinitionAttributeType } from '@divi/types';
/**
 * Get attribute value type.
 *
 * This data will be used to determine the attributes definition on the fly based on the attribute value.
 *
 * @see https://developer.wordpress.org/block-editor/reference-guides/block-api/block-attributes/#type-validation
 *
 * @since ??
 *
 * @param {unknown} value Value to check.
 *
 * @returns {string} Attribute value type.
 *
 * @example
 * ```js
 * getAttributeValueType( 'foo' );
 * // => 'string'
 * ```
 * @example
 * ```js
 * getAttributeValueType( 1 );
 * // => 'number'
 * ```s
 */
export declare const getAttributeValueType: (value: unknown) => ModuleDefinitionAttributeType;
