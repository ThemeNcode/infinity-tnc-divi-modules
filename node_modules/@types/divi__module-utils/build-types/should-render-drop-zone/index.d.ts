import { type DropZonePosition } from '@divi/types';
/**
 * Should Render Drop Zone.
 *
 * Check if dragged modules should trigger drop target rendering on given module id on position.
 *
 * @since ??
 *
 * @param {string} id Module id.
 * @param {string} position Drop target position.
 *
 * @returns {boolean} True if drop target should be rendered, false otherwise.
 *
 * @example
 * ```js
 * shouldRenderDropZone('module-id', 'inside');
 * ```
 */
export declare const shouldRenderDropZone: (id: string, position: DropZonePosition) => boolean;
