import { type Params } from './types';
/**
 * Has Highlight.
 *
 * Determine whether highlight should be rendered or not based on some boolean parameters.
 *
 * @since ??
 *
 * @param {object} param Boolean parameters.
 * @param {boolean} param.displayHighlight Whether to display highlight or not.
 * @param {boolean} param.displayHighlightOnChildHover Whether to display highlight on child hover or not.
 * @param {boolean} param.displayHighlightOnHover Whether to display highlight on hover or not.
 * @param {boolean} param.isChildHovered Whether the child is hovered or not.
 * @param {boolean} param.isColumn Whether the module is a column or not.
 * @param {boolean} param.isHovered Whether the module is hovered or not.
 * @param {boolean} param.isRowEmpty Whether the row is empty or not.
 * @param {boolean} param.isSelected Whether the module is selected or not.
 * @param {boolean} param.isChildModule Whether the module is a child module or not.
 * @param {boolean} param.isRow Whether the module is a row or not.
 * @param {boolean} param.isSection Whether the module is a section or not.
 *
 * @returns {boolean} Whether highlight should be rendered or not.
 *
 * @example
 * ```js
 * hasHighlight({
 *  isChildHovered: true,
 * });
 * // => false
 * ```
 *
 * @example
 * ```js
 * hasHighlight({
 *  isSelected: true,
 * });
 * // => true
 * ```
 */
export declare const hasHighlight: ({ displayHighlight, displayHighlightOnChildHover, displayHighlightOnHover, isChildHovered, isColumn, isHovered, isRowEmpty, isSelected, isChildModule, isRow, isSection, }: Params) => boolean;
