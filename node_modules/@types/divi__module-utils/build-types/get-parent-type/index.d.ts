import { type ModuleType } from '@divi/types';
/**
 * Get Parent Type.
 *
 * Get parent type of given module type.
 *
 * @since ??
 *
 * @param {string} moduleType Module type.
 *
 * @returns {string} Parent type of given module type.
 *
 * @example
 * ```js
 * getParentType('row');
 * // => 'section'
 * ```
 *
 * @example
 * ```js
 * getParentType('column');
 * // => 'row'
 * ```
 */
export declare const getParentType: (moduleType: ModuleType) => '' | ModuleType | ModuleType[];
