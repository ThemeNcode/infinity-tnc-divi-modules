import { type MergeAttrsParams } from './types';
/**
 * Merge Attrs.
 *
 * This function is used to merge attrs with default attrs.
 *
 * @since ??
 *
 * @param {object} param0 Merge attrs parameters.
 * @param {object} param0.defaultAttrs Default attrs.
 * @param {object} param0.presetAttrs Preset attrs.
 * @param {object} param0.attrs Attrs.
 *
 * @returns {object} Merged attrs.
 *
 * @example
 * ```js
 * mergeAttrs({
 *  defaultAttrs: {
 *   foo: 'foo',
 *   bar: 'bar',
 *  },
 *  attrs: {
 *   baz: 'baz',
 *   foo: 'foo2',
 *  },
 * });
 * // => { foo: 'foo2', bar: 'bar', baz: 'baz' }
 * ```
 */
export declare const mergeAttrs: <TParams extends MergeAttrsParams<TParams["attrs"]>>({ defaultAttrs, presetAttrs, attrs, }: TParams) => TParams["attrs"];
