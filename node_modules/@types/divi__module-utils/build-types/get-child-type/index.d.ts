import { type ModuleType } from '@divi/types';
/**
 * Get Child Type.
 *
 * Get child type of given module type.
 *
 * @since ??
 *
 * @param {string} moduleType Module type.
 *
 * @returns {string | string[]} Child type of given module type.
 *
 * @example
 * ```js
 * getChildType('row');
 * // => 'column'
 *
 * getChildType('column');
 * // => 'module'
 * ```
 */
export declare const getChildType: (moduleType: ModuleType) => '' | ModuleType | ModuleType[];
