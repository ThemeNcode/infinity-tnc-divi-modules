import { type AddModuleInlineFontParams } from './types';
/**
 * Adds a font to the list of inline fonts for a module.
 *
 * This function allows for adding an inline font to a module's inline fonts list.
 *
 * @since ??
 *
 * @param {object} params The parameters for the function.
 * @param {string} params.fontName The name of the inline font to add or remove.
 * @param {string} params.moduleId The id of the module to be updated.
 * @param {boolean} params.shouldLoadFont Whether to enqueue the newly added font. Default `true`.
 *
 * @returns {void}
 *
 * @example
 * ```ts
 * const moduleId = 'module-123';
 * const fontName = 'Arial';
 * addModuleInlineFont({ moduleId, fontName });
 * ```
 *
 * @example
 * ```ts
 * const moduleId = 'module-123';
 * const fontName = 'Arial';
 * const shouldLoadFont = false;
 * addModuleInlineFont({ moduleId, fontName, shouldLoadFont });
 * ```
 */
declare const addModuleInlineFont: (params: AddModuleInlineFontParams) => void;
export { addModuleInlineFont, };
