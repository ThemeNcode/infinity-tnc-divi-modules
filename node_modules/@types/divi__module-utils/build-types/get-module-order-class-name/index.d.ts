/**
 * Get module order class name defined in module.json config and add module id as suffix.
 *
 * The base of module order class is populated as follows:
 * - It will use the moduleOrderClassName property in module.json config if it is not falsy.
 * - It will use the moduleClassName property in module.json config if it is not falsy.
 * - It will convert module name to class name.
 *
 * @since ??
 *
 * @param {string} moduleId Module unique ID.
 *
 * @returns {string} Module order class name. Will return empty string if module is not found.
 */
export declare const getModuleOrderClassName: (moduleId: string) => string;
