/**
 * Can Edit Inline.
 *
 * Returns true if the module can be edited inline or false otherwise.
 *
 * @since ??
 *
 * @param {string} moduleId Module iD.
 *
 * @returns {boolean} True if the module can be edited inline, false otherwise.
 *
 * @example
 * ```js
 * const canEdit = canEditInline('module-123');
 * ```
 */
declare const canEditInline: (moduleId: string) => boolean;
export { canEditInline };
