import { type ModuleType } from '@divi/types';
/**
 * List of module types, and its rank; Usage example: to determine what is the highest structural
 * data exist on given module list (eg: ['row, 'column', 'module']. Answer: row). Order score
 * is given from low to high. The lowest means the closer it is to root.
 *
 * @since ??
 *
 * @type {object} Module type ranks.
 */
export declare const moduleTypeRanks: Record<ModuleType, number>;
