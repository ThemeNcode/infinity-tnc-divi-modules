import { type ModuleType } from '@divi/types';
/**
 * Extract Lowest Module Type.
 *
 * Get the lowest module type (root: lowest, module: higher) of given module types.
 *
 * @since ??
 *
 * @param {string[]} moduleTypes Array of module types.
 *
 * @returns {string} Lowest module type.
 *
 * @example
 * ```js
 * extractLowestModuleType(['root', 'module', 'module']);
 * // => 'root'
 * ```
 *
 * @example
 * ```js
 * extractLowestModuleType(['module', 'module', 'module']);
 * // => 'module'
 * ```
 *
 * @example
 * ```js
 * extractLowestModuleType(['section', 'row', 'module']);
 * // => 'section'
 * ```
 */
export declare const extractLowestModuleType: (moduleTypes: ModuleType[]) => ModuleType;
