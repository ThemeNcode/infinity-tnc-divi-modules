import { type AttrState, type Breakpoint, type FormatBreakpointStateAttr } from '@divi/types';
export type GetAttrMode = 'get' | 'getAndInheritAll' | 'getAndInheritClosest' | 'getOrInheritAll' | 'getOrInheritClosest' | 'inheritAll' | 'inheritClosest';
export interface GetAttrValueParams<TAttrValue> {
    attr: FormatBreakpointStateAttr<TAttrValue>;
    breakpoint: Breakpoint;
    state: AttrState;
    defaultValue?: TAttrValue;
    mode?: GetAttrMode;
}
