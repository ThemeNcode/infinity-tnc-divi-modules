import { type BasicModuleType, type ModuleType } from '@divi/types';
/**
 * Get Module Type Label.
 *
 * Get the label for a module type.
 *
 * @since ??
 *
 * @param {string} moduleType Module type.
 *
 * @returns {string} Module type label.
 *
 * @example
 * ```js
 * getModuleTypeLabel('fullwidth-section');
 * // => 'Fullwidth Section'
 * ```
 *
 * @example
 * ```js
 * getModuleTypeLabel('module');
 * // => 'Module'
 * ```
 */
export declare const getModuleTypeLabel: (moduleType: BasicModuleType | ModuleType) => string;
