/// <reference types="jquery" />
/// <reference types="jquery" />
/**
 * Open Add Module Modal.
 *
 * Open `divi/add-module` modal relative to add module button position.
 *
 * @since ??
 *
 * @param {JQuery} $button Button jquery object.
 * @param {string} id Module id where add module button is clicked.
 * @param {boolean} addChild Whether add module button add inside or after referred module.
 *
 * @returns {void}
 *
 * @example
 * ```js
 * openAddModuleModal($button, moduleId, false); // Add module modal will be opened after the provided module.
 * ```
 */
export declare const openAddModuleModal: ($button: JQuery, id: string, addChild: boolean) => void;
