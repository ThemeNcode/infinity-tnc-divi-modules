import { type AttrState, type Breakpoint } from '@divi/types';
/**
 * Get inherit breakpoint for a given breakpoint and state.
 *
 * @since ??
 *
 * @param {Breakpoint} breakpoint Query breakpoint.
 * @param {AttrState} state Query state.
 *
 * @returns {Breakpoint} The inherit breakpoint.
 */
export declare const getInheritBreakpoint: (breakpoint: Breakpoint, state: AttrState) => Breakpoint;
