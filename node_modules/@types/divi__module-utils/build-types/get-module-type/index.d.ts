import { type BasicModuleType, type ModuleType } from '@divi/types';
type GetModuleTypeValue<TBasic> = TBasic extends true ? BasicModuleType : ModuleType;
/**
 * Get Module Type.
 *
 * Get module type based on module name and attributes.
 *
 * @since ??
 *
 * @param {string} moduleName Module name.
 * @param {object} attrs Module attributes.
 * @param {boolean} basic Should returned value be basic type or not (eg. Section instead of fullwidth section).
 *
 * @returns {string} Module type.
 *
 * @example
 * ```js
 * getModuleType('divi/text', {});
 * // => 'module'
 * ```
 *
 * @example
 * ```js
 * getModuleType('divi/row', {});
 * // => 'row'
 * ```
 */
export declare const getModuleType: <TBasic extends boolean, TAttrs>(moduleName: string, attrs: TAttrs, basic?: TBasic) => GetModuleTypeValue<TBasic>;
export {};
