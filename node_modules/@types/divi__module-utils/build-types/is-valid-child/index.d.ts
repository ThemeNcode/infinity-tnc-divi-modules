import { type ModuleType } from '@divi/types';
/**
 * Is Valid Child.
 *
 * Check if given module type is valid child of given child module type.
 *
 * @since ??
 *
 * @param {string} parentModuleType Parent module type.
 * @param {string} childModuleType Child module type.
 *
 * @returns {boolean} True if given module type is valid child of given child module type.
 *
 * @example
 * ```js
 * isValidChild('module', 'child-module'); // true
 * isValidChild('module', 'module'); // false
 * ```
 */
export declare const isValidChild: (parentModuleType: ModuleType, childModuleType: ModuleType) => boolean;
