import { type AttrState, type Breakpoint } from '@divi/types';
import { type GetAttrMode } from '../get-attr-value/types';
export interface GetAttrByModeOptions {
    breakpoint?: Breakpoint;
    state?: AttrState;
    inheritMode?: GetAttrMode;
}
