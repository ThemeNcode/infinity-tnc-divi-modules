import { type FormatBreakpointStateAttr } from '@divi/types';
import { type GetAttrByModeOptions } from './types';
/**
 * Get Attr By Mode.
 *
 * Retrieves the value of a given attribute based on the current breakpoint, attribute
 * state, and inheritance mode.
 *
 * This function utilizes the useSelect hook and is intended for use inside React components only.
 *
 * @since ??
 *
 * @param {object} attr The attribute to retrieve the value for.
 *
 * @param {object}   [options] An optional object to set the breakpoint, state, and inheritance mode.
 *
 * @param {string} [options.breakpoint] The breakpoint parameter is an optional argument that specifies
 * the screen size at which the attribute value should be retrieved. If it is not provided, the current
 * breakpoint of the app UI is used.
 *
 * @param {string} [options.state] The state parameter is an optional argument that specifies the state of
 * the attribute. It can be used to retrieve the value of an attribute in a specific state, such as
 * hover or active. If no state is specified, the current state of the attribute will be used.
 *
 * @param {string} [options.inheritMode] The inheritMode parameter is an optional argument that specifies
 * how the attribute value should be inherited from larger breakpoints. Default is 'getAndInheritAll'.
 *
 * @returns {object} The value of the attribute.
 *
 * @example
 * ```js
 * const attr = {
 *  desktop: {
 *   value:  'desktop',
 *   hover:  'desktop-hover',
 *   sticky: 'desktop-sticky',
 *  },
 *  tablet: {
 *   value: 'tablet',
 *  },
 *  phone: {
 *   value: 'phone',
 *  },
 * };
 *
 * getAttrByMode(attr, {
 *  breakpoint: 'desktop',
 *  state: 'value',
 * });
 * // desktop
 *
 * getAttrByMode(attr, {
 *  breakpoint: 'desktop',
 *  state: 'hover',
 * });
 * // desktop-hover
 * ```
 */
export declare const getAttrByMode: <TAttrValue>(attr: Partial<Record<import("@divi/types").Breakpoint, Partial<Record<import("@divi/types").AttrState, TAttrValue>>>>, options?: GetAttrByModeOptions) => TAttrValue;
