import { type AttrState, type Breakpoint } from '@divi/types';
/**
 * Breakpoint and state inheritance assignments.
 *
 * Top level keys are of type "Breakpoint" and second level keys are of type "AttrState". The values of the
 * second level keys are arrays of length 2, where both elements are strings.
 *
 * @since ??
 *
 * @returns {Record<Breakpoint, Record<AttrState, [Breakpoint, AttrState]>>}
 */
export declare const inheritBreakpoints: Record<Breakpoint, Record<AttrState, [Breakpoint, AttrState]>>;
