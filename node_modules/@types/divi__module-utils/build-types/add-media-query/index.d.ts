/**
 * Add Media Query.
 *
 * Wrap given style with proper media query based on given breakpoint.
 *
 * @since ??
 *
 * @todo (D5i) to be removed once no longer used.
 *
 * @param {string} breakpoint Desktop|tablet|phone.
 * @param {string} style CSS Style.
 *
 * @returns {string} CSS Style wrapped with proper media query.
 *
 * @example
 * ```js
 * addMediaQuery('desktop_above', 'p { color: red; }');
 * // => \@media only screen and ( min-width: 981px ) {p { color: red; }}
 * ```
 *
 * @example
 * ```js
 * addMediaQuery('tablet', 'p { color: red; }');
 * // => \@media only screen and ( max-width: 980px ) {p { color: red; }}
 * ```
 */
export declare const addMediaQuery: (breakpoint: string, style: string) => string;
