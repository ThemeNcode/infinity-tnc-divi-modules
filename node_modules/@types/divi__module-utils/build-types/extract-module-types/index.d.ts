import { type ModuleType } from '@divi/types';
/**
 * Extract Module Types.
 *
 * Extract array of module types based on given`ModuleFlatObjects`.
 *
 * @since ??
 *
 * @param {object} modules Module flat objects.
 * @param {object} moduleTypeMap Map intended module type into other module type.
 *
 * @returns {string[]} Array of module types.
 *
 * @example
 * ```js
 * const modules = {
 *  module1: {
 *    id:       'module1',
 *    name:     'divi/text',
 *    parent:   'column1',
 *    props:    {},
 *    children: [],
 *  }
 * };
 * extractModuleTypes(modules);
 * // => ['module']
 * ```
 *
 * @example
 * ```js
 * const modules = {
 *  module1: {
 *    id:       'module1',
 *    name:     'divi/text',
 *    parent:   'column1',
 *    props:    {},
 *    children: [],
 *  }
 * };
 * const moduleTypeMap = {
 *  module: 'text',
 * };
 * extractModuleTypes(modules, moduleTypeMap);
 * // => ['text']
 * ```
 */
export declare const extractModuleTypes: <TModule>(modules: Record<string, TModule>, moduleTypeMap?: Record<string, ModuleType>) => ModuleType[];
