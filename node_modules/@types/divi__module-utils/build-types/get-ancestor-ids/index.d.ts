import { type ModuleFlatObjects } from '@divi/types';
/**
 * Get Ancestor IDs.
 *
 * Get ids of ancestor (parent, grand parent, all the way up to `root`) of given module Id.
 *
 * @since ??
 *
 * @param {object} moduleObjects Module objects.
 * @param {string} moduleId Module id.
 *
 * @returns {string[]} Array of ancestor ids.
 *
 * @example
 * ```js
 * const moduleObjects = {
 *  ...
 * };
 * const moduleId = 'module-id';
 *
 * getAncestorIds(moduleObjects, moduleId);
 *
 * // => ['parent-id', 'grand-parent-id']
 * ```
 */
export declare const getAncestorIds: (moduleObjects: ModuleFlatObjects, moduleId: string) => string[];
