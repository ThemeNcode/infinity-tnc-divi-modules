import { type SetInlineEditorClick } from './types';
/**
 * Sets the last click event in the inline editor.
 *
 * This function is responsible for determining if the last click event occurred
 * inside the inline editor or outside it. If the click event occurred inside
 * the inline editor, it sets the property to `true` in the store. Otherwise, it
 * sets the property to `false`.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function
 *
 * @example
 *
 * ```ts
 * window.divi.data.dispatch('divi/module').setInlineEditorLastClick(event);
 * ```
 */
export declare const setInlineEditorClick: (event: MouseEvent) => SetInlineEditorClick;
