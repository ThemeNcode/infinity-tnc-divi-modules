import { type Module } from '@divi/types';
/**
 * Sets script item.
 *
 * This action sets a script item for the specified script name and ID.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/module').setScriptItem({
 *   id:   'uuid-1',
 *   item: {
 *     id:                'uuid-1',
 *     selector:          '.et_pb_text_uuid-1',
 *     position:          'top',
 *     topOffset:         '0px',
 *     bottomOffset:      '0px',
 *     topLimit:          'none',
 *     bottomLimit:       'none',
 *     offsetSurrounding: 'on',
 *     transition:        'on',
 *     styles:            {
 *       module_alignment: {
 *         desktop: '',
 *         tablet:  '',
 *         phone:   '',
 *       },
 *     },
 *   },
 *   scriptName: 'sticky',
 * });
 * ```
 */
export declare const setScriptItem: ({ id, item, scriptName, }: Module.Store.Actions.SetScriptItem.Params) => Module.Store.Actions.SetScriptItem.ReturnValue;
