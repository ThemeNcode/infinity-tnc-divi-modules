import { type Module } from '@divi/types';
/**
 * Gets scripts.
 *
 * Retrieves the scripts from the module store state.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * const scripts = window.divi.data.select('divi/module').getScripts();
 * ```
 */
export declare const getScripts: (state: Module.Store.ImmutableState) => Module.Store.ImmutableState['scripts'];
