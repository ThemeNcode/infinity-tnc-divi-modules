import { type Module } from '@divi/types';
/**
 * Gets a script item.
 *
 * This action retrieves a script item from the store.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * const scriptItem = window.divi.data.select('module').getScriptItem(
 *   'sticky',
 *   'module-123'
 * );
 * ```
 */
export declare const getScriptItem: (state: Module.Store.ImmutableState, scriptName: Module.Store.ScriptName, id: string) => Module.Store.ImmutableState['scripts'][Module.Store.ScriptName][keyof Module.Store.ImmutableState['scripts'][Module.Store.ScriptName]];
