import { type Module } from '@divi/types';
/**
 * Retrieves the ID of the element that was clicked to open the inline editor.
 *
 * This is used to determine which element to focus on when the inline editor
 * is open. If the value is anything other than an empty string, the inline
 * editor will focus on the element with the matching ID, and standard right
 * click behavior will be prevented.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example
 *
 * ```ts
 * window.divi.data.select('divi/edit-post').getInlineEditorClick();
 * ```
 */
export declare const getInlineEditorClick: (state: Module.Store.ImmutableState) => boolean;
