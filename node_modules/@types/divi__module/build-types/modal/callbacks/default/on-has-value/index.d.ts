import { type Module } from '@divi/types';
/**
 * Default mechanism to check if field is considered having a value or not.
 * Returning `true` will display reset button.
 *
 * @since ??
 *
 * @param {Module.Settings.Field.OnHasValueParams} value Callback params.
 *
 * @returns {boolean}
 *
 */
export declare const onHasValue: ({ value, defaultValue, }: Module.Settings.Field.OnHasValueParams) => boolean;
