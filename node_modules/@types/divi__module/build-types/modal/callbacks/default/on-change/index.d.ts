import { type Module } from '@divi/types';
/**
 * Default input `onChange` event callback. Push change to edit-post store based on input's event.
 *
 * @since ??
 *
 * @param {Module.Settings.Field.OnChangeParams} params Event callback parameters.
 *
 * @returns {void}
 */
export declare const onChange: (params: Module.Settings.Field.OnChangeParams) => void;
