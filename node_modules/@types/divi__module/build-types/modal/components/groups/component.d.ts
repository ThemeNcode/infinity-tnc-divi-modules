import { type ReactElement } from 'react';
import { type ModuleGroupsProps } from './types';
/**
 * Module Settings Groups.
 *
 * Render module settings' groups based on given props.
 *
 * @since ??
 *
 * @param {object} params Component props.
 * @param {object} params.groups Object of groups properties.
 * @param {Function} params.dependencyCallback Function that will be executed when dependencyList changes.
 * @param {Array} params.dependencyList Array of dependencies that will be used to trigger dependencyCallback.
 *
 * @returns {ReactElement}
 */
export declare const ModuleGroups: ({ groups, dependencyCallback, dependencyList, }: ModuleGroupsProps) => ReactElement;
