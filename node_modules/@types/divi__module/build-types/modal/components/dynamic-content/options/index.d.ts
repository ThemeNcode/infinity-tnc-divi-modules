import { type DynamicContentComponentOptions } from './types';
/**
 * Filters the dynamic content options for a Divi Visual Builder module.
 *
 * This filter allows developers to modify the dynamic content options for a Divi Visual Builder module.
 * Dynamic content options are used to populate dynamic content fields in the module settings.
 * This allows use of custom components in dynamic content options.
 *
 * @since ??
 *
 * @param {Array<object>} options The current dynamic content options. Default `[]`.
 */
declare const dynamicContentOptionsOnVB: DynamicContentComponentOptions;
export { dynamicContentOptionsOnVB, };
