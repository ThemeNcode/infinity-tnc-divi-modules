import { type ReactElement } from 'react';
import { type DynamicContentButtonProps } from './types';
/**
 * Dynamic Content Button field.
 *
 * @since ??
 *
 * @param {DynamicContentButtonProps} props Props for dynamic content button field.
 *
 * @returns {ReactElement}
 */
export declare const DynamicContentButton: ({ onClick, }: DynamicContentButtonProps) => ReactElement;
