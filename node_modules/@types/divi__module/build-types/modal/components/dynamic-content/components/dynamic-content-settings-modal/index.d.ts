import { type ReactElement } from 'react';
import { type DynamicContentSettingsModalProps } from './types';
import './style.scss';
/**
 * Dynamic Content field component.
 *
 * @since ??
 *
 * @param {DynamicContentSettingsModalProps} props Props for dynamic content field.
 *
 * @returns {ReactElement}
 */
declare const DynamicContentSettingsModal: {
    ({ saveDynamicContent, editingDynamicContentName, setEditingDynamicContentName, popupModalValue, setPopupModalValue, isPopupModalOpen, setIsPopupModalOpen, }: DynamicContentSettingsModalProps): ReactElement;
    /**
     * Static `fieldName` is required to `<FieldContainer />` can recognize the field that is
     * used inside it without the need to pass prop into it. This is used for copy paste style.
     *
     * @since ??
     */
    fieldName: string;
};
declare const name = "divi/dynamic-content-modal";
export { name, DynamicContentSettingsModal, };
