import { type ReactElement } from 'react';
import { type DynamicContentWrapperProps } from './types';
import './style.scss';
/**
 * Dynamic Content Wrapper.
 *
 * This component is used to add dynamic content capability to a component that has it enabled.
 *
 * @since ??
 *
 * @param {DynamicContentWrapperProps} props Props for dynamic content wrapper.
 *
 * @returns {ReactElement}
 */
export declare const DynamicContentWrapper: (props: DynamicContentWrapperProps) => ReactElement;
