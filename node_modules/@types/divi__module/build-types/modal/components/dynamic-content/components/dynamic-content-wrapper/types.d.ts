import { type ReactElement } from 'react';
export type DynamicContentFieldProps = {
    component: {
        name: string;
    };
    renderBefore: () => void;
    renderAfter: () => void;
    renderPreview?: boolean | {
        showPlaceholderOnEmpty?: boolean;
        defaultValue?: string;
    };
};
export type DynamicContentWrapperProps = {
    children: ReactElement<DynamicContentFieldProps>[];
    settings?: false | {
        type: 'text' | 'url' | 'image';
    };
    onChange: () => void;
    value: string | null;
    defaultValue: string;
};
