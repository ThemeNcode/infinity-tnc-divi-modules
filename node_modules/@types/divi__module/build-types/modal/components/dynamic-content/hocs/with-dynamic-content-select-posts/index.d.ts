import { type WithDynamicContentSelectPosts } from './types';
/**
 * A higher order component for wrapping `<Select/>` component, filling its options with a list of posts.
 *
 * The posts shown on the list will be based on the passed `postType` to the HOC.
 *
 * Post type should be registered on WP with REST API availability.
 *
 * @param {WithDynamicContentSelectPostsHocProps} hocProps An object consisted of:
 * - name: A string, passed as `name` to select component.
 * - postType: A string, used as post type to get its posts.
 * - component: A ReactElement, representing the select component that will be rendered as
 * dynamic content option's setting.
 *
 * @returns {Function} - The component wrapped by `withDynamicContentSelectPosts` HOC.
 */
declare const withDynamicContentSelectPosts: WithDynamicContentSelectPosts;
export { withDynamicContentSelectPosts, };
