import { type WithDynamicContentFieldManagement } from './types';
/**
 * Higher order component to manage dynamic content's dynamically created fields.
 *
 * It takes a list of field components and renders them with a controlled state that can be
 * manipulated externally using the 'onChange' function from the incoming props.
 *
 * In Divi you can define dynamic content option's fields both on React and PHP, if fields are defined
 * on the PHP side, this HOC will be used to create the fields on the fly in a composable manner.
 *
 * @param {Array} fieldDetails An array of objects, with each object representing a
 * field component. Each object is consisted of:
 * - name: A string, the name of the field.
 * - default: A string, default value of the field.
 * - component: A ReactElement, representing the component that will be rendered as settings.
 * - label: A string, representing label of the field.
 * - options: An object, used only when the field is a 'divi/select'.
 * - showIf: An object, used to conditionally render the field based on the value of a sibling field.
 *
 * @returns {Function} - The component wrapped by `withDynamicContentFieldManagement` HOC.
 */
declare const withDynamicContentFieldManagement: WithDynamicContentFieldManagement;
export { withDynamicContentFieldManagement };
