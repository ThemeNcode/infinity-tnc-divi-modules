import { type ComponentType, type ReactElement } from 'react';
import { type FieldLibrary } from '@divi/types';
export type WithDynamicContentSelectPosts = (hocProps: WithDynamicContentSelectPostsHocProps) => (componentProps: WithDynamicContentSelectPostsComponentProps) => ReactElement;
export type WithDynamicContentSelectPostsHocProps = {
    name: string;
    postType: string;
    component: ComponentType<FieldLibrary.Select.Props>;
};
export type WithDynamicContentSelectPostsComponentProps = {
    onChange: (value: string) => void;
    value: string;
};
export type WithDynamicContentSelectPostsRegisteredPostTypes = Record<string, {
    description: string;
    hierarchical: boolean;
    has_archive: boolean;
    name: string;
    slug: string;
    icon: string;
    taxonomies: string[];
    rest_base: string;
    rest_namespace: string;
}>;
export type WithDynamicContentSelectPostsFetchedPosts = {
    id: number;
    title: {
        rendered: string;
    };
    slug: string;
}[];
