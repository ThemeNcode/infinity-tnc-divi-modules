/**
 * Converts a string in the format of `$variable(JSON)$` to an object.
 *
 * @since ??
 *
 * @example
 * // Returns { foo: 'bar' }
 * convertDynamicDataFormatToObject('$variable({"foo":"bar"})$');
 *
 * @param {string} variable The dynamic data format `$variable(JSON)$` to convert.
 *
 * @returns {object|null} Returns an object if the string is in the correct format, otherwise null.
 */
declare const convertDynamicDataFormatToObject: <TReturnValue>(variable: string) => TReturnValue;
export { convertDynamicDataFormatToObject, };
