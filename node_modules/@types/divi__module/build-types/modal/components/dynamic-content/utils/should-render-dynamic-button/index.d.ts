import { type ShouldRenderDynamicButtonParams } from './types';
/**
 * Check whether add dynamic content button should be rendered or not.
 *
 * @since ??
 */
export declare const shouldRenderDynamicButton: ({ isDynamicContentEnable, hasDynamicContent, fieldComponentName, fieldValue, }: ShouldRenderDynamicButtonParams) => boolean;
