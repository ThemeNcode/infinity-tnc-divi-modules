import { type UseGetDynamicContentOptionsReturnType } from './types';
/**
 * A React Hook that fetches dynamic content options from the back-end.
 * It also processes these fetched options into a dynamic content list and fields.
 *
 * @returns {UseGetDynamicContentOptionsReturnType} - An object with the following properties:
 * - dynamicContentOptions: The fetched dynamic content options.
 * - dynamicContentList: A list of dynamic content names that can be used as value in <Select/> component.
 * - dynamicContentFields: An object consisted of each option with its assigned fields.
 */
declare const useGetDynamicContentOptions: () => UseGetDynamicContentOptionsReturnType;
export { useGetDynamicContentOptions, };
