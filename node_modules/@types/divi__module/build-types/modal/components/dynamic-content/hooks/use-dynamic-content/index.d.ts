import { type UseDynamicContent } from './types';
/**
 * A React Hook for handling dynamic content. It provides dynamic content state, props for dynamic content button and
 * modal and methods that get triggered on edit and delete actions.
 *
 * @param {UseDynamicContentProps} props The parameters for the hook.
 * - initialValue: The initial value for the dynamic content to be used in the component.
 * - onChange: A callback function which will be called when the dynamic content changes.
 *
 * @returns {UseDynamicContentReturn} - An object with the following properties:
 * - dynamicContent: The current state of the dynamic content.
 * - dynamicContentSettingsModalProps: Generated props for dynamic content modal.
 * - dynamicContentButtonProps: Generated props for dynamic content button.
 * - dynamicContentOnEdit: A method to open popup modal and edit the dynamic content.
 * - dynamicContentOnDelete: A method to delete the dynamic content.
 */
declare const useDynamicContent: UseDynamicContent;
export { useDynamicContent, };
