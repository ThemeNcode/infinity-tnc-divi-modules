import { type ReactElement } from 'react';
import { type GroupTabsWrapperProps } from './types';
/**
 * Group Tabs Wrapper.
 *
 * Wrapper component for tabbed module field groups (eg. Font's body, link, quote, etc) which
 * automatically renders the tabs component.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.id Group ID.
 * @param {string} props.defaultTab Default tab name.
 * @param {Function} props.openGroupTab Function to open group tab.
 * @param {Function} props.setRightClickOptions Function to set right click options.
 * @param {object} props.lastClipboardItem Last clipboard item.
 * @param {object} props.modalSettings Modal settings.
 * @param {string} props.moduleName Module name.
 * @param {ReactNode} props.children Children components.
 *
 * @returns {ReactElement}
 */
declare const GroupTabsWrapper: {
    (props: GroupTabsWrapperProps): ReactElement;
    defaultProps: {
        defaultTab: string;
    };
};
export { GroupTabsWrapper, };
