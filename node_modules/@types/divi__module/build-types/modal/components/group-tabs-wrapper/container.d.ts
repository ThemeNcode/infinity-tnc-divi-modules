import { type ReactElement } from 'react';
import { type GroupTabsWrapperContainerProps } from './types';
/**
 * Group Tabs Wrapper Container.
 *
 * Container for <GroupTabsWrapper /> component which automatically adds state change
 * from required stores.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {ReactElement} props.children Children to render.
 * @param {string} props.id Module ID.
 * @param {string} props.defaultTab Optional default tab to open.
 *
 * @returns {ReactElement}
 */
export declare const GroupTabsWrapperContainer: (props: GroupTabsWrapperContainerProps) => ReactElement;
