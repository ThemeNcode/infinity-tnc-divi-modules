import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
/**
 * Module Field.
 *
 * Field component that is specifically made for module's settings modal. It automatically wraps
 * the field from fields registry with appropriate field wrapper component, provide onChange
 * callback, and other heavy lifting so module's settings declaration can be as lean as possible.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.id Optional. ID for the field. Defaults to empty string.
 * @param {string} props.attrName Attribute name for the field.
 * @param {string} props.subName Optional. Attribute subname for the field.
 * @param {string} props.label Optional. Label for the field.
 * @param {string} props.description Optional. Description for the field.
 * @param {string|Function} props.onChange Optional. Callback function to be triggered when field value is changed.
 * If function is provided, the return value should be a string.
 * @param {string|Function} props.onHasValue Optional. Callback function to be triggered
 * when field is considered having a value. If function is provided, the return value should be a string.
 * @param {string|Function} props.onReset Optional. Callback function to be triggered when field value is reset.
 * @param {boolean} props.responsive Optional. Whether the field is responsive or not. Defaults to `true`.
 * @param {boolean} props.sticky Optional. Whether the field is sticky or not. Defaults to `true`.
 * @param {boolean} props.hover Optional. Whether the field is hover or not. Defaults to `true`.
 * @param {string|Function} props.placeholder Optional. Placeholder for the field.
 * If function is provided, the return value should be a string.
 * @param {string} props.className Optional. Class name for the field.
 * @param {boolean|Function} props.visible Optional. Whether the field is visible or not. Defaults to `true`.
 * If function is provided, the return value should be a boolean.
 * @param {Function} props.getValue Optional. Custom function to retrieve field value.
 * @param {object} props.attrs Optional. Module attributes.
 * @param {object} props.attr Optional. Module attribute.
 * @param {string} props.groupName Optional. Module field group name.
 * @param {string} props.appBreakpoint Optional. Module app breakpoint.
 * @param {string} props.appState Optional. Module app state.
 * @param {boolean} props.enable Optional. Whether the field is enabled or not. Defaults to `true`.
 * @param {string} props.disabledDescription Optional. Disabled description for the field.
 * @param {object} props.defaultAttr Optional. Default attribute for the field.
 * @param {object} props.lastClipboardItem Optional. Last clipboard item.
 * @param {Function} props.setRightClickOptions Optional. Function to set right click options.
 * @param {boolean} props.hasSync Optional. Whether the field has sync or not. Defaults to `false`.
 * @param {boolean} props.isSynced Optional. Whether the field is synced or not. Defaults to `true`.
 * @param {Function} props.onUnsyncGlobalModuleAttribute Optional. Callback function to be triggered when
 * global module attribute is unsynced.
 * @param {object} props.features Optional. Module features.
 *
 * @returns {ReactElement}
 */
declare const Field: {
    (props: Module.Settings.Field.Props): ReactElement;
    defaultProps: {
        id: string;
        placeholder: string;
        className: string;
        visible: boolean;
        features: {
            responsive: boolean;
            sticky: boolean;
            hover: boolean;
        };
        onChange: string;
        onHasValue: string;
        onReset: string;
        enable: boolean;
    };
};
export { Field, };
