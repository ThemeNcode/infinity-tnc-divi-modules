import { type ReactElement } from 'react';
import { type DraggableChildModuleListContainerProps } from './types';
/**
 * Draggable Child Module List.
 *
 * Wrapper component for the `DraggableChildModuleList` component that passes necessary state.
 *
 * @since ??
 *
 * @param {object} props  Component props.
 * @param {Array<object>} props.value List of items.
 * @param {ReactNode} props.children Child components.
 * @param {string} props.childModuleName Child module name.
 * @param {number} props.maxItems Optional. Maximum number of items allowed.
 * @param {number} props.minItems Optional. Minimum number of items allowed.
 * @param {Function} props.onClickSettings Optional. Callback function when the settings button is clicked.
 * @param {Function} props.onChange Optional. Callback function when the value is changed.
 * @param {Function} props.onAddNew Optional. Callback function when the add new button is clicked.
 * @param {Function} props.onClickMore Optional. Callback function when the more button is clicked.
 * @param {Function} props.onClickDelete Optional. Callback function when the delete button is clicked.
 * @param {Function} props.onClickDuplicate Optional. Callback function when the duplicate button is clicked.
 *
 * @returns {ReactElement}
 */
export declare const DraggableChildModuleListContainer: (props: DraggableChildModuleListContainerProps) => ReactElement;
