import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
import { name } from './constants';
/**
 * Scroll Settings Group component.
 *
 * Module field group which includes `scroll` and `sticky` settings.
 *
 * @since ??
 *
 * @param {object} [props] Component props.
 * @param {string} [props.settingsGroupId='moduleScrollSettings'] Settings group ID.
 * @param {object} [props.fields={}] Module fields.
 * @param {boolean} [props.grouped=true] Whether to group the fields or not.
 * @param {string} [props.groupLabel='Scroll Effects'] Group label.
 *
 * @returns {ReactElement} Scroll Settings Group component.
 *
 * @example Basic usage.
 * ```tsx
 * <ScrollSettingsGroup />
 * ```
 *
 * @example **ScrollSettingsGroup** set to hide the **Sticky Position** field.
 * ```tsx
 * <ScrollSettingsGroup
 *   fields={{
 *     stickyGroup: {
 *       render: false,
 *     },
 *   }}
 * />
 * ```
 *
 * @example **ScrollSettingsGroup** set to show both **Scroll** and **Sticky** fields.
 * ```tsx
 * <ScrollSettingsGroup
 *   grouped={false}
 *   fields={{
 *     scrollGroup: {
 *       render: true,
 *     },
 *     stickyGroup: {
 *       render: true,
 *     },
 *   }}
 * />
 * ```
 */
declare const ScrollSettingsGroup: {
    ({ settingsGroupId, grouped, groupLabel, fields, }: Module.Settings.Group.Library.ScrollSettings.GroupProps): ReactElement;
    /**
     * Static `groupName` is required so `<GroupContainer />` can recognize nested module field group
     * component that is used inside it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    groupName: string;
};
export { name, ScrollSettingsGroup, };
