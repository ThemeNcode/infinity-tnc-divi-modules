import { type FC } from 'react';
import { type Module } from '@divi/types';
/**
 * Map of module option group components.
 *
 * @since ??
 */
export declare const optionsGroupComponentMap: {
    [TName in keyof Module.Settings.Group.Library.ComponentMap]?: {
        name: TName;
        component: FC<Module.Settings.Group.Library.ComponentMap[TName]['props']>;
        attributeType: string;
    };
};
