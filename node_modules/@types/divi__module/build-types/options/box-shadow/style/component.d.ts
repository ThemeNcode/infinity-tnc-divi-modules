import { type ReactElement } from 'react';
import { type BoxShadowStyleProps } from './types';
declare const name = "divi/box-shadow";
/**
 * Box Shadow Style.
 *
 * Get Box Shadow's `style` component CSS declaration.
 *
 * @since ??
 *
 * @param {object} params Component Params.
 * @param {string} params.selector Optional. Selector for the style. Defaults to empty string.
 * @param {object} params.selectors Optional. Selectors for the style. Defaults to `{}`.
 * @param {Function} params.selectorFunction Optional. Selector function for the style. Defaults to `undefined`.
 * @param {object} params.propertySelectors Optional. Property selectors for the style. Defaults to `{}`.
 * @param {string} params.attr Optional. Attribute for the style.
 * @param {object} params.defaultPrintedStyleAttr Optional. Default printed style attribute.
 * @param {boolean} params.important Optional. Whether to use `!important` or not. Defaults to `false`.
 * @param {boolean} params.asStyle Optional. Whether to return the style as a style or as a string. Defaults to `true`.
 * @param {boolean} params.useOverlay Optional. Whether to use overlay or not. Defaults to `false`.
 * @param {string} params.orderClass Optional. The selector class name.
 *
 * @returns {ReactElement}
 */
declare const BoxShadowStyle: ({ selector, selectors, selectorFunction, propertySelectors, attr, defaultPrintedStyleAttr, important, asStyle, useOverlay, orderClass, }: BoxShadowStyleProps) => ReactElement;
export { name, BoxShadowStyle, };
