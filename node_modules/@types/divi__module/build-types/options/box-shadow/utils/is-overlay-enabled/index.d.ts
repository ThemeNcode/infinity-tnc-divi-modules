import { type Module } from '@divi/types';
/**
 * Is Overlay Enabled.
 *
 * Checks if the overlay is enabled in module.
 * The box shadow overlay is enabled only when the `style` exists and the `position` is inner.
 *
 * @since ??
 *
 * @param {object} attr Box shadow group attrs.
 *
 * @returns {boolean}
 */
export declare const isOverlayEnabled: (attr: Module.Element.Decoration.BoxShadow.Attributes) => boolean;
