import { type SelectorWithOverlayParams } from './types';
/**
 * Get Selector With Overlay.
 *
 * Add suffix ` > .box-shadow-overlay` to the given selector when box shadow overlay is enabled.
 *
 * @since ??
 *
 * @param {object} params Function parameters.
 * @param {object} params.attr Box shadow attributes.
 * @param {string} params.selector Selector.
 * @param {string} params.breakpoint Optional screen breakpoint.
 * One of `desktop`, `tablet` or `phone`. Defaults to `null`.
 * @param {string} params.state Optional attribute state. One of `value`, `hover`, `sticky`. Defaults to `null`.
 * @param {string} params.orderClass Module order class name.
 *
 * @returns {string}
 */
export declare const getSelectorWithOverlay: ({ attr, selector, breakpoint, state, orderClass, }: SelectorWithOverlayParams) => string;
