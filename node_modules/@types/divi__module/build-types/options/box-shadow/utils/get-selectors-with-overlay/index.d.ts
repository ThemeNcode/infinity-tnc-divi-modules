import { type GeneratedSelectors, type SelectorsWithOverlayParams } from './types';
/**
 * Get Selector With Overlay.
 *
 * Generate set of selectors for multiple breakpoints and states that suffixed with ` > .box-shadow-overlay`
 * based on given box shadow group attributes and the original selector.
 *
 * @since ??
 *
 * @param {object} params Function parameters.
 * @param {object} params.attr Box shadow attributes.
 * @param {string} params.selector Selector.
 * @param {string} params.orderClass Module order class name.
 *
 * @returns {object} Generated selectors.
 */
export declare const getSelectorsWithOverlay: ({ attr, selector, orderClass, }: SelectorsWithOverlayParams) => Partial<GeneratedSelectors>;
