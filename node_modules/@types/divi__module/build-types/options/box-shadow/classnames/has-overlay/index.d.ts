import { type Module } from '@divi/types';
/**
 * Get BoxShadow Options classnames.
 *
 * It returns BoxShadow classname if any BoxShadow style found.
 *
 * @since ??
 *
 * @param {object} attr BoxShadow Group Attributes.
 *
 * @returns {string} BoxShadow classname.
 *
 * @example
 * ```tsx
 * const args = {
 * };
 * boxShadowHasOverlayClassnames(args)
 * // Returns '' If box shadow attribute is empty.
 * ```
 *
 * @example
 * ```tsx
 * const args = {
 *  "desktop": {
 *    "value": {
 *      "style": "preset1",
 *      "horizontal": "5px",
 *      "vertical": "5px",
 *      "blur": "5px",
 *      "spread": "5px",
 *      "color": "#d4a373",
 *      "position": "outer"
 *    }
 *  }
 * };
 * boxShadowHasOverlayClassnames(args)
 * // Returns ''
 * ```
 *
 * @example
 * ```tsx
 * const args = {
 *  "desktop": {
 *    "value": {
 *      "style": "preset1",
 *      "horizontal": "5px",
 *      "vertical": "5px",
 *      "blur": "5px",
 *      "spread": "5px",
 *      "color": "#d4a373",
 *      "position": "inner"
 *    }
 *  }
 * };
 * boxShadowHasOverlayClassnames(args)
 * // Returns 'has-box-shadow-overlay'
 * ```
 */
export declare const boxShadowHasOverlayClassnames: (attr: Module.Element.Decoration.BoxShadow.Attributes) => string;
