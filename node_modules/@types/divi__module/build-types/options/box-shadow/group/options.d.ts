import { type Module } from '@divi/types';
import { type BoxShadowStyleOption } from './types';
export declare const descriptions: {
    style: string;
    horizontal: string;
    vertical: string;
    blur: string;
    spread: string;
    color: string;
    position: string;
};
/**
 * BoxShadow Style Options.
 *
 * This contains box shadow style options with key and its respective label, classname and icon.
 * Where ever font style options are needed at that time we need to import this.
 * The styleOptions need to be type casted because applyFilters doesn't support generic.
 *
 * @since ??
 *
 * @returns {object} BoxShadow Style Options Object.
 */
export declare const boxShadowStyleOptions: Record<"none" | "preset1" | "preset2" | "preset3" | "preset4" | "preset5" | "preset6" | "preset7", BoxShadowStyleOption>;
export declare const positionOptions: Record<Module.Element.Decoration.BoxShadow.AttributeValue['position'], {
    label: string;
}>;
