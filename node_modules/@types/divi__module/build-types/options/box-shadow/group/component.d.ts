import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
import { name } from './constants';
import './style.scss';
/**
 * Module Box Shadow Group.
 *
 * Module field group component: Box Shadow.
 *
 * @since ??
 *
 * @param {object} [props] Component props.
 * @param {boolean} [props.grouped=true] Whether the group is grouped or not.
 * @param {string} [props.groupLabel='Box Shadow'] Label for the group.
 * @param {string} [props.attrName='module.decoration.boxShadow'] Attribute name for the group.
 * @param {string} [props.fieldLabel=''] Label for the field.
 * @param {boolean} [props.visible=true] Whether the group is visible or not.
 * @param {object} [props.defaultGroupAttr] Default attribute for the group.
 * @param {object} [props.fields={}] Fields for the group.
 *
 * @returns {ReactElement} The Box Shadow Group component.
 *
 * @example Basic Usage.
 * ```tsx
 * <BoxShadowGroup />
 * ```
 *
 * @example **BoxShadowGroup** rendered with the `position` field hidden.
 * ```tsx
 * <BoxShadowGroup
 *   attrName="module.decoration.boxShadow"
 *   grouped={false}
 *   fields={{
 *     position: {
 *       render: false,
 *     },
 *   }}
 * />
 * ```
 *
 * @example **BoxShadowGroup** rendered with modified default attribute.
 * ```tsx
 * <BoxShadowGroup
 *   attrName="module.decoration.boxShadow"
 *   grouped={false}
 *   fields={{
 *     position: {
 *       defaultAttr: {
 *         desktop: {
 *           value: {
 *             position: 'inner',
 *           },
 *         },
 *       },
 *     },
 *   }}
 * />
 * ```
 */
declare const BoxShadowGroup: {
    ({ grouped, groupLabel, attrName, fieldLabel, visible, defaultGroupAttr, fields, }: Module.Settings.Group.Library.BoxShadow.GroupProps): ReactElement;
    /**
     * Static `groupName` is required so `<GroupContainer />` can recognize nested module field group
     * component that is used inside it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    groupName: string;
};
export { name, BoxShadowGroup, };
