import { type ReactElement } from 'react';
import { type BoxShadowComponentOverlayProps } from './types';
/**
 * Box Shadow Component Overlay.
 *
 * Box shadow overlay component.
 * This component is used to render box shadow overlay on all breakpoints and states.
 * If no attrs are provided or if overlay is disabled, returns `null`.
 *
 * @since ??
 *
 * @param {object} params Component props.
 * @param {object} params.attr Attribute for the style.
 *
 * @returns {ReactElement}
 */
export declare const BoxShadowComponentOverlay: ({ attr, }: BoxShadowComponentOverlayProps) => ReactElement;
