import { type ReactElement } from 'react';
import { type BoxShadowComponentProps } from './types';
/**
 * Box Shadow Component.
 *
 * BoxShadow components: Parallax, Mask, and Pattern.
 *
 * @since ??
 *
 * @param {object} params Component props.
 * @param {object} params.attr Attribute for the style.
 * @param {string} params.id Module ID.
 * @param {object|boolean} params.settings Optional. Settings for the style.
 * One of `true`/`false` or an object: `{overlay: true/false}`.
 *
 *
 * @returns {ReactElement}
 */
export declare const BoxShadowComponent: ({ attr, id, settings, }: BoxShadowComponentProps) => ReactElement;
