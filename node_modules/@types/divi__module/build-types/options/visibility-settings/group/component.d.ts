import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
import { name } from './constants';
/**
 * Visibility settings group component.
 *
 * Module field group which includes `visibility` settings.
 *
 * @since ??
 *
 * @param {object} [props] Component props.
 * @param {boolean} [props.grouped=true] Whether to group the fields or not.
 * @param {string} [props.groupLabel='Visibility'] Group label.
 * @param {string} [props.settingsGroupId='moduleVisibilitySettings'] Settings group ID.
 * @param {boolean} [props.visible=true] Whether the group is visible or not.
 * @param {object} [props.fields={}] Group fields.
 *
 * @returns {ReactElement} Visibility settings group component.
 *
 * @example Basic usage.
 * ```tsx
 * <VisibilitySettingsGroup />
 * ```
 *
 * @example **VisibilitySettingsGroup** hides the entire overflow group.
 * ```tsx
 * <VisibilitySettingsGroup
 *   grouped={false}
 *   fields={{
 *     overflowGroup: {
 *       render: false,
 *     },
 *   }}
 * />
 * ```
 *
 * @example **VisibilitySettingsGroup** enders vertical but not norizontal overflow field.
 * ```tsx
 * <VisibilitySettingsGroup
 *   grouped={false}
 *   fields={{
 *     overflowGroup: {
 *       component: {
 *         props: {
 *           fields: {
 *             x: {
 *               render: false,
 *             },
 *           },
 *         },
 *       },
 *     },
 *     disabledOnGroup: {
 *       component: {
 *         props: {
 *           fields: {
 *             disabledOn: {
 *               render: false,
 *             },
 *           },
 *         },
 *       },
 *     },
 *   }}
 * />
 * ```
 */
declare const VisibilitySettingsGroup: {
    ({ grouped, groupLabel, settingsGroupId, visible, fields, }: Module.Settings.Group.Library.VisibilitySettings.GroupProps): ReactElement;
    /**
     * Static `groupName` is required so `<GroupContainer />` can recognize nested module field group
     * component that is used inside it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    groupName: string;
};
export { name, VisibilitySettingsGroup, };
