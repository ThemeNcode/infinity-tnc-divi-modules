/**
 * Conversion function map for Button Options attribute.
 *
 * This is used for making conversion function map object.
 * Attribute name value that matches the pattern is to be processed by functions listed in this object.
 *
 * @since ??
 *
 * @returns {object} Button conversion function map object.
 */
export declare const buttonValueConversionFunctionMap: {
    'decoration.border.*.radius': (value: string) => {
        topLeft?: string;
        topRight?: string;
        bottomLeft?: string;
        bottomRight?: string;
        sync?: import("@divi/types").OnOff;
    };
    'decoration.button.*.icon.settings': (value: string) => import("@divi/types").Icon.Font.AttributeValue;
    'innerContent.*.rel': (value: string) => import("@divi/types/src/module/element/inner-content/attributes-rel").AttributesRel.AttributeValue;
};
