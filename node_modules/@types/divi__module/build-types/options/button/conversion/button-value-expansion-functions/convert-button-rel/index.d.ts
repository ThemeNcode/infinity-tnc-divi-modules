import { type Module } from '@divi/types';
/**
 * Convert D4 button rel value to D5 format.
 *
 * @since ??
 *
 * @param {string} value Button rel value in D4 format (`|` separated value).
 *
 * @returns {Array} D5 button rel value.
 */
export declare const convertButtonRel: (value: string) => Module.Element.InnerContent.AttributesRel.AttributeValue;
