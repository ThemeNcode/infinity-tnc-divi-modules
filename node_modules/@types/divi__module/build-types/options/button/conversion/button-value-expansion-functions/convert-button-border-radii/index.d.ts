import { type Module } from '@divi/types';
/**
 * Convert D4 button border radii to D5 format.
 * Button border radius in Button Options uses single value for all corners. It uses fixed
 * single fields instead of using Border Options inside Button Options.
 *
 * @since ??
 *
 * @param {string} value Shortcode attribute value for border radius.
 *
 * @returns {Array} D5 border radius value.
 */
export declare const convertButtonBorderRadii: (value: string) => Module.Element.Decoration.Border.AttributeValue['radius'];
