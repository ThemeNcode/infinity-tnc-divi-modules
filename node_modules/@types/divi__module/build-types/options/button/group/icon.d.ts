import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
declare const name = "divi/button-icon";
/**
 * Button Icon.
 *
 * Button Module Field Component.
 *
 * @since ??
 * *
 * @param {object} props Component props.
 * @param {boolean} props.grouped Optional. Whether to group the fields or not. Defaults to `true`.
 * @param {string} props.groupLabel Optional label for the group. Defaults to `Button Icon`.
 * @param {string} props.attrName Optional attribute name for the field. Defaults to `button.decoration.button`.
 * @param {boolean} props.visible Optional. Whether the group is visible or not. Defaults to `true`.
 * @param {string} props.fieldLabel Optional. Label for the field.
 * @param {object} props.defaultGroupAttr Optional. Default attribute for the group.
 * @param {object} props.fields Optional. Fields for the group. Defaults to `{}`.
 *
 * @returns {ReactElement}
 */
declare const ButtonIconGroup: ({ grouped, groupLabel, attrName, visible, fieldLabel, defaultGroupAttr, fields, }: Module.Settings.Group.Library.ButtonIcon.GroupProps) => ReactElement;
/**
 * Reconfigure mocks for buttonIcon.
 *
 * @since ??
 */
declare const mocks: {
    attrsMaps: {
        attrsCategoryMap: {
            all: {
                'button.icon': {
                    color: string;
                    enable: string;
                    icon: string;
                    onHover: string;
                    placement: string;
                };
            };
            content: {
                'button.icon': {
                    color: string;
                    enable: string;
                    icon: string;
                    onHover: string;
                    placement: string;
                };
            };
            style: {};
        };
        attrsStructureMap: {
            button: {
                icon: {
                    color: string;
                    enable: string;
                    icon: string;
                    onHover: string;
                    placement: string;
                };
            };
        };
    };
};
export { ButtonIconGroup, name, mocks, };
