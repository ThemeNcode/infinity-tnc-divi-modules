import { type ReactElement } from 'react';
import { type ButtonComponentProps } from './types';
/**
 * Button Component.
 *
 * Button element that is rendered on module.
 *
 * @since ??
 *
 * @todo Ensure icon QF re-implemented.
 * @todo Ensure responsive content is re-implemented.
 * @todo Add test.
 * @todo Add storybook.
 *
 * @param {object} props Component props.
 * @param {boolean} props.allowEmptyUrl Optional. Whether to allow empty url or not. Defaults to `false`.
 * @param {object} props.buttonAttr Optional. Button attributes.
 * @param {string} props.className Optional. CSS class name for the button.
 * @param {boolean} props.hasWrapper Optional. Whether to wrap the button or not. Defaults to `true`.
 * @param {boolean} props.hasPreloader Optional. Whether the button has preloader or not. Defaults to `false`.
 * @param {boolean} props.hasTextWrapper Optional. Whether the button has text wrapper or not. Defaults to `false`.
 * @param {string} props.id Optional. ID for the button.
 * @param {object} props.innerContent Optional. Inner content for the button.
 * @param {string} props.name Optional. Name for the button. Defaults to empty string.
 * @param {boolean} props.renderIconAsDataAttribute Optional. Whether to render icon as data attribute or not.
 * Defaults to `true`.
 * @param {string} props.type Optional. Type of the button. One of `link`, `button`. Defaults to `link`.
 * @param {object} props.dynamicDataLoaderProps Optional. Props for dynamic data loader.
 *
 * @returns {ReactElement}
 */
export declare const ButtonComponent: ({ allowEmptyUrl, buttonAttr, className, hasWrapper, hasPreloader, hasTextWrapper, id, innerContent, name, renderIconAsDataAttribute, type, dynamicDataLoaderProps, }: ButtonComponentProps) => ReactElement;
