import { type SelectorFunctionParams } from '../../../../../layout';
/**
 * Modify selector based on given attr value.
 *
 * @since ??
 *
 * @param {SelectorFunctionParams} params Selector parameter.
 *
 * @returns {string}
 */
export declare const buttonIconSelectorFunction: <TParams extends SelectorFunctionParams<TParams["attr"]>>({ selector, breakpoint, state, attr, }: TParams) => string;
