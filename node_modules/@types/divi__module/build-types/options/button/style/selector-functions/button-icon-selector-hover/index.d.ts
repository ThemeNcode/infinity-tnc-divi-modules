import { type SelectorFunctionParams } from '../../../../../layout';
/**
 * Modify hover selector based on given attr value.
 *
 * @since ??
 *
 * @param {SelectorFunctionParams} params Selector parameter.
 *
 * @returns {string}
 */
export declare const buttonIconSelectorHoverFunction: <TParams extends SelectorFunctionParams<TParams["attr"]>>({ selector, breakpoint, state, attr, }: TParams) => string;
