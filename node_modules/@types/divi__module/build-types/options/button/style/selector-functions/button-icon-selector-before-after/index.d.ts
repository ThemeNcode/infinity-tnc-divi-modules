import { type SelectorFunctionParams } from '../../../../../layout';
/**
 * Get only before and after selector.
 *
 * @since ??
 *
 * @param {SelectorFunctionParams} params Selector parameter.
 *
 * @returns {string}
 */
export declare const buttonIconSelectorBeforeAfterFunction: <TParams extends SelectorFunctionParams<TParams["attr"]>>({ selector, }: TParams) => string;
