import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
import { name } from './constants';
/**
 * Sizing Group.
 *
 * This component is used to render the sizing group in the module settings modal.
 *
 * @since ??
 *
 * @param {object} [props] Component props.
 * @param {boolean} [props.grouped=true] Whether the group should be rendered as a group or as a list of fields.
 * @param {string} [props.groupLabel='Sizing'] The label of the group.
 * @param {string} [props.attrName='module.decoration.sizing'] The attribute name of the group.
 * @param {string} [props.fieldLabel=''] The label of the field.
 * @param {boolean} [props.visible=true] Whether the group should be visible or not.
 * @param {object} [props.defaultGroupAttr={}] The default group attribute.
 * @param {object} [props.fields={}] The fields of the group.
 *
 * @returns {ReactElement} The sizing group component.
 *
 * @example Basic usage.
 * ```tsx
 * <SizingGroup />
 * ```
 *
 * @example **SizingGroup** with the `maxWidth` field disabled.
 * ```tsx
 * <SizingGroup
 *   fields={{
 *     maxWidth: {
 *       render: false,
 *     },
 *   }}
 * />
 * ```
 *
 * @example **SizingGroup** with modified `max` and `defaultUnit` props.
 * ```tsx
 * <SizingGroup
 *   fields={{
 *     width: {
 *       component: {
 *         props: {
 *           defaultUnit: 'px',
 *           max:         1000,
 *         },
 *       },
 *     },
 *   }}
 * />
 * ```
 *
 * @example **SizingGroup** with modified defaults.
 * ```tsx
 * <SizingGroup
 *   grouped={false}
 *   attrName="module.decoration.sizing"
 *   fields={{
 *     width: {
 *       defaultAttr: {
 *         desktop: {
 *           value: {
 *             width: '200%',
 *           },
 *         },
 *       },
 *     },
 *   }}
 * />
 * ```
 */
declare const SizingGroup: {
    ({ grouped, groupLabel, attrName, fieldLabel, visible, defaultGroupAttr, fields, }: Module.Settings.Group.Library.Sizing.GroupProps): ReactElement;
    /**
     * Static `groupName` is required so `<GroupContainer />` can recognize nested module field group
     * component that is used inside it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    groupName: string;
};
export { name, SizingGroup, };
