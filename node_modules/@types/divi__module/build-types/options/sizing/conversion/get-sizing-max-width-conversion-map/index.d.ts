import { type ModuleConversionMap } from '@divi/types';
import { type ConversionMapProps } from '../../../types';
/**
 *
 * Get Sizing Max Width Conversion Map.
 *
 * This function is used for getting sizing max width conversion map based on D5 expected attribute name.
 * Max Width advanced option can't be looped and take no prefix value so its D4 attribute
 * is always fixed. See: `ET_Builder_Element->_add_sizing_fields()`.
 *
 * @since ??
 *
 * @param {object} props The conversion map props.
 * @param {string} [props.d5AttrName] D5 expected attribute name.
 *
 * @returns {object} Conversion map for sizing max width options.
 */
export declare const getSizingMaxWidthConversionMap: ({ d5AttrName, }: ConversionMapProps) => ModuleConversionMap;
