import { type ReactElement } from 'react';
import { type AuthorConditionProps } from './types';
declare const authorCondition: (props: AuthorConditionProps) => ReactElement;
export { authorCondition, };
