import { type ReactNode } from 'react';
export type SortableRowsProps = {
    items: string[];
    onChange: SortableRowsOnChange;
    children: ReactNode;
};
export type SortableRowsOnChange = (items: string[]) => void;
export type ConditionItemProps = {
    id: string;
    title: string;
    onClickSettings: (id: string) => void;
    onClickDuplicate: (id: string) => void;
    onClickDelete: (id: string) => void;
};
export type SortableRowProps = {
    id: string;
    children: ReactNode;
};
