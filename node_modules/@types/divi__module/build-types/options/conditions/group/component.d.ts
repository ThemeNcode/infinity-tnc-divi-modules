import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
import { name } from './constants';
import './style.scss';
/**
 * Conditions Group.
 *
 * Module field group component: admin label.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {boolean} props.grouped Optional. Whether to group the fields or not. Defaults to `true`.
 * @param {string} props.groupLabel Optional label for the group. Defaults to `Conditions`.
 * @param {string} props.attrName Optional attribute name for the field. Defaults to `module.decoration.conditions`.
 * @param {boolean} props.visible Optional. Whether the group is visible or not. Defaults to `true`.
 *
 * @returns {ReactElement}
 */
declare const ConditionsGroup: {
    ({ grouped, groupLabel, attrName, visible, }: Module.Settings.Group.Library.Conditions.GroupProps): ReactElement;
    /**
     * Static `groupName` is required so `<GroupContainer />` can recognize nested module field group
     * component that is used inside it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    groupName: string;
};
/**
 * Export alias for consistent named export on all module field groups component.
 *
 * @since ??
 */
declare const component: {
    ({ grouped, groupLabel, attrName, visible, }: Module.Settings.Group.Library.Conditions.GroupProps): ReactElement;
    /**
     * Static `groupName` is required so `<GroupContainer />` can recognize nested module field group
     * component that is used inside it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    groupName: string;
};
export { name, ConditionsGroup, component, };
