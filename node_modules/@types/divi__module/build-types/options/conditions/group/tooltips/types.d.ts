import { type ConditionsStatus } from '../../hooks/use-get-conditions-status/types';
import { type ConditionsStore } from '../conditions-fields/types';
export type ConditionTooltipProps = {
    conditionId: string;
    conditions: ConditionsStore;
    conditionsStatus: ConditionsStatus;
};
export type ConditionsOptionTooltips = Record<string, (props: unknown) => string>;
