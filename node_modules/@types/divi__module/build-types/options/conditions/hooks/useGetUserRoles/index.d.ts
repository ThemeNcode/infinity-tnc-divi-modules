import { type UseGetUserRoles } from './types';
/**
 * Gets a list of available user roles from Divi WP REST API.
 *
 * @returns {{
 * getUserRoles: GetUserRoles,
 * userRoles: UserRole[],
 * isLoading: boolean
 * }} Returns an object consisted of:
 *
 * `getUserRoles()`: Initiates a fetch request which retrieves the data from the backend.
 *
 * `userRoles`: User roles as an array of objects, Initial value is an empty array.
 *
 * `isLoading`: Loading state for `getUserRoles()`, `true` if data has not been retrieved yet, `false` otherwise.
 */
export declare const useGetUserRoles: UseGetUserRoles;
