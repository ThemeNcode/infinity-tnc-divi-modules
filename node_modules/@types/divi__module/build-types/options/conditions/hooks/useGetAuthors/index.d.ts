import { type UseGetAuthors } from './types';
/**
 * Gets a list of available authors from Divi WP REST API.
 *
 * @returns {{
 * getAuthors: GetAuthors,
 * authors: Author[],
 * isLoading: boolean,
 * }} Returns an object consisted of:
 *
 * `getAuthors()`: Initiates a fetch request which retrieves the data from the backend.
 *
 * `authors`: Authors as an array of objects, Initial value is an empty array.
 *
 * `isLoading`: Loading state for `getAuthors()`, `true` if data has not been retrieved yet, `false` otherwise.
 */
export declare const useGetAuthors: UseGetAuthors;
