import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
import { name } from './constants';
/**
 * Transform group component.
 *
 * This component is used to render the Transform group in module settings modal.
 *
 * @since ??
 *
 * @param {object} [props] Module Field Group properties.
 * @param {boolean} [props.grouped=true] Whether the group is grouped or not.
 * @param {string} [props.groupLabel='Transform'] Group label.
 * @param {string} [props.attrName='module.decoration.transform'] Attribute name.
 * @param {string} [props.fieldLabel=''] Field label.
 * @param {boolean} [props.visible=true] Whether the group is visible or not.
 * @param {object} [props.defaultGroupAttr] Default group attributes.
 * @param {string} [props.defaultTab='scale'] Default tab.
 * @param {object} [props.fields={}] Module fields.
 *
 * @returns {ReactElement} Transform group component.
 *
 * @example Basic usage.
 * ```tsx
 * <TransformGroup />
 * ```
 *
 * @example **TransformGroup** set to display the **Scale** tab by default.
 * ```tsx
 * <TransformGroup
 *   grouped={false}
 *   defaultTab="scale"
 * />
 * ```
 *
 * @example **TransformGroup** set to display the **Translate** tab by default.
 * ```tsx
 * <TransformGroup
 *   grouped={false}
 *   defaultTab="translate"
 * />
 * ```
 */
declare const TransformGroup: {
    ({ grouped, groupLabel, attrName, fieldLabel, visible, defaultGroupAttr, defaultTab, fields, }: Module.Settings.Group.Library.Transform.GroupProps): ReactElement;
    /**
     * Static `groupName` is required so `<GroupContainer />` can recognize nested module field group
     * component that is used inside it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    groupName: string;
};
export { name, TransformGroup, };
