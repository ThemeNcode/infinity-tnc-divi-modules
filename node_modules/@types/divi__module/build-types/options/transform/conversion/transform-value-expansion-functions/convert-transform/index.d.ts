import { type StringObject } from '@types/divi__conversion';
/**
 * Convert D4 transform attribute value to D5 format.
 *
 * @since ??
 *
 * @param {string} value Shortcode attribute value for transform.
 *
 * @returns {StringObject} The expanded value.
 *
 * @example
 * ```
 * convertTransform('1deg|2deg|3deg')
 * ```
 */
export declare const convertTransform: (value: string) => StringObject;
