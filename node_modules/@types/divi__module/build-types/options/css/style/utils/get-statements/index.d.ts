import { type Module } from '@divi/types';
import { type CustomCssGetStatementsParams } from '../../types';
/**
 * Get custom CSS statements based on given params.
 *
 * @since ??
 *
 * @param {object} param0 Get custom CSS statements function parameter.
 * @param {object} param0.selectors Determine the selector for a given breakpoint and state.
 * @param {Function} param0.selectorFunction Function used to modify the selector for CSS statements by taking in
 * parameters such as the current selector, breakpoint, state, and attribute values.
 * @param {object} param0.attr Attribute values for different breakpoints and states.
 * @param {object} param0.cssFields A a mapping for each CSS field name to its corresponding selector suffix.
 * @param {string} param0.orderClass Optional. The selector class name.
 *
 * @returns {string} Custom CSS statements.
 */
export declare const getStatements: ({ selectors, selectorFunction, attr, cssFields, orderClass, }: CustomCssGetStatementsParams<Module.Css.Attributes>) => string;
