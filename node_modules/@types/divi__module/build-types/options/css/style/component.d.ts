import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
import { type CssStyleProps } from './types';
import { getStatements } from './utils';
declare const name = "divi/css";
/**
 * CSS Style.
 *
 * Get custom CSS statements as `style` element.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.selector Selector for the style. Defaults to empty string.
 * @param {object} props.selectors Optional. Selectors for the style. Defaults to `{}`.
 * @param {Function} props.selectorFunction Optional. Selector function for the style.
 * @param {object} props.attr Attribute for the style.
 * @param {boolean} props.asStyle Optional. Whether to return the style as a style or as a string. Defaults to `true`.
 * @param {object} props.cssFields Optional. CSS fields for the style.
 * @param {string} props.orderClass Optional. The selector class name.
 *
 * @returns {ReactElement}
 */
declare const CssStyle: ({ selector, selectors, selectorFunction, attr, asStyle, cssFields, orderClass, }: CssStyleProps<Module.Css.Attributes>) => ReactElement;
export { name, getStatements, CssStyle, };
