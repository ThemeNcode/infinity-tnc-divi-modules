import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
import { description, name } from './constants';
/**
 * Module CSS.
 *
 * Module field group CSS Component.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {boolean} props.grouped Optional. Whether to group the fields or not. Defaults to `true`.
 * @param {string} props.groupLabel Optional label for the group. Defaults to `Custom CSS`.
 * @param {boolean} props.visible Optional. Whether the group is visible or not. Defaults to `true`.
 * @param {string} props.attrName Optional attribute name for the field. Defaults to `css`.
 * @param {string} props.mainSelector Optional. Main selector for the CSS. Defaults to `.et_pb_module`.
 * @param {object} props.cssFields Optional. CSS fields to be added to the group. Defaults to `{}`.
 * @param {ReactNode} props.children Optional. Additional children to be added to the group.
 * @param {object} props.fields Optional. Fields for the group. Defaults to `{}`.
 *
 * @returns {ReactElement}
 */
declare const CssGroup: {
    ({ grouped, groupLabel, visible, attrName, mainSelector, cssFields, children, fields, }: Module.Settings.Group.Library.Css.GroupProps): ReactElement;
    /**
     * Static `groupName` is required so `<GroupContainer />` can recognize nested module field group
     * component that is used inside it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    groupName: string;
};
export { name, description, CssGroup, };
