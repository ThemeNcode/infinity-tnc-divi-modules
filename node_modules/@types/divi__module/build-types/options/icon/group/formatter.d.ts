import { type ReactElement, type ReactNode } from 'react';
import { type ModuleFieldsProps } from '../../../modal/components/fields/types';
/**
   * Icon prop.children's formatter to force children's panel to be visible when being rendered
   * inside `<ClipboardAttrsMapsParser />` for attrName + subName & groupName map extraction.
   *
   * @since ??
   *
   * @param {Array} fieldsNodes <Icon />'s props.children node.
   *
   * @returns {Array} Modified Icon group's props.children node that displays all panel fields.
   */
export declare const clipboardAttrsMapsFormatter: (fieldsNodes: [ReactElement<ModuleFieldsProps>]) => ReactNode;
