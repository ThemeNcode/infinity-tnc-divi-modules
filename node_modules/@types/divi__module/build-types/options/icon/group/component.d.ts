import { type ReactElement } from 'react';
import { name } from './constants';
import { type IconGroupProps } from './types';
/**
 * Module Icon.
 *
 * Icon Module Field Component.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {boolean} props.grouped Optional. Whether to group the fields or not. Defaults to `true`.
 * @param {string} props.groupLabel Optional label for the group. Defaults to `Button`.
 * @param {string} props.attrName Optional attribute name for the field. Defaults to `button`.
 * @param {string} props.fieldLabel Optional label for the field. Defaults to `Button`.
 * @param {boolean} props.visible Optional. Whether the group is visible or not. Defaults to `true`.
 * @param {object} props.fields Optional. Fields for the group. Defaults to `{}`.
 * @param {object} props.defaultGroupAttr Optional. Default attributes for the group. Defaults to `{}`.
 *
 * @returns {ReactElement}
 */
declare const IconGroup: {
    ({ grouped, groupLabel, attrName, fieldLabel, visible, fields, defaultGroupAttr, }: IconGroupProps): ReactElement;
    /**
     * Static `groupName` is required so `<GroupContainer />` can recognize nested module field group
     * component that is used inside it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    groupName: string;
};
export { name, IconGroup, };
