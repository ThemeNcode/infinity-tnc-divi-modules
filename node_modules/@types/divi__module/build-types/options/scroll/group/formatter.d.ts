import { type ReactElement, type ReactNode } from 'react';
import { type ModuleFieldsProps } from '../../../modal/components/fields/types';
/**
 * Scroll props.children's formatter to emulate fields rendering of all scroll effect tabs' fields
 * which is needed to be rendered on `<ClipboardAttrsMapsParser />` so attrName + subName + grouName
 * map can be extracted.
 *
 * @since ??
 *
 * @param {Array} fieldsNodes <Scroll />'s props.children node.
 *
 * @returns {Array} Modified Scroll group's props.children node that displays all panel fields.
 */
export declare const clipboardAttrsMapsFormatter: (fieldsNodes: [ReactElement<ModuleFieldsProps>]) => ReactNode;
