import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
import { name } from './constants';
import { clipboardAttrsMapsFormatter } from './formatter';
/**
 * Scroll Group.
 *
 * Scroll Effect Module Field Component.
 *
 * @since ??
 *
 * @param {object} props Module Field Group properties.
 * @param {boolean} props.grouped Optional. Whether the group is grouped. Defaults to `true`.
 * @param {string} props.groupLabel Optional group label. Defaults to `Scroll Effects`.
 * @param {string} props.attrName Optional attribute name. Defaults to `module.decoration.scroll`.
 * @param {string} props.fieldLabel Optional field label. Defaults to empty string.
 * @param {boolean} props.visible Optional. Whether the group is visible or not. Defaults to `true`.
 * @param {object} props.defaultGroupAttr Default group attribute.
 * @param {string} props.defaultTab Default visible tab. Defaults to `verticalMotion`.
 * @param {object} props.fields Optional group fields. Defaults to `{}`.
 *
 * @returns {ReactElement}
 */
declare const ScrollGroup: {
    ({ grouped, groupLabel, attrName, fieldLabel, visible, defaultGroupAttr, defaultTab, fields, }: Module.Settings.Group.Library.Scroll.GroupProps): ReactElement;
    /**
     * Static `groupName` is required so `<GroupContainer />` can recognize nested module field group
     * component that is used inside it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    groupName: string;
};
export { name, ScrollGroup, clipboardAttrsMapsFormatter, };
