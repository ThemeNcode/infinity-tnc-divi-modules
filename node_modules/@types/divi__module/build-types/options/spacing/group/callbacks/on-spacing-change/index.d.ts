import { type OnSpacingChangeParams } from './types';
/**
 * Event callback for spacing input onChange event.
 * NOTE: This event callback is attached to field input (top | right | bottom | left) input
 * which means the event object only carries the value of one side input.
 *
 * @since ??
 *
 * @param {OnSpacingChangeParams} params0 Event callback parameters.
 *
 * @returns {void}
 */
export declare const onSpacingChange: ({ moduleId, attrName, attrSubName, responsiveMode, stateMode, inputValue, }: OnSpacingChangeParams) => void;
