import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
import { name } from './constants';
/**
 * Spacing group component.
 *
 * This component is used to render the Spacing group in module settings modal.
 *
 * @since ??
 *
 * @param {object} [props] Module Field Group properties.
 * @param {boolean} [props.grouped=true] Whether the fields should be grouped or not.
 * @param {string} [props.groupLabel='Spacing'] Group label.
 * @param {string} [props.attrName='module.decoration.spacing'] Attribute name.
 * @param {string} [props.fieldLabel=''] Field label.
 * @param {boolean} [props.visible=true] Whether the group should be visible or not.
 * @param {object} [props.defaultGroupAttr] Default group attributes.
 * @param {object} [props.fields={}] Fields.
 *
 * @returns {ReactElement} Spacing group component.
 *
 * @example Basic usage.
 * ```tsx
 * <SpacingGroup />
 * ```
 *
 * @example **SpacingGroup** with the `padding` field disabled.
 * ```tsx
 * <SpacingGroup
 *   grouped={false}
 *   fields={{
 *     padding: {
 *       render: false,
 *     },
 *   }}
 * />
 * ```
 *
 * @example **SpacingGroup** with modified `margin` defaults.
 * ```tsx
 * <SpacingGroup
 *   grouped={false}
 *   attrName="title.decoration.sizing"
 *   fields={{
 *     margin: {
 *       defaultAttr: {
 *         desktop: {
 *           value: {
 *             margin: {
 *               top:    '50px',
 *               bottom: '50px',
 *               left:   '50px',
 *               right:  '50px',
 *             },
 *           },
 *         },
 *       },
 *     },
 *   }}
 * />
 * ```
 */
declare const SpacingGroup: {
    ({ grouped, groupLabel, attrName, fieldLabel, visible, fields, defaultGroupAttr, }: Module.Settings.Group.Library.Spacing.GroupProps): ReactElement;
    /**
     * Static `groupName` is required so `<GroupContainer />` can recognize nested module field group
     * component that is used inside it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    groupName: string;
};
export { name, SpacingGroup, };
