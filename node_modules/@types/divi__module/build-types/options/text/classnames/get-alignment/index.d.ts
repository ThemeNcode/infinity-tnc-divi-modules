import { type Module } from '@divi/types';
/**
 * Get Alignment classnames.
 *
 * It returns classnames according to text alignment options.
 *
 * @since ??
 *
 * @param {object} attr Text Group Attributes.
 *
 * @returns {string} Alignment classname.
 *
 * @example
 * ```tsx
 * const attrs = {
 *  text: {
 *  }
 * };
 * getAlignmentClassnames(attrs)
 * // Returns '' If background is empty.
 * ```
 *
 * @example
 * ```tsx
 * const attrs = {
 *  text: {
 *    "desktop": {
          "value": {
            "orientation": "top"
          }
        }
 *    },
 *  };
 * getAlignmentClassnames(attrs)
 * // Returns ''
 * ```
 *
 * @example
 * ```tsx
 * const attrs = {
 *  text: {
 *   "desktop": {
        "value": {
          "orientation": "center"
        }
      }
 *  }
 * };
 * getAlignmentClassnames(attrs)
 * // Returns 'et_pb_text_align_center'
 * ```
 */
export declare const getAlignmentClassnames: (attr: Module.Element.Advanced.Text.Attributes) => string;
