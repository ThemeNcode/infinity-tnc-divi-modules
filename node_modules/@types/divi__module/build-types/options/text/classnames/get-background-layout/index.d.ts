import { type Module } from '@divi/types';
/**
 * Get Background Layout classnames.
 *
 * It is used to generated background layout classnames.
 *
 * @since ??
 *
 * @param {object} attr Text group attributes.
 * @param {boolean} skipDesktop Skip desktop breakpoint classname.
 * @param {boolean} isTextColor Render text color classname.
 *
 * @returns {string} Background layout classname.
 *
 * @example
 * ```tsx
 * const attrs = {
 *  text: {
 *    desktop: {
 *      value: {
 *        color: "sparkling"
 *      }
 *    }
 *  }
 * };
 * getBackgroundLayoutClassnames(attrs)
 * // Returns ''
 * ```
 *
 * @example
 * ```tsx
 * const attrs = {
 *  "text": {
 *    "desktop": {
 *      "value": {
 *        "color": "dark"
 *      }
 *    }
 *  }
 * };
 * getBackgroundLayoutClassnames(attrs)
 * // Returns 'et_pb_bg_layout_dark'
 * ```
 *
 * @example
 * ```tsx
 * const attrs = {
 *  "text": {
 *    "desktop": {
 *      "hover": {
 *         "color": "light"
 *      }
 *    }
 *  }
 * };
 * getBackgroundLayoutClassnames(attrs)
 * // Returns 'et_pb_bg_layout_light_hover'
 * ```
 */
export declare const getBackgroundLayoutClassnames: (attr: Module.Element.Advanced.Text.Attributes, skipDesktop?: boolean, isTextColor?: boolean) => string;
