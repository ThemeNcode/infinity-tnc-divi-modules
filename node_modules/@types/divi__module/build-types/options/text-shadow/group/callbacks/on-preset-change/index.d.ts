import { type Params as OnChangeParams } from '../../../../../modal/callbacks/button-options/types';
/**
 * Clear other fields (except color) when selected preset is changed. This is done so preset
 * default value takes over whenever selected preset is changed.
 *
 * @since ??
 *
 * @param {OnChangeParams} onChangeParams Callback parameters.
 */
export declare const onPresetChange: (onChangeParams: OnChangeParams) => void;
