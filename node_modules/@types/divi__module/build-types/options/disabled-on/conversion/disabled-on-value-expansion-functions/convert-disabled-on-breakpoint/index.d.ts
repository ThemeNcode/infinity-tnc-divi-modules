import { type StringObject } from '@divi/conversion/src/types';
type DisabledOnObject = {
    desktopAbove?: StringObject;
    tabletOnly?: StringObject;
    phone?: StringObject;
};
/**
 * Convert D4 disabled on attribute value to D5 format.
 *
 * @since ??
 *
 * @param {string} value Shortcode attribute value for disabled on.
 *
 * @returns {DisabledOnObject} The expanded value or false if no expansion was found.
 *
 * @example
 * ```
 * convertDisabledOnBreakpoint('on|off|on')
 * ```
 */
export declare const convertDisabledOnBreakpoint: (value: string) => DisabledOnObject;
export {};
