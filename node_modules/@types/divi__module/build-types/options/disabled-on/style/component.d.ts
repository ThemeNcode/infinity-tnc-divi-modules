import { type ReactElement } from 'react';
import { type DisabledModuleStyleProps } from '../types';
declare const name = "divi/disable";
/**
 * Disabled On Style.
 *
 * Get CSS statements for disabled styles as `style` element.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.selector Selector for the style.
 * @param {object} props.selectors Optional. Selectors for the style. Defaults to `{}`.
 * @param {Function} props.selectorFunction Optional. Selector function for the style.
 * @param {object} props.propertySelectors Optional. Property selectors for the style. Defaults to `{}`.
 * @param {string} props.attr Attribute for the style.
 * @param {object} props.defaultPrintedStyleAttr Optional. Default printed style attribute.
 * @param {string} props.disabledModuleVisibility Module visibility for the style. One of `transparent` or `hidden`.
 * @param {string} props.orderClass Optional. The selector class name.
 * @param {boolean} props.asStyle Optional. Whether to return the style as a style or as a string. Defaults to `true`.
 *
 * @returns {ReactElement}
 */
declare const DisabledOnStyle: ({ selector, selectors, selectorFunction, propertySelectors, attr, defaultPrintedStyleAttr, disabledModuleVisibility, orderClass, asStyle, }: DisabledModuleStyleProps) => ReactElement;
export { name, DisabledOnStyle, };
