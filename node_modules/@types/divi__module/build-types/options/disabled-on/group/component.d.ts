import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
import { name } from './constants';
/**
 * Disabled On.
 *
 * Module field group component: admin label.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {boolean} props.grouped Optional. Whether to group the fields or not. Defaults to `true`.
 * @param {string} props.groupLabel Optional label for the group. Defaults to `Disabled On`.
 * @param {string} props.attrName Optional attribute name for the field. Defaults to `disabledOn`.
 * @param {string} props.fieldLabel Optional label for the field. Defaults to empty string.
 * @param {boolean} props.visible Optional. Whether the group is visible or not. Defaults to `true`.
 * @param {object} props.defaultGroupAttr Optional. Default attribute for the group.
 * @param {object} props.fields Optional. Fields for the group. Defaults to `{}`.
 *
 * @returns {ReactElement}
 */
declare const DisabledOnGroup: {
    ({ grouped, groupLabel, attrName, fieldLabel, visible, defaultGroupAttr, fields, }: Module.Settings.Group.Library.DisabledOn.GroupProps): ReactElement;
    /**
     * Static `groupName` is required so `<GroupContainer />` can recognize nested module field group
     * component that is used inside it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    groupName: string;
};
export { name, DisabledOnGroup, };
