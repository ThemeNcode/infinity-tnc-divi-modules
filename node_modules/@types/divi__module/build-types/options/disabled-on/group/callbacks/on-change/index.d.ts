import { type Module } from '@divi/types';
/**
 * Event callback for changing value on disabled-on field. Disabled-on field needs its own
 * callback because it doesn't adhere standard field attribute format; it uses `desktopAbove`,
 * `tabletOnly`, and `phone` breakpoint to match its expected behaviour.
 *
 * @since ??
 *
 * @param {Module.Settings.Field.OnChangeParams} params Callback params.
 */
export declare const onChange: (params: Module.Settings.Field.OnChangeParams) => void;
