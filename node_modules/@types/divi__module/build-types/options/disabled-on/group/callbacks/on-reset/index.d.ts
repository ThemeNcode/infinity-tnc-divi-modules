import { type Module } from '@divi/types';
/**
 * Disabled On checkbox field `onReset` event callback. This field needs its own reset callback
 * because it doesn't adhere standard field attribute format; it uses `desktopAbove`,
 * `tabletOnly`, and `phone` breakpoint to match its expected behaviour. Thus, remove entire
 * attribute value on reset regardless current breakpoint / state.
 *
 * @since ??
 *
 * @param {Module.Settings.Field.OnResetParams} params Field reset parameters.
 */
export declare const onReset: ({ moduleId, attrName, }: Module.Settings.Field.OnResetParams) => void;
