import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
import { name } from './constants';
/**
 * Position Settings Group component.
 *
 * Module field group which includes `position` and `z-index` settings.
 *
 * @since ??
 *
 * @param {object} [props] Component props.
 * @param {string} [props.settingsGroupId='modulePositionSettings'] Settings group ID.
 * @param {object} [props.fields={}] Module fields.
 * @param {boolean} [props.grouped=true] Whether to group the fields or not.
 * @param {string} [props.groupLabel='Position'] Group label.
 *
 * @returns {ReactElement} Position Settings Group component.
 *
 * @example Basic usage.
 * ```tsx
 * <PositionSettingsGroup />
 * ```
 *
 * @example **PositionSettingsGroup** set to hide the **Horizontal Offset** field.
 * ```tsx
 * <PositionSettingsGroup
 *   grouped={false}
 *   attrName="module.decoration.position"
 *   fields={{
 *     offsetHorizontal: {
 *       render: false,
 *     },
 *   }}
 * />
 * ```
 *
 * @example **PositionSettingsGroup** using a modified default for the **Horizontal Offset** field.
 * ```tsx
 * <PositionSettingsGroup
 *   grouped={false}
 *   attrName="module.decoration.position"
 *   fields={{
 *     offsetHorizontal: {
 *       defaultAttr: {
 *         desktop: {
 *           value: {
 *             offset: {
 *               horizontal: '50px',
 *             },
 *           },
 *         },
 *       },
 *     },
 *   }}
 * />
 * ```
 */
declare const PositionSettingsGroup: {
    ({ settingsGroupId, fields, grouped, groupLabel, }: Module.Settings.Group.Library.PositionSettings.GroupProps): ReactElement;
    /**
     * Static `groupName` is required so `<GroupContainer />` can recognize nested module field group
     * component that is used inside it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    groupName: string;
};
export { name, PositionSettingsGroup, };
