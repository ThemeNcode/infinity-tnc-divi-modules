/**
 * Convert D4 svg transform attribute value to D5 format.
 *
 * @since ??
 *
 * @param {string} value Shortcode attribute value for pattern/mask transform.
 *
 * @returns {Array} D5 svg transform value.
 *
 * @example
 * ```
 * convertSvgTransform('flip_horizontal|flip_vertical|rotate_90_degree|invert);
 * // ['flipHorizontal', 'flipVertical', 'rotate', 'invert']
 * ```
 *
 * @example
 * ```
 * convertSvgTransform('flip_horizontal||rotate_90_degree|invert');
 * // ['flipHorizontal', 'rotate', 'invert']
 * ```
 *
 * @example
 * ```
 * convertSvgTransform('');
 * // []
 * ```
 */
export declare const convertSvgTransform: (value: string) => string[];
