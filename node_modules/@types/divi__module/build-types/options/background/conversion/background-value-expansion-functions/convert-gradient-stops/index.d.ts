import { type Module } from '@divi/types';
/**
 * Convert D4 gradient stop attribute value to D5 format.
 *
 * @since ??
 *
 * @param {string} value Shortcode attribute value for gradient stops.
 *
 * @returns {Array} D5 gradient stops value.
 *
 * @example
 * ```
 * convertGradientStops('#9ddbac 0%|#ffffff 24%|#2ed5db 65%')
 * // [{ color: '#9ddbac', position: '0'}, { color: '#ffffff', position: '24'}, {color: '#2ed5db', position: '65' }]
 * ```
 */
export declare const convertGradientStops: (value: string) => Module.Element.Decoration.Background.GradientStop[];
