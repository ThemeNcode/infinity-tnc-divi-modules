/**
 * Conversion function map for Background Options attribute.
 *
 * This is used for making conversion function map object.
 * Attribute name value that matches the pattern is to be processed by functions listed in this object.
 *
 * @since ??
 *
 * @returns {object} Background conversion function map object.
 */
export declare const backgroundValueConversionFunctionMap: {
    'gradient.stops': (value: string) => import("@divi/types/src/module/element/decoration/background").Background.GradientStop[];
    'mask.transform': (value: string) => string[];
    'pattern.transform': (value: string) => string[];
};
