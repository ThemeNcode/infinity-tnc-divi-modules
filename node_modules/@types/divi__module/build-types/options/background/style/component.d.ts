import { type ReactElement } from 'react';
import { type BackgroundStyleProps } from './types';
declare const name = "divi/background";
/**
 * Background Style.
 *
 * Get background style.
 * This component is used to generate the style for the background.
 *
 * @since ??
 *
 * @param {object} params Component parameters.
 * @param {string} params.selector Optional. Selector for the style. Defaults to empty string.
 * @param {object} params.selectors Optional. Selectors for the style. Defaults to `{}`.
 * @param {Function} params.selectorFunction Optional. Selector function for the style. Defaults to `null`.
 * @param {object} params.propertySelectors Optional. Property selectors for the style. Defaults to `{}`.
 * @param {object} params.featureSelectors Optional. Feature selectors for the style. Defaults to `{}`.
 * @param {string} params.attr Optional. Attribute for the style.
 * @param {object} params.defaultPrintedStyleAttr Optional. Default printed style attribute.
 * @param {boolean} params.asStyle Optional. Whether to return the style as a style or as a string. Defaults to `true`.
 * @param {boolean} params.important Optional. Whether to add `!important` to the style or not. Defaults to `false`.
 * @param {string} params.mode Optional. Mode for the style. Defaults to `builder`.
 * @param {string} params.orderClass Optional. The selector class name.
 *
 * @returns {ReactElement}
 */
declare const BackgroundStyle: ({ selector, selectors, selectorFunction, propertySelectors, featureSelectors, attr, defaultPrintedStyleAttr, important, asStyle, mode, orderClass, }: BackgroundStyleProps) => ReactElement;
export { name, BackgroundStyle, };
