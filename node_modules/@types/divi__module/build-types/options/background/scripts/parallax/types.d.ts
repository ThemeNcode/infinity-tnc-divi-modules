import { type AttrState, type Breakpoint } from '@divi/types';
declare global {
    interface Window {
        diviElementBackgroundParallaxData?: BackgroundParallaxData[];
        diviElementBackgroundParallaxInit: (customData?: BackgroundParallaxData[]) => void;
    }
}
export type BackgroundParallaxDataItemKey = keyof BackgroundParallaxDataItem;
export interface BackgroundParallaxDataItem {
    uniqueSelector: string;
    breakpoint: Breakpoint;
    state: AttrState;
    imageUrl: string;
    enabled: boolean;
    trueParallax: boolean;
}
export interface BackgroundParallaxData {
    selector: string;
    data: BackgroundParallaxDataItem[];
}
export interface BackgroundParallaxEventSticky extends Event {
    detail: {
        stickyId: string;
    };
}
