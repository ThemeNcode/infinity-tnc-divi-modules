import { type ReactElement } from 'react';
import { type BackgroundPanelPatternProps } from './types';
/**
 * Panel Pattern.
 *
 * Background Pattern Panel of Background Module Field Component.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.groupName Group name.
 * @param {string} props.attrName Attribute name. Defaults to module.decoration.background.
 * @param {string} props.fieldLabel Field label. Defaults to empty string.
 * @param {boolean} props.panelVisible Whether panel is visible. Defaults to true.
 * @param {object} props.defaultGroupAttr Default group attribute. Defaults to `{}`.
 * @param {object} props.fields Fields. Defaults to `{}`.
 *
 * @returns {ReactElement}
 */
declare const BackgroundPanelPattern: ({ groupName, attrName, fieldLabel, panelVisible, defaultGroupAttr, fields, }: BackgroundPanelPatternProps) => ReactElement;
export { BackgroundPanelPattern, };
