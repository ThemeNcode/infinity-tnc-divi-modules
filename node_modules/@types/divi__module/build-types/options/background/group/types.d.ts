import { type MouseEvent } from 'react';
import { type AttrState, type Breakpoint, type Module } from '@divi/types';
export interface BackgroundPanel {
    attrName: string;
    fieldLabel?: string;
    panelVisible?: boolean;
    groupName?: string;
    defaultGroupAttr?: Module.Element.Decoration.Background.Attributes;
}
export interface BackgroundPanelColorProps extends BackgroundPanel {
    fields?: Module.Settings.Group.Library.Background.GroupProps['fields']['color'];
}
export interface BackgroundPanelGradientProps extends BackgroundPanel {
    fields?: Module.Settings.Group.Library.Background.GroupProps['fields']['gradient'];
}
export interface BackgroundPanelImageProps extends BackgroundPanel {
    fields?: Module.Settings.Group.Library.Background.GroupProps['fields']['image'];
}
export interface BackgroundPanelVideoProps extends BackgroundPanel {
    fields?: Module.Settings.Group.Library.Background.GroupProps['fields']['video'];
}
export interface BackgroundPanelPatternProps extends BackgroundPanel {
    fields?: Module.Settings.Group.Library.Background.GroupProps['fields']['pattern'];
}
export interface BackgroundPanelMaskProps extends BackgroundPanel {
    fields?: Module.Settings.Group.Library.Background.GroupProps['fields']['mask'];
}
export interface BackgroundOption {
    label: string;
}
export type BackgroundFieldCallbackParams = Module.Settings.Field.CallbackParams<unknown, Module.Element.Decoration.Background.Attributes>;
export interface BackgroundFieldOnChangeCallbackParams {
    event: CustomEvent | MouseEvent;
    moduleId: string;
    attrName: string;
    attrSubName?: string;
    responsiveMode: string;
    stateMode: string;
    inputValue?: string;
}
export interface BackgroundFieldOnResetCallbackParams {
    attrName: string;
    attrSubName?: string;
    moduleId: string;
    responsiveMode: Breakpoint;
    stateMode: AttrState;
}
export interface BackgroundGradientFieldOnChangeCallbackParams {
    event: CustomEvent | MouseEvent;
    moduleId: string;
    attrName: string;
    attrSubName?: string;
    responsiveMode: string;
    stateMode: string;
    inputValue?: Module.Element.Decoration.Background.GradientStop[];
}
