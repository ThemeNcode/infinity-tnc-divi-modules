import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
import { name } from './constants';
/**
 * Module Background Group.
 *
 * Background Module Field Component.
 *
 * @since ??
 *
 * @param {object} [props] Module Field Group properties.
 * @param {boolean} [props.grouped=true] Whether to group the fields or not.
 * @param {string} [props.groupLabel='Background'] Group label.
 * @param {string} [props.attrName='module.decoration.background'] Attribute name.
 * @param {string} [props.fieldLabel=''] Field label.
 * @param {boolean} [props.visible=true] Whether the group is visible or not.
 * @param {object} [props.defaultGroupAttr={}] Default group attributes.
 * @param {string} [props.defaultPanel='color'] Default panel to show.
 * @param {object} [props.fields={}] Group fields.
 * @param {string[]} [props.hidePanels=[]] Panels that should not be rendered.
 *
 * @returns {ReactElement} Module Background Group component.
 *
 * @example
 *
 * Basic usage.
 *
 * ```tsx
 * <BackgroundGroup />
 * ```
 *
 * @example
 *
 * **BackgroundGroup** displays **Gradient** panel by default.
 *
 * ```tsx
 * <BackgroundGroup
 *   attrName='module.decoration.background'
 *   grouped={false}
 *   defaultPanel='gradient'
 * />
 * ```
 *
 * @example
 *
 * **BackgroundGroup** displays **Pattern** panel & sets yellow color when the pattern is enabled.
 *
 * ```tsx
 * <BackgroundGroup
 *   attrName='module.decoration.background'
 *   grouped={false}
 *   defaultPanel='pattern'
 *   defaultGroupAttr={{
 *     desktop: {
 *       value: {
 *         pattern: {
 *           color: '#ffff00',
 *         },
 *       },
 *     },
 *   }}
 * />
 * ```
 */
declare const BackgroundGroup: {
    ({ grouped, groupLabel, attrName, fieldLabel, fields, visible, defaultGroupAttr, defaultPanel, hidePanels, }: Module.Settings.Group.Library.Background.GroupProps): ReactElement;
    /**
     * Static `groupName` is required so `<GroupContainer />` can recognize nested module field group
     * component that is used inside it. This is used for copy and paste style.
     *
     * @since ??
     * @private
     */
    groupName: string;
};
export { name, BackgroundGroup, };
