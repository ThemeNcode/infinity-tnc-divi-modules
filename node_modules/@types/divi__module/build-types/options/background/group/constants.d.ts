import { type Module } from '@divi/types';
/**
 * Background default group attribute.
 *
 * This contains default background group attributes.
 * Where ever default values for background attribute are needed at that time we need to import this.
 *
 * @since ??
 *
 * @returns {object} Background default group attribute object.
 */
export declare const backgroundDefaultGroupAttr: Module.Element.Decoration.Background.Attributes;
export declare const name = "divi/background";
