import { type ReactNode } from 'react';
/**
 * Background prop.children's formatter to force children's panel to be visible when being rendered
 * inside `<ClipboardAttrsMapsParser />` for attrName + subName & groupName map extraction.
 *
 * @since ??
 *
 * @param {ReactNode} fieldsNode <BackgroundGroup />'s props.children node.
 *
 * @returns {ReactNode}
 */
export declare const clipboardAttrsMapsFormatter: (fieldsNode: ReactNode) => ReactNode;
