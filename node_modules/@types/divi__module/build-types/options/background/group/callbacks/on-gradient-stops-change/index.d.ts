import { type BackgroundGradientFieldOnChangeCallbackParams } from '../../types';
/**
 * `onChange` event callback for Background Gradient. Push change to edit-post store based on input's event.
 *
 * @since ??
 *
 * @param {BackgroundGradientFieldOnChangeCallbackParams} params Event callback parameters.
 *
 * @returns {void}
 */
export declare const onGradientStopsChange: (params: BackgroundGradientFieldOnChangeCallbackParams) => void;
