import { type BackgroundFieldOnChangeCallbackParams } from '../../types';
/**
 * `onChange` event callback for Background Mask. Push change to edit-post store based on input's event.
 *
 * @since ??
 *
 * @param {BackgroundFieldOnChangeCallbackParams} params Event callback parameters.
 *
 * @returns {void}
 */
export declare const onMaskStyleChange: (params: BackgroundFieldOnChangeCallbackParams) => void;
