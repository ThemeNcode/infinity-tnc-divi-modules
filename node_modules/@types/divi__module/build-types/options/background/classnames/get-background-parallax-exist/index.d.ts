import { type AttrState, type Breakpoint } from '@divi/types';
/**
 * Get exist classname that signifies parallax on other breakpoint / state exist.
 * The rule for exist classname is breakpoint `desktop` and state `value` returns
 * no additional suffix. Both of them returns empty string instead of exist classname.
 *
 * @since ??
 *
 * @param {Breakpoint} breakpoint Breakpoint name.
 * @param {AttrState} state State name.
 * @param {boolean}  gradientOverlaysImage Whether gradient overlays image.
 *
 * @returns {string}
 */
export declare const getBackgroundParallaxExistClassnames: (breakpoint: Breakpoint, state: AttrState, gradientOverlaysImage?: boolean) => string;
