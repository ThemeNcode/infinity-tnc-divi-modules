import { type Module } from '@divi/types';
/**
 * Get module's parallax classname (`et_pb_section_parallax` + state suffix).
 *
 * The suffixed classnames are used by CSS selector to display the parallax on the said state.
 * There are three possible classnames (due to current three states structure) that this function possibly returned:
 * - `et_pb_section_parallax`
 * - `et_pb_section_parallax_hover`
 * - `et_pb_section_parallax_sticky`.
 * NOTE: See the unit test to check the possible combination and expected outcome. The scenario where the selected
 * option cascades to lower breakpoint / different state is the one that need special attention.
 *
 * @since ??
 *
 * @param {Module.Element.Decoration.Background.Attributes} backgroundAttr Background group attribute.
 *
 * @returns {string}
 */
export declare const getBackgroundParallaxClassnames: (backgroundAttr: Module.Element.Decoration.Background.Attributes) => string;
