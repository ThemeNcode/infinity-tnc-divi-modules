import { type ReactElement } from 'react';
import { type BackgroundComponentProps } from './types';
/**
 * Module Background Component.
 *
 * Background components: Parallax, Mask, and Pattern.
 *
 * @since ??
 *
 * @param {object} params Component props.
 * @param {object} params.attr Attribute for the style.
 * @param {string} params.id Module ID.
 * @param {object} params.settings Optional. Settings for the style.
 *
 * @returns {ReactElement}
 */
export declare const BackgroundComponent: ({ attr, id, settings, }: BackgroundComponentProps) => ReactElement;
