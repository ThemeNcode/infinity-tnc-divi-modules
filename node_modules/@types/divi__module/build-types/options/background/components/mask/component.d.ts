import { type ReactElement } from 'react';
import { type BackgroundComponentMaskPatternProps } from '../types';
/**
 * Background Component Mask.
 *
 * Mask background element.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.className Optional. Class name for the component.
 * @param {object} props.style Optional. Style for the component.
 * @param {string} props.enable Optional. Whether the background mask is enabled or not.
 * One of `on`, `off`. Defaults to `on`.
 * If set to `off`, the component will not be rendered.
 *
 * @returns {ReactElement}
 */
export declare const BackgroundComponentMask: ({ className, style, enable, }: BackgroundComponentMaskPatternProps) => ReactElement;
