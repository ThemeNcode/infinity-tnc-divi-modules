import { type ReactElement } from 'react';
import { type BackgroundComponentVideoProps } from '../types';
import '../../style-static/video.scss';
/**
 * Background Video Component.
 *
 * @since ??
 *
 * @param {BackgroundComponentVideoProps} props Component props.
 *
 * @returns {ReactElement}
 */
export declare const BackgroundComponentVideo: ({ moduleId, backgroundAttr, className, mp4, webm, width, height, allowPlayerPause, pauseOutsideViewport, }: BackgroundComponentVideoProps) => ReactElement;
