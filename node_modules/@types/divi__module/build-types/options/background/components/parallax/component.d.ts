import { type ReactElement } from 'react';
import { type BackgroundComponentParallaxProps } from './types';
/**
 * Background Component Parallax.
 *
 * Parallax backgrounds element.
 * This component is used to render parallax background on all breakpoints and states.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {object} props.backgroundAttr Background attribute.
 * @param {string} props.moduleId Module ID.
 * @param {object} props.enable Optional. Whether the background parallax is enabled or not.
 * Defaults to `{value: true, hover: true, sticky: false}`.
 *
 * @returns {ReactElement}
 */
export declare const BackgroundComponentParallax: ({ backgroundAttr, moduleId, enable, }: BackgroundComponentParallaxProps) => ReactElement;
