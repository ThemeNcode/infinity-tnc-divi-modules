import { type ReactElement } from 'react';
import { type BackgroundComponentParallaxContainerProps } from './types';
/**
 * Background Component Parallax Container.
 *
 * Container of Parallax Backgrounds element that automatically checks for state enable status.
 * Wrapper component that passes relevant state to the `BackgroundComponentParallax` component.
 *
 * @since ??
 *
 * @template BackgroundComponentParallaxContainerProps - Parallax Background Container Props.
 *
 * @param {object} props Component container props.
 * @param {object} props.backgroundAttr Background parallax attributes.
 * @param {string} props.moduleId Module ID.
 *
 * @returns {ReactElement} Background Component Parallax.
 *
 */
export declare const BackgroundComponentParallaxContainer: (props: BackgroundComponentParallaxContainerProps) => ReactElement;
