import { type AttrState, type Breakpoint } from '@divi/types';
/**
 * Get parallax background classname based on given breakpoint + state.
 *
 * @since ??
 *
 * @param {Breakpoint} breakpoint Breakpoint name.
 * @param {AttrState} state State name.
 *
 * @returns {string}
 */
export declare const getParallaxClassName: (breakpoint: Breakpoint, state: AttrState) => string;
