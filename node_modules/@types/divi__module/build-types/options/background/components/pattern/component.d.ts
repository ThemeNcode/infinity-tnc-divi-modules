import { type ReactElement } from 'react';
import { type BackgroundComponentMaskPatternProps } from '../types';
/**
 * Background Component Pattern.
 *
 * Pattern background element.
 * This component is used to render pattern background on all breakpoints and states.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.className Optional. CSS class name(s) for the component.
 * @param {object} props.style Optional. Style for the component.
 * @param {string} props.enable Optional. Whether the background pattern is enabled or not. One of `on` or `off`.
 *
 * @returns {ReactElement}
 */
export declare const BackgroundComponentPattern: ({ className, style, enable, }: BackgroundComponentMaskPatternProps) => ReactElement;
