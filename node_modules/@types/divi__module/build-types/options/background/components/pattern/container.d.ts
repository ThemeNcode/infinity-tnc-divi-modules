import { type ReactElement } from 'react';
import { type BackgroundComponentContainerProps } from '../types';
/**
 * Background Component Pattern.
 *
 * Container of Pattern Backgrounds element that automatically checks for state enable status.
 * Wrapper component that passes relevant state to the `BackgroundComponentPattern` component.
 *
 * @since ??
 *
 * @template BackgroundComponentContainerProps - Background Container Props.
 *
 * @param {object} props Component container props.
 * @param {object} props.backgroundAttr Background pattern attributes.
 * @param {string} props.moduleId Module ID.
 *
 * @returns {ReactElement} VBackground Component Pattern component.
 *
 */
export declare const BackgroundComponentPatternContainer: (props: BackgroundComponentContainerProps) => ReactElement;
