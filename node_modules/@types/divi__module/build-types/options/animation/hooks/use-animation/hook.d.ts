import { type AttrState, type Breakpoint, type Module } from '@divi/types';
/**
 * Custom hook for getting animation classname: classname that marks that animation is currently being animated.
 *
 * @since ??
 *
 * @param {object} param0 Function parameters.
 * @param {object} param0.attr Animation attribute.
 * @param {string} param0.breakpoint Attribute breakpoint.
 * @param {string} param0.state Attribute state.
 *
 * @returns {string} Animation classname.
 */
export declare const useAnimation: ({ attr, breakpoint, state, }: {
    attr: Module.Element.Decoration.Animation.Attributes;
    breakpoint: Breakpoint;
    state: AttrState;
}) => string;
