import { type GetAnimationTimeoutParams } from './types';
/**
 * Get Animation Timeout.
 *
 * It is used to get animation timeout for animation effect.
 *
 * @since ??
 *
 * @param {object} props The Selector parameters.
 * @param {object} [props.attr] Animation attribute object.
 * @param {string} [props.breakpoint] Breakpoint name.
 * @param {string} [props.state] State name.
 *
 * @returns {number} Animation timeout in seconds.
 *
 * @example
 * ```tsx
 * const args = {
 *  "attr": {
 *    "desktop": {
 *      "value": {
 *        "style": "fade",
 *        "duration": "1116ms",
 *        "delay": "230ms
 *      }
 *    }
 *  },
 *  "breakpoint": "desktop",
 *  "state": "value"
 * };
 * getAnimationTimeout(args)
 * // Returns '1346'
 * ```
 */
export declare const getAnimationTimeout: ({ attr, breakpoint, state, }: GetAnimationTimeoutParams) => number;
