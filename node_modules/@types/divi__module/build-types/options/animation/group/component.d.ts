import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
import { name } from './constants';
/**
 * Module Animation.
 *
 * Component that provides animation related fields and groups for Module Settings Modal.
 *
 * @since ??
 *
 * @param {object} [props] Component props.
 * @param {boolean} [props.grouped=true] Whether the group is grouped or not.
 * @param {string} [props.groupLabel='Animation'] Label for the group.
 * @param {string} [props.attrName='module.decoration.animation'] Attribute name for the group.
 * @param {string} [props.fieldLabel=''] Label for the field.
 * @param {boolean} [props.visible=true] Whether the group is visible or not.
 * @param {object} [props.defaultGroupAttr] Default attributes for the group.
 * @param {object} [props.fields={}] Fields for the group.
 *
 * @returns {ReactElement} The AnimationGroup component.
 *
 * @example Basic Usage.
 * ```tsx
 * <AnimationGroup />
 * ```
 *
 * @example **AnimationGroup** with repeat fields hidden via `props.fields`.
 * ```tsx
 * <AnimationGroup
 *   fields={{
 *     repeat: {
 *       visible: false,
 *     },
 *   }}
 * />
 * ```
 *
 * @example **AnimationGroup** with modified `defaultAttr` to change the default `duration` value.
 * ```tsx
 * <AnimationGroup
 *   grouped={false}
 *   attrName="module.decoration.animation"
 *   fields={{
 *     duration: {
 *       defaultAttr: {
 *         desktop: {
 *           value: {
 *             duration: '500ms',
 *           },
 *         },
 *       },
 *     },
 *   }}
 * />
 * ```
 */
declare const AnimationGroup: {
    ({ grouped, groupLabel, attrName, fieldLabel, visible, defaultGroupAttr, fields, }: Module.Settings.Group.Library.Animation.GroupProps): ReactElement;
    /**
     * Static `groupName` is required so `<GroupContainer />` can recognize nested module field group
     * component that is used inside it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    groupName: string;
};
export { name, AnimationGroup, };
