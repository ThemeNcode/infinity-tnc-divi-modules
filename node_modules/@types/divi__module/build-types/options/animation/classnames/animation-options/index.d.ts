import { type Module } from '@divi/types';
/**
 * Animation Classnames.
 *
 * Get Animation Options classnames.
 *
 * @since ??
 *
 * @param {object} attr Animation Group Attributes.
 * @param {string} attr.desktop.value.style Animation style.
 * One of `none`, `fade`, `slide`, `bounce`, `zoom`, `flip`, `old` or roll`.
 * @param {string} attr.desktop.value.direction Animation direction. One of `center`, `left`, `right`, `bottom`, `top`.
 * @param {string} attr.desktop.value.duration Animation duration.
 * @param {string} attr.desktop.value.delay Animation delay.
 * @param {string} attr.desktop.value.intensity.slide Slide intensity.
 * @param {string} attr.desktop.value.intensity.zoom Zoom intensity.
 * @param {string} attr.desktop.value.intensity.flip Flip intensity.
 * @param {string} attr.desktop.value.intensity.fold Fold intensity.
 * @param {string} attr.desktop.value.intensity.roll Roll intensity.
 * @param {string} attr.desktop.value.startingOpacity Starting opacity.
 * @param {string} attr.desktop.value.speedCurve Speed curve.
 * One of `ease-in-out`, `ease`, `ease-in`, `ease-out` or `linear`.
 * @param {string} attr.desktop.value.repeat Repeat. One of `once` or `loop`.
 *
 *
 * @returns {string}
 */
export declare const animationClassnames: (attr: Module.Element.Decoration.Animation.Attributes) => string;
