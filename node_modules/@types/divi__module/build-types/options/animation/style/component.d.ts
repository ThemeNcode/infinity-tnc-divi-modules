import { type ReactElement } from 'react';
import { type AnimationStyleProps } from './types';
declare const name = "divi/animation";
/**
 * Animation Style.
 *
 * Get animation style.
 * This component is used to generate the style for the animation.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.selector Selector for the style. Defaults to empty string.
 * @param {object} props.selectors Optional. Selectors for the style. Defaults to `{}`.
 * @param {Function} props.selectorFunction Optional. Function to generate the selector CSS class(es).
 * The default function is `(params) => .et_animated${params.selector}`.
 * @param {object} props.propertySelectors Optional. Property selectors for the style. Defaults to `{}`.
 * @param {string} props.attr Attribute for the style.
 * @param {object} props.defaultPrintedStyleAttr Optional. Default printed style attribute.
 * @param {boolean} props.asStyle Optional. Whether to return the style as a style or as a string. Defaults to `true`.
 * @param {boolean} props.important Optional. Whether to add `!important` to the style or not. Defaults to `false`.
 * @param {string} props.orderClass Optional. The selector class name.
 *
 * @returns {ReactElement}
 */
declare const AnimationStyle: ({ selector, selectors, selectorFunction, propertySelectors, attr, defaultPrintedStyleAttr, asStyle, important, orderClass, }: AnimationStyleProps) => ReactElement;
export { name, AnimationStyle, };
