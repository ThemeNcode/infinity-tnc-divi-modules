import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
import { name } from './constants';
/**
 * Module field group component: Link Option.
 *
 * @since ??
 *
 * @param {Module.Settings.Group.Library.Link.GroupProps} props Component props.
 *
 * @returns {ReactElement}
 */
declare const LinkGroup: {
    ({ grouped, groupLabel, attrName, fieldLabel, visible, linkUrlDescription, linkTargetDescription, defaultGroupAttr, fields, }: Module.Settings.Group.Library.Link.GroupProps): ReactElement;
    /**
     * Static `groupName` is required so `<GroupContainer />` can recognize nested module field group
     * component that is used inside it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    groupName: string;
};
export { name, LinkGroup, };
