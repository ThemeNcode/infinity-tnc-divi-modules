import { type ModuleConversionMap } from '@divi/types';
import { type ConversionMapProps } from '../../../types';
/**
 *
 * Get Link Conversion Map.
 *
 * This function is used for getting link conversion map based on given D5 expected attribute name.
 *
 * @since ??
 *
 * @param {object} props The conversion map props.
 * @param {string} [props.d5AttrName] D5 expected attribute name.
 * @param {string} [props.d4AttributeSuffix] D4 attribute suffix.
 *
 * @returns {object} Conversion map for link options.
 */
export declare const getLinkConversionMap: ({ d5AttrName, d4AttributeSuffix, }: ConversionMapProps) => ModuleConversionMap;
