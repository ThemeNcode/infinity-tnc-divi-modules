import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
import { name } from './constants';
/**
 * Font Group.
 *
 * Module field group that contains font related fields.
 *
 * @since ??
 *
 * @param {object} [props] Component props.
 * @param {boolean} [props.grouped=true] Whether to group fields or not.
 * @param {string} [props.groupLabel='Module Text'] Group label.
 * @param {string} [props.groupTab=''] Group tab.
 * @param {string} [props.attrName='module.decoration.font'] Group attribute name.
 * @param {string} [props.fieldLabel=''] Field label.
 * @param {boolean} [props.visible=true] Whether to show the group or not.
 * @param {object} [props.defaultGroupAttr={}] Default group attributes.
 * @param {object} [props.fields={}] Group fields.
 *
 * @returns {ReactElement} Font Group component.
 *
 * @example Basic Usage.
 * ```tsx
 * <FontGroup />
 * ```
 *
 * @example **FontGroup** with `center` and `justify` removed from **Text Alignment** options.
 * ```tsx
 * <FontGroup
 *   grouped={false}
 *   fields={{
 *     textAlign: {
 *       component: {
 *         props: {
 *           options: getTextAlignOptions({
 *             excludeOptions: ['justify', 'center'],
 *           }),
 *         },
 *       },
 *     },
 *   }}
 * />
 * ```
 */
declare const FontGroup: {
    ({ attrName, fieldLabel, grouped, groupLabel, groupTab, visible, defaultGroupAttr, fields, }: Module.Settings.Group.Library.Font.GroupProps): ReactElement;
    /**
     * Static `groupName` is required so `<GroupContainer />` can recognize nested module field group
     * component that is used inside it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    groupName: string;
};
export { name, FontGroup, };
