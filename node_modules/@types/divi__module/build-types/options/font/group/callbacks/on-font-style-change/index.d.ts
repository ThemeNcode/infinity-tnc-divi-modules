import { type OnFontStyleChangeParams } from './types';
/**
 * Event callback for font-style input onChange event (when the button is clicked).
 *
 * @since ??
 *
 * @param {OnFontStyleChangeParams} params Event callback parameters.
 *
 * @returns {void}
 */
export declare const onFontStyleChange: ({ event, moduleId, attrName, attrSubName, responsiveMode, stateMode, }: OnFontStyleChangeParams) => void;
