import { type Module } from '@divi/types';
import { type FieldOption, type FieldOptionHasIcon } from '../../types';
export declare const headingLevelOptions: Record<Module.Element.Decoration.Font.AttributeValue['headingLevel'], FieldOptionHasIcon>;
/**
 * Font weight options.
 *
 * This contains font weight options with key and its respective label.
 * Where ever font weight options are needed at that time we need to import this.
 *
 * @since ??
 *
 * @returns {object} Font weight options object.
 */
export declare const fontWeightOptions: Record<Module.Element.Decoration.Font.AttributeValue['weight'], FieldOption>;
/**
 * Font style options.
 *
 * This contains font style options with key and its respective label and icon.
 * Where ever font style options are needed at that time we need to import this.
 *
 * @since ??
 *
 * @returns {object} Font style options object.
 */
export declare const fontStyleOptions: Record<Module.Element.Decoration.Font.StyleValue, FieldOptionHasIcon>;
/**
 * Returns text alignment options.
 *
 * @since ??
 *
 * @param {object} param0 Get Text Align options params.
 * @param {string[]} param0.excludeOptions Keys of the options to remove.
 * @param {object} param0.includeOptions Options to add.
 *
 * @returns {object} Text align options.
 */
export declare const getTextAlignOptions: ({ excludeOptions, includeOptions, }: {
    excludeOptions?: Array<Module.Element.Decoration.Font.AttributeValue['textAlign']>;
    includeOptions?: Record<string, FieldOptionHasIcon>;
}) => Record<string, FieldOptionHasIcon>;
/**
 * Text decoration options for underline and strikethrough.
 *
 * @since ??
 *
 * @type {object}
 * @property {object} solid
 * @property {object} double
 * @property {object} dotted
 * @property {object} dashed
 * @property {object} wavy
 */
export declare const strikethroughUnderlineOptions: Record<Exclude<Module.Element.Decoration.Font.AttributeValue['lineStyle'], ''>, FieldOption>;
export declare const listStyleUnorderedTypeOptions: Record<Module.Element.Decoration.Font.ListStyleUnordered, FieldOption>;
export declare const listStyleOrderedTypeOptions: Record<Module.Element.Decoration.Font.ListStyleOrdered, FieldOption>;
export declare const listStylePosition: Record<Module.Element.Decoration.Font.ListAttributeValue['position'], FieldOption>;
