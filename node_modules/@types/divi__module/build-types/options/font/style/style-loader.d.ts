import React, { type ReactElement } from 'react';
import { type FontStyleLoaderProps } from './types';
/**
 * Font Style Loader.
 *
 * Component for rendering DOM which loads google / custom fonts styles.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {object} props.loadedFonts Object of Google and/or custom fonts currently loaded.
 *
 * @returns {ReactElement}
 */
declare const FontStyleLoader: (props: FontStyleLoaderProps) => ReactElement<FontStyleLoaderProps>;
/**
 * Font Style Loader Container.
 *
 * Wrapper component for `FontStyleLoader` that passes all the necessary state to the component.
 *
 * @since ??
 *
 * @returns {ReactElement} FontStyleLoader component.
 */
declare const FontStyleLoaderContainer: React.ComponentType<{}>;
export { FontStyleLoader, FontStyleLoaderContainer, };
