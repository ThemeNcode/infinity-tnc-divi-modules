import { type ReactElement } from 'react';
import { type FontStyleProps } from './types';
declare const name = "divi/font";
/**
 * Font Style.
 *
 * Get Font CSS statements as `style` element.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.selector CSS selector.
 * @param {object} props.selectors CSS selectors.
 * @param {Function} props.selectorFunction CSS selector function.
 * @param {object} props.propertySelectors CSS property selectors.
 * @param {object} props.attr Font style attributes.
 * @param {object} props.defaultPrintedStyleAttr Default printed style attribute.
 * @param {boolean} props.asStyle Whether to return the style as a style or as a string. Defaults to `true`.
 * @param {boolean} props.important Whether to add `!important` to the style or not. Defaults to `false`.
 * @param {string|boolean} props.headingLevel Optional. Heading level. Defaults to `false`.
 * @param {string} props.orderClass Optional. The selector class name.
 *
 * @returns {ReactElement}
 */
declare const FontStyle: ({ selector, selectors, selectorFunction, propertySelectors, attr, defaultPrintedStyleAttr, important, asStyle, headingLevel, orderClass, }: FontStyleProps) => ReactElement;
export { name, FontStyle, };
