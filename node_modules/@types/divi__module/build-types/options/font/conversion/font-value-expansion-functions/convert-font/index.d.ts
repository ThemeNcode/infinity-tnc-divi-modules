import { type FontAttrs } from '@types/divi__conversion';
/**
 * Convert D4 font attribute value to D5 format.
 *
 * This is used to parse the string passed as argument into object of expanded D5 font attribute format.
 *
 * @since ??
 *
 * @param {string} value Shortcode attribute value for font.
 *
 * @returns {object} The expanded value or empty object if no expansion was found.
 *
 * @example
 * ```tsx
 * convertFont('Allegria|300|on|on|on|on|on|#ffffff|dotted')
 * // Returns following font attribute object
 * //{
 * // family:    'Allegria',
 * // lineColor: '#ffffff',
 * // lineStyle: 'dotted',
 * // style:     [
 * //   'italic',
 * //   'uppercase',
 * //   'underline',
 * //   'capitalize',
 * //   'strikethrough',
 * // ],
 * // weight: '300',
 * //}
 * ```
 */
export declare const convertFont: (value: string) => FontAttrs;
