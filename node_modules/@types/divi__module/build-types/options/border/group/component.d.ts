import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
import { name } from './constants';
import { clipboardAttrsMapsFormatter } from './formatter';
/**
 * Module Border Group.
 *
 * Within the Module Settings Modal, this component provides a field group where the user can set
 * the border radius, width, color, and style for a module.
 *
 * @since ??
 *
 * @example Basic usage.
 * ```tsx
 * <BorderGroup />
 * ```
 *
 * @example **BorderGroup** displays the label "Placeholder" given the following props:
 * ```tsx
 * <BorderGroup
 *   grouped={false}
 *   attrName="module.decoration.border"
 *   defaultGroupAttr={{
 *     desktop: {
 *       value: 'Placeholder',
 *     },
 *   }}
 * />
 * ```
 *
 * @example **BorderGroup** renders no fields because all of them are intentionally hidden:
 * ```tsx
 * <BorderGroup
 *   grouped={false}
 *   fields={{
 *     radius:            { label: 'Border Radius', render: false },
 *     stylesTabNav:      { label: 'Border Styles', render: false },
 *     styles:            { label: '', render: false },
 *     stylesTabbedWidth: { label: 'Border Width', render: false },
 *     stylesTabbedColor: { label: 'Border Color', render: false },
 *     stylesTabbedStyle: { label: 'Border Style', render: false },
 *   }}
 * />
 * ```
 *
 * @example **BorderGroup** will renders border width with a modified default value:
 * ```tsx
 * <BorderGroup
 *   grouped={false}
 *   fields={{
 *     stylesTabbedWidth: {
 *       defaultAttr: {
 *         desktop: {
 *           value: {
 *             styles: {
 *               all: {
 *                 width: '10px',
 *               },
 *             },
 *           },
 *         },
 *       },
 *     },
 *   }}
 * />
 * ```
 */
declare const BorderGroup: {
    ({ grouped, groupLabel, attrName, fieldLabel, visible, defaultGroupAttr, fields, }: Module.Settings.Group.Library.Border.GroupProps): ReactElement;
    /**
     * Static `groupName` is required so `<GroupContainer />` can recognize nested module field group
     * component that is used inside it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    groupName: string;
};
export { name, BorderGroup, clipboardAttrsMapsFormatter, };
