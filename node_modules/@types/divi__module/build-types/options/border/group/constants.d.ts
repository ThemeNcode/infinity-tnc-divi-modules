/**
 * Border default group attribute.
 *
 * This contains default border group attributes.
 * Wherever default values for border attribute are needed at that time we need to import this.
 *
 * @since ??
 *
 * @returns {object} Border default group attribute object.
 */
export declare const borderDefaultGroupAttr: {
    desktop: {
        value: import("@divi/types/src/module/element/decoration/border").Border.AttributeValue;
    };
};
export declare const name = "divi/border";
/**
 * Border Side Labels Object.
 *
 * This is used for mapping labels for each border side with its key.
 *
 * @since ??
 *
 * @returns {object} Border side labels object.
 */
export declare const borderSideLabels: {
    top: string;
    right: string;
    bottom: string;
    left: string;
};
