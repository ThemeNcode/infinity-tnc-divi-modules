import { type ReactElement, type ReactNodeArray } from 'react';
/**
 * Border props.children's formatter to emulate fields rendering of all available border sides
 * which is needed to be rendered on `<ClipboardAttrsMapsParser />` so attrName + subName + groupName
 * map can be extracted.
 *
 * @since ??
 *
 * @param {ReactNodeArray} fieldsNode <BorderGroup />'s props.children node.
 *
 * @returns {ReactElement}
 */
export declare const clipboardAttrsMapsFormatter: (fieldsNode: ReactNodeArray) => ReactElement;
