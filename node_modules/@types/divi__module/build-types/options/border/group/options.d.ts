import { type Module } from '@divi/types';
/**
 * Border style side options.
 *
 * This contains border style side options with key and its respective label and icon.
 * Where ever border style side options are needed at that time we need to import this.
 *
 * @since ??
 *
 * @returns {object} Border Style side options object.
 */
export declare const borderStyleSideOptions: Record<Module.Element.Decoration.Border.Side, {
    icon: string;
    label: string;
}>;
/**
 * Border style options.
 *
 * This contains border style options with key and its respective label.
 * Where ever border style options are needed at that time we need to import this.
 *
 * @since ??
 *
 * @returns {object} Border Style options object.
 */
export declare const borderStyleOptions: Record<Module.Element.Decoration.Border.SideValues['style'], {
    label: string;
}>;
