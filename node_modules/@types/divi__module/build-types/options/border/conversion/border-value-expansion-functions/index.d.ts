/**
 * Conversion function map for Border Options attribute.
 *
 * This is used for making conversion function map object.
 * Attribute name value that matches the pattern is to be processed by functions listed in this object.
 *
 * @since ??
 *
 * @returns {object} Border conversion function map object.
 */
export declare const borderValueConversionFunctionMap: {
    radius: (value: string) => {
        topLeft?: string;
        topRight?: string;
        bottomLeft?: string;
        bottomRight?: string;
        sync?: import("@divi/types").OnOff;
    };
};
