import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
import { name } from './constants';
/**
 * Email Account Group.
 *
 * Module field group component: Email Account.
 *
 * @since ??
 *
 * @returns {ReactElement}
 */
declare const EmailServiceGroup: {
    ({ grouped, groupLabel, attrName, visible, fields, defaultGroupAttr, }: Module.Settings.Group.Library.EmailService.GroupProps): ReactElement;
    /**
     * Static `groupName` is required so `<GroupContainer />` can recognize nested module field group
     * component that is used inside it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    groupName: string;
};
/**
 * Export alias for consistent named export on all module field groups component.
 *
 * @since ??
 */
declare const component: {
    ({ grouped, groupLabel, attrName, visible, fields, defaultGroupAttr, }: Module.Settings.Group.Library.EmailService.GroupProps): ReactElement;
    /**
     * Static `groupName` is required so `<GroupContainer />` can recognize nested module field group
     * component that is used inside it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    groupName: string;
};
export { name, EmailServiceGroup, component, };
