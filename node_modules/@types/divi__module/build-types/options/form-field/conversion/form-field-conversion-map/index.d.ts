/**
 * Conversion map for FormField Options attribute.
 *
 * This is used for making conversion map object.
 * It maps the D4 attribute name to corresponding D5 attribute conversion path.
 *
 * @since ??
 *
 * @returns {object} FormField conversion map object.
 */
export declare const formFieldConversionMap: {
    d4_attr_name_background_color: string;
    d4_attr_name_text_color: string;
    d4_attr_name_focus_background_color: string;
    d4_attr_name_focus_text_color: string;
    use_focus_border_color: string;
    placeholder_color: string;
};
