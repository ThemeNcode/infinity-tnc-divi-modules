import { type Module } from '@divi/types';
/**
 * Get Dividers Options classnames.
 *
 * It returns dividers classname if any dividers style found.
 *
 * @since ??
 *
 * @param {object} attr Divider Group Attributes.
 *
 * @returns {string} Divider classname.
 *
 * @example
 * ```tsx
 * const args = {
 * };
 * dividersClassnames(args)
 * // Returns '' If divider is empty.
 * ```
 *
 * @example
 * ```tsx
 * const args = {
 *  top: {
 *    desktop: {
 *      value: {
 *        style: "waves"
 *      },
 *    },
 *  },
 * };
 * dividersClassnames(args)
 * // Returns 'section_has_divider et_pb_top_divider'
 * ```
 *
 * @example
 * ```tsx
 * const args = {
 *  bottom: {
 *    desktop: {
 *      value: {
 *        style: "waves"
 *      }
 *    }
 *  }
 * };
 * dividersClassnames(args)
 * // Returns 'section_has_divider et_pb_bottom_divider'
 * ```
 */
export declare const dividersClassnames: (attr: Module.Element.Advanced.Dividers.Attributes) => string;
