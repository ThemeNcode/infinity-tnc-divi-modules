import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
/**
 * Divider.
 *
 * Divider component element.
 * If the divider `placement` or `style` is not defined, returns `null`.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.placement Divider placement. One of `top` or `bottom`.
 * @param {string} props.style Divider style. On of :
 * `none`, `arrow`, `arrow2`, `arrow3`, `asymmetric`, `asymmetric2`, `asymmetric3`,
 * `asymmetric4`, `clouds`, `clouds2`, `curve`, `curve2`, `graph`, `graph2`, `graph3`,
 * `graph4`, `mountains`, `mountains2`, `ramp`, `ramp2`, `slant`, `slant2`, `triangle`,
 * `wave`, `wave2`, `waves` or `waves2`.
 *
 * @returns {ReactElement}
 */
export declare const DividerComponent: ({ placement, style, }: Module.Element.Advanced.Dividers.Component.Props) => ReactElement;
