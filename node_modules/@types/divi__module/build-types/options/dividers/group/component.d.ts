import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
import { name } from './constants';
/**
 * Dividers.
 *
 * Dividers module field groups.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {boolean} props.grouped Optional. Whether to group the fields or not. Defaults to `true`.
 * @param {string} props.groupLabel Optional label for the group. Defaults to `Dividers`.
 * @param {string} props.fieldLabel Optional label for the field.
 * @param {string} props.attrName Optional attribute name for the field. Defaults to `dividers`.
 * @param {boolean} props.visible Optional. Whether the group is visible or not. Defaults to `true`.
 * @param {object} props.defaultGroupAttr Optional. Default attribute for the group.
 * @param {string} props.defaultTab Optional. Default tab to show. Defaults to `top`.
 * @param {object} props.fields Optional. Fields for the group. Defaults to `{}`.
 *
 * @returns {ReactElement}
 */
declare const DividersGroup: {
    ({ grouped, groupLabel, fieldLabel, attrName, visible, defaultGroupAttr, defaultTab, fields, }: Module.Settings.Group.Library.Dividers.GroupProps): ReactElement;
    /**
     * Static `groupName` is required so `<GroupContainer />` can recognize nested module field group
     * component that is used inside it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    groupName: string;
};
export { name, DividersGroup, };
