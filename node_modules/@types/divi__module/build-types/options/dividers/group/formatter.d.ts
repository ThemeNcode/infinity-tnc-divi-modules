import { type ReactElement, type ReactNode } from 'react';
import { type ModuleFieldsProps } from '../../../modal/components/fields/types';
/**
 * Dividers props.children's formatter to emulate fields rendering of all dividers tabs' fields
 * which is needed to be rendered on `<ClipboardAttrsMapsParser />` so attrName + subName + groupName
 * map can be extracted.
 *
 * @since ??
 *
 * @param {Array} fieldsNodes <Dividers />'s props.children node.
 *
 * @returns {Array} Modified Dividers group's props.children node that displays all panel fields.
 */
export declare const clipboardAttrsMapsFormatter: (fieldsNodes: [ReactElement<ModuleFieldsProps>]) => ReactNode;
