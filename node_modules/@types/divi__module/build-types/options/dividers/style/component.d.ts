import { type ReactElement } from 'react';
import { type DividersStyleProps } from './types';
declare const name = "divi/dividers";
/**
 * Get dividers style.
 *
 * Get box-shadow CSS statements as `style` element.
 * Statements used in DividersStyle has no selectors nor selectorFunction props and simply follows given
 * featureSelectors, if any.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.selector Selector for the style.
 * @param {object} props.featureSelectors Optional. Selectors for the style. Defaults to `{}`.
 * @param {string} props.attr Attribute for the style.
 * @param {string} props.placement Placement for the style.
 * @param {string} props.backgroundColors Optional. Background colors for the style. Defaults to empty string.
 * @param {boolean} props.fullwidth Optional. Whether the style is fullwidth. Defaults to `false`.
 * @param {boolean} props.important Optional. Whether to add `!important` to the style or not. Defaults to `false`.
 * @param {boolean} props.asStyle Optional. Whether to return the style as a style or as a string. Defaults to `true`.
 * @param {string} props.mode Optional. Style renderer mode. One of `builder` or `frontend` Defaults to `builder`.
 * @param {string} props.orderClass Optional. The selector class name.
 *
 * @returns {ReactElement}
 */
declare const DividersStyle: ({ selector, featureSelectors, attr, placement, backgroundColors, fullwidth, important, asStyle, mode, orderClass, }: DividersStyleProps) => ReactElement;
export { name, DividersStyle, };
