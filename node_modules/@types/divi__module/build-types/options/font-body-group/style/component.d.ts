import { type ReactElement } from 'react';
import { type FontBodyStyleProps } from './types';
declare const name = "divi/font-body";
/**
 * Font Body Style.
 *
 * Font style component that is being adjusted for body group and its group tabs.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.selector Optional. Selector for the style. Defaults to empty string.
 * @param {object} props.selectors Optional. Selectors for the style. Defaults to `{}`.
 * @param {Function} props.selectorFunction Font body style CSS selector function.
 * @param {object} props.propertySelectors Font body style CSS property selectors. Defaults to `{}`.
 * @param {object} props.attr Font body style attributes.
 * @param {object} props.defaultPrintedStyleAttr Font body style default printed attributes.
 * @param {boolean} props.important Optional. Whether to add `!important` to the style. Defaults to `false`.
 * @param {string} props.orderClass Optional. The selector class name.
 *
 * @returns {ReactElement}
 */
declare const FontBodyStyle: ({ selector, selectors, selectorFunction, propertySelectors, attr, defaultPrintedStyleAttr, important, orderClass, }: FontBodyStyleProps) => ReactElement;
export { name, FontBodyStyle, };
