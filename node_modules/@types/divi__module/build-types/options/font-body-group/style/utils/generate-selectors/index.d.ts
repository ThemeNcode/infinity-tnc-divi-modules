/**
 *
 * Generate selectors for a given base selector and sub selector.
 *
 * @param {string} baseSelector Base selector.
 * @param {string} subSelector Sub selector.
 * @returns {string}
 * @since ??
 */
export declare const generateSelectors: (baseSelector: string, subSelector: string) => string;
