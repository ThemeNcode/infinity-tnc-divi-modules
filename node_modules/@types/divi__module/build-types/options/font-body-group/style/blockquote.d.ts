import { type ReactElement } from 'react';
import { type BlockquoteFontStyleProps } from './types';
declare const name = "divi/font-blockquote";
/**
 * Get Blockquote Font style.
 *
 * @since ??
 *
 * @param {BlockquoteFontStyleProps} styleProps Parameter for CSS statements.
 *
 * @returns {ReactElement}
 */
declare const BlockquoteFontStyle: ({ selector, selectors, selectorFunction, propertySelectors, attr, important, asStyle, orderClass, }: BlockquoteFontStyleProps) => ReactElement;
export { name, BlockquoteFontStyle, };
