import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
import { name } from './constants';
/**
 * Font Body Group.
 *
 * This component is used to render the font body group in the module settings modal.
 *
 * @since ??
 *
 * @param {object} [params] Component params.
 * @param {string} [params.attrName='module.decoration.bodyFont'] The attribute name.
 * @param {string} [params.title='Body Text'] The group title.
 * @param {boolean} [params.grouped=true] Whether the group is grouped or not.
 * @param {string} [params.defaultTab='body'] The default tab.
 * @param {object} [params.defaultGroupAttr] The default group attribute.
 * @param {object} [params.groups={}] The groups attributes.
 * @param {object} [params.fields={}] The fields attributes.
 * @param {ReactNode} [params.children] The children.
 *
 * @returns {ReactElement} The font body group component.
 *
 * @example Basic usage.
 * ```tsx
 * <FontBodyGroup />
 * ```
 *
 * @example **FontBodyGroup** with the default tab set to `ul`.
 * ```tsx
 * <FontBodyGroup
 *   defaultTab="ul"
 * />
 * ```
 *
 * @example **FontBodyGroup** with the default tab set to `ul` and the `ul` group disabled.
 * ```tsx
 * <FontBodyGroup
 *   grouped={false}
 *   defaultTab="ul"
 *   fields={{
 *     ul: {
 *       textAlign:       { render: false },
 *       color:           { render: false },
 *       size:            { render: false },
 *       letterSpacing:   { render: false },
 *       lineHeight:      { render: false },
 *       textShadowGroup: { render: false },
 *     },
 *   }}
 * />
 * ```
 */
declare const FontBodyGroup: {
    ({ attrName, title, grouped, defaultTab, defaultGroupAttr, groups, fields, children, }: Module.Settings.Group.Library.FontBody.GroupProps): ReactElement;
    /**
     * Static `groupName` is required so `<GroupContainer />` can recognize nested module field group
     * component that is used inside it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    groupName: string;
};
export { name, FontBodyGroup, };
