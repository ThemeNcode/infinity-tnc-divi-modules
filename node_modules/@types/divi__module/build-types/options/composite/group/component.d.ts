import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
import { name } from './constants';
/**
 * Composite group component.
 *
 * This component is used to render composed group, most likely containing field for multiple element
 * attributes, in module settings modal. This group component has no fields defined by default which
 * means it always  requires proper fields param to be passed.
 * This component is created to enable automatically generated module settings panel from module
 * metadata to be rendered.
 *
 * @since ??
 *
 * @param {object} [props] Module Module Field Group properties.
 * @param {boolean} [props.grouped=true] Whether the fields should be grouped or not.
 * @param {string} [props.groupLabel='Spacing'] Group label.
 * @param {string} [props.attrName='module.decoration.spacing'] Attribute name.
 * @param {string} [props.fieldLabel=''] Field label.
 * @param {boolean} [props.visible=true] Whether the group should be visible or not.
 * @param {object} [props.fields={}] Fields.
 *
 * @returns {ReactElement} Composite group component.
 */
declare const CompositeGroup: {
    ({ grouped, groupLabel, attrName, fieldLabel, visible, fields, }: Module.Settings.Group.Library.Composite.GroupProps): ReactElement;
    /**
     * Static `groupName` is required so `<GroupContainer />` can recognize nested module field group
     * component that is used inside it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    groupName: string;
};
export { name, CompositeGroup, };
