import { type Module } from '@divi/types';
import { type ElementType } from '../style/types';
/**
 * Map of filter function based on `type` property.
 */
export declare const filterFunctionMap: Partial<Record<ElementType, (attrs: Module.Element.Decoration.Attributes) => Module.Element.Decoration.Attributes>>;
export { buttonTypeAttrs } from './button-type-attrs';
