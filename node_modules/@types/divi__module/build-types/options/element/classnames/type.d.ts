import { type AttrState, type Breakpoint, type Module } from '@divi/types';
import { type BackgroundComponentProps } from '../../background/components/types';
import { type BoxShadowComponentProps } from '../../box-shadow/components/types';
export interface ElementClassnamesProps {
    attrs?: Module.Element.Decoration.Attributes;
    animation?: boolean;
    background?: BackgroundComponentProps['settings'];
    border?: boolean;
    boxShadow?: BoxShadowComponentProps['settings'];
    state?: AttrState;
    breakpoint?: Breakpoint;
}
