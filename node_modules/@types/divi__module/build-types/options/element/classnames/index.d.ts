import { type ElementClassnamesProps } from './type';
/**
 * Get Element Classnames.
 *
 * It returns classnames according to attributes passed of any element.
 *
 * @since ??
 *
 * @param {object}  props The Element Attribute parameters.
 * @param {string}  [props.attrs] Elements attributes.
 * @param {boolean} [props.animation] Animation to be included or not.
 * @param {boolean} [props.background] Background to be included or not.
 * @param {boolean} [props.boxShadow] BoxShadow to be included or not.
 * @param {string} [props.breakpoint] Current breakpoint.
 * @param {string} [props.state] Current state.
 *
 * @returns {string} Element classnames.
 *
 * @example
 * ```tsx
 * const attrs = {
 *  "link": {
 *    "desktop": {
 *      "value": {
 *        "url": "https://instagram.com",
 *        "target": "off"
 *      }
 *    }
 *  },
 *  "border": {
 *    "desktop": {
 *      "value": {
 *        "radius": {
 *          "topLeft": "5px",
 *          "topRight": "5px",
 *          "bottomLeft": "5px",
 *          "bottomRight": "5px",
 *          "sync": "on"
 *        },
 *        "styles": {
 *          "all": {
 *            "width": "1px",
 *            "color": "#000000",
 *            "style": "dashed"
 *          }
 *        }
 *      }
 *    }
 *  },
 *  "boxShadow": {
 *    "desktop": {
 *      "value": {
 *        "style": "preset1",
 *        "position": "inner",
 *        "color":"#000000"
 *      }
 *    }
 * };
 * elementClassnames(attrs)
 * // Returns 'has-box-shadow-overlay'
 * ```
 */
export declare const elementClassnames: ({ attrs, animation, background, boxShadow, breakpoint, state, }: ElementClassnamesProps) => string;
