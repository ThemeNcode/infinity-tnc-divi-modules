import { type StyleComponentMapType, type StyleComponentName } from './types';
/**
 * Get style component based on style component name.
 *
 * @since ??
 *
 * @param {StyleComponentName} styleComponentName Style component name.
 *
 * @returns {FunctionComponent} Style component.
 *
 * @example
 *
 * ```tsx
 * // Get style component using default arguments.
 * const styleComponentName = '';
 * const styleComponent = getStyleComponent(styleComponentName);
 *
 * // Get style component with specific component name.
 * const styleComponentName = 'divi/text';
 * const styleComponent = getStyleComponent(styleComponentName);
 * ```
 */
export declare const getStyleComponent: (styleComponentName: StyleComponentName) => StyleComponentMapType[StyleComponentName]['component'];
