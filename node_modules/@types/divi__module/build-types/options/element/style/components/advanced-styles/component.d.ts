import { type ReactElement } from 'react';
import { type AdvancedStylesProps } from '../../types';
/**
 * Component to add advanced styles to ElementStyle.
 *
 * This will add styles without transitions for style components added to the
 * advancedStyles prop of ElementStyle component.
 *
 * @since ??
 *
 * @param {AdvancedStylesProps} props Component props.
 *
 * @returns {ReactElement}
 *
 * @example
 *
 * ```tsx
 * // Apply style using default arguments.
 * <AdvancedStyles />
 * // Result: null
 *
 * // Apply style with specific selector and advanced styles.
 * <AdvancedStyles
 *   selector=".element1"
 *   advancedStyles={[
 *     {
 *       componentName: 'divi/text',
 *       props: {
 *         attr: {
 *           text: {
 *             desktop: {
 *               value: {
 *                 orientation: 'left',
 *               },
 *             },
 *           },
 *         },
 *       },
 *     },
 *   ]}
 * />
 * // Result: ".element1 {text-align: left;}"
 */
declare const AdvancedStyles: ({ selector, advancedStyles, orderClass, }: AdvancedStylesProps) => ReactElement;
export { AdvancedStyles, };
