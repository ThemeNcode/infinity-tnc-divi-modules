import { type ReactElement } from 'react';
import { type ElementStyleProps } from './types';
/**
 * Element style.
 *
 * All styles that relate to the element itself.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.selector Selector for the style.
 * @param {object} props.attrs Attributes for the style.
 * @param {Function} props.attrsFilter Optional. Function to filter the attributes. Defaults to `null`.
 * @param {object} props.defaultPrintedStyleAttrs Optional. Default printed style attributes.
 * @param {string} props.type Optional. Type of the element. Defaults to `module`.
 * @param {string} props.orderClass Optional. The selector class name.
 * @param {object} props.background Optional. Background style.
 * @param {object} props.font Optional. Font style.
 * @param {object} props.icon Optional. Icon style.
 * @param {object} props.headingFont Optional. Heading font style.
 * @param {object} props.bodyFont Optional. Body font style.
 * @param {object} props.spacing Optional. Spacing style.
 * @param {object} props.sizing Optional. Sizing style.
 * @param {object} props.border Optional. Border style.
 * @param {object} props.boxShadow Optional. Box shadow style.
 * @param {object} props.filters Optional. Filter style.
 * @param {object} props.transform Optional. Transform style.
 * @param {object} props.transition Optional. Transition style.
 * @param {object} props.animation Optional. Animation style.
 * @param {object} props.disabledOn Optional. Disabled on style.
 * @param {object} props.overflow Optional. Overflow style.
 * @param {object} props.position Optional. Position style.
 * @param {object} props.zIndex Optional. Z index style.
 * @param {object} props.button Optional. Button style.
 * @param {object} props.advancedStyles Optional. Advanced styles.
 *
 * @returns {ReactElement}
 */
declare const ElementStyle: ({ selector, attrs, attrsFilter, defaultPrintedStyleAttrs, type, advancedStyles, orderClass, background, font, icon, headingFont, bodyFont, spacing, sizing, border, boxShadow, filters, transform, transition, animation, disabledOn, overflow, position, zIndex, button, }: ElementStyleProps) => ReactElement;
export { ElementStyle, };
