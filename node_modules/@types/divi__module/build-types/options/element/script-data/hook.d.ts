import { type UseElementScriptDataParams } from './types';
/**
 * ElementScriptData.
 *
 * All scripts that relate to the element itself.
 *
 * @since ??
 *
 * @param {object} params Function parameters.
 * @param {string} params.selector Element selector.
 * @param {object} params.attrs Element attributes.
 * @param {string} params.id Element ID.
 * @param {object} params.sticky Element sticky attributes.
 *
 * @returns {void}
 *
 * @example Basic usage.
 * ```tsx
 * useElementScriptData({
 *  attrs: {},
 *  selector: "",
 *  id: "",
 * });
 *```
 *
 * @example Basic usage with example attributes.
 * ```tsx
 * const attrs = {
 *   "decoration": {
 *       "position": {
 *         desktop: {
 *           value: {
 *             color: '#e09150',
 *           },
 *           hover: {
 *             color: '#edf000',
 *           },
 *           sticky: {
 *             color: '#e02b20',
 *           },
 *         },
 *      },
 *   },
 * };
 *
 * useElementScriptData({
 *   attrs,
 *   selector: ".my-simple-module",
 *   id: "mySimpleModuleContent",
 * });
 *```
 */
export declare const useElementScriptData: ({ selector, attrs, id, sticky, }: UseElementScriptDataParams) => void;
