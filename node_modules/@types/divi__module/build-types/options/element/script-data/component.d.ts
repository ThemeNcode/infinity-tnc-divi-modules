import { type ReactElement } from 'react';
import { type ElementScriptDataProps } from './types';
/**
 * Element Script Data.
 *
 * It is used to call the useElementScriptData hook for the module elements.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.selector Selector for the module.
 * @param {object} props.attrs Attributes for the module elements.
 * @param {string} props.id Id for the module.
 * @param {object} props.sticky Sticky Attributes for the module elements.
 *
 * @returns {ReactElement}
 */
declare const ElementScriptData: ({ selector, attrs, id, sticky, }: ElementScriptDataProps) => ReactElement;
export { ElementScriptData, };
