import { type ModuleConversionMap } from '@divi/types';
import { type ConversionMapProps } from '../../../types';
/**
 * Get Module Options Sticky conversion map based on given D4 advanced options attribute and
 * D5 expected attribute name.
 *
 * @since ??
 *
 * @param {object} param0 Get Module Options Sticky conversion map function parameter.
 * @param {string} param0.d4AdvancedOptionName The name for a D4 advanced option attribute.
 * @param {string} param0.d5AttrName The name for a D5 advanced option attribute.
 * @param {string} param0.d4AttributeSuffix Attribute name suffix in D4.
 *
 * @returns {object} Conversion Map for sticky options.
 */
export declare const getStickyConversionMap: ({ d4AdvancedOptionName, d5AttrName, d4AttributeSuffix, }: ConversionMapProps) => ModuleConversionMap;
