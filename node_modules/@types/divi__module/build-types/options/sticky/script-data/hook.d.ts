import { type UseStickyScriptDataParams } from './types';
/**
 * Custom hook for registering sticky script data to sticky frontend script.
 *
 * @since ??
 *
 * @param {object} param0 Function parameters.
 * @param {object} param0.affectingAttrs Specify the attributes that affect the sticky behavior.
 * @param {object} param0.attr The sticky attribute value.
 * @param {string} param0.breakpoint Specify the breakpoint of the attribute that affects the sticky behavior.
 * @param {string} param0.id Module Unique ID.
 * @param {boolean} param0.isSticky Is sticky.
 * @param {Function} param0.setStickySetting Function to set the sticky setting for a specific module by taking
 * the module ID and sticky setting as parameters and updating it in the frontend script, typically when registering
 * or updating sticky script data.
 * @param {string} param0.selector The selector for the module/component to which the sticky behavior should be applied.
 * @param {string} param0.state Specify the state of the attribute that affects the sticky behavior.
 * @param {Function} param0.unsetStickySetting Function to remove the sticky setting for a specific module
 * when the component using the useStickyScriptData hook is unmounted, typically during a switch to wireframe mode
 * or when the module component is removed.
 *
 * @returns {void}
 */
export declare const useStickyScriptData: ({ affectingAttrs, attr, breakpoint, id, isSticky, setStickySetting, selector, state, unsetStickySetting, }: UseStickyScriptDataParams) => void;
