import { type StickyPosition } from './types';
/**
 * Get valid stickyPosition value which implies module is sticky element.
 *
 * @since ??
 *
 * @returns {string[]} Valid stickyPosition values.
 */
export declare const getValidStickyPosition: () => StickyPosition[];
