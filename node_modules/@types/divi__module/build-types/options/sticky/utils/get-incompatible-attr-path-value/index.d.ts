/**
 * Get Incompatible attribute path value.
 *
 * It returns list of attribute path and its value which represent fields that prevent sticky element
 * to work due to the way it works..
 *
 * @since ??
 *
 * @param {object} attrNames The Attribute parameters.
 * @param {string} [attrNames.position] Position attributes.
 * @param {string} [attrNames.scroll] Scroll attributes.
 *
 * @returns {object} List of attribute path.
 */
export declare const getIncompatibleAttrPathValue: (attrNames?: {
    position?: string;
    scroll?: string;
}) => Record<string, string[]>;
