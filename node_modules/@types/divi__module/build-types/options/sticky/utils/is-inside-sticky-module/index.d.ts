import { type ModuleFlatObjects } from '@divi/types';
/**
 * Check if module is inside sticky module (check if one of module's ancestor is sticky).
 *
 * @since ??
 *
 * @param {string} moduleId Module id.
 * @param {object} moduleObjects All modules' objects.
 *
 * @returns {boolean} True if module is inside sticky module. False otherwise.
 */
export declare const isInsideStickyModule: (moduleId: string, moduleObjects: ModuleFlatObjects) => boolean;
