import { type ModuleFlatObjects } from '@divi/types';
/**
 * Check if module with given id is sticky module.
 * Nested sticky (sticky inside another sticky module) is not allowed.
 *
 * @since ??
 *
 * @param {string} moduleId Module id.
 * @param {object} moduleObjects Module objects.
 *
 * @returns {boolean} Whether module with given id is sticky module or not.
 */
export declare const isStickyModule: (moduleId: string, moduleObjects: ModuleFlatObjects) => boolean;
