import { type Module } from '@divi/types';
/**
 * Check if given attrs has incompatible attribute value which makes sticky mechanism can't
 * be used on current element.
 *
 * @since ??
 *
 * @param {object} affectingAttrs Function parameter.
 * @param {object} affectingAttrs.position An object that represents the position attributes.
 * @param {object} affectingAttrs.scroll An object that represents the scroll attributes.
 *
 * @returns {boolean} Whether given attrs has incompatible attribute value or not.
 */
export declare const hasIncompatibleAttrs: (affectingAttrs: {
    position?: Module.Element.Decoration.Position.Attributes;
    scroll?: Module.Element.Decoration.Scroll.Attributes;
}) => boolean;
