import { type IsStickyElementParams } from './types';
/**
 * Check if given element (module, module element) is a sticky element.
 * Sticky element is marked by valid sticky position. However if the element has incompatible
 * attribute (position or scroll), the sticky element will be disabled.
 *
 * @since ??
 *
 * @param {object} param0 Function parameters.
 * @param {object} param0.attr Sticky group attributes.
 * @param {object} param0.affectingAttrs Position and scroll group attributes.
 *
 * @returns {boolean} True if the element is a sticky element.
 */
export declare const isStickyElement: ({ attr, affectingAttrs, }: IsStickyElementParams) => boolean;
