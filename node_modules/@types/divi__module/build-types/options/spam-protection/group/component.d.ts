import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
import { name } from './constants';
/**
 * Spam Protection Group.
 *
 * Module field group component: Spam Protection.
 *
 * @since ??
 *
 * @returns {ReactElement}
 */
declare const SpamProtectionGroup: {
    ({ grouped, groupLabel, attrName, visible, fields, defaultGroupAttr, }: Module.Settings.Group.Library.SpamProtection.GroupProps): ReactElement;
    /**
     * Static `groupName` is required so `<GroupContainer />` can recognize nested module field group
     * component that is used inside it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    groupName: string;
};
/**
 * Export alias for consistent named export on all module field groups component.
 *
 * @since ??
 */
declare const component: {
    ({ grouped, groupLabel, attrName, visible, fields, defaultGroupAttr, }: Module.Settings.Group.Library.SpamProtection.GroupProps): ReactElement;
    /**
     * Static `groupName` is required so `<GroupContainer />` can recognize nested module field group
     * component that is used inside it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    groupName: string;
};
export { name, SpamProtectionGroup, component, };
