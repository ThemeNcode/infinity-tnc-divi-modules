/**
 * Conversion map for Filters Options attribute.
 *
 * This is used for making conversion map object.
 * It maps the D4 attribute name to corresponding D5 attribute conversion path.
 *
 * @since ??
 *
 * @returns {object} Filters conversion object.
 */
export declare const filtersConversionMap: {
    d4_attr_name_filter_blur: string;
    d4_attr_name_filter_brightness: string;
    d4_attr_name_filter_contrast: string;
    d4_attr_name_filter_hue_rotate: string;
    d4_attr_name_filter_invert: string;
    d4_attr_name_filter_opacity: string;
    d4_attr_name_filter_saturate: string;
    d4_attr_name_filter_sepia: string;
    d4_attr_name_mix_blend_mode: string;
};
