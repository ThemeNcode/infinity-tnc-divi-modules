import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
import { name } from './constants';
import './style.scss';
/**
 * Module Filters.
 *
 * Module field group component: filters.
 *
 * @since ??
 *
 * @param {object} [props] Component props.
 * @param {boolean} [props.grouped=true] Whether to group the fields.
 * @param {string} [props.groupLabel='Filters'] Group label.
 * @param {string} [props.attrName='module.decoration.filters'] Attribute name.
 * @param {string} [props.fieldLabel=''] Field label.
 * @param {boolean} [props.visible=true] Whether the group is visible.
 * @param {object} [props.defaultGroupAttr={}] Default group attributes.
 * @param {object} [props.fields={}] Fields.
 *
 * @returns {ReactElement} Filters group component.
 *
 * @example Basic Usage.
 * ```tsx
 * <FiltersGroup />
 * ```
 *
 * @example **FiltersGroup** with custom `groupLabel`.
 * ```tsx
 * <FiltersGroup
 *   groupLabel="Custom Filters"
 * />
 * ```
 *
 * @example **FiltersGroup** with modified default `hueRotate` value.
 * ```tsx
 * <FiltersGroup
 *   fields={{
 *     hueRotate: {
 *       defaultAttr: {
 *         desktop: {
 *           value: {
 *             hueRotate: '90deg',
 *           },
 *         },
 *       },
 *     },
 *   }}
 * />
 * ```
 */
declare const FiltersGroup: {
    ({ grouped, groupLabel, attrName, fieldLabel, visible, defaultGroupAttr, fields, }: Module.Settings.Group.Library.Filters.GroupProps): ReactElement;
    /**
     * Static `groupName` is required so `<GroupContainer />` can recognize nested module field group
     * component that is used inside it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    groupName: string;
};
export { name, FiltersGroup, };
