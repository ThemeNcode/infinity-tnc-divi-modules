import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
import { name } from './constants';
/**
 * Font Header Group.
 *
 * This component is used to render font header group inside modal.
 *
 * @since ??
 *
 * @param {object} params Component props.
 * @param {string} params.attrName Optional attribute name. Defaults to `module.decoration.headingFont`.
 * @param {string} params.title Optional group title. Defaults to `Heading Text`.
 * @param {boolean} params.grouped Optional. Whether the group is grouped or not. Defaults to `true`.
 * @param {string} params.defaultTab Optional default tab heading. Defaults to `h1`.
 * @param {object} params.defaultGroupAttr Optional default group attribute.
 * @param {object} params.groups Optional group attributes. Defaults to `{}`.
 * @param {object} params.fields Optional field attributes. Defaults to `{}`.
 *
 * @returns {ReactElement}
 */
declare const FontHeaderGroup: {
    ({ attrName, title, grouped, defaultTab, defaultGroupAttr, groups, fields, }: Module.Settings.Group.Library.FontHeader.GroupProps): ReactElement;
    /**
     * Static `groupName` is required so `<GroupContainer />` can recognize nested module field group
     * component that is used inside it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    groupName: string;
};
export { name, FontHeaderGroup, };
