import { type ReactElement } from 'react';
import { type FontHeaderStyleProps } from './types';
declare const name = "divi/font-header";
/**
 * Font Header Style.
 *
 * Font style component that is being adjusted for header group and its group tabs.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.selector Optional. Selector for the style. Defaults to empty string.
 * @param {object} props.selectors Optional. Selectors for the style. Defaults to `{}`.
 * @param {Function} props.selectorFunction Optional. Selector function for the style.
 * @param {object} props.propertySelectors Optional. Property selectors for the style. Defaults to `{}`.
 * @param {object} props.attr Optional. Attribute for the style.
 * @param {object} props.defaultPrintedStyleAttr Optional. Default printed style attribute for the style.
 * @param {boolean} props.important Optional. Whether to add `!important` to the style. Defaults to `false`.
 * @param {string} props.orderClass Optional. The selector class name.
 *
 * @returns {ReactElement}
 */
declare const FontHeaderStyle: ({ selector, selectors, selectorFunction, propertySelectors, attr, defaultPrintedStyleAttr, important, orderClass, }: FontHeaderStyleProps) => ReactElement;
export { name, FontHeaderStyle, };
