import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
import { name } from './constants';
/**
 * Transition group component.
 *
 * Module field group which includes `transition` settings.
 *
 * @since ??
 *
 * @param {object} [props] Component props.
 * @param {boolean} [props.grouped=true] Whether to group the fields or not.
 * @param {string} [props.groupLabel='Transitions'] Group label.
 * @param {string} [props.attrName='module.decoration.transition'] Attribute name.
 * @param {string} [props.fieldLabel=''] Field label.
 * @param {boolean} [props.visible=true] Whether the group is visible or not.
 * @param {object} [props.defaultGroupAttr] Default group attribute.
 * @param {object} [props.fields={}] Group fields.
 *
 * @returns {ReactElement} Transition group component.
 *
 * @example Basic usage.
 * ```tsx
 * <TransitionGroup />
 * ```
 *
 * @example **TransitionGroup** set to hide the **Speed Curve** field.
 * ```tsx
 * <TransitionGroup
 *   grouped={false}
 *   fields={{
 *     speedCurve: {
 *       render: false,
 *     },
 *   }}
 * />
 * ```
 *
 * @example **TransitionGroup** with modified default `delay` value.
 * ```tsx
 * <TransitionGroup
 *   grouped={false}
 *   attrName='module.decoration.transition'
 *   fields={{
 *     delay: {
 *       defaultAttr: {
 *         desktop: {
 *           value: {
 *             delay: '50ms',
 *           },
 *         },
 *       },
 *     },
 *   }}
 * />
 * ```
 */
declare const TransitionGroup: {
    ({ grouped, groupLabel, attrName, fieldLabel, visible, defaultGroupAttr, fields, }: Module.Settings.Group.Library.Transition.GroupProps): ReactElement;
    /**
     * Static `groupName` is required so `<GroupContainer />` can recognize nested module field group
     * component that is used inside it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    groupName: string;
};
export { name, TransitionGroup, };
