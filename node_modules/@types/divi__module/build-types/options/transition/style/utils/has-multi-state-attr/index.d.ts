import { type ModuleAttr } from '@divi/types';
/**
 * Check if hover or sticky are enabled for the advanced styles.
 *
 * @since ??
 *
 * @param {ModuleAttr} attr The module attribute.
 *
 * @returns {boolean} True if exist hover or sticky styles, false otherwise.
 */
export declare const hasMultiStateAttr: (attr: ModuleAttr) => boolean;
