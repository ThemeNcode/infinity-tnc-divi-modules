import { type ReactElement } from 'react';
import { type TransitionStyleProps } from './types';
declare const name = "divi/transition";
/**
 * Transition Style.
 *
 * Get transition CSS statements as `style` element.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.selector Selector for the style.
 * @param {object} props.selectors Optional. Selectors for the style. Defaults to `{}`.
 * @param {Function} props.selectorFunction Optional. Selector function for the style.
 * @param {object} props.attr Attribute for the style.
 * @param {object} props.attrs Attributes for the style.
 * @param {object} props.transitionData Transition data attribute for style.
 * @param {boolean} props.asStyle Optional. Whether to return the style as a style or as a string. Defaults to `true`.
 * @param {object} props.propertySelectors Optional. Property selectors for the style. Defaults to `{}`.
 * @param {object} props.important Optional. Whether to add `!important` to the style(s). Defaults to `false`.
 * @param {string} props.orderClass Optional. The selector class name.
 * @param {object} props.advancedStyles Optional. Advanced styles declaration for current element.
 *
 * @returns {ReactElement}
 */
declare const TransitionStyle: ({ selector, selectors, propertySelectors, attr, attrs, transitionData, selectorFunction, important, orderClass, asStyle, advancedStyles, }: TransitionStyleProps) => ReactElement;
export { name, TransitionStyle, };
