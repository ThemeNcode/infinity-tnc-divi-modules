import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
import { name } from './constants';
/**
 * Admin Label Group.
 *
 * Within the Module Settings Modal, this component provides a field where the user can set the admin label.
 *
 * @since ??
 *
 * @param {object} [Props] All component props for the Admin Label Group component.
 * @param {boolean} [Props.grouped] Whether the group is grouped or not.
 * @param {string} [Props.groupLabel='Admin Label'] Group label.
 * @param {string} [Props.attrName] Object path to the attribute that will be updated.
 * @param {string} [Props.fieldLabel=''] Field label.
 * @param {boolean} [Props.visible=true] Whether the group is visible or not.
 * @param {object} [Props.defaultGroupAttr] Default label object.
 * @param {object} [Props.fields] All related fields for the Admin Label Group component.
 *
 * @returns {ReactElement} Admin Label Group component.
 *
 * @example
 *
 * Basic usage.
 *
 * ```tsx
 * <AdminLabelGroup />
 * ```
 *
 * @example
 *
 * **AdminLabelGroup** displays the label "Placeholder" given the following props:
 *
 * ```tsx
 * <AdminLabelGroup
 *   grouped={false}
 *   attrName="module.meta.adminLabel"
 *   defaultGroupAttr={{
 *     desktop: {
 *       value: 'Placeholder',
 *     },
 *   }}
 * />
 * ```
 *
 * @example
 *
 * **AdminLabelGroup** displays the label "Foo" (not "Section") given the following props:
 *
 * ```tsx
 * <AdminLabelGroup
 *   grouped={false}
 *   attrName="module.meta.adminLabel"
 *   fields={{
 *     adminLabel: {
 *       defaultAttr: {
 *         desktop: {
 *           value: 'Foo',
 *         },
 *       },
 *     },
 *   }}
 *   defaultGroupAttr={{
 *     desktop: {
 *       value: 'Section',
 *     },
 *   }}
 * />
 * ```
 *
 * @example
 *
 * **AdminLabelGroup** renders no fields because all of them are intentionally hidden:
 *
 * ```tsx
 * <AdminLabelGroup
 *   grouped={false}
 *   fields={{
 *     adminLabel: { render: false },
 *   }}
 * />
 * ```
 */
declare const AdminLabelGroup: {
    ({ grouped, groupLabel, attrName, fieldLabel, visible, defaultGroupAttr, fields, }: Module.Settings.Group.Library.AdminLabel.GroupProps): ReactElement;
    /**
     * Static `groupName` is required so `<GroupContainer />` can recognize nested module field group
     * component that is used inside it. This is used for copy paste style.
     *
     * @since ??
     * @private
     */
    groupName: string;
};
export { name, AdminLabelGroup, };
