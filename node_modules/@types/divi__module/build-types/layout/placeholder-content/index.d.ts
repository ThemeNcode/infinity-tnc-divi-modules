/**
 * Placeholder content for newly added module.
 *
 * @since ??
 *
 * @type {object}
 * @property {string} title
 * @property {string} subtitle
 * @property {string} body
 * @property {string} number
 * @property {string} button
 * @property {object} image
 * @property {string} image.landscape
 * @property {string} image.portrait
 * @property {string} video
 */
export declare const placeholderContent: {
    title: string;
    subtitle: string;
    body: string;
    number: string;
    button: string;
    image: {
        landscape: string;
        portrait: string;
    };
    video: string;
};
