import { ReactElement } from 'react';
/**
 * Child Modules Container.
 *
 * Memoized `<ChildModules />` component that does deep props comparison.
 * Without deep comparison, the props passed to `<ChildModules />` will always be considered as
 * changed by React (which leads to unnecessary rerender) because React only does shallow comparison.
 *
 * @since ??
 *
 * @returns {ReactElement}
 */
export declare const ChildModulesContainer: import("react").MemoExoticComponent<({ ids, parentCallback, }: import("./types").ChildModulesProps) => ReactElement<any, string | import("react").JSXElementConstructor<any>>>;
