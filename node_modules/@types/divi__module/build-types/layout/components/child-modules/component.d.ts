import { type ReactElement } from 'react';
import { type ChildModulesProps } from './types';
/**
 * Component to render child modules based on given ids.
 *
 * @since ??
 *
 * @param {ChildModules} props Component props.
 *
 * @todo Moved @types/divi__data usage to container; keep component purely presentational.
 *
 * @returns {ReactElement}
 */
export declare const ChildModules: ({ ids, parentCallback, }: ChildModulesProps) => ReactElement;
