import { type DynamicDataInfo } from '../../types';
/**
 * Hooks to fetch and resolve dynamic data based on a given JSON-like format value.
 *
 * @since ??
 *
 * @param {string} value String that represents the dynamic data in JSON-like format.
 *
 * @returns {object} Object that contains the following properties: `isDynamic`, `isLoading`, `isError`,
 * and `resolvedValue`.
 */
export declare const useDynamicData: (value: string) => DynamicDataInfo;
