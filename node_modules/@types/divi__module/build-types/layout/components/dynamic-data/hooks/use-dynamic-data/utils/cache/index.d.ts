/**
 * Clears the cache object.
 *
 * @since ??
 */
export declare const clearCache: () => void;
/**
 * Adds resolved dynamic data value to a cache object based on a post ID and a JSON-like format value.
 *
 * @since ??
 *
 * @param {string} value String that represents the dynamic data in JSON-like format.
 * @param {string} postId Current post ID.
 * @param {string} resolvedValue The resolved dynamic data value.
 */
export declare const addCache: (value: string, postId: string, resolvedValue: string) => void;
/**
 * Gets resolved dynamic data value from a cache object based on a post ID and a JSON-like format value.
 *
 * @since ??
 *
 * @param {string} value String that represents the dynamic data in JSON-like format.
 * @param {string} postId Current post ID.
 *
 * @returns {string|null} The resolved dynamic data value. Will return `null` on failure.
 */
export declare const getCache: (value: string, postId: string) => string | null;
