import { type BatchRequestOptions, type ResponseData } from './types';
/**
 * Create a batch request to the server.
 *
 * @since ??
 *
 * @param {string} value String that represents the dynamic data in JSON-like format.
 * @param {string} postId Current Post ID.
 * @param {BatchRequestOptions} [options] The `options` parameter to set abort signal.
 *
 * @returns {Promise<ResponseData>}
 */
export declare const batchRequest: (value: string, postId: string, options?: BatchRequestOptions) => Promise<ResponseData>;
