import { type AllowedHTML } from './types';
/**
 * Sanitizes HTML code by removing any tags or attributes that are not allowed.
 *
 * @since ??
 *
 * @param {string} html The HTML string that needs to be sanitized.
 * @param {object} allowedHTML An object that specifies which HTML tags and attributes are allowed. The object key is
 * the HTML tag name and the value is an array of allowed attributes for that tag.
 *
 * @returns {string} A sanitized version of the input `html` string.
 */
export declare const wpKses: (html: string, allowedHTML?: AllowedHTML) => string;
