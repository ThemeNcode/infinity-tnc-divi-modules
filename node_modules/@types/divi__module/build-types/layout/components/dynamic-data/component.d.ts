import { type DynamicDataProps, type JSXElement } from './types';
/**
 * Dynamic Data Component.
 *
 * This component is used to retrieve dynamic data from the server and then passed the resolved dynamic data info
 * to the children function. The info object contains the following properties:
 *
 * - `isDynamic`: Flag to indicate if the value is dynamic or not.
 * - `isLoading`: Flag to indicate if the dynamic data is still loading.
 * - `isError`: Flag to indicate if there was an error while fetching the dynamic data.
 * - `resolvedValue`: The resolved value of the dynamic data. Or it will be the original value
 * when the `isDynamic` is false. Or it will be a `null` when the `isError` is true.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.value The dynamic data value.
 * @param {ReactNode} props.children The children function.
 * @param {boolean} props.showLoader Optional. Whether to show the loader or not. Defaults to `true`.
 * @param {string} props.loaderType Optional. The loader type. Defaults to `text`.
 * @param {string} props.loaderClassName Optional. The loader CSS class name(s).
 * @param {object} props.loaderInlineStyle Optional. The loader inline style.
 * @param {number} props.loaderWidth Optional. The loader width.
 * @param {number} props.loaderHeight Optional. The loader height.
 *
 * @returns {JSXElement} The component.
 */
declare const DynamicData: ({ value, children, showLoader, loaderType, loaderClassName, loaderInlineStyle, loaderWidth, loaderHeight, }: DynamicDataProps) => JSXElement;
export { DynamicData };
