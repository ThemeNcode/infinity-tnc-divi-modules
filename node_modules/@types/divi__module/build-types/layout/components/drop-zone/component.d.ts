import { type ReactElement } from 'react';
import { type DropZoneProps } from './types';
import './style.scss';
/**
 * Dropzone.
 *
 * Drop target indicator (to visually display where the target is) and spacer (to create space
 * before or after module).
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.draggedModuleType The type of the module being dragged.
 * One of `root`, `section`, `fullwidth-section`, `specialty-section`, `row`, `row-inner`, `column`,
 * `specialty-column`, `column-inner`, `module`, `fullwidth-module`, `child-module`, `unsupported` or `layout`.
 * @param {object} props.draggedModuleStyles The styles of the module being dragged.
 * @param {string} props.position The position of the drop zone. One of `top` or `bottom` or `inside`.
 * @param {boolean} props.isWireframe Whether the drop zone is for wireframe or not.
 * @param {string} props.type The type of the drop zone. One of `spacer` or `indicator`.
 * @param {Function} props.setDraggedModuleDropZone Function to set the dragged module drop zone.
 *
 * @returns {ReactElement}
 */
declare const DropZone: (props: DropZoneProps) => ReactElement;
export { DropZone, };
