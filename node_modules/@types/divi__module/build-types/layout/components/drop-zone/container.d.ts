import { type ReactElement } from 'react';
import { type DropZoneContainerProps } from './types';
/**
 * Dropzone Container.
 *
 * Container that pass necessary state to `<DropZoneContainer />` component.
 *
 * @since ??
 *
 * @param {object} props Container props.
 * @param {string} props.position Dropzone position. One of `top`, `bottom`, or `inside`.
 * @param {boolean} props.isWireframe Whether the module is in wireframe mode.
 * @param {object} props.draggedModuleType The type of the dragged module.
 * @param {object} props.draggedModuleStyles The styles of the dragged module.
 * @param {string} props.type Dropzone type. One of `spacer` or `indicator`.
 *
 * @returns {ReactElement} The Dropzone component.
 */
export declare const DropZoneContainer: (props: DropZoneContainerProps) => ReactElement;
