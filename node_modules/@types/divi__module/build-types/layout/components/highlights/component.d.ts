import { type ReactElement } from 'react';
import './style.scss';
/**
 * Highlight selected/hovered block (Section, Row, or Module).
 *
 * Provides a visual indicator of the Builder element in focus.
 * This helps to convey which element(s) will change when actions are performed.
 *
 * @since ??
 *
 * @returns {ReactElement}
 */
export declare const Highlights: () => ReactElement;
