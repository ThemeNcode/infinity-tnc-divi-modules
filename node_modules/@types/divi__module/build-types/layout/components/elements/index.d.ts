import { type ReactElement } from 'react';
import { type AttrState, type Breakpoint, type Module, type ModuleMetadata } from '@divi/types';
import { type ModuleElementsComponentParams, type ModuleElementsRenderParams, type ModuleElementsScriptDataParams, type ModuleElementsStyleParams } from './types';
/**
 * Module Elements.
 *
 * Simplify module's element rendering by automatically includes relevant features
 * such as Responsive Content, Dynamic Content, and Inline Editor.
 * Contains `render` method that can be used to render the module element(s).
 *
 * @since ??
 *
 * @template TModuleAttrs - Module attributes type.
 *
 * @param {object} params Module elements (constructor) parameters.
 * @param {object} params.attrs Module attributes.
 * @param {string} params.id Module ID.
 * @param {object} params.moduleMetadata Module metadata.
 * @param {string} params.breakpoint Module breakpoint.
 * @param {string} params.state Module state.
 *
 * @returns {object} Module elements instance.
 */
export declare class ModuleElements<TModuleAttrs> {
    attrs: Record<string, Module.Element.Attributes>;
    id: string;
    moduleMetadata: ModuleMetadata;
    breakpoint: Breakpoint;
    state: AttrState;
    orderClass: string;
    wrapperOrderClass: string;
    orderId: string;
    /**
     * Create new instance of ModuleElements class.
     *
     * @param {object} param0 Module elements parameters.
     * @param {object} param0.attrs Module attributes.
     * @param {string} param0.id Module ID.
     * @param {object} param0.moduleMetadata Module metadata.
     * @param {string} param0.breakpoint Module breakpoint.
     * @param {string} param0.state Module state.
     */
    constructor({ attrs, id, moduleMetadata, breakpoint, state, }: {
        attrs: TModuleAttrs;
        id: string;
        moduleMetadata: ModuleMetadata<TModuleAttrs>;
        breakpoint?: Breakpoint;
        state?: AttrState;
    });
    /**
     * Render the module element with the specified attributes.
     *
     * @since ??
     *
     * @param {ModuleElementsRenderParams} param Render parameters.
     *
     * @returns {ReactElement} The rendered element.
     */
    render(param: ModuleElementsRenderParams): ReactElement;
    /**
     *
     */
    setOrderClass(orderClass: string): void;
    /**
     * Set the wrapper order class.
     *
     * @since ??
     *
     * @param {string} wrapperOrderClass The wrapper order class.
     */
    setWrapperOrderClass(wrapperOrderClass: string): void;
    /**
     * Set the wrapper order class.
     *
     * @since ??
     *
     * @param {string} orderId The wrapper order class.
     */
    setOrderId(orderId: string): void;
    /**
     *
     * ScriptData.
     *
     * Call the module element script data with the specified attributes.
     *
     * @since ??
     *
     * @param {object} props Component props.
     * @param {string} props.attrName Attribute name for module element.
     * @param {object} props.scriptDataProps Script data attributes for the module elements.
     * @param {Function} props.attrsResolver A function that will be called to filter/resolve the attributes data.
     *
     * @returns {ReactElement} The rendered element.
     */
    scriptData({ attrName, scriptDataProps, attrsResolver, }: ModuleElementsScriptDataParams): ReactElement;
    /**
     * Applies style to a React element based on provided parameters.
     *
     * @param {object} params The parameters for applying style.
     * @param {string} params.attrName The name of the attribute.
     * @param {object} params.styleProps The additional style properties to be applied.
     * @returns {ReactElement} - The styled React element.
     */
    style({ attrName, styleProps, }: ModuleElementsStyleParams): ReactElement;
    /**
     * Create style components based on provided parameters.
     *
     * @param {object} params The parameters for styling the components.
     * @param {string} params.attrName The name of the attribute.
     * @param {object} params.styleComponentsProps The props for styling the components.
     * @returns {ReactElement} - The styled components.
     */
    styleComponents({ attrName, styleComponentsProps }: ModuleElementsComponentParams): ReactElement;
}
