import { type ReactElement } from 'react';
import { type Props } from './types';
import './style.scss';
/**
 * Mousetrap overlay.
 *
 * We use this to capture click events on Builder modules and override or redirect them as needed.
 *
 * Module mousetrap component.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {Function} props.onClickMousetrap Callback function to handle `click` event.
 *
 * @returns {ReactElement}
 */
declare const Mousetrap: ({ onClickMousetrap }: Props) => ReactElement;
export { Mousetrap, };
