import { type ReactElement } from 'react';
import { type DynamicDataLoaderProps } from './types';
import './styles.scss';
/**
 * Dynamic Data Loader.
 *
 * It can render either an image or text loader based on the `loaderType` prop. If the
 * `loaderType` is "image", it renders an SVG image loader. If the `loaderType` is "text", it renders a
 * text loader with randomized text widths.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.loaderType Optional. The loader type. One of `image` or `text`. Defaults to `text`.
 * @param {string} props.loaderClassName Optional. The loader CSS class name(s).
 * @param {object} props.loaderInlineStyle Optional. The loader inline style.
 * @param {number} props.loaderWidth Optional. The loader width.
 * @param {number} props.loaderHeight Optional. The loader height.
 *
 * @returns {ReactElement}
 */
export declare const DynamicDataLoader: ({ loaderType, loaderClassName, loaderInlineStyle, loaderWidth, loaderHeight, }: DynamicDataLoaderProps) => ReactElement;
