import { type ReactElement } from 'react';
import { type ModuleAttrs } from '@divi/types';
import { type ModuleContainerProps } from '../../../types';
import './style.scss';
/**
 * Wireframe Container.
 *
 * Wireframe component wrapped by Higher-Order Component container for easy component usage.
 *
 * @since ??
 *
 * @returns {ReactElement} Wireframe component.
 */
export declare const WireframeContainer: (props: ModuleContainerProps<ModuleAttrs>) => ReactElement;
