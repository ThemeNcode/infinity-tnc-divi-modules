import { type ReactElement } from 'react';
import { type ModuleProps } from '../../../types';
import './style.scss';
/**
 * Wireframe.
 *
 * Module wireframe component.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {object} props.attrs Module attributes.
 * @param {Array<string>} props.childrenIds Module children IDs.
 * @param {boolean} props.hasModule Whether the module has a module.
 * @param {string} props.id Module ID.
 * @param {boolean} props.canClone Whether the module can be cloned.
 * @param {boolean} props.isCommandPressed Whether the command key is pressed.
 * @param {boolean} props.isDragging Whether the module is being dragged.
 * @param {boolean} props.isDragged Whether the module is being dragged.
 * @param {boolean} props.isDraggedToOtherModule Whether the module is being dragged to another module.
 * @param {boolean} props.isHovered Whether the module is hovered.
 * @param {boolean} props.isSelected Whether the module is selected.
 * @param {string} props.modalName Module options modal name.
 * @param {string} props.name Module name.
 * @param {string} props.originalName Module original name.
 * @param {string} props.draggedStyles Module dragged styles.
 * @param {Function} props.selectModule Select module callback. Defaults to `lodash`'s `noop`.
 * @param {Function} props.deselectModule Deselect module callback. Defaults to `lodash`'s `noop`.
 * @param {string} props.title Module title.
 *
 * @returns {ReactElement}
 */
declare const Wireframe: <TProps extends ModuleProps<TProps["attrs"]>>(props: TProps) => ReactElement;
export { Wireframe, };
