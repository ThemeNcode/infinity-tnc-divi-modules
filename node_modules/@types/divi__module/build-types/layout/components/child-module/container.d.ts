import { type ReactElement } from 'react';
import { type ChildModuleContainerProps } from './types';
/**
 * Container component for `<ChildModule />` component that automatically pass the data fetched
 * from the store to the `<ChildModule />` component.
 *
 * @since ??
 *
 * @param {ChildModuleContainerProps} props Component props.
 *
 * @returns {ReactElement} ChildModuleContainer component.
 */
export declare const ChildModuleContainer: (props: ChildModuleContainerProps) => ReactElement;
