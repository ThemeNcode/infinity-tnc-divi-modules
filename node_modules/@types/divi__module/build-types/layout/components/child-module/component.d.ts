import { type ReactElement } from 'react';
import { type ChildModuleProps } from './types';
/**
 * Component to render each child module inside `<ChildModules />` component.
 *
 * @since ??
 *
 * @param {ChildModuleProps} props Component props.
 *
 * @returns {ReactElement} Child module component.
 */
export declare const ChildModule: ({ attrs, childrenIds, hasModule, id, moduleOrderIndex, innerHTML, isFirst, isLast, isUnsupported, moduleComponent, name, originalName, parentAttrs, parentDefaultAttrs, parentId, parentName, postId, shortcodeTags, parentCallback, siblingAttrs, siteSettings, pageSettings, urls, }: ChildModuleProps) => ReactElement;
