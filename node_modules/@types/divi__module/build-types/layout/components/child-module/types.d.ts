import { type ReactElement } from 'react';
import { type ImmutableArray } from 'seamless-immutable';
import { type ModuleAttrs } from '@divi/types';
export type ChildModuleContainerPropNames = 'id' | 'isFirst' | 'isLast' | 'prevId' | 'nextId' | 'parentCallback';
export type ChildModuleContainerProps = Pick<ChildModuleProps, ChildModuleContainerPropNames> & {
    isWireframe: boolean;
};
export interface ChildModuleProps {
    attrs: ModuleAttrs;
    childrenIds: ImmutableArray<string>;
    hasModule: boolean;
    id: string;
    moduleOrderIndex?: number;
    prevId?: string;
    nextId?: string;
    innerHTML: string;
    isFirst: boolean;
    isLast: boolean;
    isUnsupported: boolean;
    postId: string;
    shortcodeTags: string[];
    siblingAttrs?: {
        previous?: ModuleAttrs;
        next?: ModuleAttrs;
    };
    urls: {
        admin: string;
        ajax: string;
        adminOptionsGeneralUrl: string;
        currentPageUrl: string;
        forgotPasswordUrl: string;
        homeUrl: string;
        loginFormUrl: string;
    };
    moduleComponent: (props: any) => ReactElement;
    name: string;
    originalName: string;
    parentDefaultAttrs: ModuleAttrs;
    parentAttrs: ModuleAttrs;
    parentId: string;
    parentName: string;
    parentCallback?: (params?: unknown) => void;
    siteSettings: {
        gmtOffsetString?: string;
    };
    pageSettings?: {
        sectionBackgroundColor?: string;
    };
}
