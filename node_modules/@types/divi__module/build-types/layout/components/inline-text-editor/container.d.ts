import { type ReactElement } from 'react';
import { type InlineTextEditorContainerProps } from './types';
/**
 * Inline Text Editor field Container.
 *
 * This container is responsible for handling the state of the Inline Text Editor field.
 *
 * @since ??
 *
 * @param {object} props Container props.
 * @param {string} props.text Inline editor text content.
 * @param {string} props.attrName Attribute name.
 * @param {string} props.attrSubName Attribute subname.
 * @param {object} props.additionalProps Optional additional props for inline editor. Defaults to `{}`.
 * @param {boolean} props.allowHTML Optional. Whether to allow HTML in content. Defaults to `false`.
 * @param {string} props.className CSS class name(s).
 * @param {string} props.defaultText Optional default content.
 * @param {Function} props.callbackEditingStatusChange Callback function.
 * @param {Function} props.handleBlur Optional callback function to call on `blur` event.
 * @param {Function} props.handleClick Optional callback function to call on content `click` event.
 * @param {Function} props.handleDoubleClick Optional callback function to handle `double click` event.
 * @param {Function} props.handleKeyDown Optional callback function to handle `key down` event.
 * @param {Function} props.handleKeyUp Optional callback function to handle `key up` event.
 * @param {Function} props.handleMouseDown Optional callback function to handle `mouse down` event.
 * @param {Function} props.handleMouseUp Optional callback function to handle `mouse up` event.
 * @param {Function} props.handlePaste Optional callback function to handle `paste` event.
 * @param {Function} props.handleShortcode Optional callback function to handle `shortcode` processing.
 * @param {string} props.moduleId Module ID.
 * @param {string} props.tagName HTML tag name for the content wrapper element.
 *
 * @returns {ReactElement}
 */
export declare const InlineTextEditorContainer: ({ additionalProps, allowHTML, attrName, attrSubName, className, callbackEditingStatusChange, defaultText, handleBlur: handleBlurInherited, handleClick: handleClickInherited, handleDoubleClick: handleDoubleClickInherited, handleKeyDown: handleKeyDownInherited, handleKeyUp: handleKeyUpInherited, handleMouseDown: handleMouseDownInherited, handleMouseUp: handleMouseUpInherited, handlePaste: handlePasteInherited, handleShortcode: handleShortcodeInherited, moduleId, tagName, text, }: InlineTextEditorContainerProps) => ReactElement;
