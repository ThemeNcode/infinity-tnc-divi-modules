import { type ExpandShorthandImportantParams, type ExpandShorthandImportantReturn } from './types';
/**
 * Returns expanded shorthand property values for important.
 *
 * This functions expands any shorthand property values for important
 * based on the provided `$propertySelectorsShorthandMap`.
 * If the given important value is a boolean, it is returned as is.
 * If important is an array and it contains any shorthand property, theses are expanded to longhand properties.
 * We then update the values in the important array and return the updated important array.
 *
 * @since ??
 *
 * @param {object} params Function parameters.
 * @param {object|boolean} params.important A boolean `true/false`, or an object with property
 * name as the key and selectors as the value.
 * @param {object} params.propertySelectorsShorthandMap Optional. This is the map of shorthand
 * properties to their longhand properties. Default `{}`.
 *
 * @returns {boolean|object}
 */
export declare const getExpandedShorthandImportant: ({ important, propertySelectorsShorthandMap, }: ExpandShorthandImportantParams) => ExpandShorthandImportantReturn;
