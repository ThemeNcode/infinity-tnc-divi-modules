import { type GetStatementsParams } from './types';
/**
 * Get CSS statements based on given params.
 *
 * @since ??
 *
 * @param {object} param0 Get CSS statements function parameter.
 * @param {object} param0.selectors Determine the selector for a given breakpoint and state.
 * @param {object} param0.attr Attribute values for different breakpoints and states.
 * @param {object} param0.defaultPrintedStyleAttr Provide default attribute values for different breakpoints and states.
 * @param {boolean} param0.important Determine whether the CSS statements should be marked as important or not.
 * @param {string} param0.orderClass Optional. The selector class name.
 * @param {Function} param0.declarationFunction Function that generates the CSS declaration for each attribute value,
 * taking in parameters such as attrValue, defaultAttrValue, important, returnType, breakpoint, and state.
 * @param {Function} param0.selectorFunction Function used to modify the selector for CSS statements by taking in
 * parameters such as the current selector, breakpoint, state, and attribute values.
 * @param {object} param0.propertySelectors Assign CSS properties to certain breakpoints and states, enabling you
 * to have distinct property values based on conditions like different font sizes for breakpoints or various colors
 * for states like hover or active.
 * @param {object} param0.propertySelectorsShorthandMap A mapping to create shorter names for CSS property selectors,
 * making your code clearer and more compact by easily accessing the full property selectors for various breakpoints
 * and states.
 *
 * @returns {string} The CSS statements.
 */
export declare const getStatements: <TParams extends GetStatementsParams<TParams["attr"], TParams["important"]>>({ selectors, attr, defaultPrintedStyleAttr, important, declarationFunction, selectorFunction, propertySelectors, propertySelectorsShorthandMap, orderClass, }: TParams) => string;
