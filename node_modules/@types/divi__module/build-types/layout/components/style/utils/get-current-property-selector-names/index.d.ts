import { type GetCurrentPropertySelectorNamesParams } from './types';
/**
 * Get Current property selector names.
 *
 * It is used to get property selector names of current breakpoint + state based on given property selectors..
 *
 * @since ??
 *
 * @param {object} props The Selector parameters.
 * @param {string} [props.breakpoint] Breakpoint name.
 * @param {string} [props.state] State name.
 * @param {object} [props.propertySelectors] Property selectors object.
 *
 * @returns {string[]} Array of property selector names.
 *
 * @example
 * ```tsx
 * const args = {
 *  "propertySelectors": {
 *    "desktop": {
 *      "value": {
 *        "font-size": ".module-desktop-font-size"
 *      }
 *    }
 *  },
 *  "breakpoint": "desktop",
 *  "state": "value"
 * };
 * getCurrentPropertySelectorNames(args)
 * // Returns ['font-size']
 * ```
 *
 * @example
 * ```tsx
 * const args = {
 *  "propertySelectors": {
 *    "tablet": {
 *      "value": {
 *        "font-size": ".module-tablet-font-size"
 *      }
 *    }
 *  },
 *  "breakpoint": "desktop",
 *  "state": "value"
 * };
 * getCurrentPropertySelectorNames(args)
 * // Returns []
 * ```
 */
export declare const getCurrentPropertySelectorNames: ({ propertySelectors, breakpoint, state, }: GetCurrentPropertySelectorNamesParams) => string[];
