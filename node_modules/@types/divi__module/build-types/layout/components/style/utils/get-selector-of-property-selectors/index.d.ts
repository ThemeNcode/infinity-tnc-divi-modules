import { type CssProperties } from '@types/divi__style-library';
import { type GetSelectorOfPropertySelectorsParams } from './types';
/**
 * Get Selector.
 *
 * It is used to get CSS selector from property selectors
 * based on sets of selectors, current state, and property name given.
 *
 * @since ??
 *
 * @param {object} props The Selector parameters.
 * @param {string} [props.breakpoint] Breakpoint name.
 * @param {string} [props.state] State name.
 * @param {object} [props.selectors] Selector object.
 * @param {string} [props.propertyName] Property name.
 * @param {string} [props.orderClass] Optional. Module order class name.
 *
 * @returns {string} Classname.
 *
 * @example
 * ```tsx
 * const args = {
 *  "selectors": {
 *    "desktop": {
 *      "value": {
 *        "animation-delay": ".module-desktop-value-animation-delay",
 *        "opacity": ".module-desktop-value-opacity"
 *      },
 *    }
 *  },
 *  "breakpoint": "desktop",
 *  "state": "value",
 *  "propertyName": "animation-delay"
 * };
 * getSelectorOfPropertySelectors(args)
 * // Returns '.module-desktop-value-animation-delay'
 * ```
 */
export declare const getSelectorOfPropertySelectors: <TPropertyName extends CssProperties>({ breakpoint, state, selectors, propertyName, orderClass, }: GetSelectorOfPropertySelectorsParams<TPropertyName>) => string;
