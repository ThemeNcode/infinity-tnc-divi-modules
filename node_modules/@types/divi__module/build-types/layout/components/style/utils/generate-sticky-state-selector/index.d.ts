/**
 * Generates a sticky state selector based on the provided parameters.
 *
 * @since ??
 *
 * @param {object} options The options for generating the sticky state selector.
 * @param {string} options.breakpointBaseSelector The base selector for the breakpoint.
 * @param {string} [options.orderClass] The order class for the selector.
 *
 * @returns {string} The generated sticky state selector.
 */
export declare const generateStickyStateSelector: ({ breakpointBaseSelector, orderClass, }: {
    breakpointBaseSelector: string;
    orderClass?: string;
}) => string;
