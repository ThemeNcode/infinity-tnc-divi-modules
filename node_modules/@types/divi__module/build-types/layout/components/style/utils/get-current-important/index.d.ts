import { type GetCurrentImportantParams, type PropertyImportant } from './types';
/**
 * Get current property important based on given breakpoint and state.
 *
 * @since ??
 *
 * See below for fallback flow.
 * ----------------------------------.
 * |       | value | hover | sticky |
 * ----------------------------------.
 * Desktop |   *   |  <--  |  <--   |
 * ----------------------------------.
 * Tablet  |   ^   |  <--  |  <--   |
 * ----------------------------------.
 * Phone   |   ^   |  <--  |  <--   |
 * ----------------------------------.
 *
 * @param {object} params Function parameters.
 * @param {object|boolean} params.important A boolean `true/false`, or an object with property
 * name as the key and selectors as the value. Note if an object is passed, then this should be in expanded format.
 * Use `getExpandedShorthandImportant` to expand shorthand values.
 * @param {string} params.breakpoint The breakpoint of the selector. One of `desktop`, `tablet`, or `phone`.
 * @param {string} params.state The state of the selector. One of `value`, `hover`, or `sticky`.
 *
 * @returns {boolean|object}
 */
export declare const getCurrentImportant: ({ important, breakpoint, state, }: GetCurrentImportantParams) => boolean | PropertyImportant;
