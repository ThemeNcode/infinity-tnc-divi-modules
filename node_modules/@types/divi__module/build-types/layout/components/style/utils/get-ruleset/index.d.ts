import { type GetRulesetParams } from './types';
/**
 * Get Rule set.
 *
 * It is used to get CSS ruleset based on given style selector and style declaration..
 *
 * @since ??
 *
 * @param {object} props The Rule set params.
 * @param {string} [props.selector] Style selector name.
 * @param {string} [props.declaration] Style declaration value.
 *
 * @returns {string} Background classname.
 *
 * @example
 * ```tsx
 * const args = {
 *  "selector": ".et_pb_cta_0",
 *  "declaration": "margin: 10px;"
 * };
 * getRuleset(args)
 * // Returns '.et_pb_cta_0 {margin: 10px;}'
 * ```
 */
export declare const getRuleset: ({ selector, declaration, }: GetRulesetParams) => string;
