/**
 * Get CSS At-rules based on given breakpoint name.
 *
 * This will return the CSS media query for the given breakpoint name.
 *
 * @since ??
 *
 * @param {string} breakpoint Breakpoint name.
 *
 * @returns {string|boolean} Checkbox rule according to breakpoint or false if not found.
 */
export declare const getAtRules: (breakpoint: string) => string | boolean;
