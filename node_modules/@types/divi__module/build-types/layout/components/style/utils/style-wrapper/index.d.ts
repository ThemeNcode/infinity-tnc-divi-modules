import { type ReactElement } from 'react';
import { type StyleWrapperProps } from '../../types';
/**
 * Style Wrapper.
 *
 * Utils component to wrap style component output.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {object} props.attr Style attribute.
 * @param {ReactNode} props.children Child elements to render.
 *
 * @returns {ReactElement}
 */
export declare const StyleWrapper: <TProps extends StyleWrapperProps<TProps["attr"]>>({ attr, children, }: TProps) => ReactElement;
