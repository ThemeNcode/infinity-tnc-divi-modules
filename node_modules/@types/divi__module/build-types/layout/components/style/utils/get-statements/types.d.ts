import { type CssProperties, type CssShorthandProperties } from '@types/divi__style-library';
import { type FormatBreakpointStateAttr } from '@divi/types';
import { type SelectorFunctionParams } from '../../types';
import { type GetSelectorParams } from '../get-selector/types';
export type StatementsCssProperties = CssProperties | 'ungrouped';
export type PropertySelector = Partial<Record<CssProperties, string>>;
export type PropertySelectors = FormatBreakpointStateAttr<PropertySelector>;
export type PropertySelectorsShorthand = Partial<Record<CssShorthandProperties, StatementsCssProperties[]>>;
export interface GetStatementsParams<TAttr, TImportant = boolean> {
    selectors: GetSelectorParams['selectors'];
    attr: TAttr;
    defaultPrintedStyleAttr?: TAttr;
    important?: TImportant;
    declarationFunction: (declarationParams: unknown) => string | Record<string, string>;
    selectorFunction?: (params: SelectorFunctionParams<TAttr>) => string;
    propertySelectors?: PropertySelectors;
    propertySelectorsShorthandMap?: PropertySelectorsShorthand;
    orderClass?: string | null;
}
