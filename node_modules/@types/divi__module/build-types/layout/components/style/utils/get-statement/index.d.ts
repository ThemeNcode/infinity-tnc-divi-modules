import { type GetStatementParams } from './types';
/**
 * Get CSS statement based on given At-rules and ruleset.
 *
 * @since ??
 *
 * @param {object} param0 CSS Statement parameter.
 * @param {string|boolean} param0.atRules Represents the at-rule to be used in the CSS statement such as `@media`.
 * @param {string} param0.ruleset The CSS rules to be included in the statement. It can contain one or more CSS rules.
 *
 * @returns {string} The CSS statement.
 */
export declare const getStatement: ({ atRules, ruleset, }: GetStatementParams) => string;
