import { type GroupDeclarationByPropertySelectorNamesParams } from './types';
/**
 * Group object of declarations based on given group name. Group declaration that has no group name
 * into `otherDeclaration` group. Initially created to group declaration that has property-specific selector.
 *
 * @since ??
 *
 * @param {object} param0 Function parameter.
 * @param {string[]} param0.propertySelectorNames An array of CSS property names that should be grouped together.
 * @param {object} param0.declarations An object that contains CSS property-value pairs.
 *
 * @returns {object} An object of grouped declarations. The keys of the object are the property selector names,
 * and the values are the corresponding CSS declarations. If there are any declarations that do not
 * match any of the property selector names, they are grouped under the key "ungrouped".
 */
export declare const groupDeclarationsByPropertySelectorNames: ({ propertySelectorNames, declarations, }: GroupDeclarationByPropertySelectorNamesParams) => Record<string, string>;
