/**
 * Generates a hover state selector based on the provided parameters.
 *
 * @since ??
 *
 * @param {object} options The options for generating the hover state selector.
 * @param {string} options.breakpointBaseSelector The base selector for the breakpoint.
 * @param {string} [options.orderClass] The order class for the selector.
 *
 * @returns {string} The generated selector string.
 */
export declare const generateHoverStateSelector: ({ breakpointBaseSelector, orderClass, }: {
    breakpointBaseSelector: string;
    orderClass?: string;
}) => string;
