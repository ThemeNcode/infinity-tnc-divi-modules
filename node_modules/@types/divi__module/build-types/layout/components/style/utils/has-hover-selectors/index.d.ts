import type { GetSelectorParams } from '../get-selector/types';
import type { PropertySelectors } from '../get-statements/types';
/**
 * Checks if any of the given selectors have hover properties.
 *
 * @since ??
 *
 * @param {GetSelectorParams['selectors'] | PropertySelectors} selectors The array of selectors to check.
 * @returns {boolean} Returns `true` if any of the selectors have a `hover` property, otherwise returns `false`.
 */
export declare const hasHoverSelectors: (selectors: GetSelectorParams['selectors'] | PropertySelectors) => boolean;
