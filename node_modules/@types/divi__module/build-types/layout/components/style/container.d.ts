import { type ReactElement } from 'react';
import { type StyleContainerProps } from './types';
/**
 * Style Container.
 *
 * Container Component to wrap style component output.
 *
 * This container component intended pass the mode and state props to all child components.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.mode Renderer mode, one of `builder` or `frontend`.
 * @param {string} props.state Attr state, once of `value`, `hover` or `sticky`.
 * @param {ReactElement|Array<ReactElement>} props.children Optional child elements to render.
 * @param {boolean} props.noStyleTag Optional. Whether to use a style tag. Defaults to `undefined`.
 * @param {boolean} props.noEmptyStyle Optional. Whether to return `null` if no children are available. Default: `true`.
 *
 * @returns {ReactElement}
 *
 * @example Basic usage.
 * ```tsx
 * <StyleContainer mode="frontend" state="value">
 *    {elements.style({
 *      attrName: 'image',
 *      styleProps: {
 *        disabledOn: {
 *          disabledModuleVisibility: settings?.disabledModuleVisibility ?? false,
 *        }
 *      }
 *    })}
 * </StyleContainer>
 * ```
 *
 * @example With no style tag.
 * ```tsx
 * <StyleContainer mode="frontend" state="hover" noStyleTag={true}>
 *    {elements.style({
 *      attrName: 'content',
 *    })}
 * </StyleContainer>
 * ```
 *
 * @example With empty style.
 * ```tsx
 * <StyleContainer mode="builder" state="sticky" noEmptyStyle={false}>
 *    {elements.style({
 *      attrName: 'title',
 *    })}
 * </StyleContainer>
 * ```
 */
export declare const StyleContainer: ({ mode, state, children, noStyleTag, noEmptyStyle, }: StyleContainerProps) => ReactElement;
