import { type ReactElement } from 'react';
import { type ModuleOptionsProps } from './types';
import './style.scss';
/**
 * Module Options.
 *
 * Component to render module options, so we can showcase how `divi/edit-post` store works.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {boolean} props.isWireframe Whether the module is in wireframe mode.
 * @param {boolean} props.canClone Whether the module can be cloned.
 * @param {boolean} props.hasChooseColumn Whether the module has choose column option.
 * @param {string} props.moduleId Module ID.
 * @param {string} props.moduleName Module name.
 * @param {string} props.moduleType Module type.
 * @param {string} props.basicModuleType Basic module type.
 * @param {string} props.basicModuleTypeLabel Basic module type label.
 * @param {string} props.settingsModalName Settings modal name.
 * @param {Function} props.onMouseUp Callback function to handle `mouse up` event.
 * @param {Function} props.onMouseDown Callback function to handle `mouse down` event.
 * @param {Function} props.removeModule Callback function to handle `remove module` event.
 * @param {Function} props.openModal Callback function to handle `open modal` event.
 * @param {Function} props.cloneModule Clone module handler.
 * @param {boolean} props.unsupported Whether the module is unsupported.
 * @param {string} props.shortcodeName Shortcode name.
 * @param {boolean} props.isGlobal Whether the module is global.
 * @param {boolean} props.isGlobalChild Whether the module is global child.
 * @param {boolean} props.userCanEditGlobalItems Whether the user can edit global items.
 * @param {string} props.windowLocation Window location where the module options used.
 *
 * @returns {ReactElement} Module Options component.
 */
declare const ModuleOptions: ({ basicModuleType, basicModuleTypeLabel, canClone, cloneModule, hasChooseColumn, isGlobal, isGlobalChild, isWireframe, moduleId, moduleName, moduleType, onMouseDown, onMouseUp, openModal, removeModule, settingsModalName, shortcodeName, unsupported, userCanEditGlobalItems, windowLocation, }: ModuleOptionsProps) => ReactElement;
export { ModuleOptions, };
