import { type ReactElement } from 'react';
import { type ModuleOptionsContainerProps } from './types';
/**
 * Module Options Container.
 *
 * Wrapper component for that passes all the necessary state to the `ModuleOptions` component.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {boolean} props.isWireframe Optional. Whether the builder is in wireframe mode.
 * @param {string} props.moduleId Module ID.
 * @param {string} props.moduleType Module type.
 * @param {boolean} props.unsupported Whether the module is supported.
 *
 * @returns {ReactElement} ModuleOptions component.
 */
export declare const ModuleOptionsContainer: (props: ModuleOptionsContainerProps) => ReactElement;
