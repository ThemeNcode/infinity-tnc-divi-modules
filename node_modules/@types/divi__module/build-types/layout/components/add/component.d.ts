import { type ReactElement } from 'react';
import { type Props } from './types';
import './style.scss';
/**
 * Add Module button component.
 *
 * Dummy component to render add module button so we can showcase how module addition works without
 * address prop.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.moduleId Module ID.
 * @param {string} props.basicModuleTypeLabel Module label.
 * @param {boolean} props.addChild Optional. Whether to add as child element.
 * @param {string} props.basicModuleType One of `root`, `section`, `row`, `column`, `module`, `unsupported` or `layout`.
 *
 * @returns {ReactElement} Module add component.
 */
declare const ModuleAdd: (props: Props) => ReactElement;
export { ModuleAdd, };
