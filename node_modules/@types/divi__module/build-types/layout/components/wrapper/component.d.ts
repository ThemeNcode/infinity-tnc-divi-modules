import { type ReactElement } from 'react';
import { type ModuleWrapperProps } from './types';
import './style.scss';
/**
 * Module wrapper.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {ReactNode} props.children Children to render.
 * @param {string} props.classname CSS classname(s) for the module.
 * @param {string} props.id Module ID.
 * @param {boolean} props.isDragging Is module being dragged.
 * @param {boolean} props.isDragged Is module dragged.
 * @param {boolean} props.isHovered Is module hovered.
 * @param {object} props.styles Module styles.
 * @param {string} props.dataColumnStructure Module column structure.
 * @param {string} props.dataColumnType Module column type.
 * @param {string} props.view View type.
 * @param {object} props.domRef Module DOM reference.
 * @param {object} props.htmlAttrs Module HTML attributes.
 * @param {Function} props.onMouseEnter Mouse enter callback.
 * @param {Function} props.onMouseLeave Mouse leave callback.
 * @param {Function} props.onMouseMove Mouse move callback.
 * @param {Function} props.onContextMenu Context menu callback.
 * @param {string} props.hoveredArea Hovered area.
 * @param {boolean} props.hasDropZone Has drop zone.
 * @param {object} props.draggedModuleStyles Dragged module styles.
 * @param {string} props.draggedModuleType Dragged module type.
 * @param {boolean} props.hasDragPlaceholder Has drag placeholder.
 * @param {object} props.dragPlaceholderStyles Drag placeholder styles.
 * @param {string} props.tag Module tag. Defaults to `div`.
 * @param {boolean} props.hasModuleWrapper Has module wrapper. Defaults to `false`.
 * @param {string} props.wrapperTag Module wrapper tag. Defaults to `div`.
 * @param {object} props.wrapperHtmlAttrs Module wrapper HTML attributes.
 * @param {string} props.wrapperClassname Module wrapper CSS classname(s).
 *
 * @returns  {ReactElement}
 */
declare const ModuleWrapper: ({ children, classname, id, isDragging, isDragged, isHovered, styles, dataColumnStructure, dataColumnType, view, domRef, htmlAttrs, onMouseEnter, onMouseLeave, onMouseMove, onContextMenu, hoveredArea, hasDropZone, draggedModuleStyles, draggedModuleType, hasDragPlaceholder, dragPlaceholderStyles, tag, hasModuleWrapper, wrapperTag, wrapperHtmlAttrs, wrapperClassname, }: ModuleWrapperProps) => ReactElement;
export { ModuleWrapper, };
