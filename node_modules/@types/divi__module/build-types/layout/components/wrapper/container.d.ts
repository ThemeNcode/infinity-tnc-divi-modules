import { type ReactElement } from 'react';
import { type ModuleWrapperContainerProps } from './types';
/**
 * Module Wrapper Container.
 *
 * To keep component purely presentational, callback that uses dispatch / select and rely on given
 * props are moved to container and being calculated here so the component only accept the callback
 * as function props.
 *
 * @since ??
 *
 * @param {object} containerProps Component props.
 * @param {Array<string>} containerProps.childrenIds Children IDs.
 * @param {string} containerProps.name Module name.
 * @param {string} containerProps.moduleType Module type.
 * @param {ReactNode} containerProps.children Children to render.
 * @param {string} containerProps.classname CSS classname(s) for the module.
 * @param {string} containerProps.id Module ID.
 * @param {boolean} containerProps.isDragging Is module being dragged.
 * @param {boolean} containerProps.isDragged Is module dragged.
 * @param {boolean} containerProps.isHovered Is module hovered.
 * @param {object} containerProps.styles Module styles.
 * @param {string} containerProps.dataColumnStructure Module column structure.
 * @param {string} containerProps.dataColumnType Module column type.
 * @param {string} containerProps.view View type.
 * @param {object} containerProps.domRef Module DOM reference.
 * @param {object} containerProps.htmlAttrs Module HTML attributes.
 * @param {Function} containerProps.onMouseEnter Mouse enter callback.
 * @param {Function} containerProps.onMouseLeave Mouse leave callback.
 * @param {Function} containerProps.onMouseMove Mouse move callback.
 * @param {Function} containerProps.onContextMenu Context menu callback.
 * @param {string} containerProps.hoveredArea Hovered area.
 * @param {boolean} containerProps.hasDropZone Has drop zone.
 * @param {object} containerProps.draggedModuleStyles Dragged module styles.
 * @param {string} containerProps.draggedModuleType Dragged module type.
 * @param {boolean} containerProps.hasDragPlaceholder Has drag placeholder.
 * @param {object} containerProps.dragPlaceholderStyles Drag placeholder styles.
 * @param {string} containerProps.tag Module tag. Defaults to `div`.
 * @param {boolean} containerProps.hasModuleWrapper Has module wrapper. Defaults to `false`.
 * @param {string} containerProps.wrapperTag Module wrapper tag. Defaults to `div`.
 * @param {object} containerProps.wrapperHtmlAttrs Module wrapper HTML attributes.
 * @param {string} containerProps.wrapperClassname Module wrapper CSS classname(s).
 * @param {boolean} containerProps.isDraggedToOtherModule Whether this module is module dragged to other module.
 *
 * @returns {ReactElement} ModuleWrapper component.
 */
export declare const ModuleWrapperContainer: (containerProps: ModuleWrapperContainerProps) => ReactElement;
