import { type CSSProperties, type ElementType, type MouseEvent, type MutableRefObject, type ReactNode } from 'react';
import { type DebouncedFunc } from 'lodash';
import { type ImmutableObject } from 'seamless-immutable';
import { type Clipboard, type ModuleType } from '@divi/types';
export type ModuleHoveredArea = 'top' | 'bottom' | 'inside';
export interface ModuleWrapperContainerProps {
    children: ReactNode;
    classname: string;
    childrenIds: string[];
    id: string;
    styles?: CSSProperties;
    isDragged?: boolean;
    isDraggedToOtherModule?: boolean;
    isDragging?: boolean;
    isHovered?: boolean;
    dataColumnStructure?: string;
    dataColumnType?: string;
    name?: string;
    moduleType: ModuleType;
    view?: string;
    domRef?: MutableRefObject<HTMLDivElement>;
    htmlAttrs?: Record<string, string | number>;
    hasModuleClassName?: boolean;
    hasHoveredClassName?: boolean;
    tag?: ElementType;
    hasModuleWrapper?: boolean;
    wrapperTag?: ElementType;
    wrapperHtmlAttrs?: Record<string, string | number>;
    wrapperClassname?: string;
}
export interface ModuleWrapperProps extends ModuleWrapperContainerProps {
    onMouseEnter?: (e: MouseEvent) => void;
    onMouseLeave?: () => void;
    onMouseMove?: DebouncedFunc<(e: MouseEvent) => void>;
    onContextMenu?: (e: MouseEvent) => void;
    hoveredArea?: ModuleHoveredArea;
    hasDropZone?: boolean;
    draggedModuleStyles?: Record<string, string | number>;
    draggedModuleType?: ModuleType;
    hasDragPlaceholder?: boolean;
    dragPlaceholderStyles?: Record<string, string | number>;
    hasModuleWrapper?: boolean;
    wrapperTag?: ElementType;
    wrapperHtmlAttrs?: Record<string, string | number>;
    wrapperClassname?: string;
}
export type ModuleWrapperClipboardItem = ImmutableObject<Clipboard.Item.Module>;
export type ModuleWrapperClipboardItemStyle = ImmutableObject<Clipboard.Item.ModuleAttributes>;
