import { type ReactElement } from 'react';
import type SeamlessImmutable from 'seamless-immutable';
import { type Events, type ModuleAttrs, type ModuleFlatObject } from '@divi/types';
export interface Props {
    clientX: number;
    clientY: number;
    active?: boolean;
    draggedModuleName: string;
    customDragHandle?: ReactElement;
}
export interface DragHandleContainerProps {
    customDragHandle?: React.ComponentType<{
        draggedModule: SeamlessImmutable.ImmutableObject<ModuleFlatObject<ModuleAttrs>>;
    }>;
    dragLocation: Events.Dragged.DragLocation;
}
