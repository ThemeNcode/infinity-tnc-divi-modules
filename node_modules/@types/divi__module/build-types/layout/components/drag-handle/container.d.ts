/// <reference types="react" />
import { type DragHandleContainerProps } from './types';
/**
 * DragHandleContainer component.
 *
 * This is a wrapper component that passes necessary state to the DragHandle component.
 * It uses the useSelect hook to get the cursor position, dragging status, and dragged modules.
 * It also determines the name of the dragged module and creates a custom drag handler if provided.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {object} props.customDragHandle Custom drag handler component.
 * @param {DragHandleContainerProps.dragLocation} props.dragLocation The location of the dragging action happening.
 *
 * @returns {ReactElement} The DragHandle component with necessary props.
 */
export declare const DragHandleContainer: ({ customDragHandle, dragLocation }: DragHandleContainerProps) => JSX.Element;
