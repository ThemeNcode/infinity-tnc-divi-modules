import { type ReactElement } from 'react';
import { type Props } from './types';
import './style.scss';
/**
 * Drag handle.
 *
 * Drag handle component.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {boolean} props.active Whether the drag handle is active or not.
 * @param {number} props.clientX Client X position.
 * @param {number} props.clientY Client Y position.
 * @param {string} props.draggedModuleName Name of the dragged module.
 * @param {ReactElement} props.customDragHandle Custom drag handler component.
 *
 * @returns {ReactElement}
 */
export declare const DragHandle: ({ active, clientX, clientY, draggedModuleName, customDragHandle, }: Props) => ReactElement;
