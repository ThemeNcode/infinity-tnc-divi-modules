import { type ReactElement } from 'react';
import { type Props } from './types';
import './style.scss';
/**
 * Column Divider.
 *
 * Column divider / highlighting column division between column on row component.
 * Always appears if row has no module.
 * Only appears if hovered when row has module.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.columnStructure Column structure of the row.
 * @param {string} props.id Column ID.
 * @param {boolean} props.isRowEmpty Whether the row is empty or not.
 * @param {boolean} props.inner Whether the column divider is for inner row or not.
 * @param {string} props.sectionColumnType Column type of the section.
 *
 * @returns {ReactElement}
 */
declare const ColumnDivider: ({ columnStructure, id, isRowEmpty, inner, sectionColumnType, }: Props) => ReactElement;
export { ColumnDivider, };
