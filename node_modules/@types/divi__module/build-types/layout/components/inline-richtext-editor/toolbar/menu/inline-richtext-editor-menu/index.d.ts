import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
import './styles.scss';
/**
 * Inline rich text editor toolbar menu.
 *
 * This component renders the main rich text editor toolbar
 * menu which contains functionality to customize selected text.
 * The rich text editor toolbar handles switching the UI between different
 * formatting functionality such as changing fonts, text color, adding/removing lists e.t.c.
 *
 * @since ??
 *
 * @example
 * ```tsx
 * const menuItems = [
 *   {
 *     "key": "main-menu-text-bold",
 *     "buttonCommand": "bold",
 *     "iconName": "divi/font-style-bold",
 *     "tooltipText": "Bold Text"
 *   },
 *   {
 *     "key": "main-menu-text-italic",
 *     "buttonCommand": "italic",
 *     "iconName": "divi/font-style-italic",
 *     "tooltipText": "Italic Text"
 *   },
 *   {
 *     "key": "main-menu-text-underline",
 *     "buttonCommand": "underline",
 *     "iconName": "divi/font-style-underline",
 *     "tooltipText": "Underline Text"
 *   },
 * ];
 *
 *
 * const tinymceEditorId = window.divi.data.select('divi/module').getInlineEditorToolbar().tinymceEditorId;
 * const isSelectionChanged = window.divi.data.select('divi/module').getInlineEditorToolbar().isSelectionChanged;
 * const position = window.divi.data.select('divi/module').getInlineEditorToolbar().position;
 *
 * const InlineRichTextEditor = (
 *   <InlineRichTextEditorMenu
 *     animation={true}
 *     tinymceEditorId={tinymceEditorId}
 *     isSelectionChanged={isSelectionChanged}
 *     isWireframe={false}
 *     items={menuItems}
 *     position={position}
 *     onClick={(event, menuItem) => console.log('Menu button clicked: ', menuItem)}
 *   />
 * );
 * ```
 */
declare const InlineRichTextEditorMenu: (props: Module.InlineRichTextEditorMenuProps) => ReactElement;
export { InlineRichTextEditorMenu, };
