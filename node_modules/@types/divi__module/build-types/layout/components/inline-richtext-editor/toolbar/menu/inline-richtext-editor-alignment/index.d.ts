import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
/**
 * Inline editor text alignment menu.
 *
 * This function renders the inline rich text editor text alignment menu.
 * The alignment menu contains functionality for aligning/justifying text `center`, `left`, or `right`.
 *
 * @since ??
 *
 * @example
 * ```tsx
 * const menuItems = [
 *   {
 *     "key": "alignment-menu-text-left",
 *     "buttonCommand": "justifyLeft",
 *     "iconName": "divi/text-align-left",
 *     "tooltipText": "Left Text"
 *   },
 *   {
 *     "key": "alignment-menu-text-center",
 *     "buttonCommand": "justifyCenter",
 *     "iconName": "divi/text-align-center",
 *     "tooltipText": "Center Text"
 *   },
 *   {
 *     "key": "alignment-menu-text-right",
 *     "buttonCommand": "justifyRight",
 *     "iconName": "divi/text-align-right",
 *     "tooltipText": "Right Text"
 *   },
 * ];
 *
 * const tinymceEditorId = window.divi.data.select('divi/module').getInlineEditorToolbar().tinymceEditorId;
 * const isSelectionChanged = window.divi.data.select('divi/module').getInlineEditorToolbar().isSelectionChanged;
 * const position = window.divi.data.select('divi/module').getInlineEditorToolbar().position;
 *
 * <InlineRichTextEditorTextAlignment
 *   animation={true}
 *   isWireframe={false}
 *   tinymceEditorId={tinymceEditorId}
 *   items={menuItems}
 *   onClick={(e, menuItem) => console.log('Menu button clicked: ', menuItem)}
 *   position={position}
 *   isSelectionChanged={isSelectionChanged}
 *   backToMainMenu={() => console.log('Back to main menu please.')}
 * />
 * ```
 */
declare const InlineRichTextEditorTextAlignment: (props: Module.InlineRichTextEditorMenuProps) => ReactElement;
export { InlineRichTextEditorTextAlignment, };
