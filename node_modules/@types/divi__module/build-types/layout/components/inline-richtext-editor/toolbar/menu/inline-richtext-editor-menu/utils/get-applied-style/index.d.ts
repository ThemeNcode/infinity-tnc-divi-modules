import { type TinyMCEEditor } from '@divi/types';
/**
 * Retrieves applied style.
 *
 * This function returns the applied style for a given button in the rich text editor.
 *
 * @since ??
 *
 * @example
 * ```ts
 * import tinymce from 'tinymce';
 *
 * const tinymceEditorId = window.divi.data.select('divi/module').getInlineEditorToolbar().tinymceEditorId;
 * const tinymceEditor = tinymce.get(tinymceEditorId);
 * const buttonName = 'text-align';
 * const styleNamePassed = 'left';
 *
 * const appliedStyle = getAppliedStyle({ buttonName, styleNamePassed, tinymceEditor });
 * console.log(appliedStyle); // Output: 'left'
 * ```
 *
 * @example
 * ```ts
 * import tinymce from 'tinymce';
 *
 * const tinymceEditorId = window.divi.data.select('divi/module').getInlineEditorToolbar().tinymceEditorId;
 * const tinymceEditor = tinymce.get(tinymceEditorId);
 * const buttonName = 'list';
 *
 * const appliedStyle = getAppliedStyle({ buttonName, tinymceEditor });
 * console.log(appliedStyle); // Output: 'ol' or 'ul'
 * ```
 */
declare const getAppliedStyle: ({ buttonName, styleNamePassed, tinymceEditor, }: {
    buttonName: string;
    styleNamePassed?: string;
    tinymceEditor: TinyMCEEditor;
}) => string | false;
export { getAppliedStyle, };
