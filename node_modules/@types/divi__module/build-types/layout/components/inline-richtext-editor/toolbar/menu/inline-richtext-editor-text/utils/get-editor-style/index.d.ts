import { type TinyMCEEditor } from '@divi/types';
/**
 * Retrieves the applied style value from the rich text editor.
 *
 * This function checks if the TinymceEditor object is defined and
 * if the editor's selection is not null.
 * If either of these conditions is not met, an empty string is returned.
 * It retrieves the active editor from the TinymceEditor object.
 * The function then gets the parent tags of the selected node and iterates over them.
 * If the tag is a 'span' and no style value has been applied yet,
 * it retrieves the style value using the style name.
 * Finally, it returns the applied style value.
 *
 * @since ??
 *
 * @example
 * ```tsx
 * import tinymce from 'tinymce';
 *
 * const tinymceEditorId = window.divi.data.select('divi/module').getInlineEditorToolbar().tinymceEditorId;
 * const tinymceEditor = tinymce.get(tinymceEditorId);
 * const styleName = 'color';
 *
 * const appliedStyle = getEditorStyle({ styleName, tinymceEditor });
 * ```
 */
declare const getEditorStyle: ({ styleName, tinymceEditor, }: {
    styleName: string;
    tinymceEditor: TinyMCEEditor;
}) => string;
export { getEditorStyle, };
