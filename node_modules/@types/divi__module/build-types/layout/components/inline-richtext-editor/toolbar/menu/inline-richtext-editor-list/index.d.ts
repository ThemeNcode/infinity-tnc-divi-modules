import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
/**
 * Inline rich text editor list menu component.
 *
 * This component renders a menu that contains functionality to add/edit list items
 * into the inline rich text editor selected content. List functionality includes adding
 * ordered and unordered lists as well as list indentation settings.
 *
 * @since ??
 *
 * @example
 * ```tsx
 * const menuItems = [
 *   {
 *     "key": "list-menu-unordered-list",
 *     "buttonCommand": "insertUnOrderedList",
 *     "iconName": "divi/list-unordered",
 *     "tooltipText": "Insert Unordered List"
 *   },
 *   {
 *     "key": "list-menu-ordered-list",
 *     "buttonCommand": "insertOrderedList",
 *     "iconName": "divi/list-ordered",
 *     "tooltipText": "Insert Ordered List"
 *   },
 *   {
 *     "key": "list-menu-indent-list",
 *     "buttonCommand": "indent",
 *     "iconName": "divi/list-indent",
 *     "tooltipText": "Indent List"
 *   },
 *   {
 *     "key": "list-menu-undent-list",
 *     "buttonCommand": "outdent",
 *     "iconName": "divi/list-undent",
 *     "tooltipText": "Undent List"
 *   },
 *   {
 *     "key": "list-menu-back-to-main-menu",
 *     "iconName": "divi/close",
 *     "linkToMenu": "mainMenu",
 *     "tooltipText": "Go Back"
 *   }
 * ];
 *
 *
 * const tinymceEditorId = window.divi.data.select('divi/module').getInlineEditorToolbar().tinymceEditorId;
 * const isSelectionChanged = window.divi.data.select('divi/module').getInlineEditorToolbar().isSelectionChanged;
 * const position = window.divi.data.select('divi/module').getInlineEditorToolbar().position;
 *
 * return (
 *   <InlineRichTextEditorList
 *     animation
 *     isSelectionChanged={isSelectionChanged}
 *     isWireframe={false}
 *     items={menuItems}
 *     tinymceEditorId={tinymceEditorId}
 *     onClick={(e, menuItem) => console.log('Menu button clicked: ', menuItem)}
 *     position={position}
 *     backToMainMenu={() => console.log('Back to main menu please.')}
 *   />
 * );
 * ```
 */
declare const InlineRichTextEditorList: (props: Module.InlineRichTextEditorMenuProps) => ReactElement;
export { InlineRichTextEditorList, };
