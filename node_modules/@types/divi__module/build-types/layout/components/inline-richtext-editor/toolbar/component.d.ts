import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
import './style.scss';
/**
 * Renders the toolbar for the inline rich text editor.
 *
 * The toolbar contains functionality to customize the content of the rich text editor,
 * e.g make font bold/italic, change font family, add bullet points e.t.c.
 *
 * @since ??
 *
 * @example
 *
 * Renders the InlineEditorToolbar component
 *
 * ```tsx
 * const tinymceEditorId = window.divi.data.select('divi/module').getInlineEditorToolbar().tinymceEditorId;
 * const active = window.divi.data.select('divi/module').getInlineEditorToolbar().active;
 * const googleFonts = window.divi.data.select('divi/fonts').getGoogleFonts();
 * const customFonts = window.divi.data.select('divi/fonts').getCustomFonts();
 * const removedFonts = window.divi.data.select('divi/settings').getSetting('removedFonts');
 * const position = window.divi.data.select('divi/module').getInlineEditorToolbar().position;
 * const colorPalette = [
 *   '#37efec', '#1c0099', '#e02b20', '#e09900', '#edf000'
 * ];
 *
 * const editorToolbar = (
 *   <InlineEditorToolbar
 *     moduleId="123"
 *     colorPalette={colorPalette}
 *     googleFonts={googleFonts}
 *     customFonts={customFonts}
 *     removedFonts={removedFonts}
 *     editingColorsAllowed={true}
 *     editingFontsAllowed={true}
 *     tinymceEditorId={tinymceEditorId}
 *     position={position}
 *   />
 * );
 * ```
 *
 * @example
 *
 * Renders the InlineEditorToolbar component with limited editing options
 *
 * ```tsx
 * const tinymceEditorId = window.divi.data.select('divi/module').getInlineEditorToolbar().tinymceEditorId;
 * const active = window.divi.data.select('divi/module').getInlineEditorToolbar().active;
 * const googleFonts = window.divi.data.select('divi/fonts').getGoogleFonts();
 * const isSelectionChanged = window.divi.data.select('divi/module').getInlineEditorToolbar().isSelectionChanged;
 * const position = window.divi.data.select('divi/module').getInlineEditorToolbar().position;
 * const colorPalette = [
 *   '#37efec', '#1c0099', '#e02b20', '#e09900', '#edf000'
 * ];
 *
 * <InlineEditorToolbar
 *   active={active}
 *   colorPalette={colorPalette}
 *   editingColorsAllowed={true}
 *   editingFontsAllowed={false}
 *   googleFonts={googleFonts}
 *   isSelectionChanged={isSelectionChanged}
 *   tinymceEditorId={tinymceEditorId}
 *   position={position}
 *   moduleId="123"
 * />
 * ```
 */
export declare const InlineEditorToolbar: (props: Module.InlineEditorToolbarProps) => ReactElement;
