import { type TinyMCEEditor } from '@divi/types';
/**
 * Sets the active button and style for the inline rich text editor.
 *
 * This function sets the active button and returns the active button style and applied style, if any.
 * The active button style is determined based on the button name and format block,
 * and is retrieved from the `buttonNameToStyleMapping` object.
 * The applied style is determined by calling the `getAppliedStyle` or `getWrapperTag` function,
 * depending on the button name and format block.
 *
 * @see {@link /docs/builder-api/js-beta/divi-module/constants/buttonNameToStyleMapping buttonNameToStyleMapping}
 *
 * @see {@link /docs/builder-api/js-beta/divi-module/functions/getAppliedStyle getAppliedStyle}
 *
 * @see {@link /docs/builder-api/js-beta/divi-module/functions/getWrapperTag getWrapperTag}
 *
 * @since ??
 *
 * @example
 * ```ts
 * import tinymce from 'tinymce';
 *
 * const tinymceEditorId = window.divi.data.select('divi/module').getInlineEditorToolbar().tinymceEditorId;
 * const editor = tinymce.get(tinymceEditorId);
 *
 * const { styleName, appliedStyle } = setActiveButton({
 *   buttonName: 'bold',
 *   formatBlock: 'p',
 *   tinymceEditor: editor,
 * });
 *
 * console.log(styleName); // 'font-weight'
 * console.log(appliedStyle); // 'font-weight: bold'
 * ```
 */
declare const setActiveButton: ({ buttonName, formatBlock, tinymceEditor, }: {
    buttonName: string;
    formatBlock: string;
    tinymceEditor: TinyMCEEditor;
}) => {
    styleName: string;
    appliedStyle: string | false;
};
export { setActiveButton, };
