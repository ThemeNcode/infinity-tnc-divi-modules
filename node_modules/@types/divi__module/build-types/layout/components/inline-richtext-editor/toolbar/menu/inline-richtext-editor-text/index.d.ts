import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
import './style.scss';
/**
 * Inline rich text editor text settings component.
 *
 * This component displays the text settings for the inline rich text editor.
 * It includes options for changing the font size and font style.
 *
 * @since ??
 *
 * @example
 * ```tsx
 * const googleFonts = window.divi.data.select('divi/fonts').getGoogleFonts();
 * const customFonts = window.divi.data.select('divi/fonts').getCustomFonts();
 * const removedFonts = window.divi.data.select('divi/settings').getSetting('removedFonts');
 * const tinymceEditorId = window.divi.data.select('divi/module').getInlineEditorToolbar().tinymceEditorId;
 * const isSelectionChanged = window.divi.data.select('divi/module').getInlineEditorToolbar().isSelectionChanged;
 * const position = window.divi.data.select('divi/module').getInlineEditorToolbar().position;
 *
 * <InlineRichTextEditorTextSettings
 *   animation={false}
 *   tinymceEditorId={tinymceEditorId}
 *   isSelectionChanged={isSelectionChanged}
 *   isWireframe={false}
 *   items={menuItems}
 *   position={position}
 *   googleFonts={googleFonts}
 *   customFonts={customFonts}
 *   removedFonts={removedFonts}
 *   backToMainMenu={() => console.log('Back to main menu please.')}
 *   onClick={(e, menuItem) => console.log('Menu button clicked!: ', menuItem)}
 * />
 * ```
 */
declare const InlineRichTextEditorTextSettings: (props: Module.InlineRichTextEditorMenuProps) => ReactElement;
export { InlineRichTextEditorTextSettings, };
