import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
import './style.scss';
/**
 * Inline rich text editor color component.
 *
 * This component renders the color selection functionality for the inline rich text editor.
 * This allows updating the color of the content selected in the inline editor.
 *
 * @since ??
 *
 * @example
 * ```tsx
 * const tinymceEditorId = window.divi.data.select('divi/module').getInlineEditorToolbar().tinymceEditorId;
 * const isSelectionChanged = window.divi.data.select('divi/module').getInlineEditorToolbar().isSelectionChanged;
 * const position = window.divi.data.select('divi/module').getInlineEditorToolbar().position;
 *
 * <InlineRichTextEditorTextColor
 *   animation={false}
 *   isSelectionChanged={isSelectionChanged}
 *   isWireframe={false}
 *   colorPalette={['#000000', '#ffffff']}
 *   tinymceEditorId={tinymceEditorId}
 *   position={position}
 *   backToMainMenu={() => console.log('Back to main menu please.')}
 * />
 * ```
 */
declare const InlineRichTextEditorTextColor: (props: Module.InlineRichTextEditorMenuProps) => ReactElement;
export { InlineRichTextEditorTextColor, };
