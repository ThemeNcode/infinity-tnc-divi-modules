import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
import './style.scss';
/**
 * Font select component.
 *
 * Renders a select control component for choosing a font in the inline rich text editor toolbar.
 * The font will be applied to the selected content in the editor.
 * The font will be applied with a default weight value of `normal`.
 *
 * @since ??
 *
 * @example
 * ```tsx
 * const googleFonts = window.divi.data.select('divi/fonts').getGoogleFonts();
 * <SelectControl
 *   editor={undefined}
 *   value={'Monaco'}
 *   googleFonts={googleFonts}
 *   id="inline-rich-text-editor-toolbar-font-select"
 *   onChange={(value: string) => console.log('Font name: ', value))}
 * />
 * ```
 */
declare const SelectControl: (props: Module.SelectControlProps) => ReactElement;
export { SelectControl, };
