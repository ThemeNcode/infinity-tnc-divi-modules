/**
 * Determines whether the color picker should update based on the selected value.
 *
 * This function checks if the selected value is a valid color value.
 * It considers the following three formats as valid colors:
 * - HEX color code: `#ffffff`
 * - RGB color code: `rgb(255, 255, 255)`
 * - RGBA color code: `rgba(255, 255, 255, 1)`.
 *
 * @since ??
 *
 * @example
 * ```ts
 * const selectedValue = '#ffffff';
 * const shouldUpdate = shouldColorpickerUpdate({ selectedValue });
 * console.log(shouldUpdate); // Output: true
 * ```
 */
declare const shouldColorpickerUpdate: ({ selectedValue, }: {
    selectedValue: string;
}) => boolean;
export { shouldColorpickerUpdate, };
