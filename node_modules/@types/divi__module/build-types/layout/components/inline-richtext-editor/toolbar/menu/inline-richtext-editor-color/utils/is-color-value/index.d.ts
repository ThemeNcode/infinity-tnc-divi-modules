/**
 * Determines if a given value is a valid color value.
 *
 * This function checks if a provided value is a valid color value.
 * A value is considered valid if it follows one of the following three formats:
 * - HEX color code: `#ffffff`
 * - RGB color code: `rgb(255, 255, 255)`
 * - RGBA color code: `rgba(255, 255, 255, 1)`.
 *
 * @since ??
 *
 * @example
 * ```ts
 * isColorValue('#FF0000'); // true
 *
 * isColorValue('rgb(255, 0, 0)'); // true
 *
 * isColorValue('rgba(255, 0, 0, 0.5)'); // true
 *
 * isColorValue('blue'); // false
 * ```
 */
declare const isColorValue: (value: string) => boolean;
export { isColorValue, };
