import { type TinyMCEEditor } from '@divi/types';
/**
 * Retrieves font size of the TinyMCE editor.
 *
 * This function retrieves the font size value from the TinyMCE editor
 * and converts it into a numeric value for easier manipulation.
 * If the font size value cannot be retrieved from the editor directly,
 * it attempts to retrieve it from the editor's style using a custom script.
 * The function then maps the font size value to a corresponding numeric value and returns it.
 *
 * @see {@link /docs/builder-api/js-beta/divi-module/functions/getEditorStyle getEditorStyle}
 *
 * @since ??
 *
 * @example
 * ```tsx
 * import tinymce from 'tinymce';
 *
 * const tinymceEditorId = window.divi.data.select('divi/module').getInlineEditorToolbar().tinymceEditorId;
 * const tinymceEditor = tinymce.get(tinymceEditorId);
 * const newFontSize = updateFontSize({ tinymceEditor });
 * ```
 */
declare const updateFontSize: ({ tinymceEditor, }: {
    tinymceEditor: TinyMCEEditor;
}) => null | number;
export { updateFontSize, };
