import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
import './style.scss';
/**
 * Color control component.
 *
 * The color control component renders an input element component for selecting a color value.
 * It uses the WordPress `wpColorPicker` library to provide a color picker interface.
 *
 * @since ??
 *
 * @example
 * ```tsx
 * const colorControlElement = (
 *   <ColorControl
 *     hideOnLoad={false}
 *     name="paragraph-color"
 *     onChange={(color) => console.log('Selected color: ', color)}
 *     colorPalette={['#ff0000', '#00ff00', '#0000ff']}
 *     type="color-alpha"
 *     value="#ff0000"
 *   />
 * );
 * ```
 */
declare const ColorControl: (props: Module.ColorControlProps) => ReactElement;
export { ColorControl, };
