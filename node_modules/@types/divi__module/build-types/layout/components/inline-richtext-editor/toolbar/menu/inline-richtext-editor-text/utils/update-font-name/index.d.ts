import { type Fonts, type TinyMCEEditor } from '@divi/types';
/**
 * Updates font name.
 *
 * This function updates the rich text editor selection's font name.
 * The font name is determined based on parent font name and font-weight.
 *
 * @since ??
 *
 * @example
 *
 * Update font name using removed fonts configuration and TinyMCE editor object
 *
 * ```tsx
 * import tinymce from 'tinymce';
 *
 * const tinymceEditorId = window.divi.data.select('divi/module').getInlineEditorToolbar().tinymceEditorId;
 * const tinymceEditor = tinymce.get(tinymceEditorId);
 * const removedFonts = window.divi.data.select('divi/removedFonts').getRemovedFonts();
 *
 * const updatedFontName = updateFontName({
 *   removedFonts
 *   tinymceEditor,
 * });
 *
 * @example
 *
 * Update font name with default values
 *
 * ```tsx
 * import tinymce from 'tinymce';
 *
 * const tinymceEditorId = window.divi.data.select('divi/module').getInlineEditorToolbar().tinymceEditorId;
 * const tinymceEditor = tinymce.get(tinymceEditorId);
 * const removedFonts = window.divi.data.select('divi/removedFonts').getRemovedFonts();
 *
 * const updatedFontName = updateFontName({
 *   removedFonts,
 *   tinymceEditor,
 * });
 * ```
 */
declare const updateFontName: ({ removedFonts, tinymceEditor, }: {
    removedFonts: Fonts.Removed.Fonts;
    tinymceEditor: TinyMCEEditor;
}) => null | string;
export { updateFontName, };
