import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
import './style.scss';
/**
 * Range control component.
 *
 * This component renders a range control for an inline rich text editor toolbar
 * which is used to change/update the font size for selected content.
 *
 * @since ??
 *
 * @example
 * ```tsx
 * const currentFontSize = 4;
 *
 * <RangeControl
 *   id="inline-editor-toolbar-font-size-range-control"
 *   name="inline-editor-toolbar-font-size-range-control"
 *   min={1}
 *   max={7}
 *   step={1}
 *   onChange={(value: string) => console.log('New font size: ', value)}
 *   value={currentFontSize}
 * />
 * ```
 */
declare const RangeControl: (props: Module.RangeControlProps) => ReactElement;
export { RangeControl, };
