import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
import './style.scss';
/**
 * Inline rich text editor insert link component.
 *
 * This component displays an input for inserting a link/URL into a rich text editor.
 *
 * @since ??
 *
 * @example
 * ```tsx
 * const tinymceEditorId = window.divi.data.select('divi/module').getInlineEditorToolbar().tinymceEditorId;
 * const position = window.divi.data.select('divi/module').getInlineEditorToolbar().position;
 *
 * <InlineRichTextEditorInsertLink
 *   animation
 *   isWireframe={false}
 *   tinymceEditorId={tinymceEditorId}
 *   position={position}
 *   backToMainMenu={() => console.log('Back to main menu please.')}
 * />
 * ```
 */
declare const InlineRichTextEditorInsertLink: (props: Module.InlineRichTextEditorMenuProps) => ReactElement;
export { InlineRichTextEditorInsertLink, };
