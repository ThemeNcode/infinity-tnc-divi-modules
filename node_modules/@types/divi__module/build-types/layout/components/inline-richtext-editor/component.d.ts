import { type ReactElement } from 'react';
import { type Module } from '@divi/types';
import './style.scss';
/**
 * Inline Rich Text Editor field component.
 *
 * This component is responsible for rendering the Inline Rich Text Editor field.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {Function} props.activateEditor CSS class name(s).
 * @param {string} props.className CSS class name(s).
 * @param {string} props.content Rich text editor content.
 * @param {boolean} props.editable Whether the field is editable.
 * @param {boolean} props.editing Whether the field is in editing mode.
 * @param {Function} props.handleBlur Optional callback function to call on `onBlur` event.
 * @param {Function} props.handleContextMenu Optional callback function to call on `onContextMenu` event.
 * @param {Function} props.handleDoubleClick Optional callback function to handle `onDblClick` event.
 * @param {Function} props.handleExecCommand Optional callback function to handle `onExecCommand` event.
 * @param {Function} props.handleEditorChange Optional callback function to handle `onEditorChange` event.
 * @param {Function} props.handleFocus Optional callback function to handle `onFocus` event.
 * @param {Function} props.handleFocusOut Optional callback function to handle `onFocusOut` event.
 * @param {Function} props.handleKeyDown Optional callback function to handle `onKeyDown` event.
 * @param {Function} props.handleKeyUp Optional callback function to handle `onKeyUp` event.
 * @param {Function} props.handleLoadContent Optional callback function to handle `onLoadContent` event.
 * @param {Function} props.handleMouseDown Optional callback function to handle `onMouseDown` event.
 * @param {Function} props.handleMouseUp Optional callback function to handle `onMouseUp` event.
 * @param {Function} props.handleNodeChange Optional callback function to handle `onNodeChange` event.
 * @param {object} props.googleFonts Google fonts object.
 * @param {string} props.moduleId Module ID.
 * @param {string} props.tagName HTML tag name.
 * @param {Array<string>} props.tinyMCEPlugins Optional TinyMCE plugins.
 *
 * @returns {ReactElement} Inline Rich Text Editor field component.
 */
declare const InlineRichTextEditor: ({ activateEditor, className, content, editable, editing, handleBlur, handleContextMenu, handleDoubleClick, handleExecCommand, handleEditorChange, handleFocus, handleFocusOut, handleKeyDown, handleKeyUp, handleLoadContent, handleMouseDown, handleMouseUp, handleNodeChange, googleFonts, moduleId, tagName, tinyMCEPlugins, }: Module.InlineRichTextEditorProps) => ReactElement;
export { InlineRichTextEditor, };
