/**
 * Get the TinyMCE editor content from the event.
 *
 * Using jQuery, look at the event target and find the closest TinyMCE editor.
 * If the target is the editor itself, return the target.
 *
 * @since ??
 *
 * @param {Event} event The event.
 *
 * @returns {JQuery<EventTarget>|null} The editor, or null if not found.
 */
export declare const getEditorFromEvent: (event: Event) => JQuery<EventTarget> | null;
