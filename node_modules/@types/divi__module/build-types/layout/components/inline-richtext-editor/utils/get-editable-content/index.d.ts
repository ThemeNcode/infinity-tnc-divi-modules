/**
 * Get editable content for the Inline Rich Text Editor field.
 *
 * This function is responsible for getting the editable content for the Inline Rich Text Editor field.
 *
 * @since ??
 *
 * @param {object} options The options object.
 * @param {string} options.valueState The value of the field.
 * @param {Function} options.rawContentProcessor The raw content processor function.
 * @param {RegExpMatchArray|null} options.gutenbergEmbeds The Gutenberg embeds array (or null).
 * @param {RegExp} options.embeds The embeds to match.
 *
 * @returns {string} The processed content.
 */
export declare const getEditableContent: ({ valueState, rawContentProcessor, gutenbergEmbeds, embeds, }: {
    valueState: string;
    rawContentProcessor: (value: string) => string;
    gutenbergEmbeds: RegExpMatchArray | null;
    embeds: RegExp;
}) => string;
