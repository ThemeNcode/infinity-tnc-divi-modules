/**
 * Classnames.
 *
 * Class for populating classnames.
 * This class contains `add` member function which allows adding new classnames to the instance.
 * This member function accepts multiple types of parameters.
 * If a classname string is provided, it is added with value true.
 * If an array of classname string is provided, each item in the array is added with value true.
 * If an object of classname string as key and boolean as value is provided, each key-value pair is added.
 *
 * This class contains a getter: `value` which returns the instance's classnames as a string.
 *
 * This class contains a member function: `remove` which allows removing a classname from the instance.
 * This member function accepts a classname string as parameter.
 *
 * @since ??
 *
 * @param {object} initialClassnames Optional initial classnames object with string keys and boolean values.
 * Defaults to `{}`.
 *
 * @returns {object}
 */
export declare class Classnames {
    values: Record<string, boolean>;
    /**
     * Classname constructor.
     *
     * @since ??
     */
    constructor(initialClassnames?: Record<string, boolean>);
    /**
     * Add classname into classname instance.
     *
     * @since ??
     *
     * @param {string | string[] | Record<string, boolean>} classname Classname that should be added.
     * @param {boolean} shouldBeAdded Whether the given classname should be added or not.
     */
    add(classname: string | string[] | Record<string, boolean>, shouldBeAdded?: boolean): void;
    /**
     * Remove classname from classname instance.
     *
     * @since ??
     *
     * @param {string} classname Classname.
     */
    remove(classname: string): void;
    /**
     * Get classname from classname instance.
     *
     * @since ??
     *
     * @returns {string}
     */
    get value(): string;
}
