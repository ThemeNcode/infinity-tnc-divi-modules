import { type ContextMenuCallbackParams } from './types';
/**
 * Callback function for triggering right click options for modules that are executed via
 * right click and module options' more button.
 *
 * @since ??
 */
export declare const contextMenuCallback: ({ cursorPosition, moduleType, moduleId, moduleName, windowLocation, modalName, }: ContextMenuCallbackParams) => void;
