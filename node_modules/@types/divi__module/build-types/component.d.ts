import React, { type ReactElement } from 'react';
import { type ModuleProps } from './types';
import './style.scss';
/**
 * Module.
 *
 * Main wrapper component for module.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {object} props.attrs Module attributes.
 * @param {object} props.elements Module elements.
 * @param {object} props.parentAttrs Parent module attributes.
 * @param {object} props.siblingAttrs Sibling module attributes.
 * @param {object} props.defaultPrintedStyleAttrs Default printed style attributes.
 * @param {object} props.pageSettings Page settings.
 * @param {object} props.htmlAttrs HTML attributes.
 * @param {string} props.id Module ID.
 * @param {string} props.moduleOrderIndex Module Order Index.
 * @param {string} props.parentId Parent module ID.
 * @param {ReactNode} props.children Module children.
 * @param {Array<string>} props.childrenIds Module children IDs.
 * @param {boolean} props.disabledModuleVisibility Whether module visibility is disabled.
 * @param {boolean} props.hasHighlight Whether module has highlight. Defaults to `false`.
 * @param {boolean} props.hasMousetrap Whether module has mousetrap. Defaults to `false`.
 * @param {boolean} props.hasOptions Whether module has options. Defaults to `false`.
 * @param {boolean} props.hasModule Whether module has module.
 * @param {boolean} props.isCommandPressed Whether command key is pressed. Defaults to `false`.
 * @param {boolean} props.isShiftPressed Whether shift key is pressed.
 * @param {boolean} props.isSelected Whether module is selected. Defaults to `false`.
 * @param {string} props.modalName Module settings modal name.
 * @param {string} props.moduleCategory Module category.
 * @param {string} props.name Module name.
 * @param {string} props.parentName Parent module name.
 * @param {React.RefObject} props.domRef Module DOM reference.
 * @param {React.ComponentType} props.stylesComponent Module styles component.
 * @param {React.ComponentType} props.scriptDataComponent Module script data component.
 * @param {string} props.breakpoint Module breakpoint.
 * @param {string} props.state Module state.
 * @param {boolean} props.isEdited Whether module is edited.
 * @param {boolean} props.canClone Whether module can be cloned. Defaults to `false`.
 * @param {string} props.className Module class name.
 * @param {boolean} props.isChildHovered Whether module is child hovered. Defaults to `false`.
 * @param {boolean} props.isDragged Whether module is dragged. Defaults to `false`.
 * @param {boolean} props.isDragging Whether module is dragging. Defaults to `false`.
 * @param {boolean} props.isHovered Whether module is hovered. Defaults to `false`.
 * @param {boolean} props.isChildModule Whether module is child module. Defaults to `false`.
 * @param {boolean} props.isParentModule Whether module is parent module. Defaults to `false`.
 * @param {boolean} props.isFirst Whether module is first. Defaults to `false`.
 * @param {boolean} props.isLast Whether module is last. Defaults to `false`.
 * @param {boolean} props.hasModuleClassName Whether module has module class name. Defaults to `true`.
 * @param {boolean} props.hasAnimatedClassName Whether module has module class name. Defaults to `true`.
 * @param {boolean} props.hasHoveredClassName Whether module has hovered class name. Defaults to `false`.
 * @param {string} props.moduleClassName Module class name.
 * @param {string} props.moduleOrderClassName Module orderclass name.
 * @param {string} props.parentOrderClassName Parent order class name.
 * @param {Function} props.classnamesFunction Module classnames function.
 * @param {Function} props.onClickMousetrap On click mousetrap callback. Defaults to `lodash`'s `noop`.
 * @param {object} props.styles Module styles. Defaults to `{}`.
 * @param {string} props.tag Module tag. Defaults to `div`.
 * @param {boolean} props.hasModuleWrapper Whether module has module wrapper. Defaults to `false`.
 * @param {string} props.wrapperTag Module wrapper tag. Defaults to `div`.
 * @param {object} props.wrapperHtmlAttrs Module wrapper HTML attributes.
 * @param {string} props.wrapperClassname Module wrapper class name. Defaults to empty string.
 * @param {Function} props.wrapperClassnamesFunction Module wrapper classnames function.
 * @param {string} props.cssPosition Module CSS position. Defaults to `after`.
 * @param {boolean} props.userCanEditGlobalItems Whether user can edit global items. Defaults to `false`.
 * @param {Function} props.htmlAttributesFunction HTML attributes function.
 *
 * @returns {ReactElement}
 */
export declare const Module: <TProps extends ModuleProps<TProps["attrs"]>>({ attrs, breakpoint, canClone, children, childrenIds, className, classnamesFunction, cssPosition, defaultPrintedStyleAttrs, disabledModuleVisibility, domRef, elements, hasAnimatedClassName, hasHighlight, hasHoveredClassName, hasModule, hasModuleClassName, hasModuleWrapper, hasMousetrap, hasOptions, htmlAttributesFunction, htmlAttrs, id, isChildHovered, isChildModule, isCommandPressed, isDragged, isDragging, isEdited, isFirst, isHovered, isLast, isParentModule, isSelected, isShiftPressed, modalName, moduleCategory, moduleClassName, moduleOrderClassName, moduleOrderIndex, name, onClickMousetrap, pageSettings, parentAttrs, parentId, parentName, parentOrderClassName, scriptDataComponent, siblingAttrs, state, styles, stylesComponent, tag, userCanEditGlobalItems, wrapperClassname, wrapperClassnamesFunction, wrapperHtmlAttrs, wrapperTag, }: TProps) => React.ReactElement<TProps, string | React.JSXElementConstructor<any>>;
