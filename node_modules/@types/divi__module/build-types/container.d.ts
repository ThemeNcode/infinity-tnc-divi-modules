import { type ReactElement } from 'react';
import { type ModuleAttrs } from '@divi/types';
import { type ModuleContainerProps } from './types';
/**
 * Module Container.
 *
 * Higher order container component which automatically pass state from selected store to <Module /> component.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {string} props.id Module ID.
 * @param {boolean} props.hasModule Whether module has content or not.
 * @param {string} props.name Module name.
 * @param {boolean} props.hasModuleClassName Whether module has module class name or not.
 * @param {boolean} props.hasHoveredClassName Whether module has hovered class name or not.
 * @param {string} props.tag Module tag.
 * @param {boolean} props.hasModuleWrapper Whether module has module wrapper or not.
 * @param {string} props.wrapperTag Module wrapper tag.
 * @param {string} props.wrapperClassname Module wrapper CSS class name.
 * @param {object} props.wrapperHtmlAttrs Module wrapper HTML attributes.
 *
 * @returns {object} Module Container.
 *
 * @example Basic usage.
 * ```tsx
 * // React function component for rendering module style.
 * const ModuleStyles = ({
 *  attrs,
 *  elements,
 *  settings,
 *  orderClass,
 *  mode,
 *  state,
 *  noStyleTag
 *   }) => (
 *  <StyleContainer mode={mode} state={state} noStyleTag={noStyleTag}>
 *    // Element: Module
 *    {elements.style({
 *      attrName: 'module',
 *      styleProps: {
 *        disabledOn: {
 *          disabledModuleVisibility: settings?.disabledModuleVisibility
 *        }
 *      }
 *    })}
 *    // Element: Title
 *    {elements.style({
 *      attrName: 'title',
 *    })}
 *    // Element: Content
 *    {elements.style({
 *      attrName: 'content',
 *    })}
 *  </StyleContainer>
 *  );
 *
 *   // React function component for registering module script data.
 *  const ModuleScriptData = ({
 *    attrs,
 *    selector,
 *    id,
 *  }) => {
 *    useElementScriptData({
 *      attrs: attrs?.module?.decoration,
 *      selector,
 *      id,
 *    });
 *    return null;
 *  }
 *  // Function for registering module classnames.
 *  const moduleClassnames = ({
 *    classnamesInstance,
 *    attrs,
 *  }) => {
 *    // Add element classnames.
 *    classnamesInstance.add(
 *      elementClassnames({
 *        attrs: attrs?.module?.decoration ?? {},
 *      }),
 *    );
 *  };
 *
 * <ModuleContainer
 *   attrs={attrs}
 *   elements={elements}
 *   id={id}
 *   moduleClassName="d5_tut_simple_quick_module"
 *   name={name}
 *   scriptDataComponent={ModuleScriptData}
 *   stylesComponent={ModuleStyles}
 *   classnamesFunction={moduleClassnames}
 * >
 *   <div className="et_pb_module_inner"></div>
 * </ModuleContainer>
 *```
 */
export declare const ModuleContainer: (props: ModuleContainerProps<ModuleAttrs>) => ReactElement;
