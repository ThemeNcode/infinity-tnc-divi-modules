import { type ReactElement } from 'react';
import { type DraggableProps } from './types';
/**
 * Draggable Component wrapper.
 *
 * Wrapper component for components that are draggable.
 * This component has necessary callbacks to construct draggable component.
 *
 * @since ??
 *
 * @param {object} props Component props.
 * @param {ReactNode} props.children Children to be added to the created draggable element.
 * @param {string} props.type The draggable HTML element to be created e.g div.
 * @param {boolean} props.isTopWindow Whether this is topWindow.
 * @param {string} props.className CSS class name to apply to the component.
 * @param {Function} props.onElementClick Callback function called when element is clicked, called on mousedown.
 * @param {Function} props.onElementDrag Callback function called when element is being dragged.
 * @param {Function} props.onElementRelease Callback function called when element drag has ended, called on mouseup.
 * @param {Function} props.onContextMenu Callback function called for context menu.
 * @param {boolean} props.disable Whether draggable functionality is enabled/disabled.
 * @param {object} props.style CSS styles object.
 * @param {boolean} props.hasWrapper Whether the passed children has wrapper element.
 *
 * @returns {ReactElement}
 */
declare const Draggable: {
    (props: DraggableProps): ReactElement;
    defaultProps: {
        isTopWindow: boolean;
        className: string;
        onElementClick: (...args: any[]) => void;
        onElementDrag: (...args: any[]) => void;
        onElementRelease: (...args: any[]) => void;
        type: string;
        disable: boolean;
        style: {};
        hasWrapper: boolean;
    };
};
export { Draggable, };
