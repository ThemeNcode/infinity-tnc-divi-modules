import { type ReactElement } from 'react';
import { type SidebarRowResizeHandleContainerProps } from './types';
/**
 * Container component for sidebar row's resize handle.
 *
 * @since ??
 */
export declare const SidebarRowResizeHandleContainer: ({ height, resizing, sidebarRowId, }: SidebarRowResizeHandleContainerProps) => ReactElement;
