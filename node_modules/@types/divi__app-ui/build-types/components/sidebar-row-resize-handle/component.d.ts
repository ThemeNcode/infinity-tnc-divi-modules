import { type ReactElement } from 'react';
import { type SidebarRowResizeHandleProps } from './types';
import './styles.scss';
/**
 * UI Component for rendering sidebar row resize handle.
 *
 * @since ??
 */
export declare const SidebarRowResizeHandle: ({ height, heightMaxLimit, heightMinLimit, resizing, setHeight, setResizeStatus, sidebarRowId, updateLimits, }: SidebarRowResizeHandleProps) => ReactElement;
