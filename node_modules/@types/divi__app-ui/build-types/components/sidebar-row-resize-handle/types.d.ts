import { type AppUi } from '@divi/types';
export interface SidebarRowResizeHandleProps {
    height: number;
    heightMaxLimit: number;
    heightMinLimit: number;
    resizing: boolean;
    setHeight: (params: AppUi.Store.Actions.SetSidebarRowHeight.Params) => void;
    setResizeStatus: (params: AppUi.Store.Actions.SetSidebarRowResizingStatus.Params) => void;
    sidebarRowId: string;
    updateLimits: (sidebarRowId: string) => void;
}
export type SidebarRowResizeHandleContainerProps = Pick<SidebarRowResizeHandleProps, 'height' | 'resizing' | 'sidebarRowId'>;
