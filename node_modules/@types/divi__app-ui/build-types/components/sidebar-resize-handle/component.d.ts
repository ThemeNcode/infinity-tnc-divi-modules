import { type ReactElement } from 'react';
import { type SidebarResizeHandleProps } from './types';
import './styles.scss';
/**
 * Component for rendering sidebar's horizontal resize handle.
 *
 * @since ??
 */
export declare const SidebarResizeHandle: ({ dimension, position, setElementProperty, }: SidebarResizeHandleProps) => ReactElement;
