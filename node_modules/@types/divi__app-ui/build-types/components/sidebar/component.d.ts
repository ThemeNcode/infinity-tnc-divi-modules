import { type ReactElement } from 'react';
import { type SidebarProps } from './types';
import './styles.scss';
/**
 * AppFrame's sidebar component.
 *
 * @since ??
 */
export declare const Sidebar: ({ position, dimension, offset, setElementProperty, rowIds, }: SidebarProps) => ReactElement;
