import { type ReactElement } from 'react';
import { type SidebarContainerProps } from './types';
/**
 * Container component for <Sidebar /> that automatically pass dispatcher function as props.
 *
 * @since ??
 */
export declare const SidebarContainer: ({ position, }: SidebarContainerProps) => ReactElement;
