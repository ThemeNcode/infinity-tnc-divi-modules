import { type ReactElement } from 'react';
import { type SidebarRowNavContainerProps } from './types';
/**
 * Sidebar row multi-panel's navigation tab container component.
 *
 * @since ??
 */
export declare const SidebarRowNavContainer: ({ active, sidebarRowId, modalName, label, navContextMenuCallback, setActiveModal, }: SidebarRowNavContainerProps) => ReactElement;
