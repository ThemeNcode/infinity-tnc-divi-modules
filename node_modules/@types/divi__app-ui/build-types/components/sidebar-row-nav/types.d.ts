import { type CSSProperties } from 'react';
import { type DraggableProps } from '@types/divi__draggable';
import { type SidebarRowProps } from '../sidebar-row/types';
export interface SidebarRowNavContainerProps {
    active: boolean;
    label: string;
    modalName: string;
    navContextMenuCallback: SidebarRowProps['navContextMenuCallback'];
    setActiveModal: SidebarRowProps['setActiveModal'];
    sidebarRowId: string;
}
export interface SidebarRowNavProps extends Omit<SidebarRowNavContainerProps, 'setActiveModal'> {
    isDragging: boolean;
    style: CSSProperties;
    placeholderStyle: CSSProperties;
    onElementClick: DraggableProps['onElementClick'];
    onElementDrag: DraggableProps['onElementDrag'];
    onElementRelease: DraggableProps['onElementRelease'];
}
