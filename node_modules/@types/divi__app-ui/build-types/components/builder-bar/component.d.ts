import { type ReactElement } from 'react';
import { type BuilderBarProps } from './types';
import './style.scss';
/**
 * Builder Bar UI.
 *
 * This component is located on the left side of the Visual Builder and contains different buttons such as
 * mode buttons, builder settings button, search buttons and so on.
 *
 * @since ??
 *
 * @returns {ReactElement}
 */
export declare const BuilderBar: ({ adminBarHeight, currentMode, isBuilderSettingsModalOpen, isHelpModalOpen, isLayersModalOpen, isLoadLayoutOpen, isWireframeView, onClickBuilderSettings, onClickClickMode, onClickGridMode, onClickHelp, onClickHoverMode, onClickLayers, onClickLoadLayout, onClickWireframeView, pageBarHeight, }: BuilderBarProps) => ReactElement;
