import { type ReactElement } from 'react';
import { type SidebarRowContainerProps } from './types';
/**
 * Sidebar row container component.
 *
 * @since ??
 */
export declare const SidebarRowContainer: (props: SidebarRowContainerProps) => ReactElement;
