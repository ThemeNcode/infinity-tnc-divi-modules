import { type ReactNode } from 'react';
import { type AppUi, type RightClickOptions } from '@divi/types';
export interface SidebarRowContainerProps {
    id: string;
    index: number;
    sidebarWidth: number;
}
export interface SidebarRowProps extends SidebarRowContainerProps {
    height: number;
    resizing: boolean;
    activeModal: AppUi.Sidebar.RowItem['activeModal'];
    activeDockingArea: AppUi.SidebarRow.DockingArea;
    modalIds: AppUi.Sidebar.RowItem['modalIds'];
    modalLabels: Record<string, string>;
    multiModal?: AppUi.Sidebar.RowItem['multiModal'];
    setActiveModal: (params: AppUi.Store.Actions.SetSidebarRowActiveModal.Params) => void;
    component: ReactNode;
    componentProps: Partial<AppUi.Sidebar.Modal.Props>;
    isDraggedToUndock: boolean;
    navContextMenuCallback?: (params: {
        modalName: string;
        sidebarRowId: string;
        cursorPosition: {
            x: number;
            y: number;
        };
        trigger: RightClickOptions.Owner.SidebarRowNav['trigger'];
    }) => void;
}
