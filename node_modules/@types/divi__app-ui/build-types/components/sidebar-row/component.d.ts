import { type ReactElement } from 'react';
import { type SidebarRowProps } from './types';
import './styles.scss';
/**
 * Sidebar row component.
 *
 * @since ??
 */
export declare const SidebarRow: ({ activeDockingArea, activeModal, component, componentProps, height, id, index, isDraggedToUndock, modalIds, modalLabels, multiModal, navContextMenuCallback, resizing, setActiveModal, }: SidebarRowProps) => ReactElement;
