import { type ReactElement } from 'react';
import './style.scss';
/**
 * Page Bar UI Container.
 *
 * This component is located on the top side of the Visual Builder and contains different buttons such as
 * responsive viewports, undo/redo, history and so on.
 *
 * @since ??
 *
 * @returns {ReactElement}
 */
export declare const PageBarContainer: () => ReactElement;
