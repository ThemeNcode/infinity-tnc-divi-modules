import { type ReactElement } from 'react';
import { type PageBarSaveButtonProps } from './types';
/**
 * Page bar save button UI.
 *
 * @since ??
 *
 * @param {PageBarSaveButtonProps} props Page bar save button props.
 *
 * @returns {ReactElement}
 */
export declare const PageBarSaveButton: (props: PageBarSaveButtonProps) => ReactElement;
