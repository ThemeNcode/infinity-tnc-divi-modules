import { type ReactElement } from 'react';
import { type PageBarTitleProps } from './types';
import './styles.scss';
/**
 * UI Component for rendering editable page title in page bar.
 *
 * @since ??
 *
 * @returns {ReactElement}
 */
export declare const PageBarTitle: ({ postTitle, updatePostTitle, }: PageBarTitleProps) => ReactElement;
