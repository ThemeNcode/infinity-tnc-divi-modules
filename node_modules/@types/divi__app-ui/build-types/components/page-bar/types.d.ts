import { type IconsAnimation } from '@types/divi__icon-library';
export type PageBarSaveButtonStates = Record<'active' | 'done' | 'idle', {
    label: string;
    iconName: string | null;
    iconAnimation: IconsAnimation | null;
}>;
