import { type AppUi } from '@divi/types';
/**
 * Middleware effect for syncing app window's store state to top window's store state.
 *
 * App frame needs top window state to be updated so it can calculate the dimension and offset
 * of the app frame correctly.
 *
 * @since ??
 */
export declare const syncTopWindowStoreState: (action: AppUi.Store.Actions.ReturnValue) => void;
