import { type AppUi } from '@divi/types';
/**
 * Sync app window's element property value to top window's because it is needed by <AppFrame /> component.
 *
 * @since ??
 */
export declare const setTopWindowElementProperty: (action: AppUi.Store.Actions.SetElementProperty.ReturnValue) => void;
