import { type AppUi } from '@divi/types';
/**
 * Set window auto scroll (eg. When dragged module reaches bottom).
 *
 * @since ??
 */
export declare const windowAutoScroll: (action: AppUi.Store.Actions.SetWindowAutoScroll.ReturnValue, store: AppUi.Store.Instance) => void;
