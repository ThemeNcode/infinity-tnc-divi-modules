import { type AppUi } from '@divi/types';
/**
 * Sync app window's element visibility value to top window's because it is needed by <AppFrame /> component.
 *
 * @since ??
 */
export declare const setTopWindowElementVisibility: (action: AppUi.Store.Actions.SetElementVisibility.ReturnValue) => void;
