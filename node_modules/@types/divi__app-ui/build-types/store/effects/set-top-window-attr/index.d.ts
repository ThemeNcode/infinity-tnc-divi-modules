import { type AppUi } from '@divi/types';
/**
 * Sync top window's attribute when it is changed so top window has window's attribute value.
 *
 * @since ??
 */
export declare const setTopWindowAttr: (action: AppUi.Store.Actions.SetWindow.ReturnValue) => void;
