import { type AppUi } from '@divi/types';
/**
 * Sync top window's view when view is changed so top window adjusts the dimension of the app's iframe.
 *
 * @since ??
 */
export declare const setTopWindowView: (action: AppUi.Store.Actions.SetView.ReturnValue) => void;
