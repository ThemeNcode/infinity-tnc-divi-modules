import { type AppUi } from '@divi/types';
/**
 * Register `divi/app-ui` store.
 *
 * @since ??
 */
export declare const registerAppUiStore: (storeInitialState?: AppUi.Store.ImmutableState) => void;
/**
 * Register listener for window events thus relevant window state is saved on `divi/app-ui` store state.
 *
 * @since ??
 */
export declare const registerListeners: () => void;
