import { type AppUi } from '@divi/types';
/**
 * Add modal based on given modal name to sidebar row.
 *
 * This action docks given modal name to given sidebar row. Note that this action expects that given modal name
 * is valid and exists in the modal's store. When modal is being docked to sidebar row (albeit its position)
 * it assumes that sidebar row is valid. If invalid sidebar row is given, the store state won't be modified.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function
 *
 * @example Adding modal as new modal panel inside sidebar row.
 * ```ts
 *   window.divi.data.dispatch('divi/app-ui').dockModalToSidebarRow({
 *     modalName: 'divi/help',
 *     modalType: 'multiInstanceModal',
 *     sidebarRowId: '1234-5678-9012-3456',
 *     position: 'inside',
 *     newSidebarRowId: '2234-5678-9012-3456',
 *   });
 * ```
 *
 * @example Adding modal in new sidebar row above given sidebar row.
 * ```ts
 *   window.divi.data.dispatch('divi/app-ui').dockModalToSidebarRow({
 *     modalName: 'divi/help',
 *     modalType: 'multiInstanceModal',
 *     sidebarRowId: '1234-5678-9012-3456',
 *     position: 'above',
 *     newSidebarRowId: '2234-5678-9012-3456',
 *   });
 * ```
 *
 * @example Adding modal in new sidebar row below given sidebar row.
 * ```ts
 *   window.divi.data.dispatch('divi/app-ui').dockModalToSidebarRow({
 *     modalName: 'divi/help',
 *     modalType: 'multiInstanceModal',
 *     sidebarRowId: '1234-5678-9012-3456',
 *     position: 'below',
 *     newSidebarRowId: '2234-5678-9012-3456',
 *   });
 * ```
 */
export declare const dockModalToSidebarRow: ({ modalName, modalType, newSidebarRowId, position, sidebarRowId, }: AppUi.Store.Actions.DockModalToSidebarRow.Params) => AppUi.Store.Actions.DockModalToSidebarRow.ReturnValue;
