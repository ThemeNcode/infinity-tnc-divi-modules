import { type AppUi } from '@divi/types';
/**
 * Set element's visibility.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/edit-post').setElementVisibility({
 *   elementName: 'sidebarRight',
 *   visibility: true,
 * });
 * ```
 */
export declare const setElementVisibility: ({ elementName, visibility, }: AppUi.Store.Actions.SetElementVisibility.Params) => AppUi.Store.Actions.SetElementVisibility.ReturnValue;
