import { type AppUi } from '@divi/types';
/**
 * Resets state value.
 *
 * Resets state value into its initial value.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/app-ui').resetState();
 * ```
 */
export declare const resetState: () => AppUi.Store.Actions.ResetState.ReturnValue;
