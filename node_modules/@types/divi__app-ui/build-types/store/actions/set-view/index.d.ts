import { type AppUi } from '@divi/types';
/**
 * Sets the active view.
 *
 * This action sets the active view by syncing the top window's view when the view is
 * changed so that the top window adjusts the dimension of the app's iframe.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```
 * window.divi.data.dispatch('divi/app-ui').setView('desktop');
 * ```
 */
export declare const setView: (view: string) => AppUi.Store.Actions.SetView.ReturnValue;
