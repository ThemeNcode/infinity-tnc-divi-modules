import { type AppUi } from '@divi/types';
/**
 * Remove sidebar row from sidebar.
 *
 * This action removes sidebar row from given sidebar.
 *
 * @since ??
 *
 * @group Dispatch Functions.
 *
 * @remarks Data Store Dispatch Function.
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/app-ui').removeSidebarRow({
 *   sidebarName:  'sidebarLeft',
 *   sidebarRowId: '1234-5678-9012-3456',
 * })
 * ```
 */
export declare const removeSidebarRow: ({ sidebarName, sidebarRowId, }: AppUi.Store.Actions.RemoveSidebarRow.Params) => AppUi.Store.Actions.RemoveSidebarRow.ReturnValue;
