import { type AppUi } from '@divi/types';
/**
 * Updates the click event target when modules interaction is disabled.
 *
 * This action is dispatched to update the event click target when
 * the modules area is clicked while module interaction is disabled.
 * It accepts a new status value as an argument and returns an action
 * object with the type 'ON_MODULE_INTERACTION_EVENT', and the click event target.
 *
 * @since ??
 *
 * @group Dispatch Functions
 * @remarks Data Store Dispatch Function
 *
 * @example
 *
 * Update modules interaction event target.
 *
 * ```ts
 * window.divi.data.dispatch('divi/app-ui').onModuleInteractionEvent('off');
 * ```
 *
 * @example
 *
 * Update modules interaction event target.
 *
 * ```ts
 * const { onModuleInteractionEvent } = window.divi.data.useDispatch('divi/app-ui');
 * const eventTarget = new Event('click').target;
 * onModuleInteractionEvent(eventTarget);
 * ```
 */
export declare const onModuleInteractionEvent: (event: AppUi.Store.Actions.OnModuleInteractionEvent.Params['event']) => AppUi.Store.Actions.OnModuleInteractionEvent.ReturnValue;
