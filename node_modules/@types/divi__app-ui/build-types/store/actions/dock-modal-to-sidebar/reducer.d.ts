import { type AppUi } from '@divi/types';
/**
 * Reducer function for `dockModalToSidebar()` dispatch function.
 *
 * This reducer function add given modal name to given sidebar name.
 * The reason why the reducer gets abstracted into its own function:
 * 1. Better readability.
 * 2. Values from reducer parameter aren't modified before it reaches this reducer function.
 * 3. There is a case where reducer function being used on other reducer function.
 *
 * @since ??
 */
export declare const dockModalToSidebarReducer: (state: AppUi.Store.ImmutableState, action: AppUi.Store.Actions.DockModalToSidebar.ReturnValue) => AppUi.Store.ImmutableState;
