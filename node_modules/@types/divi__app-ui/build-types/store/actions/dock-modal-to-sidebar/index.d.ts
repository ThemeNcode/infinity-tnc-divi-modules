import { type AppUi } from '@divi/types';
/**
 * Dock modal based on given modal name to sidebar.
 *
 * This action docks given modal name to given sidebar name. Note that this action expects that given modal name
 * is valid and exists in the store. When modal is being docked to sidebar, it assumes that the sidebar is not
 * visible and it has no sidebar row inside it. Thus this makes empty and hidden sidebar into visible sidebar
 * with one automated sidebar row docked inside of it along with the given modal.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 *  window.divi.data.dispatch('divi/app-ui').dockModalToSidebar({
 *    modalName: 'divi/help',
 *    modalType: 'multiInstanceModal',
 *    sidebarName: 'sidebarLeft',
 *    newSidebarRowId: '1234-5678-9012-3456',
 *  });
 * ```
 */
export declare const dockModalToSidebar: ({ modalName, modalType, sidebarName, newSidebarRowId, }: AppUi.Store.Actions.DockModalToSidebar.Params) => AppUi.Store.Actions.DockModalToSidebar.ReturnValue;
