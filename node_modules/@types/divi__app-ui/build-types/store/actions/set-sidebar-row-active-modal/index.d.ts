import { type AppUi } from '@divi/types';
/**
 * Set sidebar row active modal.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/app-ui').setSidebarRowActiveModal({
 *   sidebarRowId: '1234-5678-9012-3456',
 *   activeModal:  'divi/help',
 * })
 * ```
 */
export declare const setSidebarRowActiveModal: ({ sidebarRowId, activeModal, }: AppUi.Store.Actions.SetSidebarRowActiveModal.Params) => AppUi.Store.Actions.SetSidebarRowActiveModal.ReturnValue;
