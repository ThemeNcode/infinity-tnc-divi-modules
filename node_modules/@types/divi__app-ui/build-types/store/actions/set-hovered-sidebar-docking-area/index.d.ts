import { type AppUi } from '@divi/types';
/**
 * Set hovered sidebar docking area.
 *
 * This action marks sidebar docking area as hovered.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function
 *
 * @example Set sidebar right's docking area as hovered
 * ```ts
 * window.divi.data.dispatch('divi/app-ui').setHoveredSidebarDockingArea({
 *   sidebarDockingArea: {
 *     id:   'sidebarRight',
 *     type: 'sidebar',
 *   },
 * });
 * ```
 *
 * @example Set sidebar docking area as not hovered
  * ```ts
 * window.divi.data.dispatch('divi/app-ui').setHoveredSidebarDockingArea({
 *   sidebarDockingArea: false,
 * });
 * ```
 */
export declare const setHoveredSidebarDockingArea: ({ sidebarDockingArea, }: AppUi.Store.Actions.SetHoveredSidebarDockingArea.Params) => AppUi.Store.Actions.SetHoveredSidebarDockingArea.ReturnValue;
