import { type AppUi } from '@divi/types';
/**
 * Sets active mode.
 *
 * This action sets the active Interaction Mode for the Divi Visual Builder.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function
 *
 * @example
 *
 * Controls and outlines will appear when the mouse cursor hovers over a given module.
 *
 * ```ts
 * window.divi.data.dispatch('divi/app-ui').setMode('hover');
 * ```
 *
 * @example
 *
 * Controls and outlines will only appear when the user clicks on a given module.
 *
 * ```ts
 * window.divi.data.dispatch('divi/app-ui').setMode('click');
 * ```
 *
 * @example
 *
 * Controls and outlines for all modules will be visible at all times.
 *
 * ```ts
 * window.divi.data.dispatch('divi/app-ui').setMode('grid');
 * ```
 */
export declare const setMode: (mode: AppUi.Mode.Type) => AppUi.Store.Actions.SetMode.ReturnValue;
