import { type AppUi } from '@divi/types';
/**
 * Updates the interaction status of modules.
 *
 * This action is dispatched to update the interaction status of modules. It accepts a
 * new status value as an argument and returns an action object with the type 'TOGGLE_MODULE_INTERACTION',
 * and the new status value.
 * The action will disable the mousetrap by adding an overlay on the modules
 * section of the page when the interaction status is 'off'.
 *
 * @since ??
 *
 * @group Dispatch Functions
 * @remarks Data Store Dispatch Function
 *
 * @example
 *
 * Update modules interaction status.
 *
 * ```ts
 * window.divi.data.dispatch('divi/app-ui').toggleModuleInteraction('off');
 * ```
 *
 * @example
 *
 * Update modules interaction status.
 *
 * ```ts
 * const { toggleModuleInteraction } = window.divi.data.useDispatch('divi/app-ui');
 * const newStatus = 'on';
 * toggleModuleInteraction(newStatus);
 * ```
 */
export declare const toggleModuleInteraction: (status: AppUi.Store.Actions.ToggleModuleInteraction.Params['status']) => AppUi.Store.Actions.ToggleModuleInteraction.ReturnValue;
