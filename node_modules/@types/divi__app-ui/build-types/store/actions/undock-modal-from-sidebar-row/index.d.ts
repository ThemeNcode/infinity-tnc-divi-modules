import { type AppUi } from '@divi/types';
/**
 * Undock sidebar modal from sidebar row.
 *
 * This action undocks the sidebar modal from the sidebar row in `divi/app-ui` store.
 * If the sidebar row is empty after the modal is undocked, the sidebar row is also undocked.
 * If the sidebar has no sidebar rows after the sidebar row is undocked, the sidebar visibility is set to `false`.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function
 *
 * @example
 *
 * Undock `divi/page-settings` modal from sidebar row with uuid `1234-5678-9012-3456`.
 *
 * ```ts
 * window.divi.data.dispatch('divi/app-ui').undockModalFromSidebarRow({
 *   modalName:    'divi/page-settings',
 *   sidebarRowId: '1234-5678-9012-3456',
 * })
 * ```
 */
export declare const undockModalFromSidebarRow: ({ modalName, sidebarRowId, }: AppUi.Store.Actions.UndockModalFromSidebarRow.Params) => AppUi.Store.Actions.UndockModalFromSidebarRow.ReturnValue;
