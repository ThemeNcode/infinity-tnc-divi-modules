import { type AppUi, type AttrState } from '@divi/types';
/**
 * Sets attribute state.
 *
 * This action sets the state of an active attribute.
 *
 * @since ??
 *
 * @group Dispatch Functions
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/app-ui').setAttributeState('value');
 * ```
 */
export declare const setAttributeState: (attributeState: AttrState) => AppUi.Store.Actions.SetAttributeState.ReturnValue;
