import { type AppUi } from '@divi/types';
/**
 * Add sidebar row to sidebar.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/app-ui').addSidebarRow({
 *   order: 1,
 *   sidebarPosition: 'left',
 *   modal: 'divi/help',
 *   newSidebarRowId: '1234-5678-9012-3456',
 * });
 * ```
 */
export declare const addSidebarRow: ({ order, sidebarPosition, modal, newSidebarRowId, }: AppUi.Store.Actions.AddSidebarRow.Params) => AppUi.Store.Actions.AddSidebarRow.ReturnValue;
