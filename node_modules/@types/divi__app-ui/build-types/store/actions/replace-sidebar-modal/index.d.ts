import { type AppUi } from '@divi/types';
/**
 * Replace existing sidebar modal with new modal.
 *
 * This action replaces modal name that is currently rendered in sidebar with given modal.
 * This is mainly used for replacing currently rendered `singleInstanceModal` on sidebar with
 * newly opened `singleInstanceModal`.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/app-ui').replaceSidebarModal({
 *   modalName:            'divi/module',
 *   overwrittenModalName: 'divi/page-settings',
 * });
 * ```
 */
export declare const replaceSidebarModal: ({ modalName, overwrittenModalName, }: AppUi.Store.Actions.ReplaceSidebarModal.Params) => AppUi.Store.Actions.ReplaceSidebarModal.ReturnValue;
