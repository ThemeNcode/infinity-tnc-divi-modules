import { type AppUi } from '@divi/types';
/**
 * Sets window auto scroll.
 *
 * This action sets the auto-scroll behavior of the window in a specified direction.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * // Scroll to top of the window.
 * window.divi.data.dispatch('divi/app-ui').setWindowAutoScroll('top', 10);
 * ```
 *
 * @example
 * ```ts
 * // Scroll to bottom of the window.
 * window.divi.data.dispatch('divi/app-ui').setWindowAutoScroll('bottom', 100);
 * ```
 *
 * @example
 * ```ts
 * // Clear window's auto scroll.
 * window.divi.data.dispatch('divi/app-ui').setWindowAutoScroll(false);
 * ```
 */
export declare const setWindowAutoScroll: (direction: AppUi.Store.Actions.SetWindowAutoScroll.WindowScrollDirection, distance?: number, location?: string) => AppUi.Store.Actions.SetWindowAutoScroll.ReturnValue;
