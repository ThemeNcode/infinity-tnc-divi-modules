import { type AppUi } from '@divi/types';
/**
 * Set sidebar row's height.
 *
 * @since ??
 *
 * @group Dispatch Functions
 *
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/app-ui').setSidebarRowHeight({
 *   sidebarRowId: '1234-5678-9012-3456',
 *   height:       600,
 * });
 * ```
 */
export declare const setSidebarRowHeight: ({ sidebarRowId, height, }: AppUi.Store.Actions.SetSidebarRowHeight.Params) => AppUi.Store.Actions.SetSidebarRowHeight.ReturnValue;
