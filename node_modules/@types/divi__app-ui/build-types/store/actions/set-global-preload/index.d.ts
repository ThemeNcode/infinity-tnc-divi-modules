import { type AppUi } from '@divi/types';
/**
 * Sets global preload.
 *
 * This action sets the global preload status.
 *
 * @since ??
 *
 * @group Dispatch Functions
 * @remarks Data Store Dispatch Function
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/app-ui').setGlobalPreload(true);
 * ```
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/app-ui').setGlobalPreload(false);
 * ```
 */
export declare const setGlobalPreload: (isActive: boolean) => AppUi.Store.Actions.SetGlobalPreload.ReturnValue;
