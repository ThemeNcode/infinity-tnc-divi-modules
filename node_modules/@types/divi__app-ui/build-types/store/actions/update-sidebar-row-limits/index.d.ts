import { type AppUi } from '@divi/types';
/**
 * Generate sidebar rows' limits.
 *
 * This action generates sidebar rows' limits such as maximum height, etc.
 *
 * @since ??
 *
 * @group Dispatch Functions.
 *
 * @remarks Data Store Dispatch Function.
 *
 * @example
 * ```ts
 * window.divi.data.dispatch('divi/app-ui').updateSidebarRowLimits();
 * ```
 */
export declare const updateSidebarRowLimits: (sidebarRowId: string) => AppUi.Store.Actions.UpdateSidebarRowLimits.ReturnValue;
