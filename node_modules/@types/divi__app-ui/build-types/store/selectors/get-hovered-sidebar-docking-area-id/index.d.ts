import { type AppUi } from '@divi/types';
/**
 * Get sidebar's docking area that is currently hovered.
 *
 * The returned value of this selector function is used to render docking indicator for given sidebar.
 * When modal is not currently dragged above sidebar's docking area, returns `false`.
 * When modal is currently dragged on sidebar's docking area, returns either `sidebarLeft` or `sidebarRight`.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * window.divi.data.select('divi/app-ui').getHoveredSidebarDockingAreaId();
 * ```
 */
export declare const getHoveredSidebarDockingAreaId: (state: AppUi.Store.ImmutableState) => false | string;
