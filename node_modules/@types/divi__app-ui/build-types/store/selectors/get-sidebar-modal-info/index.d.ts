import { type AppUi } from '@divi/types';
/**
 * Get sidebar modal info.
 *
 * Sidebar modal refers to modal that is being rendered inside sidebar.
 * Sidebar modal info refers to sidebar row's id, sidebar name, and whether the modal is currently active or not.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * window.divi.data.select('divi/app-ui').getSidebarModalInfo('divi/help');
 * ```
 */
export declare const getSidebarModalInfo: (state: AppUi.Store.ImmutableState, modalName: string) => AppUi.Sidebar.Modal.Info;
