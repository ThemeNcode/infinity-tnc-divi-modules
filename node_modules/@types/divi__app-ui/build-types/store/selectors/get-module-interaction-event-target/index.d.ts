import { type AppUi } from '@divi/types';
/**
 * Gets modules interaction click event target.
 *
 * Retrieves the status of modules interaction from the module store state.
 * The status can be 'on' or 'off'.
 * An 'off' status add an overlay on the modules section of the page to disable the mousetrap.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * const scripts = window.divi.data.select('divi/app-ui').getModuleInteractionEventTarget();
 * ```
 */
export declare const getModuleInteractionEventTarget: (state: AppUi.Store.ImmutableState) => AppUi.Store.ImmutableState['moduleInteraction']['event'];
