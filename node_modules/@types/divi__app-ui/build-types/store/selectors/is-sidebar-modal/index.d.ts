import { type AppUi } from '@divi/types';
/**
 * Check if given modal name is currently rendered in sidebar.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * window.divi.data.select('divi/app-ui').isSidebarModal('divi/help');
 * ```
 */
export declare const isSidebarModal: (state: AppUi.Store.ImmutableState, modalName: string) => boolean;
