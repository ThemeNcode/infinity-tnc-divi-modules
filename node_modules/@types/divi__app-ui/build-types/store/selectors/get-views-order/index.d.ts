import { type AppUi } from '@divi/types';
/**
 * Gets app views order.
 *
 * Retrieves the order of views from the app-ui store state.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * window.divi.data.select('divi/app-ui').getViewsOrder();
 * ```
 */
export declare const getViewsOrder: (state: AppUi.Store.ImmutableState) => AppUi.Store.ImmutableState['viewsOrder'];
