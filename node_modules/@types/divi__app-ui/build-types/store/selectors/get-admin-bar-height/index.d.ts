import { type AppUi } from '@divi/types';
/**
 * Gets admin bar height.
 *
 * Retrieves the height of the admin bar from the app-ui store.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * window.divi.data.select('divi/app-ui').getAdminBarHeight('top');
 * ```
 */
export declare const getAdminBarHeight: (state: AppUi.Store.ImmutableState, windowLocation: 'top' | 'app') => number;
