import { type AppUi } from '@divi/types';
/**
 * Gets the globalPreload status.
 *
 * This action retrieves the value of the globalPreload state from the app-ui store.
 *
 * This function checks if the globalPreload state in the app-ui store is greater than `0`.
 * If it is, it returns `true`, indicating that global preload is enabled.
 * Otherwise, it returns `false`.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * const isGlobalPreloadEnabled = window.divi.data.select('divi/app-ui').getGlobalPreload();
 * console.log(isGlobalPreloadEnabled); // true or false
 * ```
 */
export declare const getGlobalPreload: (state: AppUi.Store.ImmutableState) => boolean;
