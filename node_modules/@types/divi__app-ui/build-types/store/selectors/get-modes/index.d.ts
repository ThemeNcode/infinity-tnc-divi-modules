import { type AppUi } from '@divi/types';
/**
 * Retrieves app modes.
 *
 * Retrieves available app modes from the app-ui store.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * const modes = window.divi.data.select('divi/app-ui').getModes();
 * ```
 */
export declare const getModes: (state: AppUi.Store.ImmutableState) => AppUi.Mode.Items;
