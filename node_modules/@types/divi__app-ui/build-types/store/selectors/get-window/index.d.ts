import { type AppUi } from '@divi/types';
/**
 * Retrieves window attributes.
 *
 * Retrieves an attribute value from the `window` property based on
 * the provided `attr` and `windowLocation` parameters.
 * If the value is not found, the `defaultValue` parameter is returned.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * const windowHeight = window.divi.data.select('divi/app-ui').getWindow('height', 'app');
 * ```
 *
 * @example
 * ```ts
 * const windowWidth = window.divi.data.select('divi/app-ui').getWindow('width', 'top');
 * ```
 */
export declare const getWindow: (state: AppUi.Store.ImmutableState, attr: string, windowLocation: string, defaultValue?: number) => number;
