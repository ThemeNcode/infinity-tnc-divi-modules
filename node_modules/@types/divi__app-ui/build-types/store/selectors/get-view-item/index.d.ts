import { type AppUi } from '@divi/types';
/**
 * Gets view item info.
 *
 * Retrieves a view's settings definition info from the app-ui store. This definition
 * includes values for `width`, `height`, and `breakpoint`. It will also specify a
 * `label` and an `icon` which can be used for rendering a button to toggle the view.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * const view = window.divi.data.select('divi/app-ui').getViewItem('zoom');
 * ```
 *
 * @example
 * ```ts
 * const view = window.divi.data.select('divi/app-ui').getViewItem('desktop');
 * ```
 */
export declare const getViewItem: (state: AppUi.Store.ImmutableState, view: AppUi.View.Type) => AppUi.View.Item;
