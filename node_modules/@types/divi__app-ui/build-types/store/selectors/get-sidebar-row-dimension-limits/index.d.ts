import { type AppUi } from '@divi/types';
/**
 * Get sidebar row's dimension limit. The actual dimension of sidebar row can't be more / less than this.
 * Mostly used on resizing sidebar row's resize handle to prevent it being resized too short or tall.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example
 * ```
 * const heightLimit = window.divi.data.select('divi/app-ui').getSidebarRowDimensionLimits(rowId, 'height');
 * ```
 */
export declare const getSidebarRowDimensionLimits: (state: AppUi.Store.ImmutableState, sidebarRowId: string, property: keyof AppUi.Sidebar.Dimension) => {
    max: number;
    min: number;
};
