import { type AppUi } from '@divi/types';
/**
 * Checks if given interaction should exist in current mode.
 *
 * Checks if the given mode interaction is present in the application UI state.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * const hasInteraction = window.divi.data.select('divi/app-ui').hasModeInteraction([
 *   'parent',
 *   'highlightOnHover'
 * ]);
 * ```
 *
 * @example
 * ```ts
 * const hasInteraction = window.divi.data.select('divi/app-ui').hasModeInteraction([
 *   'module',
 *   'options'
 * ]);
 * ```
 *
 * @example
 * ```ts
 * const hasInteraction = window.divi.data.select('divi/app-ui').hasModeInteraction([
 *   'module',
 *   'highlight'
 * ]);
 * ```
 */
export declare const hasModeInteraction: (state: AppUi.Store.ImmutableState, interaction: [AppUi.Mode.InteractionLocation, keyof AppUi.Mode.Interaction]) => boolean;
