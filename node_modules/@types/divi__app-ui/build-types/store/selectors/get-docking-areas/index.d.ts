import { type AppUi } from '@divi/types';
import { type DraggedModal } from './types';
/**
 * Get all docking areas in offsets and dimensions.
 *
 * These docking areas offsets and dimensions are used to be compared with current modal's offset to see
 * whether's current modal's offset is within any of these docking areas.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example Get all available docking areas based on current `divi/app-ui` store state.
 * ```ts
 * window.divi.data.select('divi/app-ui').getDockingAreas();
 * ```
 */
export declare const getDockingAreas: (state: AppUi.Store.ImmutableState, draggedModal?: DraggedModal) => AppUi.Docking.Areas;
