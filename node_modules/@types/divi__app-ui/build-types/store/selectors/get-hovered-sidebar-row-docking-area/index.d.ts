import { type AppUi } from '@divi/types';
/**
 * Get sidebar row's hovered docking area.
 *
 * The returned value of this selector function is used to render docking indicator for given sidebar row.
 * When modal is not currently dragged above sidebar row's docking area, returns `false`.
 * When modal is currently draggade on sidebar row's docking area, returns either `above`, `inside`, or `below`.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * window.divi.data.select('divi/app-ui').getHoveredSidebarRowDockingArea('1234-5678-9012-3456');
 * ```
 */
export declare const getHoveredSidebarRowDockingArea: (state: AppUi.Store.ImmutableState, sidebarRowId: string) => AppUi.SidebarRow.DockingArea;
