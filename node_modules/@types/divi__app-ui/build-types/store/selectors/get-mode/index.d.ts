import { type AppUi } from '@divi/types';
/**
 * Retrieves the active app mode.
 *
 * Retrieves the current mode from the app-ui store.
 * If the `mode` property is not found in the state, the default value `'hover'` is returned.
 *
 * @since ??
 *
 * @group Selector Functions
 *
 * @remarks Data Store Selector Function
 *
 * @example
 * ```ts
 * window.divi.data.select('divi/app-ui').getMode();
 * ```
 */
export declare const getMode: (state: AppUi.Store.ImmutableState) => AppUi.Mode.Type;
