import { type WpMedia } from '@divi/types-external';
declare global {
    interface Window {
        wp: {
            media?: WpMedia;
            data?: typeof import('@wordpress/data');
            mediaelement?: {
                initialize?: () => void;
            };
        };
    }
}
