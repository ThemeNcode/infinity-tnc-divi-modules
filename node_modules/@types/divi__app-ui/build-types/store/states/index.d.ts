import { type AppUi } from '@divi/types';
/**
 * Initial States.
 * Initial state gets exported so test can use the exact same initial state as the store.
 *
 * @since ??
 *
 * @type {object}
 * @property {string} attributeState
 * @property {string} breakpoint
 * @property {string} mode
 * @property {string} view
 * @property {object} windowAutoScroll
 * @property {object} modeInteractions
 * @property {object} views
 * @property {string[]} viewsOrder
 * @property {object} modes
 * @property {object} window
 * @property {object} adminBarHeight
 * @property {number} globalPreload
 */
export declare const initialState: AppUi.Store.ImmutableState;
