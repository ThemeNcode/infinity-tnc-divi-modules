import { type AppPreferences, type PageSettings, type Settings } from '@divi/types';
import { type PostStatus } from '@divi/types-external';
export type AjaxRequestPayload = {
    post_id: string;
    post_status: PostStatus;
    layout_type: string;
    et_post_type: string;
    return_to_default_editor: boolean;
    options: Options;
    unsyncedGlobalSettings: string;
    et_builder_mode: 'vb' | 'limited';
    et_builder_version: string;
    ab_testing: AbTesting;
    pageSettings: PageSettings.Store.State;
    preferences: AppPreferences.Store.ImmutableState;
    content: {
        post_content: string;
        header: string;
        footer: string;
        body: string;
    };
};
export interface AbTesting {
    is_clear_stats: boolean;
}
export interface Options {
    conditional_tags: Settings.Store.ConditionalTags;
    post_type: string;
    status: PostStatus;
    return_rendered_content: boolean;
}
/**
 * Ajax response interface.
 *
 * @since ??
 */
export type AjaxResponse = {
    save_verification?: boolean;
    rendered_content?: string;
    post_status?: PostStatus;
};
