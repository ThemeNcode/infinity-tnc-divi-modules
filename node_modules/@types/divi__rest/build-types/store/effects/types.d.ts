import { type Rest } from '@divi/types';
export type Effect<TAction> = (action: TAction, store: Rest.Store.ImmutableState) => void;
export interface Effects {
    SYNC_TO_SERVER: Effect<Extract<Rest.Store.Actions.ReturnValue, {
        type: 'SYNC_TO_SERVER';
    }>>[];
}
