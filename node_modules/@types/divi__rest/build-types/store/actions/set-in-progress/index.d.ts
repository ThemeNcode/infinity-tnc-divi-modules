import { type Rest } from '@divi/types';
/**
 * Set ajax in progress state.
 *
 * @since ??
 */
export declare const setInProgress: (ajaxAction: string, isInProgress: boolean) => Rest.Store.Actions.SetInProgress.ReturnValue;
