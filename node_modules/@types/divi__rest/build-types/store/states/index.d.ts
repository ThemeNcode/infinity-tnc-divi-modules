import { type Rest } from '@divi/types';
/**
 * Writable portion of the initial state.
 *
 * The reason for this to be prefixed by `writable*` is because there is readonly property
 * which its value is retrieved from another store. Once those value is set, it won't be
 * changed on the fly thus we should only declare and use the writable portion to overwrite
 * the state (for example for `resetState()`.
 */
export declare const writableInitialState: Omit<Rest.Store.State, 'url'>;
