import { type Rest } from '@divi/types';
/**
 * Check is ajax request in progress.
 *
 * @since ??
 */
export declare const isInProgress: (state: Rest.Store.ImmutableState, ajaxAction?: string | string[]) => boolean;
