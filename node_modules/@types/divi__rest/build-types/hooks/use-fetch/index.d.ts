import { type UseFetch } from './types';
/**
 * Use Fetch.
 *
 * A React hook for handling data fetching.
 *
 * Returns an object consisted of:
 *
 * - `fetch()`: A function for making a fetch request to a specified URL.
 *
 * - `response`: The response data from a successful fetch request.
 *
 * - `abort()`: A function for aborting a fetch request.
 *
 * - `isLoading`: A flag indicating whether the current fetch request is in progress.
 *
 * @since ??
 *
 * @param {unknown} value The initial value of the response. Defaults to `null`.
 *
 * @returns {object}
 */
export declare const useFetch: <TResponseType = unknown>(value?: TResponseType) => UseFetch<TResponseType>;
