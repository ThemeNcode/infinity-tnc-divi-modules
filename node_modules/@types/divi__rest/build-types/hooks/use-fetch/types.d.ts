import { type FetchOptions } from '../../utils/fetch/types';
export type Fetch<TResponseType> = (params: FetchOptions) => Promise<TResponseType>;
export type UseFetch<TResponseType> = {
    fetch: Fetch<TResponseType>;
    response: TResponseType;
    abort: () => void;
    isLoading: boolean;
};
