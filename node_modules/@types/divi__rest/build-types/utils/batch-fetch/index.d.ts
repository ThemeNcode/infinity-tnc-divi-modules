import { type FetchOptions } from '../fetch/types';
/**
 * Batch Fetch.
 *
 * Do `fetch` that contains batch of requests if any.
 *
 * Collect all `loggedFetch` calls with same REST API endpoint in certain `delayTime`.
 * If there are more than one request, batch the collection of data requests into single
 * data request. Otherwise, continue with current data request.
 *
 * Make a request with `fetch` method and send the single or batch data to server. Once
 * it's done, update each of request logs with the updated result. Finally, return the
 * response to each calls.
 * Note this function is asynchronous.
 *
 * @since ??
 *
 * @param {object} options Accepts all native `fetch` parameters as well as WP Rest API specific ones.
 * @param {string} options.restRoute `REST API` endpoint to fetch.
 * @param {object} options.data Data to send to the endpoint.
 * @param {object} options.headers Headers to send to the endpoint.
 * @param {string} options.url URL to send to the endpoint.
 * @param {string} options.parseResponse Parse response to send to the endpoint.
 * @param {string} options.timeout Timeout to send to the endpoint.
 * @param {string} options.responseType Response type to send to the endpoint. Accepts `json` or `stream`.
 * @param {string} options.onDownloadProgress Callback function to call with download progress.
 * @param {string} options.forceRequest Whether to force the request.
 * @param {string} options.batchRequest Whether to sent request as batch.
 * @param {number} requestTimeOut REST API request time out to avoid unresolved request.
 *
 * @returns {Promise<any>} Response promise on success, Rejected promise if parsing response fails,
 * or HTTP status is not 2xx OK .
 */
export declare const batchFetch: (options: FetchOptions, requestTimeOut?: number) => Promise<any>;
