import { type FetchData } from '../types';
/**
 * Return body to be sent with the request.
 *
 * @since ??
 *
 * @param {BodyInit} body Fetch body.
 * @param {FetchData} data Data object sent with Divi fetch.
 * @param {string} method HTTP method set on the rest route.
 *
 * @returns {BodyInit}
 */
declare const getBody: (body: BodyInit, data: FetchData, method: string) => BodyInit;
export { getBody, };
