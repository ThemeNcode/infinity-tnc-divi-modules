import { type FetchData } from '../types';
import { type FetchHeaders } from './types';
/**
 * Returns default headers which should be sent with every Divi fetch request.
 *
 * WordPress backend uses the Accept header as a condition for considering an
 * incoming request as a REST request.
 *
 * @since ??
 *
 * @param {FetchHeaders} headers Headers to set the nonces in.
 *
 * @see https://core.trac.wordpress.org/ticket/44534
 */
declare const getDefaultHeaders: (headers: FetchHeaders) => FetchHeaders;
/**
 * Sets WP and ET nonces on the headers and returns it.
 *
 * @param {FetchHeaders} headers Headers to set the nonces in.
 * @param {string} restRoute WP Rest Route.
 * @param {string} method HTTP method set on the rest route.
 *
 * @returns {FetchHeaders} Headers with nonces set on it.
 */
declare const getNonceHeaders: (headers: FetchHeaders, restRoute: string, method: string) => FetchHeaders;
/**
 * Sets `'Content-Type': 'application/json'` on the headers and returns it.
 *
 * @param {FetchHeaders} headers Headers to set the nonces in.
 *
 * @returns {FetchHeaders} Headers with content type json set on it.
 */
declare const getJSONHeaders: (headers: FetchHeaders) => FetchHeaders;
/**
 * Returns headers for the given rest route and method.
 * If rest route is not provided, it will return the default headers.
 *
 * @param {FetchHeaders} headers Headers to set the nonces in.
 * @param {string} restRoute WP Rest Route.
 * @param {string} method HTTP method set on the rest route.
 * @param {FetchData} data Data to be sent with the request.
 *
 * @returns {FetchHeaders}
 */
declare const getHeaders: (headers: FetchHeaders, restRoute: string, method: string, data: FetchData) => FetchHeaders;
export { getDefaultHeaders, getNonceHeaders, getJSONHeaders, getHeaders, };
