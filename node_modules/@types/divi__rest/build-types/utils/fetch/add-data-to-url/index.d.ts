import { type FetchData } from '../types';
/**
 * Converts `data` to query parameters & treats REST route format so API calls would work with WP default permalink.
 *
 * WP default permalink REST root: http://localhost/index.php?rest_route=.
 * WP pretty permalink REST root:  http://localhost/wp-json.
 * Working URL:                    http://localhost/index.php?rest_route=/divi/v1/portfolio&postsPerPage=10.
 * Non-Working URL:                http://localhost/index.php?rest_route=/divi/v1/portfolio?postsPerPage=10.
 *
 * @param {object} data Fetch data object.
 * @param {string} restRoute WordPress rest route.
 * @param {string} rootURL WordPRess rest root URL.
 *
 * @returns {string}
 */
declare const addDataToUrl: (data: FetchData, restRoute: string, rootURL: string) => string;
export { addDataToUrl, };
