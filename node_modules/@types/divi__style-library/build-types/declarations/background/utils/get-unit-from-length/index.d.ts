/**
 * Get unit of measurement used for gradient stop positions.
 *
 * The "Gradient Length" setting contains both a number value and a unit of
 * measurement. The unit of measurement gets used in other places, so this
 * function will parse that and make that data available elsewhere.
 *
 * @since ??
 *
 * @param {string} gradientLength The "Gradient Length" setting value.
 *
 * @returns {string} The unit of measurement used for gradient stop positions.
 */
export declare const getUnitFromLength: (gradientLength: string) => string;
