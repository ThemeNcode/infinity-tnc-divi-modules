/**
 * Helper function to return CSS to Transform the SVG.
 *
 * @since ??
 *
 * We use `scale` here because CSS Transform's `rotateX`/`rotateY` trigger a 10+
 * year old Safari bug that hides rotated background images (including SVGs).
 *
 * @see https://bugs.webkit.org/show_bug.cgi?id=61824
 *
 * @param {boolean} horizontal Whether to Flip Horizontally.
 * @param {boolean} vertical Whether to Flip Vertically.
 *
 * @returns {string} Transform CSS Style.
 */
export declare const getBackgroundTransformCss: (horizontal: boolean, vertical: boolean) => string;
