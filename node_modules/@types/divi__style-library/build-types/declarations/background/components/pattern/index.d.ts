import { type BackgroundDeclarationParams } from '../../types';
/**
 * Generate a background pattern CSS declaration based on the given attribute value.
 *
 * @since ??
 *
 * @param {object} params Parameters for the background pattern declaration.
 * @param {boolean} params.important Indicates whether the declaration is important. Default is false.
 * @param {object} params.attrValue The value of the background pattern attribute.
 * @param {object} params.defaultAttr Default background pattern attributes.
 * @param {string} params.returnType The return type of the declaration.
 * @param {string} params.keyFormat The format for keys in the declaration.
 *
 * @returns {string|object} The background pattern CSS declaration.
 */
export declare const backgroundPatternStyleDeclaration: ({ important, attrValue, defaultAttr, returnType, keyFormat, }: BackgroundDeclarationParams) => string | Record<string, string>;
