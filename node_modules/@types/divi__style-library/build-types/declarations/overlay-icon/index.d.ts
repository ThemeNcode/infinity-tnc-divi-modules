import { type OverlayIconStyleDeclarationParams } from './types';
/**
 * Get overlay icon CSS declaration.
 *
 * TODO feat(D5, Refactor) this might need to be refactored into `overlay` style declaration
 * which is used by <Overlay /> style component that is used by multiple modules.
 *
 * @since ??
 *
 * @param {object} params Parameters for the overlay icon CSS declaration.
 * @param {object} params.attrValue The value of the attribute.
 * @param {boolean} params.important Indicates whether the declaration is important.
 * @param {string} params.returnType The return type of the declaration.
 *
 * @returns {string|object} Overlay Icon CSS declaration.
 */
export declare const overlayIconStyleDeclaration: ({ attrValue, important, returnType, }: OverlayIconStyleDeclarationParams) => string | Record<string, string>;
export * from './types';
