import { type ComposeTransitionCssPropertiesParams } from '../../types';
/**
 * Compose transition CSS properties for sticky and hover modes.
 *
 * @since ??
 */
declare const composeTransitionCssProperties: ({ mode, attrs, }: ComposeTransitionCssPropertiesParams) => string[];
export { composeTransitionCssProperties, };
