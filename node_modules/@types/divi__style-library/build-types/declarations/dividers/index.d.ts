import { type DividersDeclarationParams } from './types';
/**
 * Get dividers CSS declaration based on the given parameters.
 *
 * @since ??
 *
 * @param {object} params Parameters for the dividers CSS declaration.
 * @param {object} params.attrValue The value of the dividers attribute.
 * @param {object} params.defaultAttr Default dividers attributes.
 * @param {string} params.placement The placement of the dividers.
 * @param {string} params.breakpoint The breakpoint for the dividers.
 * @param {string} params.state The state of the dividers.
 * @param {string[]} params.backgroundColors The background colors for the dividers.
 * @param {boolean} params.fullwidth Whether the dividers are full width. Default is false.
 * @param {boolean} params.important Indicates whether the declaration is important. Default is false.
 * @param {string} params.returnType The return type of the declaration.
 * @param {string} params.keyFormat The format for keys in the declaration.
 *
 * @returns {string|object} Dividers CSS declaration.
 */
export declare const dividersStyleDeclaration: ({ attrValue, defaultAttr, placement, breakpoint, state, backgroundColors, fullwidth, important, returnType, keyFormat, }: DividersDeclarationParams) => string | Record<string, string>;
export * from './constants';
export * from './types';
export * from './utils';
