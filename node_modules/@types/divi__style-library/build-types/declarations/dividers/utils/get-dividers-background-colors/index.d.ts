import { type DividerBackgroundColorsParams } from './types';
/**
 * Get background colors for a divider.
 *
 * Function that, using this component's id, will get either the previous or the
 * next sibling component's background color if set. If sibling is not found or
 * sibling's background color is not set, return an empty string.
 *
 * @since ??
 *
 * @param {object} props DividerBackgroundColorsParams.
 * @param {object} props.siblingBackgroundAttr Background attributes of the sibling component.
 * @param {object} props.moduleBackgroundAttr Background attributes of the module component.
 * @param {string} props.defaultColor Default color to return if no color specified.
 * @param {string} props.breakpoint Breakpoint value.
 * @param {string} props.state State value.
 *
 * @returns {object} Sibling component's background color, or empty string if no color specified.
 */
export declare const getDividersBackgroundColors: ({ siblingBackgroundAttr, moduleBackgroundAttr, defaultColor, breakpoint, state, }: DividerBackgroundColorsParams) => Record<string, string>;
