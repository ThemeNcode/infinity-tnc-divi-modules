import { type IconStyleDeclarationParams } from './types';
/**
 * Get Icon's CSS declaration based on the given attribute value.
 *
 * @since ??
 *
 * @param {object} params Parameters for the Icon's CSS declaration.
 * @param {object} params.important Object indicating which properties are important. Default is an empty object.
 * @param {object} params.attrValue The value of the Icon attribute.
 * @param {string} params.returnType The return type of the declaration.
 *
 * @returns {string|object} Icon's CSS declaration.
 */
export declare const iconStyleDeclaration: ({ important, attrValue, returnType, }: IconStyleDeclarationParams) => string | Record<string, string>;
export * from './types';
