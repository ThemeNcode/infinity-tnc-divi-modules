/**
 * Join array of declarations into `;` separated string, suffixed by `;`.
 *
 * @since ??
 *
 * @param {string[]} declarations Array of declarations.
 *
 * @returns {string} Joined style declarations.
 */
export declare const joinDeclarations: (declarations: string[]) => string;
