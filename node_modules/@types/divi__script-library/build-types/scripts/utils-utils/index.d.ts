/**
 * Get Builder utils parameters.
 *
 * Get the `et_builder_utils_params` object for either the current window or top window (whichever is found first).
 *
 * @since ??
 *
 * @returns {object} Builder utils parameters.
 */
export declare const getBuilderUtilsParams: () => any;
/**
 * Get the Builder environment type.
 *
 * Get the Builder Type value within `et_builder_utils_params`. Possible values are:
 * - fe: Frontend
 * - vb: Visual Builder
 * - bfb: New Builder Experience
 * - tb: Theme Builder
 * - lbb: Layout Block Builder
 * - lbp: Layout Block Preview
 * - '' (empty string): Unknown
 *
 * @since ??
 *
 * @returns {object} Builder utils parameter `builderType`.
 */
export declare const getBuilderType: () => any;
/**
 * Check Builder type for the current page.
 *
 * Check if current page's builder type is the same as the one passed in the argument.
 *
 * @since 4.6.0
 *
 * @param {string} builderType Fe|vb|bfb|tb|lbb|lbp.
 *
 * @returns {bool} True if current page's builder type is the same as the one passed in the argument.
 */
export declare const isBuilderType: (builderType: any) => boolean;
/**
 * Return condition value.
 *
 * Check if current page has a given condition set to true.
 * Condition value is set in `et_builder_utils_params` object.
 * Possible values are:
 * - diviTheme: Divi Theme
 * - extraTheme: Extra Theme
 *
 * @since 4.6.0
 *
 * @param {string} conditionName Condition name.
 *
 * @returns {bool} True if current page's condition value is the same as the one passed in the argument.
 */
export declare const is: (conditionName: any) => any;
/**
 * Is current page Frontend.
 *
 * Check if current page is rendered in frontend.
 *
 * @since 4.6.0
 *
 * @returns {bool} True if current page is rendered in frontend.
 */
export declare const isFE: boolean;
/**
 * Is current page Visual Builder.
 *
 * Check if current page is rendered in Visual Builder.
 *
 * @since 4.6.0
 *
 * @returns {bool} True if current page is rendered in Visual Builder.
 */
export declare const isVB: boolean;
/**
 * Is current page BFB / New Builder Experience.
 *
 * Check if current page is rendered in BFB / New Builder Experience.
 *
 * @since 4.6.0
 *
 * @returns {bool} True if current page is rendered in BFB / New Builder Experience.
 */
export declare const isBFB: boolean;
/**
 * Is current page Theme Builder.
 *
 * Check if current page is rendered in Theme Builder.
 *
 * @since 4.6.0
 *
 * @returns {bool} True if current page is rendered in Theme Builder.
 */
export declare const isTB: boolean;
/**
 * Is current page Layout Block Builder.
 *
 * Check if current page is rendered in Layout Block Builder.
 *
 * @since ??
 *
 * @returns {bool} True if current page is rendered in Layout Block Builder.
 */
export declare const isLBB: boolean;
/**
 * Is current page uses Divi Theme.
 *
 * Check if current page uses Divi Theme.
 *
 * @since 4.6.0
 *
 * @returns {bool} True if current page uses Divi Theme.
 */
export declare const isDiviTheme: any;
/**
 * Is current page uses Extra Theme.
 *
 * Check if current page uses Extra Theme.
 *
 * @since 4.6.0
 *
 * @returns {bool} True if current page uses Extra Theme.
 */
export declare const isExtraTheme: any;
/**
 * Is current page Layout Block Preview.
 *
 * Check if current page is rendered in Layout Block Preview.
 *
 * @since 4.6.0
 *
 * @returns {bool} True if current page is rendered in Layout Block Preview.
 */
export declare const isLBP: boolean;
/**
 * Is current page Block Editor.
 *
 * Check if current window is a block editor window (gutenberg editing page).
 *
 * @since 4.6.0
 *
 * @returns {bool} True if current window is a block editor window (gutenberg editing page).
 */
export declare const isBlockEditor: boolean;
/**
 * Is current page a Builder window.
 *
 * Check if current window is a builder window (VB, BFB, TB, LBB).
 *
 * @since 4.6.0
 *
 * @returns {bool} True if current window is a builder window (VB, BFB, TB, LBB).
 */
export declare const isBuilder: boolean;
/**
 * Get offsets value of all sides.
 *
 * Get offsets value of all sides (top, right, bottom, left) of a given element.
 *
 * @since 4.6.0
 *
 * @param {object} $selector JQuery selector instance.
 * @param {number} height Height of the element.
 * @param {number} width Width of the element.
 *
 * @returns {object} Object containing offsets value of all sides (top, right, bottom, left).
 */
export declare const getOffsets: ($selector: any, width?: number, height?: number) => any;
/**
 * Maybe increase max listeners.
 *
 * Increase EventEmitter's max listeners if lister count is about to surpass the max listeners limit
 * IMPORTANT: This needs to be placed BEFORE `.on()`.
 *
 * @since 4.6.0
 *
 * @param {EventEmitter} emitter EventEmitter instance.
 * @param {string} eventName Event name.
 *
 * @returns {void}
 */
export declare const maybeIncreaseEmitterMaxListeners: (emitter: any, eventName: any) => void;
/**
 * Maybe decrease max listeners.
 *
 * Decrease EventEmitter's max listeners if listener count is less than max listener limit and above
 * 10 (default max listener limit). If listener count is less than 10, max listener limit will
 * remain at 10
 * IMPORTANT: This needs to be placed AFTER `.removeListener()`.
 *
 * @since 4.6.0
 *
 * @param {EventEmitter} emitter EventEmitter instance.
 * @param {string} eventName Event name.
 *
 * @returns {void}
 */
export declare const maybeDecreaseEmitterMaxListeners: (emitter: any, eventName: any) => void;
/**
 * Register the Frontend component.
 *
 * Expose frontend (FE) component via global object so it can be accessed and reused externally
 * Note: `window.ET_Builder` is for builder app's component; `window.ET_FE` is for frontend component.
 *
 * @since 4.6.0
 *
 * @param {string} type Module type.
 * @param {string} name Module name.
 * @param {mixed} component Module component.
 *
 * @returns {void}
 */
export declare const registerFrontendComponent: (type: any, name: any, component: any) => void;
/**
 * Set inline style with an `!important` tag.
 *
 * JQuery's .css() can't set value with an `!important` tag, so here it is.
 *
 * @since 4.6.2
 *
 * @param {object} $element JQuery selector instance.
 * @param {string} cssProp CSS property.
 * @param {string} value CSS property value.
 *
 * @returns {void}
 */
export declare const setImportantInlineValue: ($element: any, cssProp: any, value: any) => void;
