import { type Breakpoint } from '@divi/types';
import { type MultiViewDataUpdateStyle } from '../../types';
/**
 * Update element inline styles.
 *
 * @since ??
 *
 * @param {MultiViewDataUpdateStyle} item The item that will be updated.
 * @param {Breakpoint} breakpoint The current breakpoint, which can be 'phone', 'tablet', or 'desktop'.
 * @param {boolean} isInit Whether this is the first time the function is being called.
 */
export declare const updateDataStyle: (item: MultiViewDataUpdateStyle, breakpoint: Breakpoint, isInit?: boolean) => void;
