import { type Breakpoint } from '@divi/types';
import { type MultiViewDataUpdateAttrs } from '../../types';
/**
 * Update element attributes.
 *
 * @since ??
 *
 * @param {MultiViewDataUpdateAttrs} item The item that will be updated.
 * @param {Breakpoint} breakpoint The current breakpoint, which can be 'phone', 'tablet', or 'desktop'.
 * @param {boolean} isInit Whether this is the first time the function is being called.
 */
export declare const updateDataAttrs: (item: MultiViewDataUpdateAttrs, breakpoint: Breakpoint, isInit?: boolean) => void;
