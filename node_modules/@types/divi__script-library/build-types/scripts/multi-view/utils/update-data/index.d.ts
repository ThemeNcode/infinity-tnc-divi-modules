import { type MultiViewUpdateDataCallback } from '../../types';
/**
 * Update elements.
 *
 * This is function that takes in an array of data and a breakpoint, and updates the data
 * attributes, class name, content, style, or visibility based on the specified action for each item in
 * the array.
 *
 * @since ??
 *
 * @param {MultiViewData[]} items An array of objects containing data for multiple views.
 * @param {Breakpoint} breakpoint The current breakpoint, which can be 'phone', 'tablet', or 'desktop'.
 * @param {boolean} isInit Whether this is the first time the function is being called.
 */
export declare const updateData: MultiViewUpdateDataCallback;
