import { type Breakpoint } from '@divi/types';
import { type MultiViewDataUpdateClassName } from '../../types';
/**
 * Update element class name.
 *
 * @since ??
 *
 * @param {MultiViewDataUpdateClassName} item The item that will be updated.
 * @param {Breakpoint} breakpoint The current breakpoint, which can be 'phone', 'tablet', or 'desktop'.
 * @param {boolean} isInit Whether this is the first time the function is being called.
 */
export declare const updateDataClassName: (item: MultiViewDataUpdateClassName, breakpoint: Breakpoint, isInit?: boolean) => void;
