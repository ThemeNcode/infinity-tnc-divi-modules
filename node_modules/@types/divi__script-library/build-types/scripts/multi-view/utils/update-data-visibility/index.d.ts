import { type Breakpoint } from '@divi/types';
import { type MultiViewDataUpdateVisibility } from '../../types';
/**
 * Update element visibility.
 *
 * This function is used to update the element visibility by adding and removing `et_multi_view_hidden` class name.
 *
 * @since ??
 *
 * @param {MultiViewDataUpdateVisibility} item The item that will be updated.
 * @param {Breakpoint} breakpoint The current breakpoint, which can be 'phone', 'tablet', or 'desktop'.
 * @param {boolean} isInit Whether this is the first time the function is being called.
 */
export declare const updateDataVisibility: (item: MultiViewDataUpdateVisibility, breakpoint: Breakpoint, isInit?: boolean) => void;
