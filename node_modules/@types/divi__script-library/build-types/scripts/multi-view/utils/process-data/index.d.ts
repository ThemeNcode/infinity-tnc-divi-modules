import { type Breakpoint } from '@divi/types';
/**
 * The function processes raw data by grouping it by module ID and applying any registered filters
 * before calling a callback function with the grouped data and a breakpoint.
 *
 * @since ??
 *
 * @param {Breakpoint} breakpoint The current breakpoint, which can be 'phone', 'tablet', or 'desktop'.
 * @param {boolean} isInit Whether this is the first time the function is being called.
 */
export declare const processData: (breakpoint: Breakpoint, isInit?: boolean) => void;
