/**
 * This function adds event listeners for hover and touch events to switch between default and hover
 * states of an element.
 *
 * @since ??
 *
 * @param {HTMLElement} element The HTML element that the hover effect is being applied to.
 * @param {HTMLElement} hoverElement The HTML element that triggers the hover effect.
 * @param {CallableFunction} switchDataToDefaultState Function that is called when the
 * mouse or touch event leaves the `hoverElement` element. It takes an optional `event` parameter of
 * type `MouseEvent` or `TouchEvent`.
 * @param {CallableFunction} switchDataToHoverState Function that is called when the
 * `hoverElement` is hovered over or touched on a touch device. It is responsible for switching the
 * data or state of the element to its hover state. The function takes an optional `event` parameter,
 * which can be either.
 */
export declare const addEventListenerHover: (element: HTMLElement, hoverElement: HTMLElement, switchDataToDefaultState: (event?: MouseEvent | TouchEvent) => void, switchDataToHoverState: (event?: MouseEvent | TouchEvent) => void) => void;
