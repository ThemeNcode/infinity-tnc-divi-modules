import { type Breakpoint } from '@divi/types';
import { type MultiViewDataBreakpointState, type MultiViewDataParsed } from '../../types';
/**
 * This function parses data based on a given breakpoint and returns the default, hover, and sticky
 * state data for that breakpoint.
 *
 * @since ??
 *
 * @param {MultiViewDataBreakpointState} data The data parameter is an object
 * containing the data for different breakpoints (phone, tablet, desktop) and their corresponding
 * states (default, hover, sticky).
 * @param {Breakpoint} breakpoint The breakpoint parameter is a string that represents the current
 * device breakpoint, such as 'phone', 'tablet', or 'desktop'. It is used to determine which data to
 * extract from the MultiViewDataBreakpointState object.
 * @returns {MultiViewDataParsed} The function `parseData` returns an object with three properties: `defaultStateData`,
 * `hoverStateData`, and `stickyStateData`. These properties contain the parsed data for the specified
 * breakpoint. The data is of type `MultiViewDataParsed<TItem>`, which is a generic type that depends
 * on the type of data being parsed.
 */
export declare const parseData: <TItem>(data: MultiViewDataBreakpointState<TItem>, breakpoint: Breakpoint) => MultiViewDataParsed<TItem>;
