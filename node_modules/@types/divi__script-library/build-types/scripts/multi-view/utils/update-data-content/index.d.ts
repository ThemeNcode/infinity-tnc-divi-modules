import { type Breakpoint } from '@divi/types';
import { type MultiViewDataUpdateContent } from '../../types';
/**
 * Update element inner HTML.
 *
 * @since ??
 *
 * @param {MultiViewDataUpdateContent} item The item that will be updated.
 * @param {Breakpoint} breakpoint The current breakpoint, which can be 'phone', 'tablet', or 'desktop'.
 * @param {boolean} isInit Whether this is the first time the function is being called.
 */
export declare const updateDataContent: (item: MultiViewDataUpdateContent, breakpoint: Breakpoint, isInit?: boolean) => void;
