/// <reference types="node" />
import { EventEmitter } from 'events';
/**
 * Document store; track document height (at the moment) and its changes. Builder elements
 * should listen and get this store's value instead of directly getting it from document.
 * ETScriptDocumentStore is not exported; intentionally export its instance so there'll only be one
 * ETScriptDocumentStore instance.
 *
 * @since 4.6.0
 */
declare class ETScriptDocumentStore extends EventEmitter {
    /**
     * ETScriptDocumentStore constructor.
     *
     * @since 4.6.0
     */
    constructor();
    /**
     * Record document height.
     *
     * @since 4.6.0
     *
     * @param {number} height
     *
     * @returns {Window}
     */
    setHeight: (height: any) => this;
    /**
     * Record document width.
     *
     * @since 4.6.0
     *
     * @param {number} width
     *
     * @returns {Window}
     */
    setWidth: (width: any) => this;
    /**
     * Get recorded document height.
     *
     * @since 4.6.0
     *
     * @returns {number}
     */
    get height(): number;
    /**
     * Get recorded document width.
     *
     * @since 4.6.0
     *
     * @returns {number}
     */
    get width(): number;
    /**
     * Add document dimension change event listener.
     *
     * @since 4.6.0
     *
     * @param {Function} callback
     *
     * @returns {Window}
     */
    addDimensionChangeListener: (callback: any) => this;
    /**
     * Remove document dimension change event listener.
     *
     * @since 4.6.0
     *
     * @param {Function} callback
     *
     * @returns {Window}
     */
    removeDimensionChangeListener: (callback: any) => this;
    /**
     * Add document height change event listener.
     *
     * @since 4.6.0
     *
     * @param {Function} callback
     *
     * @returns {Window}
     */
    addHeightChangeListener: (callback: any) => this;
    /**
     * Remove document height change event listener.
     *
     * @since 4.6.0
     *
     * @param {Function} callback
     *
     * @returns {Window}
     */
    removeHeightChangeListener: (callback: any) => this;
    /**
     * Add document width change event listener.
     *
     * @since 4.6.0
     *
     * @param {Function} callback
     *
     * @returns {Window}
     */
    addWidthChangeListener: (callback: any) => this;
    /**
     * Remove document width change event listener.
     *
     * @since 4.6.0
     *
     * @param {Function} callback
     *
     * @returns {Window}
     */
    removeWidthChangeListener: (callback: any) => this;
}
declare const documentStoreInstance: ETScriptDocumentStore;
export default documentStoreInstance;
