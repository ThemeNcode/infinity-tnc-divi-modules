import { type HasAnimationData } from './types';
/**
 * Checks whether the element has an animation class.
 *
 * This function is used to determine whether or not an element has animation data.
 *
 * @since ??
 *
 * @returns {boolean} Whether or not the element has animation data.
 */
export declare const hasAnimationData: HasAnimationData;
