import { type AnimateElement } from './types';
/**
 * Animate Element.
 *
 * Take an element, read the data attributes on it, and then apply the desired animation to it.
 *
 * @since ??
 *
 * @param {JQuery} $elementOriginal Element to animate.
 *
 * @returns {void}
 */
export declare const animateElement: AnimateElement;
