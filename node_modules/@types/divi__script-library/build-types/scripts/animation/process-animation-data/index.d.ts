import { type ProcessAnimationData } from './types';
/**
 * Process Animation Data.
 *
 * This function is used to process the animation data that is stored in the global variable. It will loop through
 * the data and apply animation effects to all elements that have animation data.
 *
 * @since ??
 *
 * @param {boolean} waypointsEnabled Flag to determines whether or not to use the waypoints to trigger the animation.
 *
 * @returns {void}
 */
export declare const processAnimationData: ProcessAnimationData;
