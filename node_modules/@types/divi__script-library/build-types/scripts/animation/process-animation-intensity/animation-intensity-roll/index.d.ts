/**
 * Animation Intensity: roll.
 *
 * Take a given direction and intensity, and return a `transform` style declaration for an animated roll effect.
 *
 * @since ??
 *
 * @param {string} direction The direction of the animation.
 * @param {number} intensity The intensity of the animation.
 *
 * @returns {string} The `transform` style declaration.
 */
export declare const animationIntensityRoll: (direction: string, intensity: number) => string;
