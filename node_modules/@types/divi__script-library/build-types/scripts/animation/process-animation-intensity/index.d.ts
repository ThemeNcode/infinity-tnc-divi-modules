import { type ProcessAnimationIntensity } from './types';
/**
 * Process animation intensity.
 *
 * Take an animation, a direction, and an intensity, and return a `transform` style object that can be applied
 * to an element to animate it.
 *
 * @since ??
 *
 * @param {string} animation The animation type.
 * @param {string} direction The direction of the animation.
 * @param {number} intensity The intensity of the animation.
 *
 * @returns {object} The transform style object.
 */
export declare const processAnimationIntensity: ProcessAnimationIntensity;
