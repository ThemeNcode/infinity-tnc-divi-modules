/**
 * Animation Intensity: fold.
 *
 * Take a given direction and intensity, and return a `transform` style declaration for an animated fold effect.
 *
 * @since ??
 *
 * @param {string} direction The direction of the animation.
 * @param {number} intensity The intensity of the animation.
 *
 * @returns {string} The `transform` style declaration.
 */
export declare const animationIntensityFold: (direction: string, intensity: number) => string;
