/**
 * Animation Intensity: zoom.
 *
 * Take a given intensity value and return a `transform` style declaration for an animated zoom effect.
 *
 * @since ??
 *
 * @param {number} intensity The intensity of the animation.
 *
 * @returns {string} The `transform` style declaration.
 */
export declare const animationIntensityZoom: (intensity: number) => string;
