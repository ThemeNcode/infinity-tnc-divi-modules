/**
 * Check whether the version passed is the correct version used at this moment based
 * on the content area selector exists on DOM.
 *
 * @since 4.5.2
 *
 * @param {string} version
 *
 * @returns {boolean}
 */
export declare const isVersion: (version: any) => boolean;
/**
 * Check whether template editor is active or not.
 *
 * @since 4.9.8
 *
 * @return {boolean}
 */
export declare const isTemplateEditor: () => any;
