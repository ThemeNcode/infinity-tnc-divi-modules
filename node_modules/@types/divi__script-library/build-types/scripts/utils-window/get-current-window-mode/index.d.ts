import { type Breakpoint } from '@divi/types';
/**
 * Get the current window mode.
 *
 * Get the current window mode by checking the window width.
 *
 * @since ??
 *
 * @returns {string} The current window mode ('desktop', 'tablet', 'phone').
 */
declare const getCurrentWindowMode: () => Breakpoint;
export { getCurrentWindowMode };
