export declare class ScrollItem {
    /**
     * @returns {string}
     */
    get id(): any;
    /**
     * @returns {ScrollEffect[]}
     */
    get effects(): any;
    /**
     * @returns {number}
     */
    get offset(): any;
    /**
     * @returns {number}
     */
    get height(): any;
    /**
     * @returns {number}
     */
    get triggers(): any;
    /**
     * @param {string} id
     * @param {ScrollEffect[]} effects
     * @param {number} offset
     * @param {number} height
     * @param motionTriggers
     */
    constructor(id: any, effects: any, offset: any, height: any, motionTriggers: any);
}
/**
 * Adds new effect for the item
 * If effect already exists, it gets replaced.
 *
 * @param {ScrollEffect} effect
 * @param {ScrollItem} item
 */
export declare const addEffect: (effect: any, item: any) => ScrollItem;
/**
 * Adds an array of affects
 * If effects already exist, it gets replaced.
 *
 * @param {ScrollEffect[]} effects
 * @param {ScrollItem} item
 */
export declare const addEffects: (effects: any, item: any) => any;
/**
 * Sets items effects.
 *
 * @param {ScrollEffect[]} effects
 * @param {ScrollItem} item
 */
export declare const setEffects: (effects: any, item: any) => ScrollItem;
/**
 * Updates item offset.
 *
 * @param {number} offset
 * @param {ScrollItem} item
 */
export declare const addOffset: (offset: any, item: any) => ScrollItem;
/**
 * Updates item height.
 *
 * @param {number} height
 * @param {ScrollItem} item
 * @returns {ScrollItem}
 */
export declare const addHeight: (height: any, item: any) => ScrollItem;
/**
 * Returns the lowest starting point on viewport.
 *
 * @param {ScrollItem} item
 * @returns {number}
 */
export declare const getViewportStart: (item: any) => any;
/**
 * Returns the highest end point on viewport.
 *
 * @param {ScrollItem} item
 * @returns {number}
 */
export declare const getViewportEnd: (item: any) => any;
/**
 * Check if 2 scroll items by effects, offset and height equality
 * Two items are similar when:
 *  - have same offset
 *  - have same height
 *  - have same number of effects
 *  - effects with same resolver are equal.
 *
 * @param {ScrollItem} item1
 * @param {ScrollItem} item2
 * @returns {boolean}
 */
export declare const isSimilar: (item1: any, item2: any) => boolean;
/**
 * Check 2 items by equality, for this, the ids should the same and they should be similar (check isSimilar function).
 *
 * @param item1
 * @param item2
 * @returns {boolean}
 */
export declare const isEqual: (item1: any, item2: any) => boolean;
