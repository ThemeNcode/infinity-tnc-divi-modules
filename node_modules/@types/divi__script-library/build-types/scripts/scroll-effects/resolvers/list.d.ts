/**
 * Resolves CSS filter `blur` value.
 *
 * @param {number} value
 * @param {{startValue, midValue, endValue}} definition
 * @returns {{filter: string}}
 */
export declare const blur: (value: any, definition: any) => {
    filter: string;
};
export declare const opacity: (value: any, definition: any) => {
    opacity: string;
};
/**
 * Resolves CSS transform `rotate` value.
 *
 * @param {number} value
 * @param {{startValue, midValue, endValue}} definition
 * @returns {{transform: string}}
 */
export declare const rotate: (value: any, definition: any) => {
    transform: string;
};
/**
 * Resolves CSS transform `scale` value.
 *
 * @param {number} value
 * @param {{startValue, midValue, endValue}} definition
 * @returns {{transform: string}}
 */
export declare const scale: (value: any, definition: any) => {
    transform: string;
};
/**
 * Resolves CSS transform `translateX` value.
 *
 * @param {number} value
 * @param {{startValue, midValue, endValue}} definition
 * @returns {{transform: string}}
 */
export declare const translateX: (value: any, definition: any) => {
    transform: string;
};
/**
 * Resolves CSS transform `translateY` value.
 *
 * @param {number} value
 * @param {{startValue, midValue, endValue}} definition
 * @returns {{transform: string}}
 */
export declare const translateY: (value: any, definition: any) => {
    transform: string;
};
