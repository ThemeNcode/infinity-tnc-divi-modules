/**
 * @param {number} stepSize
 * @param {number} offset
 * @returns {number}
 */
export declare const getStep: (stepSize: any, offset: any) => number;
/**
 * @param {number} stepSize
 * @param {number} height
 * @returns {number}
 */
export declare const getSteps: (stepSize: any, height: any) => number;
/**
 * Returns the offset value of the specified step.
 *
 * @param {number} stepSize
 * @param {number} step
 * @returns {number}
 */
export declare const getOffsetFromStep: (stepSize: any, step: any) => number;
export declare const getBFBOffset: () => any;
export declare const isZoomMode: () => any;
/**
 * Receives the element offset in percents by viewport and calculates the effect value.
 *
 * @param {ScrollEffect} effect
 * @param {number} offset
 * @param itemOffsetStart
 * @param itemOffsetEnd
 * @returns {number}
 */
export declare const getEffectValue: (effect: any, itemOffsetStart: any, itemOffsetEnd: any) => number;
/**
 * Calculate element position on viewport in %.
 * The percent specify the element position from bottom to top.
 *
 * @param {number} offset
 * @param {number} viewportOffset
 * @param {number} viewPortHeight
 * @returns {number}
 */
export declare const getPositionOnViewport: (offset: any, viewportOffset: any, viewPortHeight: any) => number;
/**
 * Check if the element is visible on viewport.
 *
 * Element is visible if:
 *  - element bottom edge offset is greater then viewport top edge offset and lower then viewport bottom edge offset
 *  - element top edge offset is greater then viewport top edge offset and lower then viewport bottom edge offset.
 *
 * @param {number} elementOffset
 * @param {number} elementHeight
 * @param {number} viewportOffset
 * @param {number} viewportHeight
 * @returns {boolean}
 */
export declare const isVisible: (elementOffset: any, elementHeight: any, viewportOffset: any, viewportHeight: any) => boolean;
/**
 * Calculates top edge offset in case the original offset represents the center offset.
 *
 * @param {number} offset
 * @param {number} height
 * @returns {number}
 */
export declare const topOffset: (offset: any, height: any) => number;
/**
 * Group items by screen position.
 *
 * @param {ScrollItem[]} items
 * @param {number} vHeight
 * @returns {[[ScrollItem]]}
 */
export declare const byScreenPosition: (items: any, vHeight: any) => any[][];
