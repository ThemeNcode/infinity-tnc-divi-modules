/// <reference types="node" />
import { EventEmitter } from 'events';
/**
 * Window store.
 *
 * This store listen to direct window's events; builder callback listen to this store's events
 * to avoid dom-based calculation whenever possible; use the property passed by this store.
 *
 * @since 4.6.0
 */
declare class ETScriptWindowStore extends EventEmitter {
    /**
     * ETScriptWindowStore constructor.
     *
     * @since 4.6.0
     */
    constructor();
    /**
     * Set window height.
     *
     * @since 4.6.0
     *
     * @param {string} windowLocation App|top.
     * @param {number} height
     *
     * @returns {Window}
     */
    setHeight: (windowLocation: string, height: any) => this;
    /**
     * Set window width.
     *
     * @since 4.6.0
     *
     * @param {string} windowLocation App|top.
     * @param {number} width
     *
     * @returns {Window}
     */
    setWidth: (windowLocation: string, width: any) => this;
    /**
     * Set scroll location value.
     *
     * @since 4.6.0
     *
     * @param {string} scrollLocation App|top.
     *
     * @returns {ETScriptWindowStore}
     */
    setScrollLocation: (scrollLocation: any) => false | this;
    /**
     * Set scroll top value.
     *
     * @since 4.6.0
     *
     * @param {string} windowLocation App|top.
     * @param {number} scrollTop
     *
     * @returns {ETScriptWindowStore}
     */
    setScrollTop: (windowLocation: any, scrollTop: any) => this;
    /**
     * Set builder zoomed status (on builder only).
     *
     * @since 4.6.0
     *
     * @param {string} builderPreviewMode Desktop|tablet|phone|zoom|wireframe.
     */
    setBuilderZoomedStatus: (builderPreviewMode: any) => void;
    /**
     * Set BFB iframe offset.
     *
     * @since 4.6.0
     */
    setBfbIframeOffset: () => void;
    /**
     * Set Layout Block iframe offset.
     *
     * @since 4.6.0
     */
    setLayoutBlockPreviewIframeOffset: () => void;
    /**
     * Set vertical scrollbar width.
     *
     * @since 4.6.0
     *
     * @param {string} windowLocation
     * @param {number} width
     */
    setVerticalScrollBarWidth: (windowLocation: string, width: any) => this;
    /**
     * Get current window width.
     *
     * @since 4.6.0
     *
     * @returns {number}
     */
    get width(): any;
    /**
     * Get current window height.
     *
     * @since 4.6.0
     *
     * @returns {number}
     */
    get height(): any;
    /**
     * Get current window scroll location.
     *
     * @since 4.6.0
     *
     * @returns {string} App|top.
     */
    get scrollLocation(): any;
    /**
     * Get current window scroll top / distance to document.
     *
     * @since 4.6.0
     *
     * @returns {number}
     */
    get scrollTop(): number;
    /**
     * Get current app window breakpoint (by device).
     *
     * @since 4.6.0
     *
     * @returns {string}
     */
    get breakpoint(): string;
    /**
     * Get builder zoomed status.
     *
     * @since 4.6.0
     *
     * @returns {bool}
     */
    get isBuilderZoomed(): boolean;
    /**
     * Get current window vertical scrollbar width.
     *
     * @since 4.6.0
     *
     * @returns {number}
     */
    get verticalScrollBar(): any;
    /**
     * Get builder scroll location of builder context + preview mode.
     *
     * @since 4.6.0
     *
     * @param {string} previewMode Desktop|tablet|phone|zoom|wireframe.
     *
     * @returns {string} App|top.
     */
    getBuilderScrollLocation: (previewMode: any) => any;
    /**
     * Add width change event listener.
     *
     * @since 4.6.0
     *
     * @param {Function} callback
     *
     * @returns {Window}
     */
    addWidthChangeListener: (callback: any) => this;
    /**
     * Remove width change event listener.
     *
     * @since 4.6.0
     *
     * @param {Function} callback
     *
     * @returns {Window}
     */
    removeWidthChangeListener: (callback: any) => this;
    /**
     * Add height change event listener.
     *
     * @since 4.6.0
     *
     * @param {Function} callback
     *
     * @returns {Window}
     */
    addHeightChangeListener: (callback: any) => this;
    /**
     * Remove height change event listener.
     *
     * @since 4.6.0
     *
     * @param {Function} callback
     *
     * @returns {Window}
     */
    removeHeightChangeListener: (callback: any) => this;
    /**
     * Add scroll location change event listener.
     *
     * @param callback
     * @since 4.6.0
     * @returns {ETScriptWindowStore}
     */
    addScrollLocationChangeListener: (callback: any) => this;
    /**
     * Remove scroll location change event listener.
     *
     * @param callback
     * @since 4.6.0
     * @returns {ETScriptWindowStore}
     */
    removeScrollLocationChangeListener: (callback: any) => this;
    /**
     * Add scroll top change event listener.
     *
     * @param callback
     * @since 4.6.0
     * @returns {ETScriptWindowStore}
     */
    addScrollTopChangeListener: (callback: any) => this;
    /**
     * Remove scroll top change event listener.
     *
     * @param callback
     * @since 4.6.0
     * @returns {ETScriptWindowStore}
     */
    removeScrollTopChangeListener: (callback: any) => this;
    /**
     * Set breakpoint (by device) based on window width.
     *
     * @since 4.6.0
     *
     * @todo Update breakpoint setting mechanic so this won't need to define another screen size definition
     *       and able to reuse (et_screen_size()).
     *
     * @param {number} windowWidth
     *
     * @returns {ETScriptWindowStore}
     */
    setBreakpoint: (windowWidth: any) => this;
    /**
     * Add breakpoint change event listener.
     *
     * @since 4.6.0
     *
     * @param {Function} callback
     */
    addBreakpointChangeListener: (callback: any) => this;
    /**
     * Remove breakpoint change event listener.
     *
     * @since 4.6.0
     *
     * @param {Function} callback
     */
    removeBreakpointChangeListener: (callback: any) => this;
}
declare const windowStoreInstance: ETScriptWindowStore;
export default windowStoreInstance;
