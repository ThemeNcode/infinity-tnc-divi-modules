import '../polyfills-closest';
/**
 * Check whether given value can be printed or not (string). Originally a simpler way to check
 * against empty string, but later several checks were added as well to avoid unnecessary repetition.
 *
 * A value considered empty if:
 *  - is an empty string
 *  - is undefined
 *  - is false
 *  - is NaN
 *
 * @since 4.3
 *
 * @param {*} value
 * @returns {boolean}
 */
export declare const hasValue: (value: any) => boolean;
/**
 * Return given value or its default if current value returns false against hasValue()
 * Mainly used to return default if given value is empty string.
 *
 * @since 4.3
 *
 * @param {*} value
 * @param {*} defaultValue Value that will be return if value fails hasValue().
 *
 * @returns {*} Value|default.
 */
export declare const get: (value: any, defaultValue: any) => any;
/**
 * Test if provided string is a valid JSON.
 *
 * @since 4.3
 *
 * @param {string} string
 * @returns {boolean}
 */
export declare const isJson: (string: any) => boolean;
/**
 * Test if provided string is a valid HTML string.
 *
 * @since 4.3
 *
 * @param {string} html
 * @returns {boolean}
 */
export declare const isValidHtml: (html: any) => boolean;
/**
 * Check if parameter value equals to `on`.
 *
 * @since 4.3
 *
 * @param {string} value
 * @returns {boolean}
 */
export declare const isOn: (value: any) => boolean;
/**
 * Check if parameter value equals to `off`.
 *
 * @since 4.3
 *
 * @param {string} value
 * @returns {boolean}
 */
export declare const isOff: (value: any) => boolean;
/**
 * Check if parameter value equals to `on` or `off`.
 *
 * @since 4.3
 *
 * @param {string} value
 * @returns {boolean}
 */
export declare const isOnOff: (value: any) => boolean;
/**
 * Converts the given value to "on/off" notation.
 *
 * @param {boolean} value
 *
 * @returns {string}
 */
export declare const toOnOff: (value: any) => "on" | "off";
/**
 * Check if parameter value equals to `yes`.
 *
 * @since 4.3
 *
 * @param {string} value
 * @returns {boolean}
 */
export declare const isYes: (value: any) => boolean;
/**
 * Check if parameter value equals to `no`.
 *
 * @since 4.3
 *
 * @param {string} value
 * @returns {boolean}
 */
export declare const isNo: (value: any) => boolean;
/**
 * Check if parameter value equals to `default`.
 *
 * @since 4.3
 *
 * @param {string} value
 * @returns {boolean}
 */
export declare const isDefault: (value: any) => boolean;
/**
 * Check if provided url or path point to a file with specific extension.
 *
 * @since 4.3
 *
 * @param {string} url
 * @param {string} extension
 * @returns {boolean}
 */
export declare const isFileExtension: (url: any, extension: any) => boolean;
/**
 * Generates CSS input placeholders styles for provided selectors.
 *
 * @since 4.3
 *
 * @param {Array} selectors
 * @param {string} declaration
 * @returns {Array}
 */
export declare const generatePlaceholderCss: (selectors: any, declaration: any) => any[];
/**
 * Replace string entities to their character representation.
 *
 * @since 4.3
 *
 * @param {string} content
 * @returns {string}
 */
export declare const replaceCodeContentEntities: (content: any) => any;
/**
 * Check if the string is a numeric value
 * Numeric values are strings that contain a numeric value or starts with a numeric value.
 *
 * @since 4.3
 *
 * @param {string} value
 * @returns {boolean}
 */
export declare const hasNumericValue: (value: any) => boolean;
/**
 * Replace `”`&`″` characters HTML entities that is similar to the actual builder output's counterpart.
 *
 * @since 4.3
 *
 * @param {string} string
 * @returns {string}
 */
export declare const removeFancyQuotes: (string: any) => any;
/**
 * Returns allowed corners names list.
 *
 * @since 4.3
 *
 * @returns {string[]}
 */
export declare const getCorners: () => string[];
/**
 * Return specific corner name by index.
 *
 * @since 4.3
 *
 * @param {number} index
 * @returns {string}
 */
export declare const getCorner: (index: any) => string;
/**
 * Returns spacing value by provided corner.
 *
 * @since 4.3
 *
 * @param {string} spacing
 * @param {string} corner
 * @param {*} defaultValue
 * @returns {string|*}
 */
export declare const getSpacing: (spacing: any, corner: any, defaultValue?: string) => string;
/**
 * Converts a value to string by checking if the value is a valid value using `hasValue`
 * If value doesn't pass the check, return empty string.
 *
 * @since 4.3
 *
 * @param value
 * @returns {string}
 */
export declare const toString: (value: any) => string;
/**
 * Return the object property value, in case the value does not satisfy empty validation,
 * return the defaultValue.
 *
 * @since 4.3
 *
 * @param {*} defaultValue
 * @param {string} prop
 * @param {object} object
 * @returns {*}
 */
export declare const prop: (defaultValue: any, prop: any, object: any) => any;
/**
 * Sets object property with provided value.
 *
 * @since 4.3
 *
 * @param {string} prop
 * @param {*} value
 * @param {object} object
 * @returns {object}
 */
export declare const set: (prop: any, value: any, object: any) => any;
export declare function isRealMobileDevice(): boolean;
/**
 * Get percentage of given value.
 *
 * @since 4.6.0
 *
 * @param {number} value
 * @param {number|string} percentage
 *
 * @returns {number}
 */
export declare const getPercentage: (value: any, percentage: any) => number;
/**
 * Get the closest element traverses the Element and its parents (heading toward the document root)
 * until it finds a node that matches the provided selector string.
 * Will return itself or the matching ancestor.
 * If no such element exists, it returns null.
 *
 * @param {element} element  Element to search.
 * @param {string}  selector The element selector to find.
 */
export declare const closestElement: (element: any, selector: any) => any;
