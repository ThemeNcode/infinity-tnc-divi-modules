/// <reference types="node" />
import { EventEmitter } from 'events';
import { type WindowEventEmitterEventNames, type WindowEventEmitterPropNames } from './types';
/**
 * Class for emitting window events.
 *
 * Instead of directly adding event listener to real window events, the frontend script shoud add listener to events
 * emitted here. This becomes necessary because even though most Visual Builder mode has already consistent scroll
 * position in app window, some rendering such as layout block preview still has the scroll located on top window.
 * This emitter class also save attached listener callback into instance property which make listener callback removal
 * simpler.
 *
 * @since ??
 *
 * @augments EventEmitter Class that emits events.
 */
export declare class WindowEventEmitter extends EventEmitter {
    private height;
    private top;
    private width;
    registeredCallbacks: Record<string, (props: unknown) => unknown>;
    /**
     * Class constructor.
     *
     * @todo Right now this is a minified version of D4's `ETScriptWindowStore`. This refactoring
     * is inevitable because there are things in D4 that is no longer relevant in D5 such as
     * different scroll location in various mode, deprecating BFB, etc.
     *
     * @since ??
     */
    constructor();
    /**
     * Get property name's value.
     *
     * @since ??
     *
     * @param {WindowEventEmitterPropNames} propName Properity name.
     *
     * @returns {number}
     */
    getProp(propName: WindowEventEmitterPropNames): number;
    /**
     * Set property name.
     *
     * @param {WindowEventEmitterPropNames} propName Class' property name.
     * @param {number} value Property name value.
     */
    setProp(propName: WindowEventEmitterPropNames, value: number): void;
    /**
     * Add window event emitter listener.
     *
     * @since ??
     *
     * @param {WindowEventEmitterEventNames} eventName Property name that its emitted event you want to listen to.
     * @param {Function} callback Function callback.
     * @param {string} id Id for currently attached callback.
     */
    addWindowListener(eventName: WindowEventEmitterEventNames, callback: (...args: any[]) => void, id?: string): void;
    /**
     * Remove window event emitter listener.
     *
     * @since ??
     *
     * @param {WindowEventEmitterEventNames} eventName Property name that its emitted event you want to listen to.
     * @param {Function} callback Function callback.
     * @param {string} id Id for currently attached callback.
     */
    removeWindowListener(eventName: WindowEventEmitterEventNames, callback: (...args: any[]) => void, id?: string): void;
    /**
     * Register prop value change to window events.
     *
     * @since ??
     */
    attachToWindowEvents(): void;
}
/**
 * Export instance of Window Event Emitter.
 *
 * Realistically this is what every component should import instead of importing WindowsEventEmitter on its own; this
 * ensures there's only one instance of WindowEventEmitter in one page.
 *
 * @since ??
 *
 * @constant
 * @returns {WindowEventEmitter} Instance of WindowEventEmitter.
 */
export declare const WindowEventEmitterInstance: WindowEventEmitter;
