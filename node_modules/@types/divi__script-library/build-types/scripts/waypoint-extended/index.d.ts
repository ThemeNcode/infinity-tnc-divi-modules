import { type WaypointExtended } from './types';
/**
 * Extension of the jQuery Waypoint plugin.
 *
 * Waypoints are used to trigger behavior when an element is scrolled to. This function extends the default
 * waypoint functionality to allow for multiple waypoints to be created for a single element.
 *
 * @since ??
 *
 * @param {JQuery}  $element The element that you want to apply the waypoint to.
 * @param {object} options The options object that you would normally pass to the waypoint function.
 * @param {string|number} [options.offset] The offset option to pass to the waypoint function.
 * @param {boolean} [options.continuous] Whether or not the waypoint should be continuous.
 * @param {boolean} [options.enabled] Whether or not the waypoint should be enabled.
 * @param {string} [options.group] The group option to pass to the waypoint function.
 * @param {boolean} [options.horizontal] Whether or not the waypoint should be considered horizontal.
 * @param {Function} [options.handler] Custom handler function.
 * @param {HTMLElement|string} [options.context] Context to pass to the waypoint function.
 * @param {number} maxInstances The maximum number of waypoints that can be created for the element.
 *
 * @returns {void}
 */
declare const waypointExtended: WaypointExtended;
export { waypointExtended };
