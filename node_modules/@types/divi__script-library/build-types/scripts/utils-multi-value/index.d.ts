export declare const DELIMITER = "|";
/**
 * Parses an array and transforms it in an valid multi value array.
 *
 * @param {Array} array
 * @param {number} elements
 * @returns {Array}
 */
export declare const parse: (array: any, elements: any) => any[];
/**
 * Splits the multi value string in to an array of primitive values
 * User can provide also the required number of elements that value must have
 * In case the array original length will be larger then the required elements number
 * the array will be cut from head to tail
 * In cas the array length will be shorter, the array tail will be filled with empty strings `''`,
 * till array length will match the requested elements number.
 *
 * @param {string} value The multi value string.
 * @param {number} elements The required length.
 * @returns {[string]}
 */
export declare const split: (value: any, elements: any) => any[];
/**
 * Takes an array and converts it to a valid multi value
 * Provide the `elements` parameter to get the result string of the necessary length.
 *
 * @param {Array} value
 * @param {number} elements
 * @returns {string}
 */
export declare const toValue: (value: any, elements: any) => string;
/**
 * Check if the multi value is not empty.
 * A multi value is empty when all sub values are empty strings.
 *
 * @param {string} value
 * @param {number} length
 * @returns {boolean}
 */
export declare const hasValue: (value: any, length: any) => boolean;
/**
 * Merges two multi values in to one.
 * If value1 nth element is empty, value2 nth element will be used.
 *
 * @param {string} value1
 * @param {string} value2
 * @param {number} length
 * @returns {string}
 */
export declare const merge: (value1: any, value2: any, length: any) => string;
/**
 * Returns value on specific position in multiValue.
 *
 * @param {number} key
 * @param {string} value
 * @param {number} length
 * @returns {string}
 */
export declare const get: (key: any, value: any, length: any) => any;
/**
 * Sets a value at specific position in provided multiValue.
 *
 * @param {string} value
 * @param {number} key
 * @param {string} multiValue
 * @param {number} length
 * @returns {string}
 */
export declare const set: (value: any, key: any, multiValue: any, length: any) => any;
