/// <reference types="node" />
import { EventEmitter } from 'events';
/**
 * Sticky Elements store.
 *
 * This store stores selected properties of all sticky elements on the page so a sticky element
 * can use other sticky element's calculated value quickly.
 *
 * @since 4.6.0
 */
declare class ETScriptStickyStore extends EventEmitter {
    /**
     * ETScriptStickyStore constructor.
     *
     * @since 4.6.0
     */
    constructor();
    /**
     * Get registered modules.
     *
     * @since 4.6.0
     *
     * @type {object}
     */
    get modules(): {};
    /**
     * List of builder options (that is used by sticky elements) that has responsive mode.
     *
     * @since 4.6.0
     *
     * @returns {Array}
     */
    get responsiveOptions(): string[];
    /**
     * Update selected module / elements prop on document height change.
     *
     * @since 4.6.0
     */
    onDocumentHeightChange: () => void;
    /**
     * Builder did mount listener callback.
     *
     * @since 4.6.0
     */
    onBuilderDidMount: () => void;
    /**
     * Builder settings change listener callback.
     *
     * @since 4.6.0
     *
     * @param {object} event
     * @param {bool}   forceUpdate
     */
    onBuilderSettingsChange: (event: any, forceUpdate?: boolean) => void;
    /**
     * Get id of all modules.
     *
     * @since 4.6.0
     *
     * @type {object} modules
     *
     * @returns {Array}
     */
    getModulesId: (modules: any) => any[];
    /**
     * Get modules based on its rendering position; also consider its offset surrounding setting if needed.
     *
     * @since 4.6.0
     * @param {string} top|bottom
     * @param position
     * @param offsetSurrounding
     * @param {string|bool} on|off|false When false, ignore offset surrounding value.
     * @returns {bool}
     */
    getModulesByPosition: (position: any, offsetSurrounding?: boolean) => never[];
    /**
     * Sort modules from top to down based on offset prop. Passed module has no id or index prop so
     * offset which visually indicate module's position in the page will do.
     *
     * @since 4.6.0
     */
    sortModules: () => {};
    /**
     * Set prop value.
     *
     * @since 4.6.0
     *
     * @param {string} id Need to be unique.
     * @param {string} name
     * @param {string} value
     */
    setProp: (id: any, name: any, value: any) => void;
    /**
     * Get prop.
     *
     * @since 4.6.0
     * @param {string} id
     * @param {string} name
     * @param {mixed} defaultValue
     * @param returnCurrentBreakpoint
     * @param {bool} return
     * @returns {mixed}
     */
    getProp: (id: any, name: any, defaultValue: any, returnCurrentBreakpoint?: boolean) => any;
    /**
     * Set known elements' props.
     *
     * @since 4.6.0
     */
    setElementsProps: () => void;
    /**
     * Set known element prop value.
     *
     * @since 4.6.0
     *
     * @param {string} id Need to be unique.
     * @param {string} name
     * @param {string} value
     */
    setElementProp: (id: any, name: any, value: any) => void;
    /**
     * Get known element prop.
     *
     * @since 4.6.0
     *
     * @param {string} id
     * @param {string} name
     * @param {mixed} defaultValue
     *
     * @returns {mixed}
     */
    getElementProp: (id: any, name: any, defaultValue: any) => any;
    /**
     * Set element height.
     *
     * @since 4.6.0
     *
     * @param {string} name
     */
    setElementHeight: (name: any) => void;
    /**
     * Generate offset modules for offset surrounding option.
     *
     * @since 4.6.0
     */
    generateOffsetModules: () => void;
    /**
     * Check if module with given id is on sticky state.
     *
     * @since 4.6.0
     *
     * @param {string} id
     *
     * @returns {bool}
     */
    isSticky: (id: any) => any;
    /**
     * Add listener callback for settings change event.
     *
     * @since 4.6.0
     * @param callback
     * @param {Function}
     */
    addSettingsChangeListener: (callback: any) => this;
    /**
     * Remove listener callback for settings change event.
     *
     * @since 4.6.0
     * @param callback
     * @param {Function}
     */
    removeSettingsChangeListener: (callback: any) => this;
}
declare const stickyStoreInstance: ETScriptStickyStore;
export default stickyStoreInstance;
