/**
 * Get Option On Click Path.
 *
 * Get path to option's onClick property of selected option in right click options' options state.
 *
 * @since ??
 *
 * @param {Array<number>} parent Parent location.
 * @param {number} selected Order of selected option using zero indexed order.
 *
 * @returns {Array<string>}
 */
export declare const getOptionOnClickPath: (parent: number[], selected: number) => string[];
