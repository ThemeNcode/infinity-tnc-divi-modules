/**
 * Has Copy Module Option.
 *
 * Check if `getModuleOptions` should render copy module options.
 * Copy module is only shown when selected module has the same module type (eg. Both CTA and Text
 * Module has `module` module type).
 *
 * @since ??
 *
 * @param {Array<string>} selectedModuleIds Selected module ids.
 *
 * @returns {boolean}
 */
export declare const hasCopyModuleOption: (selectedModuleIds: string[]) => boolean;
