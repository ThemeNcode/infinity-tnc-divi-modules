import { type RightClickOptions } from '@divi/types';
/**
 * Get Module Options.
 *
 * Get options of module's right click options.
 *
 * @since ??
 *
 * @param {object} owner Function parameters.
 * @param {string} owner.id Module's ID.
 * @param {string} owner.moduleType Module's type. One of:
 * `module`, `root`, `section`, `fullwidth-section`, `specialty-section`, `row`, `row-inner`,
 * `column`, `specialty-column`, `column-inner`, `fullwidth-module`, `child-module`, `unsupported` or `layout`.
 * @param {object} owner.clipboardItem Clipboard item data.
 * @param {object} owner.clipboardItemStyle Clipboard item style data.
 * @param {Array<string>} owner.selectedModuleIds Selected module IDs.
 * @param {ReactNode} owner.fieldsNode Module's fields node.
 * @param {object} owner.attrsMaps Module's attributes maps.
 *
 * @returns {Array<object>}
 */
export declare const getModuleOptions: (owner: RightClickOptions.Owner.Module) => RightClickOptions.Option.Items;
