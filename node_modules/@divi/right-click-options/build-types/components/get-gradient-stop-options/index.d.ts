import { type RightClickOptions } from '@divi/types';
/**
 * Get Gradient Stop Options.
 *
 * Get options of modal field's right click options.
 *
 * @since ??
 *
 * @param {object} owner Function params.
 * @param {string} owner.optionType `gradientStop`.
 * @param {ReactNode} owner.fieldsNode Fields node of modal.
 * @param {Function} owner.removeStop Callback function to remove gradient stop.
 *
 * @returns {object}
 */
export declare const getGradientStopOptions: (owner: RightClickOptions.Owner.GradientStop) => RightClickOptions.Option.Items;
