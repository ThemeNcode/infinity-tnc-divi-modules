/**
 * On Option Mouse Enter.
 *
 * Event callback to be called when right click options' option is mouse-entered.
 *
 * @since ??
 *
 * @param {Array<number>} parent Parent structure.
 * @param {number} selected Selected option (zero-indexed).
 *
 * @returns {void}
 */
export declare const onOptionMouseEnter: (parent: number[], selected: number) => void;
