import { type Module } from '../../module';
import { type ModuleAttrs } from '../../module/library';


export namespace Styles {
  export namespace Item {
    export namespace Component {

      // TODO Needs more precise type than `ModuleAttrs`.
      export type Props = Module.StylesProps<ModuleAttrs>;
    }
  }

  export namespace Component {
    export type Props = {
      styles: Record<string, Item.Component.Props>;
    }
  }
}
