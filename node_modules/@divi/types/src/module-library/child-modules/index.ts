import { type ModuleAttrs } from '../../module';


export namespace ChildModules {
  export type Params = {
    moduleName: string,
    moduleAttrs?: ModuleAttrs,
    moduleFolder?: string,
    moduleFilter?: string,
  }
}
