import { type Folder as FolderNamespace } from '../folder';
import { type Module as ModuleNamespace } from '../module';


export namespace ModuleFolder {
  export type Collections =
    Record<string, ModuleNamespace.Settings | FolderNamespace.Definition>;
}
