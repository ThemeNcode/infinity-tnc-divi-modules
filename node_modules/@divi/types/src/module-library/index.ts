import { ChildModules as ChildModulesNamespace } from './child-modules';
import { Folder as FolderNamespace } from './folder';
import { Module as ModuleNamespace } from './module';
import { ModuleFolder as ModuleFolderNamespace } from './module-folder';
import { Store as StoreNamespace } from './store';
import { Styles as StylesNamespace } from './styles';


export namespace ModuleLibrary {
  /* eslint-disable @typescript-eslint/no-unused-vars */
  // TODO figure out a way to declare nested namespace without disabling no-unused-vars rule
  // imported namespace as marked as unused by eslint using this config; it shouldn't happen in the first place.
  export import ChildModules = ChildModulesNamespace;
  export import Folder = FolderNamespace;
  export import Module = ModuleNamespace;
  export import ModuleFolder = ModuleFolderNamespace;
  export import Store = StoreNamespace;
  export import Styles = StylesNamespace;
  /* eslint-enable @typescript-eslint/no-unused-vars */
}
