export namespace Term {
  export type Type = 'layout_category' | 'layout_tag';

  export interface Item {
    name: string;
    id: number;
    slug: string;
    count: number;
  }

  export type Fetched = {
    [key in Type]?: Item[];
  }
}
