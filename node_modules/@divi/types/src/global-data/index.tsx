import { Data as DataNamespace } from './data';
import { Presets as PresetsNamespace } from './presets';
import { Store as StoreNamespace } from './store';


/**
 * Global Data types.
 *
 * This namespace contains all the types used in the Global Data store.
 *
 * @since ??
 */
export namespace GlobalData {

  /* eslint-disable @typescript-eslint/no-unused-vars */
  // TODO figure out a way to declare nested namespace without disabling no-unused-vars rule
  // imported namespace as marked as unused by eslint using this config; it shouldn't happen in the first place.
  export import Data = DataNamespace;
  export import Store = StoreNamespace;
  export import Presets = PresetsNamespace;
  /* eslint-enable @typescript-eslint/no-unused-vars */

}
