import {
  type FormEvent,
} from 'react';
import { type ImmutableObject } from 'seamless-immutable';

import {
  type SpamProtection,
} from '../../spam-protection';


export namespace SpamProtectionProvider {

  export interface OnChangeCallbackParams {
    event?: FormEvent;
    inputValue?: string;
  }

  export type OnChangeCallback = (params: OnChangeCallbackParams) => void;

  export interface Props {
    defaultValue?: string;
    name: string;
    onChange: OnChangeCallback;
    providers: ImmutableObject<Record<string, SpamProtection.Service.Provider>>;
    value: string;
  }
}
