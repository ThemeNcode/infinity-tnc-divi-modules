import { type ImmutableObject } from 'seamless-immutable';

import { type ModuleType } from '../../module/category';
import { type ModuleFlatObjects } from '../../module/state';


export namespace Clipboard {
  export namespace Item {
    export namespace Payload {

      /**
       * Payload for module clipboard item.
       *
       * @since ??
       */
      export interface Module {
        moduleId: string;
        moduleType: ModuleType;
        moduleObjects: ImmutableObject<ModuleFlatObjects>;
      }

      /**
       * Payload for modules clipboard item.
       *
       * @since ??
       */
      export interface Modules {
        moduleIds: string[];
        moduleType: ModuleType;
        moduleObjects: ImmutableObject<ModuleFlatObjects>;
      }
    }
  }
}
