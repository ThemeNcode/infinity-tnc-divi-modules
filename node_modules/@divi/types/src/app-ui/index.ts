import {
  Docking as DockingNamespace,
} from './docking';
import {
  Element as ElementNamespace,
} from './element';
import { Mode as ModeNamespace } from './mode';
import {
  PageBar as PageBarNamespace,
} from './page-bar';
import {
  Sidebar as SidebarNamespace,
  SidebarRow as SidebarRowNamespace,
} from './sidebar';
import {
  Store as StoreNamespace,
} from './store';
import {
  View as ViewNamespace,
} from './view';
import {
  Window as WindowNamespace,
} from './window';


export namespace AppUi {
  /* eslint-disable @typescript-eslint/no-unused-vars */
  // TODO figure out a way to declare nested namespace without disabling no-unused-vars rule
  // imported namespace as marked as unused by eslint using this config; it shouldn't happen in the first place.
  export import Docking = DockingNamespace;
  export import Element = ElementNamespace;
  export import PageBar = PageBarNamespace;
  export import Sidebar = SidebarNamespace;
  export import SidebarRow = SidebarRowNamespace;
  export import Store = StoreNamespace;
  export import Window = WindowNamespace;
  export import Mode = ModeNamespace;
  export import View = ViewNamespace;
  /* eslint-enable @typescript-eslint/no-unused-vars */

  export type ColorMode = 'light' | 'dark';
  export type ColorScheme = 'blue' | 'purple' | 'green' | 'red' | 'orange';
}
