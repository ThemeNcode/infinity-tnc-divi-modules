export namespace Window {
  export type Location = 'top' | 'app';

  export type AutoScroll = {
    direction: boolean | 'top' | 'bottom';
    speed: number;
  };

  export interface PropertyValues {
    top: number;
    app: number;
  }
}
