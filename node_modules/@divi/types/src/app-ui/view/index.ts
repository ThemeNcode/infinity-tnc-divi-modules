import { type Breakpoint } from '../../format-attr';


export namespace View {
  export type Type = 'wireframe' | 'zoom' | 'desktop' | 'tablet' | 'phone';

  export interface Item {
    width: string;
    height: string;
    label: string;
    icon: string;
    breakpoint: Breakpoint;
  }

  export type Items = Record<Type, Item>;
}
