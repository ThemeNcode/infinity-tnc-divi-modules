import { type ModuleType } from '../category';


export namespace DropZone {
  export interface ContainerProps {
    position: 'top' | 'bottom' | 'inside';
    isWireframe: boolean;
    draggedModuleType: ModuleType;
    draggedModuleStyles: Record<string, string | number>;
    type: 'spacer' | 'indicator';
  }

  export interface Props extends ContainerProps {
    setDraggedModuleDropZone: (props: null | ContainerProps) => void;
  }
}
