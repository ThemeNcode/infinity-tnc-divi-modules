import { type FormatBreakpointStateAttr } from '../../../../format-attr';
import { type TextShadow } from '../../decoration/text-shadow';


export namespace Text {
  export interface AttributeValue {
    color?: 'dark' | 'light';
    orientation?: 'left' | 'center' | 'right' | 'justify';
  }

  export interface Attributes {
    text?: FormatBreakpointStateAttr<AttributeValue>;
    textShadow?: TextShadow.Attributes;
  }
}
