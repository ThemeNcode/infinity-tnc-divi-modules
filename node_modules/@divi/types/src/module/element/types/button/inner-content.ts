import { type FormatBreakpointStateAttr } from '../../../../format-attr';
import { type AttributesRel } from '../../inner-content/attributes-rel';


// Note: Need to be abstracted into its own file because Element.Decoration.Button.Component.Props needs this.
export namespace InnerContent {
  export interface AttributeValue {
    text?: string;
    linkUrl?: string;
    linkTarget?: 'off' | 'on';
    rel?: AttributesRel.AttributeValue;
  }

  export type Attributes = FormatBreakpointStateAttr<AttributeValue>;
}
