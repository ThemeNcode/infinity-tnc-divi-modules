import { Button as ButtonNamespace } from './button';
import { Content as ContentNamespace } from './content';
import { Field as FieldNamespace } from './field';
import { Image as ImageNamespace } from './image';
import { ImageLink as ImageLinkNamespace } from './image-link';
import { Title as TitleNamespace } from './title';
import { TitleLink as TitleLinkNamespace } from './title-link';
import { Wrapper as WrapperNamespace } from './wrapper';


export type Type =

  // TODO remove this on dev beta 05
  'heading' |     // Renamed into `title`.
  'headingLink' | // Renamed into `titleLink`.

  'button' |
  'content' |
  'element' |

  // TODO Add element type named `field`.
  'field' |
  'title'   |
  'titleLink' |
  'image' |
  'imageLink' |
  'wrapper';

export namespace Types {
  /* eslint-disable @typescript-eslint/no-unused-vars */
  export import Button = ButtonNamespace;
  export import Content = ContentNamespace;
  export import Field = FieldNamespace;
  export import Image = ImageNamespace;
  export import ImageLink = ImageLinkNamespace;
  export import Title = TitleNamespace;
  export import TitleLink = TitleLinkNamespace;
  export import Wrapper = WrapperNamespace;
  /* eslint-enable @typescript-eslint/no-unused-vars */
}
