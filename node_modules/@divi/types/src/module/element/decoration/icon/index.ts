import { type Css } from '../../../../css';
import { type FormatPropertySelectors } from '../../../../format-attr';
import { type Icon as IconType } from '../../../../icon';
import { type FormatStyleImportant } from '../../../../style';
import { type Components } from '../../../components';


export namespace Icon {
  export type AttributeValue = IconType.Font.AttributeValue;
  export type Attributes = IconType.Font.Attributes;

  export namespace Style {

    // TODO Replace `IconStyleCssProperties` with this.
    export interface Props extends Components.Style.Props<Attributes> {
      propertySelectors?: FormatPropertySelectors<Css.Properties.Icon>;
      important?: boolean | FormatStyleImportant<Css.Properties.Icon>;
    }
  }
}
