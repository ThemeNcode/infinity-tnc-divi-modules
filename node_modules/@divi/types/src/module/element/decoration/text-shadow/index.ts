import { type Css } from '../../../../css';
import {
  type FormatBreakpointStateAttr,
  type FormatPropertySelectors,
} from '../../../../format-attr';
import { type FormatStyleImportant } from '../../../../style';
import { type Components } from '../../../components';


export namespace TextShadow {
  export interface AttributeValue {
    style?: 'none' | 'preset1' | 'preset2' | 'preset3' | 'preset4' | 'preset5';
    horizontal?: string;
    vertical?: string;
    blur?: string;
    color?: string;
    }

  export type Attributes = FormatBreakpointStateAttr<AttributeValue>

  export namespace Style {

    // TODO Replace `TextShadowStyleProps` with this.
    export interface Props extends Components.Style.Props<Attributes> {
      propertySelectors?: FormatPropertySelectors<Css.Properties.TextShadow>;
      important?: boolean | FormatStyleImportant<Css.Properties.TextShadow>;
    }
  }
}
