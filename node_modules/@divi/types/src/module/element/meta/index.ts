import { AdminLabel as AdminLabelNamespace } from './admin-label';

/* eslint-disable @typescript-eslint/no-unused-vars */
export namespace Meta {
  export import  AdminLabel = AdminLabelNamespace;

  export type KnownAttributes = {
    adminLabel?: AdminLabel.Attributes;

    // These properties are now still at first level attribute.
    // TODO revisit whether we can move it to `module.meta` or not.
    // builderVersion?: string;
    // nonconvertible?: string;
    // globalModule?: string;
    // globalParent?: string;
    // unsyncAttrs?: UnsyncAttrs;
  }

  export type Attributes<TAttr = Record<string, any>> = KnownAttributes & TAttr;
}
/* eslint-enable @typescript-eslint/no-unused-vars */
