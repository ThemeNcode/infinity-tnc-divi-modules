import { type Decoration } from '../decoration';


export namespace Components {

  // TODO replace `ElementComponentsProps` with this.
  export interface Props {

    // General Props.
    id?: string;
    attrs: Decoration.Attributes;

    background?: boolean | Omit<Decoration.Background.Component.Props, 'attr' | 'id'>;
    boxShadow?: boolean | Omit<Decoration.BoxShadow.Component.Props, 'attr' | 'id'>;
  }
}
