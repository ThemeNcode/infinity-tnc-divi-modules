export namespace AdvancedStyle {

  // TODO Continue moving `ElementAdvancedStyles` type to here.
  // TODO replace `ElementAdvancedStyles` with this.
  export type Item = {

    // props: CommonStyleProps<string | unknown> | CssStyleProps<string | unknown> | TextStyleProps;
    // componentName: 'divi/common' | 'divi/text' | 'divi/css';
  }

  // TODO replace `AdvancedStylesProps` with this.
  export interface Props {
    selector?: string;
    advancedStyles?: Item[];
  }
}
