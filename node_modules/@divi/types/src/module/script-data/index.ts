import { type ModuleElementsType } from '../class-module-elements';


export namespace ScriptData {
  export namespace Component {

    // TODO replace `ScriptDataProps` and `ModuleScriptDataProps` with this.
    export interface Props<TAttrs> {
      id?: string;
      attrs: TAttrs;
      selector?: string;
      elements?: ModuleElementsType<TAttrs>;
    }
  }
}
