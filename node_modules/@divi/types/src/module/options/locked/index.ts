import { type OnOff } from '../../../attr';

// TODO: feat(D5, Refactor): locked feature is not available in D5, yet. Convert/move this to a proper type/location.
// Locked group has no responsive, hover, and sticky support.
export interface LockedGroupAttr {
  desktop?: {
    value?: OnOff;
  }
}
