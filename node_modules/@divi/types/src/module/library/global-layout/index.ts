import { type Unsync } from '../../unsync';


export type UnsyncAttr = Unsync.Attr;
export type UnsyncAttrs = Unsync.Attrs;

export interface GlobalLayoutAttrs extends Record<string, unknown> {
  builderVersion?: string;
  globalModule?: string;
  unsyncAttrs?: UnsyncAttrs;
}
