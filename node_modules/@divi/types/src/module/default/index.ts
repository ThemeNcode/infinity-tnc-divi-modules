import { type Element } from '../element';
import { type InternalAttrs } from '../library';


export namespace Default {
  export type Attributes = InternalAttrs & {
    [key: string]: Element.Attributes | Record<string, Element.Attributes>;
  }
}
