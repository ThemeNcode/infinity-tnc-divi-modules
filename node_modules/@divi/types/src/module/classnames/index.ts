import {
  type AttrState,
  type Breakpoint,
} from '../../format-attr';
import { type ClassnamesType } from '../class-classnames';


export namespace Classnames {

  // TODO Replace `ModuleClassnamesParams` with this.
  export interface Params<TAttrs, TParentAttrs = unknown> {
    childrenIds?: string[];
    classnamesInstance: ClassnamesType;
    attrs: TAttrs;
    parentAttrs?: TParentAttrs;
    hasModule?: boolean;
    name?: string;
    id?: string;
    isFirst?: boolean;
    isLast?: boolean;
    state?: AttrState;
    breakpoint?: Breakpoint;
  }

}
