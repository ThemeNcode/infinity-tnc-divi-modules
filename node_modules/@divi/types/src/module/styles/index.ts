import { type AppPreferences } from '../../app-preferences';
import { type AttrState } from '../../format-attr';
import { type ModuleElementsType } from '../class-module-elements';


export namespace Styles {
  export namespace Component {

    // Replace `StylesProps` with this
    export interface Props<TAttrs, TParentAttrs = unknown> {
      id?: string;
      name?: string;
      attrs: TAttrs;
      parentAttrs?: TParentAttrs;
      siblingAttrs?: {
        previous?: TAttrs;
        next?: TAttrs;
      };
      defaultPrintedStyleAttrs?: TAttrs;
      orderClass: string;
      parentOrderClass?: string;
      wrapperOrderClass?: string;
      state?: AttrState;
      mode?: 'builder' | 'frontend';
      noStyleTag?: boolean;
      settings?: {
        disabledModuleVisibility?: AppPreferences.DisabledModule.Visibility;
      }
      pageSettings?: {
        sectionBackgroundColor?: string,
      }
      elements?: ModuleElementsType<TAttrs>;
    }
  }
}
