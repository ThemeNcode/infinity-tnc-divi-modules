export namespace Conditions {
  export type OptionPopupModalOnChange = (value: Record<string, any>) => void;

  export interface GroupProps {
    grouped?: boolean;
    groupLabel?: string;
    attrName?: string;
    visible?: boolean;
  }
}
