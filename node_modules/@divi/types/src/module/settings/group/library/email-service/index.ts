import { type FieldLibrary } from '../../../../../field-library';
import { type Element } from '../../../../element';
import { type GroupFieldProps } from '../../field';
import { type GroupProps as GroupPropsSource } from '../../props';


export namespace EmailService {
  export interface GroupProps extends GroupPropsSource {
    defaultGroupAttr?: Element.Advanced.EmailService.Attributes;
    fields?: {
      provider?: GroupFieldProps & {
        component?: FieldLibrary.ComponentMap['divi/email-service-provider'];
      };
      account?: GroupFieldProps & {
        component?: FieldLibrary.ComponentMap['divi/email-service-account'];
      };
    };
  }
}
