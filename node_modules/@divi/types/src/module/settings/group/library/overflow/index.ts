import { type FieldLibrary } from '../../../../../field-library';
import { type Element } from '../../../../element';
import { type GroupFieldProps } from '../../field';
import { type GroupProps as GroupPropsSource } from '../../props';


export namespace Overflow {
  export interface GroupProps extends GroupPropsSource {
    defaultGroupAttr?: Element.Decoration.Overflow.Attributes;
    fields?: {
      x?: GroupFieldProps & {
        defaultAttr?: Element.Decoration.Overflow.Attributes;
        component?: FieldLibrary.ComponentMap['divi/select'];
      };
      y?: GroupFieldProps & {
        defaultAttr?: Element.Decoration.Overflow.Attributes;
        component?: FieldLibrary.ComponentMap['divi/select'];
      };
    }
  }
}
