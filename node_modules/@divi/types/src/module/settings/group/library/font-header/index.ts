import { type Element } from '../../../../element';
import { type Font } from '../font';


export namespace FontHeader {
  export interface GroupItemProps extends Font.GroupProps {
    groupTab?: keyof Element.Decoration.HeadingFont.Attributes;
    render?: boolean;
  }

  export interface GroupProps extends Font.MultiGroupProps {
    defaultGroupAttr?: Element.Decoration.HeadingFont.Attributes;
    defaultTab?: keyof Element.Decoration.HeadingFont.Attributes;
    fields?: {
      h1?: Font.GroupProps['fields'];
      h2?: Font.GroupProps['fields'];
      h3?: Font.GroupProps['fields'];
      h4?: Font.GroupProps['fields'];
      h5?: Font.GroupProps['fields'];
      h6?: Font.GroupProps['fields'];
    };
    groups?: Partial<Record<keyof Element.Decoration.HeadingFont.Attributes, GroupItemProps>>;
  }
}
