import { type Field } from '../../../field';
import { type Position } from '../position';
import { type ZIndex } from '../z-index';


export namespace PositionSettings {
  export interface GroupProps {
    settingsGroupId?: string;
    grouped?: boolean;
    groupLabel?: string;
    fields?: {
      positionGroup?: Field.BaseProps & {
        component?: {
          type?: 'group';
          name?: 'divi/position';
          props?: Position.GroupProps;
        }
      };
      zIndexGroup?: Field.BaseProps & {
        component?: {
          type?: 'group';
          name?: 'divi/z-index';
          props?: ZIndex.GroupProps;
        }
      }
    };
  }
}
