import { type FieldLibrary } from '../../../../../field-library';
import { type Element } from '../../../../element';
import { type GroupFieldProps } from '../../field';
import { type GroupProps as GroupPropsSource } from '../../props';


export namespace AttributesRel {
  export interface GroupProps extends GroupPropsSource {
    defaultGroupAttr?: Element.InnerContent.AttributesRel.Attributes;
    fields?: {
      rel?: GroupFieldProps & {
        defaultAttr?: Element.InnerContent.AttributesRel.Attributes;
        component?: FieldLibrary.ComponentMap['divi/checkboxes'];
      }
    }
  }
}
