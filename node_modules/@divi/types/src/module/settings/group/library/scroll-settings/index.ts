import { type Field } from '../../../field';
import { type Scroll } from '../scroll';
import { type Sticky } from '../sticky';


export namespace ScrollSettings {
  export interface GroupProps {
    settingsGroupId?: string;
    grouped?: boolean;
    groupLabel?: string;
    fields?: {
      scrollGroup?: Field.BaseProps & {
        component?: {
          type?: 'group';
          name?: 'divi/scroll';
          props?: Scroll.GroupProps;
        };
      };
      stickyGroup?: Field.BaseProps & {
        component?: {
          type?: 'group';
          name?: 'divi/sticky';
          props?: Sticky.GroupProps;
        };
      }
    };
  }
}
