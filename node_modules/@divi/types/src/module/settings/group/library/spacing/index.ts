import { type FieldLibrary } from '../../../../../field-library';
import { type Element } from '../../../../element';
import { type GroupFieldProps } from '../../field';
import { type GroupProps as GroupPropsSource } from '../../props';


export namespace Spacing {
  export interface GroupProps extends GroupPropsSource {
    defaultGroupAttr?: Element.Decoration.Spacing.Attributes;
    fields?: {
      margin?: GroupFieldProps & {
        defaultAttr?: Element.Decoration.Spacing.Attributes;
        component?: FieldLibrary.ComponentMap['divi/spacing'];
      };
      padding?: GroupFieldProps & {
        defaultAttr?: Element.Decoration.Spacing.Attributes;
        component?: FieldLibrary.ComponentMap['divi/spacing'];
      };
    }
  }
}
