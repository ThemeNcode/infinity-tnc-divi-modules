import { type ImmutableObject } from 'seamless-immutable';

import { type ModuleAttrs } from '../../library';
import { type Configuration } from '../configuration';


export namespace Panel {
  export type Name = 'content' | 'design' | 'advanced';

  // TODO replace `SettingsProps` with this.
  export interface Props<TAttrs = ModuleAttrs, TParentAttrs = ModuleAttrs> {
    attrs?: ImmutableObject<TAttrs>;
    parentAttrs?: ImmutableObject<TParentAttrs>;
    defaultSettingsAttrs?: ImmutableObject<TAttrs>;
    groupConfiguration?: Configuration.Items;
  }
}
