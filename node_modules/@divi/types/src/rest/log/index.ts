export namespace Log {
  export type Response = Promise<any>;

  export type Status = 'In Progress' | 'Success' | 'Fail';

  export interface Item {
    route: string;
    data: unknown;
    response: Response;
    status: Status;
    batchId: string;
    timestamp: number;
  }
}
