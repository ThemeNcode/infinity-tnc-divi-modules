import { type Custom } from '../custom';
import { type Google } from '../google';


export namespace Loaded {
  export type Font = Google.LoadedFont | Custom.LoadedFont;

  export type Fonts = Record<string, Font>;

  export type FontType = Font['fontType'];
}
