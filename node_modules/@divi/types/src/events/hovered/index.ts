export namespace Hovered {
  export interface Module {
    target: string;
    id: string;
    payload: {
      label?: string;
      anchestorId?: string;
      parentId?: string;
      anchestorHoveredArea?: Area;
    };
    currentParent?: Module;
    area: 'top' | 'bottom';
  }

  export type Area = 'top' | 'bottom' | 'inside';
}
