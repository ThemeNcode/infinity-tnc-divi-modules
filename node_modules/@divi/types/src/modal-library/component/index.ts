import { type Store } from '../store';


export namespace Component {
  export interface Props {
    key: string;
    name: Store.Modal['name'];
    type: Store.Modal['type'];
    attributes?: Record<string, any>;
    dimension: Store.Modal['dimension'];
    group: Store.Modal['group'];
    owner: Store.Modal['owner']
    offset: Store.Modal['offset'];
    tab: Store.Modal['tab'];
  }
}
