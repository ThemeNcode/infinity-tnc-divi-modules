/**
 * Maybe Covert Content.
 *
 * Will covert post shortcode only if it either starts with the regex `[^<>&/\\[\\]\x00-\x20=]+`
 * Or if the post shortcode contains `<!-- wp:divi/layout -->`
 * Otherwise it will be returned as is.
 * The shortcode will be converted into object format aka Gutenberg format.
 *
 * @since ??
 *
 * @param {string} content Post shortcode to convert.
 *
 * @returns {string} Converted shortcode returned in Gutenberg format.
 */
export declare const maybeConvertContent: (content: string) => string;
