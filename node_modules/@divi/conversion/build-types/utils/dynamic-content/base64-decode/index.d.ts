/**
 * Decodes a base64 encoded string with UTF-8 support.
 *
 * This is a modified version of the base64 decoder from the base64-utf8 package.
 *
 * @see https://github.com/vilien/base64-utf8/blob/master/src/base64.ts
 *
 * @since ??
 *
 * @param {string} input The string which is in base64 format.
 *
 * @returns {string} The decoded string.
 */
export declare const base64Decode: (input: string) => string;
