import { type ShortcodeObject } from '../../types';
import { type ModuleCollections } from './types';
/**
 * Parse Shortcode.
 *
 * Arranges shortcode data into an object with the same format as legacy ETBuilderBackend.shortcode_object.
 * Types of shortcode parsed:
 * - Convertible Shortcode:
 * Supported module shortcode which mean those shortcode will be converted into their
 * D5 module counterpart. The module shortcode name is defined as `d4Shortcode` prop in
 * the module.json file.
 * - Nonconvertible Shortcode:
 * Unsupported module shortcode which mean those shortcode will be converted into
 * divi/shortcode-module and called Shortcode module. The Shortcode module component
 * will render those shortcode as HTML Rendered (registered shortcode) and Placeholder
 * (unregistered shortcode) elements.
 *
 * @since ??
 *
 * @param {string} shortcode D4 shortcode.
 * @param {object} moduleCollections Modules that can be converted from D4.
 * @param {string} parentName Optional parent module name.
 *
 * @returns {object} Shortcode object.
 */
export declare const parseShortcode: (shortcode: string, moduleCollections: ModuleCollections, parentName?: string) => ShortcodeObject;
