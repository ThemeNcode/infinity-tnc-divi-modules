import { type ShortcodeObject } from '../../types';
/**
 * Convert shortcode to Gutenberg format.
 *
 * Transforms a part of the shortcode attributes into object format.
 * Since a shortcode might contain another shortcode, this function might call itself.
 *
 * @since ??
 *
 * @param {object} shortcodePart Partial part of the shortcode object.
 * @param {object} shortcodePart.attrs The shortcode's attrs.
 * @param {string|object} shortcodePart.content The content of the shortcode.
 * @param {string} shortcodePart.name The name of the shortcode.
 * @param {boolean} gbReset Whether to reset `gbString` (used only when this function calls itself). Defaults to `true`.
 * @param {string | boolean} globalID Id of the global module if element is inside a global module.
 *
 * @returns {string} Converted shortcode string.
 */
export declare const convertShortcodeToGbFormat: (shortcodePart: ShortcodeObject, gbReset?: boolean, globalID?: string | null) => string;
