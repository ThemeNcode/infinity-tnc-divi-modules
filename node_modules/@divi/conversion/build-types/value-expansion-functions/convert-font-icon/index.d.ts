import { type FontAttrs, type StringArray, type StringObject } from '../../types';
/**
 * Obtains an object of the expanded value of an icon attribute.
 *
 * @since ??
 *
 * @param {string} value Expanded attribute value.
 *
 * @returns {StringObject| StringArray | FontAttrs | boolean} The expanded value or false if no expansion was found.
 */
export declare const convertFontIcon: (value: string) => StringObject | StringArray | FontAttrs | boolean;
